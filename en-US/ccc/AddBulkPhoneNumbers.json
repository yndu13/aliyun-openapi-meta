{
  "name": "AddBulkPhoneNumbers",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ContactFlowId",
      "description": "The ID of the contact flow to bind with the phone number.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "InstanceId",
      "description": "The ID of the call center instance to which phone numbers are to be added.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "PhoneNumber",
      "description": "The phone numbers to be added.\n\nThe parameter name **PhoneNumber.1** indicates adding only one phone number. To add multiple phone numbers, use PhoneNumber.1, PhoneNumber.2, and so on.",
      "position": "Query",
      "type": "RepeatList",
      "required": true
    },
    {
      "name": "SkillGroupId",
      "description": "The ID of the skill group to bind with the phone number.\n\nThe parameter name **SkillGroupId.1** indicates only one skill group. To specify multiple skill groups, use SkillGroupId.1, SkillGroupId.2, and so on.",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    },
    {
      "name": "Usage",
      "description": "The intended usage of the phone number. Valid values: **Inbound** (inbound calls), **Outbound** (outbound calls), **Bidirection** (both inbound and outbound calls).\n\n> If the phone numbers include a 400 number, this value can only be set to **Inbound**.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
