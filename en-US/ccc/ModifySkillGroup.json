{
  "name": "ModifySkillGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AllowPrivateOutboundNumber",
      "description": "Specifies whether to allow the use of personal outbound phone numbers.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "Description",
      "description": "The new description of the skill group.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "InstanceId",
      "description": "The instance ID of the Cloud Call Center instance to which the skill group belongs.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Name",
      "description": "The new name of the skill group.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "OutboundPhoneNumberId",
      "description": "The ID of the outbound phone number to associate with the skill group.\n\n\n**OutboundPhoneNumberId.1** indicates that only one outbound phone number is associated. To associate multiple phone numbers, specify multiple parameters, such as OutboundPhoneNumberId.1, OutboundPhoneNumberId.2, and OutboundPhoneNumberId.3.",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    },
    {
      "name": "RoutingStrategy",
      "description": "The matching method.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "SkillGroupId",
      "description": "The ID of the skill group to modify.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "SkillLevel",
      "description": "The skill level.\n\n**SkillLevel.1** sets the skill level for **UserId.1**. If multiple UserIds are specified, the same number of SkillLevel parameters must be specified.",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    },
    {
      "name": "UserId",
      "description": "The ID of the agent to add to the skill group.\n\n**UserId.1** indicates that only one agent is associated. To associate multiple agents, specify multiple parameters, such as UserId.1, UserId.2, and UserId.3. Note that UserId refers to the unique identifier of a RAM user within the Cloud Call Center instance, not the RamId of the RAM user.",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    }
  ]
}
