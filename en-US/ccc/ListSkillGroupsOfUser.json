{
  "name": "ListSkillGroupsOfUser",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "The ID of the call center instance.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "UserId",
      "description": "The ID of the user whose skill group list you want to obtain.\n\nNote that this is the unique identifier of the RAM sub-account in the call center instance, not the RamId, but the UUID.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
