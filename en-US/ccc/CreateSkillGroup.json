{
  "name": "CreateSkillGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AllowPrivateOutboundNumber",
      "description": "Specifies whether to allow the use of personal outbound numbers.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "Description",
      "description": "The description of the skill group to be created.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "InstanceId",
      "description": "The instance ID of the call center instance to which the skill group to be created belongs.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Name",
      "description": "The name of the skill group to be created.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "OutboundPhoneNumberId",
      "description": "The ID of the outbound number to be associated with the skill group.\n\n\n**OutboundPhoneNumberId.1** indicates that only one outbound number is associated. To associate multiple numbers, specify multiple parameters such as OutboundPhoneNumberId.1, OutboundPhoneNumberId.2, and OutboundPhoneNumberId.3.",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    },
    {
      "name": "RoutingStrategy",
      "description": "The matching mode. Valid values: default (default mode), acquaintance (acquaintance mode).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "SkillLevel",
      "description": "The skill level. Indicates the level of the agent in the skill group. The value ranges from 1 to 10. Incoming calls are preferentially assigned to agents with higher levels.\n\n**SkillLevel.1** sets the skill group level for the corresponding **UserId.1**. If multiple UserIds are specified, the same number of SkillLevel parameters must be specified.",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    },
    {
      "name": "UserId",
      "description": "The ID of the agent to be added to the skill group.\n\n**UserId.1** indicates that only one agent is associated. To associate multiple agents, specify multiple parameters such as UserId.1, UserId.2, and UserId.3. Note that UserId refers to the unique identifier of the RAM user within the call center instance, not the RamId of the RAM user.",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    }
  ]
}
