{
  "name": "CreateAccessKey",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "UserName",
      "description": "The username. When a Resource Access Management (RAM) user invokes this operation, an AccessKey pair is created for the RAM user by default.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
