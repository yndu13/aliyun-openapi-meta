{
  "name": "DecodeDiagnosticMessage",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EncodedDiagnosticMessage",
      "description": "The encoded diagnostic information obtained from the `AccessDeniedDetail.EncodedDiagnosticMessage` field in the response body when an API request is denied due to insufficient permissions. This parameter is used to decode the permission diagnostic information and must be specified when you call this operation.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
