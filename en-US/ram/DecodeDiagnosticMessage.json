{
  "name": "DecodeDiagnosticMessage",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EncodedDiagnosticMessage",
      "description": "The encoded diagnostic message obtained from the `AccessDeniedDetail.EncodedDiagnosticMessage` field in the response body when an API request is denied due to insufficient permissions. Pass this parameter to decode the permission diagnostic information when calling this operation.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
