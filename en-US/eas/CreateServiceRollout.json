{
  "name": "CreateServiceRollout",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/api/v2/services/[ClusterId]/[ServiceName]/rollout",
  "parameters": [
    {
      "name": "ClusterId",
      "description": "The region ID where the service is deployed. Call the `ListServices` operation to view a list of available regions.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "ServiceName",
      "description": "The name of the service to update. The name must be unique within the region. Call the `DescribeService` operation to get service details.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "body",
      "description": "The request body.",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ]
}
