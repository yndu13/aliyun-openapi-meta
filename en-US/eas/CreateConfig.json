{
  "name": "CreateConfig",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/api/v2/configs/[ClusterId]/[ConfigType]/[Name]/[ConfigKey]",
  "parameters": [
    {
      "name": "ClusterId",
      "description": "The region where the service is located.",
      "position": "Path",
      "type": "String",
      "required": false
    },
    {
      "name": "ConfigKey",
      "description": "The dynamic parameter name. It can contain letters, digits, periods (.), underscores (\\_), hyphens (-), and forward slashes (/). The maximum length is 100 characters.",
      "position": "Path",
      "type": "String",
      "required": false
    },
    {
      "name": "ConfigType",
      "description": "The configuration type. Currently, only `service` is supported.",
      "position": "Path",
      "type": "String",
      "required": false
    },
    {
      "name": "Name",
      "description": "The service name.",
      "position": "Path",
      "type": "String",
      "required": false
    },
    {
      "name": "body",
      "description": "The request body.",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ]
}
