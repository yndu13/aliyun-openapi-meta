{
  "name": "DescribeDcdnRefreshTasks",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "The accelerated domain name. You can specify only one accelerated domain name. By default, this operation queries tasks for all accelerated domain names.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "EndTime",
      "description": "The end of the time range to query. Specify the time in the ISO 8601 standard in the `yyyy-MM-ddTHH:mm:ssZ` format. The time must be in UTC.\n\n> The end time must be later than the start time.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ObjectPath",
      "description": "The path of the object to query. The path must be a full path.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ObjectType",
      "description": "The type of the task. Valid values:\n\n- **file**: refreshes URLs.\n\n- **directory**: refreshes directories.\n\n- **regex**: refreshes content based on a regular expression.\n\n- **preload**: preloads URLs.\n\nThis parameter is required if you specify **DomainName** or **Status**.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PageNumber",
      "description": "The number of the page to return. Valid values: **1** to **100000**.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "PageSize",
      "description": "The number of entries to return on each page. Default value: **20**. Maximum value: **50**. Valid values: **1** to **50**.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "StartTime",
      "description": "The beginning of the time range to query. Specify the time in the ISO 8601 standard in the `yyyy-MM-ddTHH:mm:ssZ` format. The time must be in UTC.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Status",
      "description": "The status of the task. Valid values:\n\n- **Complete**: The task is complete.\n\n- **Refreshing**: The task is in progress.\n\n- **Timeout**: The task timed out.\n\n- **Canceled**: The task is canceled.\n\n- **Failed**: The task failed.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "TaskId",
      "description": "The ID of the task. A task ID is returned after you create a refresh or preload task.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
