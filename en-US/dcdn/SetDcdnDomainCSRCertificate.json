{
  "name": "SetDcdnDomainCSRCertificate",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "The accelerated domain name for the certificate. The domain name must have HTTPS acceleration enabled.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ServerCertificate",
      "description": "The content of the signing certificate for the certificate signing request (CSR) that you created by calling the [CreateDcdnCertificateSigningRequest](~~144478~~) operation. The certificate must be in PEM format.\n\n> Base64-encode the certificate content. Then, use the built-in JavaScript function encodeURIComponent to encode the result before you pass the parameter.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
