{
  "name": "DescribeDcdnDomainCname",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "The accelerated domain names. Separate multiple domain names with commas (,). You can specify up to 30 domain names in a single query.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
