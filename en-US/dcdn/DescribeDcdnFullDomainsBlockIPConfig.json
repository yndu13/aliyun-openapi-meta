{
  "name": "DescribeDcdnFullDomainsBlockIPConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "IPList",
      "description": "The list of IP addresses or CIDR blocks to query. Separate multiple IP addresses with commas (,). You can specify up to 50 IP addresses.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
