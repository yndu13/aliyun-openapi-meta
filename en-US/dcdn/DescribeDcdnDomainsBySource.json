{
  "name": "DescribeDcdnDomainsBySource",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Sources",
      "description": "The origin servers. You can specify up to 20 origin servers. Separate multiple origin servers with commas (,). Fuzzy matching is not supported.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
