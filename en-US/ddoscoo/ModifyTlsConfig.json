{
  "name": "ModifyTlsConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Config",
      "description": "The details of the TLS security policy, expressed as a JSON-formatted string with the following structure.\n- **ssl_protocols**: String type, required. The TLS version. Valid values:\n    - **tls1.0**: TLS 1.0 and later are supported.\n    - **tls1.1**: TLS 1.1 and later are supported.\n    - **tls1.2**: TLS 1.2 and later are supported.\n- **ssl_ciphers**: String type, required. The cipher suite type. Valid values:\n    - **all**: all cipher suites, including strong and weak cipher suites\n    - **improved**: enhanced cipher suites\n    - **strong**: strong cipher suites only\n    - **default**: default cipher suites, which include only strong cipher suites\n- **ssl_13_enabled**: Boolean type, optional. Specifies whether to enable TLS 1.3. Valid values:\n    - **true**: Enables TLS 1.3.\n    - **false**: Disables TLS 1.3.\n- **custom_ciphers**: Array type, optional. The list of custom cipher suites for TLS versions earlier than 1.3.\n- **tls_13_custom_ciphers**: Array type, optional. The list of custom cipher suites for TLS 1.3. If TLS 1.3 is enabled, select at least one cipher suite for TLS versions earlier than 1.3.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Domain",
      "description": "The domain name of the website.\n> The domain name must have a website forwarding rule configured. You can call [DescribeDomains](~~91724~~) to query all domain names.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ResourceGroupId",
      "description": "The ID of the resource group to which the Anti-DDoS Pro or Anti-DDoS Premium instance belongs in Resource Management. This parameter is empty by default, which indicates that the instance belongs to the default resource group.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
