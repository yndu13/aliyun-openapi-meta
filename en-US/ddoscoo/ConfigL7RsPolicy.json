{
  "name": "ConfigL7RsPolicy",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Domain",
      "description": "The domain name of the website whose forwarding rules you want to manage.\n\n> The domain name must have website forwarding rules configured. You can call [DescribeDomains](~~91724~~) to query all domain names that have website forwarding rules configured.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Policy",
      "description": "The back-to-origin policy. The value is a character string that represents a JSON structure. The JSON structure contains the following fields:\n\n - **ProxyMode**: String | Required | The load balancing algorithm for back-to-origin. Valid values:\n    - **ip_hash**: IP Hash algorithm. Requests from the same source IP address are directed to the same origin server based on hash mapping.\n    - **rr**: Polling algorithm. All requests are allocated to different origin servers in turn. When you use this algorithm, you can set weights for servers based on their performance.\n     - **least_time**: Least Time algorithm. This algorithm uses intelligent DNS resolution to ensure the lowest latency for service traffic across the entire path from the protection node to forward back to the origin server.\n- **Attributes**: JSONArray | Optional | The back-to-origin parameter information. Each element in the array contains the following fields:\n    - **RealServer**: String | Optional | The address of the origin server.\n    - **Attribute**: JSONObject | Optional | The back-to-origin parameters. This field contains the following fields:\n        - **Weight**: Integer | Optional | The weight of the server. This property takes effect only when the polling algorithm is used (**ProxyMode** is set to **rr**). Valid values: **1** to **100**. Default value: **100**. A server with a higher weight receives more requests.\n        - **ConnectTimeout**: Integer | Optional | The timeout period for new connections. Valid values: **1** to **10**. Unit: seconds. Default value: **5**.\n        - **FailTimeout**: Integer | Optional | The failed timeout period. Valid values: **1** to **3600**. Unit: seconds. Default value: **10**.\n        - **MaxFails**: Integer | Optional | The maximum number of failures for health checks. Valid values: **1** to **10**. Default value: **3**.\n        - **Mode**: String | Optional | The primary/secondary property flag. Valid values: **active** indicates primary. **backup** indicates secondary.\n        - **ReadTimeout**: Integer | Optional | The read timeout period. Valid values: **10** to **300**. Unit: seconds. Default value: **120**.\n        - **SendTimeout**: Integer | Optional | The write timeout period. Valid values: **10** to **300**. Unit: seconds. Default value: **120**.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ResourceGroupId",
      "description": "The ID of the resource group to which the Anti-DDoS Pro or Anti-DDoS Premium instance belongs in Resource Management. This parameter is empty by default, which indicates that the instance belongs to the default resource group.\n\nFor more information about resource groups, see [Create a resource group](~~94485~~).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "UpstreamRetry",
      "description": "Specifies whether to enable back-to-origin retry. Valid values:\n\n- **1**: Enabled.\n- **0**: Disabled.",
      "position": "Query",
      "type": "Integer",
      "required": false
    }
  ]
}
