{
  "name": "DescribeDomainViewTopUrl",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Domain",
      "description": "The domain name of the website service.\n\n> The domain name must have a website service forwarding rule configured. You can invoke [DescribeDomains](~~91724~~) to query all domain names that are added to Anti-DDoS Pro and Anti-DDoS Premium.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "EndTime",
      "description": "The end of the time range to query. Specify a UNIX timestamp. Unit: seconds.\n> The value must be on the exact minute.",
      "position": "Query",
      "type": "Long",
      "required": true
    },
    {
      "name": "Inerval",
      "description": "The step size for returning data, in seconds. This parameter specifies the interval at which a result is returned. The supported step size varies based on the query time range (determined by **StartTime** and **EndTime**). Recommended settings:\n\n- Query time range of up to 1 hour: minimum step size of 60 seconds, maximum not exceeding the query time range.\n- Query time range of more than 1 hour but up to 6 hours: minimum step size of 600 seconds, maximum not exceeding the query time range.\n- Query time range of more than 6 hours but up to 24 hours: minimum step size of 1800 seconds, maximum not exceeding the query time range.\n- Query time range of more than 24 hours but up to 7 days: minimum step size of 3600 seconds, maximum not exceeding the query time range.\n- Query time range of more than 7 days but up to 15 days: minimum step size of 14400 seconds, maximum not exceeding the query time range.\n- Query time range of more than 15 days: minimum step size of 43200 seconds, maximum not exceeding the query time range.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "ResourceGroupId",
      "description": "The ID of the resource group to which the Anti-DDoS Pro or Anti-DDoS Premium instance belongs in Resource Management. This parameter is empty by default, which indicates that the instance belongs to the default resource group.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "StartTime",
      "description": "The beginning of the time range to query. Specify a UNIX timestamp. Unit: seconds.\n> The value must be on the exact minute.",
      "position": "Query",
      "type": "Long",
      "required": true
    },
    {
      "name": "Top",
      "description": "The number of URLs to return. Valid values: **1** to **100**.",
      "position": "Query",
      "type": "Integer",
      "required": true
    }
  ]
}
