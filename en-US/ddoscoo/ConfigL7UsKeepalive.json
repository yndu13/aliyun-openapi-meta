{
  "name": "ConfigL7UsKeepalive",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Domain",
      "description": "The domain name of the website service.\n> The domain name must have a website service forwarding rule configured. You can call [DescribeDomains](~~91724~~) to query all domain names.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DownstreamKeepalive",
      "description": "The idle timeout period of the persistent connection between the client and Anti-DDoS Proxy.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "UpstreamKeepalive",
      "description": "The persistent connection settings for back-to-origin requests of Anti-DDoS Proxy. The value is a string that represents a JSON struct. The JSON struct contains the following fields:\n\n- **enabled**: Boolean | Required | Specifies whether to enable persistent connections for back-to-origin requests.\n- **keepalive_requests**: Integer | Required | The number of requests that can be sent over a persistent connection.\n- **keepalive_timeout**: Integer | Required | The idle timeout period of a persistent connection.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
