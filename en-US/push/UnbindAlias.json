{
  "name": "UnbindAlias",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AliasName",
      "description": "The alias to detach.\n\nAn alias supports up to 128 bytes (one Chinese character counts as 3 bytes). This field is required when UnbindAll is false. You can detach up to 10 aliases at once. Separate multiple aliases with commas.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "AppKey",
      "description": "The AppKey.",
      "position": "Query",
      "type": "Long",
      "required": true
    },
    {
      "name": "DeviceId",
      "description": "The unique device identifier for push notifications. It is 32 characters long and consists of digits and lowercase letters.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "UnbindAll",
      "description": "Whether to detach all aliases. Default is false.\n\nIf true, detaches all aliases currently bound to the device. If false, detaches only the aliases specified in AliasName.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    }
  ]
}
