{
  "name": "CheckDevices",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppKey",
      "description": "The AppKey value.",
      "position": "Query",
      "type": "Long",
      "required": true
    },
    {
      "name": "DeviceIds",
      "description": "The unique identifier for each device in the push service. Each ID is 32 characters long and contains only digits and lowercase letters. Separate multiple IDs with commas. You can check up to 100 devices per request.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
