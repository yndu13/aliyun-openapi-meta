{
  "name": "GetKeyPolicy",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "KeyId",
      "description": "The ID or Alibaba Cloud Resource Name (ARN) of the key.\n\n> When you access a key in another Alibaba Cloud account, you must enter the ARN of the key. The ARN of a key is in the `acs:kms:${region}:${account}:key/${keyid}` format.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "PolicyName",
      "description": "The name of the key policy. Only the static value default is supported.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
