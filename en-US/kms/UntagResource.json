{
  "name": "UntagResource",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CertificateId",
      "description": "The ID of the certificate.\n\n> You must specify one and only one of the KeyId, SecretName, and CertificateId parameters.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "KeyId",
      "description": "The ID of the key. This is the globally unique identifier (GUID) of the master key (CMK).\n\n> You must specify one and only one of the KeyId, SecretName, and CertificateId parameters.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "SecretName",
      "description": "The name of the credential.\n\n> You must specify one and only one of the KeyId, SecretName, and CertificateId parameters.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "TagKeys",
      "description": "One or more tag keys. Separate multiple tag keys with commas (,).<br> You need to specify only tag keys, not tag values.<br> The tag key can be 1 to 128 bytes in length.<br><br>",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
