{
  "name": "ImportKeyMaterial",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EncryptedKeyMaterial",
      "description": "The key material encrypted with the public key returned by **GetParametersForImport**, and then Base64-encoded.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ImportToken",
      "description": "The import token obtained by calling **GetParametersForImport**.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "KeyId",
      "description": "The ID of the CMK to be imported.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "KeyMaterialExpireUnix",
      "description": "The time when the key material expires.\n\nIf this parameter is not specified or set this parameter to 0, the key material does not expire.\n\n> The value cannot be earlier than the time when the operation is called (based on the server time).",
      "position": "Query",
      "type": "Long",
      "required": true
    }
  ]
}
