{
  "name": "CreateSkillSpace",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but make sure that the token is unique among different requests. The value of ClientToken can contain only ASCII characters and cannot exceed 64 characters in length.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "SkillSpaceDescription",
      "description": "The description of the SkillSpace.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "SkillSpaceName",
      "description": "The name of the SkillSpace.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
