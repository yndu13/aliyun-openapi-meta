{
  "name": "DeleteParameter",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Name",
      "description": "The name of the parameter. The name can be up to 180 characters in length and can contain letters, digits, hyphens (-), and underscores (\\_). The name cannot start with aliyun, acs, alibaba, alicloud, or oos.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region ID.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
