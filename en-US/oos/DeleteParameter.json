{
  "name": "DeleteParameter",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Name",
      "description": "The parameter name. The name can contain letters, digits, hyphens (-), and underscores (_). The name cannot exceed 180 characters in length and cannot start with aliyun, acs, alibaba, alicloud, or oos.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region ID.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
