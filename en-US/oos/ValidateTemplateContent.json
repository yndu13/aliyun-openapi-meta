{
  "name": "ValidateTemplateContent",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Content",
      "description": "The template content.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "TemplateURL",
      "description": "The URL of the OOS template stored in Alibaba Cloud Object Storage Service (OSS). Only URLs with public-read permission are supported. You can specify this parameter to directly create a task execution based on the template content stored at the TemplateURL, without creating a template first. This parameter is not required if you select an existing template to create a task execution.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
