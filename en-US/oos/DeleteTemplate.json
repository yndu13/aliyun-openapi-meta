{
  "name": "DeleteTemplate",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoDeleteExecutions",
      "description": "Specifies whether to delete the related executions when the template is deleted.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "TemplateName",
      "description": "The name of the template. The name can be up to 200 characters in length and can contain letters, digits, hyphens (-), and underscores (\\_). The name cannot start with ALIYUN, ACS, ALIBABA, or ALICLOUD.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
