{
  "name": "GetTemplate",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "description": "The ID of the region.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "TemplateName",
      "description": "The name of the template. The name can be up to 200 characters in length and can contain letters, digits, hyphens (-), and underscores (\\_).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "TemplateVersion",
      "description": "The version of the template. The format is v followed by a number. Valid values: `v1` to `v200`.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
