{
  "name": "DeleteApiMcpServerCore",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "DELETE",
  "pathPattern": "/apimcpservercore",
  "parameters": [
    {
      "name": "clientToken",
      "description": "The unique identifier used to ensure request idempotence. When creating or updating a resource, if the same ClientToken is used for multiple requests, the system treats them as the same operation to avoid duplicate creation or updates. The ClientToken is generated by the client. Use the same ClientToken in retry scenarios.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "id",
      "description": "The unique ID.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
