{
  "name": "DeleteApiMcpServer",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "DELETE",
  "pathPattern": "/apimcpserver",
  "parameters": [
    {
      "name": "clientToken",
      "description": "Ensures the idempotence of the request. Generate a parameter value from your client that is unique across different requests. ClientToken supports only ASCII characters and cannot exceed 64 characters in length. We recommend that you use a UUID. The token expires after 3 days.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "id",
      "description": "The ID of the API MCP service.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
