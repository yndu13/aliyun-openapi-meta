{
  "name": "DescribeCenBandwidthPackages",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Filter",
      "description": "The filter information.",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "Key",
          "description": "The filter condition.\nYou can use filter conditions to filter the bandwidth package instances you want to view. The following filter conditions are supported:\n\n- **CenId**: The ID of the Cloud Enterprise Network (CEN) instance.\n\n- **Status**: The status of the bandwidth package instance. Valid values:\n\n    - **Idle**: Not associated.\n    - **InUse**: Associated.\n\n- **CenBandwidthPackageId**: The ID of the bandwidth package.\n\n- **Name**: The name of the bandwidth package.\nYou can specify one or more filter conditions. The maximum value of **N** is **5**.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "Value",
          "description": "The filter values based on the specified **Key**. You can specify multiple filter values for a single **Key**. The filter values have an **OR** relationship, which means that a bandwidth package matching any one of the filter values is considered a match for the filter condition.",
          "position": "Query",
          "type": "RepeatList",
          "required": false
        }
      ]
    },
    {
      "name": "IncludeReservationData",
      "description": "Specifies whether to include renewal data. Valid values:\n\n- **true**: Includes renewal data.\n\n- **false**: Does not include renewal data.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "IsOrKey",
      "description": "The relationship between filter conditions. Valid values:\n\n- **false** (default): The filter conditions have an **AND** relationship. A bandwidth package must meet all filter conditions to be considered a match.\n\n- **true**: The filter conditions have an **OR** relationship. A bandwidth package that meets any one of the filter conditions is considered a match.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "PageNumber",
      "description": "The page number of the list. Default value: **1**.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "PageSize",
      "description": "The number of entries per page for a paged query. Maximum value: **50**. Default value: **10**.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "ResourceGroupId",
      "description": "The resource group ID.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Tag",
      "description": "The tag information list.\n\nYou can specify up to 20 tags at a time.",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "Key",
          "description": "The tag key of the resource.\n\nOnce specified, the tag key cannot be an empty string. The tag key can be up to 64 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`.\n\nYou can specify up to 20 tag keys at a time.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "Value",
          "description": "The tag value of the resource.\n\nThe tag value can be empty or a string of up to 128 characters. It cannot start with `aliyun` or `acs:` and cannot contain `http://` or `https://`.\n\nEach tag key corresponds to one tag value. You can specify up to 20 tag values at a time.",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    }
  ]
}
