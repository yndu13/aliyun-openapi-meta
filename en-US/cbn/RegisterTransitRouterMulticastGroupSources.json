{
  "name": "RegisterTransitRouterMulticastGroupSources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The client token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** of the API request as the **ClientToken**. The **RequestId** may be different for each API request.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform a dry run. Valid values:\n\n- **true**: performs a dry run. The system checks the required parameters, request syntax, and limits. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n- **false** (default): performs a dry run and sends the request. If the request passes the dry run, the multicast source is created.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "GroupIpAddress",
      "description": "The IP address of the multicast group to which the multicast source belongs. Valid values: **224.0.1.0** to **239.255.255.254**.\n><notice>224.0.0.0 to 224.0.0.127 are system reserved IP addresses and cannot be used as multicast group IP addresses.></notice>\nIf the multicast group that you specify does not exist in the current multicast domain, the system automatically creates a multicast group.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "NetworkInterfaceIds",
      "description": "The list of network interface controller (NIC) IDs of the elastic network interfaces (ENIs).",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    },
    {
      "name": "TransitRouterMulticastDomainId",
      "description": "The ID of the multicast domain to which the multicast source belongs.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "VpcId",
      "description": "The ID of the VPC-connected instance to which the network interface controller (NIC) of the elastic network interfaces (ENIs) belongs.\n\n- If the ENI belongs to the same Alibaba Cloud account as the account that you use to logon, you do not need to set this parameter.\n- If the ENI belongs to a different Alibaba Cloud account from the account that you use to logon, this parameter is required.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
