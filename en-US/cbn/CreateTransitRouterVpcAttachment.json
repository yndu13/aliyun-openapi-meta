{
  "name": "CreateTransitRouterVpcAttachment",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoPublishRouteEnabled",
      "description": "Specifies whether to allow the Enterprise Edition transit router to automatically publish routing entries to the VPC instance.\n\n- **false** (default): No.\n- **true**: Yes.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "CenId",
      "description": "The instance ID of the Cloud Enterprise Network (CEN).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ChargeType",
      "description": "The billing method. Default value: **POSTPAY**, which indicates pay-as-you-go.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The client token can contain only ASCII characters.\n\n>If you do not specify this parameter, the system automatically uses the RequestId of the API request as the ClientToken. The RequestId may be different for each API request.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to execute a dry run, including permission and instance status verification. Valid values:\n\n- **false** (default): Sends a normal request and creates the VPC connection after the request passes the check.\n- **true**: Sends a check request. Only the check is performed, and the VPC connection is not created. The check items include whether required parameters are specified and the request format. If the check fails, the corresponding error is returned. If the check succeeds, the error code `DryRunOperation` is returned.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "Options",
      "description": "The collection of feature attributes.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID of the VPC-connected instance.\n\nYou can invoke the [DescribeChildInstanceRegions](~~132080~~) operation to query the region ID.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Tag",
      "description": "The list of tags.\n\nYou can specify up to 20 tags at a time.",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "Key",
          "description": "The tag key of the resource.\n\nOnce specified, the tag key cannot be an empty string. The tag key can be up to 64 characters in length, and cannot start with `aliyun` or `acs:`, or contain `http://` or `https://`.\n\nYou can specify up to 20 tag keys at a time.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "Value",
          "description": "The tag value of the resource.\n\nOnce specified, the tag value cannot be empty. The tag value can be up to 128 characters in length, and cannot start with aliyun or acs:, or contain http:// or https://.\n\nEach tag key corresponds to one tag value. You can specify up to 20 tag values at a time.",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    },
    {
      "name": "TransitRouterAttachmentDescription",
      "description": "The description of the VPC connection.\n\nThe description can be empty or 1 to 256 characters in length, and cannot start with http:// or https://.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "TransitRouterAttachmentName",
      "description": "The name of the VPC connection.\n\nThe name can be empty or 1 to 128 characters in length, and cannot start with http:// or https://.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "TransitRouterId",
      "description": "The instance ID of the Enterprise Edition transit router.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "TransitRouterVPCAttachmentOptions",
      "description": "The list of feature attributes for the VPC connection (to be deprecated, use the new parameter Options instead).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "VpcId",
      "description": "The instance ID of the VPC-connected instance.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "VpcOwnerId",
      "description": "The Alibaba Cloud account ID to which the VPC-connected instance belongs. The default value is the Alibaba Cloud account ID of the current logon user.\n\n> This parameter is required if you want to load a cross-account network instance.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "ZoneMappings",
      "description": "Select a vSwitch instance in a zone supported by the Enterprise Edition transit router.\n\nYou can add up to 10 entries at a time.",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "sub_parameters": [
        {
          "name": "VSwitchId",
          "description": "The ID of the vSwitch instance in a zone supported by the Enterprise Edition transit router.\n\nYou can select vSwitch instances for up to 10 zones at a time.",
          "position": "Query",
          "type": "String",
          "required": true
        },
        {
          "name": "ZoneId",
          "description": "The ID of a zone supported by the Enterprise Edition transit router.\n\nYou can call the [DescribeZones](~~36064~~) operation to query zone IDs.\n\nYou can select up to 10 zones at a time.",
          "position": "Query",
          "type": "String",
          "required": true
        }
      ]
    }
  ]
}
