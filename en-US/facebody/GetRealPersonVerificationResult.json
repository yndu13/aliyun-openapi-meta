{
  "name": "GetRealPersonVerificationResult",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "VerificationToken",
      "description": "The unique identifier of the trusted real-person verification.\n\n> A VerificationToken that has already been verified cannot be queried again. Otherwise, an error is returned.",
      "position": "Body",
      "type": "String",
      "required": true
    }
  ]
}
