{
  "name": "CreateFaceDb",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Name",
      "description": "The database name. The name can contain letters, digits, and underscores (_), and must be 1 to 64 characters in length.",
      "position": "Body",
      "type": "String",
      "required": true
    }
  ]
}
