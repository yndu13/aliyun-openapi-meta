{
  "name": "CreateResourceRecord",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/resources/[resourceName]/records",
  "parameters": [
    {
      "name": "body",
      "description": "The resource record to create.",
      "position": "Body",
      "type": "Json",
      "required": true
    },
    {
      "name": "resourceName",
      "description": "The resource name.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
