{
  "name": "GetScheduledSQL",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/scheduledsqls/[scheduledSQLName]",
  "parameters": [
    {
      "name": "project",
      "description": "The name of the project.",
      "position": "Host",
      "type": "String",
      "required": true
    },
    {
      "name": "scheduledSQLName",
      "description": "The name of an existing scheduled query job. The name must be unique.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
