{
  "name": "DeleteResourceRecord",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/resources/[resourceName]/records",
  "parameters": [
    {
      "name": "ids",
      "description": "The IDs of the records to delete. Separate multiple IDs with commas (,). You can specify up to 200 IDs at a time.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "resourceName",
      "description": "The resource name.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
