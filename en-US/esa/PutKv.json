{
  "name": "PutKv",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Base64",
      "description": "Indicates whether the Value parameter uses Base64 encoding. Set this to true when storing a binary value. If this parameter is true, Value must be a valid Base64-encoded string.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "Expiration",
      "description": "The expiration time for the key-value pair, as a Unix timestamp in seconds. This value cannot be earlier than the current time. If you specify both Expiration and ExpirationTtl, ExpirationTtl takes precedence.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "ExpirationTtl",
      "description": "The time-to-live (TTL) for the key-value pair, in seconds. If you specify both Expiration and ExpirationTtl, ExpirationTtl takes precedence.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "Key",
      "description": "The key can be up to 512 bytes long and cannot contain spaces or forward slashes (/).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Namespace",
      "description": "The name of the namespace. You specify this name when you call the [CreateKvNamespace](~~2850317~~) operation.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Value",
      "description": "The value to associate with the key. The maximum size is 2 MB (2,000,000 bytes). For values that exceed this limit, use the [PutKvWithHighCapacity](~~2850486~~) operation.",
      "position": "Body",
      "type": "String",
      "required": true
    }
  ]
}
