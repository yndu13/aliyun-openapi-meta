{
  "name": "BatchPutKvWithHighCapacity",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Namespace",
      "description": "The name specified when you called [CreateKvNamespace](~~2850317~~).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Url",
      "description": "A publicly accessible HTTP(S) URL that points to a JSON file containing the key-value pairs to be batch set. The server actively downloads the content from this URL.\n\n- If you use an SDK, the SDK automatically uploads the file and generates the URL.\n\n- In non-SDK scenarios, upload the JSON payload to any publicly accessible HTTP service and provide the URL.\n\nThe file content pointed to by the URL must be in the following JSON format: {\"Namespace\":\"<namespace_name>\",\"KvList\":[{\"Key\":\"<key>\",\"Value\":\"<value>\"},...]}.If the URL content does not conform to this format, the API silently returns an empty SuccessKeys array.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
