{
  "name": "UpdateSitePause",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Paused",
      "description": "Specifies whether to temporarily pause the proxy acceleration feature for the entire site. After the feature is paused, all DNS records directly return record values to the client. Valid values:\n- true: Pauses site acceleration.\n- false: Resumes normal site acceleration.\n\nWhen site acceleration is paused, only activated sites with NS access mode are supported.",
      "position": "Query",
      "type": "Boolean",
      "required": true
    },
    {
      "name": "SiteId",
      "description": "The site ID. You can call the [ListSites](~~2850189~~) operation to obtain the site ID. Check the Status field to confirm the site status and the AccessType field to confirm the access mode of the site.",
      "position": "Query",
      "type": "Long",
      "required": true
    }
  ]
}
