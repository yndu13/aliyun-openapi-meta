{
  "name": "ReleaseInstance",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "The security instance ID. The value must be a DDoS security instance ID in the format of esa-ddos-*. You can obtain the ID by calling the ListDDoSInstances operation. Site instance IDs in the format of esa-site-* are not supported.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
