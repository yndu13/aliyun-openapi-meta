{
  "name": "SubmitUploadTask",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Force",
      "description": "Specifies whether to purge resources in the corresponding directory when the back-to-origin content is inconsistent with the origin server resources. Default value: false. This parameter is valid only for purge tasks.\n\n- **true**: Purges all resources in the corresponding directory.\n- **false**: Purges only the changed resources in the corresponding directory.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "SiteId",
      "description": "The site ID. You can call the [ListSites](~~ListSites~~) operation to obtain the site ID.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "UploadId",
      "description": "The file upload task ID, which is generated when you call the [UploadTask](~~UploadTask~~) operation.",
      "position": "Query",
      "type": "Long",
      "required": false
    }
  ]
}
