{
  "name": "DescribeSpotPriceHistory",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EndTime",
      "description": "The end of the time range to query the historical prices of spot instances. Specify the time in the [ISO 8601](~~25696~~) standard in the `yyyy-MM-ddTHH:mm:ssZ` format. The time must be in UTC.\n\nDefault value: null, which indicates the current time.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "InstanceType",
      "description": "The instance type.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "IoOptimized",
      "description": "Specifies whether the spot instance is I/O optimized. Valid values:\n\n- optimized: The spot instance is an I/O optimization instance.\n\n- none: The spot instance is not an I/O optimization instance.\n\nDefault value for Generation I instance families: none.\n\nDefault value for other instance families: optimized.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "NetworkType",
      "description": "The network type of the spot instance. Valid values:\n\n- vpc: virtual private cloud (VPC).\n- classic: classic network. This feature has been retired. For more information, see [Retirement notice](~~2833134~~).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "OSType",
      "description": "The type of the operating system platform. Valid values:\n\n- linux.\n- windows.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Offset",
      "description": "The row number to start the query from.\n\nDefault value: 0.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID of the instance. You can call [DescribeRegions](~~25609~~) to query the most recent region list.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "SpotDuration",
      "description": "The protection period of the spot instance. Unit: hours. Default value: 1. Valid values:\n- 1: After a spot instance is created, Alibaba Cloud ensures that the instance is not automatically released for 1 hour. After the 1-hour protection period ends, the system compares the bid price with the market price and checks the resource inventory to determine whether to retain automatic release the instance.\n- 0: After a spot instance is created, Alibaba Cloud does not ensure that the instance runs for 1 hour. The system compares the bid price with the market price and checks the resource inventory to determine whether to retain automatic release the instance.\n\nAlibaba Cloud sends an ECS system event notification 5 minutes before the instance is released. Spot instances are billed by second. Specify a protection period based on the expected task execution duration.\n\n> This parameter takes effect only when SpotStrategy is set to SpotWithPriceLimit or SpotAsPriceGo.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "StartTime",
      "description": "The beginning of the time range to query the historical prices of spot instances. The maximum time range between the start time and end time is 30 days. Specify the time in the [ISO 8601](~~25696~~) standard in the `yyyy-MM-ddTHH:mm:ssZ` format. The time must be in UTC.\n\nDefault value: null, which indicates 3 hours before the end time.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ZoneId",
      "description": "The zone ID.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
