{
  "name": "CreateSnapshot",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Category",
      "description": "The snapshot type. Valid values:\n\n- Standard: standard snapshot.\n- Flash: local snapshot.\n\n> This parameter will be deprecated. Standard snapshots for enterprise SSDs have been upgraded to [instant access by default](~~193667~~). No additional configuration or fees are required. The snapshot is active immediately after creation.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but make sure that the token is unique among different requests. The **ClientToken** value can contain only ASCII characters and cannot exceed 64 characters in length. For more information, see [How to ensure idempotence](~~25693~~).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Description",
      "description": "The description of the snapshot. The description must be 2 to 256 characters in length and cannot start with `http://` or `https://`.\n\nDefault value: null.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DiskId",
      "description": "The disk ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "InstantAccess",
      "description": "Specifies whether to enable the snapshot instant access feature. Valid values:\n- true: enables the feature. Only enterprise SSDs support this feature.\n- false: disables the feature. A standard snapshot is created.\n\nDefault value: false.\n\n> This parameter is deprecated. Standard snapshots for enterprise SSDs have been upgraded to [instant access by default](~~193667~~). No additional configuration or fees are required. The snapshot is active immediately after creation.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "InstantAccessRetentionDays",
      "description": "Settings for the retention period of the snapshot instant access feature. After the retention period expires, the snapshot is subject to automatic release. This parameter takes effect only when `InstantAccess=true`. Unit: days. Valid values: 1 to 65535.\n\nThe default value is the same as the value of the `RetentionDays` parameter.\n\n> This parameter is deprecated. Standard snapshots for enterprise SSDs have been upgraded to [instant access by default](~~193667~~). No additional configuration or fees are required. The snapshot is active immediately after creation.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "ResourceGroupId",
      "description": "The ID of the resource group to which the snapshot belongs.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RetentionDays",
      "description": "Settings for the retention period of the snapshot. Unit: days. Valid values: 1 to 65536. The snapshot is subject to automatic release when the retention period expires.\n\nDefault value: null, which indicates that the snapshot is not subject to automatic release.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "SnapshotName",
      "description": "The name of the snapshot. The name must be 2 to 128 characters in length. It must start with a letter and cannot start with `http://` or `https://`. The name can contain Unicode characters under the letter category (including letters in English and Chinese), ASCII digits (0-9), colons (:), underscores (_), periods (.), and hyphens (-).\n\n> The name cannot start with `auto` to avoid conflicts with the names of automatic snapshots.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "StorageLocationArn",
      "description": "> This parameter is not publicly available.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Tag",
      "description": "The tags.",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "Key",
          "description": "The tag key of the snapshot. Valid values of N: 1 to 20. The tag key cannot be an empty string. The tag key can be up to 128 characters in length and cannot start with aliyun or acs:. The tag key cannot contain http:// or https://.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "Value",
          "description": "The tag value of the snapshot. Valid values of N: 1 to 20. The tag value can be an empty string. The tag value can be up to 128 characters in length and cannot contain http:// or https://.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "key",
          "description": "The tag key of the snapshot.\n\n> For compatibility purposes, use the Tag.N.Key parameter.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "value",
          "description": "The tag value of the snapshot.\n\n> For compatibility purposes, use the Tag.N.Value parameter.",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    }
  ]
}
