{
  "name": "DescribeDiskMonitorData",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DiskId",
      "description": "The ID of the cloud disk that you want to query.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "EndTime",
      "description": "The end time of the data. Specify the time in the [ISO 8601](~~25696~~) standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC. If the value of seconds (ss) is not 00, the end time is automatically rounded up to the next minute.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Period",
      "description": "The granularity of the data. Unit: seconds. Valid values:\n\n- 60.\n- 600. \n- 3600.\n\nDefault value: 60.\n\n> The value of (EndTime – StartTime) / Period must be less than or equal to 400. A maximum of 400 data entries can be returned at a time.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "StartTime",
      "description": "The start time of the data. Specify the time in the [ISO 8601](~~25696~~) standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC. If the value of seconds (ss) is not 00, the start time is automatically rounded up to the next minute.\n\n> You can query monitoring information only for the last 30 days. The `StartTime` parameter cannot be more than 30 days earlier than the current time.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
