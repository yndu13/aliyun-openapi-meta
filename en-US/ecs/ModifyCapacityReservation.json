{
  "name": "ModifyCapacityReservation",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Description",
      "description": "The description of the capacity reservation. The description must be 2 to 256 characters in length and cannot start with `http://` or `https://`.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "EndTime",
      "description": "The expiration time of the capacity reservation. This parameter takes effect only when `EndTimeType=Limited`. Specify the time in the ISO 8601 standard in the `yyyy-MM-ddTHH:mm:ssZ` format. The time must be in UTC. For more information, see [ISO 8601](~~25696~~).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "EndTimeType",
      "description": "The expiration method of the capacity reservation. Valid values:\n\n- Limited: The capacity reservation is released at a specified time. You must also specify the `EndTime` parameter.\n- Unlimited: The capacity reservation is released by manual release. No time limit is imposed.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "InstanceAmount",
      "description": "The total number of instances that the capacity reservation needs to reserve. Valid values: the number of used instances to 1000.\n\n> When you increase the total number of instances, the operation may fail due to insufficient inventory.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "Platform",
      "description": "The operating system type of the image used by the instance. Valid values:\n\n- Windows: Windows Server operating system.\n- Linux: Linux and Unix-like operating system.\n\n> This parameter is not publicly available.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PrivatePoolOptions.Id",
      "description": "The ID of the capacity reservation.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "PrivatePoolOptions.Name",
      "description": "The name of the capacity reservation. The name must be 2 to 128 characters in length. It must start with a letter and cannot start with `http://` or `https://`. It can contain digits, colons (:), underscores (_), or hyphens (-).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The ID of the region where the capacity reservation resides. You can call [DescribeRegions](~~25609~~) to query the most recent region list.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "StartTime",
      "description": "The method by which the capacity reservation takes effect. Currently, only immediate activation is supported, and you do not need to specify a value for this parameter.\n\n> If you leave this parameter empty, the capacity reservation takes effect immediately.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
