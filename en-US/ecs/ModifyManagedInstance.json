{
  "name": "ModifyManagedInstance",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "The ID of the managed instance.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "InstanceName",
      "description": "The name of the managed instance. The name must be 2 to 128 characters in length and must start with a letter. It cannot start with a special character or a digit. The name can contain periods (.), underscores (_), hyphens (-), and colons (:). It cannot start with `http://` or `https://`.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region ID. The following regions are supported: China (Qingdao), China (Beijing), China (Zhangjiakou), China (Hohhot), China (Ulanqab), China (Hangzhou), China (Shanghai), China (Shenzhen), China (Heyuan), China (Guangzhou), China (Chengdu), Hong Kong (China), Singapore, Japan (Tokyo), US (Silicon Valley), and US (Virginia).\n\nYou can call [DescribeRegions](~~25609~~) to query the region IDs and other information.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
