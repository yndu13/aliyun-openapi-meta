{
  "name": "ModifyAutoSnapshotPolicyEx",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CopiedSnapshotsRetentionDays",
      "description": "The retention period of cross-region snapshot replicas. Unit: days. Valid values:\n\n- -1: Snapshot replicas are permanently retained.\n- 1 to 65535: specifies the number of days for which snapshot replicas are retained.\n\nDefault value: -1.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "CopyEncryptionConfiguration",
      "description": "The encryption parameter object for cross-region snapshot replication.",
      "position": "Query",
      "type": "Struct",
      "required": false
    },
    {
      "name": "EnableCrossRegionCopy",
      "description": "Specifies whether to allow automatic cross-region replication.\n\n- true: allowed.\n- false: not allowed.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "TargetCopyRegions",
      "description": "The destination region to which snapshots are replicated. Currently, you can set only one destination region.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "TargetTags",
      "description": "The list of target resource tags. The automatic snapshot policy matches target resources based on tags.\nThis parameter is required when AssociationType is set to AssociatedWithInstanceTag.",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "Key",
          "description": "The tag key.\nValid values of N: 1 to 10.\nThe tag key cannot be an empty string. The tag key can be up to 128 characters in length and cannot start with aliyun or acs:. The tag key cannot contain http:// or https://.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "Value",
          "description": "The tag value.\nValid values of N: 1 to 10. The tag value can be up to 128 characters in length and cannot contain http:// or https://.\nNote: If you pass in an empty value or an empty string, it indicates any value.",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    },
    {
      "name": "autoSnapshotPolicyId",
      "description": "The ID of the automatic snapshot policy. You can call [DescribeAutoSnapshotPolicyEx](~~25530~~) to query available automatic snapshot policies.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "autoSnapshotPolicyName",
      "description": "The name of the automatic snapshot policy. If this parameter is left empty, the name is not modified.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "regionId",
      "description": "The region ID of the automatic snapshot policy. You can call [DescribeRegions](~~25609~~) to query the most recent region list.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "repeatWeekdays",
      "description": "The days of the week on which automatic snapshots are created. Unit: days. The cycle is weekly. Valid values: 1 to 7, where 1 indicates Monday.\n\nTo create multiple automatic snapshots within a week, specify multiple days:\n\n- You can specify up to 7 days.\n- Specify multiple days in a JSON array in the format of `\"1\", \"2\", … \"7\"`. Separate the days with commas (,).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "retentionDays",
      "description": "The retention period of automatic snapshots. Unit: days. Valid values:\n\n- -1: Automatic snapshots are permanently retained.\n- 1 to 65536: specifies the number of days for which automatic snapshots are retained.\n\nDefault value: -1.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "timePoints",
      "description": "The points in time at which automatic snapshots are created. The time is displayed in UTC+8. Unit: hours. Valid values: 0 to 23, which correspond to the 24 points in time from 00:00 to 23:00. For example, 1 indicates 01:00.\n\nTo create multiple automatic snapshots within a day, specify multiple points in time:\n\n- You can specify up to 24 points in time.\n- Specify multiple points in time in a JSON array in the format of `\"0\", \"1\", … \"23\"`. Separate the points in time with commas (,).",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
