{
  "name": "CreateDisk",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AdvancedFeatures",
      "description": "This parameter is not publicly available.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Arn",
      "description": ">This parameter is not publicly available.",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "AssumeRoleFor",
          "description": ">This parameter is not publicly available.",
          "position": "Query",
          "type": "Long",
          "required": false
        },
        {
          "name": "RoleType",
          "description": ">This parameter is not publicly available.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "Rolearn",
          "description": ">This parameter is not publicly available.",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    },
    {
      "name": "BurstingEnabled",
      "description": "Specifies whether to enable the performance burst feature. Valid values:\n\n- true: enables the performance burst feature.\n- false: disables the performance burst feature.\n\n>This parameter is supported only when `DiskCategory` is set to `cloud_auto`. For more information, see [ESSD AutoPL disk](~~368372~~).",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but make sure that the token is unique among different requests. The **ClientToken** value can contain only ASCII characters and cannot exceed 64 characters in length. For more information, see [How to ensure idempotence](~~25693~~).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Description",
      "description": "The description of the disk. The description must be 2 to 256 characters in length and cannot start with `http://` or `https://`.\n\nDefault value: empty.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DiskCategory",
      "description": "The category of the data disk. Valid values:\n\n- cloud: basic disk.\n- cloud_efficiency: ultra disk.\n- cloud_ssd: standard SSD.\n- cloud_essd: enterprise SSD.\n- cloud_auto: ESSD AutoPL disk.\n- cloud_essd_entry: ESSD Entry disk.\n- cloud_regional_disk_auto: regional Enterprise SSD (ESSD).\n- elastic_ephemeral_disk_standard: elastic ephemeral disk - Standard Edition.\n- elastic_ephemeral_disk_premium: elastic ephemeral disk - Premium Edition.\n\nDefault value: cloud.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DiskName",
      "description": "The disk name. The name must be 2 to 128 characters in length and can contain characters that are categorized as letter in Unicode, including Chinese and English characters, and ASCII digits (0-9). The name can contain colons (:), underscores (_), periods (.), or hyphens (-). The name must start with a character that is categorized as letter in Unicode.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "EncryptAlgorithm",
      "description": "This parameter is not publicly available.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Encrypted",
      "description": "Specifies whether to encrypt the disk. Valid values:",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "InstanceId",
      "description": "Creates a subscription disk and automatically attaches it to the specified subscription instance.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "KMSKeyId",
      "description": "The ID of the KMS key used by the disk.\n\n> If Encrypted is set to true and KMSKeyId is not specified, the default key is used for encryption, and the KMSKeyId value is returned after the instance is created.\n> - - Disk created from a non-shared encrypted snapshot: The encryption key used by the snapshot is used by default.\n> - - Disk created from a shared encrypted snapshot: The service key is used by default.\n> - - Disk created in a region where block storage account-level default encryption is enabled: The specified account-level key is used by default.\n> - - Other cases: The service key is used by default.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "MultiAttach",
      "description": "Specifies whether to enable the multi-attach feature. Valid values:\n\n- Disabled: Multi-attach is not enabled.\n- Enabled: Multi-attach is enabled. Currently, only enterprise SSDs support this feature.\n\nDefault value: Disabled.\n\n> Disks with the multi-attach feature enabled support only the pay-as-you-go billing method. Therefore, when `MultiAttach=Enabled`, you cannot set the `InstanceId` parameter at the same time. After the disk is created, you can call [AttachDisk](~~25515~~) to attach it. Note that disks with the multi-attach feature enabled can be attached only as data disks.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PerformanceLevel",
      "description": "The performance level of the ESSD to create. Valid values:\n\n- PL0: A single disk can deliver up to 10,000 random read/write IOPS.\n- PL1: A single disk can deliver up to 50,000 random read/write IOPS.\n- PL2: A single disk can deliver up to 100,000 random read/write IOPS.\n- PL3: A single disk can deliver up to 1,000,000 random read/write IOPS.\n\nDefault value: PL1.\n\nFor information about how to select an ESSD performance level, see [ESSD](~~122389~~).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ProvisionedIops",
      "description": "The provisioned read/write IOPS of an ESSD AutoPL disk (per disk). Valid values:\n\n- Capacity (GiB) <= 3: Provisioned performance cannot be configured.\n\n- Capacity (GiB) >= 4: [0, min{(1,000 IOPS/GiB × Capacity - Baseline IOPS), 50,000}]\n\n\nBaseline performance = max{min{1,800 + 50 × Capacity, 50,000}, 3,000}.\n\n\n> This parameter is supported only when DiskCategory is set to cloud_auto. For more information, see [ESSD AutoPL disk](~~368372~~).",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID. You can call [DescribeRegions](~~25609~~) to query the most recent region list.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ResourceGroupId",
      "description": "The ID of the resource group to which the disk belongs.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Size",
      "description": "The disk size. Unit: GiB. You must specify a value for this parameter. Valid values:",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "SnapshotId",
      "description": "The snapshot ID that is used to create the disk. Snapshots created on or before July 15, 2013 cannot be used to create disks.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "StorageClusterId",
      "description": "The ID of the dedicated block storage cluster. To create a disk in a specific dedicated block storage cluster, specify this parameter.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "StorageSetId",
      "description": "The storage set ID.\n\n> You can set either the storage set parameters (StorageSetId and StorageSetPartitionNumber) or the dedicated block storage cluster parameter (StorageClusterId), but not both. If you set both, the API call fails.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "StorageSetPartitionNumber",
      "description": "The number of partitions in the storage set. Valid values: greater than or equal to 2, up to the maximum allowed by the privilege quota displayed after you call [DescribeAccountAttributes](~~73772~~).\n\nDefault value: 2.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "Tag",
      "description": "The tags of the disk.",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "Key",
          "description": "The tag key of the disk. Valid values of N: 1 to 20. The tag key cannot be an empty string. The tag key can be up to 128 characters in length and cannot start with `aliyun` or `acs:`. The tag key cannot contain `http://` or `https://`.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "Value",
          "description": "The tag value of the disk. Valid values of N: 1 to 20. The tag value can be an empty string. The tag value can be up to 128 characters in length and cannot contain `http://` or `https://`.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "key",
          "description": "The tag key of the disk.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "value",
          "description": "The tag value of the disk.",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    },
    {
      "name": "ZoneId",
      "description": "Creates a pay-as-you-go disk in the specified zone.\n\n- If you do not set InstanceId, ZoneId is required.\n- You cannot specify both ZoneId and InstanceId.\n\n\n> Disks of the `cloud_regional_disk_auto` type do not require ZoneId.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
