{
  "name": "ResetDisks",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Disk",
      "description": "The list of cloud disks.",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "sub_parameters": [
        {
          "name": "DiskId",
          "description": "The ID of the cloud disk to be rolled back. Valid values of N: 1 to 10.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "SnapshotId",
          "description": "The snapshot ID that corresponds to the specified cloud disk in the instance snapshot. Valid values of N: 1 to 10.",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform a dry run. Valid values:\n\n- true: performs a dry run without actually rolling back the cloud disks. The system checks whether required parameters are specified, whether the request format is valid, and whether resource status constraints are met. If the check fails, the corresponding error message is returned. If the check succeeds, the error code `DryRunOperation` is returned.\n- false: performs a dry run and sends the request. If the check succeeds, the cloud disk rollback operation is initiated.\n\nDefault value: false.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID. You can call [DescribeRegions](~~25609~~) to query the most recent region list.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
