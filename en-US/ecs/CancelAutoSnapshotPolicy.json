{
  "name": "CancelAutoSnapshotPolicy",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "autoSnapshotPolicyId",
      "description": "The ID of the automatic snapshot policy that you want to cancel.\n- Default value: empty. If you use the default value, note the following items:\n  - If only one automatic snapshot policy is applied to the cloud disk, the automatic snapshot policy is canceled.\n  - If more than one automatic snapshot policy is applied to the cloud disk, the `OperationDenied.TooManyAutoSnapshotPolicies` error code is returned and the request fails. Specify the `autoSnapshotPolicyId` parameter to specify the ID of the automatic snapshot policy that you want to cancel.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "diskIds",
      "description": "The IDs of the cloud disks. To cancel the automatic snapshot policy for multiple cloud disks, specify the cloud disk IDs in a JSON array in the format of \"d-xxxxxxxxx\", \"d-yyyyyyyyy\", … \"d-zzzzzzzzz\". Separate multiple cloud disk IDs with commas (,).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "regionId",
      "description": "The region ID of the automatic snapshot policy and cloud disks. You can call [DescribeRegions](~~25609~~) to query the most recent region list.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
