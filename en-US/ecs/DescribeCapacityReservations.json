{
  "name": "DescribeCapacityReservations",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceChargeType",
      "description": "The billing method of the instance. Valid values:\n\n- PostPaid: pay-as-you-go.\n- PrePaid: subscription.\n\nDefault value: PostPaid.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "InstanceType",
      "description": "The instance type. You can use the instance type to query only active capacity reservations. Released capacity reservations can be queried only by using PrivatePoolOptions.Ids.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "MaxResults",
      "description": "The maximum number of entries per page for a paged query.\n\nMaximum value: 100.\n\nDefault value: 10.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "NextToken",
      "description": "The pagination token for the capacity reservation query. Obtain the value from the result of the previous request.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Platform",
      "description": "The operating system of the instance. Valid values:\n\n- windows: queries only capacity reservations for Windows instances.\n- linux: queries only capacity reservations for Linux instances.\n- all: queries all capacity reservations.\n\nDefault value: all.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PrivatePoolOptions.Ids",
      "description": "The list of capacity reservation IDs. The value can be a JSON array that consists of up to 100 IDs. Separate the IDs with commas (,).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID of the capacity reservation. You can call [DescribeRegions](~~25609~~) to query the most recent region list.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ResourceGroupId",
      "description": "The resource group ID. When you use this parameter to filter resources, the resource count cannot exceed 1000.\n\n>Filtering by the default resource group is not supported.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Status",
      "description": "The status of the capacity reservation. Valid values:\n\n- All: all states.\n- Pending: initializing. A capacity reservation that takes effect at a specified time enters the initializing state first.\n- Preparing: being prepared. A capacity reservation that takes effect at a specified time is in the Preparing state during the resource delivery phase.\n- Prepared: to take effect. A capacity reservation that takes effect at a specified time is in the Prepared state after resource delivery is complete but before the service takes effect.\n- Active: active.\n- Released: released, including manual release and automatic release upon expiration.\n\nIf you do not specify this parameter, capacity reservations in all states except Pending and Released are queried.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Tag",
      "description": "The tags bound to the capacity reservation.",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "Key",
          "description": "The tag key. N indicates that you can set multiple tag keys for filtering. Valid values of N: 1 to 20.\n\nIf you use a single tag to filter resources, the resource count with the specified tag cannot exceed 1000. If you use multiple tags to filter resources, the resource count with all specified tags attached cannot exceed 1000. If the resource count exceeds 1000, call the [ListTagResources](~~110425~~) operation to query resources.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "Value",
          "description": "The tag value. N indicates that you can set multiple tag values for filtering. Valid values of N: 1 to 20.",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    },
    {
      "name": "ZoneId",
      "description": "The zone ID of the capacity reservation.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
