{
  "name": "DescribeKeyPairs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "IncludePublicKey",
      "description": "Specifies whether to include PublicKey in the response.\nDefault value: false.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "KeyPairFingerPrint",
      "description": "The fingerprint of the key pair. The public key fingerprint format is defined in RFC 4716 and uses the MD5 message digest algorithm. For more information, see [RFC 4716](https://tools.ietf.org/html/rfc4716).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "KeyPairName",
      "description": "The name of the key pair. Fuzzy search with regular expressions is supported. You can use an asterisk (*) to match subexpressions. Examples:\n\n- `*SshKey`: queries key pair names that end with SshKey, including SshKey.\n- `SshKey*`: queries key pair names that start with SshKey, including SshKey.\n- `*SshKey*`: queries key pair names that contain SshKey, including SshKey.\n- `SshKey`: exact match of SshKey.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PageNumber",
      "description": "The page number of the key pair list. Minimum value: 1.\n\nDefault value: 1.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "PageSize",
      "description": "The number of entries per page for a paged query. Maximum value: 50.\n\nDefault value: 10.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID of the key pairs. You can call [DescribeRegions](~~25609~~) to query the most recent region list.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ResourceGroupId",
      "description": "The ID of the resource group to which the key pairs belong. When you use this parameter to filter resources, the resource count cannot exceed 1000.\n\n>Filtering by the default resource group is not supported.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Tag",
      "description": "The tags.",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "Key",
          "description": "The tag key of the key pair. Valid values of N: 1 to 20.\n\nIf you use a single tag to filter resources, the resource count with the specified tag cannot exceed 1000. If you use multiple tags to filter resources, the resource count of resources that are attached with all specified tags cannot exceed 1000. If the resource count exceeds 1000, call the [ListTagResources](~~110425~~) operation to query resources.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "Value",
          "description": "The tag value of the key pair. Valid values of N: 1 to 20.",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    }
  ]
}
