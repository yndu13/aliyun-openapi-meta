{
  "name": "ModifyInstanceSpec",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AllowMigrateAcrossZone",
      "description": "Specifies whether cross-cluster instance type upgrade is supported. Valid values:\n- true: Supported.\n- false: Not supported.\n\nDefault value: false.\n\nIf you set the parameter `AllowMigrateAcrossZone` to true and upgrade the Elastic Compute Service instance based on the response, note the following:\n\nClassic network type instances:\n    \n* For [retired instance types](~~55263~~), when a non-I/O optimized instance is changed to an I/O optimized instance, the private IP address, disk device names, and software authorization codes of the instance change. For Linux instances, basic disks (`cloud`) are identified as **xvda** or **xvdb**, and ultra disks (`cloud_efficiency`) and standard SSDs (`cloud_ssd`) are identified as **vda** or **vdb**.\n    \n* For [instance families that are available for purchase](~~25378~~), the private IP address of the instance changes.\n\nVPC-type instances: For [retired instance types](~~55263~~), when a non-I/O optimized instance is changed to an I/O optimized instance, the server disk device names and software authorization codes of the instance change. For Linux instances, basic disks (`cloud`) are identified as **xvda** or **xvdb**, and ultra disks (`cloud_efficiency`) and standard SSDs (`cloud_ssd`) are identified as **vda** or **vdb**.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "Async",
      "description": "Specifies whether to submit an asynchronous request. Valid values:\n- true: The request is submitted asynchronously.\n- false: The request is not submitted asynchronously.\n\nDefault value: false.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but make sure that the token is unique among different requests. The **ClientToken** value can contain only ASCII characters and cannot exceed 64 characters in length. For more information, see [How to ensure idempotence](~~25693~~).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Disk",
      "description": "> This parameter is not publicly available.",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "Category",
          "description": "> This parameter is not publicly available.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "DiskId",
          "description": "> This parameter is not publicly available.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "PerformanceLevel",
          "description": "> This parameter is not publicly available.",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform only a dry run. Valid values:\n\n- true: performs only a dry run. The instance type and public bandwidth are not modified. The system checks whether the required parameters are specified, whether the request format is valid, whether business restrictions are met, and whether ECS resources are available. If the check fails, the corresponding error is returned. If the check succeeds, the `DryRunOperation` error code is returned.\n- false (default): performs a dry run and sends the request. If the check succeeds, the instance type and public bandwidth are directly modified.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "InstanceId",
      "description": "The instance ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "InstanceType",
      "description": "The target instance type. For more information, see [Instance family](~~25378~~). You can also invoke [DescribeInstanceTypes](~~25620~~) to query the most recent instance type list.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "InternetMaxBandwidthIn",
      "description": "The maximum inbound public bandwidth. Unit: Mbit/s. Valid values:\n\n- If the purchased outbound public bandwidth is less than or equal to 10 Mbit/s: 1 to 10. Default value: 10.\n- If the purchased outbound public bandwidth is greater than 10 Mbit/s: 1 to the value of `InternetMaxBandwidthOut`. Default value: the value of `InternetMaxBandwidthOut`.\n\n> In **pay-by-traffic** mode, the peak inbound and outbound bandwidths are used as bandwidth upper limits and are not guaranteed. When resource contention occurs, the peak bandwidths may be limited. If your business requires guaranteed bandwidth, use the **pay-by-bandwidth** mode.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "InternetMaxBandwidthOut",
      "description": "The maximum outbound public bandwidth. Unit: Mbit/s. Valid values: 0 to 100.\n\n> In **pay-by-traffic** mode, the peak inbound and outbound bandwidths are used as bandwidth upper limits and are not guaranteed. When resource contention occurs, the peak bandwidths may be limited. If your business requires guaranteed bandwidth, use the **pay-by-bandwidth** mode.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "ModifyMode",
      "description": "> This parameter is not publicly available.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "SystemDisk.Category",
      "description": "The new category of the system disk. Valid values:\n\n- cloud_efficiency: ultra disk\n\n- cloud_ssd: standard SSD\n\n> This parameter is valid only when you upgrade from a [retired instance type](~~55263~~) to an [instance family that is available for purchase](~~25378~~) and change a non-I/O optimized instance to an I/O optimized instance.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Temporary.EndTime",
      "description": "> This parameter is in invitational preview and is not publicly available.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Temporary.InternetMaxBandwidthOut",
      "description": "> This parameter is in invitational preview and is not publicly available.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "Temporary.StartTime",
      "description": "> This parameter is in invitational preview and is not publicly available.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
