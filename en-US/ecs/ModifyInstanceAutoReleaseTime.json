{
  "name": "ModifyInstanceAutoReleaseTime",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoReleaseTime",
      "description": "The automatic release time. Specify the time in the [ISO 8601](~~25696~~) standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC.\n\n- If the value of seconds (`ss`) is not `00`, the time is automatically set to the start of the current minute (`mm`).\n\n- The earliest release time must be at least 30 minutes after the current time.\n\n- The latest release time cannot be more than three years from the current time.\n\nIf you do not specify the AutoReleaseTime parameter, the automatic release feature is canceled and the ECS instance is no longer automatically released.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "InstanceId",
      "description": "The instance ID of the ECS instance for which to configure automatic release.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region ID of the instance. You can call [DescribeRegions](~~25609~~) to query the most recent region list.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
