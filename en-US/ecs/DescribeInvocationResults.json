{
  "name": "DescribeInvocationResults",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CommandId",
      "description": "The command ID.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ContentEncoding",
      "description": "The encoding type of the `CommandContent` and `Output` fields in the response. Valid values:\n- PlainText: Returns the original command content and output.\n- Base64: Returns the Base64-encoded command content and output.\n\nDefault value: Base64.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "IncludeHistory",
      "description": "Specifies whether to return the execution history of scheduled commands. Valid values:\n\n - true: Returns the execution results of scheduled commands. When this parameter is set to true, the InvokeId parameter cannot be empty and must be the execution ID of a scheduled command (RepeatMode is Period) or a command that runs on each system startup (RepeatMode is EveryReboot).\n - false: Does not return the execution history.\n\nDefault value: false.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "InstanceId",
      "description": "The instance ID.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "InvokeId",
      "description": "The execution ID of the command. You can call [DescribeInvocations](~~64840~~) to query the InvokeId.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "InvokeRecordStatus",
      "description": "The execution status of the command. Valid values:\n\n- Running: The command is running.\n    - Scheduled execution: The execution status remains running until you manually stop the scheduled command.\n    - One-time execution: The overall execution status is running as long as the command is running on any instance.\n- Finished: The command execution is complete.\n    - Scheduled execution: The command process cannot be in the finished state.\n    - One-time execution: The command execution is complete on all instances, or the command process is manually stopped on some instances and the execution is complete on the remaining instances.\n- Success:\n    - One-time execution: The command execution is complete and the exit code is 0.\n    - Scheduled execution: The last execution was successful with an exit code of 0, and the specified execution time has ended.\n- Failed: The command execution failed.\n    - Scheduled execution: The command process cannot be in the failed state.\n    - One-time execution: The command execution failed on all instances.\n- PartialFailed: The command execution partially failed.\n    - Scheduled execution: The command process cannot be in the partially failed state.\n    - One-time execution: The command execution failed on some instances, so the overall execution status is partially failed.\n- Stopped: The command execution is stopped.\n- Stopping: The command execution is being stopped.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "MaxResults",
      "description": "The maximum number of entries per page for paging query.\n\nMaximum value: 50.\n\nDefault value: 10.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "NextToken",
      "description": "The pagination token. Set this parameter to the NextToken value returned in the previous API call.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PageNumber",
      "description": "> This parameter is about to go offline. Use NextToken and MaxResults to perform paging query operations.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "PageSize",
      "description": "> This parameter is about to go offline. Use NextToken and MaxResults to perform paging query operations.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID. You can call [DescribeRegions](~~25609~~) to query the most recent region list.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ResourceGroupId",
      "description": "The ID of the resource group to which the command execution belongs. After you specify this parameter, you must also specify ResourceGroupId when you run the command. This parameter is used to filter the corresponding command execution results.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Tag",
      "description": "The tags.",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "Key",
          "description": "The tag key of the command execution. Valid values of N: 1 to 20. The tag key cannot be an empty string.\n\nIf you use a single tag to filter resources, the resource count with this tag cannot exceed 1,000. If you use multiple tags to filter resources, the resource count with all the specified tags attached cannot exceed 1,000. If the resource count exceeds 1,000, use the [ListTagResources](~~110425~~) operation to execute the query.\n\nThe tag key can be up to 64 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "Value",
          "description": "The tag value of the command execution. Valid values of N: 1 to 20. The tag value can be an empty string.\n\nThe tag value can be up to 128 characters in length and cannot contain `http://` or `https://`.",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    }
  ]
}
