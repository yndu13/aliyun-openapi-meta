{
  "name": "DeleteStack",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "DELETE",
  "pathPattern": "/stacks/[stackId]",
  "parameters": [
    {
      "name": "cleanResources",
      "description": "Specifies whether to synchronously clean up resources managed by the stack. By default, resources are not cleaned up.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "stackId",
      "description": "The stack ID. This is the unique identifier generated after the stack is created.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
