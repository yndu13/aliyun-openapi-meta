{
  "name": "GetStack",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/stacks/[stackId]",
  "parameters": [
    {
      "name": "stackId",
      "description": "The unique identifier of the stack, which is generated after the stack is created.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
