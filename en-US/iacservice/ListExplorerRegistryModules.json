{
  "name": "ListExplorerRegistryModules",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/explorerRegistryModule",
  "parameters": [
    {
      "name": "keyword",
      "description": "The search keyword for the module name. Fuzzy matching is performed based on `moduleName`.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "maxResults",
      "description": "The maximum number of entries per page.\n\nValid values: 0 to 200.\n\nDefault value: 100.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "moduleName",
      "description": "The name of the module.\n\nNote: The module name is not necessarily the same as the product name or resource name.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "nextToken",
      "description": "The pagination token for the next page of results.\n\nIf the total number of entries exceeds the maxResults limit, the data is truncated. You can use nextToken to query the next page of data.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "sort",
      "description": "The sort order of the returned results. Valid values:\n- Normal (default): returns results in normal order.\n- Top: returns results sorted by popularity.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
