{
  "name": "Id2MetaPeriodVerify",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "IdentifyNum",
      "description": "The ID card number.\n\n- If paramType is set to normal, enter the ID card number in plaintext.\n- If paramType is set to md5, the value is in the following format: first 6 digits of the ID card number (plaintext) + date of birth (ciphertext) + last 4 digits of the ID card number (plaintext).",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "ParamType",
      "description": "The parameter type. Valid values:\n\n- normal: not encrypted.\n- md5: MD5-encrypted.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "UserName",
      "description": "The name.\n\n- paramType = normal: enter the name in plaintext.\n- paramType = md5: the first character of the name is MD5-encrypted (32-bit lowercase MD5) + the remaining characters of the name in plaintext.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "ValidityEndDate",
      "description": "The expiration date of the ID card validity period. Format: YYYYMMDD.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "ValidityStartDate",
      "description": "The start date of the ID card validity period. Format: YYYYMMDD.",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ]
}
