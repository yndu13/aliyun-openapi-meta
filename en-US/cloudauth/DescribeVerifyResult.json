{
  "name": "DescribeVerifyResult",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BizId",
      "description": "The verification ID. A unique ID that identifies a verification task. The value can be up to 64 characters in length. For a single verification task, the system supports unlimited submissions until the verification is passed and the task is completed.\n> Use a different BizId for each verification task.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "BizType",
      "description": "The business scenario identifier of the ID Verification service.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
