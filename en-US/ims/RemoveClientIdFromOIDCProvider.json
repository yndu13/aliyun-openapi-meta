{
  "name": "RemoveClientIdFromOIDCProvider",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientId",
      "description": "The client ID to remove.\n\nFormat: letters, digits, and the special characters `.-_:/` are allowed. The value cannot start with the special characters `.-_:/`.\n\nLength: up to 128 characters.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "OIDCProviderName",
      "description": "The name of an existing OIDC IdP that has the target `ClientId` attached. If you have not created or attached one, call `CreateOIDCProvider` (with `ClientIds` specified) or `AddClientIdToOIDCProvider` first.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
