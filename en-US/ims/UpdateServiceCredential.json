{
  "name": "UpdateServiceCredential",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ServiceCredentialId",
      "description": "The service credential ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ServiceCredentialName",
      "description": "The name of the service credential. The name must be 1 to 128 characters in length and can contain letters, digits, periods (.), hyphens (-), and underscores (_). At least one of Status and ServiceCredentialName must be specified.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Status",
      "description": "The status of the service credential. Valid values:\n- Active\n- Inactive\n\nAt least one of Status and ServiceCredentialName must be specified.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "UserPrincipalName",
      "description": "The logon name of the Resource Access Management (RAM) user. If this parameter is not specified, the service credential of the identity that you use to invoke this operation is modified.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
