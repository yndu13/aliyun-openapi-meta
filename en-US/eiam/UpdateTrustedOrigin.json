{
  "name": "UpdateTrustedOrigin",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "InstanceId",
      "description": "The ID of the IDaaS EIAM instance.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "TrustOriginName",
      "description": "If this parameter is not specified, the trusted origin name is not modified.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "TrustedOriginId",
      "description": "The ID of the trusted origin.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "TrustedOriginScene",
      "description": "When specified, the existing values are entirely replaced. Only iframe_embed and cors are supported.",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    }
  ]
}
