{
  "name": "ListGatewayZone",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "description": "The language of the response. Valid values: `zh` (Chinese) and `en` (English). Default value: `zh`.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
