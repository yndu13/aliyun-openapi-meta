{
  "name": "DeleteSiteMonitors",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "IsDeleteAlarms",
      "description": "Specifies whether to delete the alert rules associated with the tasks. Valid values:\n\n- true (default): Deletes the alert rules.\n\n- false: Does not delete the alert rules.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "TaskIds",
      "description": "The IDs of the tasks to be deleted. Separate multiple task IDs with commas (,).",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
