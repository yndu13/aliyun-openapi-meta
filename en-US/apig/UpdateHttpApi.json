{
  "name": "UpdateHttpApi",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "PUT",
  "pathPattern": "/v1/http-apis/[httpApiId]",
  "parameters": [
    {
      "name": "body",
      "description": "The request body parameters.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "dryRun",
      "description": "Specifies whether to perform only a dry run. If set to true, all synchronous validations identical to a real update are performed without updating any configurations or producing side effects. If not specified or set to false, the behavior is the same as the existing version.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "httpApiId",
      "description": "The ID of the HTTP API to update.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
