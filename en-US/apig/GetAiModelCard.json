{
  "name": "GetAiModelCard",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/v1/ai-model-cards/[modelCardId]",
  "parameters": [
    {
      "name": "modelCardId",
      "description": "The ID of the model card to query. This ID is returned by the model creation operation and is in a format that starts with mc-.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
