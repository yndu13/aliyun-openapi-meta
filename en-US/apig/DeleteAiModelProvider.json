{
  "name": "DeleteAiModelProvider",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "DELETE",
  "pathPattern": "/v1/ai-model-providers/[modelProviderId]",
  "parameters": [
    {
      "name": "modelProviderId",
      "description": "The ID of the model provider to delete. This ID is returned by the model creation operation and is in a format that starts with mp-.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
