{
  "name": "CreateHttpApiOperation",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/v1/http-apis/[httpApiId]/operations",
  "parameters": [
    {
      "name": "body",
      "description": "The request parameters. When calling this operation, you must nest the operations array within a JSON object named body. Do not place it directly in the top-level parameters.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "httpApiId",
      "description": "The ID of the HTTP API to which the operations belong.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
