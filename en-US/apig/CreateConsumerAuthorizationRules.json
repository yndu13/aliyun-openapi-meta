{
  "name": "CreateConsumerAuthorizationRules",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/v1/authorization-rules",
  "parameters": [
    {
      "name": "body",
      "description": "The request body parameters. This is a ROA-style API. All business parameters must be nested within the body key and transmitted as a JSON request body. Do not place fields such as authorizationRules at the top level of the request parameters. Otherwise, the server returns a 400 error because the request body is empty (authorizationRules parameter is empty).",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ]
}
