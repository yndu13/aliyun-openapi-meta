{
  "name": "DescribeRegions",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "/v1/regions",
  "parameters": [
    {
      "name": "language",
      "description": "The language of the response. Valid values: zh, en.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
