{
  "name": "DeleteAiModelCard",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "DELETE",
  "pathPattern": "/v1/ai-model-cards/[modelCardId]",
  "parameters": [
    {
      "name": "modelCardId",
      "description": "The ID of the model card to delete. This ID is returned by the model creation operation and is in a format that starts with mc-.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
