{
  "name": "SubmitFpDBDeleteJob",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DelType",
      "description": "The type of operation. Valid values:\n\n- **Purge**: Purges the DNA library. This operation removes all content from the library but does not delete the library itself.\n\n- **Delete**: Deletes the DNA library. This operation permanently removes the library. Note: This is the only supported operation for fingerprint libraries with a `modelId` from 11 to 14.\n\n- Default value: **Purge**.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "FpDBId",
      "description": "The API call to [create a DNA library](~~170149~~) returns this ID in its response.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "PipelineId",
      "description": "The ID of the ApsaraVideo Media Processing (MPS) pipeline for the job. MPS uses this ID to send message notifications. You can obtain the pipeline ID in the **MPS console** by navigating to **Global Settings** > **Pipelines**.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "UserData",
      "description": "The user-defined data. The value can be a string of up to 128 bytes.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
