{
  "name": "SubmitImageCopyright",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Message",
      "description": "The content of the watermark.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Output",
      "description": "The OSS location for the output transparent image watermark.\n\n> The OSS bucket must be in the same region where MPS is activated.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Params",
      "description": "The watermark parameters, provided as a JSON string. This string contains the following fields:\n\n- `width`: The width of the output image.\n\n- `height`: The height of the output image.\n\n- `afa`: The transparency level of the watermark. Valid values: 0 to 255.\n\n- `type`: The watermark algorithm. Valid values: `0` (for game rendering) and `1` (for general application rendering).\n\n- `version`: The watermark version. Currently, only the value `0` is supported.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
