{
  "name": "QueryFpDBDeleteJobList",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "JobIds",
      "description": "The IDs of the jobs to query. You can get job IDs from the response of the [SubmitFpDBDeleteJob](~~209341~~) operation. To query multiple jobs, separate the IDs with a comma (,). If you omit this parameter, the operation returns the 20 most recent jobs.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
