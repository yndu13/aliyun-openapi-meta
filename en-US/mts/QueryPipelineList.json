{
  "name": "QueryPipelineList",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PipelineIds",
      "description": "A comma-separated list of pipeline IDs to query. Find these IDs in the **MPS console** > **Global Settings** > **Pipelines**. Specify up to 10 IDs, separated by commas (,).",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
