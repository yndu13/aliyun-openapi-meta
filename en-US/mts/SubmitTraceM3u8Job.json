{
  "name": "SubmitTraceM3u8Job",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "KeyUri",
      "description": "The key server URI.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "MediaId",
      "description": "The media ID of the processed AB stream. You can obtain this ID from the response of the operation that submits the AB stream processing job.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Output",
      "description": "The OSS location of the output M3U8 file.\n\n> The OSS bucket must be in the same region as your MPS service.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Params",
      "description": "The parameters for the watermark job, specified as a JSON string. The following parameters are supported:\n\n- `m3u8Type`: The algorithm type. The default value is `v1`.\n\n  - `v1`: Generates an M3U8 file with absolute paths that can be played directly. The signature authentication is valid for 24 hours. After expiration, you must submit a new job to get a new M3U8 file.\n\n  - `v2`: Generates an M3U8 file with relative paths. This file must be in the same directory as the TS files.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Trace",
      "description": "The trace watermark information.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
