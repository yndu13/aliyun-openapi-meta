{
  "name": "QueryJobList",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "JobIds",
      "description": "Transcoding job IDs.\n- You can query up to 10 jobs at a time. Separate multiple IDs with commas (,).\n- You can obtain job IDs from the [MPS console](https://mps.console.aliyun.com/overview) > **Task Management**, or from the response parameters of the [SubmitJobs](~~29226~~) API.\n\n> If this parameter is not specified, the `InvalidParameter` (invalid parameter) error code will be returned.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
