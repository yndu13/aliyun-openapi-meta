{
  "name": "GrantAccountPrivilegeZonal",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccountName",
      "description": "The account name.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "AccountPrivilege",
      "description": "The account permissions. Valid values:\n\n- ReadWrite: Read and write permissions.\n\n- ReadOnly: Read-only permissions.\n\n- DMLOnly: DML-only permissions.\n\n- DDLOnly: DDL-only permissions.\n\n- ReadIndex: Read-only and index permissions.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ClientToken",
      "description": "A client-generated token that ensures the idempotence of the request. Make sure that the token is unique among different requests. The token is case-sensitive and can be up to 64 ASCII characters in length.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DBClusterId",
      "description": "The cluster ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "DBName",
      "description": "The name of the database for which to grant permissions. To grant permissions for multiple databases, separate the database names with a comma (,).",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
