{
  "name": "RestoreTable",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BackupId",
      "description": "The backup set ID.\n\n> This parameter is required if you want to restore databases and tables from a backup set. Call the [DescribeBackups](~~98102~~) operation to query backup set IDs.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DBClusterId",
      "description": "The cluster ID.\n\n> Call the [DescribeDBClusters](~~98094~~) operation to query the details of all clusters in your account.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RestoreTime",
      "description": "The point in time to which you want to restore data. Specify the time in the YYYY-MM-DDThh:mmZ format. The time must be in Coordinated Universal Time (UTC).\n\n> - This parameter is required if you want to restore data to a specific point in time.\n>\n> - Data can be restored to any point in time within the last seven days.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "TableMeta",
      "description": "A JSON string that specifies the destination databases and tables to restore. All values in the JSON string must be strings.\nFor example: `[ { \"tables\":[ { \"name\":\"testtb\", \"type\":\"table\", \"newname\":\"testtb_restore\" } ], \"name\":\"testdb\", \"type\":\"db\", \"newname\":\"testdb_restore\" } ]`.\n\n> Call the [DescribeMetaList](~~194770~~) operation to query the names of the databases and tables that can be restored. Then, enter the information into the example format.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
