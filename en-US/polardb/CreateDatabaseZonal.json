{
  "name": "CreateDatabaseZonal",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccountName",
      "description": "The name of the account that is authorized to access the database.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "AccountPrivilege",
      "description": "The permissions of the account. Valid values:\n\n- ReadWrite: read and write permissions.\n\n- ReadOnly: read-only permissions.\n\n- DMLOnly: DML permissions only.\n\n- DDLOnly: DDL permissions only.\n\n- ReadIndex: read-only and index permissions.\n\nIf you do not specify this parameter, the default value is ReadWrite.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "CharacterSetName",
      "description": "The character set.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ClientToken",
      "description": "A client token to ensure request idempotence. The client generates this token. The token must be unique across requests. It is case-sensitive and can be up to 64 ASCII characters long.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Collate",
      "description": "The locale setting. This specifies the collation for the new database.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Ctype",
      "description": "The locale setting. This specifies the character classification for the database.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DBClusterId",
      "description": "The cluster ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "DBDescription",
      "description": "The description of the database. The description must meet the following requirements:\n\n- It cannot start with `http://` or `https://`.\n\n- It must be 2 to 256 characters in length.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DBName",
      "description": "The name of the database. The name must meet the following requirements:\n\n- It must consist of lowercase letters, digits, hyphens (-), and underscores (\\_).\n\n- It must start with a letter and end with a letter or a digit. The name can be up to 64 characters long.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
