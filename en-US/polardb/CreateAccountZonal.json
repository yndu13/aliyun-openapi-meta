{
  "name": "CreateAccountZonal",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccountDescription",
      "description": "The description of the account. The description must meet the following requirements:\n\n- Cannot start with `http://` or `https://`.\n\n- Be 2 to 256 characters in length.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "AccountName",
      "description": "The account name. The name must meet the following requirements:\n\n- Start with a lowercase letter and end with a letter or a digit.\n\n- Contain only lowercase letters, digits, and underscores (\\_).\n\n- Be 2 to 16 characters in length.\n\n- Cannot be a reserved username, such as root or admin.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "AccountPassword",
      "description": "The account password. The password must meet the following requirements:\n\n- Contain at least three of the following character types: uppercase letters, lowercase letters, digits, and special characters.\n\n- Be 8 to 32 characters in length.\n\n- Special characters are `!@#$%^&*()_+-=`.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "AccountPrivilege",
      "description": "The permissions of the account. Valid values:\n\n- ReadWrite: read and write permissions.\n\n- ReadOnly: read-only permissions.\n\n- DMLOnly: DML permissions only.\n\n- DDLOnly: DDL permissions only.\n\n- ReadIndex: read and index permissions.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "AccountType",
      "description": "The account type. Valid values:\n\n- Normal: a standard account.\n\n- Super: a privileged account.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ClientToken",
      "description": "A client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token is case-sensitive and can contain a maximum of 64 ASCII characters.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DBClusterId",
      "description": "The cluster ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "DBName",
      "description": "The name of the database that the destination account can access. You can specify multiple database names. Separate them with commas (,).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "NodeType",
      "description": "The node type.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PrivForAllDB",
      "description": "Specifies whether to grant permissions on all current and future databases in the cluster. Valid values:\n\n- 0 or empty: Does not grant permissions.\n\n- 1: Grants permissions.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
