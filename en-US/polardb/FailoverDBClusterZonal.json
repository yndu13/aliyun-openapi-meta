{
  "name": "FailoverDBClusterZonal",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "A client token to ensure the idempotence of the request. The client generates this token. It must be unique across requests. The token is case-sensitive and cannot exceed 64 ASCII characters.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DBClusterId",
      "description": "The cluster ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RollBackForDisaster",
      "description": "Specifies whether to fail back to the original primary zone after a failover. Valid values:\n\n- true: Yes.\n\n- false: No.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "TargetDBNodeId",
      "description": "The ID of the node to promote to the primary node. If you do not specify this parameter, the system automatically selects a node. Call the DescribeDBClusters operation to query node information, such as node IDs.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "TargetZoneType",
      "description": "The type of failover. Valid values:\n\n- Primary: A primary/secondary failover within the primary zone.\n\n- Standby: A switch to the hot standby storage cluster.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
