{
  "name": "DeleteDBNodes",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request. The client generates this value, which must be unique for each request. The token can contain only ASCII characters and cannot exceed 64 characters in length. The token is case-sensitive.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "CloudProvider",
      "description": "The service provider of the node.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DBClusterId",
      "description": "The cluster ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "DBNodeId",
      "description": "The IDs of the nodes to be deleted.\n\n> Call the [DescribeDBClusters](~~185342~~) operation to query the details of all clusters in your account, including node IDs.",
      "position": "Query",
      "type": "RepeatList",
      "required": true
    },
    {
      "name": "DBNodeType",
      "description": "The node type. Valid values:\n\n- RO\n\n- STANDBY\n\n- DLNode",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
