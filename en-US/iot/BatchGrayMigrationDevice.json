{
  "name": "BatchGrayMigrationDevice",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DeviceNames",
      "description": "The list of device names to be migrated through canary release.\n\nYou can specify up to 10 device names in a single call. Device names that exceed the limit of 10 are discarded.",
      "position": "Body",
      "type": "RepeatList",
      "required": true
    },
    {
      "name": "ProductKey",
      "description": "The **ProductKey** of the product to which the device belongs.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
