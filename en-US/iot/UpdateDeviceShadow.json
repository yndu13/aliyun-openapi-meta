{
  "name": "UpdateDeviceShadow",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DeltaUpdate",
      "description": "Specifies whether to incrementally update the **desired** parameter of the device shadow.\n\n- **true**: incrementally updates the parameter.\n\n- **false** (default): fully updates the parameter.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "DeviceName",
      "description": "The device name of the device whose shadow information you want to modify.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "IotInstanceId",
      "description": "The instance ID. You can view the **ID** of the current instance on the **Overview** page in the IoT Platform console.\n\n><notice>\n- If this **ID** value exists, you must specify it. Otherwise, the call fails.\n- If the **Overview** page or **ID** value is not available, you do not need to specify this parameter.\n\n></notice>\n\nFor more information about instances, see [Overview](~~356505~~).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ProductKey",
      "description": "The ProductKey of the product to which the device whose shadow information you want to modify belongs.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ShadowMessage",
      "description": "The modified device shadow information.\nThe shadow information contains the following parameters:\n- **method**: String. The operation type. Set the value to **update**.\n\n- **state**: String. The specific state sent to the device shadow. The **desired** parameter specifies the desired property value of the device shadow.\n\n- **version**: Long. The version of the device shadow.\n    - If version control is not required, set this value to 0.\n    - If version control is required, this value must be greater than the current shadow version.\n\n\n><notice>When version control is required, do not directly query the current shadow version from IoT Platform. This is because when update and query operations have high concurrency, multiple update operations may query the same shadow version. In this case, the last initiated update operation may complete first, which causes the final updated shadow information to not be the latest. We recommend that you maintain the shadow version locally and obtain the latest shadow version from your local application when calling this operation.\n></notice>",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
