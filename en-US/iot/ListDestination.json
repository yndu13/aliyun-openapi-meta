{
  "name": "ListDestination",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "IotInstanceId",
      "description": "The instance ID. You can view the **ID** of the current instance on the **Overview** page in the IoT Platform console.\n\n><notice>\n- If this parameter is available, you must specify this parameter. Otherwise, the call fails.\n- If the **Overview** page or the ID is not available, you do not need to specify this parameter.\n\n></notice>\n\nFor more information about instances, see [Overview](~~356505~~).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Page",
      "description": "The page number to display in the response. Valid values: 1 to 100.",
      "position": "Query",
      "type": "Integer",
      "required": true
    },
    {
      "name": "PageSize",
      "description": "The number of records to display on each page in the response. Maximum value: 100.",
      "position": "Query",
      "type": "Integer",
      "required": true
    },
    {
      "name": "SearchName",
      "description": "A string contained in the data destination name. This parameter is used to perform a fuzzy match on data destination names.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Types",
      "description": "The list of data destination operation types. Valid values:\n\n- **REPUBLISH**: forwards the topic data processed by the parse script to another communication topic on IoT Platform.\n- **AMQP**: forwards the topic data processed by the parse script to an AMQP consumer group through data forwarding.\n- **DATAHUB**: forwards the topic data processed by the parse script to Alibaba Cloud DataHub for data stream processing.\n- **ONS**: forwards the topic data processed by the parse script to ApsaraMQ for RocketMQ for message distribution.\n- **MNS**: sends the topic data processed by the parse script to Alibaba Cloud Message Service for message transmission.\n- **FC**: sends the topic data processed by the parse script to Alibaba Cloud Function Compute for event computing.\n- **OTS**: sends the topic data processed by the parse script to Alibaba Cloud Tablestore for NoSQL data storage.",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    }
  ]
}
