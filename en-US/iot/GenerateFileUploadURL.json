{
  "name": "GenerateFileUploadURL",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BizCode",
      "description": "The business scenario code to which the file belongs.\n\nFor device task files, use **DEVICE_JOB_FILE**.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "FileName",
      "description": "The file name. The name cannot contain hyphens (-), periods (.), asterisks (*), forward slashes (/), number signs (#), percent signs (%), ampersands (&), at signs (@), or backslashes (\\). The name cannot exceed 64 characters in length.\n\nIf you do not specify this parameter, the system automatically generates a random string as the file name.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "FileSuffix",
      "description": "The file name extension. Valid values: **bin**, **apk**, **tar**, **gz**, **tar.gz**, **zip**, and **gzip**.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "IotInstanceId",
      "description": "The instance ID.\n\nYou can view the **ID** of the current instance on the **Overview** page in the IoT Platform console. If the ID value exists, you must pass this ID value. Otherwise, the call fails.\n\n> If the public instance does not have an ID value, see [How to obtain an instance ID](~~267533~~).",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
