{
  "name": "PubBroadcast",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "IotInstanceId",
      "description": "The instance ID. You can view the **ID** of the current instance on the **Overview** page in the IoT Platform console.\n\n><notice>\n- If this parameter is available, you must specify this parameter. Otherwise, the call fails.\n- If <props=\"intl\"><ph>the **Overview** page or </ph></props>this parameter is not available, you do not need to specify this parameter.\n\n></notice>\n\nFor more information about instances, see [Overview](~~356505~~).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "MessageContent",
      "description": "The message body to send. The maximum message size is 64 KB.\n\nYou must convert the original message to binary data and then perform Base64 encoding to generate the message body.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ProductKey",
      "description": "The **ProductKey** of the product to which you want to send the broadcast message.\n\n**ProductKey** is the globally unique identifier issued by IoT Platform for a product. You can view the information of all products under the current account in the IoT Platform console or by calling [QueryProductList](~~69271~~).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "TopicFullName",
      "description": "Optional parameter:\n\n- If this parameter is not specified, a full online device broadcast is performed. The message is pushed to all online devices of the specified **ProductKey**. The broadcast topic received by the device is in the format of `/sys/${productKey}/${deviceName}/broadcast/request/${MessageId}`, where **MessageId** is generated by IoT Platform.\n- If this parameter is specified, a topic-subscription-based broadcast is performed. The message is pushed to online devices of the specified **ProductKey** that have subscribed to the broadcast topic. Set this parameter to the full name of the topic that receives the broadcast message, in the format of `/broadcast/${productKey}/custom field`. In this format, **${productKey}** is the **ProductKey** of the product that receives the broadcast message, and you can specify any value for the custom field.\n\n> - Broadcast topics are defined during device development and do not need to be created in the console.\n- A broadcast topic can be subscribed to by a maximum of 1,000 devices. If you have more devices, you can group them. For example, if you have 5,000 devices, you can divide them into 5 groups of 1,000 devices each. You need to call the broadcast topic 5 times with the custom field set to group1/2/3/4/5 respectively, and then have each group of devices subscribe to their respective broadcast topic.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
