{
  "name": "BatchGetEdgeDriver",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DriverIds",
      "description": "The list of driver IDs. You can specify up to 30 driver IDs, which means you can query information about up to 30 drivers at a time. For the format, see the request example in this topic.",
      "position": "Query",
      "type": "RepeatList",
      "required": true
    },
    {
      "name": "IotInstanceId",
      "description": "The instance ID. Leave this parameter empty for public instances. Specify this parameter for Enterprise instances.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
