{
  "name": "GetTrailStatus",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "IsOrganizationTrail",
      "description": "Specifies whether to query the status of a multi-account trail. Valid values:  \n\n- true: queries the status of a multi-account trail.\n\n- false (default): queries the status of a single-account trail.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "Name",
      "description": "The name of the trail.  \nThe name must be 6 to 36 characters in length, must start with a lowercase letter, and can contain lowercase letters, digits, hyphens (-), and underscores (_).  \n\n> Trail names must be unique within an account.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
