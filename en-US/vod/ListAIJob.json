{
  "name": "ListAIJob",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "JobIds",
      "description": "The list of job IDs. You can obtain the JobId from the PlayInfo struct returned by the [GetPlayInfo](~~56124~~) operation.\n\n> Separate multiple IDs with commas (,). You can specify up to 10 job IDs at a time.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
