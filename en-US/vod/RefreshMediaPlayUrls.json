{
  "name": "RefreshMediaPlayUrls",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Definitions",
      "description": "Specifies the definitions of the streams that you want to purge or prefetch. You can specify multiple definitions. Separate multiple definitions with commas (,). If you do not specify this parameter, **streams in all definitions are purged or prefetched by default**.\n> The value must be one of the values defined in **Definition** in [Metric description for media assets](~~124671~~).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Formats",
      "description": "The streaming formats that you want to refresh or prefetch. You can specify multiple formats. Separate multiple formats with commas (,). If you do not specify this parameter, **streams in all formats are refreshed or prefetched by default**. Valid values:\n- **mp4**\n- **m3u8**\n- **mp3**\n- **flv**\n- **webm**\n- **ts**",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "MediaIds",
      "description": "The IDs of the audio or video files that you want to refresh or prefetch. You can specify one or more IDs. Separate multiple IDs with commas (,). You can specify up to 20 IDs.\nYou can obtain audio or video IDs by using the following methods:\n- For audio or video files uploaded through the console, log on to the [ApsaraVideo VOD console](https://vod.console.aliyun.com) and choose **Media Files** > **Audio/Video** to view the audio or video ID.\n- When you call the [CreateUploadVideo](~~55407~~) operation to obtain the upload URL and credential, the audio or video ID is the value of the VideoId response parameter.\n- After the audio or video file is uploaded, you can call the [SearchMedia](~~86044~~) operation to query the audio or video ID, which is the value of the VideoId response parameter.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ResultType",
      "description": "The result type of the refresh or prefetch task. Valid values:\n- **Single** (default): Only the latest transcoded stream for each definition and format is refreshed or prefetched.\n- **Multiple**: All transcoded streams for each definition and format are refreshed or prefetched.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "SliceCount",
      "description": "The number of TS file playback URLs to refresh or prefetch for M3U8 streams. Only the first N TS file playback URLs of each M3U8 stream are refreshed or prefetched. Valid values: 1 to 20. **Default value: 5**.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "SliceFlag",
      "description": "Specifies whether to refresh or prefetch the playback URLs of TS files in M3U8 streams. Valid values:\n- **false** (default): No.\n- **true**: Yes.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "StreamType",
      "description": "The types of the streams that you want to refresh or prefetch. You can specify multiple stream types. Separate multiple stream types with commas (,). If you do not specify this parameter, **all stream types are refreshed or prefetched by default**. Valid values:\n- **video**: video.\n- **audio**: audio.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "TaskType",
      "description": "The type of the task. Valid values:\n- **Refresh**: purge.\n- **Preload**: prefetch.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "UserData",
      "description": "The custom settings. The value is a JSON string that supports settings such as message callbacks and upload acceleration. For more information, see [UserData](~~86952~~).\n> - To use message callbacks in this parameter, configure an HTTP callback URL and select the corresponding callback event types in the console. Otherwise, the callback settings do not take effect. For information about how to configure HTTP callbacks in the console, see [Callback settings](~~86071~~).\n> - To use the upload acceleration feature, submit a ticket to activate it. For more information, see [Upload instructions](~~55396~~). For information about how to submit a ticket, see [Contact us](~~464625~~).",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
