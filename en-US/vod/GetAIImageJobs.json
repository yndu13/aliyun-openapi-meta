{
  "name": "GetAIImageJobs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "JobIds",
      "description": "The IDs of AI image processing tasks. The value consists of one or more job IDs. A job ID is the value of the JobId parameter returned by the [SubmitAIImageJob](~~186922~~) operation.\n- A maximum of 10 IDs are supported.\n- Separate multiple IDs with commas (,).",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
