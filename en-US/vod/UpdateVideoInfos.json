{
  "name": "UpdateVideoInfos",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "UpdateContent",
      "description": "The update content. You can modify the information about up to 20 audio and video files at a time. Separate multiple audio and video object information entries with commas (,). If you specify more than 20 objects, the update is failed and the `CountExceededMax` error is returned.\nThe value is a JSON character string. For more details about the parameters, see the **UpdateContent** table below.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
