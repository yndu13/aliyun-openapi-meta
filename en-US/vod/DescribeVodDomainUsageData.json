{
  "name": "DescribeVodDomainUsageData",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Area",
      "description": "The region code. Default value: CN (the Chinese mainland). Valid values:\n- **CN**: the Chinese mainland.\n- **OverSeas**: outside the Chinese mainland.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DomainName",
      "description": "The accelerated domain name. If this parameter is left empty, the merged data of all accelerated domain names is returned by default. Batch queries are supported. Separate multiple domain names with commas (,).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "EndTime",
      "description": "The end of the time range to query. The end time must be later than the start time. Specify the time in the <i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z format (UTC).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Field",
      "description": "The data type. Valid values:\n- **bps**: bandwidth.\n- **traf**: traffic.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Interval",
      "description": "Forces the retrieval of data at the specified time granularity, in seconds. Valid values: **300** (5 minutes), **3600** (1 hour), and **86400** (1 day).\n- **Interval**=**300**: You can query data for up to the last half year. The maximum time span for a single query is 3 days.\n- **Interval**=**3600** or **86400**: You can query data for up to the last year.\n- If **Interval** is not specified: The maximum time span for a single query is 1 month. If the query time range is 1 to 3 days, data is returned at hourly granularity. If the query time range is 4 days or more, data is returned at daily granularity.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "StartTime",
      "description": "The beginning of the time range to query. Specify the time in the <i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z format (UTC).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Type",
      "description": "The type of usage data to retrieve. Valid values:\n\n - **static**: static content.\n- **dynamic**: dynamic content.\n- **all**: all content.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
