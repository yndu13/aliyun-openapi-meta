{
  "name": "CancelUrlUploadJobs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "JobIds",
      "description": "The list of task IDs. You can obtain the task ID (JobId) from the PlayInfo struct returned by the [GetPlayInfo](~~56124~~) operation.\n- A maximum of 10 IDs are supported.\n- Separate multiple IDs with commas (,).\n\n> You must specify either JobIds or UploadUrls. If both are specified, only JobIds is processed.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "UploadUrls",
      "description": "The list of source video upload URLs. Separate multiple URLs with commas (,). A maximum of 10 URLs are supported.\n\n> - URL-encode the URLs before use.\n> - You must specify either JobIds or UploadUrls. If both are specified, only JobIds is processed.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
