{
  "name": "GetURLUploadInfos",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "JobIds",
      "description": "The list of upload task IDs (JobId). The list consists of one or more JobId values. A JobId is the value of the JobId parameter returned when you call the [UploadMediaByURL](~~86311~~) operation.\n- A maximum of 10 IDs are supported.\n- Separate multiple IDs with commas (,).\n\n> You must specify either JobIds or UploadURLs. If both are specified, only JobIds is processed.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "UploadURLs",
      "description": "The list of source video file URLs. Separate multiple URLs with commas (,). A maximum of 10 URLs are supported.\n> - URL-encode the URLs before use.\n> - If the same URL video is uploaded multiple times, pass in a single URL for the query.\n> - You must specify either JobIds or UploadURLs. If both are specified, only JobIds is processed.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
