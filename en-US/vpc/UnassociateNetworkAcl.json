{
  "name": "UnassociateNetworkAcl",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request.\n\nThe client generates the value. The value must be unique among different requests and cannot exceed 64 ASCII characters in length.\n\n> If you do not specify this parameter, the system uses the **RequestId** of the API request as the **ClientToken**. The **RequestId** may be different for each API request.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform a dry run. Valid values:\n* **true**: performs a dry run. The system checks the request for potential issues without performing the actual operation.\n* **false** (default): performs the actual request.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "NetworkAclId",
      "description": "The ID of the network ACL to disassociate from the associated resources.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region ID of the network ACL.\n\nYou can call [DescribeRegions](~~36063~~) to query the most recent region list.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Resource",
      "description": "The information about the associated resources.",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "ResourceId",
          "description": "The ID of the associated resource to disassociate.",
          "position": "Query",
          "type": "String",
          "required": true
        },
        {
          "name": "ResourceType",
          "description": "The type of the associated resource to disassociate. Set the value to **VSwitch**.\n\nValid values of **N**: 0 to 29. You can disassociate up to 30 resources at a time.",
          "position": "Query",
          "type": "String",
          "required": true
        }
      ]
    }
  ]
}
