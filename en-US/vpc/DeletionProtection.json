{
  "name": "DeletionProtection",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** of the API request as the **ClientToken**. The **RequestId** may be different for each API request.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "InstanceId",
      "description": "The ID of the instance for which you want to set deletion protection.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ProtectionEnable",
      "description": "Specifies whether to enable deletion protection. Valid values:\n\n- **true**: Enabled.\n\n- **false**: Disabled.",
      "position": "Query",
      "type": "Boolean",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region ID of the instance for which you want to set deletion protection. You can call [DescribeRegions](~~36063~~) to query the most recent region list.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Type",
      "description": "The type of instance for which you want to set deletion protection. Valid values:\n\n- **EIP**: elastic IP address.\n\n- **CBWP**: Internet Shared Bandwidth.\n\n- **NATGW**: NAT gateway.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
