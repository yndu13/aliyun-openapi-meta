{
  "name": "AddCommonBandwidthPackageIp",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BandwidthPackageId",
      "description": "The ID of the Internet Shared Bandwidth instance.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The ClientToken value can contain only ASCII characters.\n\n> If you do not specify this parameter, the system uses the **RequestId** of the API request as the **ClientToken**. The **RequestId** may be different for each API request.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform a dry run without actually executing the operation.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "IpInstanceId",
      "description": "The ID of the EIP instance. \n\nYou can call [DescribeEipAddresses](~~36018~~) to query the ID of the EIP instance.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "IpType",
      "description": "The IP type. Valid values: **EIP**, which indicates that an EIP is added to the Internet Shared Bandwidth instance.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID of the Internet Shared Bandwidth instance. \n\nYou can call [DescribeRegions](~~36063~~) to query the region ID.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
