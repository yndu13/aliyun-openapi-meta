{
  "name": "UnassociatePhysicalConnectionFromVirtualBorderRouter",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The ClientToken value can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** as the **ClientToken**. The **RequestId** of each API request may be different.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PhysicalConnectionId",
      "description": "The ID of the Express Connect circuit.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region ID of the Express Connect circuit.\n\nYou can call [DescribeRegions](~~36063~~) to query the region ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "VbrId",
      "description": "The instance ID of the VBR to disassociate from the Express Connect circuit.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
