{
  "name": "CreateTrafficMirrorFilter",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** value as the **ClientToken** value. The **RequestId** value is different for each API request.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform a dry run. Valid values:\n\n- **true**: performs a dry run. The system checks the required parameters, request format, and limits. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n\n- **false** (default): sends a normal request. If the request passes the dry run, a 2xx HTTP status code is returned and the traffic mirror filter is created.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "EgressRules",
      "description": "The information about outbound rules.",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "Action",
          "description": "The collection policy of the outbound rule. Valid values:\n\n- **accept**: collects network traffic.\n- **drop**: does not collect network traffic.",
          "position": "Query",
          "type": "String",
          "required": true
        },
        {
          "name": "DestinationCidrBlock",
          "description": "The destination CIDR block of network traffic for the outbound rule.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "DestinationPortRange",
          "description": "The destination port range of network traffic for the outbound rule. The port range is **1** to **65535**. Separate the start port and end port with a forward slash (/). Example: **1/200** or **80/80**. The value **-1/-1** cannot be set independently and indicates that all ports are available.\n\n> If **EgressRules.N.Protocol** is set to **ALL** or **ICMP**, you do not need to set this parameter, which indicates that all ports are available.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "IpVersion",
          "description": "The IP version of the instance. Valid values:\n\n- **IPv4**: IPv4.\n- **IPv6**: IPv6.",
          "position": "Query",
          "type": "String",
          "required": true
        },
        {
          "name": "Priority",
          "description": "The priority of the outbound rule. A smaller value indicates a higher priority.\nThe maximum value of **N** is **10**, which indicates that you can configure up to 10 outbound rules for a traffic mirror filter.",
          "position": "Query",
          "type": "Integer",
          "required": false
        },
        {
          "name": "Protocol",
          "description": "The protocol type of network traffic to be mirrored for the outbound rule. Valid values:\n\n- **ALL**: all protocols.\n- **ICMP**: Internet Control Message Protocol.\n- **TCP**: Transmission Control Protocol.\n- **UDP**: User Datagram Protocol.",
          "position": "Query",
          "type": "String",
          "required": true
        },
        {
          "name": "SourceCidrBlock",
          "description": "The source CIDR block of network traffic for the outbound rule.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "SourcePortRange",
          "description": "The source port range of network traffic for the outbound rule. The port range is **1** to **65535**. Separate the start port and end port with a forward slash (/). Example: **1/200** or **80/80**. The value **-1/-1** cannot be set independently and indicates that all ports are available.\n\n> If **EgressRules.N.Protocol** is set to **ALL** or **ICMP**, you do not need to set this parameter, which indicates that all ports are available.",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    },
    {
      "name": "IngressRules",
      "description": "The information about inbound rules.",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "Action",
          "description": "The collection policy of the inbound rule. Valid values:\n\n- **accept**: collects network traffic.\n- **drop**: does not collect network traffic.",
          "position": "Query",
          "type": "String",
          "required": true
        },
        {
          "name": "DestinationCidrBlock",
          "description": "The destination CIDR block of network traffic for the inbound rule.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "DestinationPortRange",
          "description": "The destination port range of network traffic for the inbound rule. The port range is **1** to **65535**. Separate the start port and end port with a forward slash (/). Example: **1/200** or **80/80**.\n\n> If **IngressRules.N.Protocol** is set to **ALL** or **ICMP**, you do not need to set this parameter, which indicates that all ports are available.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "IpVersion",
          "description": "The IP version of the instance. Valid values:\n\n- **IPv4**: IPv4.\n- **IPv6**: IPv6.",
          "position": "Query",
          "type": "String",
          "required": true
        },
        {
          "name": "Priority",
          "description": "The priority of the inbound rule. A smaller value indicates a higher priority.\nThe maximum value of **N** is **10**, which indicates that you can configure up to 10 inbound rules for a traffic mirror filter.",
          "position": "Query",
          "type": "Integer",
          "required": false
        },
        {
          "name": "Protocol",
          "description": "The protocol type of network traffic to be mirrored for the inbound rule. Valid values:\n\n- **ALL**: all protocols.\n- **ICMP**: Internet Control Message Protocol.\n- **TCP**: Transmission Control Protocol.\n- **UDP**: User Datagram Protocol.",
          "position": "Query",
          "type": "String",
          "required": true
        },
        {
          "name": "SourceCidrBlock",
          "description": "The source CIDR block of network traffic for the inbound rule.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "SourcePortRange",
          "description": "The source port range of network traffic for the inbound rule. The port range is **1** to **65535**. Separate the start port and end port with a forward slash (/). Example: **1/200** or **80/80**.\n\n> If **IngressRules.N.Protocol** is set to **ALL** or **ICMP**, you do not need to set this parameter, which indicates that all ports are available.",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    },
    {
      "name": "RegionId",
      "description": "The region ID of the traffic mirror filter.\n\nYou can call [DescribeRegions](~~36063~~) to query the most recent region list. For more information about regions that support traffic mirroring, see [Traffic mirroring overview](~~207513~~).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ResourceGroupId",
      "description": "The ID of the resource group to which the traffic mirroring filter belongs.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Tag",
      "description": "The tags of the resource.",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "Key",
          "description": "The tag key of the resource. You can specify up to 20 tag keys. The tag key cannot be an empty string.\n\nThe tag key can be up to 128 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "Value",
          "description": "The tag value of the resource. You can specify up to 20 tag values. The tag value can be an empty string.\n\nThe tag value can be up to 128 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`.",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    },
    {
      "name": "TrafficMirrorFilterDescription",
      "description": "The description of the traffic mirror filter.\n\nThe description must be 1 to 256 characters in length and cannot start with `http://` or `https://`.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "TrafficMirrorFilterName",
      "description": "The name of the traffic mirror filter.\n\nThe name must be 1 to 128 characters in length and cannot start with `http://` or `https://`.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
