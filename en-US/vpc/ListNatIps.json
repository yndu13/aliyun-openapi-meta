{
  "name": "ListNatIps",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the value, but you must make sure that the value is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length.\n\n> If you do not specify this parameter, the system uses **RequestId** as **ClientToken**. The value of **RequestId** may differ for each API request.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform a dry run. Valid values:\n- **true**: performs a dry run. The system checks the required parameters, request syntax, and limits. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n- **false** (default): performs a dry run and sends the request. If the request passes the dry run, an HTTP 2xx status code is returned and the operation is performed.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "IpOrigin",
      "description": "The origin of the NAT IP address to query. Valid values:\n- prefix: a NAT IP address that belongs to an IP prefix.\n\n- cidr: a standalone NAT IP address that does not belong to any IP prefix.\n\n- Empty: queries all NAT IP addresses.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Ipv4Prefix",
      "description": "The CIDR block of the IP prefix to query.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "MaxResults",
      "description": "The number of entries per page for a paged query. Valid values: **1** to **100**. Default value: **20**.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "NatGatewayId",
      "description": "The instance ID of the NAT gateway to which the NAT IP addresses belong.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "NatIpCidr",
      "description": "The CIDR block to which the NAT IP addresses belong.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "NatIpIds",
      "description": "The instance ID of the NAT IP address to query. Valid values of **N**: **1** to **20**.",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    },
    {
      "name": "NatIpName",
      "description": "The name of the NAT IP address to query. Valid values of **N**: **1** to **20**.",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    },
    {
      "name": "NatIpStatus",
      "description": "The status of the NAT IP addresses to query. Valid values:\n\n- **Available**: available.\n- **Deleting**: being deleted.\n- **Creating**: being created.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "NextToken",
      "description": "The pagination token that is used in the next request to retrieve a new page of results. Valid values:\n- You do not need to specify this parameter for the first request or if no subsequent query exists.\n- If a next query exists, set the value to the NextToken value returned in the previous API call.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID of the NAT gateway instance to which the NAT IP addresses belong.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the most recent region list.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
