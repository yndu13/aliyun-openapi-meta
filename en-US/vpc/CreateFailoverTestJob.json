{
  "name": "CreateFailoverTestJob",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request.\n\nGenerate a parameter value from your client to ensure uniqueness across different requests. ClientToken supports only ASCII characters.\n\n> If you do not specify this parameter, the system uses the **RequestId** of the API request as the **ClientToken**. The **RequestId** may be different for each API request.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Description",
      "description": "The description of the failover test job.\n\nThe description must be 0 to 256 characters in length and cannot start with `http://` or `https://`.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform a dry run. Valid values:\n\n- **true**: sends the request without creating the failover test node. The system checks the AccessKey validity, Resource Access Management (RAM) user authorization, and required parameters. If the check fails, the corresponding error is returned. If the check passes, the DryRunOperation error code is returned.\n- **false** (default): sends a Normal request. After the check passes, a 2xx HTTP status code is returned and the failover test job is created.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "JobDuration",
      "description": "The duration of the failover test job. Unit: minutes. Valid values: **1 to 4320**.",
      "position": "Query",
      "type": "Integer",
      "required": true
    },
    {
      "name": "JobType",
      "description": "The failover test job type. Valid values:\n\n- **StartNow**: starts immediately. The test job starts executing immediately after it is created.\n\n- **StartLater**: does not start. Only creates the test job without executing it.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Name",
      "description": "The name of the failover test job.\n\nThe name must be 0 to 128 characters in length and cannot start with `http://` or `https://`.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID of the failover test job.\n\nYou can call [DescribeRegions](~~36063~~) to query region IDs.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ResourceId",
      "description": "The list of test resource IDs. You can add up to 16 test resources.",
      "position": "Query",
      "type": "RepeatList",
      "required": true
    },
    {
      "name": "ResourceType",
      "description": "The type of the test resource. Valid values: **PHYSICALCONNECTION**: Express Connect circuit.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
