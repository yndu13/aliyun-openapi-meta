{
  "name": "ListFullNatEntries",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** as the **ClientToken**. The **RequestId** of each API request may be different.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "FullNatEntryId",
      "description": "The ID of the FULLNAT entry to query.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "FullNatEntryNames",
      "description": "The names of the FULLNAT entries to query. You can specify up to 20 names.\n\nThe name must be 2 to 128 characters in length, and can contain letters, digits, hyphens (-), and underscores (_). The name must start with a letter or Chinese character.",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    },
    {
      "name": "FullNatTableId",
      "description": "The ID of the FULLNAT table to which the FULLNAT entries belong.\n\n> You must specify at least one of **FullNatTableId** and **NatGatewayId**.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "IpProtocol",
      "description": "The protocol type of the Redirection Port. Valid values:\n\n- **TCP**: forwards TCP packets.\n- **UDP**: forwards UDP packets.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "MaxResults",
      "description": "The number of entries per page for a paged query. Valid values: **1** to **100**. Default value: **20**.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "NatGatewayId",
      "description": "The ID of the NAT gateway to query.\n\n> You must specify at least one of **FullNatTableId** and **NatGatewayId**.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "NatIp",
      "description": "The NAT IP address in the FULLNAT entry to query.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "NatIpPort",
      "description": "The frontend port in the FULLNAT entry to query. Valid values: 1 to 65535.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "NetworkInterfaceIds",
      "description": "The IDs of the elastic network interfaces (ENIs) to query.",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    },
    {
      "name": "NextToken",
      "description": "The pagination token that is used in the next request to retrieve a new page of results. Valid values:\n\n- You do not need to specify this parameter for the first request or if no next query exists.\n- If a next query exists, set the value to the **NextToken** value returned in the previous API call.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID of the VPC NAT gateway to which the FULLNAT entries belong.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the most recent region list.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
