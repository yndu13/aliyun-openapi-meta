{
  "name": "ModifyVpcAttribute",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CidrBlock",
      "description": "The new IPv4 CIDR block of the VPC.\n\nYou can expand or shrink the CIDR block within the original IPv4 CIDR block of the VPC. The recommended subnet mask is 16 to 28 bits. If you shrink the IPv4 CIDR block of the VPC and IP addresses that are already in use fall outside the target CIDR block, the modification fails.\n\n> Modifying the IPv4 CIDR block of a VPC does not affect existing services.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Description",
      "description": "The new description of the VPC.\n\nThe description must be 1 to 256 characters in length and cannot start with `http://` or `https://`.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "EnableDnsHostname",
      "description": "Specifies whether to enable the DNS hostname feature. Valid values:\n\n- **false** (default): Disabled.\n- **true**: Enabled.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "EnableIPv6",
      "description": "Specifies whether to enable IPv6. Valid values:\n- **false** (default): Disabled.\n- **true**: Enabled.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "Ipv6CidrBlock",
      "description": "The IPv6 CIDR block of the VPC.\nWhen you enable IPv6 for a VPC, the system will assign an IPv6 CIDR block. To specify an IPv6 CIDR block, you need to first invoke the [AllocateVpcIpv6Cidr](~~448916~~) operation to reserve the specified IPv6 CIDR block, and then pass it in.\n> For a VPC that already has IPv6 enabled, you cannot modify the IPv6 CIDR block by passing in this parameter.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Ipv6Isp",
      "description": "The type of the IPv6 CIDR block of the VPC. Valid values:\n\n- **BGP** (default): Alibaba Cloud BGP IPv6.\n- **ChinaMobile**: China Mobile (single-ISP).\n- **ChinaUnicom**: China Unicom (single-ISP).\n- **ChinaTelecom**: China Telecom (single-ISP).\n\n> If you are a user who has the single-ISP bandwidth whitelist enabled, you can set this parameter to **ChinaTelecom** (China Telecom), **ChinaUnicom** (China Unicom), or **ChinaMobile** (China Mobile).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID of the VPC.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the most recent region list.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "VpcId",
      "description": "The ID of the VPC that you want to modify.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "VpcName",
      "description": "The new name of the VPC.\n\nThe name must be 1 to 128 characters in length and cannot start with `http://` or `https://`.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
