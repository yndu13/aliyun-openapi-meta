{
  "name": "CreateIPv6Translator",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoPay",
      "description": "Specifies whether automatic payment is enabled for the subscription bill. Valid values: **true|false**.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "Bandwidth",
      "description": "The billing bandwidth of the IPv6 Translation Service instance, in Mbit/s. Valid values: **1** to **200**. If you do not set the bandwidth for translation mapping entries, the mapping entries in the instance share this bandwidth. \n\n> If you do not specify the bandwidth, the default value is 10 Mbit/s.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request. The client token must be unique among different requests and cannot exceed 64 ASCII characters in length.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Duration",
      "description": "The subscription duration. Valid values:\n\n- If the billing cycle is **Month**, valid values are **1** to **9**.  \n- If the billing cycle is **Year**, the value is **3**.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "Name",
      "description": "The name of the IPv6 Translation Service instance. The name defaults to the instance ID. The name must be 2 to 100 characters in length and must start with a letter or a Chinese character. It can contain digits, periods (.), underscores (_), and hyphens (-), but cannot start with `http://` or `https://`.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PayType",
      "description": "The payment method of the IPv6 Translation Service instance. Valid values: \n\n- **PREPAY**: subscription.\n\n- **POSTPAY**: pay-as-you-go.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PricingCycle",
      "description": "The billing cycle for subscription. Valid values: \n\n- **Month** (default): monthly subscription.\n\n- **Year**: yearly subscription.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region of the IPv6 Translation Service instance. You can call the [DescribeRegions](~~36063~~) operation to query region IDs.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Spec",
      "description": "The specification of the IPv6 Translation Service instance. Valid values: **small**.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
