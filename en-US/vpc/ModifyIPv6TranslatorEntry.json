{
  "name": "ModifyIPv6TranslatorEntry",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AclId",
      "description": "The ID of the associated access control policy group.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "AclStatus",
      "description": "Specifies whether to enable access control. Valid values:\n- **on**: enabled.\n- **off**: disabled.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "AclType",
      "description": "The type of the access control policy. Valid values:\n\n- **white**: allows the IPv6 addresses in the access control policy group to access backend services.\n\n- **black**: denies the IPv6 addresses in the access control policy group from accessing backend services.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "AllocateIpv6Port",
      "description": "The port used by the IPv6 address allocated by the IPv6 Translation Service instance.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "BackendIpv4Addr",
      "description": "The public IPv4 address that needs to provide IPv6 services. This is the IPv4 address of the IPv4-only server.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "BackendIpv4Port",
      "description": "The port of the public IPv4 address that needs to provide IPv6 services.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "EntryBandwidth",
      "description": "The maximum bandwidth of the IPv6 Translation Service mapping entry. Unit: Mbit/s. Valid values: \n\n- **-1** (default): does not limit the maximum bandwidth of the mapping entry.  \n\n- **1** to **200**: the bandwidth value of the mapping entry.\n\n> The sum of the maximum bandwidth values of all IPv6 Translation Service mapping entries cannot exceed the maximum bandwidth of the instance.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "EntryDescription",
      "description": "The description of the IPv6 Translation Service mapping entry. The description must be 2 to 100 characters in length and must start with a letter or a Chinese character. It can contain digits, periods (.), underscores (_), and hyphens (-). It cannot start with http:// or https://.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "EntryName",
      "description": "The name of the IPv6 Translation Service mapping entry. The name must be 2 to 100 characters in length and must start with a letter or a Chinese character. It can contain digits, periods (.), underscores (_), and hyphens (-). It cannot start with http:// or https://.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Ipv6TranslatorEntryId",
      "description": "The ID of the IPv6 Translation Service mapping entry.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region of the IPv6 Translation Service instance. You can call the DescribeRegions operation to query region IDs.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "TransProtocol",
      "description": "The protocol type. Valid values:\n\n- **tcp**: forwards TCP packets.  \n- **udp**: forwards UDP packets.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
