{
  "name": "ModifyRouterInterfaceAttribute",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DeleteHealthCheckIp",
      "description": "Specifies whether to delete the health check IP addresses configured on the router interface. Valid values:\n\n- **true**: Deletes the health check IP addresses.\n \n- **false** (default): Does not delete the health check IP addresses.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "Description",
      "description": "The description of the router interface. \n\nThe description must be 2 to 256 characters in length and must start with a letter or a Chinese character. It cannot start with `http://` or `https://`.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "HcRate",
      "description": "The health check rate. Unit: milliseconds. Recommended value: **2000**. This parameter specifies the interval between consecutive probe packets sent during a health check.\n\nIn this example, **HcThreshold** is set to **8** and **HcRate** is set to **2000**. This means that during a health check, a probe packet is sent from **HealthCheckSourceIp** (the source IP address for health checks) to **HealthCheckTargetIp** (the destination IP address for health checks) every 2000 milliseconds. If 8 consecutive probe packets receive no response, the health check fails.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "HcThreshold",
      "description": "The health check threshold. Unit: packets. Recommended value: **8**. This parameter specifies the number of probe packets sent during a health check.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "HealthCheckSourceIp",
      "description": "The source IP address for health checks. The IP address must be an unused IP address in the local VPC.\n\n> You can specify this parameter in Express Connect circuit scenarios.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "HealthCheckTargetIp",
      "description": "The destination IP address for health checks. \n\n> This parameter is required if **HealthCheckSourceIp** is specified.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Name",
      "description": "The name of the router interface.\n\nThe name must be 2 to 128 characters in length and must start with a letter or a Chinese character. It can contain digits, underscores (_), and hyphens (-).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "OppositeInterfaceId",
      "description": "The ID of the peer router interface.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "OppositeInterfaceOwnerId",
      "description": "The ID of the account to which the peer router interface belongs.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "OppositeRouterId",
      "description": "The ID of the peer router.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "OppositeRouterType",
      "description": "The type of the router to which the peer router interface belongs. Valid values:\n\n- **VRouter**: vRouter.\n\n- **VBR** (default): Virtual Border Router.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID of the router interface.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query region IDs.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RouterInterfaceId",
      "description": "The ID of the router interface.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
