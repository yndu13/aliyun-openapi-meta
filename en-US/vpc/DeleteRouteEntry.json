{
  "name": "DeleteRouteEntry",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DestinationCidrBlock",
      "description": "The destination CIDR block of the route. IPv4 CIDR blocks, IPv6 CIDR blocks, prefix list CIDR blocks, and prefix list instance IDs are supported. This parameter is mutually exclusive with the RouteEntryId parameter.\n> If the **RouteEntryId** parameter is not specified, the **DestinationCidrBlock** and **RouteTableId** parameters are required. Configure the **NextHopId** or **NextHopList** parameter as needed.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform a dry run. Valid values:\n\n**true**: performs a dry run without deleting the route. The system checks the AccessKey pair, the authorization of the Resource Access Management (RAM) user, and the required parameters. If the check fails, the corresponding error is returned. If the check succeeds, the error code `DryRunOperation` is returned.\n\n**false** (default): sends a Normal request. If the check succeeds, a 2xx HTTP status code is returned and the route entry is deleted.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "NextHopId",
      "description": "The ID of the next hop instance.\n\n- To delete a non-ECMP route, specify **NextHopId**. You do not need to specify **NextHopList**.\n- To delete an ECMP route, specify **NextHopList**. You do not need to specify **NextHopId**.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "NextHopList",
      "description": "The information about the next hop instances of the ECMP route. A maximum of 16 next hop instances are supported.",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "NextHopId",
          "description": "The ID of the next hop instance of the ECMP route. A maximum of 16 next hop instances are supported.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "NextHopType",
          "description": "The type of the next hop of the ECMP route. Set the value to **RouterInterface** (router interface). A maximum of 16 next hop instances are supported.",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    },
    {
      "name": "RegionId",
      "description": "The ID of the region where the route table resides.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the region ID.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RouteEntryId",
      "description": "The ID of the route that you want to delete. This parameter is mutually exclusive with the DestinationCidrBlock parameter.\n> If the **DestinationCidrBlock** parameter is not specified, the **RouteEntryId** parameter is required.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RouteTableId",
      "description": "The ID of the route table that contains the route.  \n> If the **RouteEntryId** parameter is not specified, the **DestinationCidrBlock** and **RouteTableId** parameters are required. Configure the **NextHopId** or **NextHopList** parameter as needed.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
