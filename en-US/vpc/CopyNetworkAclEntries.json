{
  "name": "CopyNetworkAclEntries",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The ClientToken value can contain only ASCII characters.\n\n> If you do not specify this parameter, the system uses the **RequestId** of the API request as the **ClientToken**. The **RequestId** may be different for each API request.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform a dry run. Valid values:\n* **true**: performs a dry run.\n* **false** (default): sends the request.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "NetworkAclId",
      "description": "The ID of the network ACL.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region where the network ACL resides. You can call the [DescribeRegions](~~36063~~) operation to query the region ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "SourceNetworkAclId",
      "description": "The ID of the source network ACL from which rules are copied.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
