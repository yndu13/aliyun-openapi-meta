{
  "name": "DescribeNatGateways",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AvailabilityMode",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform a dry run. Valid values:\n\n- **true**: performs a dry run. The system checks the request for potential issues, including invalid AccessKey pairs, unauthorized RAM user authorization, and missing parameter values. If the request fails the dry run, the corresponding error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n\n- **false** (default): performs a dry run and sends the request. After the request passes the dry run, a 2xx HTTP status code is returned and the resource status is queried. This is the Normal request behavior.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "InstanceChargeType",
      "description": "<props=\"china\">The billing method of the NAT gateway instance that you want to query. Valid values:</props>\n\n<props=\"china\">\n- **PostPaid**: pay-as-you-go.</props>\n- **PrePaid**: the legacy subscription billing method. New purchases under the subscription billing method are no longer supported.\n\n</props>\n\n<props=\"intl\">The billing method of the NAT gateway instance that you want to query. Valid value: **PostPaid** (pay-as-you-go).</props>",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Name",
      "description": "The name of the NAT gateway that you want to query.\n\nThe name must be 1 to 128 characters in length and cannot start with `http://` or `https://`.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "NatGatewayId",
      "description": "The ID of the NAT gateway that you want to query.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "NatType",
      "description": "The type of the NAT gateway. Valid value: **Enhanced** (enhanced NAT gateway).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "NetworkType",
      "description": "The type of the NAT gateway that you want to query. Valid values:\n\n- **internet**: Internet NAT gateway.\n- **intranet**: VPC NAT gateway.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PageNumber",
      "description": "The page number. Default value: **1**.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "PageSize",
      "description": "The number of entries per page in a paged query. Maximum value: **50**. Default value: **10**.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID of the NAT gateway that you want to query.\n\nYou can call the [DescribeRegions](~~36063~~) operation to obtain the region ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ResourceGroupId",
      "description": "The ID of the resource group to which the NAT gateway that you want to query belongs.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Spec",
      "description": "<props=\"china\">The specification of the Internet NAT gateway. This parameter is supported only when **InstanceChargeType** is set to **PrePaid** (legacy subscription Internet NAT gateway) to create a NAT gateway with defined specifications. Valid values:</props>\n\n<props=\"china\">\n- **Small** (default): small.\n- **Middle**: medium.\n- **Large**: large.\n\n</props>\n<props=\"intl\">The specification of the NAT gateway. Leave this parameter empty.</props>",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Status",
      "description": "The status of the NAT gateway that you want to query. Valid values:\n- **Creating**: The NAT gateway is being created. Creating a NAT gateway is an asynchronous operation. The NAT gateway remains in the **Creating** state until the operation is complete.\n\n- **Available**: The NAT gateway is available. This is a stable state after the NAT gateway is created.\n\n- **Modifying**: The NAT gateway is being modified. Modifying a NAT gateway is an asynchronous operation. The NAT gateway remains in the **Modifying** state until the operation is complete.\n\n- **Deleting**: The NAT gateway is being deleted. Deleting a NAT gateway is an asynchronous operation. The NAT gateway remains in the **Deleting** state until the operation is complete.\n\n- **Converting**: The NAT gateway is being upgraded from a standard NAT gateway to an enhanced NAT gateway. This is an asynchronous operation. The NAT gateway remains in the **Converting** state until the operation is complete.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Tag",
      "description": "The list of tags.",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "Key",
          "description": "The tag key of the NAT gateway instance. You can specify up to 20 tag keys.\n\nThe tag key can be up to 128 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "Value",
          "description": "The tag value of the NAT gateway instance. You can specify up to 20 tag values.\n\nThe tag value can be up to 128 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`.",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    },
    {
      "name": "VpcId",
      "description": "The ID of the VPC to which the NAT gateway that you want to query belongs.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ZoneId",
      "description": "The zone ID of the NAT gateway.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
