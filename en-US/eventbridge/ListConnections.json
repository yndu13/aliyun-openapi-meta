{
  "name": "ListConnections",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ConnectionNamePrefix",
      "description": "The prefix of the connection configuration name. Supports fuzzy match by prefix.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "MaxResults",
      "description": "The maximum number of entries to return per request. You can use this parameter together with NextToken to implement paging.\n\n- Default value: 10.",
      "position": "Body",
      "type": "Long",
      "required": false
    },
    {
      "name": "NextToken",
      "description": "The pagination token. If the number of results exceeds the value of MaxResults, a NextToken value is returned.\n\n- The NextToken value starts from 0. Default value: 0.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "Type",
      "description": "Filters query results by connection type. Valid values: Http, MySQL, PostgreSQL, Elasticsearch, OSS_TABLES, SLS, OTS, MaxCompute, MongoDB, Redis, SQLServer, ClickHouse, Oracle, Hive, Iceberg, and lakehouse. If this parameter is not specified, all types are returned.",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ]
}
