{
  "name": "UpdatePool",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PoolName",
      "description": "The name of the resource pool.\n- The name can be up to 15 characters in length.\n- The name can contain digits, uppercase letters, lowercase letters, underscores (_), and periods (.).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Priority",
      "description": "The priority of the resource pool.\n\n- Valid values: 1 to 99. Default value: 1, which indicates the lowest priority.\n- Jobs submitted to a resource pool with a higher priority value are scheduled before pending jobs in resource pools with lower priority values. The priority of a resource pool takes precedence over the priority of a job.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "ResourceLimits",
      "description": "The resource quota limit for concurrent usage by a user within a resource pool.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "SchedulingPolicyId",
      "description": "The scheduling policy ID.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
