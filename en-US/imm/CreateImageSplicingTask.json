{
  "name": "CreateImageSplicingTask",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Align",
      "description": "The alignment value, in pixels, for the width or height of the images to be stitched. The value can range from 1 to 4096.\n\n- If you set **Direction** to `vertical`, this parameter specifies the width alignment.\n\n- If you set **Direction** to `horizontal`, this parameter specifies the height alignment.\n\n> If you do not specify this parameter, the width or height of the first image is used for alignment by default.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "BackgroundColor",
      "description": "The fill color for the areas specified by `Padding` and `Margin`. The value can be in the `#FFFFFF` format or a keyword such as `red` or `alpha`.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "CredentialConfig",
      "description": "The chained authorization configuration. For more information, see [Use chained authorization to access resources of other entities](~~465340~~).",
      "position": "Query",
      "type": "Json",
      "required": false
    },
    {
      "name": "Direction",
      "description": "The image stitching method. Valid values:\n\n- vertical (default): Stitches images vertically. The widths of all images must be the same.\n\n- horizontal: Stitches images horizontally. The heights of all images must be the same.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ImageFormat",
      "description": "The compression format of the output image. Valid values:\n\n- jpg (default)\n\n- png\n\n- webp",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Margin",
      "description": "The blank margin, in pixels, of the stitched image. The default value is 0.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "Notification",
      "description": "The message notification configuration. For information about the format of asynchronous notification messages, see [Asynchronous notification message format](~~2743997~~).",
      "position": "Query",
      "type": "Json",
      "required": false
    },
    {
      "name": "Padding",
      "description": "The space, in pixels, between sub-images in the stitched image. The default value is 0.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "ProjectName",
      "description": "The project name. For more information about how to obtain the project name, see [Create a project](~~478153~~).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Quality",
      "description": "The compression quality of the output image. This parameter is valid only for JPG and WebP images. The value range is 0 to 100. The default value is 80.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "ScaleType",
      "description": "The scaling method used when the width or height of an image is aligned. Valid values:\n\n- fit (default): Scales the image without adding black bars. Only proportional scaling is supported.\n\n- stretch: Stretches the image to fill the space.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Sources",
      "description": "The list of input images. The images are stitched in the order of their URIs in the list.",
      "position": "Query",
      "type": "Json",
      "required": true,
      "sub_parameters": [
        {
          "name": "Rotate",
          "description": "The rotation angle of the image. Valid values:\n\n- 0 (default)\n\n- 90\n\n- 180\n\n- 270",
          "position": "Query",
          "type": "Long",
          "required": false
        },
        {
          "name": "URI",
          "description": "The OSS URI of the source image.\n\nThe URI must be in the oss\\://${Bucket}/${Object} format. `${Bucket}` is the name of the OSS bucket that is in the same region as the project. `${Object}` is the full path of the file, including the file name extension.\n\nSupported image formats: JPG and PNG.",
          "position": "Query",
          "type": "String",
          "required": true
        }
      ]
    },
    {
      "name": "Tags",
      "description": "Custom tags used to search for and filter asynchronous tasks.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "TargetURI",
      "description": "The OSS URI where the output image is stored.\n\nThe URI must be in the oss\\://${bucketname}/${objectname} format. ${bucketname} is the name of the OSS bucket that is in the same region as the project. ${objectname} is the path of the file, including the file name.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "UserData",
      "description": "The custom information. This information is returned in the asynchronous notification message. Use this information to associate the notification message with your system. The maximum length is 2,048 bytes.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
