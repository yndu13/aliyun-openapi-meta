{
  "name": "ListConsumerGroupSubscriptions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/instances/[instanceId]/consumerGroups/[consumerGroupId]/subscriptions",
  "parameters": [
    {
      "name": "consumerGroupId",
      "description": "The ID of the consumer group whose subscription relationships you want to query.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "instanceId",
      "description": "The ID of the ApsaraMQ for RocketMQ instance.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "topicName",
      "description": "The name of the topic. If you do not specify this parameter, all subscription relationships are queried.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
