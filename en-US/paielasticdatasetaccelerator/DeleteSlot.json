{
  "name": "DeleteSlot",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/api/v1/slots/[SlotId]",
  "parameters": [
    {
      "name": "Force",
      "description": "Specifies whether to force delete the slot without waiting for it to stop. Valid values:\n- true: The slot is force deleted without waiting for it to stop.\n- false: The slot is not force deleted.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "SlotId",
      "description": "The slot ID. To obtain the slot ID, call [ListSlots](~~467905~~).",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
