{
  "name": "CreateHbaseHaSlb",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BdsId",
      "description": "The ID of the BDS cluster.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the value, but you must make sure that the value is unique among different requests. The value cannot exceed 64 printable ASCII characters in length.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "HaId",
      "description": "The high-availability ID in the BDS active-active management.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "HaTypes",
      "description": "The high-availability type. Valid values:\n\n- thrift\n- phoenix.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "HbaseType",
      "description": "Specifies whether the high-availability type is on the primary or secondary instance. Valid values:\n\n- Active: The high-availability type is on the primary instance.\n- Standby: The high-availability type is on the secondary instance.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
