{
  "name": "UpdateMetaEntity",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Attributes",
      "description": "The entity attributes. Complex values must be serialized into a JSON string.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "Comment",
      "description": "The comment on the entity.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "CustomAttributes",
      "description": "The custom attribute values. Each key specifies a custom attribute, and its value is an array that can contain at most one item. To delete an attribute value, provide an empty array.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "Id",
      "description": "The ID of the entity to update. The entity name, entity type, and parent-child relationship are determined by the ID and cannot be modified using this operation.",
      "position": "Body",
      "type": "String",
      "required": true
    }
  ]
}
