{
  "name": "KillSemanticJob",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ExecutorJobId",
      "description": "The executor job ID of the run to stop. Use the Data.ExecutorJobId value from the RunSemanticJob response or the ExecutorJobId value from a ListSemanticJobRuns record.",
      "position": "Body",
      "type": "String",
      "required": true
    },
    {
      "name": "ProjectId",
      "description": "The ID of the DataWorks workspace to which the job belongs. Use the ProjectId value from the CreateSemanticJob response or a ListSemanticJobs list item.",
      "position": "Body",
      "type": "Long",
      "required": true
    },
    {
      "name": "RetryTimes",
      "description": "The number of retries when sending the stop request to the executor. This parameter is typically optional. If specified, use a non-negative integer. After the call, confirm the final status by calling GetSemanticJobDetail.",
      "position": "Body",
      "type": "Integer",
      "required": false
    }
  ]
}
