{
  "name": "CreateMetaEntityDef",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AttributeDefs",
      "description": "The attribute definition list for custom entities. This parameter cannot be specified when extend is set to TABLE.",
      "position": "Body",
      "type": "Json",
      "required": false
    },
    {
      "name": "Description",
      "description": "The description.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "DisplayName",
      "description": "The display name, up to 32 characters.",
      "position": "Body",
      "type": "String",
      "required": true
    },
    {
      "name": "Extend",
      "description": "The extension mode. Valid values:\n\n- NONE: default value. Indicates a custom entity with freely defined attributes.\n- TABLE: indicates an extended table type. This type integrates in the same way as existing table types in DataWorks Data Map. You do not need to provide attribute definitions and can create corresponding Database/Table objects.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "Name",
      "description": "The name of the type definition. Custom types must match `^[a-z0-9][a-z0-9_]*$`. Extended table types must match `^[a-z0-9][a-z0-9_]*-table$`.",
      "position": "Body",
      "type": "String",
      "required": true
    }
  ]
}
