{
  "name": "CreateDataQualityScan",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The idempotency token.",
      "position": "Body",
      "type": "String",
      "required": true
    },
    {
      "name": "ComputeResource",
      "description": "The compute engine used at runtime. If not specified, the data source defined in the Spec is used.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "Description",
      "description": "The description of the data quality monitor.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "Hooks",
      "description": "The Hook configurations after the data quality monitoring run ends.",
      "position": "Body",
      "type": "Json",
      "required": false,
      "sub_parameters": [
        {
          "name": "Condition",
          "description": "The Hook trigger condition. The hook will run if the condition is met. Currently, only one type of expression syntax is supported:\n\nYou can specify multiple combinations of rule severity levels and validation statuses using an expression such as `results.any { r -> r.status == 'Fail' && r.rule.severity == 'Normal' || r.status == 'Error' && r.rule.severity == 'High' || r.status == 'Warn' && r.rule.severity == 'High' }`. This expression means the condition is met if any executed rule has a result of Fail with severity Normal, Error with severity High, or Warn with severity High. In the condition expression, the values of severity and status are predefined enums. The values of severity must match those defined in the Spec, and the values of status must match those in DataQualityResult.",
          "position": "FormData",
          "type": "String",
          "required": false
        },
        {
          "name": "Type",
          "description": "The type of the Hook.\n\nValid values:\n\n- BlockTaskInstance: Blocks the scheduling of the task instance.",
          "position": "FormData",
          "type": "String",
          "required": false
        }
      ]
    },
    {
      "name": "Name",
      "description": "The data quality monitoring name.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "Owner",
      "description": "The ID of the user who owns of the data quality monitor.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "Parameters",
      "description": "The definition of execution parameters for the data quality monitoring.",
      "position": "Body",
      "type": "Json",
      "required": false,
      "sub_parameters": [
        {
          "name": "Name",
          "description": "The parameter name.",
          "position": "FormData",
          "type": "String",
          "required": false
        },
        {
          "name": "Value",
          "description": "The parameter values.",
          "position": "FormData",
          "type": "String",
          "required": false
        }
      ]
    },
    {
      "name": "ProjectId",
      "description": "The DataWorks workspace ID. You can log on to the DataWorks console and go to the workspace configuration page to obtain the workspace ID. This parameter is required to specify the target DataWorks workspace for this API operation.",
      "position": "Body",
      "type": "Long",
      "required": false
    },
    {
      "name": "RuntimeResource",
      "description": "The resource group used during execution of the data quality monitoring.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "Spec",
      "description": "Spec code for the content of the data quality monitoring.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "Trigger",
      "description": "The trigger configurations of the data quality monitoring task.",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ]
}
