{
  "name": "ListDataQualityRuleTemplates",
  "deprecated": true,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CreationSource",
      "description": "The creation source of the rule template. This parameter is required. Valid values:\n\n- System: system template.\n- UserDefined: user-defined template.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "DirectoryPath",
      "description": "The category directory where the custom template is stored. Levels are separated by forward slashes (/). Each level name can be up to 1024 characters in length and cannot contain whitespace characters or backslashes.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Name",
      "description": "The fuzzy match for the template rule name. If the template is a system template, the internationalized name of the system template is fuzzy matched based on the language.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PageNumber",
      "description": "The page size for the paging query. Default value: 10.",
      "position": "Query",
      "type": "Integer",
      "required": true
    },
    {
      "name": "PageSize",
      "description": "The page number for the paging query. Default value: 1.",
      "position": "Query",
      "type": "Integer",
      "required": true
    },
    {
      "name": "ProjectId",
      "description": "The DataWorks workspace ID.",
      "position": "Query",
      "type": "Long",
      "required": true
    }
  ]
}
