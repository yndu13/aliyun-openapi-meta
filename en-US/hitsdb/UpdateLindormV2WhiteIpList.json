{
  "name": "UpdateLindormV2WhiteIpList",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "The ID of the instance. You can call the GetLindormV2InstanceList operation to obtain the instance ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The ID of the region where the instance is located. You can call the [DescribeRegions](~~426062~~) operation to obtain this ID.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "WhiteIpGroupList",
      "description": "The list of IP whitelist groups.",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "sub_parameters": [
        {
          "name": "GroupName",
          "description": "The name of the IP whitelist group.",
          "position": "Query",
          "type": "String",
          "required": true
        },
        {
          "name": "WhiteIpList",
          "description": "The IP addresses to add to the IP whitelist.\n\n> The value 127.0.0.1 denies access from all IP addresses. For example, 192.168.0.0/24 allows all IP addresses in that range to access the Lindorm instance. Use a comma (,) to separate multiple IP addresses or CIDR blocks.",
          "position": "Query",
          "type": "String",
          "required": true
        }
      ]
    }
  ]
}
