{
  "name": "ListProducts",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "MaxResults",
      "description": "The maximum number of records to return for the query.\n\nValid values: 1 to 200. Default: 30.",
      "position": "Body",
      "type": "Integer",
      "required": false
    },
    {
      "name": "NextToken",
      "description": "The token that marks the position to start the query. If this parameter is empty, the query starts from the beginning.",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ]
}
