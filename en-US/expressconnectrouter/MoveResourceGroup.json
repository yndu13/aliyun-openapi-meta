{
  "name": "MoveResourceGroup",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request. Generate a token that is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform a dry run. Valid values:\n\n- **true**: Sends a check request without changing the resource group. The system checks whether the required parameters are specified, the request format is valid, and the service limits are met. If the check fails, the corresponding error is returned. If the check passes, the `DryRunOperation` is returned.\n- **false** (default): Sends a normal request. If the request passes the check, a 2xx HTTP status code is returned and the operation is performed.",
      "position": "Body",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "NewResourceGroupId",
      "description": "The ID of the new resource group. See [What is a resource group?](~~94475~~)",
      "position": "Body",
      "type": "String",
      "required": true
    },
    {
      "name": "ResourceId",
      "description": "The ID of the instance whose resource group you want to change.",
      "position": "Body",
      "type": "String",
      "required": true
    },
    {
      "name": "ResourceType",
      "description": "The resource type. Valid value:\n\n- EXPRESSCONNECTROUTER: Express Connect Router.",
      "position": "Body",
      "type": "String",
      "required": true
    }
  ]
}
