{
  "name": "TagResources",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request.\n\nGenerate a parameter value from your client to ensure uniqueness across different requests. ClientToken supports only ASCII characters.\n\n> If you do not specify this parameter, the system uses the **RequestId** of the API request as the **ClientToken**. The **RequestId** may be different for each API request.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform only a dry run. Valid values:\n\n- **true**: performs a dry run without modifying EPS resources.\n- **false** (default): sends a normal request.",
      "position": "Body",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "ResourceId",
      "description": "The list of resource instance IDs.\n\nYou can specify up to 20 resources.",
      "position": "Body",
      "type": "RepeatList",
      "required": true
    },
    {
      "name": "ResourceType",
      "description": "The resource type. Valid values:\n- **EXPRESSCONNECTROUTER**: an Express Connect Router (ECR) instance.\n- **EXPRESSCONNECTROUTERVPCASSOCIATION**: an associate relationship between an ECR and a VPC.\n- **EXPRESSCONNECTROUTERTRASSOCIATION**: an associate relationship between an ECR and a transit router.\n- **ECRFLOWLOG**: an ECR flow log.",
      "position": "Body",
      "type": "String",
      "required": true
    },
    {
      "name": "Tag",
      "description": "The list of tags to bind.",
      "position": "Body",
      "type": "RepeatList",
      "required": true,
      "sub_parameters": [
        {
          "name": "Key",
          "description": "The tag key of the resource.\n\nThis parameter cannot be an empty string once specified. The tag key can be up to 64 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`.\n\nYou can specify up to 20 tag keys in a single call.",
          "position": "FormData",
          "type": "String",
          "required": false
        },
        {
          "name": "Value",
          "description": "The tag value. The tag value can be up to 128 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`. Once specified, the value can be an empty string.\n\nYou can specify up to 20 tag values in a single call.",
          "position": "FormData",
          "type": "String",
          "required": false
        }
      ]
    }
  ]
}
