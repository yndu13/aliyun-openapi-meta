{
  "name": "DescribeFlowLogs",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "A client token that you can use to ensure the idempotence of the request.\n\nYou must use a client token that is unique among different requests. The token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system uses the **RequestId** of the API request as the **ClientToken**. The **RequestId** may be different for each API request.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform a dry run. Valid values:\n\n- **true**: Performs a dry run. The system checks the request for potential issues, including missing parameter values, incorrect request syntax, and service limits. If the request fails the dry run, an error message is returned. If the request passes the dry run, the DryRunOperation error code is returned. No service resources are modified.\n\n- **false** (default): Sends a normal request. If the request passes the check, a 2xx HTTP status code is returned and the operation is performed.",
      "position": "Body",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "EcrId",
      "description": "The ID of the Express Connect Router (ECR) instance.",
      "position": "Body",
      "type": "String",
      "required": true
    },
    {
      "name": "FlowLogId",
      "description": "The ID of the flow log instance.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "FlowLogName",
      "description": "The name of the flow log. The name can be 0 to 128 characters long.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "InstanceId",
      "description": "The ID of the virtual border router (VBR) that is associated with the ECR instance.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "LogStoreName",
      "description": "The name of the Logstore where captured traffic is stored.\n\n- If you have a Logstore in the current region, enter the Logstore name.\n\n- If you do not have a Logstore in the current region, enter a custom name. The system automatically creates a Logstore for you. The name must meet the following requirements:\n\n- The name must be unique within a project.\n\n- The name can contain only lowercase letters, digits, hyphens (-), and underscores (\\_).\n\n- The name must start and end with a lowercase letter or a digit.\n\n- The name must be 3 to 63 characters long.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "MaxResults",
      "description": "The number of entries to return on each page. Valid values: 1 to 2,147,483,647. Default value: 10.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "NextToken",
      "description": "The token that is used for the next query. Valid values:\n\n- If this is your first query or if no next query is to be sent, do not specify this parameter.\n\n- If a value is returned for NextToken, the value is the token for the next query.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ProjectName",
      "description": "The name of the Log Service project where captured traffic is stored.\n\n- If you have a project in the current region, enter the project name.\n\n- If you do not have a project in the current region, enter a custom name. The system automatically creates a project for you.\n\nA project name must be globally unique within an Alibaba Cloud region and cannot be modified after it is created. The name must meet the following requirements:\n\n- The name must be globally unique.\n\n- The name can contain only lowercase letters, digits, and hyphens (-).\n\n- The name must start and end with a lowercase letter or a digit.\n\n- The name must be 3 to 63 characters long.\n\n- Must be 3 to 63 characters long.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ResourceGroupId",
      "description": "The ID of the resource group to which the flow log instance belongs.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Tag",
      "description": "The tags.\n\nYou can specify up to 20 tags.",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "Key",
          "description": "The tag key. The tag key cannot be an empty string. The tag key can be up to 128 characters long. It cannot start with aliyun or acs: and cannot contain http\\:// or https\\://.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "Value",
          "description": "The tag value. The tag value can be an empty string. The tag value can be up to 128 characters long. It cannot start with acs: and cannot contain http\\:// or https\\://.",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    }
  ]
}
