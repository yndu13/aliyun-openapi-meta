{
  "name": "ListTagResources",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "MaxResults",
      "description": "The maximum number of entries to return in a single request. Default value: 20.",
      "position": "Body",
      "type": "Integer",
      "required": false
    },
    {
      "name": "NextToken",
      "description": "The pagination token. Valid values:\n\n- If NextToken is empty, no next query exists.\n- If NextToken is returned, the value indicates the token for the next query.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "ResourceId",
      "description": "The list of resource instance IDs.\n\nYou can specify up to 20 resource instance IDs. If you exceed this limit, the IllegalParamSize.ResourceIds error is returned.\n\n> You must specify at least one of ResourceId and Tag. Otherwise, the MissingParam.TagsAndResourceIds error is returned.",
      "position": "Body",
      "type": "RepeatList",
      "required": false
    },
    {
      "name": "ResourceType",
      "description": "The resource type. Valid values:\n- **EXPRESSCONNECTROUTER**: Express Connect Router (ECR) instance.\n- **EXPRESSCONNECTROUTERVPCASSOCIATION**: association between an ECR and a VPC.\n- **EXPRESSCONNECTROUTERTRASSOCIATION**: association between an ECR and a transit router.\n- **ECRFLOWLOG**: ECR flow log.",
      "position": "Body",
      "type": "String",
      "required": true
    },
    {
      "name": "Tag",
      "description": "The tags.\n\nYou can bind up to 20 tags.",
      "position": "Body",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "Key",
          "description": "The tag key of the resource.\n\nThe tag key cannot be an empty string. It can be up to 64 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`.\n\nYou can specify up to 20 tag keys at a time.",
          "position": "FormData",
          "type": "String",
          "required": false
        },
        {
          "name": "Value",
          "description": "The tag value. It can be up to 128 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`. It can be an empty string.\n\nYou can specify up to 20 tag values at a time.",
          "position": "FormData",
          "type": "String",
          "required": false
        }
      ]
    }
  ]
}
