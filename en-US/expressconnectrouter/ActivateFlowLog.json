{
  "name": "ActivateFlowLog",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request.\n\nGenerate a unique value from your client to ensure that different requests have unique ClientToken values. ClientToken supports only ASCII characters.\n\n> If you do not specify this parameter, the system uses the RequestId of the API request as the ClientToken. The RequestId may differ for each API request.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform only a dry run. Valid values:\n- **true**: Sends a check request without modifying EPS resources.\n- **false** (default): Sends a normal request.",
      "position": "Body",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "EcrId",
      "description": "The Express Connect Router (ECR) instance ID.",
      "position": "Body",
      "type": "String",
      "required": true
    },
    {
      "name": "FlowLogId",
      "description": "The flow log instance ID.\n> The flow log must be in the DEACTIVATE state. After you invoke DeactivateFlowLog, the flow log enters the DEACTIVATING transitional state. Use polling by invoking DescribeFlowLogs and wait until the status changes to DEACTIVATE before invoking this operation.",
      "position": "Body",
      "type": "String",
      "required": true
    }
  ]
}
