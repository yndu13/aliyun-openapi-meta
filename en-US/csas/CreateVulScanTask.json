{
  "name": "CreateVulScanTask",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EndTimestamp",
      "description": "The task expiration time, in seconds-level UNIX timestamp. After this time is reached, endpoints no longer pull and execute this task, and incomplete scans are not continued.",
      "position": "Body",
      "type": "Long",
      "required": false
    },
    {
      "name": "MatchMode",
      "description": "The matching mode for the effective scope. Valid values:\n\n- **UserGroupAll**: Takes effect for all users under the current Alibaba Cloud account.\n- **UserGroupNormal**: Takes effect only for users in specified user groups. In this case, UserGroupIds is required.",
      "position": "Body",
      "type": "String",
      "required": true
    },
    {
      "name": "TaskDescription",
      "description": "The task description.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "TaskName",
      "description": "The task name. The name can be up to 128 characters in length and can contain Chinese characters, uppercase and lowercase letters, digits, periods (.), underscores (_), and hyphens (-). Spaces are not supported.",
      "position": "Body",
      "type": "String",
      "required": true
    },
    {
      "name": "UserGroupIds",
      "description": "The collection of user group IDs for which the task takes effect. This parameter is required when MatchMode is set to UserGroupNormal and cannot be specified when MatchMode is set to UserGroupAll. The collection must contain at least 1 and at most 100 entries. Duplicate values are not allowed.",
      "position": "Body",
      "type": "Array",
      "required": false
    },
    {
      "name": "Whitelist",
      "description": "The list of exempt usernames. Users in this list are excluded from this scan. The list can contain up to 1000 entries. Duplicate values are not allowed.",
      "position": "Body",
      "type": "Array",
      "required": false
    }
  ]
}
