{
  "name": "UpdateDynamicRoute",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ApplicationIds",
      "description": "A collection of private network access application IDs for the dynamic route. You can specify up to 200 IDs. This parameter is required when **ApplicationType** is set to **Application**. Specify either this parameter or **TagIds**, but not both. Do not specify this parameter when **ApplicationType** is set to **All**.",
      "position": "Body",
      "type": "Array",
      "required": false
    },
    {
      "name": "ApplicationType",
      "description": "The application type of the dynamic route. Valid values:\n\n- **All**: All applications.\n\n- **Application**: Application.\n\n- **Tag**: Tag.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "Description",
      "description": "The description of the dynamic route. The description must be 1 to 128 characters long and can contain letters, digits, periods (.), underscores (\\_), hyphens (-), and spaces.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "DynamicRouteId",
      "description": "The ID of the dynamic route.",
      "position": "Body",
      "type": "String",
      "required": true
    },
    {
      "name": "DynamicRouteType",
      "description": "The type of the dynamic route. Valid value: **connector**: leased line.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "ModifyType",
      "description": "The modification type of the dynamic route. Valid values:\n\n- **Cover** (default): Use the values of **RegionIds**, **ApplicationIds**, and **TagIds** to overwrite the existing regions, private network access application IDs, and private network access tag IDs supported by the SASE POP cluster access points.\n\n- **Append**: Add the values specified in **RegionIds**, **ApplicationIds**, and **TagIds** to the existing regions, private network access application IDs, and private network access tag IDs supported by the SASE POP cluster access points.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "Name",
      "description": "The name of the dynamic route. The name must be 1 to 128 characters long and can contain letters, digits, periods (.), underscores (\\_), and hyphens (-).",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "NextHop",
      "description": "The next hop instance ID of the dynamic route. To get valid values, see:\n\n- [ListConnectors](~~ListConnectors~~): Query connectors in bulk.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "Priority",
      "description": "The route priority. A value of 1 indicates the highest priority. Valid values: 1 to 99.",
      "position": "Body",
      "type": "Integer",
      "required": false
    },
    {
      "name": "RegionIds",
      "description": "A list of regions supported by the SASE POP cluster access points.",
      "position": "Body",
      "type": "Array",
      "required": false
    },
    {
      "name": "Status",
      "description": "The status of the dynamic route. Valid values:\n\n- **Enabled**: Enable the route.\n\n- **Disabled**: Disable the route.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "TagIds",
      "description": "A collection of private network access tag IDs for the dynamic route. You can specify up to 40 IDs. This parameter is required when **ApplicationType** is set to **Tag**. Specify either this parameter or **ApplicationIds**, but not both. Do not specify this parameter when **ApplicationType** is set to **All**.",
      "position": "Body",
      "type": "Array",
      "required": false
    }
  ]
}
