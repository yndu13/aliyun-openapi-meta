{
  "name": "DeleteBackupClientResource",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientIds",
      "description": "A list of client IDs. The list can contain up to 100 client IDs.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
