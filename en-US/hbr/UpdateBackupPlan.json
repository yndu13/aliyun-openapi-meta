{
  "name": "UpdateBackupPlan",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ChangeListPath",
      "description": "The configuration for the incremental file synchronization list. (This parameter is required only for file synchronization.)",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Detail",
      "description": "The details of the ECS instance backup. This is a JSON string.\n\n- snapshotGroup: Specifies whether to use a snapshot-consistent group. This feature is available only when all disks of the instance are Enhanced Solid-State Drives (ESSDs).\n\n- appConsistent: Specifies whether to enable application consistency. You must also configure the preScriptPath and postScriptPath parameters.\n\n- preScriptPath: The path to the pre-freeze script.\n\n- postScriptPath: The path to the post-thaw script.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Edition",
      "description": "The edition. Valid values are BASIC and STANDARD. The default value is STANDARD.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Exclude",
      "description": "This parameter is required only when **SourceType** is set to **ECS\\_FILE**. This parameter specifies the paths to the files to exclude from the backup. All files in the specified paths are not backed up. The value can be up to 255 characters in length.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "Include",
      "description": "This parameter is required only when **SourceType** is set to **ECS\\_FILE**. This parameter specifies the paths to the files to back up. All files in the specified paths are backed up. The value can be up to 255 characters in length.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "KeepLatestSnapshots",
      "description": "Specifies whether to permanently retain the latest backup version.\n\n- 0: No\n\n- 1: Yes",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "Options",
      "description": "This parameter is required only when **SourceType** is set to **ECS\\_FILE**. This parameter specifies whether to use Volume Shadow Copy Service (VSS) to define the backup path.\n\n- This feature is available only for Windows ECS instances.\n\n- If data changes occur in the backup source, set this parameter to `[\"UseVSS\":true]` to ensure data consistency.\n\n- If you enable VSS, you cannot back up multiple file directories at the same time.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "OtsDetail",
      "description": "The details of the Tablestore instance.",
      "position": "Body",
      "type": "Json",
      "required": false
    },
    {
      "name": "Path",
      "description": "The backup paths.",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    },
    {
      "name": "PlanId",
      "description": "The ID of the backup plan.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "PlanName",
      "description": "The name of the backup plan.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Prefix",
      "description": "This parameter is required only when **SourceType** is set to **OSS**. This parameter specifies the prefix of objects to back up. After you specify a prefix, only objects that match the prefix are backed up.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Retention",
      "description": "The number of days to retain backups. The minimum value is 1.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "Rule",
      "description": "The rules of the backup plan.",
      "position": "Body",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "BackupType",
          "description": "The backup type. Set the value to **COMPLETE**. This indicates a full backup.",
          "position": "FormData",
          "type": "String",
          "required": false
        },
        {
          "name": "DestinationRegionId",
          "description": "The ID of the destination region for the geo-redundant backup.",
          "position": "FormData",
          "type": "String",
          "required": false
        },
        {
          "name": "DestinationRetention",
          "description": "The number of days to retain the geo-redundant backup.",
          "position": "FormData",
          "type": "Long",
          "required": false
        },
        {
          "name": "Disabled",
          "description": "Specifies whether to disable the policy.",
          "position": "FormData",
          "type": "Boolean",
          "required": false
        },
        {
          "name": "DoCopy",
          "description": "Specifies whether to enable geo-redundant replication.",
          "position": "FormData",
          "type": "Boolean",
          "required": false
        },
        {
          "name": "Retention",
          "description": "The number of days to retain backups. The minimum value is 1.",
          "position": "FormData",
          "type": "Long",
          "required": false
        },
        {
          "name": "RuleName",
          "description": "The name of the policy.",
          "position": "FormData",
          "type": "String",
          "required": false
        },
        {
          "name": "Schedule",
          "description": "The backup policy. Use the I|{startTime}|{interval} format. This specifies that a backup job runs at a recurring interval. The {startTime} is when the backup starts. The {interval} is the time between jobs. HBR does not run overdue backup jobs. If the previous backup job is not finished, the next one does not start. For example, I|1631685600|P1D means the backup runs once a day, starting at 14:00:00 on September 15, 2021.\n\nstartTime: The start time of the backup. This is a UNIX timestamp in seconds. interval: The time interval. Use the ISO 8601 standard. For example, PT1H specifies an interval of one hour. P1D specifies an interval of one day.",
          "position": "FormData",
          "type": "String",
          "required": false
        }
      ]
    },
    {
      "name": "Schedule",
      "description": "The backup policy. Use the `I|{startTime}|{interval}` format. This specifies that a backup job runs at a recurring interval. The `{startTime}` is when the backup starts. The `{interval}` is the time between jobs. HBR does not run overdue backup jobs. If the previous backup job is not finished, the next one does not start. For example, `I|1631685600|P1D` means the backup runs once a day, starting at 14:00:00 on September 15, 2021.\n\n- **startTime**: The start time of the backup. This is a UNIX timestamp in seconds.\n\n- **interval**: The time interval. Use the ISO 8601 standard. For example, PT1H specifies an interval of one hour. P1D specifies an interval of one day.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "SourceType",
      "description": "The type of the data source. Valid values:\n\n- **ECS\\_FILE**: Backs up ECS files.\n\n- **OSS**: Backs up Alibaba Cloud OSS.\n\n- **NAS**: Backs up Alibaba Cloud NAS.\n\n- **OTS**: Backs up Alibaba Cloud Tablestore.\n\n- **UDM\\_ECS**: Backs up an entire ECS instance.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "SpeedLimit",
      "description": "This parameter is required only when **SourceType** is set to **ECS\\_FILE**. This parameter specifies traffic shaping for backups. Traffic shaping helps you control backup traffic during peak business hours to avoid affecting your services. The format is `{start}|{end}|{bandwidth}`. You can specify multiple traffic shaping rules. Separate them with vertical bars (|). The time ranges of the rules cannot overlap.\n\n- **start**: The start hour.\n\n- **end**: The end hour.\n\n- **bandwidth**: The maximum speed. Unit: KB/s.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "UpdatePaths",
      "description": "Specifies whether to update the backup paths if the Path parameter is empty.\n\n- true: Updates the backup paths based on the paths specified in this call.\n\n- false: Does not update the backup paths. The backup paths that were configured when the backup plan was created are used.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "VaultId",
      "description": "The ID of the backup repository.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
