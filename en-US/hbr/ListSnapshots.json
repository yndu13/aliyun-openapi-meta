{
  "name": "ListSnapshots",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CompleteTimeEnd",
      "description": "The end completion time. A UNIX timestamp, in seconds. Only backup points with a completion time earlier than this value are returned.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "CompleteTimeStart",
      "description": "The start completion time. A UNIX timestamp, in seconds. Only backup points with a completion time greater than or equal to this value are returned.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "MaxResults",
      "description": "The maximum number of results to return in a paged query. Default value: 10. The value must be greater than 1.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "NextToken",
      "description": "The pagination token. You do not need to specify this parameter for the first request. For subsequent pages, pass in the **NextToken** value returned from the previous request. An empty **NextToken** indicates that no more pages are available. This parameter cannot be used together with **Skip**.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PlanId",
      "description": "The backup plan ID. This is an optional filter condition. If specified, only backup points under the specified backup plan are returned.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ProtectedResourceId",
      "description": "The ID of the protected resource. The protected resource must exist, and its data source type must match the **SourceType** parameter.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Skip",
      "description": "The number of entries to skip. The value must be a non-negative integer and a multiple of **MaxResults**. This parameter cannot be used together with **NextToken**.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "SourceType",
      "description": "The data source type. The value must match the data source type of the protected resource (ProtectedResourceId). The supported data source types are listed in the valid values.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "VaultId",
      "description": "The vault ID. This is an optional filter condition. If specified, only backup points in the specified vault are returned.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
