{
  "name": "CreateReplicationVault",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Description",
      "description": "The description of the backup vault. The description must be 0 to 255 characters in length.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "EncryptType",
      "description": "The encryption type of the replication target vault. This parameter is valid only when VaultType is set to STANDARD. The encryption type must be the same as that of the source backup repository. Valid values:\n- **HBR_PRIVATE**: fully managed by Cloud Backup. The built-in secret key encryption method of the backup service is used.\n- **KMS**: uses a custom key from Alibaba Cloud Key Management Service (KMS) for encryption.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "KmsKeyId",
      "description": "The custom key or alias from Alibaba Cloud KMS. This parameter is required only when EncryptType is set to KMS.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RedundancyType",
      "description": "The data redundancy storage method of the backup vault. Valid values:\n\n- LRS: locally redundant storage (LRS). The data redundancy storage mechanism is used to store redundant copies of each object on multiple devices across multiple facilities within the same zone, ensuring data durability and availability in the event of hardware failure.\n- ZRS: zone-redundant storage (ZRS). The multi-zone mechanism is used to distribute user data across three zones in the same region. If one zone becomes unavailable, the data can still be accessed normally.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ReplicationSourceRegionId",
      "description": "The region ID of the source vault.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ReplicationSourceVaultId",
      "description": "The ID of the source vault.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "VaultName",
      "description": "The name of the backup vault. The name must be 1 to 64 characters in length.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "VaultRegionId",
      "description": "The region ID of the backup vault.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "VaultStorageClass",
      "description": "The storage class of the backup vault. The value can only be **STANDARD**, which indicates standard storage.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
