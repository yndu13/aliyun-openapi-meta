{
  "name": "DescribeFeatureTrialInfo",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "FeatureType",
      "description": "The feature type. Currently, only the free trial information of Tablestore backup can be queried.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
