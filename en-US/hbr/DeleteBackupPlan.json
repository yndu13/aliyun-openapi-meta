{
  "name": "DeleteBackupPlan",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Edition",
      "description": "The edition type. Valid values are BASIC and STANDARD. The default value is STANDARD.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PlanId",
      "description": "The ID of the backup plan.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RequireNoRunningJobs",
      "description": "Specifies whether to prevent the deletion of the backup plan if backup jobs are running.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "SourceType",
      "description": "The type of the data source. Valid values:\n\n- **ECS\\_FILE**: Backs up Elastic Compute Service (ECS) files.\n\n- **OSS**: Backs up Object Storage Service (OSS) buckets.\n\n- **NAS**: Backs up Network Attached Storage (NAS) file systems.\n\n- **UDM\\_ECS**: Backs up entire ECS instances.\n\n- **OTS**: Backs up Tablestore instances.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "VaultId",
      "description": "The ID of the backup vault. This parameter is required if the SourceType parameter is not set to UDM\\_ECS.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
