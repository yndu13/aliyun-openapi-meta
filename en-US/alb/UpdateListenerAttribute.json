{
  "name": "UpdateListenerAttribute",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CaCertificates",
      "description": "The CA certificate list. Only one CA certificate is supported.",
      "position": "Query",
      "type": "Array",
      "required": false
    },
    {
      "name": "CaEnabled",
      "description": "Specifies whether to enable mutual authentication. Valid values:\n- **true**: enabled.\n- **false**: disabled.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "Certificates",
      "description": "The certificate list.",
      "position": "Query",
      "type": "Array",
      "required": false
    },
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** as the **ClientToken**. The **RequestId** may be different for each API request.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DefaultActions",
      "description": "The list of default rule actions.",
      "position": "Query",
      "type": "Array",
      "required": false
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform only a dry run, without performing the actual request. Valid values:\n\n- **true**: performs only a dry run. The system checks the request for potential issues, including missing parameter values, incorrect request syntax, and service limits. If the request fails the dry run, an error code is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n\n- **false** (default): performs a dry run and performs the actual request. If the request passes the dry run, an `HTTP 2xx` status code is returned and the operation is performed.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "GzipEnabled",
      "description": "Specifies whether to enable Gzip compression to compress specific types of files. Valid values:\n\n- **true**: enabled.\n- **false**: disabled.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "Http2Enabled",
      "description": "Specifies whether to enable HTTP/2. Valid values:\n- **true**: enabled.\n- **false**: disabled.\n\n> Only HTTPS listeners support this parameter.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "IdleTimeout",
      "description": "The idle connection timeout period. Unit: seconds. Valid values: **1 to 600**.\n\nIf no access request is received within the timeout period, load balancing temporarily breaks the current connection and restores the connection when the next request arrives.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "ListenerDescription",
      "description": "The custom listener name.\n\nThe name must be 2 to 256 characters in length, and can contain letters, digits, Chinese characters, commas (,), periods (.), semicolons (;), forward slashes (/), at signs (@), underscores (\\_), and hyphens (-).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ListenerId",
      "description": "The listener ID of the Application Load Balancing (ALB) instance.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "QuicConfig",
      "description": "The configuration information when an associated QUIC listener is enabled.",
      "position": "Query",
      "type": "Struct",
      "required": false
    },
    {
      "name": "RequestTimeout",
      "description": "The request timeout period. Unit: seconds. Valid values: **1 to 600**.\n\nIf no response is received from the backend server within the timeout period, load balancing returns an `HTTP 504` error code to the client.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "SecurityPolicyId",
      "description": "The security policy. System security policies and custom security policies are supported.\n\n> Only HTTPS listeners support this parameter.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "XForwardedForConfig",
      "description": "The configuration of the XForwardedFor feature.",
      "position": "Query",
      "type": "Struct",
      "required": false
    }
  ]
}
