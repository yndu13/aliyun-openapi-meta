{
  "name": "DescribeGroupedContainerInstances",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Criteria",
      "description": "The conditions for searching assets. This parameter is in JSON format. Separate multiple conditions with commas (,). Example: `[{\"name\":\"riskStatus\",\"value\":\"YES\"},{\"name\":\"riskLevel\",\"value\":\"2\"}]`.\n> You can search for assets by instance ID, instance name, VPC ID, region, public IP address, and other conditions. Call [DescribeCriteria](~~DescribeCriteria~~) to query the supported search conditions.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "CurrentPage",
      "description": "The page number of the page to return. Default value: **1**, which indicates that the first page is returned.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "FieldValue",
      "description": "The search condition for the specified group type. Set the search condition based on the type specified by GroupField:\n- If **GroupField** is set to **pod**: specify the pod name to query.\n- If **GroupField** is set to **appName**: specify the application name to query.\n- If **GroupField** is set to **namespace**: specify the namespace to query.\n- If **GroupField** is set to **clusterId**: specify the cluster ID to query.\n- If **GroupField** is set to **image**: specify the image name to query.\n> All the preceding search conditions support fuzzy match.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "GroupField",
      "description": "The group type to query. Valid values:\n- **pod**: pod\n- **appName**: application name\n- **namespace**: namespace\n- **clusterId**: cluster ID\n- **image**: image.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "LogicalExp",
      "description": "The logical relationship among multiple search conditions. Valid values:\n- **OR**: The search conditions are evaluated with a logical OR.\n- **AND**: The search conditions are evaluated with a logical AND.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PageSize",
      "description": "The number of container assets to display on each page when paging is used. Default value: **20**, which indicates that 20 container assets are displayed on each page.\n> Do not leave PageSize empty.",
      "position": "Query",
      "type": "Integer",
      "required": false
    }
  ]
}
