{
  "name": "ModifyOpenLogShipper",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "From",
      "description": "The identifier of the request source. Default value: **aegis**. Valid values:\n\n- **aegis**: Server Guard edition.\n- **sas**: Security Center edition.\n\n> Server Guard users must use **aegis**, and Security Center users must use **sas**.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ResourceDirectoryAccountId",
      "description": "The ID of the member accounts in the resource directory (Alibaba Cloud account).\n> You can call the [DescribeMonitorAccounts](~~DescribeMonitorAccounts~~) operation to obtain this parameter.",
      "position": "Query",
      "type": "Long",
      "required": false
    }
  ]
}
