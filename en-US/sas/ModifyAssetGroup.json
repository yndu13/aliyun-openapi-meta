{
  "name": "ModifyAssetGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "GroupId",
      "description": "The ID of the new group to which you want to assign the servers.\n> You can call the [DescribeAllGroups](~~DescribeAllGroups~~) operation to query the group IDs of servers.",
      "position": "Query",
      "type": "Long",
      "required": true
    },
    {
      "name": "SourceIp",
      "description": "The IP address of the access source.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Uuids",
      "description": "The UUIDs of the servers whose group you want to modify. Separate multiple UUIDs with commas (,).\n> You can call the [DescribeCloudCenterInstances](~~DescribeCloudCenterInstances~~) operation to query the UUIDs of servers.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
