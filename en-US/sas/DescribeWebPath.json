{
  "name": "DescribeWebPath",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CurrentPage",
      "description": "The page number of the current page in a paged query. Paging is used to display results.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "PageSize",
      "description": "The maximum number of entries per page in a paged query. Paging is used to display results.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "Type",
      "description": "The alert configuration type. Valid values:\n\n- **web_path**.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
