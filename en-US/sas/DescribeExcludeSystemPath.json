{
  "name": "DescribeExcludeSystemPath",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CurrentPage",
      "description": "The page number of the page to return. Default value: 1, which indicates that the first page is returned.",
      "position": "Query",
      "type": "Integer",
      "required": true
    },
    {
      "name": "PageSize",
      "description": "The maximum number of entries to return on each page when paging is used for a paged query.",
      "position": "Query",
      "type": "Integer",
      "required": true
    }
  ]
}
