{
  "name": "UpdateFileUploadLimit",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Limit",
      "description": "The QPS throttling threshold for file uploads from the client. Valid values: 100 to 10000.",
      "position": "Query",
      "type": "Long",
      "required": true
    }
  ]
}
