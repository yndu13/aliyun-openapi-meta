{
  "name": "VerifyCheckResult",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CheckIds",
      "description": "The list of check item IDs.",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    },
    {
      "name": "Force",
      "description": "Specifies whether to forcibly run the specified check items. Default value: false.\n\n- true: Forcibly runs the specified check items. Forced execution bypasses frequency and quantity limits and initiates the check directly, which may cause duplicate checks to run multiple times within a short period.\n- false (default): Does not forcibly run the specified check items. This ensures that the same check item is executed only once within a short period.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "InstanceIds",
      "description": "The list of instance IDs of the assets associated with the check items.",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    },
    {
      "name": "TaskSource",
      "description": "The task source. Valid values:\n\n- **YAO_CHI**: ApsaraDB console.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
