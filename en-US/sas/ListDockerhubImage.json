{
  "name": "ListDockerhubImage",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Query",
      "description": "The image query condition. The format is `[namespace/]repoName[:version]`. The query conditions in `[]` are optional.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
