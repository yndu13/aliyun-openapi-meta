{
  "name": "UpdateAutoThrottleRulesAsync",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AbnormalDuration",
      "description": "The duration threshold of the anomaly that triggers automatic SQL throttling. The value must be a positive integer greater than or equal to 2. Unit: minutes.",
      "position": "Query",
      "type": "Double",
      "required": true
    },
    {
      "name": "ActiveSessions",
      "description": "The active sessions threshold.\t\n\n- If the relationship with the CPU utilization threshold is **OR**, the value must be greater than or equal to 16.\n- If the relationship with the CPU utilization threshold is **AND**, the value must be greater than or equal to 2.",
      "position": "Query",
      "type": "Long",
      "required": true
    },
    {
      "name": "AllowThrottleEndTime",
      "description": "The end time of the throttling time window (UTC).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "AllowThrottleStartTime",
      "description": "The start time of the throttling time window (UTC).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "AutoKillSession",
      "description": "Specifies whether to simultaneously kill abnormal SQL statements that are being executed.\n> Abnormal SQL statements are those that match the SQL templates to be throttled.\n\nValid values:\n- **true**: Yes.\n- **false**: No.",
      "position": "Query",
      "type": "Boolean",
      "required": true
    },
    {
      "name": "ConsoleContext",
      "description": "A reserved parameter.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "CpuSessionRelation",
      "description": "The logical relationship between the CPU utilization threshold and the active sessions threshold. Valid values:\n- **AND**: both conditions must be met.\n- **OR**: either condition must be met.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "CpuUsage",
      "description": "The CPU utilization threshold. Valid values: 70% to 100%.",
      "position": "Query",
      "type": "Double",
      "required": true
    },
    {
      "name": "InstanceIds",
      "description": "The database instance IDs.\n\n> The data format is JSONArray, such as `['Instance ID 1','Instance ID 2']`. Separate instance IDs with commas (,).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "MaxThrottleTime",
      "description": "The maximum throttling duration. The value must be a positive integer. Unit: minutes.",
      "position": "Query",
      "type": "Double",
      "required": true
    },
    {
      "name": "ResultId",
      "description": "The ID of the asynchronous request.\n> An asynchronous call does not immediately return complete results. First, call this operation to obtain the **ResultId**. Then, use the returned **ResultId** to initiate the call again until **isFinish** is **true**, at which point the complete results are returned. This means that you must call this operation at least twice to obtain complete data.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
