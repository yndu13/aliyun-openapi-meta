{
  "name": "GetAsyncErrorRequestStatResult",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DbName",
      "description": "The name of the database.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "End",
      "description": "The end time of the query, specified as a Unix timestamp in milliseconds.\n\n> The end time must be later than the start time. The interval between the start time and the end time cannot exceed 1 day.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "InstanceId",
      "description": "The ID of the instance.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "NodeId",
      "description": "The ID of the node.\n\n> This parameter is required for PolarDB for MySQL instances.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "SqlIdList",
      "description": "One or more SQL IDs. Separate multiple SQL IDs with commas. You can call the [GetAsyncErrorRequestListByCode](~~410746~~) operation to query the SQL IDs that generated MySQL error codes on the instance.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Start",
      "description": "The start time of the query, specified as a Unix timestamp in milliseconds.\n\n> The start time must be within the data retention period of SQL Explorer for the database instance. The start time cannot be earlier than 90 days before the current time.",
      "position": "Query",
      "type": "Long",
      "required": false
    }
  ]
}
