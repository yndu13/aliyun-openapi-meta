{
  "name": "DescribeAutoScalingHistory",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoScalingTaskType",
      "description": "The type of elastic scaling task to query. Currently, only **SPEC** is supported, which indicates querying the automatic performance scaling history.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "EndTime",
      "description": "The end time of the query task. Specify the value as a UNIX timestamp. Unit: milliseconds.\n> The end time must be later than the start time.",
      "position": "Query",
      "type": "Long",
      "required": true
    },
    {
      "name": "InstanceId",
      "description": "The instance ID.\n\n> Currently, only ApsaraDB RDS for MySQL instances are supported.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "StartTime",
      "description": "The start time of the query task. Specify the value as a UNIX timestamp. Unit: milliseconds.\n\n> The start time cannot be earlier than 45 days before the current time.",
      "position": "Query",
      "type": "Long",
      "required": true
    }
  ]
}
