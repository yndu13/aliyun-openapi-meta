{
  "name": "MigrateEcu",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/pop/v5/resource/migrate_ecu",
  "parameters": [
    {
      "name": "InstanceIds",
      "description": "The IDs of the instances. To specify multiple instances, separate the IDs with commas (,).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "LogicalRegionId",
      "description": "The ID of the namespace.\n\n- A custom namespace ID is in the format `Region ID:Namespace identifier`. Example: cn-beijing:tdy218.\n\n- A default namespace ID is the same as its region ID. Example: cn-beijing.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
