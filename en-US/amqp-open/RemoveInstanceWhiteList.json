{
  "name": "RemoveInstanceWhiteList",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "The instance ID of the instance for which you want to delete the whitelist entry.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "whiteListItemId",
      "description": "The ID of the whitelist entry. You can call the ListInstanceWhiteList operation to query the whitelist entry ID.",
      "position": "Query",
      "type": "Long",
      "required": true
    },
    {
      "name": "whiteListType",
      "description": "The whitelist type. Valid values:\n\n- 1: VPCID whitelist type.\n- 2: IP whitelist type.\n\nIf WhiteListItem is set to an IP address, set WhiteListType to 2. If WhiteListItem is set to a VPC ID, set WhiteListType to 1.\n\nVPC whitelists apply only to instances whose VPC access points are of the anytunnel type. The VPC access points of the latest instances are of the PrivateLink type, so you cannot configure VPC whitelists for such instances.",
      "position": "Query",
      "type": "Integer",
      "required": true
    }
  ]
}
