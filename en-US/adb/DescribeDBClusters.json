{
  "name": "DescribeDBClusters",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBClusterDescription",
      "description": "The cluster description.\n\n- It cannot start with `http://` or `https://`.\n\n- It must be 2 to 256 characters in length.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DBClusterIds",
      "description": "The cluster ID.\n\n> You can specify any cluster ID in the specified region.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DBClusterStatus",
      "description": "The cluster status. Valid values:\n\n- **Preparing**: Preparing.\n\n- **Creating**: Creating.\n\n- **Restoring**: Restoring from a backup.\n\n- **Running**: Running.\n\n- **Deleting**: Deleting.\n\n- **ClassChanging**: Upgrading or downgrading specifications.\n\n- **NetAddressCreating**: Creating a network endpoint.\n\n- **NetAddressDeleting**: Deleting a network endpoint.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DBClusterVersion",
      "description": "Cluster version\n\n- **3.0**: Data Warehouse Edition.\n\n- **5.0** (default): Includes the Data Lakehouse Edition, Enterprise Edition, and Basic Edition.\n\n- **All**: All versions, including the Data Warehouse Edition, Data Lakehouse Edition, Enterprise Edition, and Basic Edition.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DBVersion",
      "description": "The database version. Valid value: **3.0**.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PageNumber",
      "description": "The page number. Valid values are integers greater than 0 and less than or equal to the maximum value of the integer data type. Default value: **1**.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "PageSize",
      "description": "The number of entries to return on each page. Valid values:\n\n- **30** (default)\n\n- **50**\n\n- **100**",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID.\n\n> You can call the [DescribeRegions](~~143074~~) operation to view available region IDs.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ResourceGroupId",
      "description": "The resource group ID.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Tag",
      "description": "The list of tags.",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "Key",
          "description": "The tag key. You can filter clusters by tag keys. You can specify up to 20 key-value pairs. The numbers n in `Tag.N.Key` and `Tag.N.Value` must be unique and consecutive integers starting from 1.\n\n> A tag key can be up to 64 characters in length. It cannot start with `aliyun`, `acs:`, `http://`, or `https://`.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "Value",
          "description": "The tag value. You can filter clusters by tag values. You can specify up to 20 key-value pairs. The numbers n in `Tag.N.Key` and `Tag.N.Value` must be unique and consecutive integers starting from 1.\n\n> A tag value can be up to 64 characters in length. It cannot start with `aliyun`, `acs:`, `http://`, or `https://`.",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    }
  ]
}
