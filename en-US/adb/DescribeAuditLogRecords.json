{
  "name": "DescribeAuditLogRecords",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBClusterId",
      "description": "The ID of the Data Warehouse Edition cluster.\n\n> Call the [DescribeDBClusters](~~129857~~) operation to view the IDs of all Data Warehouse Edition clusters in the destination region.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "DBName",
      "description": "The name of the database on which the SQL statement was executed.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "EndTime",
      "description": "The end of the time range to query. Specify the time in the yyyy-MM-ddTHH:mmZ format. The time must be in UTC.\n\n> - The end time must be later than the start time.\n>\n> - The time range to query cannot exceed 24 hours.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "HostAddress",
      "description": "The IP address and port number of the client that was used to execute the SQL statement.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Order",
      "description": "The sorting order of the query results. Specify the value as an ordered JSON array. The results are sorted based on the order of fields in the array. Each object in the array contains the \\`Field\\` and \\`Type\\` fields.\n\n- Field specifies the field by which to sort the query results. Valid values:\n\n  - HostAddress: the IP address of the client that connects to the database.\n\n  - Succeed: specifies whether the SQL statement was successfully executed.\n\n  - TotalTime: the total amount of time that is consumed to execute the SQL statement.\n\n  - DBName: the name of the database on which the SQL statement was executed.\n\n  - SQLType: the type of the SQL statement.\n\n  - User: the username that was used to execute the SQL statement.\n\n  - ExecuteTime: the start time of the SQL statement execution.\n\n- Type specifies the sorting method. Valid values:\n\n  - Desc: descending.\n\n  - Asc: ascending.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "OrderType",
      "description": "The order by which to sort the query results based on the execution time of the SQL statement. Valid values:\n\n- **asc**: ascending.\n\n- **desc**: descending.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PageNumber",
      "description": "The page number. The value must be an integer that is greater than 0 and does not exceed the maximum value of the integer data type. Default value: **1**.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "PageSize",
      "description": "The number of entries to return on each page. Valid values:\n\n- **10**\n\n- **30**\n\n- **50**\n\n- **100**\n\n> If you do not specify this parameter, the default value is 10.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "QueryKeyword",
      "description": "The keyword for the query. This keyword is used to filter SQL statements.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID.\n\n> Call the [DescribeRegions](~~143074~~) operation to view the regions and zones, including the region IDs, that are supported by AnalyticDB for MySQL.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "SqlType",
      "description": "The type of the SQL statement. Valid values:\n\n- **DELETE**\n\n- **SELECT**\n\n- **UPDATE**\n\n- **INSERT\\_INTO\\_SELECT**\n\n- **ALTER**\n\n- **DROP**\n\n- **CREATE**\n\n> You can query for only one type of SQL statement at a time. If you leave this parameter empty, all types of SQL statements are queried.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "StartTime",
      "description": "The start of the time range to query. Specify the time in the yyyy-MM-ddTHH:mmZ format. The time must be in UTC.\n\n> You can query SQL audit logs only when SQL Audit is enabled. You can query only the SQL audit logs from the last 30 days. If you disable and then re-enable SQL Audit, you can query only the logs generated after it was re-enabled.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Succeed",
      "description": "Specifies whether the SQL statement was successfully executed. Valid values:\n\n- **true**: The SQL statement was successfully executed.\n\n- **false**: The SQL statement failed to be executed.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "User",
      "description": "The username that was used to execute the SQL statement.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
