{
  "name": "ModifyDBClusterShardNumber",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBClusterId",
      "description": "The ID of the AnalyticDB for MySQL cluster.\n\n> You can call the [DescribeDBClusters](~~129857~~) API to query the IDs of all AnalyticDB for MySQL clusters in a specific region.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform a dry run. Valid values:\n\n- **true**: Sends a check request to verify prerequisites and the validity of the specified number of shards. The number of shards is **not** modified.\n\n- **false** (default): Sends a standard request. After the request passes the check, the system triggers a backend task to modify the number of shards for the cluster.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "IsRollback",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "NewShardNumber",
      "description": "The new number of shards.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID.\n\n> You can call the [DescribeRegions](~~143074~~) API to query the most recent list of regions.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "SwitchTime",
      "description": "The scheduled time to apply the modification. Specify the time in the `yyyy-MM-ddTHH:mm:ssZ` format. The time must be in UTC.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "SwitchTimeMode",
      "description": "Specifies when to modify the number of shards. Valid values:\n\n- **Immediate**: The modification takes effect immediately. This is the default value.\n\n- **MaintainTime**: The modification takes effect during the cluster's maintenance window. You can call the ModifyDBInstanceMaintainTime API to change the maintenance window.\n\n- **ScheduleTime**: The modification takes effect at a specified time. If you set this parameter to ScheduleTime, you must also specify the **SwitchTime** parameter.",
      "position": "Query",
      "type": "Long",
      "required": false
    }
  ]
}
