{
  "name": "CreateDBResourcePool",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBClusterId",
      "description": "The ID of the AnalyticDB for MySQL cluster.\n\n> Call the [DescribeDBClusters](~~129857~~) operation to query the IDs of all AnalyticDB for MySQL clusters in a specific region.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "NodeNum",
      "description": "The number of nodes. The default value is 0.\n\n- A single node provides 16 cores and 64 GB of memory.\n\n- The total resources, calculated as (Number of nodes × 16 cores and 64 GB of memory), cannot exceed the total resources of the cluster.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "PoolName",
      "description": "The name of the resource group.\n\n- The name can be up to 255 characters in length.\n\n- It must start with a digit, an uppercase letter, or a lowercase letter.\n\n- It can contain digits, uppercase letters, lowercase letters, hyphens (-), and underscores (\\_).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "QueryType",
      "description": "The execution mode of the SQL query. Valid values:\n\n- **batch**: batch processing mode.\n\n- **interactive** (default): interactive mode.\n\n> For more information, see [Query execution modes](~~189502~~).",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
