{
  "name": "GetAsyncJobResult",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "JobId",
      "description": "The RequestId returned by the asynchronous operation. You can use this ID to query the actual result of the asynchronous operation.",
      "position": "Body",
      "type": "String",
      "required": true
    }
  ]
}
