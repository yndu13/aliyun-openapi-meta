{
  "name": "DescribePrepayBillTotal",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BillType",
      "description": "The bill type of the user. This parameter is required in practice. An error is returned if this parameter is not specified. Valid values:\n- elastic_traffic: elastic traffic\n- sdl: sensitive data leak detection traffic",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "CurrentPage",
      "description": "The page number in a paged query. Default value: 1.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "EndTime",
      "description": "The end time. Specify a UNIX timestamp in seconds.\n> Because billing data is aggregated at the daily granularity, the timestamp must correspond to 00:00:00 of the day in CST (UTC+8). If the timestamp is not aligned to the start of the day, no data may be returned.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Lang",
      "description": "The language. Enumerated value.\nDefault value: zh.\nValid value: en.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PageSize",
      "description": "The number of entries per page. Default value: 10.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "StartTime",
      "description": "The start time of the query. Specify a UNIX timestamp in seconds.\n\n> Because billing data is aggregated at the daily granularity, the timestamp must correspond to 00:00:00 of the day in CST (UTC+8). If the timestamp is not aligned to the start of the day, no data may be returned.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
