{
  "name": "GetTaskTemplateViews",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/openapi/api/v1/tenants/[TenantId]/tasks/[TaskId]/template/views",
  "parameters": [
    {
      "name": "TaskId",
      "description": "Job ID, which can be obtained from the Task Hub in iTAG.  ",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "TenantId",
      "description": "Tenant ID, which can be queried by using the ListTenantId API for querying the tenant list.  ",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
