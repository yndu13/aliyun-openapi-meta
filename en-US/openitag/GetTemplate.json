{
  "name": "GetTemplate",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/openapi/api/v1/tenants/[TenantId]/templates/[TemplateId]",
  "parameters": [
    {
      "name": "TemplateId",
      "description": "Template ID, which can be obtained from template management in iTAG.  ",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "TenantId",
      "description": "Tenant ID, which can be queried by invoking the ListTenantId API for the tenant list.  ",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
