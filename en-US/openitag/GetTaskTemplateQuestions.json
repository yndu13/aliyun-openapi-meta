{
  "name": "GetTaskTemplateQuestions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/openapi/api/v1/tenants/[TenantId]/tasks/[TaskId]/template/questions",
  "parameters": [
    {
      "name": "TaskId",
      "description": "Job ID, which can be obtained from the Task Hub in iTAG.  ",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "TenantId",
      "description": "Tenant ID, which can be queried by using the ListTenantId API to retrieve the tenant list.  ",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
