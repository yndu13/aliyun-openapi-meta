{
  "name": "DeleteProject",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/api/v1/instances/[InstanceId]/projects/[ProjectId]",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "The ID of the instance. You can call the ListInstances operation to get the instance ID.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "ProjectId",
      "description": "The ID of the project. You can call the ListProjects operation to get the project ID.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
