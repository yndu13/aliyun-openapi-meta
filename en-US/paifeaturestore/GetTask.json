{
  "name": "GetTask",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/instances/[InstanceId]/tasks/[TaskId]",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "The instance ID. Call the ListInstances API to obtain this ID.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "TaskId",
      "description": "The task ID. Call the ListTasks API to obtain this ID.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
