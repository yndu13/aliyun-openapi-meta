{
  "name": "GetProject",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/instances/[InstanceId]/projects/[ProjectId]",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "The ID of the instance. Call the `ListInstances` operation to get this ID.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "ProjectId",
      "description": "The ID of the project. Call the `ListProjects` operation to get this ID.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
