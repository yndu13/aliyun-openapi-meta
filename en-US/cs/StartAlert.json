{
  "name": "StartAlert",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/alert/[ClusterId]/alert_rule/start",
  "parameters": [
    {
      "name": "ClusterId",
      "description": "The cluster ID. You can call ListClusters to obtain the cluster ID.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "body",
      "description": "The request body parameters.",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ]
}
