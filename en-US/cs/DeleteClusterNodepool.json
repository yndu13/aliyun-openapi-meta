{
  "name": "DeleteClusterNodepool",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/clusters/[ClusterId]/nodepools/[NodepoolId]",
  "parameters": [
    {
      "name": "ClusterId",
      "description": "The cluster ID.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "NodepoolId",
      "description": "The node pool ID.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "force",
      "description": "Specifies whether to force delete the node pool.\n\n- true: Force deletes the node pool. If the node pool contains existing instances, the existing nodes are forcibly deleted.\n\n- false: Does not force delete the node pool. If the node pool contains existing instances, the node pool cannot be deleted, and an error is returned.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    }
  ]
}
