{
  "name": "GetClusterAuditProject",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/clusters/[clusterid]/audit",
  "parameters": [
    {
      "name": "clusterid",
      "description": "The ACK cluster ID.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
