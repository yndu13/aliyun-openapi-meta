{
  "name": "DescribeClusterNodes",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/clusters/[ClusterId]/nodes",
  "parameters": [
    {
      "name": "ClusterId",
      "description": "The cluster ID.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "instanceIds",
      "description": "The instance IDs of nodes. Separate multiple IDs with commas (,).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "nodeIps",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "nodeLabels",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "nodeNames",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "nodepool_id",
      "description": "The node pool ID.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "pageNumber",
      "description": "The page number of the current query.\n\nDefault value: 1.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "pageSize",
      "description": "The maximum number of records that can be displayed on each page. Valid values: [1, 100].\n\nDefault value: 10.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "state",
      "description": "The status of cluster nodes. Used to filter by node running status. Valid values:\n\n- `all`: does not filter by running status. All nodes are returned.\n- `running`: running nodes.\n- `removing`: nodes that are being removed.\n- `initial`: nodes that are being initialized.\n- `failed`: nodes that failed to be created.\n\nDefault value: `all`.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
