{
  "name": "DeleteSecret",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/pop/v1/sam/secret/secret",
  "parameters": [
    {
      "name": "NamespaceId",
      "description": "The ID of the namespace. If the secret is in the default namespace, you need to specify only the region ID, for example, `cn-beijing`.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "SecretId",
      "description": "The ID of the secret to delete. You can obtain the ID by calling the [ListSecrets](~~466929~~) operation.",
      "position": "Query",
      "type": "Long",
      "required": true
    }
  ]
}
