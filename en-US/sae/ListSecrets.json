{
  "name": "ListSecrets",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/pop/v1/sam/secret/secrets",
  "parameters": [
    {
      "name": "NamespaceId",
      "description": "The ID of the namespace that contains the Secret. For the default namespace, use the region ID, such as `cn-beijing`.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
