{
  "name": "UpdateSecret",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "PUT",
  "pathPattern": "/pop/v1/sam/secret/secret",
  "parameters": [
    {
      "name": "NamespaceId",
      "description": "The ID of the namespace where the Secret instance is located. If the instance is in the default namespace, specify only the region ID, such as `cn-beijing`.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "SecretData",
      "description": "The Secret data.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "SecretId",
      "description": "The ID of the Secret instance.",
      "position": "Query",
      "type": "Long",
      "required": true
    }
  ]
}
