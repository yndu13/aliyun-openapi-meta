{
  "name": "CreateAccount",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccountDescription",
      "description": "The description of the account. The description must be 2 to 256 characters in length and must start with a letter or a Chinese character. It can contain digits, Chinese characters, letters, underscores (_), and hyphens (-).\n>The description cannot start with `http://` or `https://`.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "AccountName",
      "description": "The name of the database account.\n\n> The name must be unique and can contain uppercase letters (supported only by MySQL), lowercase letters, digits, or underscores. For specific naming conventions, refer to the tutorials for each engine: [Create a MySQL account](~~96089~~), [Create a PostgreSQL account](~~96753~~), [Create an SQL Server account](~~95810~~), [Create a MariaDB account](~~97132~~).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "AccountPassword",
      "description": "The password of the database account.\n> * The password must be 8 to 32 characters in length.\n> * The password must contain at least three of the following character types: uppercase letters, lowercase letters, digits, and special characters (`!@#$%^&*()_+-=`).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "AccountType",
      "description": "The account type. Valid values:\n\n- **Normal** (default): standard account.\n- **Super**: privileged user. You can create at most one privileged user per instance.\n- **Sysadmin** (ApsaraDB RDS for SQL Server only): database account with SA permissions. Before creating this account, check whether the instance meets the [prerequisites](~~170736~~).\n- **GlobalRO** (ApsaraDB RDS for SQL Server only): global read-only account. You can create at most two global read-only accounts per instance. The database engine version must be SQL Server 2016 or later, and the instance type must be dedicated or general-purpose.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "CheckPolicy",
      "description": "The [account password policy](~~2845728~~) of the SQL Server instance. Valid values:\n- **true**: The policy is applied.\n- **false**: The policy is not applied.\n> - If you set this parameter to true, [configure the SQL Server account password policy](~~2848317~~) in advance.\n> - This parameter is not supported for SQL Server [shared](~~57184~~), [2008 R2](~~145468~~), or [Serverless](~~603466~~) instances.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "DBInstanceId",
      "description": "The instance ID. You can call [DescribeDBInstances](~~610396~~) to query the instance ID.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
