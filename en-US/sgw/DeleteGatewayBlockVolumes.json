{
  "name": "DeleteGatewayBlockVolumes",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "GatewayId",
      "description": "The ID of the gateway.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "IndexId",
      "description": "The ID of the index. You can call the DescribeGatewayBlockVolumes operation to query the ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "IsSourceDeletion",
      "description": "Specifies whether to delete the source data. Valid values:\n\n- true: When the iSCSI gateway volume is deleted, the data in the OSS bucket is also deleted. Proceed with caution.\n- false: When the iSCSI gateway volume is deleted, the data in the OSS bucket is not deleted. Default value: false.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    }
  ]
}
