{
  "name": "CreateExpressSync",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BucketName",
      "description": "The name of the bucket. Note:\n\n- You can specify only one OSS bucket for a synchronization group. All data changes in the bucket are synchronized to your on-premises environment.\n- If you have not created a share for the OSS bucket, you must first create a share between the file gateway and the OSS bucket.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "BucketPrefix",
      "description": "The prefix of the subdirectory in the bucket. If you want to synchronize data changes in a specific subdirectory of the bucket, you can specify the subdirectory.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "BucketRegion",
      "description": "The region where the OSS bucket is located.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Description",
      "description": "The description of the express synchronization group. The description can be up to 255 characters in length.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Name",
      "description": "The name of the express synchronization group. The name must be 1 to 128 characters in length and can contain letters, Chinese characters, digits, periods (.), underscores (\\_), and hyphens (-). The name must start with a letter or a Chinese character.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
