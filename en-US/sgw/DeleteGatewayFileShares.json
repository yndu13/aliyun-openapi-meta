{
  "name": "DeleteGatewayFileShares",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Force",
      "description": "Specifies whether to force delete the file share. Valid values:\n\n- true: Force deletes the file share even if data synchronization is incomplete. Use this option with caution.\n\n- false (default): Does not delete the file share if data synchronization is incomplete.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "GatewayId",
      "description": "The ID of the gateway.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "IndexId",
      "description": "The ID of the file share. You can call the DescribeGatewayFileShares operation to query the IDs of file shares.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
