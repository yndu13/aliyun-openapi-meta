{
  "name": "RemoveUserFromVpcEndpointService",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The **ClientToken** value can contain only ASCII characters.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform a dry run. Valid values:\n\n- **true**: performs a dry run. The system checks the required parameters, request syntax, and limits. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned. The dry run checks whether the AccessKey pair is valid, whether the Resource Access Management (RAM) user is granted the required permissions (authorization), and whether the required parameters are specified.\n\n- **false** (default): sends a Normal request. If the request passes the check, a 2xx HTTP status code is returned and the user account is removed from the service whitelist.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID of the endpoint service from which you want to remove the service whitelist. You can call the [DescribeRegions](~~120468~~) operation to query the region ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ServiceId",
      "description": "The ID of the endpoint service.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "UserARN",
      "description": "The user whitelist in ARN format.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "UserId",
      "description": "The ID of the account to remove from the service whitelist.",
      "position": "Query",
      "type": "Long",
      "required": false
    }
  ]
}
