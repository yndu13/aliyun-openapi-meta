{
  "name": "DetachResourceFromVpcEndpointService",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. **ClientToken** supports only ASCII characters.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform a dry run. Valid values:\n- **true**: performs a dry run without removing the service resource. The system checks the required parameters, request syntax, and business restrictions. If the check fails, the corresponding error is returned. If the check succeeds, the error code `DryRunOperation` is returned.\n- **false** (default): performs a dry run and sends the request. If the check succeeds, an HTTP 2xx status code is returned and the operation is performed.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID of the endpoint service from which you want to remove a service resource. You can call the [DescribeRegions](~~120468~~) operation to query the region ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ResourceId",
      "description": "The service resource ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ResourceType",
      "description": "The type of the service resource. Valid values:\n\n- **slb**: Classic Load Balancer (CLB).\n\n- **alb**: Application Load Balancer (ALB).\n\n- **nlb**: Network Load Balancer (NLB).\n- **gwlb**: Gateway Load Balancer (GWLB).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ServiceId",
      "description": "The ID of the endpoint service from which you want to remove a service resource.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ZoneId",
      "description": "The zone of the endpoint service from which you want to remove a service resource. You can call the [DescribeRegions](~~120468~~) operation to query the region ID. ZoneId is required when the service resource type is ALB, NLB, or GWLB.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
