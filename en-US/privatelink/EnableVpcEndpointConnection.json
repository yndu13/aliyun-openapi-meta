{
  "name": "EnableVpcEndpointConnection",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Bandwidth",
      "description": "The bandwidth of the endpoint connection. Unit: Mbit/s.\n\n> The valid values vary based on the service resource type:\n> - NLB: 100 to 50000.\n> - ALB: 100 to 25000.\n> - CLB: 100 to 10240. Default value: 3072.\n> - GWLB: 100 to 25000.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DryRun",
      "description": "Specifies whether to perform only a dry run, without performing the actual request. Valid values:\n\n- **true**: performs only a dry run. The system checks the request for potential issues, including missing parameter values, incorrect request syntax, and service limits. If the request fails the dry run, an error code is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n\n- **false** (default): performs a dry run and performs the actual request. If the request passes the dry run, an HTTP 2xx status code is returned and the endpoint connection request is accepted.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "EndpointId",
      "description": "The endpoint ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region ID of the endpoint connection to accept.\n\nYou can call the [DescribeRegions](~~120468~~) operation to query the most recent region list.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ServiceId",
      "description": "The endpoint service ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "TrafficControlMode",
      "description": "- Scalability: automatic scaling. In this pattern, the bandwidth settings for the endpoint connection do not take effect.\n\n- BandwidthLimit: supports settings a bandwidth limit for the endpoint connection. The bandwidth limit is the Bandwidth value.\n \n>- When the service resource is NLB, TrafficControlMode is set to Scalability by default. You can set it to BandwidthLimit and modify the Bandwidth value to set a bandwidth limit.\n>- When the service resource is CLB, TrafficControlMode supports only BandwidthLimit, which indicates that the service consumer provides a default bandwidth limit for each endpoint.\n> - When the service resource is GWLB, TrafficControlMode supports only Scalability.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
