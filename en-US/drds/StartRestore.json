{
  "name": "StartRestore",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BackupDbNames",
      "description": "The list of database names to restore. Separate multiple database names with commas (,).\n> If you do not specify any database names, all databases in the instance are restored by default.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "BackupId",
      "description": "The ID of the DRDS backup set.\n> If you do not specify this parameter, the restoration is performed based on the point in time specified by PreferredBackupTime.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "BackupLevel",
      "description": "The backup level. Valid values:\n\n* db: database level\n* instance: instance level.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "BackupMode",
      "description": "The backup mode. Valid values:\n\n* logic: logical backup\n* phy: fast backup.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "DrdsInstanceId",
      "description": "The instance ID of the DRDS instance.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "PreferredBackupTime",
      "description": "The restoration time of the instance. The value is in the `yyyy-MM-dd HH:mm:ss` format.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
