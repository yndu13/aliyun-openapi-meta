{
  "name": "UpdateMFAAuthenticationSettings",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AllowedVerificationTypes",
      "position": "Query",
      "type": "Json",
      "required": false
    },
    {
      "name": "DirectoryId",
      "description": "The directory ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "MFAAuthenticationSettings",
      "description": "The global MFA settings. Valid values:\n\n- Enabled: MFA verification is enabled for all users.\n- Byuser: MFA verification depends on the individual MFA settings of each user. For more information about individual user MFA settings, see [UpdateUserMFAAuthenticationSettings](~~450135~~).\n- Disabled: MFA verification is disabled for all users.\n- OnlyRiskyLogin: MFA verification is required only for unusual logon attempts.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "OperationForRiskLogin",
      "description": "The action to take when the MFA settings option is set to verify only for unusual logon attempts. Valid values:\n\n- Autonomous: Users can skip MFA binding during unusual logon, but users who have already bound MFA must complete MFA verification.\n- EnforceVerify: Users who have not bound MFA are required to bind it, and users who have already bound MFA must complete verification.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
