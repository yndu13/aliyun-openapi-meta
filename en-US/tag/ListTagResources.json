{
  "name": "ListTagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Category",
      "description": "The tag type. Valid values:\n\n- Custom: custom tag.\n- System: system tag.\n- All: all tags.\n\nDefault value: All.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "NextToken",
      "description": "The token for the next query.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PageSize",
      "description": "The number of entries per page.\n\nMaximum value: 1000. Default value: 50.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID. Valid values:\n\n- For centrally deployed cloud services, specify the region ID based on [Regions supported by centrally deployed cloud services that support tags](~~2579691~~).\n- For non-centrally deployed cloud services, specify the region ID of the resource.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ResourceARN",
      "description": "The Alibaba Cloud Resource Name (ARN) of the resource.\n\nValid values of N: 1 to 50.\n\nThe resource ARN format: `arn:acs:${ProductCode}:${Region}:${Account}:${ResourceType}/${ResourceId}`. The following list describes the fields:\n\n- `ProductCode`: the cloud service code. You can obtain the value from the response of [ListSupportResourceTypes](~~2330915~~).\n- `Region`: the region ID of the resource. You can use an asterisk (\\*) to indicate the current region.\n- `Account`: the Alibaba Cloud account ID. You can use an asterisk (\\*) to indicate the current Alibaba Cloud account.\n- `ResourceType`: the resource type. You can obtain the value from the response of [ListSupportResourceTypes](~~2330915~~).\n- `ResourceId`: the resource ID.\n\n> ResourceARN supports the `ProductCode` and `ResourceType` values of Resource Group, ActionTrail, and Resource Management.",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    },
    {
      "name": "Tags",
      "description": "The tag keys and tag values. You can specify 1 to 10 key-value pairs.\n\nIf you specify multiple tags, the system queries resources that are bound to all the specified tags.\n\nLimits:\n\n- A tag key must be 1 to 128 characters in length.\n- A tag value must be 1 to 128 characters in length.\n- Tag keys and tag values are case-sensitive.\n- Each tag key on a resource can have only one tag value. If you add a tag that has the same key as an existing tag, the new tag value overwrites the existing tag value.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
