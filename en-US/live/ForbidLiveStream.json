{
  "name": "ForbidLiveStream",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppName",
      "description": "The name of the application to which the ingest stream belongs. You can view the AppName on the [Stream Management](~~197397~~) page.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "DomainName",
      "description": "The ingest domain.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "LiveStreamType",
      "description": "Specifies whether to disable stream ingest or streaming. Currently, only disabling stream ingest is supported: **publisher**.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Oneshot",
      "description": "Specifies whether to only interrupt the stream without adding it to the blacklist. Valid values:\n\n- **yes**: Only interrupts the stream without adding it to the blacklist (supports upstream ingest or upstream streaming).\n\n- **no**: Interrupts the stream and adds it to the blacklist.\n\n> Default value: no.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ResumeTime",
      "description": "The time to resume the stream. Format: yyyy-MM-ddTHH:mm:ssZ (UTC).\n\n> - If the **Oneshot** parameter is set to **no** and ResumeTime is not specified, the live stream is disabled for 6 months by default.\n> - If a value is specified, the restriction is lifted at the time specified by ResumeTime and the live stream is resumed.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "StreamName",
      "description": "The name of the ingest stream. You can view the StreamName on the [Stream Management](~~197397~~) page.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
