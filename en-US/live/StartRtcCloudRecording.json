{
  "name": "StartRtcCloudRecording",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "description": "The ID of the app to which the channel to be recorded belongs. The app must belong to the Alibaba Cloud account that calls this operation.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ChannelId",
      "description": "The ID of the channel to be recorded. Make sure that the channel has active users when you call this operation. Otherwise, the recording task fails to be created.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "MaxIdleTime",
      "description": "The idle timeout period. When the task remains idle for longer than MaxIdleTime, the task is automatically stopped. Unit: seconds. Valid values: 10 to 14400 (maximum 4 hours). Default value: 300.\n- In stream mixing recording mode, the task is considered idle when all subscribed user streams stop stream ingest.\n- In individual stream recording mode, the subscribed streams are independent of each other. When any stream stops stream ingest, it is considered idle. After MaxIdleTime is reached, the recording of that stream is stopped. When all subscribed streams have been idle beyond the timeout, the entire cloud recording task is stopped.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "MixLayoutParams",
      "description": "The layout parameters. Leave this parameter empty in single-stream recording mode. This parameter is required in stream mixing recording mode when the recording output is expected to contain non-audio-only files.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "MixTranscodeParams",
      "description": "The transcoding parameters. Leave this parameter empty in single-stream recording mode. This parameter is required in stream mixing recording mode.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "NotifyAuthKey",
      "description": "The authentication key for callback messages. If this parameter is left empty (default), no authentication is performed. If specified, the value must be 16 to 64 characters in length and can contain only uppercase and lowercase letters and digits.\n- If NotifyUrl is not specified, NotifyAuthKey is also invalid.\n- If a valid NotifyAuthKey is specified, authentication content is included in the callback messages.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "NotifyFileUploadedFormat",
      "description": "The specified formats for which a callback message is sent when the recording file generation event (RecordFileUploaded) is triggered.",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    },
    {
      "name": "NotifyUrl",
      "description": "The URL for receiving callback messages. Task status messages are sent to this URL in JSON format by using the POST method. The maximum length is 2048 characters.\n\nFor more information about callback messages, see [Documentation](https://www.alibabacloud.com/help/en/live/on-cloud-recording-callback-message).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RecordParams",
      "description": "The recording parameters.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "StorageParams",
      "description": "The storage parameters.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "SubscribeParams",
      "description": "The subscription parameters.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
