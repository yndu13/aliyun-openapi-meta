{
  "name": "AddLiveStreamWatermarkRule",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "App",
      "description": "The AppName of the live stream.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Description",
      "description": "The description of the custom rule.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Domain",
      "description": "The streaming domain.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Name",
      "description": "The name of the custom rule.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region ID.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Stream",
      "description": "The stream name. The following rules apply:\n\n- To match a specific stream, enter the full stream name. For example, liveStreamA.\n\n- You can use a wildcard match. The asterisk (`*`) matches all streams.\n\n- You can perform prefix and suffix matching.\n\n> * You can use only one asterisk (`*`). The asterisk must be at the beginning or end of the string. The matching items must be enclosed in parentheses (`()`) and separated by a vertical bar (`|`).\n>\n> * For example, `*(t1|t2)` matches all streams that end with `t1` or `t2`. `(abc|123)*` matches all streams that start with `abc` or `123`.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "TemplateId",
      "description": "The ID of the watermark template.\n\n> Get the template ID from the response of the [AddLiveStreamWatermark](~~2848096~~) operation.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
