{
  "name": "CreateLiveDelayConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "App",
      "description": "The AppName of the live stream. You can specify an asterisk (\\*) to match all AppNames under the domain name.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "DelayTime",
      "description": "The duration for which the playback is delayed. The value must be an integer. Valid values: 16 to 3600. Unit: seconds.",
      "position": "Query",
      "type": "Integer",
      "required": true
    },
    {
      "name": "Domain",
      "description": "The main streaming domain.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region ID.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Stream",
      "description": "The name of the live stream. You can use the wildcard (\\*) to specify all StreamNames under the AppName.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "TaskTriggerMode",
      "description": "The trigger mode. Valid values:\n\n- **PUBLISH\\_ONLY**: Stream delay is triggered only by relevant parameters in the ingest URL.\n\n- **CONFIG\\_ONLY**: Stream delay is triggered only by the configuration. Parameters in the ingest URL are ignored.\n\n- **PUBLISH\\_CONFIG**: Stream delay is triggered by both the configuration and parameters in the ingest URL. Parameters in the ingest URL have a higher priority than the configuration.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
