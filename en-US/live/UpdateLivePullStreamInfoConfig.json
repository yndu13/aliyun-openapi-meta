{
  "name": "UpdateLivePullStreamInfoConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppName",
      "description": "The name of the application to which the live stream belongs.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "DomainName",
      "description": "The streaming domain used as the streamer's stream domain.\n> - When you specify DomainName, make sure that the domain is a live streaming domain and that the user calling this operation has the permission to operate on the specified domain.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "EndTime",
      "description": "The end time of stream pulling.\n\nThe interval between StartTime and EndTime must be within 7 days, and EndTime must be later than the current time. Format: <i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z (UTC).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region ID.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "SourceUrl",
      "description": "The full URL of the origin server where the live stream resides. You can specify multiple origin URLs separated by semicolons (;).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "StartTime",
      "description": "The start time of stream pulling.\n\nThe interval between StartTime and EndTime must be within 7 days. Format: <i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z (UTC).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "StreamName",
      "description": "The name of the live stream.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
