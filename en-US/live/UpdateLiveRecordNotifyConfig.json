{
  "name": "UpdateLiveRecordNotifyConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "The streaming domain of the streamer.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "NeedStatusNotify",
      "description": "Specifies whether recording task status callbacks are required. Valid values:\n\n- **true**: Recording task status callbacks are required.\n- **false** (default): Recording task status callbacks are not required.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "NotifyAuthKey",
      "description": "The callback authentication key. The key is 16 to 32 characters in length and can contain only letters and digits.\n>This parameter is required when the NotifyReqAuth parameter is set to **true**.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "NotifyReqAuth",
      "description": "Specifies whether to enable callback authentication. Valid values:\n\n- **true**: Enabled.\n\n- **false**: Disabled.\n\n>Default value: **false**. If this parameter is set to **true**, the NotifyAuthKey parameter is required.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "NotifyUrl",
      "description": "The callback URL for recording events, including event callbacks and status callbacks.\n\n> - The URL must start with `http://` or `https://`.\n> - To properly identify Chinese characters, spaces, and other special characters in the input, perform URL encoding.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "OnDemandUrl",
      "description": "The on-demand recording callback URL.\n\n> - The URL must start with `http://` or `https://`.\n> - To properly identify Chinese characters, spaces, and other special characters in the input, perform URL encoding.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
