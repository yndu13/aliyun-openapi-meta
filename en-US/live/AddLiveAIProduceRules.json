{
  "name": "AddLiveAIProduceRules",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "App",
      "description": "The name of the live streaming application. The name can be up to 256 characters long and can contain digits, uppercase and lowercase letters, hyphens (-), and underscores (\\_). The AppName must match the AppName in the ingest URL for the template to take effect.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Description",
      "description": "The description of the subtitle rule. The description can contain Chinese and English characters, digits, and special characters. It can be up to 128 characters in length.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Domain",
      "description": "The streaming domain.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "IsLazy",
      "description": "Specifies whether the rule is triggered by stream pulling. Valid values:\n\n- true: Subtitles are generated when stream pulling starts. If no stream is pulled for 5 minutes, subtitle generation stops. Subtitle generation resumes when stream pulling starts again.\n\n- false: Subtitles are generated when stream ingest starts, regardless of whether a stream is being pulled.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "LiveTemplate",
      "description": "The specifications of the output subtitles. Valid values:\n\n- Landscape low definition 360p (640 × 360): `lp_ld`\n\n- Portrait low definition 360p (360 × 640): `lp_ld_v`\n\n- Landscape standard definition 480p (854 × 480): `lp_sd`\n\n- Portrait standard definition 480p (480 × 854): `lp_sd_v`\n\n- Landscape high definition 720p (1280 × 720): `lp_hd`\n\n- Portrait high definition 720p (720 × 1280): `lp_hd_v`\n\n- Landscape ultra high definition 1080p (1920 × 1080): `lp_ud`\n\n- Portrait ultra high definition 1080p (1080 × 1920): `lp_ud_v`",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region ID.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "StudioName",
      "description": "The name of the virtual background template.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "SubtitleName",
      "description": "The name of the subtitle template.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Suffix",
      "description": "The suffix to match.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
