{
  "name": "AddLivePullStreamInfoConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppName",
      "description": "The custom name of the application.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "DomainName",
      "description": "The streaming domain where the ingested stream will be hosted and distributed from.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "EndTime",
      "description": "The time when stream pulling ends.\n\nThe time range specified by the StartTime and EndTime parameters cannot exceed seven days. The time must be later than the current time. Specify the value in the ISO 8601 standard in the *yyyy-MM-dd*T*HH:mm:ss*Z format. The time must be in UTC.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region ID.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "SourceUrl",
      "description": "The origin URL of the live stream. You can specify multiple URLs. Separate them with semicolons (;).\n\n> You can pull only live streams in the RTMP, FLV, HLS, and SRT formats.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "StartTime",
      "description": "The time when stream pulling starts. Specify the time in the ISO 8601 standard in the *yyyy-MM-dd*T*HH:mm:ss*Z format. The time must be in UTC.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "StreamName",
      "description": "The custom name of the live stream.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
