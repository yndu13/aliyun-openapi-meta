{
  "name": "StartRtcCloudTranscode",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "description": "The ID of the application to which the channel belongs. The ID can contain uppercase letters, lowercase letters, digits, underscores (\\_), and hyphens (-). The maximum length is 64 characters.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ChannelId",
      "description": "The ID of the channel to which the user to be transcoded belongs. The ID can contain uppercase letters, lowercase letters, digits, underscores (\\_), and hyphens (-). The maximum length is 64 characters.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "InputParam",
      "description": "The parameters for the input stream subscription.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "MaxIdleTime",
      "description": "The idle timeout period in seconds. If a task cannot subscribe to the specified streamer's stream and remains idle for longer than this period, the task automatically stops. The value must be an integer from 10 to 14,400. The default value is 300.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "OutputParams",
      "description": "The parameters for the transcoded output.",
      "position": "Query",
      "type": "Json",
      "required": true,
      "sub_parameters": [
        {
          "name": "ChannelId",
          "description": "The ID of the channel to which the transcoded stream is pushed. The ID can contain uppercase letters, lowercase letters, digits, underscores (\\_), and hyphens (-). The maximum length is 64 characters. (Pushing streams to a different channel is not supported. This setting is invalid.)",
          "position": "Query",
          "type": "String",
          "required": true
        },
        {
          "name": "TranscodeTemplate",
          "description": "The name of the transcoding template. Valid values:\n\n- alimcopy\n\n- lld\n\n- lsd\n\n- lhd\n\n- lud",
          "position": "Query",
          "type": "String",
          "required": true
        },
        {
          "name": "UserId",
          "description": "The user ID for the transcoded stream in the destination channel. This ID must be unique within the channel.",
          "position": "Query",
          "type": "String",
          "required": true
        },
        {
          "name": "UserToken",
          "description": "The token required to push the transcoded stream to the channel. For more information, see [Token-based authentication](https://www.alibabacloud.com/help/en/apsaravideo-live/latest/token-based-authentication).",
          "position": "Query",
          "type": "String",
          "required": true
        }
      ]
    }
  ]
}
