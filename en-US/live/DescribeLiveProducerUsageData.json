{
  "name": "DescribeLiveProducerUsageData",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "The streaming domain name of the cloud producer studio.\n\n- Supports single or batch domain name queries. Separate multiple domain names with commas (,) for batch queries.\n- If this parameter is left empty, merged data of all live streaming domain names is returned by default.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "EndTime",
      "description": "The end time. Specify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC.\n\n> The end time must be later than the start time.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Instance",
      "description": "The instance to query. Batch query is supported. Separate multiple instances with commas (,).\n\n> If this parameter is left empty, merged data of all instances is returned by default.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Interval",
      "description": "The time granularity of the queried data. Valid values: 3600 (1 hour) and 86400 (1 day). Unit: seconds.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Region",
      "description": "The region to which the domain name belongs. If this parameter is left empty, merged data of all regions is returned by default. Batch query is supported. Separate multiple regions with commas (,).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "SplitBy",
      "description": "The grouping key. You can specify one or more of the following: domain, region, instance, or type. Separate multiple values with commas (,). The specified fields will be grouped in the output.\n\n\n> If this parameter is left empty, only aggregated data is returned.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "StartTime",
      "description": "The start time. Specify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Type",
      "description": "The producer type. Batch query is supported. Separate multiple types with commas (,). Valid values:\n\n- **slidelive**: playlist-based.\n\n- **universal**: general-purpose.\n\n> If this parameter is left empty, merged data of all producer types is returned by default.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "app",
      "description": "The name of the application to which the stream belongs.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
