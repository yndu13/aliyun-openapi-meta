{
  "name": "UntagResources",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "All",
      "description": "Specifies whether to remove all tags from the specified resources. Valid values:\n\n- false\n\n- true\n\nDefault value: false.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The ID of the region where the resource share resides.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ResourceId",
      "description": "The IDs of the resource shares.\n\nYou can specify up to 20 resource share IDs.",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    },
    {
      "name": "ResourceType",
      "description": "The resource type.\n\nSet this parameter to `ResourceShare`, which indicates a resource share.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "TagKey",
      "description": "The tag keys.\n\nYou can remove up to 20 tag keys.\n\n> You do not need to specify tag keys if you set the `All` parameter to `true`.",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    }
  ]
}
