{
  "name": "ExecContainerCommand",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Command",
      "description": "The commands to execute in the container. You can specify up to 20 commands. Each command can be up to 256 characters long.<br>\nTo execute multiple commands, pass the string in JSON format. For example, `[\"/bin/sh\", \"-c\", \"ls -a\"]`.<br>",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ContainerGroupId",
      "description": "The instance ID, which is the container group ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ContainerName",
      "description": "The name of the container.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region ID of the instance.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Stdin",
      "description": "Specifies whether to enable standard input. The default value is true.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "Sync",
      "description": "Specifies whether to execute the command immediately and return the result synchronously. The default value is false.<br>\nIf you set this parameter to true, you must set TTY to false. This means Command cannot be `/bin/bash`.<br>",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "TTY",
      "description": "Specifies whether to enable interaction. The default value is false.<br>\nIf you set Command to `/bin/bash`, set this parameter to true.<br>",
      "position": "Query",
      "type": "Boolean",
      "required": false
    }
  ]
}
