{
  "name": "CreateInstanceOpsTask",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ContainerGroupId",
      "description": "The Elastic Container Instance (ECI) instance ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "OpsType",
      "description": "The type of the O&M task. Valid values:\n\n- coredump\n- tcpdump",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "OpsValue",
      "description": "The parameters of the O&M task. You can set the corresponding parameters based on the value of OpsType.\n- If OpsType is set to coredump, the valid values of OpsValue are:\n    - enable: enables coredump.\n    - disable: disables coredump.\n- If OpsType is set to tcpdump, OpsValue is a JSON-formatted parameter string, such as `{\"Enable\":true, \"IfDeviceName\":\"eth0\"}`. The following parameters are supported:\n    - Enable: specifies whether to enable tcpdump. This parameter is required. A value of true enables tcpdump, and a value of false disables tcpdump.\n    - Protocol: the network protocol. Valid values: tcp, udp, and icmpv4.\n    - SourceIp: the source IP address.\n    - SourceCidr: the source CIDR block in CIDR notation. If both an IP address and a CIDR block are specified, the IP address setting is ignored when the CIDR block is valid.\n    - SourcePort: the source port. Valid values: 1 to 65535.\n    - DestIp: the destination IP address.\n    - DestCidr: the destination CIDR block in CIDR notation. If both an IP address and a CIDR block are specified, the IP address setting is ignored when the CIDR block is valid.\n    - DestPort: the destination port. Valid values: 1 to 65535.\n    - IfDeviceName: the target network interface controller (NIC). Default value: eth0.\n    - Snaplen: the packet capture length. Default value: 65535 bytes.\n    - Duration: the capture duration. Unit: seconds.\n    - PacketNum: the target number of packets to capture.\n    - FileSize: the target file size of captured packets. Unit: bytes. Maximum value: 1073741824 bytes (1 GB).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region ID.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
