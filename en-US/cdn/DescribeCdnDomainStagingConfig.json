{
  "name": "DescribeCdnDomainStagingConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "The accelerated domain name. You can specify only one domain name in each request.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "FunctionNames",
      "description": "The list of feature names, separated by commas (,). For the features that can be configured, see [Feature list](~~388460~~). If the feature list that you specify is empty, the response returns empty configurations.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
