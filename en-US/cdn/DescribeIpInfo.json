{
  "name": "DescribeIpInfo",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "IP",
      "description": "The IP address to query. Batch queries are not supported.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
