{
  "name": "DescribeDomainCname",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "The accelerated domain names to query. Separate multiple domain names with commas (,). You can specify up to 30 domain names in a single request.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
