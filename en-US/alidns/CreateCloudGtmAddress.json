{
  "name": "CreateCloudGtmAddress",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "description": "The language of the response. Valid values:\n\n- zh-CN: Chinese.\n\n- en-US (default): English.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Address",
      "description": "The IP address or domain name.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "AttributeInfo",
      "description": "The attribution information of the address.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "AvailableMode",
      "description": "The switchover mode for the address when a health check is abnormal. Valid values:\n\n- auto: The system automatically manages the address status based on health check results. If an address is unhealthy, DNS resolution for it stops. If the address becomes healthy, DNS resolution resumes.\n\n- manual: You manually manage the address status. If you set an address to abnormal, DNS resolution for it stops. It does not resume even if the address becomes healthy. If you set an address to normal, DNS resolution for it resumes. If a healthy address becomes unhealthy, the system sends an alert but does not stop DNS resolution.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request. Make sure that the token is unique for each request. The token can contain a maximum of 64 ASCII characters.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "EnableStatus",
      "description": "The status of the address. Valid values:\n\n- enable: The address is enabled.\n\n- disable: The address is disabled.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "HealthJudgement",
      "description": "The condition for determining the health of the address. This parameter is required if you specify HealthTasks. Valid values:\n\n- any\\_ok: At least one health check is successful.\n\n- p30\\_ok: At least 30% of health checks are successful.\n\n- p50\\_ok: At least 50% of health checks are successful.\n\n- p70\\_ok: At least 70% of health checks are successful.\n\n- all\\_ok: All health checks are successful.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "HealthTasks",
      "description": "The health check tasks for the address.",
      "position": "Query",
      "type": "Json",
      "required": false,
      "sub_parameters": [
        {
          "name": "Port",
          "description": "The service port of the destination address for the health check. This parameter is not supported for health checks that use the ping protocol.",
          "position": "Query",
          "type": "Integer",
          "required": false
        },
        {
          "name": "TemplateId",
          "description": "The ID of the health check template.",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    },
    {
      "name": "ManualAvailableStatus",
      "description": "The availability status of the address when the health check-based switchover mode is set to **manual**. Valid values:\n\n- available: The address is available. In this state, DNS resolution for the address is normal. If a health check is abnormal, the system only sends an alert and does not stop DNS resolution.\n\n- unavailable: The address is unavailable. In this state, DNS resolution for the address is stopped. DNS resolution is not resumed even if a health check is normal.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Name",
      "description": "The name of the address.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Remark",
      "description": "The remarks about the address.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Type",
      "description": "The type of the address. Valid values:\n\n- IPv4\n\n- IPv6\n\n- domain",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
