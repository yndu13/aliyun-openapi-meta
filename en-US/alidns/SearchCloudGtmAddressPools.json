{
  "name": "SearchCloudGtmAddressPools",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "description": "The language of the response. Valid values:\n\n- zh-CN: Chinese.\n\n- en-US (default): English.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "AddressPoolName",
      "description": "The name of the address pool. Fuzzy match is supported.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "AddressPoolType",
      "description": "The type of the address pool. Exact match is supported. Valid values:\n\n- IPv4\n\n- IPv6\n\n- domain",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "AvailableStatus",
      "description": "The availability of the address pool. Exact match is supported. Valid values:\n\n- available: The address pool is available.\n\n- unavailable: The address pool is unavailable.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request. Generate a unique value from your client for this parameter. The client token can contain only ASCII characters and must be a maximum of 64 characters in length.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "EnableStatus",
      "description": "The status of the address pool. Exact match is supported. Valid values:\n\n- enable: The address pool is enabled.\n\n- disable: The address pool is disabled.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "HealthStatus",
      "description": "The health status of the address pool. Exact match is supported. Valid values:\n\nok: Normal. All addresses in the address pool are available.\n\nok\\_alert: Warning. Some addresses in the address pool are unavailable, but the address pool is still considered normal. In the warning state, available addresses are resolved as expected, while unavailable addresses are not.\n\nexceptional: Abnormal. Some or all addresses in the address pool are unavailable, and the address pool is considered abnormal.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PageNumber",
      "description": "The page number. The value starts from 1. The default value is 1.",
      "position": "Query",
      "type": "Integer",
      "required": true
    },
    {
      "name": "PageSize",
      "description": "The number of entries to return on each page. The maximum value is 100. The default value is 20.",
      "position": "Query",
      "type": "Integer",
      "required": true
    },
    {
      "name": "Remark",
      "description": "The remarks for the address pool. Fuzzy match is supported.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
