{
  "name": "CreateCloudGtmAddressPool",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "description": "The language of the response. Valid values:\n\n- zh-CN: Chinese.\n\n- en-US: English. This is the default value.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "AddressPoolName",
      "description": "The name of the address pool. The name helps you identify the purpose of the address pool.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "AddressPoolType",
      "description": "The type of the address pool. Valid values:\n\n- IPv4: The endpoint is an IPv4 address.\n\n- IPv6: The endpoint is an IPv6 address.\n\n- domain: The endpoint is a domain name.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request. Generate a unique token for each request. The token can be up to 64 ASCII characters in length.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "EnableStatus",
      "description": "The status of the address pool. Valid values:\n\n- enable: The address pool is enabled and can be used for DNS resolution if it passes health checks.\n\n- disable: The address pool is disabled and cannot be used for DNS resolution, regardless of its health check status.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "HealthJudgement",
      "description": "The health status condition of the address pool. Valid values:\n\n- any\\_ok: At least one address in the address pool is active.\n\n- p30\\_ok: At least 30% of the addresses in the address pool are active.\n\n- p50\\_ok: At least 50% of the addresses in the address pool are active.\n\n- p70\\_ok: At least 70% of the addresses in the address pool are active.\n\n- all\\_ok: All addresses in the address pool are active.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Remark",
      "description": "Remarks about the address pool. The remarks help you identify the scenario in which the address pool is used.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
