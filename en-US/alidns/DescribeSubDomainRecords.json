{
  "name": "DescribeSubDomainRecords",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "description": "The domain name.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Lang",
      "description": "The language of the request and response. Default value: **zh**. Valid values:\n\n- **zh**: Chinese\n\n- **en**: English",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Line",
      "description": "The DNS resolution line.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PageNumber",
      "description": "The page number. The value starts from **1**. Default value: **1**.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "PageSize",
      "description": "The number of entries to return on each page. Maximum value: **100**. Default value: **20**.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "SubDomain",
      "description": "For example, if you set SubDomain to a.www\\.example.com:\n\nIf you do not specify DomainName, the system queries the DNS records for the hostname a.www of the domain name example.com.\n\nIf you set DomainName to www\\.example.com, the system queries the DNS records for the hostname a of the domain name www\\.example.com.\n\nIf you set DomainName to \"a.www\\.example.com\", the system queries the DNS records for the hostname @ of the domain name a.www\\.example.com.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Type",
      "description": "The type of the DNS record. If you do not specify this parameter, all types of DNS records for the subdomain are returned.\n\nThe record types are case-insensitive and include the following: **A, MX, CNAME, TXT, REDIRECT\\_URL, FORWARD\\_URL, NS, AAAA, and SRV**.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "UserClientIp",
      "description": "The client IP address.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
