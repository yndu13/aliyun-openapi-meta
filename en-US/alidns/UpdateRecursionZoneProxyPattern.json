{
  "name": "UpdateRecursionZoneProxyPattern",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "A client token that ensures the request is idempotent. This token is generated by the client and must be unique for each request. The token can be up to 64 ASCII characters long.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ProxyPattern",
      "description": "Specifies whether to enable recursive proxy for subdomains. Valid values:\n\nzone: Disables recursive proxy. If a request is made for a subdomain that does not exist, the system returns an NXDOMAIN response.\nrecord: Enables recursive proxy. If a request is made for a subdomain that does not exist, the system queries the forwarding and recursion modules in order. The final result is used to respond to the request.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ZoneId",
      "description": "The unique ID of the zone.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
