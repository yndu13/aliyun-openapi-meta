{
  "name": "SearchCloudGtmInstanceConfigs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "description": "The language of the response. Valid values:\n\n- `zh-CN`: Chinese\n\n- `en-US` (default): English",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "AvailableStatus",
      "description": "The service availability status of the instance configuration. Valid values:\n\n- `available`: The service for the GTM access domain is available when the instance configuration is **enabled** and its health status is **Normal** or Alert.\n\n- `unavailable`: The service for the GTM access domain is unavailable when the instance configuration is **disabled** or its health status is **Exceptional**.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ClientToken",
      "description": "A client-generated token to ensure the idempotence of the request. The token must be unique across requests and can contain up to 64 ASCII characters.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "EnableStatus",
      "description": "The status of the instance configuration. Valid values:\n\n- `enable`: Enabled. The intelligent scheduling policy of the GTM instance is in effect.\n\n- `disable`: Disabled. The intelligent scheduling policy of the GTM instance is unavailable.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "HealthStatus",
      "description": "The health status of the instance configuration. Valid values:\n\n- `ok`: Normal. All address pools referenced by the GTM access domain are available.\n\n- `ok_alert`: Alert. Some address pools referenced by the GTM access domain are unavailable. In this state, DNS resolution continues for available address pools but stops for unavailable ones.\n\n- `exceptional`: Exceptional. All address pools referenced by the GTM access domain are unavailable. In this case, failover resolution uses the addresses from the non-empty address pool with the smallest sequence number to ensure clients receive a resolution result.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "InstanceId",
      "description": "The ID of the Global Traffic Manager (GTM) 3.0 instance.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PageNumber",
      "description": "The page number. Pages start from 1. The default value is 1.",
      "position": "Query",
      "type": "Integer",
      "required": true
    },
    {
      "name": "PageSize",
      "description": "The number of entries per page for a paged query. The maximum value is **100** and the default value is **20**.",
      "position": "Query",
      "type": "Integer",
      "required": true
    },
    {
      "name": "Remark",
      "description": "A note for the instance configuration.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ScheduleDomainName",
      "description": "The GTM access domain. It is formed by combining the host record (`ScheduleHostname`) with the primary or subdomain name (`ScheduleZoneName`).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ScheduleZoneName",
      "description": "The primary domain name (for example, `example.com`) or subdomain name (for example, `a.example.com`) of the GTM access domain. This is typically a domain name managed by Alibaba Cloud DNS under the same account that owns the GTM instance.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
