{
  "name": "Sign",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CertIdentifier",
      "description": "The unique identifier of the certificate. You can get this value by calling the [ListCert](~~455806~~) operation.\n\n- The identifier of an SSL certificate is typically in the format '{Certificate ID}-cn-hangzhou'.\n\n- For a PCA certificate, this is the Identifier from the corresponding private certificate.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "CustomIdentifier",
      "description": "A unique, user-defined identifier.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Message",
      "description": "The data to sign. The MessageType parameter determines the format of this data. If MessageType is set to RAW, Message is the raw data. If MessageType is set to BASE64, Message is the Base64-encoded raw data. If MessageType is set to DIGEST, Message is the message digest (hash value). If MessageType is set to BLIND, Message is the Base64-encoded blinded message.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "MessageType",
      "description": "The message type. Valid values:\n\n- `RAW` (default): The raw data.\n\n- `DIGEST`: The message digest (hash value) of the raw data.\n\n- `BASE64`: The Base64-encoded raw data.\n\n- `BLIND`: Enables blind signing. This is supported only for certificates that use an RSA algorithm.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "SigningAlgorithm",
      "description": "The signature algorithm. Valid values:\n\n- `SHA256withRSA`\n\n- `SHA256withRSA/PSS`\n\n- `SHA256withECDSA`\n\n- `SM3withSM2`\n\n- `SHA256withRSA/P7`",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "WarehouseId",
      "description": "The repository ID.\n\n> You can get this ID by calling the [ListCertWarehouse](~~455805~~) operation.",
      "position": "Query",
      "type": "Long",
      "required": false
    }
  ]
}
