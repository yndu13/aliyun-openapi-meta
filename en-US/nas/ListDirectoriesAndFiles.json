{
  "name": "ListDirectoriesAndFiles",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DirectoryOnly",
      "description": "Specifies whether to query only directories.\n\nValid values:\n\n- false (default): No. Both directories and files can be queried.\n- true: Yes. Only directories are queried.\n> When StorageType is set to All, DirectoryOnly must be set to true and cannot be set to false.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "FileSystemId",
      "description": "The file system ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "MaxResults",
      "description": "The number of directories or files included in each query result.\n\nValid values: 10 to 128.\n\nDefault value: 100.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "NextToken",
      "description": "The pagination token that is used in the next request to retrieve a new page of results. If the return results are truncated, you can use NextToken to initiate a new request to retrieve the content after the current truncation position.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Path",
      "description": "The absolute path of the specified directory.\n\nThe path must start with a forward slash (/) and must be an existing path in the mount target.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "StorageType",
      "description": "The storage class type.\n- InfrequentAccess: IA storage class.\n- Archive: Archive storage class.\n- All: queries data of all storage classes.\n> When StorageType is set to All, you must set DirectoryOnly to true.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
