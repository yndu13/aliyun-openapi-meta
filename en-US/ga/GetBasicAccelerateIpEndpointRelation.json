{
  "name": "GetBasicAccelerateIpEndpointRelation",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccelerateIpId",
      "description": "The accelerated IP address instance ID of the basic Global Accelerator (GA) instance.\n> **EndpointId** and **AccelerateIpId** cannot both be empty.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "AcceleratorId",
      "description": "The instance ID of the basic Global Accelerator (GA) instance.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The client token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** value as the **ClientToken** value. The **RequestId** value of each API request is different.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "EndpointId",
      "description": "The endpoint ID of the basic Global Accelerator (GA) instance.\n\n> **EndpointId** and **AccelerateIpId** cannot both be empty.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID of the Global Accelerator instance. Set the value to **cn-hangzhou**.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
