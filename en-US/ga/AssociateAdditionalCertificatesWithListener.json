{
  "name": "AssociateAdditionalCertificatesWithListener",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceleratorId",
      "description": "The instance ID of the Alibaba Cloud Global Accelerator (GA).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Certificates",
      "description": "The list of additional certificates.\n\nYou can specify up to 10 certificates at a time.",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "sub_parameters": [
        {
          "name": "Domain",
          "description": "The domain name for which the certificate takes effect. Each domain name can be bound to only one additional certificate.\n\nYou can specify up to 10 domain names at a time.",
          "position": "Query",
          "type": "String",
          "required": true
        },
        {
          "name": "Id",
          "description": "The certificate ID. Only server certificates are supported.\n\nYou can specify up to 10 certificate IDs at a time.",
          "position": "Query",
          "type": "String",
          "required": true
        }
      ]
    },
    {
      "name": "ClientToken",
      "description": "The client token that is used to ensure the idempotence of the request.\n\nGenerate a parameter value from your client to ensure uniqueness across different requests. ClientToken supports only ASCII characters.\n\n> If you do not specify this parameter, the system uses the **RequestId** of the API request as the **ClientToken**. The **RequestId** of each API request is different.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ListenerId",
      "description": "The instance ID of the listener. Only HTTPS listeners are supported.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region ID of the Alibaba Cloud Global Accelerator (GA) instance. Set the value to **cn-hangzhou**.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
