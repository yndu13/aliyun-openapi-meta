{
  "name": "DeleteStack",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DeleteOptions",
      "description": "Stack deletion options.",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    },
    {
      "name": "Parallelism",
      "description": "Maximum number of concurrent resource operations.\n\nDefault: empty. Accepts an integer >= 0.\n\n> - A value greater than 0 is used as-is. A value of 0 or empty imposes no limit on ROS stacks. For Terraform stacks, the Terraform default (typically 10) applies.\n>\n> - This value persists on the stack and affects subsequent operations such as updates.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "RamRoleName",
      "description": "The RAM role name. ROS assumes this role to create the stack and call Alibaba Cloud service APIs.<br> Even if you have stack operation permissions but lack RAM role permissions, ROS still assumes the role. Ensure the role follows the least-privilege principle.<br> If empty, ROS cannot assume the existing RAM role associated with the stack. To have ROS assume a role, specify this parameter. If no RAM role is available, ROS uses a temporary credential from your account credentials.<br> Maximum length: 64 bytes.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "The region ID of the stack. Call [DescribeRegions](~~131035~~) to query available regions.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RetainAllResources",
      "description": "Whether to retain all resources in the stack.\n\nValid values:\n\n- true\n\n- false (default)",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "RetainResources",
      "description": "Resources to retain.",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    },
    {
      "name": "StackId",
      "description": "The ID of the stack.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
