{
  "name": "ListChatMessages",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AgentVersion",
      "description": "Set to V2 to route requests to the ROS Agent V2 backend.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "MaxResults",
      "description": "A compatibility field. V2 does not return a paged message array, and the service does not consume this field.",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "NextToken",
      "description": "A compatibility field. V2 does not return a paged message array, and the service does not consume this field.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "SessionId",
      "description": "The session ID to query. You can only query sessions owned by the current user.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
