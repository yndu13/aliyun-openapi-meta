{
  "name": "SegmentHead",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ImageURL",
      "description": "The URL of the image. We recommend that you use an Object Storage Service (OSS) link in the Shanghai region. If the file is stored locally or in an OSS bucket outside the Shanghai region, see [File URL processing](~~155645~~).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ReturnForm",
      "description": "The format of the returned image.\n\n- If you set this parameter to `mask`, a single-channel mask is returned.\n- If you leave this parameter empty or set it to any value other than mask, a four-channel PNG image is returned.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
