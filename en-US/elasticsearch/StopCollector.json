{
  "name": "StopCollector",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/openapi/collectors/[ResId]/actions/stop",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "A client token that is used to ensure the idempotence of the request. The value is generated by the client and must be unique among different requests. The maximum length is 64 printable ASCII characters.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ResId",
      "description": "The collector ID.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
