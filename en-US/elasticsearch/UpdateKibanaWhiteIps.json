{
  "name": "UpdateKibanaWhiteIps",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "PATCH|POST",
  "pathPattern": "/openapi/instances/[InstanceId]/kibana-white-ips",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "The instance ID.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "body",
      "description": "The request body.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "clientToken",
      "description": "A unique token generated by the client to ensure the idempotence of the request. The value must be unique across different requests and can be up to 64 ASCII characters in length.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "modifyMode",
      "description": "The modification mode. Valid values:\n\n- Cover (default): Overwrites the original IP whitelist with the value of the ips parameter.\n\n- Append: Adds the IP addresses specified in the ips parameter to the original IP whitelist.\n\n- Delete: Removes the IP addresses specified in the ips parameter from the original IP whitelist. At least one IP address must be retained.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
