{
  "name": "MigrateToOtherZone",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/openapi/instances/[InstanceId]/actions/migrate-zones",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "The instance ID.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "dryRun",
      "description": "Specifies whether to perform a dry run of the zone node migration. A value of true indicates that only validation is performed without executing the migration task. A value of false indicates that the migration task is executed after validation passes.",
      "position": "Query",
      "type": "Boolean",
      "required": true
    }
  ]
}
