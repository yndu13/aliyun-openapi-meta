{
  "name": "UpdateLogstashDescription",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "PATCH|POST",
  "pathPattern": "/openapi/logstashes/[InstanceId]/description",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "The instance ID.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "clientToken",
      "description": "A unique token generated by the client to ensure idempotence of the request. The value must be unique across different requests and cannot exceed 64 ASCII characters in length.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
