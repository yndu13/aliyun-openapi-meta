{
  "name": "DescribePipelineManagementConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/openapi/logstashes/[InstanceId]/pipeline-management-config",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "The Logstash instance ID.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "clientToken",
      "description": "A unique token generated by the client to ensure idempotence of the request. The token can contain up to 64 ASCII characters and must be unique across different requests.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
