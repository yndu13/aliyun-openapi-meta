{
  "name": "UpdateCollectorName",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/openapi/collectors/[ResId]/actions/rename",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "A unique token generated by the client to ensure the idempotence of the request. The value must be unique among different requests and cannot exceed 64 ASCII characters in length.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ResId",
      "description": "The collector ID.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ]
}
