{
  "name": "StartCollector",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/openapi/collectors/[ResId]/actions/start",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "A unique token that is used to ensure idempotence. This value is generated by the client and must be unique among different requests. The value can be up to 64 ASCII characters in length.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ResId",
      "description": "The collector ID.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
