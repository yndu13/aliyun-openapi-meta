{
  "name": "RestartCollector",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/openapi/collectors/[ResId]/actions/restart",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "A unique token that is used to ensure the idempotence of the request. The client generates this value. The value must be unique among different requests and cannot exceed 64 ASCII characters in length.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ResId",
      "description": "The collector instance ID.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
