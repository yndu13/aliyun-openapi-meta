{
  "name": "ValidateConnection",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/openapi/logstashes/[InstanceId]/validate-connection",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "A unique token used to ensure idempotence of the request. The token is generated by the client and must be unique across different requests. The maximum length is 64 ASCII characters.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "InstanceId",
      "description": "The Logstash instance ID.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "body",
      "description": "The information about the Elasticsearch instance to connect to.",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ]
}
