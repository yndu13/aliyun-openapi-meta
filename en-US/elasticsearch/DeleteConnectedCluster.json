{
  "name": "DeleteConnectedCluster",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/openapi/instances/[InstanceId]/connected-clusters",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "The current instance ID.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "clientToken",
      "description": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "connectedInstanceId",
      "description": "The instance ID of the remote instance that is connected over the network.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
