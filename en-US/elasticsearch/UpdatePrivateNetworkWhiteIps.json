{
  "name": "UpdatePrivateNetworkWhiteIps",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/openapi/instances/[InstanceId]/private-network-white-ips",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "The instance ID.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "clientToken",
      "description": "A client token that is used to ensure the idempotence of the request. The value is generated by the client and must be unique among different requests. The maximum length is 64 ASCII characters.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "modifyMode",
      "description": "The modification mode. Valid values:\n\n- Cover (default): overwrites the original IP whitelist with the value of the ips parameter.\n\n- Append: adds the IP addresses specified in the ips parameter to the original IP whitelist.\n\n- Delete: removes the IP addresses specified in the ips parameter from the original IP whitelist. At least one IP address must be retained.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
