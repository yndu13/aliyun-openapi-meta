{
  "name": "DescribeConnectableClusters",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/openapi/instances/[InstanceId]/connectable-clusters",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "The current instance ID.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "alreadySetItems",
      "description": "Specifies whether to return instances that are already connected. Valid values:\n\n- true (default): The returned instance list includes instances that are already connected.\n- false: The returned instance list does not include instances that are already connected.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    }
  ]
}
