{
  "name": "UpdateTemplate",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/openapi/instances/[InstanceId]/templates/[TemplateName]",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "A client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "InstanceId",
      "description": "The instance ID.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "TemplateName",
      "description": "The template name. Valid values:\n- dynamicSettings: cluster dynamic configuration\n- indexTemplate: index template configuration\n- ilmPolicy: index lifecycle configuration\n- staticSettings: cluster static configuration",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "body",
      "description": "The template configuration content. For more information, see the RequestBody section below.",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ]
}
