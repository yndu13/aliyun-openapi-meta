{
  "name": "DeleteCollector",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/openapi/collectors/[ResId]",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "A unique token used to ensure idempotence. Generated by the client. The value must be unique across different requests and cannot exceed 64 ASCII characters in length.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ResId",
      "description": "The collector ID.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
