{
  "name": "AddSnapshotRepo",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/openapi/instances/[InstanceId]/snapshot-repos",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "The instance ID of the target instance for which you want to configure cross-cluster OSS repository Settings.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "body",
      "description": "The cross-cluster backup information, which specifies the reference instance.",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ]
}
