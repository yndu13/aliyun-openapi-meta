{
  "name": "CancelLogstashDeletion",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/openapi/logstashes/[InstanceId]/actions/cancel-deletion",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "The instance ID.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "clientToken",
      "description": "A unique token generated by the client to ensure idempotence of the request. The token must be unique across different requests and can contain up to 64 ASCII characters.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
