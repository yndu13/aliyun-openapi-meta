{
  "name": "ListAlternativeSnapshotRepos",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/openapi/instances/[InstanceId]/alternative-snapshot-repos",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "The instance ID.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "alreadySetItems",
      "description": "Specifies whether to return the OSS reference repositories that have already been added. Valid values:\n\n- true: Returns the already added repositories.\n- false: Does not return the already added repositories.\n\nDefault value: true.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    }
  ]
}
