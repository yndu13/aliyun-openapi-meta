{
  "name": "CreateScheduledScalingRule",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DurationMinutes",
      "description": "The duration (unit: minutes) of a single scheduled elastic scaling task.\n\n> The value must be at least 15 minutes.",
      "position": "Query",
      "type": "Integer",
      "required": true
    },
    {
      "name": "Enable",
      "description": "Specifies whether to enable or disable the scheduled policy. Valid values:\n\n- **true**: Enabled.\n- **false**: Disabled.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "FirstScheduledTime",
      "description": "The time when the scheduled policy starts to take effect.\n\nIf the schedule type is single execution, the start time must be at least 30 minutes later than the current time.\n\n\n\n><notice>To prevent the server from continuously performing scale-up and scale-down tasks, the interval between different scheduled tasks must be at least 60 minutes.></notice>",
      "position": "Query",
      "type": "Long",
      "required": true
    },
    {
      "name": "InstanceId",
      "description": "The instance ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region ID of the instance.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RepeatType",
      "description": "The repeat type. This parameter is required when ScheduleType is set to repeat. Valid values:\n      <li> Daily: timed scheduling every day.\n      <li> Weekly: timed scheduling every week.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ReservedPubFlow",
      "description": "The reserved production specification for scheduled elastic scaling (unit: MB/s).\n\n> At least one of ReservedPubFlow and ReservedSubFlow must be higher than the current specification.",
      "position": "Query",
      "type": "Integer",
      "required": true
    },
    {
      "name": "ReservedSubFlow",
      "description": "The reserved consumption specification for scheduled elastic scaling (unit: MB/s).\n> At least one of ReservedSubFlow and ReservedPubFlow must be higher than the current specification.",
      "position": "Query",
      "type": "Integer",
      "required": true
    },
    {
      "name": "RuleName",
      "description": "The name of the scheduled policy rule.\n\n> The name must be unique among all rules of the same instance.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ScheduleType",
      "description": "The schedule type. Valid values:\n\n- at: scheduled once only.\n\n- repeat: scheduled repeatedly.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "TimeZone",
      "description": "The time zone (Coordinated Universal Time).",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "WeeklyTypes",
      "description": "The days of the week. Multiple days are supported for repeated execution.",
      "position": "Query",
      "type": "Json",
      "required": false
    }
  ]
}
