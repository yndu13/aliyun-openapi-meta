{
  "name": "GetToken",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/tokens",
  "parameters": [
    {
      "name": "ExpireTime",
      "description": "Time-to-live (TTL) of the share link in seconds. Default is 604800. Minimum is 0.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "TrainingJobId",
      "description": "ID of the training task.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
