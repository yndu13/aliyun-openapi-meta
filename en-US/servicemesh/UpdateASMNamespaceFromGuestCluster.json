{
  "name": "UpdateASMNamespaceFromGuestCluster",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "K8sClusterId",
      "description": "The ID of the Kubernetes cluster from which to sync namespaces. The Kubernetes cluster is added to the service mesh that is specified by the ServiceMeshId parameter.",
      "position": "Body",
      "type": "String",
      "required": true
    },
    {
      "name": "ServiceMeshId",
      "description": "The Service Mesh (ASM) instance ID.",
      "position": "Body",
      "type": "String",
      "required": true
    }
  ]
}
