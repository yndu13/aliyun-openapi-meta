{
  "name": "ReActivateAudit",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EnableAudit",
      "description": "Specifies whether to recreate the ASM audit log. Valid values:\n\n- true: Recreates the ASM audit log.\n\n- false: Does not recreate the ASM audit log.",
      "position": "Body",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "ServiceMeshId",
      "description": "The ID of the ASM instance.",
      "position": "Body",
      "type": "String",
      "required": true
    }
  ]
}
