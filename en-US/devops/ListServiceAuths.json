{
  "name": "ListServiceAuths",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/organization/[organizationId]/serviceAuths",
  "parameters": [
    {
      "name": "organizationId",
      "description": "The organization ID. You can find this ID in your browser's address bar after logging in to the Alibaba Cloud DevOps console. The URL is in the format `https://devops.aliyun.com/organization/{organizationId}`.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "serviceAuthType",
      "description": "The service authorization type.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
