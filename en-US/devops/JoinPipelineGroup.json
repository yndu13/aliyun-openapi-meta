{
  "name": "JoinPipelineGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/organization/[organizationId]/pipelineGroups/join",
  "parameters": [
    {
      "name": "groupId",
      "description": "The pipeline group ID. A value of 0 indicates that the pipeline will be set to ungrouped.",
      "position": "Query",
      "type": "Long",
      "required": true
    },
    {
      "name": "organizationId",
      "description": "The enterprise identity, also known as the enterprise ID, is a string that can be obtained from the Alibaba Cloud DevOps access link, for example: https://devops.aliyun.com/organization/【OrganizationId】\n\n",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "pipelineIds",
      "description": "Pipeline IDs, separated by commas if multiple.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
