{
  "name": "UpdateMergeRequestPersonnel",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/api/v4/projects/[repositoryId]/merge_requests/[localId]/person/[personType]",
  "parameters": [
    {
      "name": "accessToken",
      "description": "The access token. This parameter is not required if you use an Alibaba Cloud AccessKey pair or Security Token Service (STS) for authentication.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "body",
      "description": "The request body.",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "localId",
      "description": "The local ID of the merge request. This ID is unique within the repository.",
      "position": "Path",
      "type": "Long",
      "required": true
    },
    {
      "name": "organizationId",
      "description": "The ID of the organization. You can find the organization ID in the URL for your organization's homepage on Alibaba Cloud DevOps, which is structured as `https://devops.aliyun.com/organization/{organizationId}`.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "personType",
      "description": "The type of personnel.\n\n- `REVIEWER`: A reviewer.\n\n- `SUBSCRIBER`: A subscriber.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "repositoryId",
      "description": "The ID of the repository.",
      "position": "Path",
      "type": "Long",
      "required": true
    }
  ]
}
