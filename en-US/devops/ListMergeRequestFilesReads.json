{
  "name": "ListMergeRequestFilesReads",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/api/v4/projects/merge_requests/diffs/files_read_infos",
  "parameters": [
    {
      "name": "accessToken",
      "description": "The personal access token. This parameter is not required if you authenticate using an Alibaba Cloud AccessKey pair (AK/SK) or temporary credentials from the Security Token Service (STS).",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "fromPatchSetBizId",
      "description": "The business ID of the patch set for the target branch.\n\n> In APIs for merge requests and comparisons, the `from` and `to` parameters can be counter-intuitive: `from` is the target branch and `to` is the source branch. This order matches the Git execution sequence. The UI reverses these terms for clarity.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "localId",
      "description": "The sequential ID of the merge request within the repository.",
      "position": "Query",
      "type": "Long",
      "required": true
    },
    {
      "name": "organizationId",
      "description": "The ID of the organization. You can find this ID in your Alibaba Cloud DevOps URL, for example, `https://devops.aliyun.com/organization/[organizationId]`.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "repositoryIdentity",
      "description": "The ID or full path of the repository.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "toPatchSetBizId",
      "description": "The business ID of the patch set for the source branch.\n\n> In APIs for merge requests and comparisons, the `from` and `to` parameters can be counter-intuitive: `from` is the target branch and `to` is the source branch. This order matches the Git execution sequence. The UI reverses these terms for clarity.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
