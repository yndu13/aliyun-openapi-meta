{
  "name": "DeletePipelineGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/organization/[organizationId]/pipelineGroups/[groupId]",
  "parameters": [
    {
      "name": "groupId",
      "description": "The pipeline group ID.",
      "position": "Path",
      "type": "Long",
      "required": true
    },
    {
      "name": "organizationId",
      "description": "The enterprise identity, also known as the enterprise ID, in string format. It can be obtained from the Alibaba Cloud DevOps access link, for example: https://devops.aliyun.com/organization/【OrganizationId】",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
