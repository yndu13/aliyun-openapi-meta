{
  "name": "ListProjectMembers",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/organization/[organizationId]/projects/[projectId]/listMembers",
  "parameters": [
    {
      "name": "organizationId",
      "description": "The ID of the organization. You can get this ID from the Alibaba Cloud DevOps URL, for example, `https://devops.aliyun.com/organization/{organizationId}`.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "projectId",
      "description": "The ID of the project, which is equivalent to `spaceIdentifier`.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "targetType",
      "description": "The resource type. The value must be Space.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
