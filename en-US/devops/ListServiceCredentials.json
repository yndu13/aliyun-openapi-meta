{
  "name": "ListServiceCredentials",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/organization/[organizationId]/serviceCredentials",
  "parameters": [
    {
      "name": "organizationId",
      "description": "The ID of the organization. You can get the organization ID from the URL of your organization in Alibaba Cloud DevOps, for example, `https://devops.aliyun.com/organization/{organizationId}`.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "serviceCredentialType",
      "description": "Filters the results by the service credential type.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
