{
  "name": "ListResourceMembers",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/organization/[organizationId]/[resourceType]/[resourceId]/members",
  "parameters": [
    {
      "name": "organizationId",
      "description": "The enterprise identifier, also known as the enterprise ID, is a string that can be obtained from the Alibaba Cloud DevOps access link, such as https://devops.aliyun.com/organization/【OrganizationId】.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "resourceId",
      "description": "Resource ID",
      "position": "Path",
      "type": "Long",
      "required": false
    },
    {
      "name": "resourceType",
      "description": "Resource type: pipeline for pipelines, hostGroup for host groups",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
