{
  "name": "GetWorkitemRelations",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/organization/[organizationId]/workitems/[workitemId]/getRelations",
  "parameters": [
    {
      "name": "organizationId",
      "description": "Organization ID",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "relationType",
      "description": "There are six possible values: BLOCKING, BLOCKED, ASSOCIATED, DUPLICATED, SUB, and PARENT. Only relationships of work item type will be retrieved.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "workitemId",
      "description": "Work item ID, same as workitemidentifier, the UUID of the work item",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
