{
  "name": "ListUserResources",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/api/v4/user/vision/user_resources",
  "parameters": [
    {
      "name": "accessToken",
      "description": "The personal access token. You do not need to specify this parameter if you use an AccessKey pair or an STS token for authentication.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "organizationId",
      "description": "The ID of the enterprise. This parameter is a string. You can obtain the enterprise ID from the URL of your enterprise on the Alibaba Cloud DevOps platform. The URL is in the following format: https\\://devops.aliyun.com/organization/{OrganizationId}.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "page",
      "description": "The page number of the page to return. The first page is page 1. The default value is 1.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "pageSize",
      "description": "The number of entries to return on each page. The valid values are 1 to 100. The default value is 20.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "userIds",
      "description": "The comma-separated list of Alibaba Cloud account IDs of users.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
