{
  "name": "GetWorkItemInfo",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/organization/[organizationId]/workitems/[workitemId]",
  "parameters": [
    {
      "name": "organizationId",
      "description": "Organization identifier, also known as organization ID, in string format. It can be obtained from the Alibaba Cloud DevOps access link, such as https://devops.aliyun.com/organizatio",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "workitemId",
      "description": "The work item ID, which is the same as the workitemIdentifier field and serves as the UUID of the work item. You can obtain it from the ListWorkItems API.",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
