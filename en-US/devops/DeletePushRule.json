{
  "name": "DeletePushRule",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "DELETE",
  "pathPattern": "/api/v4/projects/[repositoryId]/push_rule/[pushRuleId]",
  "parameters": [
    {
      "name": "accessToken",
      "description": "The personal access token. This parameter is not required if you use an Alibaba Cloud AccessKey pair (AccessKey ID and AccessKey secret) or Security Token Service (STS) for authentication.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "organizationId",
      "description": "The organization ID. You can obtain it from the URL in your browser's address bar after you log on to Alibaba Cloud DevOps. The URL has the format `https://devops.aliyun.com/organization/{organizationId}`.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "pushRuleId",
      "description": "The push rule ID.",
      "position": "Path",
      "type": "Long",
      "required": true
    },
    {
      "name": "repositoryId",
      "description": "The repository ID.",
      "position": "Path",
      "type": "Long",
      "required": true
    }
  ]
}
