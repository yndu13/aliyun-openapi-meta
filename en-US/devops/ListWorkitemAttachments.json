{
  "name": "ListWorkitemAttachments",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/organization/[organizationId]/workitem/[workitemIdentifier]/attachments",
  "parameters": [
    {
      "name": "organizationId",
      "description": "The ID of the enterprise. You can view the ID of an enterprise in the URL of the enterprise homepage in CloudFlow. The URL is in the format of https\\://devops.aliyun.com/organization/{organizationId}.",
      "position": "Path",
      "type": "String",
      "required": false
    },
    {
      "name": "workitemIdentifier",
      "description": "The unique identifier of the work item.",
      "position": "Path",
      "type": "String",
      "required": false
    }
  ]
}
