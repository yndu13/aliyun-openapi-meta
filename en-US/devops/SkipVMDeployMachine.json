{
  "name": "SkipVMDeployMachine",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "PUT",
  "pathPattern": "/organization/[organizationId]/pipelines/[pipelineId]/deploy/[deployOrderId]/machine/[machineSn]/skip",
  "parameters": [
    {
      "name": "deployOrderId",
      "description": "Deployment order ID",
      "position": "Path",
      "type": "Long",
      "required": true
    },
    {
      "name": "machineSn",
      "description": "Machine SN",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "organizationId",
      "description": "Organization identity, also known as organization ID, in string format. It can be obtained from the Alibaba Cloud DevOps access link, such as https://devops.aliyun.com/organization/【OrganizationId】.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "pipelineId",
      "description": "Pipeline ID, which can be obtained from the pipeline link, such as https://flow.aliyun.com/pipelines/【PipelineId】/current.",
      "position": "Path",
      "type": "Long",
      "required": true
    }
  ]
}
