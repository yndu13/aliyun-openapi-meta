{
  "name": "SetSniConfig",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DefaultCertificate",
      "description": "The default certificate. This certificate is returned if no matching domain name is found in the server certificate list.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "MqttInstanceId",
      "description": "The instance ID.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "SniConfig",
      "description": "The Server Name Indication (SNI) configuration that maps domain names to certificates. Format: DomainName#CertificateID#Password(if any);DomainName#CertificateID#Password(if any)",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
