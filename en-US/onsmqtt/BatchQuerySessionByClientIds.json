{
  "name": "BatchQuerySessionByClientIds",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientIdList",
      "description": "The client IDs to query.",
      "position": "Query",
      "type": "RepeatList",
      "required": true
    },
    {
      "name": "InstanceId",
      "description": "The instance ID of ApsaraMQ for MQTT. Must match the instance ID used by the client. Find it on the **Instance Details** page in the [console](https://mqtt.console.aliyun.com).",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
