{
  "name": "CreateSanityCheckTask",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/api/v2/sanitychecks/[CheckType]",
  "parameters": [
    {
      "name": "CheckType",
      "description": "The check type. Valid values:\n\n- **SSH**: triggers an SSH connectivity health check for the instance.\n- **CustomService**: triggers a connectivity health check for a custom service on the instance.",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "body",
      "description": "The request body.",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ]
}
