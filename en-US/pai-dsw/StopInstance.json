{
  "name": "StopInstance",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "PUT",
  "pathPattern": "/api/v2/instances/[InstanceId]/stop",
  "parameters": [
    {
      "name": "InstanceId",
      "description": "The ID of the instance. For information about how to obtain an instance ID, see [ListInstances](~~470439~~).",
      "position": "Path",
      "type": "String",
      "required": true
    },
    {
      "name": "SaveImage",
      "description": "Specifies whether to save the environment before the instance is stopped.\n\n- true: The environment is saved before the instance is stopped.\n\n- false: The instance is stopped without saving the environment data.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    }
  ]
}
