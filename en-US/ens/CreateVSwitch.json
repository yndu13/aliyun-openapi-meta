{
  "name": "CreateVSwitch",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CidrBlock",
      "description": "The CIDR block of the VSwitch. The following requirements apply:\n\n- The prefix length must be an integer from 16 to 29.\n\n- The CIDR block of the VSwitch must be a subset of the CIDR block of the VPC to which the VSwitch belongs.\n\n- The CIDR block of the VSwitch cannot be the same as the destination CIDR block of an existing route in the VPC, but it can be a subset of the destination CIDR block.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Description",
      "description": "The description of the VSwitch.\n\nThe description must be 2 to 256 characters in length. It must start with a letter or a Chinese character, and cannot start with `http://` or `https://`.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "EnsRegionId",
      "description": "The ID of the ENS node.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Ipv6CidrBlock",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "NetworkId",
      "description": "The ID of the network to which the VSwitch belongs.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Tag",
      "description": "The tags of the resource. You can specify up to 20 tags.",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "Key",
          "description": "The key of the tag.\n\n- The tag key cannot start with `aliyun`, `acs:`, `http://`, or `https://`.\n\n- The tag key can be up to 64 characters long.\n\n- The tag key cannot be an empty string.",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "Value",
          "description": "The value of the tag. It can be up to 128 characters long, can be an empty string, and cannot start with `acs:` or contain `http://` or `https://`.",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    },
    {
      "name": "VSwitchName",
      "description": "The name of the VSwitch. The following requirements apply:\n\n- The name must be 2 to 128 characters in length.\n\n- The name must start with a letter or a Chinese character, and cannot start with `http://` or `https://`. The name can contain digits, colons (:), underscores (\\_), and hyphens (-).\n\nDefault value: an empty string.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
