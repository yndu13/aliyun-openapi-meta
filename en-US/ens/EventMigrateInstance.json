{
  "name": "EventMigrateInstance",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DataPolicy",
      "description": "The data migration policy. Valid values:\n\n- `abandon`: Does not migrate data. This is the default value.\n\n- `force_transfer`: Forces data migration.\n\n- `try_transfer`: Attempts to migrate data.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "EventId",
      "description": "The ID of the system event.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "OpsType",
      "description": "The type of the O\\&M task. Valid values:\n\n- `immediate`: Executes the task immediately.\n\n- `scheduled`: Executes the task at the specified time.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "Password",
      "description": "The instance password. This parameter is optional. If you do not specify a password, the system generates a random one.\n\nThe password must be 8 to 30 characters long and contain uppercase letters, lowercase letters, and digits.\n\nNote: This parameter is not supported for scheduled execution.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PlanTime",
      "description": "The scheduled execution time. This must be a Unix timestamp in milliseconds. This parameter is required when `OpsType` is set to `scheduled`.",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "PlanUtcTime",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ResourceId",
      "description": "The ID of the resource.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
