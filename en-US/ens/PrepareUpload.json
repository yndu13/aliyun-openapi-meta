{
  "name": "PrepareUpload",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BucketName",
      "description": "The bucket name.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ClientIp",
      "description": "The client's IP address. Use this parameter when the client's IP address differs from the IP address used to call the API. For example, a server obtains authorization and then sends it to a client.",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
