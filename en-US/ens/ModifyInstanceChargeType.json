{
  "name": "ModifyInstanceChargeType",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoPay",
      "description": "Specifies whether to automatically complete the payment when you switch the billing method of the instance from pay-as-you-go to subscription. Valid values:\n\ntrue: The payment is automatically completed. Make sure that your account balance is sufficient.\n\nfalse: Only an order is generated but no payment is made. This is the default value.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "AutoRenew",
      "description": "Specifies whether to enable auto-renewal when converting the instance from pay-as-you-go to subscription. Valid values:\n\n- true: Auto-renewal is enabled.\n- false: Auto-renewal is not enabled (default).",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "BillingCycle",
      "description": "The billing cycle. Valid values: Month, Day, and Hour.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "IncludeDataDisks",
      "description": "Specifies whether to convert all pay-as-you-go data cloud disks created with the instance to subscription data cloud disks when converting an instance-level pay-as-you-go instance to a subscription instance. Valid values:",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "InstanceChargeType",
      "description": "The target billing method of the instance. Valid values:",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "InstanceIds",
      "description": "The list of instance IDs.",
      "position": "Query",
      "type": "Json",
      "required": true
    },
    {
      "name": "Period",
      "description": "The subscription duration when converting a pay-as-you-go instance to subscription. This parameter is required in this scenario. Valid values:\n\nIf PeriodUnit is set to Day, Period can only be set to 3.\n\nIf PeriodUnit is set to Month, Period can be set to 1-9 or 12.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "PeriodUnit",
      "description": "The unit of the subscription duration when converting an instance-level pay-as-you-go instance to a subscription instance. This parameter is required in this scenario. Valid values:",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
