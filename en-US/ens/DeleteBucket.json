{
  "name": "DeleteBucket",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BucketName",
      "description": "The name of the bucket to delete. Only one bucket can be deleted at a time.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
