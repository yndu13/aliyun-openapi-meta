{
  "name": "ModifySnatEntry",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EipAffinity",
      "description": "Specifies whether to enable IP affinity. Default value: true. Valid values:\n\n- **false**\n\n- **true**\n\n> Description After you enable IP affinity, if multiple EIPs are associated with an SNAT entry, one client uses the same EIP to for communication. If IP affinity is disabled, the client uses a random EIP for communication.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "IspAffinity",
      "description": "Specifies whether to enable carrier affinity.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "SnatEntryId",
      "description": "The ID of the SNAT entry.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "SnatEntryName",
      "description": "The name of the SNAT entry. The name must be 1 to 128 characters in length and cannot start with `http://` or `https://`.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "SnatIp",
      "description": "Separate multiple EIPs in the SNAT entry with commas (,).",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
