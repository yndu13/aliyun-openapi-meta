{
  "name": "AssociateEnsEipAddress",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AllocationId",
      "description": "The ID of the EIP to be associated.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "InstanceId",
      "description": "The ID of the cloud product instance to attach the EIP to.\n>Load balancing (ELB) instance IDs are supported.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "InstanceType",
      "description": "The type of the EIP instance. Valid values:\n\n- **Nat**: NAT gateway.\n- **SlbInstance**: load balancing (ELB).\n- **NetworkInterface**: secondary network interface controller (NIC).\n- **NatSlbInstance**: select this value when attaching multiple EIPs to an ELB.\n- **EnsInstance** (default): ENS instance.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Standby",
      "description": "Specifies whether the EIP is a standby EIP. Valid values:\n\n- true: standby.\n\n- false: not standby.",
      "position": "Query",
      "type": "Boolean",
      "required": false
    }
  ]
}
