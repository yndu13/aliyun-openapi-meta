{
  "name": "CreateLoadBalancer",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BillingCycle",
      "description": "The billing cycle of the ELB instance.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ClientToken",
      "description": "Ensures the idempotency of the request to avoid duplicate operations caused by multiple retries.\n- Generate a parameter value from your client and ensure that the value is unique across different requests. The ClientToken value supports only ASCII characters and cannot exceed 64 characters in length.\n\n- If you use a ClientToken that has been used before and the other request parameters remain unchanged, the client receives the same result as the first submission, with no impact on your server-side state.\n\n- You can retry when the API times out or returns the PROCESSING error code, and idempotency takes effect. If the return result is an HTTP 200 status code, the retry returns the same result as the previous call with no impact on your server-side state. If the return result is an HTTP 4xx status code and the error code is not PROCESSING, idempotency does not take effect.\n- The ClientToken is valid for 10 minutes.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "EnsRegionId",
      "description": "The ID of the ENS edge zone.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "LoadBalancerName",
      "description": "The name of the load balancing instance. The name must be 1 to 80 characters in length and can contain letters and Chinese characters. If you do not specify this parameter, the system automatically assigns an instance name.\n\n> The name cannot start with `http://` or `https://`.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "LoadBalancerSpec",
      "description": "The specification of the load balancing instance.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "LoadBalancerType",
      "description": "The type of the Edge Load Balancing instance.\n\n- CLB: Classic Load Balancing instance.\n- InternetDR: Public pattern load balancing instance.",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "NetworkId",
      "description": "The network ID of the Edge Load Balancing (ELB) instance to create.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "PayType",
      "description": "The billing type. PostPaid (currently the only supported type): pay-as-you-go.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "VSwitchId",
      "description": "The ID of the vSwitch to which the VPC instance belongs.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
