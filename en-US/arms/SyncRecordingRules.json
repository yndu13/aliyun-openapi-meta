{
  "name": "SyncRecordingRules",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterId",
      "description": "The ID of the source cluster whose rules are to be synchronized.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "The region ID. This can be the same as the region ID of the source cluster.",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "TargetClusters",
      "description": "The IDs of the target clusters for batch synchronization.",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
