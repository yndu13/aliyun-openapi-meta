{
  "camel_example": "",
  "cmd_full_name": "rds describe-dedicated-hosts",
  "cmd_name": "describe-dedicated-hosts",
  "deprecated": false,
  "description_en": "Queries the information about the hosts in a dedicated cluster",
  "description_zh": "查询专属集群内的主机信息",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeDedicatedHosts",
  "operation": {
    "action": "DescribeDedicatedHosts",
    "api_style": "RPC",
    "api_version": "2014-08-15",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID. You can call the DescribeRegions operation to query the most recent region list",
      "help_zh": "地域ID。可以通过接口DescribeRegions查看地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": false,
      "type": "string"
    },
    {
      "example": "dhg-7a9xxxxxxxx",
      "help_en": "The dedicated cluster ID. You can call the DescribeDedicatedHostGroups operation to query the dedicated cluster ID",
      "help_zh": "专属集群ID。可调用DescribeDedicatedHostGroups获取",
      "location": "query",
      "name": "dedicated_host_group_id",
      "options": [
        "--dedicated-host-group-id"
      ],
      "raw_name": "DedicatedHostGroupId",
      "required": false,
      "type": "string"
    },
    {
      "example": "102565235",
      "help_en": "The order ID",
      "help_zh": "订单ID",
      "location": "query",
      "name": "order_id",
      "options": [
        "--order-id"
      ],
      "raw_name": "OrderId",
      "required": false,
      "type": "int"
    },
    {
      "example": "dhg_cloud_ssd",
      "help_en": "The storage type of the host. Valid values:\n\ndhg_cloud_ssd: enhanced SSD (ESSD)\ndhg_local_ssd: local SSD",
      "help_zh": "主机存储类型。取值：\ndhg_cloud_ssd：ESSD云盘。\ndhg_local_ssd：SSD本地盘",
      "location": "query",
      "name": "host_type",
      "options": [
        "--host-type"
      ],
      "raw_name": "HostType",
      "required": false,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The status of the host. Valid values:\n\n0: creating\n1: running\n2: faulty\n3: being replaced\n4: deprecated\n5: deleting\n6: restarting",
      "help_zh": "主机状态。取值：\n0：创建中\n1：使用中\n2：宕机\n3：宕机下线（替换主机中）\n4：下线\n5：删除\n6：重启中",
      "location": "query",
      "name": "host_status",
      "options": [
        "--host-status"
      ],
      "raw_name": "HostStatus",
      "required": false,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "Specifies whether instances can be deployed on the host. Valid values:\n\n0: Instances cannot be deployed on the host.\n1: Instances can be deployed on the host",
      "help_zh": "主机当前是否允许分配实例。取值：\n0：不允许分配。\n1：允许分配",
      "location": "query",
      "name": "allocation_status",
      "options": [
        "--allocation-status"
      ],
      "raw_name": "AllocationStatus",
      "required": false,
      "type": "string"
    },
    {
      "example": "cn-hangzhou-i",
      "help_en": "The zone ID",
      "help_zh": "可用区ID",
      "location": "query",
      "name": "zone_id",
      "options": [
        "--zone-id"
      ],
      "raw_name": "ZoneId",
      "required": false,
      "type": "string"
    },
    {
      "example": "ch-t4nn100ddxxxxxxxx",
      "help_en": "The ID of the host in the dedicated cluster",
      "help_zh": "专属集群主机ID",
      "location": "query",
      "name": "dedicated_host_id",
      "options": [
        "--dedicated-host-id"
      ],
      "raw_name": "DedicatedHostId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Rds",
  "protocol": "HTTP|HTTPS"
}
