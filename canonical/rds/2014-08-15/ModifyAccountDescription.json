{
  "camel_example": "",
  "cmd_full_name": "rds modify-account-description",
  "cmd_name": "modify-account-description",
  "deprecated": false,
  "description_en": "Modifies the description of a database account",
  "description_zh": "该接口用于修改数据库账号的描述信息",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ModifyAccountDescription",
  "operation": {
    "action": "ModifyAccountDescription",
    "api_style": "RPC",
    "api_version": "2014-08-15",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "rm-uf6wjk5****",
      "help_en": "The instance ID. You can call the DescribeDBInstances operation to query the instance ID",
      "help_zh": "实例ID。可调用DescribeDBInstances获取",
      "location": "query",
      "name": "db_instance_id",
      "options": [
        "--db-instance-id"
      ],
      "raw_name": "DBInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "test1",
      "help_en": "The username of the account. You can call the DescribeAccounts operation to obtain the username of the account",
      "help_zh": "账号名称。可调用DescribeAccounts获取",
      "location": "query",
      "name": "account_name",
      "options": [
        "--account-name"
      ],
      "raw_name": "AccountName",
      "required": true,
      "type": "string"
    },
    {
      "example": "testuser",
      "help_en": "The description of the account. The value must be 2 to 256 characters in length. The value can contain letters, digits, underscores (_), and hyphens (-), and must start with a letter.\n\nThe description cannot start with http:// or https://",
      "help_zh": "账号描述，长度为2~256个字符。以中文、英文字母开头，可以包含数字、中文、英文、下划线（_）、短横线（-）。\n不能以 http:// 和 https:// 开头",
      "location": "query",
      "name": "account_description",
      "options": [
        "--account-description"
      ],
      "raw_name": "AccountDescription",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Rds",
  "protocol": "HTTP|HTTPS"
}
