{
  "camel_example": "",
  "cmd_full_name": "rds describe-cross-backup-meta-list",
  "cmd_name": "describe-cross-backup-meta-list",
  "deprecated": false,
  "description_en": "Queries the databases and tables whose data is included in a cross-region backup file of an instance",
  "description_zh": "该接口用于查询RDS实例跨地域备份的库表信息",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeCrossBackupMetaList",
  "operation": {
    "action": "DescribeCrossBackupMetaList",
    "api_style": "RPC",
    "api_version": "2014-08-15",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "123456",
      "help_en": "The ID of the cross-region backup file that you want to use. You can call the https://help.aliyun.com/document_detail/121733.html operation to query the ID of the cross-region backup file",
      "help_zh": "跨地域备份集ID。可以通过接口DescribeCrossRegionBackups查询",
      "location": "query",
      "name": "backup_set_id",
      "options": [
        "--backup-set-id"
      ],
      "raw_name": "BackupSetId",
      "required": true,
      "type": "string"
    },
    {
      "example": "testdb1",
      "help_en": "The name of the database that you want to query. The system implements exact match based on the value of this parameter and returns the name of the matched database and the names of the tables in the matched database",
      "help_zh": "要查询的数据库名。精确匹配，会返回具体的数据库名以及库内的表名",
      "location": "query",
      "name": "get_db_name",
      "options": [
        "--get-db-name"
      ],
      "raw_name": "GetDbName",
      "required": false,
      "type": "string"
    },
    {
      "example": "test",
      "help_en": "The name of the database that you want to query. The system implements fuzzy match based on the value of this parameter and returns only the name of the matched database.\n\nYou can implement fuzzy match and then exact match. For example, you can set the Pattern parameter to test to query the testdb1 and testdb2 databases. Then, you can specify the GetDbName parameter to query only the matched database and the tables in the matched database",
      "help_zh": "要查询的数据库名。模糊匹配，只返回匹配的数据库名，不返回表名。\n您可以先模糊匹配，例如传入test匹配testdb1和testdb2，确定目标数据库名称后再精确匹配，通过传入GetDbName查看具体的数据库名和表名",
      "location": "query",
      "name": "pattern",
      "options": [
        "--pattern"
      ],
      "raw_name": "Pattern",
      "required": false,
      "type": "string"
    },
    {
      "example": "30",
      "help_en": "The number of entries to return per page. Default value: 1.\n\nThis parameter only takes effect when you specify the PageIndex parameter",
      "help_zh": "每页记录数。默认值：1。\n需要和PageIndex一起传入才生效",
      "location": "query",
      "name": "page_size",
      "options": [
        "--page-size"
      ],
      "raw_name": "PageSize",
      "required": false,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The number of the page to return. Valid values: any non-zero positive integer.\n\nThis parameter only takes effect when you specify the PageSize parameter",
      "help_zh": "页码，取值：大于0且不超过Integer的最大值。\n需要和PageSize一起传入才生效",
      "location": "query",
      "name": "page_index",
      "options": [
        "--page-index"
      ],
      "raw_name": "PageIndex",
      "required": false,
      "type": "string"
    },
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID of the instance",
      "help_zh": "实例所在地域",
      "location": "query",
      "name": "region",
      "options": [
        "--biz-region"
      ],
      "raw_name": "Region",
      "required": false,
      "type": "string"
    },
    {
      "example": "rg-acfmy****",
      "help_en": "The ID of the resource group",
      "help_zh": "资源组ID",
      "location": "query",
      "name": "resource_group_id",
      "options": [
        "--resource-group-id"
      ],
      "raw_name": "ResourceGroupId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Rds",
  "protocol": "HTTP|HTTPS"
}
