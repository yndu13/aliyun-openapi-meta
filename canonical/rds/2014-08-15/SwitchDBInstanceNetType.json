{
  "camel_example": "",
  "cmd_full_name": "rds switch-db-instance-net-type",
  "cmd_name": "switch-db-instance-net-type",
  "deprecated": false,
  "description_en": "Switches between internal and public endpoints of an instance in the classic network",
  "description_zh": "该接口用于切换经典网络实例的内外网地址",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "SwitchDBInstanceNetType",
  "operation": {
    "action": "SwitchDBInstanceNetType",
    "api_style": "RPC",
    "api_version": "2014-08-15",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "ETnLKlblzczshOTUbOCz****",
      "help_en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length",
      "help_zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符，且该参数值中不能包含非ASCII字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "rm-bp1****",
      "help_en": "The instance ID. You can call the DescribeDBInstances operation to query the instance ID",
      "help_zh": "实例ID。可调用DescribeDBInstances获取",
      "location": "query",
      "name": "db_instance_id",
      "options": [
        "--db-instance-id"
      ],
      "raw_name": "DBInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "new****",
      "help_en": "The prefix of the custom endpoint. The prefix must be 8 to 64 characters in length and can contain letters and digits. It must start with a lowercase letter. A valid endpoint is in the following format: Prefix.Database engine.rds.aliyuncs.com. Example: test1234.mysql.rds.aliyuncs.com",
      "help_zh": "自定义连接地址的前辍。由字母，数字组成，小写字母开头，8~64个字符。完整连接地址为：前缀.引擎名.rds.aliyuncs.com。例如test1234.mysql.rds.aliyuncs.com",
      "location": "query",
      "name": "connection_string_prefix",
      "options": [
        "--connection-string-prefix"
      ],
      "raw_name": "ConnectionStringPrefix",
      "required": true,
      "type": "string"
    },
    {
      "example": "3306",
      "help_en": "The number of the port that is used to connect to the instance. Valid values: 3001 to 3999",
      "help_zh": "端口号，取值：3001~3999",
      "location": "query",
      "name": "port",
      "options": [
        "--port"
      ],
      "raw_name": "Port",
      "required": false,
      "type": "string"
    },
    {
      "example": "Normal",
      "help_en": "The type of the endpoint. Valid values:\n\nNormal\nReadWriteSplitting\n\nBy default, the system returns both types of endpoints",
      "help_zh": "连接地址类型，取值：\nNormal：普通连接\nReadWriteSplitting：读写分离连接\n\n默认返回所有连接",
      "location": "query",
      "name": "connection_string_type",
      "options": [
        "--connection-string-type"
      ],
      "raw_name": "ConnectionStringType",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Rds",
  "protocol": "HTTP|HTTPS"
}
