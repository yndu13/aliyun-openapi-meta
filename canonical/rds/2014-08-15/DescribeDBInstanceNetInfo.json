{
  "camel_example": "",
  "cmd_full_name": "rds describe-db-instance-net-info",
  "cmd_name": "describe-db-instance-net-info",
  "deprecated": false,
  "description_en": "Queries all endpoints of an instance",
  "description_zh": "该接口用于查询RDS实例的所有连接地址信息",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeDBInstanceNetInfo",
  "operation": {
    "action": "DescribeDBInstanceNetInfo",
    "api_style": "RPC",
    "api_version": "2014-08-15",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "ETnLKlblzczshOTUbOC****",
      "help_en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length",
      "help_zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符，且该参数值中不能包含非ASCII字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "docRequired": true,
      "example": "rm-uf6wjk5****",
      "help_en": "The instance ID. You can call the DescribeDBInstances operation to query the instance ID",
      "help_zh": "实例ID。可调用DescribeDBInstances获取",
      "location": "query",
      "name": "db_instance_id",
      "options": [
        "--db-instance-id"
      ],
      "raw_name": "DBInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "test",
      "help_en": "A reserved parameter. You do not need to specify this parameter",
      "help_zh": "备用参数，无需配置",
      "location": "query",
      "name": "flag",
      "options": [
        "--flag"
      ],
      "raw_name": "Flag",
      "required": false,
      "type": "int"
    },
    {
      "example": "Normal",
      "help_en": "The type of the endpoint. Valid values:\n\nNormal: regular endpoint\nReadWriteSplitting: read/write splitting endpoint\n\nBy default, the system returns both types of endpoints",
      "help_zh": "连接地址类型，取值：\nNormal：普通连接地址。\nReadWriteSplitting：读写分离连接地址。\n\n默认返回所有类型连接地址",
      "location": "query",
      "name": "db_instance_net_rw_split_type",
      "options": [
        "--db-instance-net-rw-split-type"
      ],
      "raw_name": "DBInstanceNetRWSplitType",
      "required": false,
      "type": "string"
    },
    {
      "example": "rgc-2ze****",
      "help_en": "The name of the dedicated cluster to which the instance belongs. This parameter takes effect only when the instance runs MySQL on RDS Standard Edition and is created in a dedicated cluster",
      "help_zh": "专属集群MySQL通用版实例所属的组名",
      "location": "query",
      "name": "general_group_name",
      "options": [
        "--general-group-name"
      ],
      "raw_name": "GeneralGroupName",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Rds",
  "protocol": "HTTP|HTTPS"
}
