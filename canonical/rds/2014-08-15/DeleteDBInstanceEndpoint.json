{
  "camel_example": "",
  "cmd_full_name": "rds delete-db-instance-endpoint",
  "cmd_name": "delete-db-instance-endpoint",
  "deprecated": false,
  "description_en": "Deletes an endpoint for an instance that runs RDS Cluster Edition",
  "description_zh": "该接口用于删除RDS集群系列实例的Endpoint",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteDBInstanceEndpoint",
  "operation": {
    "action": "DeleteDBInstanceEndpoint",
    "api_style": "RPC",
    "api_version": "2014-08-15",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "0c593ea1-3bea-11e9-b96b-88****",
      "help_en": "The client token that is used to ensure the idempotency of requests. You can use the client to generate the value, but you must ensure that it is unique among different requests. The token can only contain ASCII characters and cannot exceed 64 characters in length",
      "help_zh": "该参数用于保证请求的幂等性，防止重复提交请求。参数值由客户端生成，保证在不同请求间唯一。\n\n该参数值由ASCII字符组成，最长不超过64个字符，不能包含非ASCII字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "rm-****",
      "help_en": "The instance ID. You can call the DescribeDBInstances operation to query the instance ID",
      "help_zh": "实例ID。可调用DescribeDBInstances查询",
      "location": "query",
      "name": "db_instance_id",
      "options": [
        "--db-instance-id"
      ],
      "raw_name": "DBInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "ep-****",
      "help_en": "The endpoint ID of the instance. You can call the DescribeDBInstanceEndpoints operation to query the endpoint ID",
      "help_zh": "实例的Endpoint ID。可调用DescribeDBInstanceEndpoints查询",
      "location": "query",
      "name": "db_instance_endpoint_id",
      "options": [
        "--db-instance-endpoint-id"
      ],
      "raw_name": "DBInstanceEndpointId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Rds",
  "protocol": "HTTP|HTTPS"
}
