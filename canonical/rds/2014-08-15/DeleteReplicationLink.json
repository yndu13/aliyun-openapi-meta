{
  "camel_example": "",
  "cmd_full_name": "rds delete-replication-link",
  "cmd_name": "delete-replication-link",
  "deprecated": false,
  "description_en": "Deletes the data synchronization link for a disaster recovery (DR) ApsaraDB RDS instance and promotes the DR instance to the primary instance",
  "description_zh": "该接口用于删除RDS灾备实例的数据同步链路，并将灾备实例提升为主实例",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteReplicationLink",
  "operation": {
    "action": "DeleteReplicationLink",
    "api_style": "RPC",
    "api_version": "2014-08-15",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "m-2zecuz9tolf******",
      "help_en": "The ID of the DR instance",
      "help_zh": "灾备实例ID",
      "location": "query",
      "name": "db_instance_id",
      "options": [
        "--db-instance-id"
      ],
      "raw_name": "DBInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to delete the data synchronization link between the DR instance and the primary instance and promote the DR instance to the primary instance. Valid values:\n\ntrue\nfalse",
      "help_zh": "是否删除主实例和灾备实例间的数据同步链路，并提升灾备实例为主实例，取值：\n\ntrue：是\nfalse：否",
      "location": "query",
      "name": "promote_to_master",
      "options": [
        "--promote-to-master"
      ],
      "raw_name": "PromoteToMaster",
      "required": true,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Rds",
  "protocol": "HTTP|HTTPS"
}
