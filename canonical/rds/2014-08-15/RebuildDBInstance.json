{
  "camel_example": "",
  "cmd_full_name": "rds rebuild-db-instance",
  "cmd_name": "rebuild-db-instance",
  "deprecated": false,
  "description_en": "Rebuilds the secondary instance of a primary instance in a dedicated cluster",
  "description_zh": "调用RebuildDBInstance接口重建专属集群中的RDS备实例",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "RebuildDBInstance",
  "operation": {
    "action": "RebuildDBInstance",
    "api_style": "RPC",
    "api_version": "2014-08-15",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID. You can call the DescribeRegions operation to query the most recent region list",
      "help_zh": "地域ID。可调用DescribeRegions获取",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": false,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "dhg-4nxxxxxxx",
      "help_en": "The dedicated cluster ID. You can call the DescribeDedicatedHostGroups operation to query the dedicated cluster ID",
      "help_zh": "专属集群ID。可调用DescribeDedicatedHostGroups获取",
      "location": "query",
      "name": "dedicated_host_group_id",
      "options": [
        "--dedicated-host-group-id"
      ],
      "raw_name": "DedicatedHostGroupId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "rm-uf6wjk5xxxxxxx",
      "help_en": "The instance ID",
      "help_zh": "专属集群内的实例ID",
      "location": "query",
      "name": "db_instance_id",
      "options": [
        "--db-instance-id"
      ],
      "raw_name": "DBInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "i-bpxxxxxxx",
      "help_en": "The ID of the host on which the system rebuilds the secondary instance.\n\nIf you do not specify this parameter, the system preferentially rebuilds the secondary instance on the original host on which the secondary instance resides. If the remaining storage of the original host is insufficient, the system rebuilds the secondary instance on a host on which the primary instance does not reside. If no suitable hosts are found, the system reports an error that indicates insufficient storage",
      "help_zh": "需要重建备实例的主机ID。\n不传入该参数时，备实例会优先在原主机上重建，如果原主机空间不足，系统会选择非主实例所在的主机，若还是找不到足够空间，会返回空间不足错误",
      "location": "query",
      "name": "dedicated_host_id",
      "options": [
        "--dedicated-host-id"
      ],
      "raw_name": "DedicatedHostId",
      "required": false,
      "type": "string"
    },
    {
      "example": "FOLLOWER",
      "help_en": "The role of the secondary instance that you want to rebuild. Valid values:\n\nFOLLOWER: secondary instance\nLOG: logger instance",
      "help_zh": "需要重建的备实例类型。取值：\nFOLLOWER：备节点。\nLOG：日志节点",
      "location": "query",
      "name": "rebuild_node_type",
      "options": [
        "--rebuild-node-type"
      ],
      "raw_name": "RebuildNodeType",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Rds",
  "protocol": "HTTP|HTTPS"
}
