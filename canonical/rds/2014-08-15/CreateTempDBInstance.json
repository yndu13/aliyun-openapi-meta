{
  "camel_example": "",
  "cmd_full_name": "rds create-temp-db-instance",
  "cmd_name": "create-temp-db-instance",
  "deprecated": false,
  "description_en": "Creates a temporary instance for an RDS instance that runs SQL Server 2008 R2 and uses local disks",
  "description_zh": "该接口用于为RDS SQL Server 2008 R2高性能本地盘实例创建临时实例",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CreateTempDBInstance",
  "operation": {
    "action": "CreateTempDBInstance",
    "api_style": "RPC",
    "api_version": "2014-08-15",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "example": "rm-bp13****",
      "help_en": "The instance ID",
      "help_zh": "实例ID",
      "location": "query",
      "name": "db_instance_id",
      "options": [
        "--db-instance-id"
      ],
      "raw_name": "DBInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "1883****",
      "help_en": "The backup set ID. You can call the DescribeBackups operation to query the backup set ID.\n\nYou must specify at least one of BackupId and RestoreTime parameters",
      "help_zh": "备份集ID。可通过接口DescribeBackups查询。\nBackupId和RestoreTime两者至少传入一个",
      "location": "query",
      "name": "backup_id",
      "options": [
        "--backup-id"
      ],
      "raw_name": "BackupId",
      "required": false,
      "type": "int"
    },
    {
      "example": "2023-06-11T16:00:00Z",
      "help_en": "The specified point in time within the backup retention period. Specify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC.\n\nThe time can be set to a point in time within the last seven days and must be more than 30 minutes earlier than the current time. The default time zone is UTC.\nYou must specify at least one of the BackupId and RestoreTime parameters",
      "help_zh": "用户指定备份保留时间内的任意时间点。格式：<i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z（UTC时间）。\n\n可以设置为7天之内并且早于当前时间半小时以上的任意时间点，默认时区为UTC。\nBackupId和RestoreTime两者至少传入一个",
      "location": "query",
      "name": "restore_time",
      "options": [
        "--restore-time"
      ],
      "raw_name": "RestoreTime",
      "required": false,
      "type": "string"
    },
    {
      "example": "rg-acfmy****",
      "help_en": "The resource group ID",
      "help_zh": "资源组ID",
      "location": "query",
      "name": "resource_group_id",
      "options": [
        "--resource-group-id"
      ],
      "raw_name": "ResourceGroupId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Rds",
  "protocol": "HTTP|HTTPS"
}
