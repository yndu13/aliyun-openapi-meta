{
  "camel_example": "",
  "cmd_full_name": "rds describe-migrate-tasks",
  "cmd_name": "describe-migrate-tasks",
  "deprecated": false,
  "description_en": "Queries the tasks that are created to migrate the backup data of an ApsaraDB RDS for SQL Server instance",
  "description_zh": "该接口用于查询RDS SQL Server实例备份数据上云任务列表",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeMigrateTasks",
  "operation": {
    "action": "DescribeMigrateTasks",
    "api_style": "RPC",
    "api_version": "2014-08-15",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "rm-uf6wjk5****",
      "help_en": "The instance ID. You can call the DescribeDBInstances operation to query the instance ID",
      "help_zh": "实例ID。可调用DescribeDBInstances获取",
      "location": "query",
      "name": "db_instance_id",
      "options": [
        "--db-instance-id"
      ],
      "raw_name": "DBInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "2017-10-20T01:00Z",
      "help_en": "The beginning of the time range to query. Specify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mmZ format. The time must be in UTC",
      "help_zh": "查询开始时间。格式：<i>yyyy-MM-dd</i>T<i>HH:mm</i>Z（UTC时间）",
      "location": "query",
      "name": "start_time",
      "options": [
        "--start-time"
      ],
      "raw_name": "StartTime",
      "required": true,
      "type": "string"
    },
    {
      "example": "2017-10-25T01:00Z",
      "help_en": "The end of the time range to query. The end time must be later than the start time. Specify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mmZ format. The time must be in UTC",
      "help_zh": "查询结束时间，必须大于开始时间。格式：<i>yyyy-MM-dd</i>T<i>HH:mm</i>Z（UTC时间）",
      "location": "query",
      "name": "end_time",
      "options": [
        "--end-time"
      ],
      "raw_name": "EndTime",
      "required": true,
      "type": "string"
    },
    {
      "example": "30",
      "help_en": "The number of entries to return on each page. Valid values: 30 to 100. Default value: 30",
      "help_zh": "每页记录数，取值：30~100。默认值：30",
      "location": "query",
      "name": "page_size",
      "options": [
        "--page-size"
      ],
      "raw_name": "PageSize",
      "required": false,
      "type": "int"
    },
    {
      "example": "1",
      "help_en": "The page number. Valid values: any non-zero positive integer.\n\nDefault value: 1",
      "help_zh": "页码，取值：大于0且不超过Integer的最大值。\n\n默认值：1",
      "location": "query",
      "name": "page_number",
      "options": [
        "--page-number"
      ],
      "raw_name": "PageNumber",
      "required": false,
      "type": "int"
    },
    {
      "example": "rg-acfmy****",
      "help_en": "The resource group ID",
      "help_zh": "资源组ID",
      "location": "query",
      "name": "resource_group_id",
      "options": [
        "--resource-group-id"
      ],
      "raw_name": "ResourceGroupId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Rds",
  "protocol": "HTTP|HTTPS"
}
