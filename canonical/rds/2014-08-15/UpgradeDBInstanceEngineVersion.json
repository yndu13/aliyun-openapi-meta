{
  "camel_example": "",
  "cmd_full_name": "rds upgrade-db-instance-engine-version",
  "cmd_name": "upgrade-db-instance-engine-version",
  "deprecated": false,
  "description_en": "Upgrades the major engine version of an ApsaraDB RDS for MySQL instance",
  "description_zh": "该接口用于升级RDS MySQL的数据库大版本",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UpgradeDBInstanceEngineVersion",
  "operation": {
    "action": "UpgradeDBInstanceEngineVersion",
    "api_style": "RPC",
    "api_version": "2014-08-15",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "ETnLKlblzczshOTUbOCz****",
      "help_en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length",
      "help_zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符，且该参数值中不能包含非ASCII字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "rm-uf6wjk5****",
      "help_en": "The instance ID. You can call the DescribeDBInstances operation to query the instance ID",
      "help_zh": "实例ID。可调用DescribeDBInstances获取",
      "location": "query",
      "name": "db_instance_id",
      "options": [
        "--db-instance-id"
      ],
      "raw_name": "DBInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "5.7",
      "help_en": "The major engine version that the new instance runs. Valid values:\n\n8.0\n5.7\n5.6",
      "help_zh": "目标数据库版本，取值：\n8.0\n5.7\n5.6",
      "location": "query",
      "name": "engine_version",
      "options": [
        "--engine-version"
      ],
      "raw_name": "EngineVersion",
      "required": true,
      "type": "string"
    },
    {
      "example": "Immediate",
      "help_en": "The effective time. Valid values:\n\nImmediate: This is the default value.\nMaintainTime: The effective time is within the maintenance window. For more information, see ModifyDBInstanceMaintainTime",
      "help_zh": "生效时间，取值：\nImmediate（默认）：立即生效。\nMaintainTime：在可运维时间段内生效，请参见ModifyDBInstanceMaintainTime",
      "location": "query",
      "name": "effective_time",
      "options": [
        "--effective-time"
      ],
      "raw_name": "EffectiveTime",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Rds",
  "protocol": "HTTP|HTTPS"
}
