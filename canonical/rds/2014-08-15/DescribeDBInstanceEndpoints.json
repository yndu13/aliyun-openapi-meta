{
  "camel_example": "",
  "cmd_full_name": "rds describe-db-instance-endpoints",
  "cmd_name": "describe-db-instance-endpoints",
  "deprecated": false,
  "description_en": "Queries the information about the endpoints of an instance that runs RDS Cluster Edition",
  "description_zh": "该接口用于查询RDS集群系列实例的Endpoint信息",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeDBInstanceEndpoints",
  "operation": {
    "action": "DescribeDBInstanceEndpoints",
    "api_style": "RPC",
    "api_version": "2014-08-15",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "ETnLKlblzczshOTUbOC****",
      "help_en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests.\n\nThe token can contain only ASCII characters and cannot exceed 64 characters in length",
      "help_zh": "该参数用于保证请求的幂等性，防止重复提交请求。参数值由客户端生成，保证在不同请求间唯一。\n\n该参数值由ASCII字符组成，最长不超过64个字符，不能包含非ASCII字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "rm-u****",
      "help_en": "The instance ID. You can call the DescribeDBInstances operation to query the instance ID",
      "help_zh": "实例ID。可调用DescribeDBInstances查询",
      "location": "query",
      "name": "db_instance_id",
      "options": [
        "--db-instance-id"
      ],
      "raw_name": "DBInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "ep-****-ro",
      "help_en": "The endpoint ID of the instance.\n\nIf this parameter is not specified, the information about all endpoints is returned",
      "help_zh": "实例的Endpoint ID。\n\n未传该参数表示返回所有Endpoint信息",
      "location": "query",
      "name": "db_instance_endpoint_id",
      "options": [
        "--db-instance-endpoint-id"
      ],
      "raw_name": "DBInstanceEndpointId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Rds",
  "protocol": "HTTP|HTTPS"
}
