{
  "camel_example": "",
  "cmd_full_name": "rds describe-class-details",
  "cmd_name": "describe-class-details",
  "deprecated": false,
  "description_en": "Queries the details about the instance types of an instance by using the code of the instance types",
  "description_zh": "该接口用于通过规格代码查询规格详情",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeClassDetails",
  "operation": {
    "action": "DescribeClassDetails",
    "api_style": "RPC",
    "api_version": "2014-08-15",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "ETnLKlblzczshOTUbOCz****",
      "help_en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the generated token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length",
      "help_zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符，且该参数值中不能包含非ASCII字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID. You can call the DescribeRegions operation to query the most recent region list",
      "help_zh": "地域ID，可以通过接口DescribeRegions查看可用的地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "rds",
      "help_en": "The commodity code of the instance. Valid values:\n\nbards_intl: The instance is a pay-as-you-go primary instance.\nrds_intl: The instance is a subscription primary instance.\nrords_intl: The instance is a pay-as-you-go read-only instance.\nrds_rordspre_public_intl: The instance is a subscription read-only instance",
      "help_zh": "商品码。取值：\n\nbards：主实例按量付费\nrds：主实例包年包月\nrords：只读实例按量付费\nrds_rordspre_public_cn：只读实例包年包月\nbards_intl：主实例按量付费\nrds_intl：主实例包年包月\nrords_intl：只读实例按量付费\nrds_rordspre_public_intl：只读实例包年包月",
      "location": "query",
      "name": "commodity_code",
      "options": [
        "--commodity-code"
      ],
      "raw_name": "CommodityCode",
      "required": true,
      "type": "string"
    },
    {
      "example": "rds.mysql.s3.large",
      "help_en": "The code of the instance type",
      "help_zh": "规格代码",
      "location": "query",
      "name": "class_code",
      "options": [
        "--class-code"
      ],
      "raw_name": "ClassCode",
      "required": true,
      "type": "string"
    },
    {
      "example": "5.6",
      "help_en": "The database engine version of the instance",
      "help_zh": "数据库版本",
      "location": "query",
      "name": "engine_version",
      "options": [
        "--engine-version"
      ],
      "raw_name": "EngineVersion",
      "required": true,
      "type": "string"
    },
    {
      "example": "MySQL",
      "help_en": "The type of the database engine",
      "help_zh": "数据库引擎类型",
      "location": "query",
      "name": "engine",
      "options": [
        "--engine"
      ],
      "raw_name": "Engine",
      "required": true,
      "type": "string"
    },
    {
      "example": "rg-acfmy****",
      "help_en": "The resource group ID. You can call the DescribeDBInstanceAttribute operation to query the resource group ID",
      "help_zh": "资源组ID。可调用DescribeDBInstanceAttribute获取",
      "location": "query",
      "name": "resource_group_id",
      "options": [
        "--resource-group-id"
      ],
      "raw_name": "ResourceGroupId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Rds",
  "protocol": "HTTP|HTTPS"
}
