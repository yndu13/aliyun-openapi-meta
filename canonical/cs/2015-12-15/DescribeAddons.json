{
  "camel_example": "",
  "cmd_full_name": "cs describe-addons",
  "cmd_name": "describe-addons",
  "deprecated": true,
  "description_en": "Queries the details of all components supported by the platform",
  "description_zh": "查询平台支持的所有组件的详情",
  "kebab_example": "",
  "method": "GET",
  "multi_version": false,
  "name": "DescribeAddons",
  "operation": {
    "action": "DescribeAddons",
    "api_style": "ROA",
    "api_version": "2015-12-15",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": "/clusters/components/metadata"
  },
  "parameters": [
    {
      "example": "cn-beijing",
      "help_en": "The ID of the region where the cluster resides",
      "help_zh": "集群所在地域ID",
      "location": "query",
      "name": "region",
      "options": [
        "--biz-region"
      ],
      "raw_name": "region",
      "required": true,
      "type": "string"
    },
    {
      "example": "ManagedKubernetes",
      "help_en": "`Kubernetes`: ACK dedicated cluster.\n\n`ManagedKubernetes`: ACK managed cluster types, including ACK managed clusters (ACK Pro and ACK Basic), ACK Serverless clusters (Pro and Basic), ACK Edge clusters (Pro and Basic), and ACK Lingjun clusters (Pro).\n\n`ExternalKubernetes`: registered cluster",
      "help_zh": "`Kubernetes`: ACK专有集群。\n\n`ManagedKubernetes`：ACK托管类的集群，包括ACK托管集群（ACK集群Pro版、ACK集群基础版）、ACK Serverless集群（Pro版、基础版）、ACK Edge集群（Pro版、基础版）、ACK灵骏集群（Pro版）。\n\n`ExternalKubernetes`：注册集群",
      "location": "query",
      "name": "cluster_type",
      "options": [
        "--cluster-type"
      ],
      "raw_name": "cluster_type",
      "required": false,
      "type": "string"
    },
    {
      "example": "Default",
      "help_en": "The cluster type. Valid values:\n`Default`: managed cluster.\n`Serverless`: serverless cluster.\n`Edge`: edge cluster",
      "help_zh": "集群类型，取值：\n`Default`：托管版集群。\n`Serverless`：Serverless集群。\n`Edge`：边缘集群",
      "location": "query",
      "name": "cluster_profile",
      "options": [
        "--cluster-profile"
      ],
      "raw_name": "cluster_profile",
      "required": false,
      "type": "string"
    },
    {
      "example": "ack.pro.small",
      "help_en": "After you set `cluster_type` to `ManagedKubernetes` and configure `profile`, you can further specify the cluster specification.\n\n`ack.pro.small`: Pro cluster.\n\n`ack.standard`: Basic cluster (selected by default if this parameter is left empty)",
      "help_zh": "当您选择`cluster_type`为`ManagedKubernetes`并配置`profile`后，您可以进一步指定集群的规格。\n\n`ack.pro.small`：集群Pro版。\n\n`ack.standard`：集群基础版（取值为空时默认选择）",
      "location": "query",
      "name": "cluster_spec",
      "options": [
        "--cluster-spec"
      ],
      "raw_name": "cluster_spec",
      "required": false,
      "type": "string"
    },
    {
      "example": "1.24.6-aliyun.1",
      "help_en": "The cluster version",
      "help_zh": "集群版本",
      "location": "query",
      "name": "cluster_version",
      "options": [
        "--cluster-version"
      ],
      "raw_name": "cluster_version",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/clusters/components/metadata",
  "product_code": "CS",
  "protocol": "HTTP|HTTPS"
}
