{
  "camel_example": "",
  "cmd_full_name": "cs describe-resources-delete-protection",
  "cmd_name": "describe-resources-delete-protection",
  "deprecated": false,
  "description_en": "Queries whether deletion protection is enabled for a specified resource in a cluster. Resources that support deletion protection include namespaces and services",
  "description_zh": "查询集群内的指定资源是否开启删除保护，当前支持删除保护的资源包括命名空间和服务",
  "kebab_example": "",
  "method": "GET",
  "multi_version": false,
  "name": "DescribeResourcesDeleteProtection",
  "operation": {
    "action": "DescribeResourcesDeleteProtection",
    "api_style": "ROA",
    "api_version": "2015-12-15",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": "/clusters/{ClusterId}/resources/{ResourceType}/protection"
  },
  "parameters": [
    {
      "example": "c82e6987e2961451182edacd74faf****",
      "help_en": "The ACK cluster ID",
      "help_zh": "ACK集群ID",
      "location": "path",
      "name": "cluster_id",
      "options": [
        "--cluster-id"
      ],
      "raw_name": "ClusterId",
      "required": true,
      "type": "string"
    },
    {
      "enum": [
        "namespaces",
        "services"
      ],
      "example": "namespaces",
      "help_en": "The type of resource to query. Valid values:\n\nnamespaces: namespace.\n\nservices: service",
      "help_zh": "待查询的资源类型。取值如下：\n\nnamespaces：命名空间。\n\nservices：服务",
      "location": "path",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": true,
      "type": "string"
    },
    {
      "example": "default",
      "help_en": "The namespace of the resource to query.\n\nThis parameter is required when resource_type is set to services. If this parameter is not specified, the namespace defaults to default",
      "help_zh": "待查询资源所在的命名空间。\n\nresource_type选择为services时该参数必须填写，若未填写则默认按照命名空间为default处理",
      "location": "query",
      "name": "namespace",
      "options": [
        "--namespace"
      ],
      "raw_name": "namespace",
      "required": false,
      "type": "string"
    },
    {
      "example": "test1,test2",
      "help_en": "The name of the resource to query. Separate multiple resources with commas (,).\n\nIf resource_type is set to namespaces, set this parameter to namespace names. If this parameter is not specified, the deletion protection status of all namespaces in the cluster is queried.\n\nIf resource_type is set to services, this parameter is required. Set this parameter to service names",
      "help_zh": "查询的目标资源名称，多个资源以英文半角逗号（,）分割。\n\nresource_type选择为namespaces时，本参数需要填写为命名空间名称。若不填写，则查询集群下所有命名空间的删除保护状态。\n\nresource_type选择为services时，本参数必须填写，需要填写为服务名称",
      "location": "query",
      "name": "resources",
      "options": [
        "--resources"
      ],
      "raw_name": "resources",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/clusters/[ClusterId]/resources/[ResourceType]/protection",
  "product_code": "CS",
  "protocol": "HTTPS"
}
