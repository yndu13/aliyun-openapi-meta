{
  "camel_example": "",
  "cmd_full_name": "cs check-service-role",
  "cmd_name": "check-service-role",
  "deprecated": false,
  "description_en": "Checks whether the current service account has been granted the specified service role permissions. Container Service for Kubernetes (ACK) can call other cloud services (such as ECS, OSS, NAS, and SLB) that are associated with service roles only after the corresponding role permissions are granted",
  "description_zh": "检查当前服务账号是否已授予指定的服务角色权限。只有授予对应的角色权限后，容器服务才能正常调用服务角色相关的其他云服务（ECS、OSS、NAS、SLB等）",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "CheckServiceRole",
  "operation": {
    "action": "CheckServiceRole",
    "api_style": "ROA",
    "api_version": "2015-12-15",
    "body_mapping": {
      "roles": "roles"
    },
    "content_type": "application/json",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "req_body_type": "json",
    "system_operation_type": "none",
    "url": "/ram/check-service-role"
  },
  "parameters": [
    {
      "element": {
        "fields": [
          {
            "example": "AliyunCSManagedAutoScalerRole",
            "help_en": "Container service role name. For information about container service roles and permission scopes included in ACK, see https://help.aliyun.com/document_detail/86483.html",
            "help_zh": "服务角色名称。容器服务ACK包含的服务角色以及权限范围，请参见https://help.aliyun.com/document_detail/86483.html",
            "name": "name",
            "options": [
              "--name"
            ],
            "raw_name": "name",
            "required": true,
            "type": "string"
          }
        ],
        "type": "object"
      },
      "help_en": "The list of service roles to check",
      "help_zh": "需要检查的服务角色列表",
      "location": "body",
      "name": "roles",
      "options": [
        "--roles"
      ],
      "param_style": "json",
      "raw_name": "roles",
      "required": true,
      "type": "array"
    }
  ],
  "pathPattern": "/ram/check-service-role",
  "product_code": "CS",
  "protocol": "HTTPS",
  "v1_body_parameters": [
    {
      "help_en": "The request body parameters",
      "help_zh": "请求体参数",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    }
  ]
}
