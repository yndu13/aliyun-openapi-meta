{
  "camel_example": "",
  "cmd_full_name": "cs delete-cluster-nodes",
  "cmd_name": "delete-cluster-nodes",
  "deprecated": false,
  "description_en": "When you no longer need cluster nodes to continue working, you can call the DeleteClusterNodes operation to remove nodes from the cluster. When removing nodes, you can choose whether to simultaneously release the ECS instances and whether to automatically drain the nodes",
  "description_zh": "当您不再需要集群节点继续工作时，可以调用DeleteClusterNodes接口将集群中的节点移出集群。移除节点时，您可以选择是否同时释放ECS、是否自动排空节点",
  "kebab_example": "",
  "method": "DELETE|POST",
  "multi_version": false,
  "name": "DeleteClusterNodes",
  "operation": {
    "action": "DeleteClusterNodes",
    "api_style": "ROA",
    "api_version": "2015-12-15",
    "body_mapping": {
      "drain_node": "drain_node",
      "nodes": "nodes",
      "release_node": "release_node"
    },
    "content_type": "application/json",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "req_body_type": "json",
    "system_operation_type": "delete",
    "url": "/clusters/{ClusterId}/nodes"
  },
  "parameters": [
    {
      "example": "c850429a2287b4d968e27e87a4921****",
      "help_en": "Cluster ID",
      "help_zh": "集群ID",
      "location": "path",
      "name": "cluster_id",
      "options": [
        "--cluster-id"
      ],
      "raw_name": "ClusterId",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Whether to automatically drain Pods on the node. Valid values:\n`true`: Automatically drain Pods on the node.\n`false`: Do not automatically drain Pods on the node.\n\nDefault value: `false`",
      "help_zh": "是否自动排空节点上的Pod。取值：\n`true`：自动排空节点上的Pod。\n`false`：不自动排空节点上的Pod。\n\n默认值：`false`",
      "location": "body",
      "name": "drain_node",
      "options": [
        "--drain-node"
      ],
      "raw_name": "drain_node",
      "required": false,
      "type": "bool"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "List of nodes to remove. The node names must be the names of the nodes in the cluster, for example: `cn-hangzhou.192.168.xx.xx`",
      "help_zh": "移除节点列表，节点名称必须是节点在集群中的名称，例如：`cn-hangzhou.192.168.xx.xx`",
      "location": "body",
      "name": "nodes",
      "options": [
        "--nodes"
      ],
      "raw_name": "nodes",
      "required": true,
      "type": "array"
    },
    {
      "example": "true",
      "help_en": "Whether to simultaneously remove ECS instances. Valid values:\n\n`true`: Simultaneously remove ECS instances.\n`false`: Retain ECS instances.\n\nDefault value: `false`.\n\nSimultaneous removal of ECS instances is not supported when the nodes are subscription instances",
      "help_zh": "是否同时移除ECS。取值：\n\n`true`：同时移除ECS。\n`false`：保留ECS。\n\n默认值：`false`。\n\n当节点为包年包月实例时，不支持同时移除ECS",
      "location": "body",
      "name": "release_node",
      "options": [
        "--release-node"
      ],
      "raw_name": "release_node",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "/clusters/[ClusterId]/nodes",
  "product_code": "CS",
  "protocol": "HTTP|HTTPS",
  "v1_body_parameters": [
    {
      "help_en": "Request body parameters",
      "help_zh": "请求体参数",
      "name": "body",
      "position": "body",
      "required": false,
      "type": "object"
    }
  ]
}
