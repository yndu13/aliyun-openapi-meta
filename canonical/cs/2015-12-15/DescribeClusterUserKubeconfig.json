{
  "camel_example": "",
  "cmd_full_name": "cs describe-cluster-user-kubeconfig",
  "cmd_name": "describe-cluster-user-kubeconfig",
  "deprecated": false,
  "description_en": "KubeConfig is used to configure access credentials for ACK clusters on the client. It contains identity and authentication data for accessing the target cluster. When you use kubectl for cluster management, you must first connect to the cluster by using KubeConfig. You can invoke the DescribeClusterUserKubeconfig operation to query the KubeConfig of a specified cluster",
  "description_zh": "KubeConfig用于在客户端配置ACK集群的访问凭据，包含访问目标集群的身份和认证数据等信息。使用kubectl管理集群时，需要先通过KubeConfig来连接集群，您可以调用DescribeClusterUserKubeconfig接口查询指定集群的KubeConfig",
  "kebab_example": "",
  "method": "GET",
  "multi_version": false,
  "name": "DescribeClusterUserKubeconfig",
  "operation": {
    "action": "DescribeClusterUserKubeconfig",
    "api_style": "ROA",
    "api_version": "2015-12-15",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": "/k8s/{ClusterId}/user_config"
  },
  "parameters": [
    {
      "example": "c5b5e80b0b64a4bf6939d2d8fbbc5****",
      "help_en": "The cluster ID",
      "help_zh": "集群ID",
      "location": "path",
      "name": "cluster_id",
      "options": [
        "--cluster-id"
      ],
      "raw_name": "ClusterId",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to obtain the internal network connection configuration. Valid values:\n`true`: Obtains only the internal network connection credential.\n`false`: Obtains only the public network connection credential. \n\nDefault value: `false`",
      "help_zh": "是否获取内网连接配置。取值：\n`true`：仅获取内网连接凭据。\n`false`：仅获取公网连接凭据。 \n\n默认值：`false`",
      "location": "query",
      "name": "private_ip_address",
      "options": [
        "--private-ip-address"
      ],
      "raw_name": "PrivateIpAddress",
      "required": false,
      "type": "bool"
    },
    {
      "example": "15",
      "help_en": "The validity period of the temporary KubeConfig. Unit: minutes. Valid values: 15 (15 minutes) to 4320 (3 days).\nIf you do not set this parameter, the system automatically determines a longer validity period. The specific expiration time is determined by the value of the `expiration` field in the response",
      "help_zh": "临时KubeConfig有效期，单位：分钟。取值范围：15（15分钟）～4320（3天）。\n当不设置该参数时，将由系统自动确定一个更长的有效期，具体过期时间通过返回的`expiration`字段的值确定",
      "location": "query",
      "name": "temporary_duration_minutes",
      "options": [
        "--temporary-duration-minutes"
      ],
      "raw_name": "TemporaryDurationMinutes",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "/k8s/[ClusterId]/user_config",
  "product_code": "CS",
  "protocol": "HTTP|HTTPS"
}
