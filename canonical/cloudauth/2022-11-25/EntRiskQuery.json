{
  "cmd_full_name": "cloudauth ent-risk-query",
  "cmd_name": "ent-risk-query",
  "deprecated": false,
  "description_en": "Queries the business abnormality information of an enterprise",
  "description_zh": "查询企业经营异常情况",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "EntRiskQuery",
  "operation": {
    "action": "EntRiskQuery",
    "api_style": "RPC",
    "api_version": "2022-11-25",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "1000000086",
      "help_en": "The custom scene code that identifies a business scenario. The value is a 10-digit number",
      "help_zh": "自定义场景code，用于区分业务场景，长度为10位的数字",
      "location": "query",
      "name": "scene_code",
      "options": [
        "--scene-code"
      ],
      "raw_name": "SceneCode",
      "required": false,
      "type": "string"
    },
    {
      "example": "32198****193000",
      "help_en": "The merchant-side custom business unique identifier, which is used for subsequent troubleshooting. The value is a combination of letters and digits up to 32 characters in length. Make sure the value is unique",
      "help_zh": "商户侧自定义的业务唯一标识，用于后续定位排查问题使用。支持长度为32位的字母和数字的组合，请确保唯一",
      "location": "query",
      "name": "merchant_biz_id",
      "options": [
        "--merchant-biz-id"
      ],
      "raw_name": "MerchantBizId",
      "required": false,
      "type": "string"
    },
    {
      "example": "无",
      "help_en": "The custom user ID in your business, which is used for subsequent troubleshooting",
      "help_zh": "您业务中自定义用户ID，用于后续定位排查问题使用",
      "location": "query",
      "name": "merchant_user_id",
      "options": [
        "--merchant-user-id"
      ],
      "raw_name": "MerchantUserId",
      "required": false,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "Specifies whether user authorization is obtained. Valid values:\n1: Authorization is obtained.\n0: Authorization is not obtained",
      "help_zh": "是否获得得用户授权。\n1：获得授权\n0：未获得授权",
      "location": "query",
      "name": "user_authorization",
      "options": [
        "--user-authorization"
      ],
      "raw_name": "UserAuthorization",
      "required": false,
      "type": "string"
    },
    {
      "example": "00",
      "help_en": "The parameter type. Valid values:\n00: enterprise name\n01: business registration number\n02: unified social credit code\n03: organization code",
      "help_zh": "参数类型。\n00：企业名称；\n01：工商注册号；\n02：统一社会信用代码；\n03：组织机构代码；",
      "location": "query",
      "name": "param_type",
      "options": [
        "--param-type"
      ],
      "raw_name": "ParamType",
      "required": false,
      "type": "string"
    },
    {
      "example": "91330106673959****",
      "help_en": "The value that corresponds to the ParamType parameter. Enter one of the following values based on the parameter type: enterprise name, business registration number, unified social credit code, or organization code",
      "help_zh": "根据ParamType参数类型输入企业名称、工商注册号、统一社会信用代码、组织机构代码其中一种",
      "location": "query",
      "name": "param_value",
      "options": [
        "--param-value"
      ],
      "raw_name": "ParamValue",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Cloudauth",
  "protocol": "HTTPS"
}
