{
  "cmd_full_name": "imm detect-image-faces",
  "cmd_name": "detect-image-faces",
  "deprecated": false,
  "description_en": "Calls the DetectImageFaces operation to detect faces and face information in an image",
  "description_zh": "调用DetectImageFaces接口检测图片中的人脸及人脸信息",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "DetectImageFaces",
  "operation": {
    "action": "DetectImageFaces",
    "api_style": "RPC",
    "api_version": "2017-09-06",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "immimagetest",
      "help_en": "The project name",
      "help_zh": "项目名称",
      "location": "query",
      "name": "project",
      "options": [
        "--project"
      ],
      "raw_name": "Project",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "oss://imm-test/testcases/facetest.jpg",
      "help_en": "The OSS URI of the image.\n\nThe OSS URI follows the format `oss://bucket/object`, where bucket is the name of an OSS bucket in the same region as the current project, and object is the file path",
      "help_zh": "图片的OSS URI。\n\nOSS地址规则为`oss://bucket/object`，其中bucket为和当前项目处于同一区域的OSS Bucket名称，object为文件路径",
      "location": "query",
      "name": "image_uri",
      "options": [
        "--image-uri"
      ],
      "raw_name": "ImageUri",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "imm",
  "protocol": "HTTP|HTTPS"
}
