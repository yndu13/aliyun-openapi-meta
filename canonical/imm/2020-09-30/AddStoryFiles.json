{
  "camel_example": "",
  "cmd_full_name": "imm add-story-files",
  "cmd_name": "add-story-files",
  "deprecated": false,
  "description_en": "Adds objects to a story",
  "description_zh": "向故事中添加文件",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "AddStoryFiles",
  "operation": {
    "action": "AddStoryFiles",
    "api_style": "RPC",
    "api_version": "2020-09-30",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "example": "test-project",
      "help_en": "The name of the project",
      "help_zh": "项目名称",
      "location": "formData",
      "name": "project_name",
      "options": [
        "--project-name"
      ],
      "raw_name": "ProjectName",
      "required": true,
      "type": "string"
    },
    {
      "example": "test-dataset",
      "help_en": "The name of the dataset",
      "help_zh": "数据集名称",
      "location": "formData",
      "name": "dataset_name",
      "options": [
        "--dataset-name"
      ],
      "raw_name": "DatasetName",
      "required": true,
      "type": "string"
    },
    {
      "example": "testid",
      "help_en": "The ID of the story",
      "help_zh": "故事ID",
      "location": "formData",
      "name": "object_id",
      "options": [
        "--object-id"
      ],
      "raw_name": "ObjectId",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "fields": [
          {
            "example": "oss://test-bucket/test-object",
            "help_en": "The URI of the object.\n\nSpecify the OSS URI in the oss://${Bucket}/${Object} format, where `${Bucket}` is the name of the bucket in the same region as the current project and `${Object}` is the path of the object with the extension included",
            "help_zh": "文件的存储位置。\n\nOSS地址规则为oss://${Bucket}/${Object}，其中`${Bucket}`为和当前项目处于同一区域(Region)的OSS Bucket名称，`${Object}`为包含文件扩展名的文件完整路径",
            "name": "uri",
            "options": [
              "--uri"
            ],
            "raw_name": "URI",
            "required": false,
            "type": "string"
          }
        ],
        "type": "object"
      },
      "help_en": "The objects that you want to add",
      "help_zh": "要添加的文件列表",
      "location": "formData",
      "name": "files",
      "options": [
        "--files"
      ],
      "param_style": "json",
      "raw_name": "Files",
      "required": true,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "imm",
  "protocol": "HTTP|HTTPS"
}
