{
  "camel_example": "",
  "cmd_full_name": "imm get-file-meta",
  "cmd_name": "get-file-meta",
  "deprecated": false,
  "description_en": "Queries metadata of a file whose metadata is indexed into the dataset",
  "description_zh": "获取数据集内已完成索引的文件的元数据",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "GetFileMeta",
  "operation": {
    "action": "GetFileMeta",
    "api_style": "RPC",
    "api_version": "2020-09-30",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "test-project",
      "help_en": "The name of the project.[](~~478153~~)",
      "help_zh": "项目名称，获取方式请参见https://help.aliyun.com/document_detail/478153.html",
      "location": "query",
      "name": "project_name",
      "options": [
        "--project-name"
      ],
      "raw_name": "ProjectName",
      "required": true,
      "type": "string"
    },
    {
      "example": "test-dataset",
      "help_en": "The name of the dataset.[](~~478160~~)",
      "help_zh": "数据集名称，获取方式请参见https://help.aliyun.com/document_detail/478160.html",
      "location": "query",
      "name": "dataset_name",
      "options": [
        "--dataset-name"
      ],
      "raw_name": "DatasetName",
      "required": true,
      "type": "string"
    },
    {
      "example": "oss://test-bucket/test-object",
      "help_en": "The URI of the file. Make sure that the file is indexed\\\\.\n\nSpecify the OSS URI in the oss\\://${Bucket}/${Object} format, where `${Bucket}` is the name of the bucket in the same region as the current project and `${Object}` is the path of the object with the extension included.\n\nSpecify the URI of the file in Photo and Drive Service in the pds\\://domains/${domain}/drives/${drive}/files/${file}/revisions/${revision} format",
      "help_zh": "文件的URI，需确保该文件已经完成索引。\n\nOSS地址规则为oss://${Bucket}/${Object}，其中`${Bucket}`为和当前项目处于同一区域(Region)的OSS Bucket名称，`${Object}`为包含文件扩展名的文件完整路径。\n\nPDS地址规则为pds://domains/${domain}/drives/${drive}/files/${file}/revisions/${revision}",
      "location": "query",
      "name": "uri",
      "options": [
        "--uri"
      ],
      "raw_name": "URI",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The metadata fields that you want to include in the response. You can use this parameter to reduce the size of the response.\n\nIf you do not specify this parameter or leave this parameter empty, the operation returns all metadata fields of the file",
      "help_zh": "仅返回特定字段的值，而不是全部已有的元信息字段。可用于降低返回的结构体大小。\n\n不填或留空则返回所有字段",
      "location": "query",
      "name": "with_fields",
      "options": [
        "--with-fields"
      ],
      "param_style": "json",
      "raw_name": "WithFields",
      "required": false,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "imm",
  "protocol": "HTTP|HTTPS"
}
