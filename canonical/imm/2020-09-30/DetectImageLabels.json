{
  "camel_example": "",
  "cmd_full_name": "imm detect-image-labels",
  "cmd_name": "detect-image-labels",
  "deprecated": false,
  "description_en": "Detects scene, object, and event information in an image. Scene information includes natural landscapes, daily life, and disasters. Event information includes talent shows, office events, performances, and production events. Object information includes tableware, electronics, furniture, and transportation. The DetectImageLabels operation supports more than 30 different categories and thousands of labels",
  "description_zh": "检测图片中的场景、物体和事件标签信息。场景信息包括：自然景观、生活场景、灾难场景等大类；事件信息包括：才艺、办公、表演、生产等大类；物体信息包括：餐具、电子产品、家具、交通工具等大类。图片标签检测目前支持的标签种类包含三十多个分类、数千个标签",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "DetectImageLabels",
  "operation": {
    "action": "DetectImageLabels",
    "api_style": "RPC",
    "api_version": "2020-09-30",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "test-project",
      "help_en": "The name of the project. You can obtain the name of the project from the response of the https://help.aliyun.com/document_detail/478153.html operation",
      "help_zh": "项目名称，获取方式请参见https://help.aliyun.com/document_detail/478153.html",
      "location": "query",
      "name": "project_name",
      "options": [
        "--project-name"
      ],
      "raw_name": "ProjectName",
      "required": true,
      "type": "string"
    },
    {
      "example": "oss://test-bucket/test-object.jpg",
      "help_en": "The URI of the Object Storage Service (OSS) bucket in which you store the image.\n\nSpecify the value in the oss\\://${Bucket}/${Object} format. `${Bucket}` specifies the name of the OSS bucket that resides in the same region as the current project. `${Object}` specifies the complete path to the image file that has an extension",
      "help_zh": "图片的OSS URI。\n\nOSS地址规则为oss://${Bucket}/${Object}，其中`${Bucket}`为和当前项目处于同一区域(Region)的OSS Bucket名称，`${Object}`为包含文件扩展名的文件完整路径",
      "location": "query",
      "name": "source_uri",
      "options": [
        "--source-uri"
      ],
      "raw_name": "SourceURI",
      "required": true,
      "type": "string"
    },
    {
      "example": "0.7",
      "help_en": "The threshold of the label confidence. Labels whose confidence is lower than the specified threshold are not returned in the response. Valid values: 0 to 1. If you leave this parameter empty, the algorithm provides a default threshold",
      "help_zh": "标签置信度过滤阈值，返回结果不包含置信度小于该设定阈值的标签。取值范围为0~1。如果不配置，则使用算法内部的默认值",
      "location": "query",
      "name": "threshold",
      "options": [
        "--threshold"
      ],
      "raw_name": "Threshold",
      "required": false,
      "type": "float"
    },
    {
      "fields": [
        {
          "element": {
            "fields": [
              {
                "example": "10232100246xxxxx",
                "help_en": "The ID of the account that you use to grant permissions",
                "help_zh": "提供授权的账号ID",
                "name": "assume_role_for",
                "options": [
                  "--assume-role-for"
                ],
                "raw_name": "AssumeRoleFor",
                "required": false,
                "type": "string"
              },
              {
                "example": "AliyunOSSRole",
                "help_en": "The RAM role that can be assumed",
                "help_zh": "允许扮演的RAM角色",
                "name": "role",
                "options": [
                  "--role"
                ],
                "raw_name": "Role",
                "required": false,
                "type": "string"
              },
              {
                "example": "user",
                "help_en": "The role type. Valid values:\n\nuser: Alibaba Cloud account.\nservice: Alibaba Cloud service",
                "help_zh": "角色类型，可选值如下。\nuser：阿里云账号。\nservice：阿里云服务",
                "name": "role_type",
                "options": [
                  "--role-type"
                ],
                "raw_name": "RoleType",
                "required": false,
                "type": "string"
              }
            ],
            "type": "object"
          },
          "help_en": "The authorization chains. All roles in the array must have the `sts:AssumeRole` permission. You need to only grant other permissions, such as read and write permissions on OSS, to the last role in the array. You can grant permissions in the RAM console",
          "help_zh": "需要传递授权的链列表，数组中的所有角色都需要赋予访问 `sts:AssumeRole` 权限。\n只有数组中的最后一个角色需要额外配置需要真正访问的其他权限，比如OSS的读写权限，可以通过RAM控制台进行授权操作",
          "name": "chain",
          "options": [
            "--chain"
          ],
          "raw_name": "Chain",
          "required": false,
          "type": "array"
        },
        {
          "example": "{\"Statement\": [{\"Action\": \"oss:*\",\"Effect\": \"Allow\",\"Resource\": \"*\"}],\"Version\": \"1\"}",
          "help_en": "The policy that is attached to the role specified by the ServiceRole parameter. For example, the policy allows access to OSS. This parameter is optional",
          "help_zh": "ServiceRole参数的角色所需配置的权限策略，例如允许访问OSS。可选",
          "name": "policy",
          "options": [
            "--policy"
          ],
          "raw_name": "Policy",
          "required": false,
          "type": "string"
        },
        {
          "example": "AliyunSTSAssumeForIMMServiceRole",
          "help_en": "The service role in the account that is used to call an IMM API operation. The role must have the `sts:AssumeRole` permission. You can configure permissions for the role in the Resource Access Management (RAM) console",
          "help_zh": "服务角色，调用IMM接口的账号需要创建的一个角色，必须包含 `sts:AssumeRole` 权限，可以通过RAM控制台进行配置",
          "name": "service_role",
          "options": [
            "--service-role"
          ],
          "raw_name": "ServiceRole",
          "required": false,
          "type": "string"
        }
      ],
      "help_en": "If you have no special requirements, leave this parameter empty.\n\nThe authorization chain settings. For more information, see https://help.aliyun.com/document_detail/465340.html",
      "help_zh": "如无特殊需求，请保持留空即可。\n\n链式授权配置，非必填。更多信息，请参见https://help.aliyun.com/document_detail/465340.html",
      "location": "query",
      "name": "credential_config",
      "options": [
        "--credential-config"
      ],
      "param_style": "json",
      "raw_name": "CredentialConfig",
      "required": false,
      "type": "object"
    }
  ],
  "pathPattern": "",
  "product_code": "imm",
  "protocol": "HTTP|HTTPS"
}
