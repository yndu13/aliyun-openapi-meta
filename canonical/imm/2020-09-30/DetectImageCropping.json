{
  "camel_example": "",
  "cmd_full_name": "imm detect-image-cropping",
  "cmd_name": "detect-image-cropping",
  "deprecated": false,
  "description_en": "Detects visually optimal cropping regions in an image at a specified aspect ratio by using AI model capabilities",
  "description_zh": "通过AI模型能力在给定的目标图片比例下检测出视觉效果较好的裁剪框区域",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "DetectImageCropping",
  "operation": {
    "action": "DetectImageCropping",
    "api_style": "RPC",
    "api_version": "2020-09-30",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "immtest",
      "help_en": "The project name",
      "help_zh": "项目名称",
      "location": "query",
      "name": "project_name",
      "options": [
        "--project-name"
      ],
      "raw_name": "ProjectName",
      "required": true,
      "type": "string"
    },
    {
      "example": "oss://imm-test/testcases/facetest.jpg",
      "help_en": "The OSS URI of the image.\n\nThe OSS URI follows the format oss://${Bucket}/${Object}, where `${Bucket}` is the name of an OSS bucket in the same region as the current project, and `${Object}` is the full path of the file including the file name extension",
      "help_zh": "图片的OSS地址。\n\nOSS地址规则为oss://${Bucket}/${Object}，其中`${Bucket}`为和当前项目处于同一区域(Region)的OSS Bucket名称，`${Object}`为包含文件扩展名的文件完整路径",
      "location": "query",
      "name": "source_uri",
      "options": [
        "--source-uri"
      ],
      "raw_name": "SourceURI",
      "required": false,
      "type": "string"
    },
    {
      "example": "[\"1:1\"]",
      "help_en": "The list of cropping aspect ratios. You can specify up to 5 ratios. Each ratio must meet the following requirements:\n\nThe ratio must consist of integers in the range of (0, 20).\n\nThe ratio value must be in the range of [0.5, 2].\n\nIf you do not specify this parameter, the default value `[\"auto\"]` is used.\n\nThe following cases cause an error:<br>- More than 5 ratios are specified.<br>- An empty list is passed.<br>- The ratio contains non-integer values, such as `4.1:3`.<br>- The ratio value is less than 0.5 or greater than 2",
      "help_zh": "裁剪比例列表。最多可设置5个比例，单个裁剪比例要求如下。\n\n比例必须为整数比例，取值范围为（0，20）。\n\n比例范围必须控制在[0.5，2]之间。\n\n不填写此参数时，默认按照`[\"auto\"]`处理。\n\n以下情况会出现报错提示：<br>-设置的比例大于5个。<br>-传入的列表为空。<br>-比例不为整数，例如`4.1:3`。<br>-比例范围小于0.5或大于2",
      "location": "query",
      "name": "aspect_ratios",
      "options": [
        "--aspect-ratios"
      ],
      "raw_name": "AspectRatios",
      "required": false,
      "type": "string"
    },
    {
      "fields": [
        {
          "element": {
            "fields": [
              {
                "example": "10232100246xxxxx",
                "help_en": "The ID of the account that you use to grant permissions",
                "help_zh": "提供授权的账号ID",
                "name": "assume_role_for",
                "options": [
                  "--assume-role-for"
                ],
                "raw_name": "AssumeRoleFor",
                "required": false,
                "type": "string"
              },
              {
                "example": "AliyunOSSRole",
                "help_en": "The RAM role that can be assumed",
                "help_zh": "允许扮演的RAM角色",
                "name": "role",
                "options": [
                  "--role"
                ],
                "raw_name": "Role",
                "required": false,
                "type": "string"
              },
              {
                "example": "user",
                "help_en": "The role type. Valid values:\n\nuser: Alibaba Cloud account.\nservice: Alibaba Cloud service",
                "help_zh": "角色类型，可选值如下。\nuser：阿里云账号。\nservice：阿里云服务",
                "name": "role_type",
                "options": [
                  "--role-type"
                ],
                "raw_name": "RoleType",
                "required": false,
                "type": "string"
              }
            ],
            "type": "object"
          },
          "help_en": "The authorization chains. All roles in the array must have the `sts:AssumeRole` permission. You need to only grant other permissions, such as read and write permissions on OSS, to the last role in the array. You can grant permissions in the RAM console",
          "help_zh": "需要传递授权的链列表，数组中的所有角色都需要赋予访问 `sts:AssumeRole` 权限。\n只有数组中的最后一个角色需要额外配置需要真正访问的其他权限，比如OSS的读写权限，可以通过RAM控制台进行授权操作",
          "name": "chain",
          "options": [
            "--chain"
          ],
          "raw_name": "Chain",
          "required": false,
          "type": "array"
        },
        {
          "example": "{\"Statement\": [{\"Action\": \"oss:*\",\"Effect\": \"Allow\",\"Resource\": \"*\"}],\"Version\": \"1\"}",
          "help_en": "The policy that is attached to the role specified by the ServiceRole parameter. For example, the policy allows access to OSS. This parameter is optional",
          "help_zh": "ServiceRole参数的角色所需配置的权限策略，例如允许访问OSS。可选",
          "name": "policy",
          "options": [
            "--policy"
          ],
          "raw_name": "Policy",
          "required": false,
          "type": "string"
        },
        {
          "example": "AliyunSTSAssumeForIMMServiceRole",
          "help_en": "The service role in the account that is used to call an IMM API operation. The role must have the `sts:AssumeRole` permission. You can configure permissions for the role in the Resource Access Management (RAM) console",
          "help_zh": "服务角色，调用IMM接口的账号需要创建的一个角色，必须包含 `sts:AssumeRole` 权限，可以通过RAM控制台进行配置",
          "name": "service_role",
          "options": [
            "--service-role"
          ],
          "raw_name": "ServiceRole",
          "required": false,
          "type": "string"
        }
      ],
      "help_en": "Leave this parameter empty unless otherwise required.\n\nThe China authorization configuration. This parameter is optional. For more information, see https://help.aliyun.com/document_detail/465340.html",
      "help_zh": "如无特殊需求，请保持留空即可。\n\n链式授权配置，非必填。更多信息，请参见https://help.aliyun.com/document_detail/465340.html",
      "location": "query",
      "name": "credential_config",
      "options": [
        "--credential-config"
      ],
      "param_style": "json",
      "raw_name": "CredentialConfig",
      "required": false,
      "type": "object"
    },
    {
      "element": {
        "type": "string"
      },
      "example": "[\"sign\"]",
      "help_en": "The list of semantic text descriptions for objects that you want the cropping result to include. Each element is free-form text, such as \"signboard\" or \"dish\".\n\nUsage limits of the InclusionHints parameter:\n<br>- You can pass in up to 1 hint in the array to specify the type of object to include in the cropping result, such as \"signboard\".\n<br>- The algorithm detects all objects in the image that match the hint and generates cropping regions that include as many matched objects as possible.\n<br>- Each cropping region includes up to 10 matched objects. If more than 10 objects match in the image, the cropping region includes up to 10 of them.\n<br>- You can use the MatchedInclusionHints response field to determine whether the hint was successfully matched.\n<br>- This parameter is supported only in regions in the Chinese mainland",
      "help_zh": "希望裁剪结果包含的物体的语义文本描述列表。每个元素为自由文本（如 \"招牌\"、\"菜品\"）。\n\nInclusionHInts参数使用限制：\n<br>-数组最多传入 1 个提示词，用于指定裁剪时希望包含的物体类型（如\"招牌\"）。\n<br>-算法会在图片中检测符合该提示词的所有物体，并生成尽量包含全部匹配物体的裁剪框。\n<br>-单个裁剪框最多包含 10 个匹配物体。若图片中匹配物体超过 10 个，裁剪框将最多包含其中 10 个。\n<br>-可通过响应字段 MatchedInclusionHints 判断提示词是否成功匹配。\n<br>-仅在国内地域支持此参数",
      "location": "query",
      "name": "inclusion_hints",
      "options": [
        "--inclusion-hints"
      ],
      "param_style": "json",
      "raw_name": "InclusionHints",
      "required": false,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "imm",
  "protocol": "HTTP|HTTPS"
}
