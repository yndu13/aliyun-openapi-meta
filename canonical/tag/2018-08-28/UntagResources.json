{
  "camel_example": "",
  "cmd_full_name": "tag untag-resources",
  "cmd_name": "untag-resources",
  "deprecated": false,
  "description_en": "Removes tags from the resources of various Alibaba Cloud services",
  "description_zh": "为指定的多个云产品的多个云资源统一解绑标签",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UntagResources",
  "operation": {
    "action": "UntagResources",
    "api_style": "RPC",
    "api_version": "2018-08-28",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID.\n\nIf the resources belong to a service that is centrally deployed, set the value to `cn-hangzhou` or to the region ID of the resources by referring to https://help.aliyun.com/document_detail/2579691.html.\nIf the resources belong to a service that is not centrally deployed, set the value to the region ID of the resources",
      "help_zh": "地域ID。取值：\n\n对于中心化部署的云服务，您可以填写通用地域`cn-hangzhou`，或者参照https://help.aliyun.com/document_detail/2579691.html填写资源对应的地域ID。\n对于非中心化部署的云服务，您需要填写资源所属的地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "example": "arn:acs:vpc:cn-hangzhou:123456789****:vpc/vpc-bp19dd90tkt6tz7wu****",
      "help_en": "The Alibaba Cloud Resource Name (ARN) of a resource",
      "help_zh": "资源ARN（Aliyun Resource Name）。\n\nN的取值范围：1~50。\n\n资源ARN的格式：`arn:acs:${ProductCode}:${Region}:${Account}:${ResourceType}/${ResourceId}`。各字段含义如下：\n\n`ProductCode`：云服务code。从https://help.aliyun.com/document_detail/2330915.html返回参数中获取。\n`Region`：资源所属的地域ID。支持使用星号（*）表示当前地域。\n`Account`：资源所属的阿里云账号（主账号）ID。支持使用星号（*）表示当前登录的阿里云账号（主账号）。\n`ResourceType`：资源类型。从https://help.aliyun.com/document_detail/2330915.html返回参数中获取。\n`ResourceId`：资源ID。\n\nResourceARN支持传递资源组、操作审计和资源管理的`ProductCode`和`ResourceType`",
      "location": "query",
      "name": "resource_arn",
      "options": [
        "--resource-arn"
      ],
      "param_style": "repeatList",
      "raw_name": "ResourceARN",
      "required": true,
      "type": "array"
    },
    {
      "element": {
        "type": "string"
      },
      "example": "k1",
      "help_en": "The key of tag N.\n\nValid values of N: 1 to 10",
      "help_zh": "标签键。\n\nN的取值范围：1~10",
      "location": "query",
      "name": "tag_key",
      "options": [
        "--tag-key"
      ],
      "param_style": "repeatList",
      "raw_name": "TagKey",
      "required": true,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "Tag",
  "protocol": "HTTP|HTTPS"
}
