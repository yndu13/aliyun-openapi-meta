{
  "camel_example": "",
  "cmd_full_name": "dataworks-public batch-delete-meta-entities",
  "cmd_name": "batch-delete-meta-entities",
  "deprecated": false,
  "description_en": "Deletes multiple meta entities in a batch. This operation deletes custom meta entities and extended table-based objects, such as databases and tables. You cannot delete a column individually. Instead, you must delete the entire table to remove its associated columns",
  "description_zh": "批量删除元数据实体对象，支持删除创建的纯自定义实体，扩展表类型对象(Database/Table)，不支持单独删除column，可以通过删除Table,删除关联Column对象",
  "kebab_example": "",
  "method": "POST",
  "multi_version": true,
  "name": "BatchDeleteMetaEntities",
  "operation": {
    "action": "BatchDeleteMetaEntities",
    "api_style": "RPC",
    "api_version": "2024-05-18",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "element": {
        "type": "string"
      },
      "help_en": "An array of IDs for the meta entities to delete. You can specify up to 10 IDs in a single request. All entities in the batch must have the same EntityType",
      "help_zh": "待删除实体ID列表，最多10个；同一批次必须为同一种entityType",
      "location": "formData",
      "name": "ids",
      "options": [
        "--ids"
      ],
      "param_style": "simple",
      "raw_name": "Ids",
      "required": true,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "dataworks-public",
  "protocol": "HTTPS"
}
