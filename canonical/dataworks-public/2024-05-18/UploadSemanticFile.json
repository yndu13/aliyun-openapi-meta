{
  "camel_example": "",
  "cmd_full_name": "dataworks-public upload-semantic-file",
  "cmd_name": "upload-semantic-file",
  "deprecated": false,
  "description_en": "Requests a temporary OSS PUT upload URL. Complete the PUT upload before the URL expires, then pass the returned FileId to the ReferenceFileIds parameter of CreateSemanticJob",
  "description_zh": "申请临时 OSS PUT 上传地址后，先在有效期内完成 PUT 上传，再将返回的 FileId 填入 CreateSemanticJob 的 ReferenceFileIds",
  "kebab_example": "",
  "method": "POST",
  "multi_version": true,
  "name": "UploadSemanticFile",
  "operation": {
    "action": "UploadSemanticFile",
    "api_style": "RPC",
    "api_version": "2024-05-18",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "example": "reference.pdf",
      "help_en": "The original file name of the reference file to upload. Maximum length: 255 characters. When singleTableFile uses a FileId, only CSV or XLSX files are supported",
      "help_zh": "待上传参考文件的原始文件名，最长 255 个字符。singleTableFile 使用 FileId 时仅支持 CSV 或 XLSX 文件",
      "location": "formData",
      "maxLength": "255",
      "name": "file_name",
      "options": [
        "--file-name"
      ],
      "raw_name": "FileName",
      "required": true,
      "type": "string"
    },
    {
      "example": "application/pdf",
      "help_en": "The MIME type of the object to upload. Maximum length: 128 characters. This value is included in the UploadUrl signature. Use the same Content-Type when performing the PUT request",
      "help_zh": "上传对象的 MIME 类型，最长 128 个字符。该值会参与 UploadUrl 的签名，执行 PUT 时应使用相同的 Content-Type",
      "location": "formData",
      "maxLength": "128",
      "name": "content_type",
      "options": [
        "--content-type"
      ],
      "raw_name": "ContentType",
      "required": true,
      "type": "string"
    },
    {
      "example": "1024",
      "help_en": "The size of the file to upload, in bytes. This value is recorded as attachment metadata. Specify the actual file size",
      "help_zh": "待上传文件大小，单位为字节。该值记录为附件元数据；请填写实际文件大小",
      "location": "formData",
      "name": "size_bytes",
      "options": [
        "--size-bytes"
      ],
      "raw_name": "SizeBytes",
      "required": true,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "dataworks-public",
  "protocol": "HTTPS"
}
