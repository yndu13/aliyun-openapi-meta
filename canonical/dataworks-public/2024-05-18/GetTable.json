{
  "camel_example": "",
  "cmd_full_name": "dataworks-public get-table",
  "cmd_name": "get-table",
  "deprecated": false,
  "description_en": "Retrieves the details of a specified table in Data Map. You can choose whether to return business metadata",
  "description_zh": "获取数据地图中给定数据表详情。是否返回业务元数据可选",
  "kebab_example": "",
  "method": "GET",
  "multi_version": true,
  "name": "GetTable",
  "operation": {
    "action": "GetTable",
    "api_style": "RPC",
    "api_version": "2024-05-18",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "maxcompute-table:::project_name:[schema_name]:table_name",
      "help_en": "The ID of the table. You can obtain this value from the response of the ListTables operation. For more information, see https://help.aliyun.com/document_detail/2880092.html.\n\n\nThe format is `${EntityType}:${InstanceID or encoded URL}:${DataCatalogIdentifier}:${DatabaseName}:${SchemaName}:${TableName}`. Use an empty string as a placeholder for levels that do not exist.\n\nFor MaxCompute and DLF types, use an empty string as a placeholder for the instance ID.\n\nFor StarRocks, the data catalog identifier is the catalog name. For DLF, the data catalog identifier is the catalog ID. Other types do not support the catalog level. Use an empty string as a placeholder.\n\nFor MaxCompute, the database name is the MaxCompute project name. Projects with the three-layer model enabled require a schema name. For projects without the three-layer model enabled, use an empty string as a placeholder for the schema name.\n\nThe following are ID format examples for common types:\n\n`maxcompute-table:::project_name:[schema_name]:table_name`\n\n`dlf-table::catalog_id:database_name::table_name`\n\n`hms-table:instance_id::database_name::table_name`\n\n`holo-table:instance_id::database_name:schema_name:table_name`\n\n`mysql-table:(instance_id|encoded_jdbc_url)::database_name::table_name`\n\nWhere  \n`instance_id`: The instance ID. Required when the data source is registered in instance mode.  \n`encoded_jdbc_url`: The URL-encoded JDBC connection string. Required when the data source is registered by using a connection string.   \n`catalog_id`: The DLF catalog ID.   \n`project_name`: The MaxCompute project name.   \n`database_name`: The database name.   \n`schema_name`: The schema name. For MaxCompute, this is required only when the three-layer model is enabled for the project. Otherwise, use an empty string as a placeholder.   \n`table_name`: The table name.\n\nRecommended procedure for obtaining this parameter: First, call ListCrawlers to obtain the MetaEntityId of the metadata crawler. For types that include a data catalog level, such as DLF and StarRocks, call ListCatalogs to obtain the catalog ID. Then, call ListDatabases to obtain the database ID. If necessary, call ListSchemas to obtain the schema ID. Finally, call ListTables to obtain the target table ID, and use the returned table ID as the Id for this operation",
      "help_zh": "ID，可参考ListTables接口返回结果，以及https://help.aliyun.com/document_detail/2880092.html。\n\n\n格式为`${EntityType}:${实例ID或转义后的URL}:${数据目录标识}:${数据库名称}:${模式名称}:${数据表名称}`，对于不存在的层级使用空字符占位\n\n对于maxcompute和dlf类型，实例ID使用空字符占位。\n\nstarrocks类型的数据目录标识为目录名称，dlf类型的数据目录标识为目录ID, 其他类型不支持目录层级，可用空字符占位。\n\n对于maxcompute类型，数据库名称即为maxcompute项目名称，开启三层模型的项目需要提供模式名称，未开启的项目模式名称处使用空字符占位。\n\n几种常用类型ID格式参考如下\n\n`maxcompute-table:::project_name:[schema_name]:table_name`\n\n`dlf-table::catalog_id:database_name::table_name`\n\n`hms-table:instance_id::database_name::table_name`\n\n`holo-table:instance_id::database_name:schema_name:table_name`\n\n`mysql-table:(instance_id|encoded_jdbc_url)::database_name::table_name`\n\n其中  \n`instance_id`: 实例ID，对应数据源以实例模式注册时需要提供  \n`encoded_jdbc_url`: JDBC连接串的URLEncode编码,对应数据源以连接串方式注册时需要提供   \n`catalog_id`: dlf目录ID   \n`project_name`: maxcompute项目名称   \n`database_name`: 数据库名称   \n`schema_name`: schema名称，对于maxcompute类型，仅所属项目开启三层模型后需要提供。若未开启则使用空字符占位   \n`table_name`: 表名称\n\n获取本参数的推荐流程：先调用 ListCrawlers 获取元数据采集器的 MetaEntityId；对于 DLF、StarRocks 等包含数据目录层级的类型，再调用 ListCatalogs 获取目录 ID；随后调用 ListDatabases 获取数据库 ID，必要时调用 ListSchemas 获取模式 ID，最后调用 ListTables 获取目标表 ID，并将返回的表 ID 作为本 API 的 Id",
      "location": "query",
      "name": "id",
      "options": [
        "--id"
      ],
      "raw_name": "Id",
      "required": true,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to include business metadata. Default value: false",
      "help_zh": "是否包含业务元数据，默认为false",
      "location": "query",
      "name": "include_business_metadata",
      "options": [
        "--include-business-metadata"
      ],
      "raw_name": "IncludeBusinessMetadata",
      "required": false,
      "type": "bool"
    },
    {
      "help_en": "Specifies whether to return extended properties. Set this parameter to `true` to return extended properties, or `false` to not return them",
      "help_zh": "是否返回扩展属性。设置为 `true` 时返回扩展属性，设置为 `false` 时不返回",
      "location": "query",
      "name": "include_extended_properties",
      "options": [
        "--include-extended-properties"
      ],
      "raw_name": "IncludeExtendedProperties",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "dataworks-public",
  "protocol": "HTTPS"
}
