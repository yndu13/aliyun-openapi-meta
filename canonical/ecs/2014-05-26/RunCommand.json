{
  "camel_example": "",
  "cmd_full_name": "ecs run-command",
  "cmd_name": "run-command",
  "deprecated": false,
  "description_en": "Creates and runs a Cloud Assistant command on one or more ECS instances. This operation supports Shell, PowerShell, and Bat scripts, and provides features such as scheduled execution, custom parameters, and execution within containers on instances",
  "description_zh": "本接口用于在一台或多台ECS实例中创建并执行云助手命令，支持Shell、PowerShell或者Bat类型的脚本，支持定时执行、自定义参数和实例内容器执行等功能",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "RunCommand",
  "operation": {
    "action": "RunCommand",
    "api_style": "RPC",
    "api_version": "2014-05-26",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID. You can call https://help.aliyun.com/document_detail/25609.html to query the most recent region list",
      "help_zh": "地域ID。您可以调用https://help.aliyun.com/document_detail/25609.html查看最新的阿里云地域列表。 ",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "rg-bp67acfmxazb4p****",
      "help_en": "The ID of the resource group for the command execution. When you specify this parameter:\n\nIf the ECS instance specified by InstanceId belongs to a non-default resource group, the ECS instance must belong to this resource group.\n\nYou can filter command execution results by specifying this parameter when you call https://help.aliyun.com/document_detail/64840.html or https://help.aliyun.com/document_detail/64845.html",
      "help_zh": "命令执行的资源组ID，当指定该参数时：\n\n当InstanceId对应的ECS实例属于非默认资源组时，该ECS实例必须属于该资源组。\n\n支持通过指定该参数筛选出对应的命令执行结果（通过调用https://help.aliyun.com/document_detail/64840.html或https://help.aliyun.com/document_detail/64845.html）",
      "location": "query",
      "name": "resource_group_id",
      "options": [
        "--resource-group-id"
      ],
      "raw_name": "ResourceGroupId",
      "required": false,
      "type": "string"
    },
    {
      "example": "testName",
      "help_en": "The command name. The name supports all character sets and can be up to 128 characters in length",
      "help_zh": "命令名称。支持全字符集，长度不得超过128个字符",
      "location": "query",
      "name": "name",
      "options": [
        "--name"
      ],
      "raw_name": "Name",
      "required": false,
      "type": "string"
    },
    {
      "example": "testDescription",
      "help_en": "The command description. The description supports all character sets and can be up to 512 characters in length",
      "help_zh": "命令描述。支持全字符集，长度不得超过512个字符",
      "location": "query",
      "name": "description",
      "options": [
        "--description"
      ],
      "raw_name": "Description",
      "required": false,
      "type": "string"
    },
    {
      "example": "RunShellScript",
      "help_en": "The type of the command. Valid values:\n\nRunBatScript: Bat command for Windows instances.\nRunPowerShellScript: PowerShell command for Windows instances.\nRunShellScript: Shell command for Linux instances",
      "help_zh": "命令类型。取值范围：\n\nRunBatScript：适用于Windows实例的Bat命令。\nRunPowerShellScript：适用于Windows实例的PowerShell命令。\nRunShellScript：适用于Linux实例的Shell命令",
      "location": "query",
      "name": "type",
      "options": [
        "--type"
      ],
      "raw_name": "Type",
      "required": true,
      "type": "string"
    },
    {
      "example": "ZWNobyAxMjM=",
      "help_en": "The command content. The command content can be plaintext or Base64-encoded. Note the following:\n\nThe size of the command content after Base64 encoding cannot exceed 24 KB. You can use `KeepCommand` to specify whether to retain the command.\nIf the command content is Base64-encoded, set `ContentEncoding=Base64`.\nSet `EnableParameter=true` to enable custom parameters in the command content:\n    - Custom parameters are defined by enclosing them in `{{}}`. Spaces and line breaks before and after the parameter name within `{{}}` are ignored.\n    - A maximum of 20 custom parameters are supported.\n    - Custom parameter names can contain only a-z, A-Z, 0-9, hyphens (-), and underscores (_). The acs:: prefix for specifying non-built-in environment parameters is not supported. Other characters are not supported. Parameter names are case-insensitive.\n    - Each custom parameter name cannot exceed 64 bytes.\n\nYou can specify built-in environment parameters as custom parameters. When the command is run, Cloud Assistant automatically replaces the parameters with the corresponding values in the environment without manual assignment. The following built-in environment parameters are supported:\n    - `{{ACS::RegionId}}`: The region ID.\n    - `{{ACS::AccountId}}`: The UID of the Alibaba Cloud account.\n    - `{{ACS::InstanceId}}`: The instance ID. When the command is sent to multiple instances and you want to use `{{ACS::InstanceId}}` as a built-in environment parameter, make sure that the Cloud Assistant Agent version is not earlier than the following:\n        - Linux: 2.2.3.309\n        - Windows: 2.1.3.309\n    - `{{ACS::InstanceName}}`: The instance name. When the command is sent to multiple instances and you want to use `{{ACS::InstanceName}}` as a built-in environment parameter, make sure that the Cloud Assistant Agent version is not earlier than the following:\n        - Linux: 2.2.3.344\n        - Windows: 2.1.3.344\n    - `{{ACS::InvokeId}}`: The command execution ID. To use `{{ACS::InvokeId}}` as a built-in environment parameter, make sure that the Cloud Assistant Agent version is not earlier than the following:\n        - Linux: 2.2.3.309\n        - Windows: 2.1.3.309\n    - `{{ACS::CommandId}}`: The command ID. When you call this operation to run a command and want to use `{{ACS::CommandId}}` as a built-in environment parameter, make sure that the Cloud Assistant Agent version is not earlier than the following:\n        - Linux: 2.2.3.309\n        - Windows: 2.1.3.309",
      "help_zh": "命令内容。命令内容可以是明文内容或Base64编码后的内容。您需要注意：\n\n命令内容在Base64编码后的大小不能超过24 KB。您可通过`KeepCommand`设置是否保留命令。\n如果您的命令内容是Base64编码后的内容，则必须设置`ContentEncoding=Base64`。\n指定参数`EnableParameter=true`可在命令内容中启用自定义参数功能：\n    - 用`{{}}`包含的方式定义自定义参数，在`{{}}`内参数名前后的空格以及换行符会被忽略。\n    - 自定义参数个数不能超过20个。\n    - 自定义参数名允许a-zA-Z0-9-_的组合，不支持acs::前缀指定非内置环境参数，不支持其余字符，参数名不区分大小写。\n    - 单个自定义参数名不能超过64字节。\n\n您可以指定内置环境参数作为自定义参数，执行命令时无需手动对参数赋值，云助手将为您自动替换为环境中对应的值。支持指定以下内置环境参数：\n    - `{{ACS::RegionId}}`：地域ID。\n    - `{{ACS::AccountId}}`：阿里云主账号UID。\n    - `{{ACS::InstanceId}}`：实例ID。命令下发到多个实例时，如需指定`{{ACS::InstanceId}}`作为内置环境参数，需确保云助手Agent不低于以下版本：\n        - Linux：2.2.3.309\n        - Windows：2.1.3.309\n    - `{{ACS::InstanceName}}`：实例名称。命令下发到多个实例时，如需指定`{{ACS::InstanceName}}`作为内置环境参数，需确保云助手Agent不低于以下版本：\n        - Linux：2.2.3.344\n        - Windows：2.1.3.344\n    - `{{ACS::InvokeId}}`：命令执行ID。如需指定`{{ACS::InvokeId}}`作为内置环境参数，需确保云助手Agent不低于以下版本：\n        - Linux：2.2.3.309\n        - Windows：2.1.3.309\n    - `{{ACS::CommandId}}`：命令ID。通过调用本接口执行命令时，如需指定`{{ACS::CommandId}}`作为内置环境参数，需确保云助手Agent不低于以下版本： \n        - Linux：2.2.3.309\n        - Windows：2.1.3.309",
      "location": "query",
      "name": "command_content",
      "options": [
        "--command-content"
      ],
      "raw_name": "CommandContent",
      "required": true,
      "type": "string"
    },
    {
      "example": "/home/user",
      "help_en": "The working directory of the command on the ECS instance. Maximum length: 200 characters.\n\nDefault value:\n\nFor Linux instances, the default directory is the home directory of the root user, which is `/root`.\nFor Windows instances, the default directory is the directory where the Cloud Assistant Agent process resides, such as `C:\\Windows\\System32`",
      "help_zh": "命令在ECS实例中的运行目录。长度不得超过200个字符。\n\n默认值：\n\nLinux系统实例默认在管理员（root用户）的home目录下，即`/root`。\nWindows系统实例默认在云助手Agent进程所在目录，例如`C:\\Windows\\System32`",
      "location": "query",
      "name": "working_dir",
      "options": [
        "--working-dir"
      ],
      "raw_name": "WorkingDir",
      "required": false,
      "type": "string"
    },
    {
      "example": "3600",
      "help_en": "The timeout period for the command execution. Unit: seconds.\n\nA timeout occurs when the command cannot run due to process issues, missing modules, or missing Cloud Assistant Agent. After a timeout, the command process is forcefully terminated.\n\nDefault value: 60",
      "help_zh": "执行命令的超时时间，单位：秒。\n\n当因为进程原因、缺失模块、缺失云助手Agent等原因无法运行命令时，会出现超时现象。超时后，会强制终止命令进程。\n\n默认值：60",
      "location": "query",
      "name": "timeout",
      "options": [
        "--timeout"
      ],
      "raw_name": "Timeout",
      "required": false,
      "type": "int"
    },
    {
      "example": "false",
      "help_en": "Specifies whether the command contains custom parameters",
      "help_zh": "命令中是否包含自定义参数。\n\n默认值：false",
      "location": "query",
      "name": "enable_parameter",
      "options": [
        "--enable-parameter"
      ],
      "raw_name": "EnableParameter",
      "required": false,
      "type": "bool"
    },
    {
      "example": "Once",
      "help_en": "The execution mode of the command. Valid values:\n\nOnce: immediately executes the command.\nPeriod: executes the command on a schedule. If you set this parameter to `Period`, you must also specify the `Frequency` parameter.\nNextRebootOnly: automatically executes the command the next time the instance starts.\nEveryReboot: automatically executes the command every time the instance starts.\nDryRun: performs only a dry run of the request without actually executing the command. The dry run checks items such as request parameters, instance execution environment, and Cloud Assistant Agent status.\n\nDefault value:\nIf the `Frequency` parameter is not specified, the default value is `Once`.\nIf the `Frequency` parameter is specified, the command is executed as `Period` regardless of whether this parameter is set.\n\nPrecautions:\nYou can call https://help.aliyun.com/document_detail/64838.html to stop a pending or scheduled command.\nIf this parameter is set to `Period` or `EveryReboot`, you can call https://help.aliyun.com/document_detail/64845.html and specify `IncludeHistory=true` to view the historical records of scheduled command executions",
      "help_zh": "设置命令执行的方式。取值范围：\n\nOnce：立即执行命令。\nPeriod：定时执行命令。当该参数取值为`Period`时，必须同时指定`Frequency`参数。\nNextRebootOnly：当实例下一次启动时，自动执行命令。\nEveryReboot：实例每一次启动都将自动执行命令。\nDryRun：只预检此次请求，命令执行不会实际生效，检查项包括请求参数、实例执行环境、云助手Agent运行状态等。\n\n默认值：\n当不指定`Frequency`参数时，默认值为`Once`。\n当指定`Frequency`参数时，无论是否已设置了该参数值，都将按照`Period`处理。\n\n注意事项：\n您可以调用https://help.aliyun.com/document_detail/64838.html停止待执行的命令或定时执行的命令。\n当该参数取值`Period`或者`EveryReboot`时，您可以调用https://help.aliyun.com/document_detail/64845.html，然后指定8查看命令定时执行的历史记录",
      "location": "query",
      "name": "repeat_mode",
      "options": [
        "--repeat-mode"
      ],
      "raw_name": "RepeatMode",
      "required": false,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "This parameter is deprecated and has no effect if specified",
      "help_zh": "该参数已废弃，传入该参数不会生效",
      "location": "query",
      "name": "timed",
      "options": [
        "--timed"
      ],
      "raw_name": "Timed",
      "required": false,
      "type": "bool"
    },
    {
      "example": "0 */20 * * * ?",
      "help_en": "The execution time for scheduled command execution. Three scheduling methods are supported: fixed interval execution (based on Rate expressions), one-time execution at a specified time, and clock-based scheduled execution (based on Cron expressions).\n\nFixed interval execution: Based on Rate expressions, commands are executed at the specified time interval. The time interval can be specified in seconds (s), minutes (m), hours (h), or days (d). This method is suitable for scenarios that require task execution at fixed intervals. The format is `rate(<interval value><interval unit>)`. For example, to execute a command every 5 minutes, use `rate(5m)`. The following limits apply to fixed interval execution:\n    - The specified interval cannot exceed 7 days or be less than 60 seconds, and must be greater than the timeout period of the scheduled task.\n    - The execution interval is based on a fixed frequency and is independent of the actual execution time of the task. For example, if a command is set to execute every 5 minutes and the task takes 2 minutes to complete, the next execution starts 3 minutes after the task is completed.\n    - The task is not executed immediately upon creation. For example, if a command is set to execute every 5 minutes, the command is not executed immediately when the task is created. Instead, execution starts 5 minutes after the task is created.\n\nOne-time execution at a specified time: The command is executed once at the specified time and time zone. The format is `at(yyyy-MM-dd HH:mm:ss <time zone>)`. If no time zone is specified, the default is UTC. The following three time zone formats are supported:\n    - Full time zone name: For example, `Asia/Shanghai` (China/Shanghai time) or `America/Los_Angeles` (US/Los Angeles time).\n    - GMT offset from Greenwich Mean Time: For example, `GMT+8:00` (UTC+8) or `GMT-7:00` (UTC-7). When using the GMT format, leading zeros are not supported for the hour value.\n    - Time zone abbreviation: Only UTC (Coordinated Universal Time) is supported.\n\n  For example, to execute a command once at 13:15:30 on June 6, 2022 in China/Shanghai time, use `at(2022-06-06 13:15:30 Asia/Shanghai)`. To execute a command once at 13:15:30 on June 6, 2022 in UTC-7, use `at(2022-06-06 13:15:30 GMT-7:00)`.\n\nClock-based scheduled execution (based on Cron expressions): Based on Cron expressions, commands are executed according to the scheduled task settings. The format is `<seconds> <minutes> <hours> <day of month> <month> <day of week> <year (optional)> <time zone>`, which is `<Cron expression> <time zone>`. The scheduled task execution time is calculated based on the Cron expression in the specified time zone. If no time zone is specified, the default is the internal system time zone of the instance that runs the scheduled task. For more information about Cron expressions, see https://help.aliyun.com/document_detail/64769.html. The following three time zone formats are supported:\n    - Full time zone name: For example, `Asia/Shanghai` (China/Shanghai time) or `America/Los_Angeles` (US/Los Angeles time).\n    - GMT offset from Greenwich Mean Time: For example, `GMT+8:00` (UTC+8) or `GMT-7:00` (UTC-7). When using the GMT format, leading zeros are not supported for the hour value.\n    - Time zone abbreviation: Only UTC (Coordinated Universal Time) is supported.\n\n  For example, to execute a command once at 10:15 every day in 2022 in China/Shanghai time, use `0 15 10 ? * * 2022 Asia/Shanghai`. To execute a command every 30 minutes from 10:00 to 11:30 every day in 2022 in UTC+8, use `0 0/30 10-11 * * ? 2022 GMT+8:00`. To execute a command every 5 minutes from 14:00 to 14:55 every day in October every two years starting from 2022 in UTC, use `0 0/5 14 * 10 ? 2022/2 UTC`.\n\n    >The minimum time interval must be greater than or equal to the timeout period of the scheduled task and no less than 10 seconds",
      "help_zh": "定时执行命令的执行时间。目前支持三种定时执行方式：固定时间间隔执行（基于Rate表达式）、仅在指定时间执行一次、基于时钟定时执行（基于Cron表达式）。\n\n固定时间间隔执行：基于Rate表达式，按照设置的时间间隔执行命令。时间间隔支持按秒（s） 、分钟（m） 、小时（h）和天（d）来选择，适用于在固定时间间隔执行任务的场景。格式为`rate(<执行间隔数值><执行间隔单位>)`，如5分钟执行一次，格式为`rate(5m)`。使用固定时间间隔执行有以下限制：\n    - 设置的时间间隔不大于7天、不小于60秒，且需大于定时任务的超时时间。\n    - 执行间隔只基于固定频率，与任务实际执行需要的时间无关。例如设置每5分钟执行一次命令，任务需要2分钟执行完成，则在任务完成3分钟后继续执行下一轮。\n    - 创建任务时不会立即执行。例如设置每5分钟执行一次命令，创建任务时不会立即执行一次命令，而是在任务创建完成后的5分钟后开始执行。\n\n仅在指定时间执行一次：按照设置的时区和执行时间点执行一次命令。格式为`at(yyyy-MM-dd HH:mm:ss <时区>)`，即`at(年-月-日 时:分:秒 <时区>)`。如果不指定时区，默认为UTC时区。时区支持以下三种形式：\n    - 时区全称：  如`Asia/Shanghai`（中国/上海时间）、`America/Los_Angeles`（美国/洛杉矶时间）等。\n    - 时区相对于格林威治时间的偏移量：  如`GMT+8:00`（东八区）、`GMT-7:00`（西七区）等。使用GMT格式时，小时位不支持添加前导零。\n    - 时区缩写：  仅支持UTC（协调世界时间）。\n\n  如果指定在中国/上海时间2022年06月06日13时15分30秒执行一次，格式为：`at(2022-06-06 13:15:30 Asia/Shanghai)`；如果指定在西七区2022年06月06日13时15分30秒执行一次，格式为：`at(2022-06-06 13:15:30 GMT-7:00)`。\n\n基于时钟定时执行（基于Cron表达式）：基于Cron表达式，按照设置的定时任务执行命令。格式为`<秒> <分钟> <小时> <日期> <月份> <星期> <年份（可选）> <时区>`，即`<Cron表达式> <时区>`。在指定的时区下，根据Cron表达式推算定时任务执行时间并执行。若不指定时区，默认为执行定时任务实例的系统内部时区。关于Cron表达式的更多信息，请参见https://help.aliyun.com/document_detail/64769.html。时区支持以下三种形式：\n    - 时区全称：  如`Asia/Shanghai`（中国/上海时间）、`America/Los_Angeles`（美国/洛杉矶时间）等。\n    - 时区相对于格林威治时间的偏移量：  如`GMT+8:00`（东八区）、`GMT-7:00`（西七区）等。使用GMT格式时，小时位不支持添加前导零。\n    - 时区缩写：  仅支持UTC（协调世界时间）。\n  例如，在中国/上海时间，2022年每天上午10:15执行一次命令，格式为`0 15 10 ? * * 2022 Asia/Shanghai`；在东八区时间，2022年每天上午10:00到11:30每隔半小时执行，格式为`0 0/30 10-11 * * ? 2022 GMT+8:00`；在UTC时间，从2022年开始，每隔两年的10月每天下午14:00到下午14:55时间段内每隔5分钟执行，格式为`0 0/5 14 * 10 ? 2022/2 UTC`。\n\n    >设置的最小时间间隔需大于或等于定时任务的超时时间，且不小于10 秒",
      "location": "query",
      "name": "frequency",
      "options": [
        "--frequency"
      ],
      "raw_name": "Frequency",
      "required": false,
      "type": "string"
    },
    {
      "example": "{\"name\":\"Jack\", \"accessKey\":\"LTAI*************\"}",
      "help_en": "The key-value pairs of custom parameters to pass in when the command contains custom parameters. For example, if the command content is `echo {{name}}`, you can use the `Parameter` parameter to pass in the key-value pair `{\"name\":\"Jack\"}`. The custom parameter automatically replaces the variable value `name`, and the command that is actually run is `echo Jack`",
      "help_zh": "命令中包含自定义参数时，执行命令时传入的自定义参数的键值对。例如，命令内容为`echo {{name}}`，则可以通过`Parameter`参数传入键值对`{\"name\":\"Jack\"}`。自定义参数将自动替换变量值`name`，得到一条新的命令，实际执行的是`echo Jack`。\n\n自定义参数的个数范围为0~10，且您需要注意：\n\n键不允许为空字符串，最多支持64个字符。\n值允许为空字符串。\n自定义参数与原始命令内容在Base64编码后，命令内容在Base64编码后的大小不能超过24 KB。您可通过`KeepCommand`设置是否保留命令。\n设置的自定义参数名集合必须为创建命令时定义的参数集的子集。对于未传入的参数，您可以使用空字符串代替。\n\n默认值为空，表示取消设置该参数从而禁用自定义参数",
      "location": "query",
      "name": "parameters",
      "options": [
        "--parameters"
      ],
      "param_style": "json",
      "raw_name": "Parameters",
      "required": false,
      "type": "map",
      "value": {
        "type": "any"
      }
    },
    {
      "example": "false",
      "help_en": "Specifies whether to retain the command after it is run. Valid values:\n\ntrue: The command is retained. You can run it again by calling InvokeCommand. The command counts against the Cloud Assistant command quota.\nfalse: The command is not retained. It is automatically deleted after execution and does not count against the Cloud Assistant command quota.\n\nDefault value: false",
      "help_zh": "执行完该命令后，是否保留下来。取值范围：\n\ntrue：保留。可以通过InvokeCommand再次执行。会占用云助手命令的保有量配额。\nfalse：不保留。执行完成后自动删除，不占用云助手命令的保有量配额。\n\n默认值：false",
      "location": "query",
      "name": "keep_command",
      "options": [
        "--keep-command"
      ],
      "raw_name": "KeepCommand",
      "required": false,
      "type": "bool"
    },
    {
      "example": "Base64",
      "help_en": "The encoding mode of the command content (`CommandContent`). Valid values (case-insensitive):",
      "help_zh": "命令内容（`CommandContent`）的编码方式。取值范围（不区分大小写）：\n\nPlainText：不编码，采用明文传输。\nBase64：Base64编码。\n\n默认值：PlainText，乱填或错填该取值会当作PlainText处理",
      "location": "query",
      "name": "content_encoding",
      "options": [
        "--content-encoding"
      ],
      "raw_name": "ContentEncoding",
      "required": false,
      "type": "string"
    },
    {
      "example": "test",
      "help_en": "The username that is used to run the command on the ECS instance. The username can be up to 255 characters in length.\n\nFor Linux ECS instances, the command is run by the root user by default.\nFor Windows ECS instances, the command is run by the System user by default.\n\nYou can also specify another existing user of the instance to run the command. Running Cloud Assistant commands as a regular user is more secure. For more information, see https://help.aliyun.com/document_detail/203771.html",
      "help_zh": "在ECS实例中执行命令的用户名称。长度不得超过255个字符。\n\nLinux系统的ECS实例，默认以root用户执行命令。\nWindows系统的ECS实例，默认以System用户执行命令。\n\n您也可以指定实例中已存在的其他用户执行命令，以普通用户执行云助手命令更加安全。更多信息，请参见https://help.aliyun.com/document_detail/203771.html",
      "location": "query",
      "name": "username",
      "options": [
        "--username"
      ],
      "raw_name": "Username",
      "required": false,
      "type": "string"
    },
    {
      "example": "axtSecretPassword",
      "help_en": "The name of the password for the user who runs the command on a Windows instance. The value can be up to 255 characters in length",
      "help_zh": "在Windows实例中执行命令的用户的密码名称。长度不得超过255个字符。\n\n当您希望以非默认用户（System）在Windows实例中执行命令时，需要同时传入`Username`和该参数。为降低密码泄露的风险，需要将密码明文托管在系统运维管理的参数仓库中，此处仅传入密码的名称。更多信息，请参见https://help.aliyun.com/document_detail/186828.html以及https://help.aliyun.com/document_detail/203771.html。\n\n当您使用Linux实例的root用户或Windows实例的System用户执行命令时，不需要传递该参数",
      "location": "query",
      "name": "windows_password_name",
      "options": [
        "--windows-password-name"
      ],
      "raw_name": "WindowsPasswordName",
      "required": false,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "example": "i-bp185dy2o3o6neg****",
      "help_en": "The ECS instance ID array. Array length: 1 to 100",
      "help_zh": "ECS实例ID数组，数组长度：1~100。\n\n若指定了多台实例后，其中某台实例不满足执行条件时，您需要重新选择。\n\n您也可以在配额中心申请提升配额（配额名称为命令执行支持实例上限数）",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "param_style": "repeatList",
      "raw_name": "InstanceId",
      "required": false,
      "type": "array"
    },
    {
      "element": {
        "fields": [
          {
            "example": "TestKey",
            "help_en": "The tag key of the command execution. If this value is specified, it cannot be an empty string",
            "help_zh": "命令执行的标签键。一旦传入该值，则不允许为空字符串。\n\n使用一个标签过滤资源，查询到该标签下的资源数量不能超过1000个。使用多个标签过滤资源，查询到同时绑定了多个标签的资源数量不能超过1000个。如果资源数量超过1000个，您需要使用https://help.aliyun.com/document_detail/110425.html接口进行查询。\n\n最多支持64个字符，不能以`aliyun`或`acs:`开头，不能包含`http://`或`https://`",
            "name": "key",
            "options": [
              "--key"
            ],
            "raw_name": "Key",
            "required": false,
            "type": "string"
          },
          {
            "example": "TestValue",
            "help_en": "The tag value of the command execution. The value can be an empty string",
            "help_zh": "命令执行的标签值。该值可以为空字符串。\n\n最多支持128个字符，不能包含`http://`或`https://`",
            "name": "value",
            "options": [
              "--value"
            ],
            "raw_name": "Value",
            "required": false,
            "type": "string"
          }
        ],
        "type": "object"
      },
      "help_en": "The tags. Array length: 0 to 20",
      "help_zh": "标签对数组，数组长度：0~20",
      "location": "query",
      "name": "tag",
      "options": [
        "--tag"
      ],
      "param_style": "repeatList",
      "raw_name": "Tag",
      "required": false,
      "type": "array"
    },
    {
      "example": "ab141ddfbacfe02d9dbc25966ed971536124527097398d419a6746873fea****",
      "help_en": "The container ID. Only 64-bit hexadecimal strings are supported. The `docker://`, `containerd://`, or `cri-o://` prefix can be added to specify the container runtime.\n\nPrecautions:\nIf this parameter is specified, Cloud Assistant runs the script in the specified container of the instance.\nIf this parameter is specified, the script can run only on Linux instances whose Cloud Assistant Agent version is 2.2.3.344 or later.\nIf this parameter is specified, the specified `Username` and `WorkingDir` parameters do not take effect. Commands can be run only by using the default user of the container in the default working directory of the container. For more information, see https://help.aliyun.com/document_detail/456641.html.\nIn Linux containers, only shell scripts are supported. You cannot use a command such as `#!/usr/bin/python` at the beginning of a script to specify the interpreter. For more information, see https://help.aliyun.com/document_detail/456641.html",
      "help_zh": "容器ID。仅支持64位16进制字符串，允许存在`docker://`、`containerd://`或者`cri-o://`前缀来明确指定的容器运行。\n\n注意事项：\n如果指定了该参数，云助手将在实例的指定容器内执行脚本。\n如果指定了该参数，仅支持在云助手Agent版本不低于2.2.3.344的Linux实例内运行。\n如果指定了该参数，已指定的`Username`参数和`WorkingDir`参数将不会生效。仅支持通过容器默认用户在容器的默认工作目录下执行命令。详细信息，请参见https://help.aliyun.com/document_detail/456641.html。\n在Linux容器中只支持执行Shell脚本，不支持在脚本开头使用类似`#!/usr/bin/python`命令的形式指定脚本内容的解释器。详细信息，请参见https://help.aliyun.com/document_detail/456641.html",
      "location": "query",
      "name": "container_id",
      "options": [
        "--container-id"
      ],
      "raw_name": "ContainerId",
      "required": false,
      "type": "string"
    },
    {
      "example": "test-container",
      "help_en": "The container name.\n\nPrecautions:\nIf this parameter is specified, Cloud Assistant runs the script in the specified container of the instance.\nIf this parameter is specified, the script can run only on Linux instances whose Cloud Assistant Agent version is 2.2.3.344 or later.\nIf this parameter is specified, the Username and WorkingDir parameters do not take effect. Commands can be run only by using the default user in the default working directory of the container. For more information, see https://help.aliyun.com/document_detail/456641.html.\nIn Linux containers, only shell scripts can be run. You cannot specify an interpreter for the script content by adding a command such as `#!/usr/bin/python` to the beginning of the script. For more information, see https://help.aliyun.com/document_detail/456641.html",
      "help_zh": "容器名称。\n\n注意事项：\n如果指定了该参数，云助手将在实例的指定容器内执行脚本。\n如果指定了该参数，仅支持在云助手Agent版本不低于2.2.3.344的Linux实例内运行。\n如果指定了该参数，已指定的`Username`参数和`WorkingDir`参数将不会生效。仅支持通过容器默认用户在容器的默认工作目录下执行命令。详细信息，请参见https://help.aliyun.com/document_detail/456641.html。\n在Linux容器中只支持执行Shell脚本，不支持在脚本开头使用类似`#!/usr/bin/python`命令的形式指定脚本内容的解释器。详细信息，请参见https://help.aliyun.com/document_detail/456641.html",
      "location": "query",
      "name": "container_name",
      "options": [
        "--container-name"
      ],
      "raw_name": "ContainerName",
      "required": false,
      "type": "string"
    },
    {
      "example": "123e4567-e89b-12d3-a456-426655440000",
      "help_en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but make sure that the token is unique among different requests. ClientToken supports only ASCII characters and cannot exceed 64 characters in length. For more information, see https://help.aliyun.com/document_detail/25693.html",
      "help_zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符，且不能超过64个字符。更多信息，请参见https://help.aliyun.com/document_detail/25693.html",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "oss://testBucket/testPrefix",
      "help_en": "The OSS delivery configuration for command execution output.\n\nFormat: oss://${BucketName}/${Prefix}, where ${BucketName} is the name of the destination OSS bucket and ${Prefix} is the directory prefix of the destination",
      "help_zh": "命令执行Output OSS投递配置。\n\n格式：oss://${BucketName}/${Prefix}，${BucketName}为待投递到的OSS Bucket名称，${Prefix}为待投递到的目录前缀",
      "location": "query",
      "name": "oss_output_delivery",
      "options": [
        "--oss-output-delivery"
      ],
      "raw_name": "OssOutputDelivery",
      "required": false,
      "type": "string"
    },
    {
      "element": {
        "fields": [
          {
            "example": "TestKey",
            "help_en": "The tag key that is used to filter instances",
            "help_zh": "用于筛选实例的标签键。\n\n注意事项：\n\n与参数InstanceId冲突，不能同时指定。\n\n一旦传入该值，则不允许为空字符串。\n\n标签下的实例数量不能超过InstanceId.N的数量限制；如果实例数量超出限制，建议通过添加批次标签等方式控制实例数量，例如batch: b1。\n\n最多支持64个字符，不能以aliyun或acs:开头，不能包含http://或https://",
            "name": "key",
            "options": [
              "--key"
            ],
            "raw_name": "Key",
            "required": false,
            "type": "string"
          },
          {
            "example": "TestValue",
            "help_en": "The tag value used to filter instances.\n\nPrecautions:\nThe value can be an empty string.\nThe value can be up to 128 characters in length and cannot contain http:// or https://",
            "help_zh": "用于筛选实例的标签值。\n\n注意事项：\n该值可以为空字符串。\n最多支持128个字符，不能包含http://或https://",
            "name": "value",
            "options": [
              "--value"
            ],
            "raw_name": "Value",
            "required": false,
            "type": "string"
          }
        ],
        "type": "object"
      },
      "help_en": "The tags that are used to filter instances. Array length: 0 to 20. You can run commands on instances that have the same tags in batches without specifying InstanceId",
      "help_zh": "用于筛选实例的标签数组，数组长度：0~20。可以在不指定InstanceId的情况下，向具有相同标签的实例批量执行命令",
      "location": "query",
      "name": "resource_tag",
      "options": [
        "--resource-tag"
      ],
      "param_style": "repeatList",
      "raw_name": "ResourceTag",
      "required": false,
      "type": "array"
    },
    {
      "example": "ProcessTree",
      "help_en": "The mode in which the task is stopped (manually stopped or interrupted due to timeout). Valid values:",
      "help_zh": "停止任务（手动停止或执行超时打断）时的模式。可能值：\nProcess：停止当前脚本进程。\nProcessTree：停止当前进程树（脚本进程以及它创建的所有子进程的集合）",
      "location": "query",
      "name": "termination_mode",
      "options": [
        "--termination-mode"
      ],
      "raw_name": "TerminationMode",
      "required": false,
      "type": "string"
    },
    {
      "example": "python3 -u {{ACS::ScriptFileName|Ext(\".py\")}}",
      "help_en": "The bootstrap program for script execution. The value can be up to 1 KB in length",
      "help_zh": "脚本执行的引导程序。长度不能超过 1 KB",
      "location": "query",
      "name": "launcher",
      "options": [
        "--launcher"
      ],
      "raw_name": "Launcher",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ecs",
  "protocol": "HTTP|HTTPS",
  "v1_parameters": [
    {
      "help_en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but make sure that the token is unique among different requests. ClientToken supports only ASCII characters and cannot exceed 64 characters in length. For more information, see https://help.aliyun.com/document_detail/25693.html",
      "help_zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符，且不能超过64个字符。更多信息，请参见https://help.aliyun.com/document_detail/25693.html",
      "name": "ClientToken",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The command content. The command content can be plaintext or Base64-encoded. Note the following:\n\nThe size of the command content after Base64 encoding cannot exceed 24 KB. You can use `KeepCommand` to specify whether to retain the command.\nIf the command content is Base64-encoded, set `ContentEncoding=Base64`.\nSet `EnableParameter=true` to enable custom parameters in the command content:\n    - Custom parameters are defined by enclosing them in `{{}}`. Spaces and line breaks before and after the parameter name within `{{}}` are ignored.\n    - A maximum of 20 custom parameters are supported.\n    - Custom parameter names can contain only a-z, A-Z, 0-9, hyphens (-), and underscores (_). The acs:: prefix for specifying non-built-in environment parameters is not supported. Other characters are not supported. Parameter names are case-insensitive.\n    - Each custom parameter name cannot exceed 64 bytes.\n\nYou can specify built-in environment parameters as custom parameters. When the command is run, Cloud Assistant automatically replaces the parameters with the corresponding values in the environment without manual assignment. The following built-in environment parameters are supported:\n    - `{{ACS::RegionId}}`: The region ID.\n    - `{{ACS::AccountId}}`: The UID of the Alibaba Cloud account.\n    - `{{ACS::InstanceId}}`: The instance ID. When the command is sent to multiple instances and you want to use `{{ACS::InstanceId}}` as a built-in environment parameter, make sure that the Cloud Assistant Agent version is not earlier than the following:\n        - Linux: 2.2.3.309\n        - Windows: 2.1.3.309\n    - `{{ACS::InstanceName}}`: The instance name. When the command is sent to multiple instances and you want to use `{{ACS::InstanceName}}` as a built-in environment parameter, make sure that the Cloud Assistant Agent version is not earlier than the following:\n        - Linux: 2.2.3.344\n        - Windows: 2.1.3.344\n    - `{{ACS::InvokeId}}`: The command execution ID. To use `{{ACS::InvokeId}}` as a built-in environment parameter, make sure that the Cloud Assistant Agent version is not earlier than the following:\n        - Linux: 2.2.3.309\n        - Windows: 2.1.3.309\n    - `{{ACS::CommandId}}`: The command ID. When you call this operation to run a command and want to use `{{ACS::CommandId}}` as a built-in environment parameter, make sure that the Cloud Assistant Agent version is not earlier than the following:\n        - Linux: 2.2.3.309\n        - Windows: 2.1.3.309",
      "help_zh": "命令内容。命令内容可以是明文内容或Base64编码后的内容。您需要注意：\n\n命令内容在Base64编码后的大小不能超过24 KB。您可通过`KeepCommand`设置是否保留命令。\n如果您的命令内容是Base64编码后的内容，则必须设置`ContentEncoding=Base64`。\n指定参数`EnableParameter=true`可在命令内容中启用自定义参数功能：\n    - 用`{{}}`包含的方式定义自定义参数，在`{{}}`内参数名前后的空格以及换行符会被忽略。\n    - 自定义参数个数不能超过20个。\n    - 自定义参数名允许a-zA-Z0-9-_的组合，不支持acs::前缀指定非内置环境参数，不支持其余字符，参数名不区分大小写。\n    - 单个自定义参数名不能超过64字节。\n\n您可以指定内置环境参数作为自定义参数，执行命令时无需手动对参数赋值，云助手将为您自动替换为环境中对应的值。支持指定以下内置环境参数：\n    - `{{ACS::RegionId}}`：地域ID。\n    - `{{ACS::AccountId}}`：阿里云主账号UID。\n    - `{{ACS::InstanceId}}`：实例ID。命令下发到多个实例时，如需指定`{{ACS::InstanceId}}`作为内置环境参数，需确保云助手Agent不低于以下版本：\n        - Linux：2.2.3.309\n        - Windows：2.1.3.309\n    - `{{ACS::InstanceName}}`：实例名称。命令下发到多个实例时，如需指定`{{ACS::InstanceName}}`作为内置环境参数，需确保云助手Agent不低于以下版本：\n        - Linux：2.2.3.344\n        - Windows：2.1.3.344\n    - `{{ACS::InvokeId}}`：命令执行ID。如需指定`{{ACS::InvokeId}}`作为内置环境参数，需确保云助手Agent不低于以下版本：\n        - Linux：2.2.3.309\n        - Windows：2.1.3.309\n    - `{{ACS::CommandId}}`：命令ID。通过调用本接口执行命令时，如需指定`{{ACS::CommandId}}`作为内置环境参数，需确保云助手Agent不低于以下版本： \n        - Linux：2.2.3.309\n        - Windows：2.1.3.309",
      "name": "CommandContent",
      "position": "query",
      "required": true,
      "type": "string"
    },
    {
      "help_en": "The container ID. Only 64-bit hexadecimal strings are supported. The `docker://`, `containerd://`, or `cri-o://` prefix can be added to specify the container runtime.\n\nPrecautions:\nIf this parameter is specified, Cloud Assistant runs the script in the specified container of the instance.\nIf this parameter is specified, the script can run only on Linux instances whose Cloud Assistant Agent version is 2.2.3.344 or later.\nIf this parameter is specified, the specified `Username` and `WorkingDir` parameters do not take effect. Commands can be run only by using the default user of the container in the default working directory of the container. For more information, see https://help.aliyun.com/document_detail/456641.html.\nIn Linux containers, only shell scripts are supported. You cannot use a command such as `#!/usr/bin/python` at the beginning of a script to specify the interpreter. For more information, see https://help.aliyun.com/document_detail/456641.html",
      "help_zh": "容器ID。仅支持64位16进制字符串，允许存在`docker://`、`containerd://`或者`cri-o://`前缀来明确指定的容器运行。\n\n注意事项：\n如果指定了该参数，云助手将在实例的指定容器内执行脚本。\n如果指定了该参数，仅支持在云助手Agent版本不低于2.2.3.344的Linux实例内运行。\n如果指定了该参数，已指定的`Username`参数和`WorkingDir`参数将不会生效。仅支持通过容器默认用户在容器的默认工作目录下执行命令。详细信息，请参见https://help.aliyun.com/document_detail/456641.html。\n在Linux容器中只支持执行Shell脚本，不支持在脚本开头使用类似`#!/usr/bin/python`命令的形式指定脚本内容的解释器。详细信息，请参见https://help.aliyun.com/document_detail/456641.html",
      "name": "ContainerId",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The container name.\n\nPrecautions:\nIf this parameter is specified, Cloud Assistant runs the script in the specified container of the instance.\nIf this parameter is specified, the script can run only on Linux instances whose Cloud Assistant Agent version is 2.2.3.344 or later.\nIf this parameter is specified, the Username and WorkingDir parameters do not take effect. Commands can be run only by using the default user in the default working directory of the container. For more information, see https://help.aliyun.com/document_detail/456641.html.\nIn Linux containers, only shell scripts can be run. You cannot specify an interpreter for the script content by adding a command such as `#!/usr/bin/python` to the beginning of the script. For more information, see https://help.aliyun.com/document_detail/456641.html",
      "help_zh": "容器名称。\n\n注意事项：\n如果指定了该参数，云助手将在实例的指定容器内执行脚本。\n如果指定了该参数，仅支持在云助手Agent版本不低于2.2.3.344的Linux实例内运行。\n如果指定了该参数，已指定的`Username`参数和`WorkingDir`参数将不会生效。仅支持通过容器默认用户在容器的默认工作目录下执行命令。详细信息，请参见https://help.aliyun.com/document_detail/456641.html。\n在Linux容器中只支持执行Shell脚本，不支持在脚本开头使用类似`#!/usr/bin/python`命令的形式指定脚本内容的解释器。详细信息，请参见https://help.aliyun.com/document_detail/456641.html",
      "name": "ContainerName",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The encoding mode of the command content (`CommandContent`). Valid values (case-insensitive):",
      "help_zh": "命令内容（`CommandContent`）的编码方式。取值范围（不区分大小写）：\n\nPlainText：不编码，采用明文传输。\nBase64：Base64编码。\n\n默认值：PlainText，乱填或错填该取值会当作PlainText处理",
      "name": "ContentEncoding",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The command description. The description supports all character sets and can be up to 512 characters in length",
      "help_zh": "命令描述。支持全字符集，长度不得超过512个字符",
      "name": "Description",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "Specifies whether the command contains custom parameters",
      "help_zh": "命令中是否包含自定义参数。\n\n默认值：false",
      "name": "EnableParameter",
      "position": "query",
      "required": false,
      "type": "bool"
    },
    {
      "help_en": "The execution time for scheduled command execution. Three scheduling methods are supported: fixed interval execution (based on Rate expressions), one-time execution at a specified time, and clock-based scheduled execution (based on Cron expressions).\n\nFixed interval execution: Based on Rate expressions, commands are executed at the specified time interval. The time interval can be specified in seconds (s), minutes (m), hours (h), or days (d). This method is suitable for scenarios that require task execution at fixed intervals. The format is `rate(<interval value><interval unit>)`. For example, to execute a command every 5 minutes, use `rate(5m)`. The following limits apply to fixed interval execution:\n    - The specified interval cannot exceed 7 days or be less than 60 seconds, and must be greater than the timeout period of the scheduled task.\n    - The execution interval is based on a fixed frequency and is independent of the actual execution time of the task. For example, if a command is set to execute every 5 minutes and the task takes 2 minutes to complete, the next execution starts 3 minutes after the task is completed.\n    - The task is not executed immediately upon creation. For example, if a command is set to execute every 5 minutes, the command is not executed immediately when the task is created. Instead, execution starts 5 minutes after the task is created.\n\nOne-time execution at a specified time: The command is executed once at the specified time and time zone. The format is `at(yyyy-MM-dd HH:mm:ss <time zone>)`. If no time zone is specified, the default is UTC. The following three time zone formats are supported:\n    - Full time zone name: For example, `Asia/Shanghai` (China/Shanghai time) or `America/Los_Angeles` (US/Los Angeles time).\n    - GMT offset from Greenwich Mean Time: For example, `GMT+8:00` (UTC+8) or `GMT-7:00` (UTC-7). When using the GMT format, leading zeros are not supported for the hour value.\n    - Time zone abbreviation: Only UTC (Coordinated Universal Time) is supported.\n\n  For example, to execute a command once at 13:15:30 on June 6, 2022 in China/Shanghai time, use `at(2022-06-06 13:15:30 Asia/Shanghai)`. To execute a command once at 13:15:30 on June 6, 2022 in UTC-7, use `at(2022-06-06 13:15:30 GMT-7:00)`.\n\nClock-based scheduled execution (based on Cron expressions): Based on Cron expressions, commands are executed according to the scheduled task settings. The format is `<seconds> <minutes> <hours> <day of month> <month> <day of week> <year (optional)> <time zone>`, which is `<Cron expression> <time zone>`. The scheduled task execution time is calculated based on the Cron expression in the specified time zone. If no time zone is specified, the default is the internal system time zone of the instance that runs the scheduled task. For more information about Cron expressions, see https://help.aliyun.com/document_detail/64769.html. The following three time zone formats are supported:\n    - Full time zone name: For example, `Asia/Shanghai` (China/Shanghai time) or `America/Los_Angeles` (US/Los Angeles time).\n    - GMT offset from Greenwich Mean Time: For example, `GMT+8:00` (UTC+8) or `GMT-7:00` (UTC-7). When using the GMT format, leading zeros are not supported for the hour value.\n    - Time zone abbreviation: Only UTC (Coordinated Universal Time) is supported.\n\n  For example, to execute a command once at 10:15 every day in 2022 in China/Shanghai time, use `0 15 10 ? * * 2022 Asia/Shanghai`. To execute a command every 30 minutes from 10:00 to 11:30 every day in 2022 in UTC+8, use `0 0/30 10-11 * * ? 2022 GMT+8:00`. To execute a command every 5 minutes from 14:00 to 14:55 every day in October every two years starting from 2022 in UTC, use `0 0/5 14 * 10 ? 2022/2 UTC`.\n\n    >The minimum time interval must be greater than or equal to the timeout period of the scheduled task and no less than 10 seconds",
      "help_zh": "定时执行命令的执行时间。目前支持三种定时执行方式：固定时间间隔执行（基于Rate表达式）、仅在指定时间执行一次、基于时钟定时执行（基于Cron表达式）。\n\n固定时间间隔执行：基于Rate表达式，按照设置的时间间隔执行命令。时间间隔支持按秒（s） 、分钟（m） 、小时（h）和天（d）来选择，适用于在固定时间间隔执行任务的场景。格式为`rate(<执行间隔数值><执行间隔单位>)`，如5分钟执行一次，格式为`rate(5m)`。使用固定时间间隔执行有以下限制：\n    - 设置的时间间隔不大于7天、不小于60秒，且需大于定时任务的超时时间。\n    - 执行间隔只基于固定频率，与任务实际执行需要的时间无关。例如设置每5分钟执行一次命令，任务需要2分钟执行完成，则在任务完成3分钟后继续执行下一轮。\n    - 创建任务时不会立即执行。例如设置每5分钟执行一次命令，创建任务时不会立即执行一次命令，而是在任务创建完成后的5分钟后开始执行。\n\n仅在指定时间执行一次：按照设置的时区和执行时间点执行一次命令。格式为`at(yyyy-MM-dd HH:mm:ss <时区>)`，即`at(年-月-日 时:分:秒 <时区>)`。如果不指定时区，默认为UTC时区。时区支持以下三种形式：\n    - 时区全称：  如`Asia/Shanghai`（中国/上海时间）、`America/Los_Angeles`（美国/洛杉矶时间）等。\n    - 时区相对于格林威治时间的偏移量：  如`GMT+8:00`（东八区）、`GMT-7:00`（西七区）等。使用GMT格式时，小时位不支持添加前导零。\n    - 时区缩写：  仅支持UTC（协调世界时间）。\n\n  如果指定在中国/上海时间2022年06月06日13时15分30秒执行一次，格式为：`at(2022-06-06 13:15:30 Asia/Shanghai)`；如果指定在西七区2022年06月06日13时15分30秒执行一次，格式为：`at(2022-06-06 13:15:30 GMT-7:00)`。\n\n基于时钟定时执行（基于Cron表达式）：基于Cron表达式，按照设置的定时任务执行命令。格式为`<秒> <分钟> <小时> <日期> <月份> <星期> <年份（可选）> <时区>`，即`<Cron表达式> <时区>`。在指定的时区下，根据Cron表达式推算定时任务执行时间并执行。若不指定时区，默认为执行定时任务实例的系统内部时区。关于Cron表达式的更多信息，请参见https://help.aliyun.com/document_detail/64769.html。时区支持以下三种形式：\n    - 时区全称：  如`Asia/Shanghai`（中国/上海时间）、`America/Los_Angeles`（美国/洛杉矶时间）等。\n    - 时区相对于格林威治时间的偏移量：  如`GMT+8:00`（东八区）、`GMT-7:00`（西七区）等。使用GMT格式时，小时位不支持添加前导零。\n    - 时区缩写：  仅支持UTC（协调世界时间）。\n  例如，在中国/上海时间，2022年每天上午10:15执行一次命令，格式为`0 15 10 ? * * 2022 Asia/Shanghai`；在东八区时间，2022年每天上午10:00到11:30每隔半小时执行，格式为`0 0/30 10-11 * * ? 2022 GMT+8:00`；在UTC时间，从2022年开始，每隔两年的10月每天下午14:00到下午14:55时间段内每隔5分钟执行，格式为`0 0/5 14 * 10 ? 2022/2 UTC`。\n\n    >设置的最小时间间隔需大于或等于定时任务的超时时间，且不小于10 秒",
      "name": "Frequency",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The ECS instance ID array. Array length: 1 to 100",
      "help_zh": "ECS实例ID数组，数组长度：1~100。\n\n若指定了多台实例后，其中某台实例不满足执行条件时，您需要重新选择。\n\n您也可以在配额中心申请提升配额（配额名称为命令执行支持实例上限数）",
      "name": "InstanceId",
      "param_style": "repeatList",
      "position": "query",
      "required": false,
      "type": "array"
    },
    {
      "help_en": "Specifies whether to retain the command after it is run. Valid values:\n\ntrue: The command is retained. You can run it again by calling InvokeCommand. The command counts against the Cloud Assistant command quota.\nfalse: The command is not retained. It is automatically deleted after execution and does not count against the Cloud Assistant command quota.\n\nDefault value: false",
      "help_zh": "执行完该命令后，是否保留下来。取值范围：\n\ntrue：保留。可以通过InvokeCommand再次执行。会占用云助手命令的保有量配额。\nfalse：不保留。执行完成后自动删除，不占用云助手命令的保有量配额。\n\n默认值：false",
      "name": "KeepCommand",
      "position": "query",
      "required": false,
      "type": "bool"
    },
    {
      "help_en": "The bootstrap program for script execution. The value can be up to 1 KB in length",
      "help_zh": "脚本执行的引导程序。长度不能超过 1 KB",
      "name": "Launcher",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The command name. The name supports all character sets and can be up to 128 characters in length",
      "help_zh": "命令名称。支持全字符集，长度不得超过128个字符",
      "name": "Name",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The OSS delivery configuration for command execution output.\n\nFormat: oss://${BucketName}/${Prefix}, where ${BucketName} is the name of the destination OSS bucket and ${Prefix} is the directory prefix of the destination",
      "help_zh": "命令执行Output OSS投递配置。\n\n格式：oss://${BucketName}/${Prefix}，${BucketName}为待投递到的OSS Bucket名称，${Prefix}为待投递到的目录前缀",
      "name": "OssOutputDelivery",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The key-value pairs of custom parameters to pass in when the command contains custom parameters. For example, if the command content is `echo {{name}}`, you can use the `Parameter` parameter to pass in the key-value pair `{\"name\":\"Jack\"}`. The custom parameter automatically replaces the variable value `name`, and the command that is actually run is `echo Jack`",
      "help_zh": "命令中包含自定义参数时，执行命令时传入的自定义参数的键值对。例如，命令内容为`echo {{name}}`，则可以通过`Parameter`参数传入键值对`{\"name\":\"Jack\"}`。自定义参数将自动替换变量值`name`，得到一条新的命令，实际执行的是`echo Jack`。\n\n自定义参数的个数范围为0~10，且您需要注意：\n\n键不允许为空字符串，最多支持64个字符。\n值允许为空字符串。\n自定义参数与原始命令内容在Base64编码后，命令内容在Base64编码后的大小不能超过24 KB。您可通过`KeepCommand`设置是否保留命令。\n设置的自定义参数名集合必须为创建命令时定义的参数集的子集。对于未传入的参数，您可以使用空字符串代替。\n\n默认值为空，表示取消设置该参数从而禁用自定义参数",
      "name": "Parameters",
      "param_style": "json",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The region ID. You can call https://help.aliyun.com/document_detail/25609.html to query the most recent region list",
      "help_zh": "地域ID。您可以调用https://help.aliyun.com/document_detail/25609.html查看最新的阿里云地域列表",
      "name": "RegionId",
      "position": "query",
      "required": true,
      "type": "string"
    },
    {
      "help_en": "The execution mode of the command. Valid values:\n\nOnce: immediately executes the command.\nPeriod: executes the command on a schedule. If you set this parameter to `Period`, you must also specify the `Frequency` parameter.\nNextRebootOnly: automatically executes the command the next time the instance starts.\nEveryReboot: automatically executes the command every time the instance starts.\nDryRun: performs only a dry run of the request without actually executing the command. The dry run checks items such as request parameters, instance execution environment, and Cloud Assistant Agent status.\n\nDefault value:\nIf the `Frequency` parameter is not specified, the default value is `Once`.\nIf the `Frequency` parameter is specified, the command is executed as `Period` regardless of whether this parameter is set.\n\nPrecautions:\nYou can call https://help.aliyun.com/document_detail/64838.html to stop a pending or scheduled command.\nIf this parameter is set to `Period` or `EveryReboot`, you can call https://help.aliyun.com/document_detail/64845.html and specify `IncludeHistory=true` to view the historical records of scheduled command executions",
      "help_zh": "设置命令执行的方式。取值范围：\n\nOnce：立即执行命令。\nPeriod：定时执行命令。当该参数取值为`Period`时，必须同时指定`Frequency`参数。\nNextRebootOnly：当实例下一次启动时，自动执行命令。\nEveryReboot：实例每一次启动都将自动执行命令。\nDryRun：只预检此次请求，命令执行不会实际生效，检查项包括请求参数、实例执行环境、云助手Agent运行状态等。\n\n默认值：\n当不指定`Frequency`参数时，默认值为`Once`。\n当指定`Frequency`参数时，无论是否已设置了该参数值，都将按照`Period`处理。\n\n注意事项：\n您可以调用https://help.aliyun.com/document_detail/64838.html停止待执行的命令或定时执行的命令。\n当该参数取值`Period`或者`EveryReboot`时，您可以调用https://help.aliyun.com/document_detail/64845.html，然后指定8查看命令定时执行的历史记录",
      "name": "RepeatMode",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The ID of the resource group for the command execution. When you specify this parameter:\n\nIf the ECS instance specified by InstanceId belongs to a non-default resource group, the ECS instance must belong to this resource group.\n\nYou can filter command execution results by specifying this parameter when you call https://help.aliyun.com/document_detail/64840.html or https://help.aliyun.com/document_detail/64845.html",
      "help_zh": "命令执行的资源组ID，当指定该参数时：\n\n当InstanceId对应的ECS实例属于非默认资源组时，该ECS实例必须属于该资源组。\n\n支持通过指定该参数筛选出对应的命令执行结果（通过调用https://help.aliyun.com/document_detail/64840.html或https://help.aliyun.com/document_detail/64845.html）",
      "name": "ResourceGroupId",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The tags that are used to filter instances. Array length: 0 to 20. You can run commands on instances that have the same tags in batches without specifying InstanceId",
      "help_zh": "用于筛选实例的标签数组，数组长度：0~20。可以在不指定InstanceId的情况下，向具有相同标签的实例批量执行命令",
      "name": "ResourceTag",
      "param_style": "repeatList",
      "position": "query",
      "required": false,
      "sub_parameters": [
        {
          "name": "Key",
          "position": "query",
          "required": false,
          "type": "string"
        },
        {
          "name": "Value",
          "position": "query",
          "required": false,
          "type": "string"
        }
      ],
      "type": "array"
    },
    {
      "help_en": "The tags. Array length: 0 to 20",
      "help_zh": "标签对数组，数组长度：0~20",
      "name": "Tag",
      "param_style": "repeatList",
      "position": "query",
      "required": false,
      "sub_parameters": [
        {
          "name": "Key",
          "position": "query",
          "required": false,
          "type": "string"
        },
        {
          "name": "Value",
          "position": "query",
          "required": false,
          "type": "string"
        }
      ],
      "type": "array"
    },
    {
      "help_en": "The mode in which the task is stopped (manually stopped or interrupted due to timeout). Valid values:",
      "help_zh": "停止任务（手动停止或执行超时打断）时的模式。可能值：\nProcess：停止当前脚本进程。\nProcessTree：停止当前进程树（脚本进程以及它创建的所有子进程的集合）",
      "name": "TerminationMode",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "This parameter is deprecated and has no effect if specified",
      "help_zh": "该参数已废弃，传入该参数不会生效",
      "name": "Timed",
      "position": "query",
      "required": false,
      "type": "bool"
    },
    {
      "help_en": "The timeout period for the command execution. Unit: seconds.\n\nA timeout occurs when the command cannot run due to process issues, missing modules, or missing Cloud Assistant Agent. After a timeout, the command process is forcefully terminated.\n\nDefault value: 60",
      "help_zh": "执行命令的超时时间，单位：秒。\n\n当因为进程原因、缺失模块、缺失云助手Agent等原因无法运行命令时，会出现超时现象。超时后，会强制终止命令进程。\n\n默认值：60",
      "name": "Timeout",
      "position": "query",
      "required": false,
      "type": "int"
    },
    {
      "help_en": "The type of the command. Valid values:\n\nRunBatScript: Bat command for Windows instances.\nRunPowerShellScript: PowerShell command for Windows instances.\nRunShellScript: Shell command for Linux instances",
      "help_zh": "命令类型。取值范围：\n\nRunBatScript：适用于Windows实例的Bat命令。\nRunPowerShellScript：适用于Windows实例的PowerShell命令。\nRunShellScript：适用于Linux实例的Shell命令",
      "name": "Type",
      "position": "query",
      "required": true,
      "type": "string"
    },
    {
      "help_en": "The username that is used to run the command on the ECS instance. The username can be up to 255 characters in length.\n\nFor Linux ECS instances, the command is run by the root user by default.\nFor Windows ECS instances, the command is run by the System user by default.\n\nYou can also specify another existing user of the instance to run the command. Running Cloud Assistant commands as a regular user is more secure. For more information, see https://help.aliyun.com/document_detail/203771.html",
      "help_zh": "在ECS实例中执行命令的用户名称。长度不得超过255个字符。\n\nLinux系统的ECS实例，默认以root用户执行命令。\nWindows系统的ECS实例，默认以System用户执行命令。\n\n您也可以指定实例中已存在的其他用户执行命令，以普通用户执行云助手命令更加安全。更多信息，请参见https://help.aliyun.com/document_detail/203771.html",
      "name": "Username",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The name of the password for the user who runs the command on a Windows instance. The value can be up to 255 characters in length",
      "help_zh": "在Windows实例中执行命令的用户的密码名称。长度不得超过255个字符。\n\n当您希望以非默认用户（System）在Windows实例中执行命令时，需要同时传入`Username`和该参数。为降低密码泄露的风险，需要将密码明文托管在系统运维管理的参数仓库中，此处仅传入密码的名称。更多信息，请参见https://help.aliyun.com/document_detail/186828.html以及https://help.aliyun.com/document_detail/203771.html。\n\n当您使用Linux实例的root用户或Windows实例的System用户执行命令时，不需要传递该参数",
      "name": "WindowsPasswordName",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The working directory of the command on the ECS instance. Maximum length: 200 characters.\n\nDefault value:\n\nFor Linux instances, the default directory is the home directory of the root user, which is `/root`.\nFor Windows instances, the default directory is the directory where the Cloud Assistant Agent process resides, such as `C:\\Windows\\System32`",
      "help_zh": "命令在ECS实例中的运行目录。长度不得超过200个字符。\n\n默认值：\n\nLinux系统实例默认在管理员（root用户）的home目录下，即`/root`。\nWindows系统实例默认在云助手Agent进程所在目录，例如`C:\\Windows\\System32`",
      "name": "WorkingDir",
      "position": "query",
      "required": false,
      "type": "string"
    }
  ]
}
