{
  "camel_example": "",
  "cmd_full_name": "ecs describe-disk-monitor-data",
  "cmd_name": "describe-disk-monitor-data",
  "deprecated": false,
  "description_en": "Queries the usage information of a cloud disk within a specified time period, such as read IOPS, write IOPS, read bandwidth (B/s), write bandwidth (B/s), read latency (μs), and write latency (μs)",
  "description_zh": "查询一块云盘在指定时间内的使用信息，例如，云盘读IOPS、写IOPS、读带宽（B/s）、写带宽（B/s）、读时延（μs）以及写时延（μs）",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeDiskMonitorData",
  "operation": {
    "action": "DescribeDiskMonitorData",
    "api_style": "RPC",
    "api_version": "2014-05-26",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "d-bp1bq5g3dxxo1x4o****",
      "help_en": "The ID of the cloud disk that you want to query",
      "help_zh": "待查询的云盘ID",
      "location": "query",
      "name": "disk_id",
      "options": [
        "--disk-id"
      ],
      "raw_name": "DiskId",
      "required": true,
      "type": "string"
    },
    {
      "example": "2014-07-23T12:07:00Z",
      "help_en": "The start time of the data. Specify the time in the https://help.aliyun.com/document_detail/25696.html standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC. If the value of seconds (ss) is not 00, the start time is automatically rounded up to the next minute.\n\nYou can query monitoring information only for the last 30 days. The `StartTime` parameter cannot be more than 30 days earlier than the current time",
      "help_zh": "数据的起始时间。按照https://help.aliyun.com/document_detail/25696.html标准表示，并使用UTC +0时间，格式为yyyy-MM-ddTHH:mm:ssZ。如果秒（ss）不是00，则自动取为下一分钟开始时。\n\n一次最多能查询近30天内的监控信息，即参数`StartTime`距今不能超过30天",
      "location": "query",
      "name": "start_time",
      "options": [
        "--start-time"
      ],
      "raw_name": "StartTime",
      "required": true,
      "type": "string"
    },
    {
      "example": "2014-07-23T12:09:00Z",
      "help_en": "The end time of the data. Specify the time in the https://help.aliyun.com/document_detail/25696.html standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC. If the value of seconds (ss) is not 00, the end time is automatically rounded up to the next minute",
      "help_zh": "数据的结束时间。按照https://help.aliyun.com/document_detail/25696.html标准表示，并使用UTC +0时间，格式为yyyy-MM-ddTHH:mm:ssZ。如果秒（ss）不是00，则自动取为下一分钟开始时",
      "location": "query",
      "name": "end_time",
      "options": [
        "--end-time"
      ],
      "raw_name": "EndTime",
      "required": true,
      "type": "string"
    },
    {
      "example": "60",
      "help_en": "The granularity of the data. Unit: seconds. Valid values:\n\n60.\n600. \n3600.\n\nDefault value: 60.\n\nThe value of (EndTime – StartTime) / Period must be less than or equal to 400. A maximum of 400 data entries can be returned at a time",
      "help_zh": "数据的精度，单位为秒。取值范围：\n\n60。\n600。 \n3600。\n\n默认值：60。\n\n（EndTime–StartTime）/Period必须小于等于400，即一次最多返回400个周期的数据",
      "location": "query",
      "name": "period",
      "options": [
        "--period"
      ],
      "raw_name": "Period",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "Ecs",
  "protocol": "HTTP|HTTPS"
}
