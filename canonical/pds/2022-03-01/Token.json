{
  "camel_example": "",
  "cmd_full_name": "pds token",
  "cmd_name": "token",
  "deprecated": false,
  "description_en": "Obtains an access token as part of the OAuth 2.0 authorization flow",
  "description_zh": "OAuth2.0授权流程的获取访问令牌",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "Token",
  "operation": {
    "action": "Token",
    "api_style": "ROA",
    "api_version": "2022-03-01",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "url": "/v2/oauth/token"
  },
  "parameters": [
    {
      "example": "refresh_token",
      "help_en": "The grant type. Valid values:\n\n`authorization_code`: Exchanges an authorization code, returned after successful authentication, for an access token.\n\n`refresh_token`: Uses a refresh token from a previous authorization to obtain a new access token.\n\n`urn:ietf:params:oauth:grant-type:jwt-bearer`: Obtains an access token by using a JWT",
      "help_zh": "授权类型，有以下取值：\n\nauthorization_code （通过认证完成后获取的code来换取token）\n\nrefresh_token（通过上次授权获取的refresh_token来获取token）\n\nurn:ietf:params:oauth:grant-type:jwt-bearer（通过JWT方式获取token）",
      "location": "formData",
      "name": "grant_type",
      "options": [
        "--grant-type"
      ],
      "raw_name": "grant_type",
      "required": true,
      "type": "string"
    },
    {
      "example": "0045157fa8e24f4f9a0d9e3ff158c1e0",
      "help_en": "The authorization code returned in the redirect URI after successful authentication. This parameter is required when `grant_type` is `authorization_code`",
      "help_zh": "认证完成后回调地址中携带的code，当grant_type为authorization_code时，此参数必传",
      "location": "formData",
      "name": "code",
      "options": [
        "--code"
      ],
      "raw_name": "code",
      "required": false,
      "type": "string"
    },
    {
      "example": "https://aliyun.com/pds\n",
      "help_en": "The redirect URI used in the authorization request. This parameter is required when `grant_type` is `authorization_code`",
      "help_zh": "填写发起authorize时传入的redirect_uri，当grant_type为authorization_code时必填",
      "location": "formData",
      "name": "redirect_uri",
      "options": [
        "--redirect-uri"
      ],
      "raw_name": "redirect_uri",
      "required": false,
      "type": "string"
    },
    {
      "example": "1Zu***flH",
      "help_en": "The ID of the app you create in the Alibaba Cloud PDS console",
      "help_zh": "在阿里云PDS控制台申请的App对应的ID",
      "location": "formData",
      "name": "client_id",
      "options": [
        "--client-id"
      ],
      "raw_name": "client_id",
      "required": true,
      "type": "string"
    },
    {
      "example": "80D***3i5\n",
      "help_en": "The secret of the app you create in the Alibaba Cloud PDS console. This parameter is required for WebServer applications",
      "help_zh": "在阿里云PDS控制台申请的App对应的Secret，WebServer类型的App必填此参数",
      "location": "formData",
      "name": "client_secret",
      "options": [
        "--client-secret"
      ],
      "raw_name": "client_secret",
      "required": false,
      "type": "string"
    },
    {
      "example": "399623e13353490391266c7d48a13ed1",
      "help_en": "The token used to refresh an access token. This parameter is required when `grant_type` is `refresh_token`",
      "help_zh": "用于刷新Token的令牌，当grant_type为refresh_token时此参数必传",
      "location": "formData",
      "name": "refresh_token",
      "options": [
        "--refresh-token"
      ],
      "raw_name": "refresh_token",
      "required": false,
      "type": "string"
    },
    {
      "example": "ey***asd\n",
      "help_en": "A JWT-signed string that contains user information and authorization parameters to be granted. For the structure, see JWTPayload. This parameter is required when `grant_type` is `urn:ietf:params:oauth:grant-type:jwt-bearer`",
      "help_zh": "通过JWT私钥签名的字符串，里面包含待授权的用户信息和授权参数，结构参考JWTPayload。当grant_type为urn:ietf:params:oauth:grant-type:jwt-bearer时此参数必填",
      "location": "formData",
      "name": "assertion",
      "options": [
        "--assertion"
      ],
      "raw_name": "assertion",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/v2/oauth/token",
  "product_code": "pds",
  "protocol": "HTTPS",
  "tags": [
    "api"
  ],
  "v1_parameters": [
    {
      "help_en": "A JWT-signed string that contains user information and authorization parameters to be granted. For the structure, see JWTPayload. This parameter is required when `grant_type` is `urn:ietf:params:oauth:grant-type:jwt-bearer`",
      "help_zh": "通过JWT私钥签名的字符串，里面包含待授权的用户信息和授权参数，结构参考JWTPayload。当grant_type为urn:ietf:params:oauth:grant-type:jwt-bearer时此参数必填",
      "name": "assertion",
      "position": "body",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The ID of the app you create in the Alibaba Cloud PDS console",
      "help_zh": "在阿里云PDS控制台申请的App对应的ID",
      "name": "client_id",
      "position": "body",
      "required": true,
      "type": "string"
    },
    {
      "help_en": "The secret of the app you create in the Alibaba Cloud PDS console. This parameter is required for WebServer applications",
      "help_zh": "在阿里云PDS控制台申请的App对应的Secret，WebServer类型的App必填此参数",
      "name": "client_secret",
      "position": "body",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The authorization code returned in the redirect URI after successful authentication. This parameter is required when `grant_type` is `authorization_code`",
      "help_zh": "认证完成后回调地址中携带的code，当grant_type为authorization_code时，此参数必传",
      "name": "code",
      "position": "body",
      "required": false,
      "type": "string"
    },
    {
      "name": "domain_id",
      "position": "host",
      "required": true,
      "type": "string"
    },
    {
      "help_en": "The grant type. Valid values:\n\n`authorization_code`: Exchanges an authorization code, returned after successful authentication, for an access token.\n\n`refresh_token`: Uses a refresh token from a previous authorization to obtain a new access token.\n\n`urn:ietf:params:oauth:grant-type:jwt-bearer`: Obtains an access token by using a JWT",
      "help_zh": "授权类型，有以下取值：\n\nauthorization_code （通过认证完成后获取的code来换取token）\n\nrefresh_token（通过上次授权获取的refresh_token来获取token）\n\nurn:ietf:params:oauth:grant-type:jwt-bearer（通过JWT方式获取token）",
      "name": "grant_type",
      "position": "body",
      "required": true,
      "type": "string"
    },
    {
      "help_en": "The redirect URI used in the authorization request. This parameter is required when `grant_type` is `authorization_code`",
      "help_zh": "填写发起authorize时传入的redirect_uri，当grant_type为authorization_code时必填",
      "name": "redirect_uri",
      "position": "body",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The token used to refresh an access token. This parameter is required when `grant_type` is `refresh_token`",
      "help_zh": "用于刷新Token的令牌，当grant_type为refresh_token时此参数必传",
      "name": "refresh_token",
      "position": "body",
      "required": false,
      "type": "string"
    }
  ]
}
