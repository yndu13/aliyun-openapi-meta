{
  "camel_example": "",
  "cmd_full_name": "pds list-revision",
  "cmd_name": "list-revision",
  "deprecated": false,
  "description_en": "Lists the revisions of a file",
  "description_zh": "列举版本信息",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "ListRevision",
  "operation": {
    "action": "ListRevision",
    "api_style": "ROA",
    "api_version": "2022-03-01",
    "body_mapping": {
      "drive_id": "drive_id",
      "fields": "fields",
      "file_id": "file_id",
      "limit": "limit",
      "marker": "marker"
    },
    "content_type": "application/json",
    "method": "POST",
    "protocol": "HTTPS",
    "req_body_type": "json",
    "url": "/v2/file/revision/list"
  },
  "parameters": [
    {
      "example": "1",
      "help_en": "The ID of the drive",
      "help_zh": "空间 id",
      "location": "body",
      "name": "drive_id",
      "options": [
        "--drive-id"
      ],
      "param_style": "json",
      "raw_name": "drive_id",
      "required": true,
      "type": "string"
    },
    {
      "example": "*",
      "help_en": "Specifies the fields to return.\n\nBy default, this parameter is empty. If you set this parameter to `*`, all fields are returned, including the name of the creator",
      "help_zh": "筛选返回字段\n\n默认为空。当填 * 时，返回所有字段，会比为空时多返回创建者名称",
      "location": "body",
      "name": "fields",
      "options": [
        "--fields"
      ],
      "param_style": "json",
      "raw_name": "fields",
      "required": false,
      "type": "string"
    },
    {
      "example": "9520943DC264",
      "help_en": "The ID of the file",
      "help_zh": "文件 id",
      "location": "body",
      "name": "file_id",
      "options": [
        "--file-id"
      ],
      "param_style": "json",
      "raw_name": "file_id",
      "required": true,
      "type": "string"
    },
    {
      "example": "100",
      "help_en": "The maximum number of revisions to return. The value must be an integer from 1 to 100.\n\nThe default value is 50.\n\nThe actual number of returned revisions may be less than the specified value",
      "help_zh": "限定此次返回资源的数量，取值范围[1,100]。\n\n默认值为50\n\n返回结果可以小于指定的数量，但不能多于指定的数量",
      "location": "body",
      "name": "limit",
      "options": [
        "--limit"
      ],
      "param_style": "json",
      "raw_name": "limit",
      "required": false,
      "type": "int"
    },
    {
      "example": "40CB7794C929",
      "help_en": "A pagination marker. The response includes only revisions that occur alphabetically after the specified value. If you do not specify this parameter, the query starts from the beginning of the collection.\n\nDefault value: None",
      "help_zh": "设定结果从marker之后按字母排序的第一个开始返回。如果不设定，则从头开始返回数据。\n\n默认值：无",
      "location": "body",
      "name": "marker",
      "options": [
        "--marker"
      ],
      "param_style": "json",
      "raw_name": "marker",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/v2/file/revision/list",
  "product_code": "pds",
  "protocol": "HTTPS",
  "tags": [
    "api"
  ],
  "v1_body_parameters": [
    {
      "help_en": "The request body",
      "help_zh": "请求体参数",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    }
  ],
  "v1_parameters": [
    {
      "help_en": "The request body",
      "help_zh": "请求体参数",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The ID of the domain",
      "help_zh": "A short description of struct",
      "name": "domain_id",
      "position": "host",
      "required": true,
      "type": "string"
    }
  ]
}
