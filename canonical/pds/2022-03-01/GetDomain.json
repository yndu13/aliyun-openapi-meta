{
  "camel_example": "",
  "cmd_full_name": "pds get-domain",
  "cmd_name": "get-domain",
  "deprecated": false,
  "description_en": "Obtain domain information",
  "description_zh": "获取Domain信息",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "GetDomain",
  "operation": {
    "action": "GetDomain",
    "api_style": "ROA",
    "api_version": "2022-03-01",
    "body_mapping": {
      "domain_id": "domain_id",
      "fields": "fields",
      "get_quota_used": "get_quota_used"
    },
    "content_type": "application/json",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "req_body_type": "json",
    "system_operation_type": "get",
    "url": "/v2/domain/get"
  },
  "parameters": [
    {
      "example": "bj1",
      "help_en": "The ID of the domain",
      "help_zh": "domain id",
      "location": "body",
      "name": "domain_id",
      "options": [
        "--domain-id"
      ],
      "param_style": "json",
      "raw_name": "domain_id",
      "required": true,
      "type": "string"
    },
    {
      "example": "file_category",
      "help_en": "The domain property fields to obtain, separated by commas. An empty value indicates that no enumeration fields are obtained",
      "help_zh": "需要获取的 domain 属性字段，英文逗号分隔，为空代表不获取任何枚举字段",
      "location": "body",
      "name": "fields",
      "options": [
        "--fields"
      ],
      "param_style": "json",
      "raw_name": "fields",
      "required": false,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to return the used quota of the domain. Default value: false. If the quota of the domain is greater than 0 and you set this parameter to true, the used quota of the domain is returned",
      "help_zh": "是否需要获取domain的已用quota，默认为false。当domain容量配额size_quota大于0时，传true可获取domain已用容量配额",
      "location": "body",
      "name": "get_quota_used",
      "options": [
        "--get-quota-used"
      ],
      "param_style": "json",
      "raw_name": "get_quota_used",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "/v2/domain/get",
  "product_code": "pds",
  "protocol": "HTTP|HTTPS",
  "tags": [
    "admin"
  ],
  "v1_body_parameters": [
    {
      "help_en": "The request parameters",
      "help_zh": "请求参数",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    }
  ]
}
