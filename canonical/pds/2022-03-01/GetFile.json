{
  "camel_example": "",
  "cmd_full_name": "pds get-file",
  "cmd_name": "get-file",
  "deprecated": false,
  "description_en": "Gets the details of a file",
  "description_zh": "获取文件详情",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "GetFile",
  "operation": {
    "action": "GetFile",
    "api_style": "ROA",
    "api_version": "2022-03-01",
    "body_mapping": {
      "drive_id": "drive_id",
      "fields": "fields",
      "file_id": "file_id",
      "share_id": "share_id",
      "thumbnail_processes": "thumbnail_processes",
      "url_expire_sec": "url_expire_sec"
    },
    "content_type": "application/json",
    "method": "POST",
    "protocol": "HTTPS",
    "req_body_type": "json",
    "system_operation_type": "get",
    "url": "/v2/file/get"
  },
  "parameters": [
    {
      "example": "1",
      "help_en": "The drive ID",
      "help_zh": "空间 id",
      "location": "body",
      "name": "drive_id",
      "options": [
        "--drive-id"
      ],
      "param_style": "json",
      "raw_name": "drive_id",
      "required": false,
      "type": "string"
    },
    {
      "example": "*",
      "help_en": "Specifies which fields to return:\n\nIf you set this parameter to `*`, this operation returns all file fields, except for those that must be explicitly requested.\n\nTo return specific fields, provide a comma-separated list, such as `url,thumbnail`. Supported fields include `url`, `thumbnail`, `exif`, `cropping_suggestion`, `characteristic_hash`, `video_metadata`, and `video_preview_metadata`.\n\nYou must include `investigation_info` in the value to retrieve the `investigation_info` field.\n\nBy default, this operation returns all file fields, except for those that require an explicit request",
      "help_zh": "筛选返回字段：\n\n当填 * 时，返回文件所有字段（必须在请求中填写的除外）；\n当只需要特定字段时，可填写为：url、thumbnail、exif、cropping_suggestion、characteristic_hash、video_metadata、video_preview_metadata，当需要多个字段时，以逗号 , 分割，如 url,thumbnail；\n必须填写investigation_info字符串，才会在响应中返回investigation_info字段。\n\n默认返回所有字段（必须进行填写的除外）",
      "location": "body",
      "name": "fields",
      "options": [
        "--fields"
      ],
      "param_style": "json",
      "raw_name": "fields",
      "required": false,
      "type": "string"
    },
    {
      "example": "9520943DC264",
      "help_en": "The file ID",
      "help_zh": "文件 id",
      "location": "body",
      "name": "file_id",
      "options": [
        "--file-id"
      ],
      "param_style": "json",
      "raw_name": "file_id",
      "required": true,
      "type": "string"
    },
    {
      "example": "7JQX1FswpQ8",
      "help_en": "The share ID. To access a file in a share, provide this parameter and authenticate with the `x-share-token` header, in which case `drive_id` is ignored. Otherwise, provide `drive_id` and authenticate with an AccessKey pair or an `access_token`. You must specify either `share_id` or `drive_id`",
      "help_zh": "分享id。\n如果通过分享操作文件，请携带`x-share-token` header鉴权并传递此字段，此时`drive_id`无效。否则请使用`aksk`或者`access_token`方式鉴权，并传递`drive_id`字段。\n`share_id`和`drive_id`两者必须有一个字段赋值",
      "location": "body",
      "name": "share_id",
      "options": [
        "--share-id"
      ],
      "param_style": "json",
      "raw_name": "share_id",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "Specifies the thumbnail configuration. Request up to five thumbnails in a single call by defining custom keys for the map. The response returns the URL for each requested thumbnail under its corresponding key",
      "help_zh": "缩略图配置，可一次性返回最多5个缩略图，map的key可以自定义，返回时按key返回对应的缩略图链接",
      "location": "body",
      "name": "thumbnail_processes",
      "options": [
        "--thumbnail-processes"
      ],
      "param_style": "json",
      "raw_name": "thumbnail_processes",
      "required": false,
      "type": "map",
      "value": {
        "fields": [
          {
            "example": "image/resize,m_fill,h_128,w_128,limit_0",
            "help_en": "The thumbnail processing rules for images. For more information, see the \"IMG implementation modes\" topic of Object Storage Service (OSS). Default value: image/resize,m_fill,h_128,w_128,limit_0",
            "help_zh": "图片类型文件的缩略图规则，参考OSS的图片处理规则。默认为：image/resize,m_fill,h_128,w_128,limit_0",
            "name": "image_thumbnail_process",
            "options": [
              "--image-thumbnail-process"
            ],
            "raw_name": "image_thumbnail_process",
            "required": false,
            "type": "string"
          },
          {
            "example": "image/resize,m_fill,h_128,w_128,limit_0",
            "help_en": "The thumbnail processing rules for documents. For a document, the snapshot of one of the pages in the document is used as the thumbnail. This parameter takes effect on this snapshot. Default value: image/resize,m_fill,h_128,w_128,limit_0",
            "help_zh": "文档类型文件的缩略图规则，文档类型的文件会选择文档中一页的截图作为原图，此参数是基于该截图来做处理。默认为：image/resize,m_fill,h_128,w_128,limit_0",
            "name": "office_thumbnail_process",
            "options": [
              "--office-thumbnail-process"
            ],
            "raw_name": "office_thumbnail_process",
            "required": false,
            "type": "string"
          },
          {
            "example": "video/snapshot,t_1000,f_jpg,w_0,h_0,m_fast,ar_auto",
            "help_en": "The thumbnail processing rules for videos. For more information, see the \"Video snapshots\" topic of OSS. Default value: video/snapshot,t_1000,f_jpg,w_0,h_0,m_fast,ar_auto",
            "help_zh": "视频类型文件的缩略图规则，参考OSS的视频截帧处理规则。默认为：video/snapshot,t_1000,f_jpg,w_0,h_0,m_fast,ar_auto",
            "name": "video_thumbnail_process",
            "options": [
              "--video-thumbnail-process"
            ],
            "raw_name": "video_thumbnail_process",
            "required": false,
            "type": "string"
          }
        ],
        "type": "object"
      }
    },
    {
      "example": "100",
      "help_en": "The expiration time in seconds. The value must be an integer from 10 to 14,400, inclusive",
      "help_zh": "过期时间，单位为秒，取值范围为：[10,14400] 秒",
      "location": "body",
      "name": "url_expire_sec",
      "options": [
        "--url-expire-sec"
      ],
      "param_style": "json",
      "raw_name": "url_expire_sec",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "/v2/file/get",
  "product_code": "pds",
  "protocol": "HTTPS",
  "tags": [
    "api"
  ],
  "v1_body_parameters": [
    {
      "help_en": "The request body parameters",
      "help_zh": "请求体参数",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    }
  ],
  "v1_parameters": [
    {
      "help_en": "The request body parameters",
      "help_zh": "请求体参数",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    },
    {
      "name": "domain_id",
      "position": "host",
      "required": true,
      "type": "string"
    }
  ]
}
