{
  "camel_example": "",
  "cmd_full_name": "pds search-domains",
  "cmd_name": "search-domains",
  "deprecated": false,
  "description_en": "Searches for domains",
  "description_zh": "搜索 domain",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "SearchDomains",
  "operation": {
    "action": "SearchDomains",
    "api_style": "ROA",
    "api_version": "2022-03-01",
    "body_mapping": {
      "limit": "limit",
      "marker": "marker",
      "name": "name",
      "order_by": "order_by"
    },
    "content_type": "application/json",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "req_body_type": "json",
    "system_operation_type": "list",
    "url": "/v2/domain/search"
  },
  "parameters": [
    {
      "example": "50",
      "help_en": "The maximum number of results to return. Valid values: 1 to 100. Default value: 100.\n\nThe number of returned results must be less than or equal to the specified number",
      "help_zh": "限定此次返回资源的数量，取值范围[0,100]，默认100\n\n返回结果可以小于指定的数量，但不能多于指定的数量",
      "location": "body",
      "name": "limit",
      "options": [
        "--limit"
      ],
      "param_style": "json",
      "raw_name": "limit",
      "required": false,
      "type": "int"
    },
    {
      "example": "NWQ1Yjk4YmI1ZDRlYmU1Y2E0YWE0NmJhYWJmODBhNDQ2NzhlMTRhMg",
      "help_en": "The pagination token that is used in the next request to retrieve a new page of results. You do not need to specify this parameter for the first request. You must specify the token that is obtained from the previous query as the value of marker.<br> By default, this parameter is empty",
      "help_zh": "设定结果从marker之后按字母排序的第一个开始返回。如果不设定，则从头开始返回数据。\n<br>默认值：无",
      "location": "body",
      "name": "marker",
      "options": [
        "--marker"
      ],
      "param_style": "json",
      "raw_name": "marker",
      "required": false,
      "type": "string"
    },
    {
      "example": "Hello Drive",
      "help_en": "The name of the domain. Fuzzy search is supported",
      "help_zh": "要搜索的 domain 名称，支持模糊搜索",
      "location": "body",
      "name": "name",
      "options": [
        "--name"
      ],
      "param_style": "json",
      "raw_name": "name",
      "required": false,
      "type": "string"
    },
    {
      "example": "created_at",
      "help_en": "The sorting rule. Set the value to created_at, which specifies that the results are sorted based on the time when the domain was created",
      "help_zh": "排序规则，当前仅支持：\ncreated_at，创建时间",
      "location": "body",
      "name": "order_by",
      "options": [
        "--order-by"
      ],
      "param_style": "json",
      "raw_name": "order_by",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/v2/domain/search",
  "product_code": "pds",
  "protocol": "HTTP|HTTPS",
  "tags": [
    "admin"
  ],
  "v1_body_parameters": [
    {
      "help_en": "The request parameters",
      "help_zh": "请求body",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    }
  ]
}
