{
  "camel_example": "",
  "cmd_full_name": "pds list-file",
  "cmd_name": "list-file",
  "deprecated": false,
  "description_en": "Lists the files and folders that are immediate children of a specified folder",
  "description_zh": "直接文件夹下列举文件和文件夹",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "ListFile",
  "operation": {
    "action": "ListFile",
    "api_style": "ROA",
    "api_version": "2022-03-01",
    "body_mapping": {
      "category": "category",
      "drive_id": "drive_id",
      "fields": "fields",
      "limit": "limit",
      "marker": "marker",
      "order_by": "order_by",
      "order_direction": "order_direction",
      "parent_file_id": "parent_file_id",
      "share_id": "share_id",
      "status": "status",
      "thumbnail_processes": "thumbnail_processes",
      "type": "type"
    },
    "content_type": "application/json",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "req_body_type": "json",
    "system_operation_type": "list",
    "url": "/v2/file/list"
  },
  "parameters": [
    {
      "enum": [
        "image",
        "video",
        "audio",
        "doc",
        "app",
        "zip",
        "others"
      ],
      "example": "image",
      "help_en": "The file category. Valid values:\napp: installation package zip: compressed package image doc: document video audio others\nBy default, files of all categories are returned",
      "help_zh": "文件分类，可选字段如下：\n\napp：安装包\nzip：压缩包\nimage：图片\ndoc：文档\nvideo：视频\naudio：音频\nothers：其他\n\n默认返回所有分类文件",
      "location": "body",
      "name": "category",
      "options": [
        "--category"
      ],
      "param_style": "json",
      "raw_name": "category",
      "required": false,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The drive ID",
      "help_zh": "空间 id",
      "location": "body",
      "name": "drive_id",
      "options": [
        "--drive-id"
      ],
      "param_style": "json",
      "raw_name": "drive_id",
      "required": false,
      "type": "string"
    },
    {
      "example": "url,dir_size,user_tags",
      "help_en": "The field that is used to return additional information about a child subject. Valid values:\n* url: returns the URL of the thumbnail of a file in the response.\n* exif: returns the Exchangeable Image File Format (EXIF) data of a file in the response.\n* cropping_suggestion: returns the cropping suggestion on a file in the response.\n* characteristic_hash: returns the characteristic hash value of a file in the response.\n* video_metadata: returns the metadata of a video file, such as the video duration, bitrate, height, and width, in the response.\n* video_preview_metadata: returns the transcoding information of a video file, such as the transcoding specification for each definition, in the response.\n* investigation_info: returns the investigation information in the response.\n* dir_size: returns the statistics on each subfolder in the response.\n* user_tags: returns the user tags of each child subject in the response.\nYou can specify multiple fields by separating them with commas (,). Example: \"url,dir_size,user_tags\"",
      "help_zh": "设置 fields 时，可以返回子对象的额外信息时，支持的字段为：\nurl 响应中返回文件缩略图地址，下载地址\nexif 响应中返回文件的 exif 信息\ncropping_suggestion 响应中返回文件的裁剪建议信息\ncharacteristic_hash 响应中返回文件的特征 hash 值\nvideo_metadata 响应中返回视频文件的 meta 信息，比如视频时长，比特率，高度，宽度等\nvideo_preview_metadata 响应中返回视频文件的转码信息，比如每个清晰度的转码信息\ninvestigation_info 响应中返回 investigation_info字段\ndir_size 响应中返回每个子文件夹的统计信息\nuser_tags 响应中返回每个子对象的 user_tags\n\n当需要多个信息时，不同字段以逗号分割，如 \"url,dir_size,user_tags\"",
      "location": "body",
      "name": "fields",
      "options": [
        "--fields"
      ],
      "param_style": "json",
      "raw_name": "fields",
      "required": false,
      "type": "string"
    },
    {
      "example": "50",
      "help_en": "The maximum number of results to return. Valid values: 1 to 100.\nThe number of returned entries must be less than or equal to the value of this parameter",
      "help_zh": "限定此次返回资源的数量，取值范围[1,100]。\n\n返回结果可以小于指定的数量，但不能多于指定的数量",
      "location": "body",
      "name": "limit",
      "options": [
        "--limit"
      ],
      "param_style": "json",
      "raw_name": "limit",
      "required": false,
      "type": "int"
    },
    {
      "example": "NWQ1Yjk4YmI1ZDRlYmU1Y2E0YWE0NmJhYWJmODBhNDQ2NzhlMTRhMg",
      "help_en": "The name of the entry after which the list begins. Entries whose names are alphabetically after the value of this parameter are returned. If you do not specify this parameter, all entries are returned.\\\nThis parameter is left empty by default",
      "help_zh": "设定结果从marker之后按字母排序的第一个开始返回。如果不设定，则从头开始返回数据。\n<br>默认值：无",
      "location": "body",
      "name": "marker",
      "options": [
        "--marker"
      ],
      "param_style": "json",
      "raw_name": "marker",
      "required": false,
      "type": "string"
    },
    {
      "enum": [
        "created_at",
        "name",
        "size",
        "updated_at"
      ],
      "example": "updated_at",
      "help_en": "The sorting field. Valid values:\ncreated_at: sorts the entries by creation time. updated_at: sorts the entries by update time. size: sorts the entries by file size. name: sorts the entries by file name.\nDefault value: created_at.\nEnumeration:\n* updated_at: update time\n* size: file size\n* name: file name\n* created_at: creation time",
      "help_zh": "排序字段，可选值如下：\n\ncreated_at：根据创建时间排序\nupdated_at：根据更新时间排序\nsize：根据文件大小排序\nname：根据文件名排序\n\n默认根据 created_at 排序",
      "location": "body",
      "name": "order_by",
      "options": [
        "--order-by"
      ],
      "param_style": "json",
      "raw_name": "order_by",
      "required": false,
      "type": "string"
    },
    {
      "enum": [
        "ASC",
        "DESC"
      ],
      "example": "ASC",
      "help_en": "The sorting direction. Valid values:\nASC: ascending order DESC: descending order\nDefault value: ASC",
      "help_zh": "排序方向，可选字段为：\n\nASC：正序\nDESC：倒序\n\n默认值为ASC",
      "location": "body",
      "name": "order_direction",
      "options": [
        "--order-direction"
      ],
      "param_style": "json",
      "raw_name": "order_direction",
      "required": false,
      "type": "string"
    },
    {
      "example": "root",
      "help_en": "The ID of the parent folder. If the parent folder is a root directory, set this parameter to root",
      "help_zh": "父文件夹 id，当父文件为根目录时，填写 root",
      "location": "body",
      "name": "parent_file_id",
      "options": [
        "--parent-file-id"
      ],
      "param_style": "json",
      "raw_name": "parent_file_id",
      "required": true,
      "type": "string"
    },
    {
      "example": "7JQX1FswpQ8",
      "help_en": "The share ID. If you want to share a file, carry the `x-share-token` header for authentication in the request and specify share_id. In this case, `drive_id` is invalid. Otherwise, use an `AccessKey pair` or `access token` for authentication and specify `drive_id`. You must specify one of `share_id` and `drive_id`",
      "help_zh": "分享id。\n如果通过分享操作文件，请携带`x-share-token` header鉴权并传递此字段，此时`drive_id`无效。否则请使用`aksk`或者`access_token`方式鉴权，并传递`drive_id`字段。\n`share_id`和`drive_id`两者必须有一个字段赋值",
      "location": "body",
      "name": "share_id",
      "options": [
        "--share-id"
      ],
      "param_style": "json",
      "raw_name": "share_id",
      "required": false,
      "type": "string"
    },
    {
      "enum": [
        "uploading",
        "available"
      ],
      "example": "available",
      "help_en": "The state of the files to return. Valid values:\navailable: returns only normal files. uploading: returns only files that are being uploaded.\nBy default, only files in the available state are returned",
      "help_zh": "文件状态，可选字段如下：\n\navailable：只返回正常文件\nuploading：只返回上传中文件\n\n默认只返回 available 状态文件",
      "location": "body",
      "name": "status",
      "options": [
        "--status"
      ],
      "param_style": "json",
      "raw_name": "status",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The thumbnail configurations. Up to five thumbnails can be returned at a time. The value contains key-value pairs. You can customize the keys. The URL of a thumbnail is returned based on the key",
      "help_zh": "缩略图配置，可一次性返回最多5个缩略图，map的key可以自定义，返回时按key返回对应的缩略图链接",
      "location": "body",
      "name": "thumbnail_processes",
      "options": [
        "--thumbnail-processes"
      ],
      "param_style": "json",
      "raw_name": "thumbnail_processes",
      "required": false,
      "type": "map",
      "value": {
        "fields": [
          {
            "example": "image/resize,m_fill,h_128,w_128,limit_0",
            "help_en": "The thumbnail processing rules for images. For more information, see the \"IMG implementation modes\" topic of Object Storage Service (OSS). Default value: image/resize,m_fill,h_128,w_128,limit_0",
            "help_zh": "图片类型文件的缩略图规则，参考OSS的图片处理规则。默认为：image/resize,m_fill,h_128,w_128,limit_0",
            "name": "image_thumbnail_process",
            "options": [
              "--image-thumbnail-process"
            ],
            "raw_name": "image_thumbnail_process",
            "required": false,
            "type": "string"
          },
          {
            "example": "image/resize,m_fill,h_128,w_128,limit_0",
            "help_en": "The thumbnail processing rules for documents. For a document, the snapshot of one of the pages in the document is used as the thumbnail. This parameter takes effect on this snapshot. Default value: image/resize,m_fill,h_128,w_128,limit_0",
            "help_zh": "文档类型文件的缩略图规则，文档类型的文件会选择文档中一页的截图作为原图，此参数是基于该截图来做处理。默认为：image/resize,m_fill,h_128,w_128,limit_0",
            "name": "office_thumbnail_process",
            "options": [
              "--office-thumbnail-process"
            ],
            "raw_name": "office_thumbnail_process",
            "required": false,
            "type": "string"
          },
          {
            "example": "video/snapshot,t_1000,f_jpg,w_0,h_0,m_fast,ar_auto",
            "help_en": "The thumbnail processing rules for videos. For more information, see the \"Video snapshots\" topic of OSS. Default value: video/snapshot,t_1000,f_jpg,w_0,h_0,m_fast,ar_auto",
            "help_zh": "视频类型文件的缩略图规则，参考OSS的视频截帧处理规则。默认为：video/snapshot,t_1000,f_jpg,w_0,h_0,m_fast,ar_auto",
            "name": "video_thumbnail_process",
            "options": [
              "--video-thumbnail-process"
            ],
            "raw_name": "video_thumbnail_process",
            "required": false,
            "type": "string"
          }
        ],
        "type": "object"
      }
    },
    {
      "enum": [
        "file",
        "folder"
      ],
      "example": "file",
      "help_en": "The file type. Valid values:\nfile: returns only files. folder: returns only folders.\nBy default, files of all types are returned",
      "help_zh": "文件类型，可选字段如下：\n\nfile：只返回文件\nfolder：只返回文件夹\n\n默认返回所有类型文件",
      "location": "body",
      "name": "type",
      "options": [
        "--type"
      ],
      "param_style": "json",
      "raw_name": "type",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/v2/file/list",
  "product_code": "pds",
  "protocol": "HTTPS",
  "tags": [
    "api"
  ],
  "v1_body_parameters": [
    {
      "help_en": "The request body",
      "help_zh": "请求体参数",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    }
  ],
  "v1_parameters": [
    {
      "help_en": "The request body",
      "help_zh": "请求体参数",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    },
    {
      "name": "domain_id",
      "position": "host",
      "required": true,
      "type": "string"
    }
  ]
}
