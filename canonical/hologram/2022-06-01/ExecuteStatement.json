{
  "camel_example": "",
  "cmd_full_name": "hologram execute-statement",
  "cmd_name": "execute-statement",
  "deprecated": false,
  "description_en": "Executes an SQL statement synchronously on a specified database via OpenAPI and returns the result",
  "description_zh": "通过OpenAPI在指定数据库上同步执行SQL语句并返回结果",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "ExecuteStatement",
  "operation": {
    "action": "ExecuteStatement",
    "api_style": "ROA",
    "api_version": "2022-06-01",
    "body_mapping": {
      "db_name": "dbName",
      "max_bytes": "maxBytes",
      "max_rows": "maxRows",
      "parameters": "parameters",
      "query_timeout": "queryTimeout",
      "sql": "sql"
    },
    "content_type": "application/json",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "req_body_type": "json",
    "system_operation_type": "none",
    "url": "/api/v1/instances/{instanceId}/executeStatement"
  },
  "parameters": [
    {
      "example": "hgprecn-cn-i7m2ucpyu005",
      "help_en": "The ID of the instance",
      "help_zh": "实例id",
      "location": "path",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "instanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "test_db",
      "help_en": "The name of the database",
      "help_zh": "数据库",
      "location": "body",
      "name": "db_name",
      "options": [
        "--db-name"
      ],
      "param_style": "json",
      "raw_name": "dbName",
      "required": false,
      "type": "string"
    },
    {
      "example": "1024",
      "help_en": "The maximum size of the response, in bytes. Default: 10,485,760 (10 MB). If the response size exceeds this limit, it is truncated, as indicated by the Truncated field in the response",
      "help_zh": "单次返回的最大数据量（字节）。默认 10485760（10 MB），超出时截断并标记 truncated=true",
      "location": "body",
      "name": "max_bytes",
      "options": [
        "--max-bytes"
      ],
      "param_style": "json",
      "raw_name": "maxBytes",
      "required": false,
      "type": "int"
    },
    {
      "example": "300",
      "help_en": "The maximum number of rows to return. Default: 200. Maximum: 1,000. If the result set exceeds this limit, it is truncated, as indicated by the `Truncated` field in the response",
      "help_zh": "最大返回行数，默认 200，最大值 1000。超出部分截断并在响应中通过 `Truncated` 字段标识",
      "location": "body",
      "name": "max_rows",
      "options": [
        "--max-rows"
      ],
      "param_style": "json",
      "raw_name": "maxRows",
      "required": false,
      "type": "int"
    },
    {
      "element": {
        "type": "any"
      },
      "help_en": "An array of bound parameters for a parameterized query. These parameters map sequentially to placeholders (e.g., `$1`, `$2`) in the SQL statement. Supported types include `String`, `Long`, `Double`, `Boolean`, and `null` (for SQL `NULL`). Using parameterized queries helps prevent SQL injection and improves the cache reuse rate for prepared statements. These parameters apply to all SQL statements in the sql field",
      "help_zh": "SQL 参数化查询的绑定参数列表，与 SQL 中的 `$1`, `$2`, ... 占位符按顺序对应。支持的值类型包括：`String`、`Long`、`Double`、`Boolean` 和 `null`（表示 `NULL`）。使用参数化查询可有效防止 SQL 注入，并提升预编译语句的缓存复用率。参数列表作用于 `Statements` 中的每条 SQL",
      "location": "body",
      "name": "parameters",
      "options": [
        "--parameters"
      ],
      "param_style": "json",
      "raw_name": "parameters",
      "required": false,
      "type": "array"
    },
    {
      "example": "5",
      "help_en": "The query timeout, in seconds. Default: 30. Maximum: 30. Minimum: 1. If a query exceeds this time limit, the server cancels it",
      "help_zh": "SQL 执行超时时间（秒）。默认 30，最大 30，最小 1。超时后服务端主动取消查询",
      "location": "body",
      "name": "query_timeout",
      "options": [
        "--query-timeout"
      ],
      "param_style": "json",
      "raw_name": "queryTimeout",
      "required": false,
      "type": "int"
    },
    {
      "example": "select * from test_table limit 10;",
      "help_en": "The SQL statement to execute. The maximum length is 16,384 characters. You can specify multiple SQL statements separated by semicolons. If multiple statements are provided, the API returns the result of the last statement",
      "help_zh": "待执行的 SQL 语句，最大长度 16384 字符。支持多条 SQL 以分号分隔，多条时返回最后一条语句的结果",
      "location": "body",
      "name": "sql",
      "options": [
        "--sql"
      ],
      "param_style": "json",
      "raw_name": "sql",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/api/v1/instances/[instanceId]/executeStatement",
  "product_code": "Hologram",
  "protocol": "HTTPS",
  "v1_body_parameters": [
    {
      "help_en": "The request body",
      "help_zh": "请求体",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    }
  ],
  "v1_parameters": [
    {
      "help_en": "The request body",
      "help_zh": "请求体",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The ID of the instance",
      "help_zh": "实例id",
      "name": "instanceId",
      "position": "path",
      "required": false,
      "type": "string"
    }
  ]
}
