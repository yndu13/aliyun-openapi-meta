{
  "camel_example": "",
  "cmd_full_name": "hologram renew-instance",
  "cmd_name": "renew-instance",
  "deprecated": false,
  "description_en": "Renews a subscription Hologres instance. You can also enable monthly auto-renewal",
  "description_zh": "该接口用于为实例手动续费。可以在续费的同时开启按月自动续费",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "RenewInstance",
  "operation": {
    "action": "RenewInstance",
    "api_style": "ROA",
    "api_version": "2022-06-01",
    "body_mapping": {
      "auto_renew": "autoRenew",
      "duration": "duration"
    },
    "content_type": "application/json",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "json",
    "system_operation_type": "update",
    "url": "/api/v1/instances/{instanceId}/renew"
  },
  "parameters": [
    {
      "example": "hgprecn-cn-i7m2v08uu00a",
      "help_en": "The instance ID",
      "help_zh": "实例id",
      "location": "path",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "instanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to enable monthly auto-renewal. Valid values:\n\ntrue: Enabled.\n\nfalse (default): Disabled.\n\nEnabling auto-renewal on an instance where it is already enabled causes an error",
      "help_zh": "是否开启按月自动续费，默认不开启。取值：\ntrue：开启。\nfalse：不开启\n\n已开启自动续费的实例，再次开启会报错",
      "location": "body",
      "name": "auto_renew",
      "options": [
        "--auto-renew"
      ],
      "param_style": "json",
      "raw_name": "autoRenew",
      "required": false,
      "type": "bool"
    },
    {
      "example": "2",
      "help_en": "The renewal period, in months",
      "help_zh": "续费周期。单位：月",
      "location": "body",
      "name": "duration",
      "options": [
        "--duration"
      ],
      "param_style": "json",
      "raw_name": "duration",
      "required": true,
      "type": "int"
    }
  ],
  "pathPattern": "/api/v1/instances/[instanceId]/renew",
  "product_code": "Hologram",
  "protocol": "HTTP|HTTPS",
  "v1_body_parameters": [
    {
      "help_en": "The request body",
      "help_zh": "请求体",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    }
  ]
}
