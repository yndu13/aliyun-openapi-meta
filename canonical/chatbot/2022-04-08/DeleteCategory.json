{
  "cmd_full_name": "chatbot delete-category",
  "cmd_name": "delete-category",
  "deprecated": false,
  "description_en": "Deletes a category. A published category is marked as deleted and unpublished, while an unpublished category is deleted permanently",
  "description_zh": "删除类目， 已发布的类目变成已删除未发布状态，未发布的类目则直接删除",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "DeleteCategory",
  "operation": {
    "action": "DeleteCategory",
    "api_style": "RPC",
    "api_version": "2022-04-08",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "url": ""
  },
  "parameters": [
    {
      "example": "30000049006",
      "help_en": "The unique identifier of the category",
      "help_zh": "类目ID",
      "location": "formData",
      "name": "category_id",
      "options": [
        "--category-id"
      ],
      "raw_name": "CategoryId",
      "required": true,
      "type": "int"
    },
    {
      "example": "ac627989eb4f8a98ed05fd098bbae5_p_beebot_public",
      "help_en": "The agent key. If not specified, the default agent is used. You can obtain the key from the agent management page of your main account",
      "help_zh": "业务空间key，不设置则访问默认业务空间，key值在主账号业务管理页面获取",
      "location": "query",
      "name": "agent_key",
      "options": [
        "--agent-key"
      ],
      "raw_name": "AgentKey",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Chatbot",
  "protocol": "HTTP|HTTPS"
}
