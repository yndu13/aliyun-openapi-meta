{
  "camel_example": "",
  "cmd_full_name": "airec create-filtering-algorithm",
  "cmd_name": "create-filtering-algorithm",
  "deprecated": false,
  "description_en": "Creates a filtering table. After the filtering table is trained on Machine Learning Platform for AI (PAI), export it to MaxCompute and specify the information about the MaxCompute table in Artificial Intelligence Recommendation (AIRec)",
  "description_zh": "创建召回表，目前召回表由PAI训练完成导出到odps中，在AIRec中指定ODPS表信息",
  "kebab_example": "",
  "method": "POST",
  "multi_version": true,
  "name": "CreateFilteringAlgorithm",
  "operation": {
    "action": "CreateFilteringAlgorithm",
    "api_style": "ROA",
    "api_version": "2020-11-26",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": "/v2/openapi/instances/{instanceId}/filtering-algorithms"
  },
  "parameters": [
    {
      "example": "airec-cn-****",
      "help_en": "The state of the filtering table",
      "help_zh": "实例ID",
      "location": "path",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "instanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "xxx",
      "help_zh": "是否预检参数。\n\n取值：\n\ntrue：不会真实执行。\n\nfalse：会直接执行",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "dryRun",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/v2/openapi/instances/[instanceId]/filtering-algorithms",
  "product_code": "Airec",
  "protocol": "HTTP|HTTPS"
}
