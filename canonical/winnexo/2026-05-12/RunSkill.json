{
  "camel_example": "",
  "cmd_full_name": "winnexo run-skill",
  "cmd_name": "run-skill",
  "deprecated": false,
  "description_en": "Asynchronously triggers skill execution and immediately returns a RunId",
  "description_zh": "异步触发技能执行，立即返回 RunId",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "RunSkill",
  "operation": {
    "action": "RunSkill",
    "api_style": "ROA",
    "api_version": "2026-05-12",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "none",
    "url": "/openapi/runSkill"
  },
  "parameters": [
    {
      "example": "10000",
      "help_en": "The tenant ID. This is a common parameter. If not specified, the default tenant of the caller is used",
      "help_zh": "租户ID，公共参数，缺省时使用调用方默认租户",
      "location": "query",
      "name": "tenant_id",
      "options": [
        "--tenant-id"
      ],
      "raw_name": "tenantId",
      "required": false,
      "type": "string"
    },
    {
      "example": "string_value",
      "help_en": "The skill code (globally unique). This parameter takes priority over skillName",
      "help_zh": "技能编码（全局唯一），优先级高于skillName",
      "location": "formData",
      "name": "skill_code",
      "options": [
        "--skill-code"
      ],
      "raw_name": "skillCode",
      "required": false,
      "type": "string"
    },
    {
      "example": "string_value",
      "help_en": "The skill name. Used when skillCode is not specified. The name must be unique within the tenant scope",
      "help_zh": "技能名称，未传 skillCode 时使用；租户范围内必须唯一",
      "location": "formData",
      "name": "skill_name",
      "options": [
        "--skill-name"
      ],
      "raw_name": "skillName",
      "required": false,
      "type": "string"
    },
    {
      "example": "string_value",
      "help_en": "The placeholder variable value that stores the error message for a fault",
      "help_zh": "保存错误信息的占位变量值",
      "location": "formData",
      "name": "arguments",
      "options": [
        "--arguments"
      ],
      "param_style": "json",
      "raw_name": "arguments",
      "required": false,
      "type": "map",
      "value": {
        "type": "any"
      }
    },
    {
      "example": "string_value",
      "help_en": "The digital worker name. Used to calculate the CodeAgent allowedSkills whitelist based on binding relationships",
      "help_zh": "数字员工名称；用于按绑定关系计算CodeAgent allowedSkills 白名单",
      "location": "formData",
      "name": "operating_object_name",
      "options": [
        "--operating-object-name"
      ],
      "raw_name": "operatingObjectName",
      "required": false,
      "type": "string"
    },
    {
      "example": "quick",
      "help_en": "The abstract model name (model tier). Defaults to standard if not specified",
      "help_zh": "抽象模型名（模型档位），不传默认standard",
      "location": "formData",
      "name": "model",
      "options": [
        "--model"
      ],
      "raw_name": "model",
      "required": false,
      "type": "string"
    },
    {
      "example": "string_value",
      "help_en": "The idempotency token generated by the caller. In the current version, this value is only recorded in metadata and is not used for deduplication",
      "help_zh": "幂等 token，调用方自行生成；当前版本仅记录到 metadata，未做去重",
      "location": "formData",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "clientToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/openapi/runSkill",
  "product_code": "WinNexo",
  "protocol": "HTTPS"
}
