{
  "camel_example": "",
  "cmd_full_name": "arms open-vcluster",
  "cmd_name": "open-vcluster",
  "deprecated": true,
  "description_en": "Activates a virtual cluster",
  "description_zh": "开通虚拟集群",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "OpenVCluster",
  "operation": {
    "action": "OpenVCluster",
    "api_style": "RPC",
    "api_version": "2019-08-08",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "docRequired": true,
      "example": "cn-hangzhou",
      "help_en": "The region ID",
      "help_zh": "地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "docRequired": true,
      "example": "cloud-product-prometheus",
      "help_en": "The type of the cluster. For cloud services, set this parameter to `cloud-product-prometheus`",
      "help_zh": "集群类型，云产品统一为`cloud-product-prometheus`",
      "location": "query",
      "name": "cluster_type",
      "options": [
        "--cluster-type"
      ],
      "raw_name": "ClusterType",
      "required": true,
      "type": "string"
    },
    {
      "example": "10",
      "help_en": "The length of the cluster ID. Default value: 10",
      "help_zh": "集群ID长度，默认为10",
      "location": "query",
      "name": "length",
      "options": [
        "--length"
      ],
      "raw_name": "Length",
      "required": false,
      "type": "int"
    },
    {
      "example": "influxdb",
      "help_en": "The name of the cloud service. This parameter must be specified when ClusterType is set to `cloud-product-prometheus`. Valid values: influxdb, mongodb, and DLA. You cannot specify multiple service names",
      "help_zh": "云产品名称，当`clusterType=cloud-product-prometheus`时该字段必传。目前已经接入的云产品名为influxdb、mongodb、DLA，该参数不支持传入多个",
      "location": "query",
      "name": "product",
      "options": [
        "--product"
      ],
      "raw_name": "Product",
      "required": false,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to create or query a virtual cluster. This parameter provides backward compatibility",
      "help_zh": "创建或者查询虚拟集群开关参数，可以对老数据兼容控制",
      "location": "query",
      "name": "recreate_switch",
      "options": [
        "--recreate-switch"
      ],
      "raw_name": "RecreateSwitch",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "ARMS",
  "protocol": "HTTP|HTTPS"
}
