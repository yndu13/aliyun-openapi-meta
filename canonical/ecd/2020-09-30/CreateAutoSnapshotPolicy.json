{
  "cmd_full_name": "ecd create-auto-snapshot-policy",
  "cmd_name": "create-auto-snapshot-policy",
  "deprecated": false,
  "description_en": "Creates an automatic snapshot policy that enables cloud computers to perform automatic creation of snapshots based on the schedule specified by a Cron expression",
  "description_zh": "创建自动快照策略，让云电脑按照Cron表达式对应的时间自动创建快照",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "CreateAutoSnapshotPolicy",
  "operation": {
    "action": "CreateAutoSnapshotPolicy",
    "api_style": "RPC",
    "api_version": "2020-09-30",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID. You can call https://help.aliyun.com/document_detail/196646.html to query the regions supported by Elastic Desktop Service",
      "help_zh": "地域ID。可以调用https://help.aliyun.com/document_detail/196646.html获取无影云电脑支持的地域列表",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "test_auto_policy",
      "help_en": "The name of the automatic snapshot policy. The name must be 2 to 128 characters in length and can contain letters, digits, colons (:), underscores (_), and hyphens (-). The name must start with a letter or a Chinese character and cannot start with `http://` or `https://`. Default value: empty",
      "help_zh": "自动快照策略的名称。长度为2\\~128个英文或中文字符。必须以大小写字母或中文开头，不能以`http://`和`https://`开头。可以包含数字、半角冒号（:）、下划线（_）或者短划线（-）。默认值：空",
      "location": "query",
      "name": "policy_name",
      "options": [
        "--policy-name"
      ],
      "raw_name": "PolicyName",
      "required": true,
      "type": "string"
    },
    {
      "example": "2",
      "help_en": "The retention period of automatic snapshots. Unit: days. Valid values: 1 to 180",
      "help_zh": "自动快照的保留时间，单位为天。取值范围：1\\~180",
      "location": "query",
      "name": "retention_days",
      "options": [
        "--retention-days"
      ],
      "raw_name": "RetentionDays",
      "required": true,
      "type": "int"
    },
    {
      "example": "0 0 2 ? * 1-7",
      "help_en": "The Cron expression for the scheduled snapshot creation",
      "help_zh": "时间周期调度的Cron表达式",
      "location": "query",
      "name": "cron_expression",
      "options": [
        "--cron-expression"
      ],
      "raw_name": "CronExpression",
      "required": true,
      "type": "string"
    },
    {
      "example": "SYSTEM",
      "help_en": "The cloud disk type",
      "help_zh": "磁盘类型",
      "location": "query",
      "name": "disk_type",
      "options": [
        "--disk-type"
      ],
      "raw_name": "DiskType",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "ecd",
  "protocol": "HTTP|HTTPS"
}
