{
  "cmd_full_name": "ccc change-visibility",
  "cmd_name": "change-visibility",
  "deprecated": false,
  "description_en": "Changes an agent's status to invisible. This is equivalent to the outbound-only mode",
  "description_zh": "修改隐身状态（等同于仅外呼场景）",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "ChangeVisibility",
  "operation": {
    "action": "ChangeVisibility",
    "api_style": "RPC",
    "api_version": "2020-07-01",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "docRequired": true,
      "example": "szpczf",
      "help_en": "The ID of the Cloud Connect Center instance",
      "help_zh": "云联络中心实例ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "sam@szpczf",
      "help_en": "The ID of the agent",
      "help_zh": "坐席ID",
      "location": "query",
      "name": "user_id",
      "options": [
        "--user-id"
      ],
      "raw_name": "UserId",
      "required": false,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to set the agent's status to invisible",
      "help_zh": "是否切换为隐身状态。隐身状态和仅外呼含义一致",
      "location": "query",
      "name": "invisible",
      "options": [
        "--invisible"
      ],
      "raw_name": "Invisible",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "CCC",
  "protocol": "HTTP|HTTPS"
}
