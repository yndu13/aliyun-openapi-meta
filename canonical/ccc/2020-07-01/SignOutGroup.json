{
  "cmd_full_name": "ccc sign-out-group",
  "cmd_name": "sign-out-group",
  "deprecated": false,
  "description_en": "You can call SignOutGroup to check out an agent from the skill group they are currently signed into, which is equivalent to performing an unpublish operation for the agent",
  "description_zh": "调用SignOutGroup使坐席签出当前签入的技能组，即坐席下线操作",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "SignOutGroup",
  "operation": {
    "action": "SignOutGroup",
    "api_style": "RPC",
    "api_version": "2020-07-01",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "ccc-test",
      "help_en": "Instance ID",
      "help_zh": "实例ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "agent@ccc-test",
      "help_en": "Agent ID. If not specified, the agent mapped to the current Resource Access Management (RAM) user is used by default",
      "help_zh": "坐席ID，不填默认使用当前RAM账号映射的坐席",
      "location": "query",
      "name": "user_id",
      "options": [
        "--user-id"
      ],
      "raw_name": "UserId",
      "required": true,
      "type": "string"
    },
    {
      "example": "device",
      "help_en": "Device ID. This parameter is meaningless and can be filled with any value",
      "help_zh": "设备ID，无意义，可以随便填写",
      "location": "query",
      "name": "device_id",
      "options": [
        "--device-id"
      ],
      "raw_name": "DeviceId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "CCC",
  "protocol": "HTTP|HTTPS"
}
