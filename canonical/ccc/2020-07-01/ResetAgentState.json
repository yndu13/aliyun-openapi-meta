{
  "cmd_full_name": "ccc reset-agent-state",
  "cmd_name": "reset-agent-state",
  "deprecated": false,
  "description_en": "Resets the state of a specific agent in a specific instance",
  "description_zh": "调用ResetAgentState重置指定实例下指定坐席的坐席状态",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "ResetAgentState",
  "operation": {
    "action": "ResetAgentState",
    "api_style": "RPC",
    "api_version": "2020-07-01",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "ccc-test",
      "help_en": "The ID of the Cloud Call Center (CCC) instance",
      "help_zh": "实例ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "agent@ccc-test",
      "help_en": "The ID of the agent. This parameter is optional. If omitted, the agent mapped to the current RAM account is reset",
      "help_zh": "坐席ID，非必填，不填默认使用当前RAM账户映射的坐席",
      "location": "query",
      "name": "user_id",
      "options": [
        "--user-id"
      ],
      "raw_name": "UserId",
      "required": false,
      "type": "string"
    },
    {
      "example": "device",
      "help_en": "A string that identifies the device. The value is not processed by the system and can be any string",
      "help_zh": "设备ID，可以随便填写，无特殊作用，无特殊要求",
      "location": "query",
      "name": "device_id",
      "options": [
        "--device-id"
      ],
      "raw_name": "DeviceId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "CCC",
  "protocol": "HTTP|HTTPS"
}
