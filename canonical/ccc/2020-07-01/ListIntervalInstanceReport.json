{
  "cmd_full_name": "ccc list-interval-instance-report",
  "cmd_name": "list-interval-instance-report",
  "deprecated": false,
  "description_en": "You can invoke ListIntervalInstanceReport to obtain the segment statistical report for a specified instance",
  "description_zh": "调用ListIntervalInstanceReport获取指定实例的分段统计报表",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "ListIntervalInstanceReport",
  "operation": {
    "action": "ListIntervalInstanceReport",
    "api_style": "RPC",
    "api_version": "2020-07-01",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "docRequired": true,
      "example": "ccc-test",
      "help_en": "Instance ID",
      "help_zh": "实例ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "1620230400000",
      "help_en": "Start Time, in UNIX timestamp format with millisecond precision. This parameter is optional. The default value is 00:00 of the current day. The time granularity for statistics is hourly, rounded down to the nearest hour, and uses a closed interval",
      "help_zh": "开始时间，格式为Unix时间戳，单位毫秒，非必填，默认为当天的0时，统计的时间精度为小时，向前取整，闭区间",
      "location": "query",
      "maximum": "4133952000000",
      "minimum": "0",
      "name": "start_time",
      "options": [
        "--start-time"
      ],
      "raw_name": "StartTime",
      "required": false,
      "type": "int"
    },
    {
      "example": "1620316799000",
      "help_en": "End Time, formatted as a UNIX timestamp in milliseconds. This parameter is optional. The default value is the current time. If Interval is Daily, the maximum interval between StartTime and EndTime is 180 days. If Interval is Hourly, the maximum interval is 10 days. The time precision for statistics is hourly, snapped backward to the start of the hour, using an open interval. For example, if the original Start Time is 11:12:20 and End Time is 11:45:50, the aligned time range becomes [11:00:00, 12:00:00), meaning greater than or equal to 11:00:00 and less than 12:00:00",
      "help_zh": "截止时间，格式为Unix时间戳，单位毫秒，非必填，默认是当前时间，如果Interval为Daily，StartTime和EndTime最大间隔是180天。如果Interval为Hourly，最大间隔时间为10天。统计的时间精度为小时，向后取整，开区间。例如：开始时间为11:12:20，结束时间为11:45:50，则对齐后的入参时间范围为[11:00:00,12:00:00) ，即大于等于11点，小于12点",
      "location": "query",
      "maximum": "4133952000000",
      "minimum": "0",
      "name": "end_time",
      "options": [
        "--end-time"
      ],
      "raw_name": "EndTime",
      "required": false,
      "type": "int"
    },
    {
      "example": "Hourly",
      "help_en": "Segment statistics type. This parameter is optional. The default value is Daily (daily aggregation)",
      "help_zh": "分段统计类型，非必填，默认为Daily（按天汇总）",
      "location": "query",
      "name": "interval",
      "options": [
        "--interval"
      ],
      "raw_name": "Interval",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "CCC",
  "protocol": "HTTP|HTTPS"
}
