{
  "cmd_full_name": "ccc ready-for-service",
  "cmd_name": "ready-for-service",
  "deprecated": false,
  "description_en": "You can invoke ReadyForService to make a specified agent under a specified instance ready. After becoming ready, the agent enters the idle status",
  "description_zh": "调用ReadyForService使指定实例下的指定坐席就绪，就绪后坐席进入空闲状态",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "ReadyForService",
  "operation": {
    "action": "ReadyForService",
    "api_style": "RPC",
    "api_version": "2020-07-01",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "ccc-test",
      "help_en": "Instance ID",
      "help_zh": "实例ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "user-test@ccc-test",
      "help_en": "Agent ID. If not specified, it defaults to the agent mapped to the current RAM account",
      "help_zh": "坐席ID，不填默认为当前RAM账户映射的坐席",
      "location": "query",
      "name": "user_id",
      "options": [
        "--user-id"
      ],
      "raw_name": "UserId",
      "required": false,
      "type": "string"
    },
    {
      "example": "device",
      "help_en": "Device ID. This parameter is meaningless and can be filled with any value",
      "help_zh": "设备ID，无意义，可以随便填写",
      "location": "query",
      "name": "device_id",
      "options": [
        "--device-id"
      ],
      "raw_name": "DeviceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to enter outbound-only mode after being published. This parameter is optional and defaults to false",
      "help_zh": "上线后是否进入仅外呼模式，选填，默认false",
      "location": "query",
      "name": "outbound_scenario",
      "options": [
        "--outbound-scenario"
      ],
      "raw_name": "OutboundScenario",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "CCC",
  "protocol": "HTTP|HTTPS"
}
