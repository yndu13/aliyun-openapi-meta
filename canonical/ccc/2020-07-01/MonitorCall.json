{
  "cmd_full_name": "ccc monitor-call",
  "cmd_name": "monitor-call",
  "deprecated": false,
  "description_en": "You can invoke MonitorCall to listen to a call associated with a specified call ID under a specified instance. The call must be in a state where both the agent and the customer are normally connected, and the agent initiating the listening must have permission to monitor the target agent",
  "description_zh": "调用MonitorCall监听指定实例下指定通话ID对应的通话。通话必须处于坐席和客户双方正常接通状态，且发起监听的坐席有权限对被监听坐席进行监听",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "MonitorCall",
  "operation": {
    "action": "MonitorCall",
    "api_style": "RPC",
    "api_version": "2020-07-01",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "docRequired": true,
      "example": "ccc-test",
      "help_en": "Instance ID",
      "help_zh": "实例ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "docRequired": true,
      "example": "agent@ccc-test",
      "help_en": "Agent ID",
      "help_zh": "坐席ID",
      "location": "query",
      "name": "user_id",
      "options": [
        "--user-id"
      ],
      "raw_name": "UserId",
      "required": false,
      "type": "string"
    },
    {
      "docRequired": true,
      "example": "device",
      "help_en": "Device ID. This parameter is meaningless and can be filled with any value",
      "help_zh": "设备ID，无意义，可以随便填写",
      "location": "query",
      "name": "device_id",
      "options": [
        "--device-id"
      ],
      "raw_name": "DeviceId",
      "required": false,
      "type": "string"
    },
    {
      "docRequired": true,
      "example": "agent2@ccc-test",
      "help_en": "The ID of the agent being monitored",
      "help_zh": "被监听的座席的ID",
      "location": "query",
      "name": "monitored_user_id",
      "options": [
        "--monitored-user-id"
      ],
      "raw_name": "MonitoredUserId",
      "required": true,
      "type": "string"
    },
    {
      "example": "30",
      "help_en": "The timeout period for the listening operation, in seconds. If the listening operation does not succeed within the specified time, it is canceled. Normally, the listening operation succeeds immediately. The timeout setting is provided to handle abnormal scenarios. This field is optional and defaults to 30 seconds",
      "help_zh": "监听超时时间，经过指定的时间监听仍未成功，则取消监听，正常情况下，监听操作会马上成功，设置超时时间是为了防止异常发生，此字段选填，默认30，单位秒",
      "location": "query",
      "name": "timeout_seconds",
      "options": [
        "--timeout-seconds"
      ],
      "raw_name": "TimeoutSeconds",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "CCC",
  "protocol": "HTTP|HTTPS"
}
