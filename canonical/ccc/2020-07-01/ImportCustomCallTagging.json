{
  "cmd_full_name": "ccc import-custom-call-tagging",
  "cmd_name": "import-custom-call-tagging",
  "deprecated": true,
  "description_en": "You can invoke ImportCustomCallTagging to import multiple inbound number tags into a specified instance",
  "description_zh": "调用ImportCustomCallTagging导入多个呼入号码标记到指定实例中",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "ImportCustomCallTagging",
  "operation": {
    "action": "ImportCustomCallTagging",
    "api_style": "RPC",
    "api_version": "2020-07-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "ccc-test",
      "help_en": "The instance ID",
      "help_zh": "实例ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "temp/ImportCustomCallTagging.xlsx",
      "help_en": "The ObjectKey of the OSS object that contains the inbound number mark file. OSS is configured with the public customer storage bucket for Cloud Call Center. You can upload the inbound number mark file to this public customer storage bucket through the inbound management page of Cloud Call Center. After the upload succeeds, invoking this API reads the file content from OSS and imports it in batch. We do not recommend directly invoking this API. Instead, you can perform this operation through the default public cloud CRM System provided by Cloud Call Center",
      "help_zh": "呼入号码标记文件的OSS对象的ObjectKey。OSS配置的是云呼的客户公共存储空间，您可以通过云呼叫中心的呼入控制页面上传呼入号码标记文件到客户公共存储空间，上传成功后，调用该接口就会从OSS中读取文件内容，然后批量导入。该接口不建议直接调用，建议通过云呼叫中心提供的公有云默认CRM系统操作",
      "location": "query",
      "name": "file_path",
      "options": [
        "--file-path"
      ],
      "raw_name": "FilePath",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "CCC",
  "protocol": "HTTP|HTTPS"
}
