{
  "camel_example": "",
  "cmd_full_name": "cloudcontrol list-supported-pricing-apis",
  "cmd_name": "list-supported-pricing-apis",
  "deprecated": false,
  "description_en": "Lists the OpenAPI triplets that currently support price inquiry",
  "description_zh": "列出当前支持询价的 OpenAPI 三元组",
  "kebab_example": "",
  "method": "GET",
  "multi_version": false,
  "name": "ListSupportedPricingApis",
  "operation": {
    "action": "ListSupportedPricingApis",
    "api_style": "ROA",
    "api_version": "2022-08-30",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "list",
    "url": "/api/v1/price/supported-apis"
  },
  "parameters": [
    {
      "help_en": "分页 token：传入上一次响应里返回的 nextToken；首次调用不传。当前列表 < 100，默认一页就返完",
      "help_zh": "分页 token：传入上一次响应里返回的 nextToken；首次调用不传。当前列表 < 100，默认一页就返完",
      "location": "query",
      "name": "next_token",
      "options": [
        "--next-token"
      ],
      "raw_name": "nextToken",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "本页最大返回条数。范围 1-100，默认 100",
      "help_zh": "本页最大返回条数。范围 1-100，默认 100",
      "location": "query",
      "name": "max_results",
      "options": [
        "--max-results"
      ],
      "raw_name": "maxResults",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "/api/v1/price/supported-apis",
  "product_code": "cloudcontrol",
  "protocol": "HTTPS"
}
