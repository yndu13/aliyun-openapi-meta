{
  "camel_example": "",
  "cmd_full_name": "resourcesharing tag-resources",
  "cmd_name": "tag-resources",
  "deprecated": false,
  "description_en": "Attaches tags to resource shares",
  "description_zh": "为共享单元绑定标签",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "TagResources",
  "operation": {
    "action": "TagResources",
    "api_style": "RPC",
    "api_version": "2020-01-10",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "ResourceShare",
      "help_en": "The type of the resource.\n\nSet this parameter to `ResourceShare`, which indicates a resource share",
      "help_zh": "资源类型。\n\n取值为`ResourceShare`，表示共享单元",
      "location": "query",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The IDs of the resource shares",
      "help_zh": "共享单元ID",
      "location": "query",
      "name": "resource_id",
      "options": [
        "--resource-id"
      ],
      "param_style": "repeatList",
      "raw_name": "ResourceId",
      "required": true,
      "type": "array"
    },
    {
      "example": "cn-hangzhou",
      "help_en": "The ID of the region where the resource share resides",
      "help_zh": "共享单元所属的地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "fields": [
          {
            "example": "k1",
            "help_en": "The tag key.\n\nThe tag key cannot be an empty string. It can be up to 128 characters in length and cannot start with `acs:` or `aliyun`. The tag key cannot contain `http://` or `https://`",
            "help_zh": "标签键。\n\n如需传入该值，则不能输入空字符串。最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`",
            "name": "key",
            "options": [
              "--key"
            ],
            "raw_name": "Key",
            "required": false,
            "type": "string"
          },
          {
            "example": "v1",
            "help_en": "The tag value.\n\nThe tag value can be up to 128 characters in length and cannot start with `acs:`. The tag value cannot contain `http://` or `https://`",
            "help_zh": "标签值。\n最多支持128个字符，不能以`acs:`开头，不能包含`http://`或者`https://`",
            "name": "value",
            "options": [
              "--value"
            ],
            "raw_name": "Value",
            "required": false,
            "type": "string"
          }
        ],
        "type": "object"
      },
      "help_en": "The tags.\n\nYou can attach up to 20 tags",
      "help_zh": "标签列表。\n\n最多可以绑定20个标签",
      "location": "query",
      "name": "tag",
      "options": [
        "--tag"
      ],
      "param_style": "repeatList",
      "raw_name": "Tag",
      "required": true,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "ResourceSharing",
  "protocol": "HTTPS"
}
