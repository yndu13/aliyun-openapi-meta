{
  "camel_example": "",
  "cmd_full_name": "kms put-secret-value",
  "cmd_name": "put-secret-value",
  "deprecated": false,
  "description_en": "Stores a new version of a secret value for a generic secret",
  "description_zh": "为通用凭据存入一个新版本的凭据值",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "PutSecretValue",
  "operation": {
    "action": "PutSecretValue",
    "api_style": "RPC",
    "api_version": "2016-01-20",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "v3",
      "help_en": "The version number of the secret. The value must be unique in the secret",
      "help_zh": "凭据的版本号，在该凭据内唯一",
      "location": "query",
      "name": "version_id",
      "options": [
        "--version-id"
      ],
      "raw_name": "VersionId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "secret001",
      "help_en": "The name or Alibaba Cloud Resource Name (ARN) of the secret.\n\nWhen you access a secret in another Alibaba Cloud account, you must specify the ARN of the secret. The ARN of a secret is in the format of `acs:kms:${region}:${account}:secret/${secret-name}`",
      "help_zh": "凭据名称或凭据资源名称（ARN）。\n访问其他阿里云账号下的凭据时，必须输入凭据ARN。凭据ARN的格式为`acs:kms:${region}:${account}:secret/${secret-name}`",
      "location": "query",
      "name": "secret_name",
      "options": [
        "--secret-name"
      ],
      "raw_name": "SecretName",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "importantdata",
      "help_en": "The secret value. The value is encrypted and stored in the specified new version",
      "help_zh": "凭据值。加密后存入指定的新版本中",
      "location": "query",
      "name": "secret_data",
      "options": [
        "--secret-data"
      ],
      "raw_name": "SecretData",
      "required": true,
      "type": "string"
    },
    {
      "enum": [
        "text",
        "binary"
      ],
      "example": "text",
      "help_en": "The type of the secret value. Valid values:\n\ntext (default)\n\nbinary",
      "help_zh": "凭据值类型。取值：\ntext（默认值）\nbinary",
      "location": "query",
      "name": "secret_data_type",
      "options": [
        "--secret-data-type"
      ],
      "raw_name": "SecretDataType",
      "required": false,
      "type": "string"
    },
    {
      "example": "[\"ACSCurrent\",\"ACSNext\"]",
      "help_en": "The stage labels that are used to mark the new version. If you do not specify this parameter, KMS marks the new version with ACSCurrent",
      "help_zh": "凭据版本在存入时需要被同时标记的版本状态。如果您不指定此参数，KMS默认为新版本标记ACSCurrent",
      "location": "query",
      "name": "version_stages",
      "options": [
        "--version-stages"
      ],
      "raw_name": "VersionStages",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Kms",
  "protocol": "HTTPS"
}
