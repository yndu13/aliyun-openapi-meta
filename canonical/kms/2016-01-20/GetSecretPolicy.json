{
  "camel_example": "",
  "cmd_full_name": "kms get-secret-policy",
  "cmd_name": "get-secret-policy",
  "deprecated": false,
  "description_en": "Queries the access policy of a specified secret in a KMS instance",
  "description_zh": "查询指定凭据的凭据策略",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "GetSecretPolicy",
  "operation": {
    "action": "GetSecretPolicy",
    "api_style": "RPC",
    "api_version": "2016-01-20",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "secret_test",
      "help_en": "The name or Alibaba Cloud Resource Name (ARN) of the credential.\n\nIf you access a credential that belongs to another Alibaba Cloud account, you must specify the ARN of the credential. The ARN of a credential must be in the `acs:kms:${region}:${account}:secret/${secret-name}` format",
      "help_zh": "凭据名称或凭据资源名称（ARN）。\n访问其他阿里云账号下的凭据时，必须输入凭据ARN。凭据ARN的格式为`acs:kms:${region}:${account}:secret/${secret-name}`",
      "location": "query",
      "name": "secret_name",
      "options": [
        "--secret-name"
      ],
      "raw_name": "SecretName",
      "required": true,
      "type": "string"
    },
    {
      "example": "default",
      "help_en": "The name of the credential policy. Only the static field default is supported",
      "help_zh": "凭据策略名称。仅支持固定值default",
      "location": "query",
      "name": "policy_name",
      "options": [
        "--policy-name"
      ],
      "raw_name": "PolicyName",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Kms",
  "protocol": "HTTPS"
}
