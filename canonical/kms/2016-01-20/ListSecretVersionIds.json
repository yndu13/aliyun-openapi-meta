{
  "camel_example": "",
  "cmd_full_name": "kms list-secret-version-ids",
  "cmd_name": "list-secret-version-ids",
  "deprecated": false,
  "description_en": "Queries all version information of a secret",
  "description_zh": "查询凭据的所有版本信息",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ListSecretVersionIds",
  "operation": {
    "action": "ListSecretVersionIds",
    "api_style": "RPC",
    "api_version": "2016-01-20",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "secret001",
      "help_en": "The secret name or secret Amazon Resource Name (ARN).\nWhen accessing a secret under another Alibaba Cloud account, you must specify the secret ARN. The format of the secret ARN is `acs:kms:${region}:${account}:secret/${secret-name}`",
      "help_zh": "凭据名称或凭据资源名称（ARN）。\n访问其他阿里云账号下的凭据时，必须输入凭据ARN。凭据ARN的格式为`acs:kms:${region}:${account}:secret/${secret-name}`",
      "location": "query",
      "name": "secret_name",
      "options": [
        "--secret-name"
      ],
      "raw_name": "SecretName",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to include secret versions that do not have version stages in the response.\n\nValid values:\nfalse (default): does not include\ntrue: includes",
      "help_zh": "返回值中是否包含没有版本状态的凭据版本。\n\n取值范围：\nfalse（默认值）：不包含\ntrue：包含",
      "location": "query",
      "name": "include_deprecated",
      "options": [
        "--include-deprecated"
      ],
      "raw_name": "IncludeDeprecated",
      "required": false,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The page number of the current page in a paged query. Default value: 1",
      "help_zh": "分页查询时，设置当前页面的页码。默认值：1",
      "location": "query",
      "name": "page_number",
      "options": [
        "--page-number"
      ],
      "raw_name": "PageNumber",
      "required": false,
      "type": "int"
    },
    {
      "example": "10",
      "help_en": "The number of entries per page in a paged query. Default value: 20",
      "help_zh": "分页查询时，设置每页大小。默认值：20",
      "location": "query",
      "name": "page_size",
      "options": [
        "--page-size"
      ],
      "raw_name": "PageSize",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "Kms",
  "protocol": "HTTPS"
}
