{
  "camel_example": "",
  "cmd_full_name": "kms describe-secret",
  "cmd_name": "describe-secret",
  "deprecated": false,
  "description_en": "Queries the metadata of a secret",
  "description_zh": "查询凭据的元数据信息",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeSecret",
  "operation": {
    "action": "DescribeSecret",
    "api_style": "RPC",
    "api_version": "2016-01-20",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "secret001",
      "help_en": "The name of the secret",
      "help_zh": "凭据名称或凭据资源名称（ARN）。\n\n访问其他阿里云账号下的凭据时，必须输入凭据ARN。凭据ARN的格式为`acs:kms:${region}:${account}:secret/${secret-name}`",
      "location": "query",
      "name": "secret_name",
      "options": [
        "--secret-name"
      ],
      "raw_name": "SecretName",
      "required": true,
      "type": "string"
    },
    {
      "enum": [
        "false",
        "true"
      ],
      "example": "true",
      "help_en": "Specifies whether to return the resource tags of the secret. Valid values:\n\ntrue: The resource tags are returned.\nfalse: The resource tags are not returned. This is the default value",
      "help_zh": "是否在返回参数中包含凭据的资源标签。取值：  \n\ntrue：包含资源标签。 \n\nfalse（默认值）：不包含资源标签",
      "location": "query",
      "name": "fetch_tags",
      "options": [
        "--fetch-tags"
      ],
      "raw_name": "FetchTags",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Kms",
  "protocol": "HTTPS"
}
