{
  "camel_example": "",
  "cmd_full_name": "kms batch-get-secret-value",
  "cmd_name": "batch-get-secret-value",
  "deprecated": false,
  "description_en": "Retrieves secret values in batches",
  "description_zh": "批量获取凭据",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "BatchGetSecretValue",
  "operation": {
    "action": "BatchGetSecretValue",
    "api_style": "RPC",
    "api_version": "2016-01-20",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "element": {
        "fields": [
          {
            "example": "true",
            "help_en": "Specifies whether to retrieve the extended configuration of the secret. Valid values:\n\ntrue: Retrieve the extended configuration.\nfalse (default): Do not retrieve the extended configuration.\n\nGeneric secrets do not support extended configurations. This parameter is ignored for generic secrets",
            "help_zh": "是否获取凭据的拓展配置。取值：\n\ntrue：获取\nfalse（默认值）：不获取\n\n通用凭据不支持拓展配置，设置该参数将被忽略",
            "name": "fetch_extended_config",
            "options": [
              "--fetch-extended-config"
            ],
            "raw_name": "FetchExtendedConfig",
            "required": false,
            "type": "string"
          },
          {
            "example": "secret001",
            "help_en": "The secret name or secret Alibaba Cloud Resource Name (ARN).\nWhen accessing a secret in another Alibaba Cloud account, you must specify the secret ARN. The format of a secret ARN is `acs:kms:${region}:${account}:secret/${secret-name}`",
            "help_zh": "凭据名称或凭据资源名称（ARN）。\n访问其他阿里云账号下的凭据时，必须输入凭据ARN。凭据ARN的格式为`acs:kms:${region}:${account}:secret/${secret-name}`",
            "name": "secret_name",
            "options": [
              "--secret-name"
            ],
            "raw_name": "SecretName",
            "required": true,
            "type": "string"
          },
          {
            "example": "v1",
            "help_en": "The version number",
            "help_zh": "版本号",
            "name": "version_id",
            "options": [
              "--version-id"
            ],
            "raw_name": "VersionId",
            "required": false,
            "type": "string"
          },
          {
            "example": "ACSCurrent",
            "help_en": "The version stage. Default value: ACSCurrent.\n\nIf you specify this parameter, the secret value of the specified version stage is returned. If you do not specify this parameter, the secret value of the ACSCurrent version stage is returned.\nFor ApsaraDB RDS secrets, PolarDB secrets, Redis/Tair secrets, RAM secrets, and ECS secrets, you can retrieve only the secret values of the ACSPrevious and ACSCurrent versions",
            "help_zh": "版本状态。默认值：ACSCurrent。\n\n输入该参数时返回指定版本状态的凭据值，不输入该参数时返回ACSCurrent版本状态的凭据值。\nRDS凭据、PolarDB凭据、Redis/Tair凭据、RAM凭据和ECS凭据只能获取ACSPrevious和ACSCurrent对应版本的凭据值",
            "name": "version_stage",
            "options": [
              "--version-stage"
            ],
            "raw_name": "VersionStage",
            "required": false,
            "type": "string"
          }
        ],
        "type": "object"
      },
      "help_en": "The list of secret information. You can query up to 20 different secrets at a time",
      "help_zh": "凭据信息列表（最多同时查询 20 组不同的凭据信息）",
      "location": "query",
      "name": "secrets_list",
      "options": [
        "--secrets-list"
      ],
      "param_style": "json",
      "raw_name": "SecretsList",
      "required": false,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "Kms",
  "protocol": "HTTPS"
}
