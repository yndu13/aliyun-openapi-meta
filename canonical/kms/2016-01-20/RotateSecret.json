{
  "camel_example": "",
  "cmd_full_name": "kms rotate-secret",
  "cmd_name": "rotate-secret",
  "deprecated": false,
  "description_en": "Immediately rotates a secret",
  "description_zh": "立即轮转凭据",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "RotateSecret",
  "operation": {
    "action": "RotateSecret",
    "api_style": "RPC",
    "api_version": "2016-01-20",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "docRequired": true,
      "example": "RdsSecret/Mysql5.4/MyCred",
      "help_en": "The name of the secret",
      "help_zh": "凭据名称或凭据资源名称（ARN）。\n访问其他阿里云账号下的凭据时，必须输入凭据ARN。凭据ARN的格式为`acs:kms:${region}:${account}:secret/${secret-name}`",
      "location": "query",
      "name": "secret_name",
      "options": [
        "--secret-name"
      ],
      "raw_name": "SecretName",
      "required": true,
      "type": "string"
    },
    {
      "docRequired": true,
      "example": "000000123",
      "help_en": "The version number of the secret after the secret is rotated.\n\nThe version number is used to ensure the idempotence of the request. Secrets Manager uses this version number to prevent your application from creating the same version of the secret when the application retries a request. If a version number already exists, Secrets Manager ignores the request for rotation and returns a success message",
      "help_zh": "轮转后的新凭据版本的版本号。\n\n版本号用于保证请求的幂等性。KMS使用版本号来防止您的应用在请求失败后进行重试时，意外创建重复的版本。如果相同的版本号已经存在，轮转的请求会被忽略，服务端会返回成功",
      "location": "query",
      "name": "version_id",
      "options": [
        "--version-id"
      ],
      "raw_name": "VersionId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Kms",
  "protocol": "HTTPS"
}
