{
  "camel_example": "",
  "cmd_full_name": "kms update-rotation-policy",
  "cmd_name": "update-rotation-policy",
  "deprecated": false,
  "description_en": "Updates the automatic rotation policy of a CMK",
  "description_zh": "更新密钥的周期性自动轮转策略",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UpdateRotationPolicy",
  "operation": {
    "action": "UpdateRotationPolicy",
    "api_style": "RPC",
    "api_version": "2016-01-20",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "key-hzz62f1cb66fa42qo****",
      "help_en": "The ID of the customer master key (CMK). The ID must be globally unique",
      "help_zh": "密钥ID，即密钥的全局唯一标识符",
      "location": "query",
      "name": "key_id",
      "options": [
        "--key-id"
      ],
      "raw_name": "KeyId",
      "required": true,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to enable automatic key rotation. Valid values:\n\ntrue: enables automatic key rotation.\n\nfalse: disables automatic key rotation",
      "help_zh": "是否开启周期性自动轮转。取值：\n\ntrue：开启。\n\nfalse：关闭。\n                           ",
      "location": "query",
      "name": "enable_automatic_rotation",
      "options": [
        "--enable-automatic-rotation"
      ],
      "raw_name": "EnableAutomaticRotation",
      "required": true,
      "type": "bool"
    },
    {
      "example": "30d",
      "help_en": "The period of automatic key rotation. Specify the value in the integer[unit] format. The following units are supported: d (day), h (hour), m (minute), and s (second). For example, you can use either 7d or 604800s to specify a seven-day period. The period can range from 7 days to 730 days.\n\nIf you set the EnableAutomaticRotation parameter to true, you must also specify this parameter. If you set the EnableAutomaticRotation parameter to false, you can leave this parameter unspecified",
      "help_zh": "自动轮转的时间周期。格式为integer[unit]，其中integer表示时间长度，unit表示时间单位。合法的unit单位为：d（天）、h（小时）、m（分钟）、s（秒）。7d或者604800s均表示7天的周期。取值：\n\n默认密钥：仅支持设置为365天。\n\n软件密钥：7~365天。\n\n当EnableAutomaticRotation参数为true时，必须设置此参数，否则无需设置",
      "location": "query",
      "name": "rotation_interval",
      "options": [
        "--rotation-interval"
      ],
      "raw_name": "RotationInterval",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Kms",
  "protocol": "HTTPS",
  "v1_parameters": [
    {
      "help_en": "Specifies whether to enable automatic key rotation. Valid values:\n\ntrue: enables automatic key rotation.\n\nfalse: disables automatic key rotation",
      "help_zh": "是否开启周期性自动轮转。取值：\n\ntrue：开启。\n\nfalse：关闭",
      "name": "EnableAutomaticRotation",
      "position": "query",
      "required": true,
      "type": "bool"
    },
    {
      "help_en": "The ID of the customer master key (CMK). The ID must be globally unique",
      "help_zh": "密钥ID，即密钥的全局唯一标识符",
      "name": "KeyId",
      "position": "query",
      "required": true,
      "type": "string"
    },
    {
      "help_en": "The period of automatic key rotation. Specify the value in the integer[unit] format. The following units are supported: d (day), h (hour), m (minute), and s (second). For example, you can use either 7d or 604800s to specify a seven-day period. The period can range from 7 days to 730 days.\n\nIf you set the EnableAutomaticRotation parameter to true, you must also specify this parameter. If you set the EnableAutomaticRotation parameter to false, you can leave this parameter unspecified",
      "help_zh": "自动轮转的时间周期。格式为integer[unit]，其中integer表示时间长度，unit表示时间单位。合法的unit单位为：d（天）、h（小时）、m（分钟）、s（秒）。7d或者604800s均表示7天的周期。取值：\n\n默认密钥：仅支持设置为365天。\n\n软件密钥：7~365天。\n\n当EnableAutomaticRotation参数为true时，必须设置此参数，否则无需设置",
      "name": "RotationInterval",
      "position": "query",
      "required": false,
      "type": "string"
    }
  ]
}
