{
  "camel_example": "",
  "cmd_full_name": "kms get-key-policy",
  "cmd_name": "get-key-policy",
  "deprecated": false,
  "description_en": "Queries the key policy of a CMK in a KMS instance",
  "description_zh": "查询指定密钥的密钥策略",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "GetKeyPolicy",
  "operation": {
    "action": "GetKeyPolicy",
    "api_style": "RPC",
    "api_version": "2016-01-20",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "key-hzz630494463ejqjx****",
      "help_en": "The ID or Alibaba Cloud Resource Name (ARN) of the key.\n\nWhen you access a key in another Alibaba Cloud account, you must enter the ARN of the key. The ARN of a key is in the `acs:kms:${region}:${account}:key/${keyid}` format",
      "help_zh": "密钥ID或密钥资源名称（ARN）。\n访问其他阿里云账号下的密钥时，必须输入密钥ARN。密钥ARN的格式为`acs:kms:${region}:${account}:key/${keyid}`",
      "location": "query",
      "name": "key_id",
      "options": [
        "--key-id"
      ],
      "raw_name": "KeyId",
      "required": true,
      "type": "string"
    },
    {
      "example": "default",
      "help_en": "The name of the key policy. Only the static value default is supported",
      "help_zh": "密钥策略名称。仅支持固定值default",
      "location": "query",
      "name": "policy_name",
      "options": [
        "--policy-name"
      ],
      "raw_name": "PolicyName",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Kms",
  "protocol": "HTTPS"
}
