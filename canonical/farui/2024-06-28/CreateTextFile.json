{
  "camel_example": "",
  "cmd_full_name": "farui create-text-file",
  "cmd_name": "create-text-file",
  "deprecated": false,
  "description_en": "This API uploads documents to the contract review module. After a successful upload, the system automatically parses the file. The API returns a file ID that you can use to generate review rules and results. File parsing uses a queuing mechanism. If the queue is long, parsing may be delayed",
  "description_zh": "该接口用于将文档上传到合同审查模块中，添加成功之后，系统会自动启动文件的解析，并返回对应的文件ID用于后续生成审查规则和审查结果。文件解析有排队机制，如果队列较长，文件可能需要等待一段时间才能解析完成",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "CreateTextFile",
  "operation": {
    "action": "CreateTextFile",
    "api_style": "ROA",
    "api_version": "2024-06-28",
    "content_type": "application/x-www-form-urlencoded",
    "is_file_transfer_api": true,
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "create",
    "url": "/{WorkspaceId}/data/textFile"
  },
  "parameters": [
    {
      "example": "llm-9w5y60lseff0jiqm",
      "help_en": "Workspace ID for Alibaba Cloud Model Studio",
      "help_zh": "阿里云百炼平台工作空间ID",
      "location": "path",
      "name": "workspace_id",
      "options": [
        "--workspace-id"
      ],
      "raw_name": "WorkspaceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "e9a93201-7e96-4dc1-9678-2832fc132d08",
      "help_en": "ClientToken ensures request idempotence. Generate a unique parameter value from your client for each request. ClientToken only supports ASCII characters. If you do not specify it, the system automatically uses the RequestId of the API request as the ClientToken identity. The RequestId for each API request may differ",
      "help_zh": "客户端Token，用于保证请求的幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId可能不一样",
      "location": "formData",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "1714476549",
      "help_en": "Creation time",
      "help_zh": "创建时间",
      "location": "formData",
      "name": "create_time",
      "options": [
        "--create-time"
      ],
      "raw_name": "CreateTime",
      "required": false,
      "type": "string"
    },
    {
      "example": "测试文件.docx",
      "help_en": "Uploaded file name (with extension)",
      "help_zh": "上传文件名(带后缀)",
      "location": "formData",
      "name": "text_file_name",
      "options": [
        "--text-file-name"
      ],
      "raw_name": "TextFileName",
      "required": false,
      "type": "string"
    },
    {
      "example": "https://xx.测试文件.docx",
      "help_en": "Download URL of the uploaded file. To upload files directly, use the CreateFileAdvanceRequest from the SDK",
      "help_zh": "上传文件的下载url。需要通过文件直接上传的请使用SDK的CreateFileAdvanceRequest请求",
      "is_file_transfer_url": true,
      "location": "formData",
      "name": "text_file_url",
      "options": [
        "--text-file-url"
      ],
      "raw_name": "TextFileUrl",
      "required": false,
      "type": "string"
    },
    {
      "example": "123",
      "help_en": "Used in contract comparison scenarios. The first file upload returns a ContractId. If comparison is needed, pass this parameter during the second upload and upload the standard document",
      "help_zh": "合同比对场景下使用，第一次上传文件会返回ContractId，如果需要比对，则第二次上传传递该参数并上传标准文档",
      "location": "formData",
      "name": "contract_id",
      "options": [
        "--contract-id"
      ],
      "raw_name": "ContractId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/[WorkspaceId]/data/textFile",
  "product_code": "FaRui",
  "protocol": "HTTPS",
  "v1_parameters": [
    {
      "help_en": "ClientToken ensures request idempotence. Generate a unique parameter value from your client for each request. ClientToken only supports ASCII characters. If you do not specify it, the system automatically uses the RequestId of the API request as the ClientToken identity. The RequestId for each API request may differ",
      "help_zh": "客户端Token，用于保证请求的幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId可能不一样",
      "name": "ClientToken",
      "position": "body",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "Used in contract comparison scenarios. The first file upload returns a ContractId. If comparison is needed, pass this parameter during the second upload and upload the standard document",
      "help_zh": "合同比对场景下使用，第一次上传文件会返回ContractId，如果需要比对，则第二次上传传递该参数并上传标准文档",
      "name": "ContractId",
      "position": "body",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "Creation time",
      "help_zh": "创建时间",
      "name": "CreateTime",
      "position": "body",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "Uploaded file name (with extension)",
      "help_zh": "上传文件名(带后缀)",
      "name": "TextFileName",
      "position": "body",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "Download URL of the uploaded file. To upload files directly, use the CreateFileAdvanceRequest from the SDK",
      "help_zh": "上传文件的下载url。需要通过文件直接上传的请使用SDK的CreateFileAdvanceRequest请求",
      "name": "TextFileUrl",
      "position": "body",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "Workspace ID for Alibaba Cloud Model Studio",
      "help_zh": "阿里云百炼平台工作空间ID",
      "name": "WorkspaceId",
      "position": "path",
      "required": false,
      "type": "string"
    }
  ]
}
