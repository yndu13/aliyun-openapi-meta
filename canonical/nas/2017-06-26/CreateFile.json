{
  "camel_example": "",
  "cmd_full_name": "nas create-file",
  "cmd_name": "create-file",
  "deprecated": false,
  "description_en": "Creates a directory or file",
  "description_zh": "创建目录或者文件",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CreateFile",
  "operation": {
    "action": "CreateFile",
    "api_style": "RPC",
    "api_version": "2017-06-26",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "example": "1ca404****",
      "help_en": "The ID of the file system",
      "help_zh": "文件系统ID",
      "location": "query",
      "name": "file_system_id",
      "options": [
        "--file-system-id"
      ],
      "raw_name": "FileSystemId",
      "required": true,
      "type": "string"
    },
    {
      "example": "/test/",
      "help_en": "The absolute path of the directory or file. The path must start and end with a forward slash (/) and must be 2 to 1024 characters in length",
      "help_zh": "指定目录或文件的绝对路径，必须以正斜线（/）开头和结尾，长度为2~1024",
      "location": "query",
      "name": "path",
      "options": [
        "--path"
      ],
      "raw_name": "Path",
      "required": true,
      "type": "string"
    },
    {
      "example": "File",
      "help_en": "The type of the object. Valid values:\n\nFile\nDirectory",
      "help_zh": "对象类型。取值：\n\nFile：文件。\nDirectory：目录",
      "location": "query",
      "name": "type",
      "options": [
        "--type"
      ],
      "raw_name": "Type",
      "required": true,
      "type": "string"
    },
    {
      "example": "378cc7630f26****",
      "help_en": "The ID of the portable account. The ID must be a 16-digit string. The string can contain digits and lowercase letters",
      "help_zh": "便携账号ID。\n限制：长度16位字符串，支持数字和英文小写字母",
      "location": "query",
      "name": "owner",
      "options": [
        "--owner"
      ],
      "raw_name": "Owner",
      "required": false,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to share the directory. Valid values:\n\nfalse (default): does not share the directory.\ntrue: shares the directory.\n\nThis parameter takes effect only if the Type parameter is set to Directory and the Owner parameter is not empty.\nThe permissions on a directory can be inherited by the owner. The owner has read and write permissions on the subdirectories and subfiles created in the directory, even if they are created by others",
      "help_zh": "共享目录文件。\n取值：\nfalse（默认值）：不共享目录权限。\ntrue：共享目录权限。\n当Type=Directory并且Owner不为空时，该参数生效。\n目录带有可继承的Owner权限。该目录下创建的子目录和子文件，即使是其他人创建的，这个Owner都具有读写权限",
      "location": "query",
      "name": "owner_access_inheritable",
      "options": [
        "--owner-access-inheritable"
      ],
      "raw_name": "OwnerAccessInheritable",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "NAS",
  "protocol": "HTTP|HTTPS"
}
