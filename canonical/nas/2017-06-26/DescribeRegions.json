{
  "camel_example": "",
  "cmd_full_name": "nas describe-regions",
  "cmd_name": "describe-regions",
  "deprecated": false,
  "description_en": "Queries the available Alibaba Cloud regions",
  "description_zh": "查询可以使用的阿里云地域",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeRegions",
  "operation": {
    "action": "DescribeRegions",
    "api_style": "RPC",
    "api_version": "2017-06-26",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "10",
      "help_en": "The number of regions on each page during a paged query.\n\nValid values: 1 to 100.\n\nDefault value: 10",
      "help_zh": "分页查询时，每页包含的地域数。\n\n取值范围：1~100\n\n默认值：10",
      "location": "query",
      "maximum": "100",
      "minimum": "1",
      "name": "page_size",
      "options": [
        "--page-size"
      ],
      "raw_name": "PageSize",
      "required": false,
      "type": "int"
    },
    {
      "example": "1",
      "help_en": "The page number of the list.\n\nStart value (default value): 1",
      "help_zh": "列表的分页页码。\n\n起始值（默认值）：1",
      "location": "query",
      "minimum": "1",
      "name": "page_number",
      "options": [
        "--page-number"
      ],
      "raw_name": "PageNumber",
      "required": false,
      "type": "int"
    },
    {
      "enum": [
        "standard",
        "extreme",
        "cpfs",
        "all"
      ],
      "example": "standard",
      "help_en": "The file system type.\n\nValid values:\nall: all types.\nstandard (default): General-purpose NAS.\nextreme: Extreme NAS.\ncpfs: CPFS",
      "help_zh": "文件系统类型。\n\n取值范围：\n取值：\nall：所有类型。\nstandard（默认值）：通用型NAS。\nextreme：极速型NAS。\ncpfs：CPFS",
      "location": "query",
      "name": "file_system_type",
      "options": [
        "--file-system-type"
      ],
      "raw_name": "FileSystemType",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "NAS",
  "protocol": "HTTP|HTTPS"
}
