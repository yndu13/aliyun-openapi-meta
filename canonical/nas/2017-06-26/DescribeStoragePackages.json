{
  "camel_example": "",
  "cmd_full_name": "nas describe-storage-packages",
  "cmd_name": "describe-storage-packages",
  "deprecated": false,
  "description_en": "You can call the DescribeStoragePackages operation to query the list of storage plans",
  "description_zh": "查询存储包列表信息",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeStoragePackages",
  "operation": {
    "action": "DescribeStoragePackages",
    "api_style": "RPC",
    "api_version": "2017-06-26",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "cn-hangzhou",
      "help_en": "The region ID",
      "help_zh": "地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "10",
      "help_en": "The number of storage plans to return on each page.\n\nValid values: 1 to 100.\n\nDefault value: 10",
      "help_zh": "分页查询时，每个分页包含的存储包个数。\n\n取值范围：1~100\n\n默认值：10",
      "location": "query",
      "maximum": "100",
      "minimum": "1",
      "name": "page_size",
      "options": [
        "--page-size"
      ],
      "raw_name": "PageSize",
      "required": false,
      "type": "int"
    },
    {
      "example": "true",
      "help_en": "Specifies whether the time to return is in UTC.\n\nValid values:\n\ntrue (default): returns UTC time.\nfalse: returns UNIX timestamp",
      "help_zh": "返回的时间是否按照UTC标准格式表示。\n\n取值：\n\ntrue（默认值）：返回的时间为UTC标准格式。\nfalse：返回Unix时间戳",
      "location": "query",
      "minimum": "1",
      "name": "use_utc_date_time",
      "options": [
        "--use-utc-date-time"
      ],
      "raw_name": "UseUTCDateTime",
      "required": false,
      "type": "bool"
    },
    {
      "example": "1",
      "help_en": "The number of the page to return.\n\nPages start from page 1. Default value: 1",
      "help_zh": "列表的分页页码。\n\n起始值（默认值）：1",
      "location": "query",
      "minimum": "1",
      "name": "page_number",
      "options": [
        "--page-number"
      ],
      "raw_name": "PageNumber",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "NAS",
  "protocol": "HTTP|HTTPS"
}
