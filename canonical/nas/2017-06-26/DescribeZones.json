{
  "camel_example": "",
  "cmd_full_name": "nas describe-zones",
  "cmd_name": "describe-zones",
  "deprecated": false,
  "description_en": "Queries all zones in a region and the file system types that are supported in each zone",
  "description_zh": "查询一个地域下的所有可用区及可用区所支持的文件系统类型",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeZones",
  "operation": {
    "action": "DescribeZones",
    "api_style": "RPC",
    "api_version": "2017-06-26",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The ID of the region where you want to query zones.\n\nYou can call the DescribeRegions operation to query the latest region list",
      "help_zh": "可用区所在的地域ID。\n\n您可以调用DescribeRegions查看最新地域列表",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "standard",
      "help_en": "The type of the file system.\n\nValid value:\n\nstandard: General-purpose Apsara File Storage NAS (NAS) file system\nextreme: Extreme NAS file system.\ncpfs: CPFS file system",
      "help_zh": "文件系统类型。\n\n取值：\n\nstandard（默认值）：通用型NAS。\nextreme：极速型NAS。\ncpfs：文件存储CPFS",
      "location": "query",
      "name": "file_system_type",
      "options": [
        "--file-system-type"
      ],
      "raw_name": "FileSystemType",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "NAS",
  "protocol": "HTTP|HTTPS"
}
