{
  "camel_example": "",
  "cmd_full_name": "nas tag-resources",
  "cmd_name": "tag-resources",
  "deprecated": false,
  "description_en": "Creates tags and binds the tags to file systems",
  "description_zh": "为指定资源创建并绑定标签。支持文件系统和接入点",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "TagResources",
  "operation": {
    "action": "TagResources",
    "api_style": "RPC",
    "api_version": "2017-06-26",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "example": "filesystem",
      "help_en": "The resource type. Set the value to filesystem",
      "help_zh": "资源类型。\n取值：\nfilesystem，表示文件系统。\naccesspoint，表示为接入点",
      "location": "query",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "example": "03e08484f0",
      "help_en": "The resource IDs. Valid values of N: 1 to 50",
      "help_zh": "资源ID。取值范围：1~50",
      "location": "query",
      "name": "resource_id",
      "options": [
        "--resource-id"
      ],
      "param_style": "repeatList",
      "raw_name": "ResourceId",
      "required": true,
      "type": "array"
    },
    {
      "element": {
        "fields": [
          {
            "example": "nastest",
            "help_en": "The key of tag N to add to the resource.\n\nLimits:\n\nThe tag key cannot be left empty.\nValid values of N: 1 to 20.\nThe tag key must be 1 to 128 characters in length.\nThe tag key cannot start with `aliyun` or `acs:`.\nThe tag key cannot contain `http://` or `https://`",
            "help_zh": "标签键。\n\n限制：\n\n不允许为空和空字符串。 \n最多支持128个字符。\n不能以`aliyun`或`acs:`开头。\n不能包含`http://`和`https://`",
            "name": "key",
            "options": [
              "--key"
            ],
            "raw_name": "Key",
            "required": true,
            "type": "string"
          },
          {
            "example": "filetest",
            "help_en": "The value of tag N to add to the resource.\n\nLimits:\n\nValid values of N: 1 to 20.\nThe tag value must be 1 to 128 characters in length.\nThe tag value cannot start with `aliyun` or `acs:`.\nThe tag value cannot contain `http://` or `https://`",
            "help_zh": "标签值。\n\n限制：\n\n最多支持128个字符。\n不能包含`http://`和`https://`",
            "name": "value",
            "options": [
              "--value"
            ],
            "raw_name": "Value",
            "required": true,
            "type": "string"
          }
        ],
        "type": "object"
      },
      "help_en": "The details about the tags",
      "help_zh": "标签数组。数组长度：1~20。如果数组中有多个标签对象，标签键Key不允许重复",
      "location": "query",
      "name": "tag",
      "options": [
        "--tag"
      ],
      "param_style": "repeatList",
      "raw_name": "Tag",
      "required": true,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "NAS",
  "protocol": "HTTP|HTTPS"
}
