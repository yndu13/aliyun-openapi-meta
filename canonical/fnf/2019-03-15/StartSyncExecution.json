{
  "camel_example": "",
  "cmd_full_name": "fnf start-sync-execution",
  "cmd_name": "start-sync-execution",
  "deprecated": false,
  "description_en": "Synchronously starts a flow execution",
  "description_zh": "同步调用开始一个流程的执行",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "StartSyncExecution",
  "operation": {
    "action": "StartSyncExecution",
    "api_style": "RPC",
    "api_version": "2019-03-15",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "docRequired": true,
      "example": "my_flow_name",
      "help_en": "The name of the flow to execute",
      "help_zh": "开始执行的流程名称",
      "location": "formData",
      "name": "flow_name",
      "options": [
        "--flow-name"
      ],
      "raw_name": "FlowName",
      "required": true,
      "type": "string"
    },
    {
      "example": "my_exec_name",
      "help_en": "The name of the execution. The name must meet the following requirements:\n\nIt can contain letters (a–z and A–Z), digits (0–9), underscores (_), and hyphens (-).\n\nIt must start with a letter (a–z or A–Z) or an underscore (_).\n\nIt is case-sensitive.\n\nIt must be 1 to 128 characters in length.\n\nUnlike the StartExecution operation, the execution name for a synchronous call does not need to be unique within a flow. You can specify an execution name to identify the execution. The system appends a UUID to the name in the \\`{ExecutionName}:{UUID}\\` format. If you do not specify an execution name, the system automatically generates one",
      "help_zh": "执行名称。取值说明如下：\n\n支持英文字符（a~z）或（A~Z）、数字（0~9）、下划线（_）和短划线（-）。\n首字母必须为英文字母（a~z）、（A~Z）或下划线（_）。\n区分大小写。\n长度为1~128个字符。\n\n不同于StartExecution接口，考虑到同步调用的特殊性，在同步执行模式下，不再要求执行名称在同一流程内唯一，调用侧可以选择提供执行名称，对本次执行进行标识，系统会在当前执行名称后添加UUID，具体形式如 {ExecutionName}:{UUID}，如果用户没有指定相关的执行名称信息，那么系统会自动生成标识本次执行的ExecutionName",
      "location": "formData",
      "name": "execution_name",
      "options": [
        "--execution-name"
      ],
      "raw_name": "ExecutionName",
      "required": false,
      "type": "string"
    },
    {
      "example": "{\"key\":\"value\"}",
      "help_en": "The input of the execution. The input must be in JSON format",
      "help_zh": "执行的输入，为JSON对象格式",
      "location": "formData",
      "name": "input",
      "options": [
        "--input"
      ],
      "raw_name": "Input",
      "required": false,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The version or alias of the flow",
      "help_zh": "指定流程版本或别名",
      "location": "formData",
      "name": "qualifier",
      "options": [
        "--qualifier"
      ],
      "raw_name": "Qualifier",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "fnf",
  "protocol": "HTTPS"
}
