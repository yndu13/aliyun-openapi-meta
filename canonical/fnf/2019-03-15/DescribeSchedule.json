{
  "camel_example": "",
  "cmd_full_name": "fnf describe-schedule",
  "cmd_name": "describe-schedule",
  "deprecated": false,
  "description_en": "Queries the detailed information about a time-based schedule",
  "description_zh": "获取一个定时调度信息",
  "kebab_example": "",
  "method": "GET",
  "multi_version": false,
  "name": "DescribeSchedule",
  "operation": {
    "action": "DescribeSchedule",
    "api_style": "RPC",
    "api_version": "2019-03-15",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "my_test_flow",
      "help_en": "The name of the flow that is associated with the time-based schedule. The name must be unique within the region and cannot be modified after the time-based schedule is created. The name must meet the following conventions:\n\nThe name can contain letters, digits, underscores (_), and hyphens (-).\nThe name must start with a letter or an underscore (_).\nThe name is case-sensitive.\nThe name must be 1 to 128 characters in length",
      "help_zh": "定时调度绑定的流程名称。该名称在同一地域内唯一，创建后不可修改。取值说明如下：\n\n支持英文字符（a~z）或（A~Z）、数字（0~9）、下划线（_）和短划线（-）。\n首字母必须为英文字母（a~z）、（A~Z）或下划线（_）。\n区分大小写。\n长度为1~128个字符",
      "location": "query",
      "name": "flow_name",
      "options": [
        "--flow-name"
      ],
      "raw_name": "FlowName",
      "required": true,
      "type": "string"
    },
    {
      "example": "my_schedule_name",
      "help_en": "The name of the time-based schedule. The name must meet the following conventions:\n\nThe name can contain letters, digits, underscores (_), and hyphens (-).\nThe name must start with a letter or an underscore (_).\nThe name is case-sensitive.\nThe name must be 1 to 128 characters in length",
      "help_zh": "定时调度的名称。取值说明如下：\n\n支持英文字符（a~z）或（A~Z）、数字（0~9）、下划线（_）和短划线（-）。\n首字母必须为英文字母（a~z）、（A~Z）或下划线（_）。\n区分大小写。\n长度为1~128个字符",
      "location": "query",
      "name": "schedule_name",
      "options": [
        "--schedule-name"
      ],
      "raw_name": "ScheduleName",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "fnf",
  "protocol": "HTTP|HTTPS"
}
