{
  "camel_example": "",
  "cmd_full_name": "bssopenapi set-all-expiration-day",
  "cmd_name": "set-all-expiration-day",
  "deprecated": false,
  "description_en": "Sets an expiration date for all Elastic Compute Service (ECS) instances",
  "description_zh": "为ECS实例的续费设置统一到期日",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "SetAllExpirationDay",
  "operation": {
    "action": "SetAllExpirationDay",
    "api_style": "RPC",
    "api_version": "2017-12-14",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "10",
      "help_en": "The expiration date. You can set an expiration date only for ECS instances that have not expired. The expiration date that you specify do not take effect on expired ECS instances. After the expiration date is set, the expiration date is used when you renew ECS instances.\n\nYou can set the expiration date to a day from the 1st to the 28th of each month",
      "help_zh": "当前统一到期日设置只支持未过期云服务器ECS实例，已过期实例无法对齐统一到期日。设置完成后，可在续费时对齐设置的统一到期时间\n\n日期范围：每月1号至28号",
      "location": "query",
      "name": "unify_expire_day",
      "options": [
        "--unify-expire-day"
      ],
      "raw_name": "UnifyExpireDay",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "BssOpenApi",
  "protocol": "HTTP|HTTPS"
}
