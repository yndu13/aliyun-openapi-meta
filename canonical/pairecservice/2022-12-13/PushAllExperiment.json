{
  "camel_example": "",
  "cmd_full_name": "pairecservice push-all-experiment",
  "cmd_name": "push-all-experiment",
  "deprecated": false,
  "description_en": "If an experiment is stable and performs well, you can push all traffic to it. This action retires the original experiment group and creates a new one that contains only this experiment. The new group receives 100% of the traffic",
  "description_zh": "对于效果较优且稳定的实验，可对该实验进行推全。对某个实验推全后，该实验所属的实验组将结束生命周期，不再支持继续实验。平台会生成一个新的实验组，被推全的实验将在该实验组下占有全额的流量",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "PushAllExperiment",
  "operation": {
    "action": "PushAllExperiment",
    "api_style": "ROA",
    "api_version": "2022-12-13",
    "body_mapping": {
      "instance_id": "InstanceId"
    },
    "content_type": "application/json",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "req_body_type": "json",
    "system_operation_type": "none",
    "url": "/api/v1/experiments/{ExperimentId}/action/pushall"
  },
  "parameters": [
    {
      "example": "3",
      "help_en": "The experiment ID. Call the ListExperiments API to obtain this ID",
      "help_zh": "实验ID，可通过接口ListExperiments获取",
      "location": "path",
      "name": "experiment_id",
      "options": [
        "--experiment-id"
      ],
      "raw_name": "ExperimentId",
      "required": true,
      "type": "string"
    },
    {
      "example": "pairec-cn-abcdefg1234",
      "help_en": "The instance ID. Call the ListInstances API to obtain this ID",
      "help_zh": "实例ID，可通过接口ListInstances获取",
      "location": "body",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "param_style": "json",
      "raw_name": "InstanceId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/api/v1/experiments/[ExperimentId]/action/pushall",
  "product_code": "PaiRecService",
  "protocol": "HTTP|HTTPS",
  "v1_body_parameters": [
    {
      "help_en": "The request body",
      "help_zh": "请求body",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    }
  ]
}
