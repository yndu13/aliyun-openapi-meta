{
  "camel_example": "",
  "cmd_full_name": "btripopen car-order-query",
  "cmd_name": "car-order-query",
  "deprecated": false,
  "description_en": "Queries the details of a car service order",
  "description_zh": "用车订单详情",
  "kebab_example": "",
  "method": "GET",
  "multi_version": true,
  "name": "CarOrderQuery",
  "operation": {
    "action": "CarOrderQuery",
    "api_style": "ROA",
    "api_version": "2022-05-20",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": "/car/v1/order"
  },
  "parameters": [
    {
      "example": "1012000000000000",
      "help_en": "The ID of the car service main order.\nSpecify either order_id or sub_order_id. If both are specified, order_id takes precedence",
      "help_zh": "用车主订单id\norder_id与sub_order_id参数二选一，同时传递以order_id为准",
      "location": "query",
      "name": "order_id",
      "options": [
        "--order-id"
      ],
      "raw_name": "order_id",
      "required": false,
      "type": "int"
    },
    {
      "example": "1000000",
      "help_en": "The ID of the car service sub-order.\nSpecify either order_id or sub_order_id. If both are specified, order_id takes precedence",
      "help_zh": "用车子订单id\norder_id与sub_order_id参数二选一，同时传递以order_id为准",
      "location": "query",
      "name": "sub_order_id",
      "options": [
        "--sub-order-id"
      ],
      "raw_name": "sub_order_id",
      "required": false,
      "type": "int"
    },
    {
      "example": "feth00jqwls",
      "help_en": "A header parameter obtained from the \"Enterprise Access Credential\" operation (added in the header). In HTTP mode, you can use so_corp_token=value or dtb_corp_token=value in the URL as an alternative",
      "help_zh": "header参数,由“企业访问凭证”接口获取（添加在header里）,http模式可以在链接中使用so_corp_token=value或者dtb_corp_token=value来替代",
      "location": "header",
      "name": "x_acs_btrip_so_corp_token",
      "options": [
        "--x-acs-btrip-so-corp-token"
      ],
      "raw_name": "x-acs-btrip-so-corp-token",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/car/v1/order",
  "product_code": "btripOpen",
  "protocol": "HTTPS"
}
