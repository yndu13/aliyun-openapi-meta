{
  "camel_example": "",
  "cmd_full_name": "btripopen create-sub-corp",
  "cmd_name": "create-sub-corp",
  "deprecated": false,
  "description_en": "Creates a sub-enterprise for a group-type enterprise customer",
  "description_zh": "调用本接口，可以实现为集团型的企业客户创建子企业",
  "kebab_example": "",
  "method": "POST",
  "multi_version": true,
  "name": "CreateSubCorp",
  "operation": {
    "action": "CreateSubCorp",
    "api_style": "ROA",
    "api_version": "2022-05-20",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "url": "/sub_corps/v1/corps"
  },
  "parameters": [
    {
      "example": "123",
      "help_en": "The employee ID that exists in an enterprise that has signed the Alibaba Business Travel Parent Account Agreement",
      "help_zh": "存在于已签署《阿里商旅主账号协议》的企业的员工 ID",
      "location": "formData",
      "name": "user_id",
      "options": [
        "--user-id"
      ],
      "raw_name": "user_id",
      "required": true,
      "type": "string"
    },
    {
      "example": "corp123",
      "help_en": "The unique ID of the sub-enterprise created through this operation. Reusing this ID causes the call to fail",
      "help_zh": "通过本接口建立的子企业唯一 ID，重复使用时将导致调用失败",
      "location": "formData",
      "name": "outer_corp_id",
      "options": [
        "--outer-corp-id"
      ],
      "raw_name": "outer_corp_id",
      "required": true,
      "type": "string"
    },
    {
      "example": "测试企业",
      "help_en": "The unique name of the sub-enterprise created through this operation. Reusing this name causes the call to fail",
      "help_zh": "通过本接口创建的子企业唯一名称，重复使用时将会导致调用失败",
      "location": "formData",
      "name": "outer_corp_name",
      "options": [
        "--outer-corp-name"
      ],
      "raw_name": "outer_corp_name",
      "required": true,
      "type": "string"
    },
    {
      "example": "feth00jqwls",
      "help_en": "The enterprise access token.\nWhen calling this operation over HTTP, include this as a required parameter in the request URL. For information about how to obtain the token, refer to Enterprise Access Token (https://openapi.alibtrip.com/doc/toDocDetail?docId=3769985).\nWhen appending to the URL, use `crop_token=value` instead",
      "help_zh": "企业访问凭证。\n通过 HTTP 方式调用本接口时，需要作为必填参数拼接在请求地址中。获取方式请参见：企业访问凭证 (https://openapi.alibtrip.com/doc/toDocDetail?docId=3769985)。\n在拼接时，推荐使用 `crop_token=value` 替代",
      "location": "header",
      "name": "x_acs_btrip_corp_token",
      "options": [
        "--x-acs-btrip-corp-token"
      ],
      "raw_name": "x-acs-btrip-corp-token",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/sub_corps/v1/corps",
  "product_code": "btripOpen",
  "protocol": "HTTPS"
}
