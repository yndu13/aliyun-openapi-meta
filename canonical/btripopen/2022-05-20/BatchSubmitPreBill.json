{
  "camel_example": "",
  "cmd_full_name": "btripopen batch-submit-pre-bill",
  "cmd_name": "batch-submit-pre-bill",
  "deprecated": false,
  "description_en": "Submits a batch",
  "description_zh": "批次提交",
  "kebab_example": "",
  "method": "PUT",
  "multi_version": true,
  "name": "BatchSubmitPreBill",
  "operation": {
    "action": "BatchSubmitPreBill",
    "api_style": "ROA",
    "api_version": "2022-05-20",
    "method": "PUT",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": "/prebill/v1/batchSubmit"
  },
  "parameters": [
    {
      "example": "feth00jqwls",
      "help_en": "A header parameter obtained from the \"enterprise access credential\" operation (added in the header). In HTTP mode, you can use so_corp_token=value or dtb_corp_token=value in the URL as a substitute",
      "help_zh": "header参数 由“企业访问凭证”接口获取（添加在header里）,http模式可以在链接中使用so_corp_token=value或者dtb_corp_token=value来替代",
      "location": "header",
      "name": "x_acs_btrip_so_corp_token",
      "options": [
        "--x-acs-btrip-so-corp-token"
      ],
      "raw_name": "x-acs-btrip-so-corp-token",
      "required": false,
      "type": "string"
    },
    {
      "example": "2026-06-21",
      "help_en": "The bill batch date in the format of yyyy-MM-dd, such as 2026-06-21",
      "help_zh": "yyyy-MM-dd, 如2026-06-21",
      "location": "query",
      "name": "bill_batch",
      "options": [
        "--bill-batch"
      ],
      "raw_name": "bill_batch",
      "required": true,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The dimension type. Valid values:\n\n1: bill ID.\n2: order number.\n3: approval form.\n4: invoice title",
      "help_zh": "1: 账单id 2: 订单号 3: 审批单 4: 发票抬头",
      "location": "query",
      "name": "dimension",
      "options": [
        "--dimension"
      ],
      "raw_name": "dimension",
      "required": true,
      "type": "int"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The values determined by the dimension parameter. For example, if dimension is set to 1, the values should be bill IDs",
      "help_zh": "基于dimension决定, 如dimension values 应当传入账单id",
      "location": "query",
      "name": "values",
      "options": [
        "--values"
      ],
      "param_style": "json",
      "raw_name": "values",
      "required": true,
      "type": "array"
    },
    {
      "example": "1",
      "help_en": "The customer decision. Valid values:\n\n1: bill in the current period.\n2: deferred billing.\nnull: bill according to the current billing decision of the record",
      "help_zh": "1: 本期出账 2: 延期出账 null: 按照record当前出账决策进行出账",
      "location": "query",
      "name": "customer_decision",
      "options": [
        "--customer-decision"
      ],
      "raw_name": "customer_decision",
      "required": false,
      "type": "int"
    },
    {
      "example": "100.66.54.114",
      "help_en": "A system parameter. You do not need to manually specify this parameter",
      "help_zh": "（系统参数，无需手动输入）",
      "location": "query",
      "name": "app_ip",
      "options": [
        "--app-ip"
      ],
      "raw_name": "app_ip",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/prebill/v1/batchSubmit",
  "product_code": "btripOpen",
  "protocol": "HTTPS"
}
