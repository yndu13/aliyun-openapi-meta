{
  "camel_example": "",
  "cmd_full_name": "btripopen hotel-order-query",
  "cmd_name": "hotel-order-query",
  "deprecated": false,
  "description_en": "Queries the details of a hotel order",
  "description_zh": "查询酒店订单详情",
  "kebab_example": "",
  "method": "GET",
  "multi_version": true,
  "name": "HotelOrderQuery",
  "operation": {
    "action": "HotelOrderQuery",
    "api_style": "ROA",
    "api_version": "2022-05-20",
    "method": "GET",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": "/hotel/v1/order"
  },
  "parameters": [
    {
      "example": "1002145190081005400",
      "help_en": "The order ID",
      "help_zh": "订单id",
      "location": "query",
      "name": "order_id",
      "options": [
        "--order-id"
      ],
      "raw_name": "order_id",
      "required": true,
      "type": "int"
    },
    {
      "example": "feth00jqwls",
      "help_en": "A header parameter. Obtained from the enterprise access token API and added to the request header. In HTTP mode, you can use so_corp_token=value or dtb_corp_token=value in the URL as an alternative",
      "help_zh": "header参数,由“企业访问凭证”接口获取（添加在header里）,http模式可以在链接中使用so_corp_token=value或者dtb_corp_token=value来替代",
      "location": "header",
      "name": "x_acs_btrip_so_corp_token",
      "options": [
        "--x-acs-btrip-so-corp-token"
      ],
      "raw_name": "x-acs-btrip-so-corp-token",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/hotel/v1/order",
  "product_code": "btripOpen",
  "protocol": "HTTPS"
}
