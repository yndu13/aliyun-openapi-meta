{
  "camel_example": "",
  "cmd_full_name": "btripopen car-apply-query",
  "cmd_name": "car-apply-query",
  "deprecated": false,
  "description_en": "Queries intra-city car service approval orders",
  "description_zh": "查询市内用车申请单",
  "kebab_example": "",
  "method": "GET",
  "multi_version": true,
  "name": "CarApplyQuery",
  "operation": {
    "action": "CarApplyQuery",
    "api_style": "ROA",
    "api_version": "2022-05-20",
    "method": "GET",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": "/apply/v1/car"
  },
  "parameters": [
    {
      "example": "apply1",
      "help_en": "The third-party approval order ID",
      "help_zh": "三方审批单id",
      "location": "query",
      "name": "third_part_apply_id",
      "options": [
        "--third-part-apply-id"
      ],
      "raw_name": "third_part_apply_id",
      "required": false,
      "type": "string"
    },
    {
      "example": "2021-03-18 20:26:56",
      "help_en": "The start time for filtering approval orders by creation time (greater than or equal to). The time must be in the yyyy-MM-dd HH:mm:ss format",
      "help_zh": "审批单创建时间大于等于值（时间参数填写必须为yyyy-MM-dd HH:mm:ss字符串格式）",
      "location": "query",
      "name": "created_start_at",
      "options": [
        "--created-start-at"
      ],
      "pattern": "^\\d{4}(-\\d{2}){2}\\s+\\d{2}(:\\d{2}){2}$",
      "raw_name": "created_start_at",
      "required": false,
      "type": "string"
    },
    {
      "example": "2021-03-18 20:26:56",
      "help_en": "The end time for filtering approval orders by creation time (less than). The time must be in the yyyy-MM-dd HH:mm:ss format",
      "help_zh": "审批单创建时间小于值（时间参数填写必须为yyyy-MM-dd HH:mm:ss字符串格式）",
      "location": "query",
      "name": "created_end_at",
      "options": [
        "--created-end-at"
      ],
      "pattern": "^\\d{4}(-\\d{2}){2}\\s+\\d{2}(:\\d{2}){2}$",
      "raw_name": "created_end_at",
      "required": false,
      "type": "string"
    },
    {
      "example": "20",
      "help_en": "The number of entries per page. The value must be greater than or equal to 1. Default value: 20",
      "help_zh": "每页数据量，要求大于等于1，默认20",
      "location": "query",
      "name": "page_size",
      "options": [
        "--page-size"
      ],
      "raw_name": "page_size",
      "required": false,
      "type": "int"
    },
    {
      "example": "1",
      "help_en": "The page number. The value must be greater than or equal to 1. Default value: 1",
      "help_zh": "页码，要求大于等于1，默认1",
      "location": "query",
      "name": "page_number",
      "options": [
        "--page-number"
      ],
      "raw_name": "page_number",
      "required": false,
      "type": "int"
    },
    {
      "example": "userid",
      "help_en": "The third-party employee ID",
      "help_zh": "三方员工id",
      "location": "query",
      "name": "user_id",
      "options": [
        "--user-id"
      ],
      "raw_name": "user_id",
      "required": false,
      "type": "string"
    },
    {
      "example": "feth00jqwls",
      "help_en": "A header parameter obtained from the \"Enterprise Access Credential\" operation (added in the header). In HTTP mode, you can use so_corp_token=value or dtb_corp_token=value in the URL as an alternative",
      "help_zh": "header参数,由“企业访问凭证”接口获取（添加在header里）,http模式可以在链接中使用so_corp_token=value或者dtb_corp_token=value来替代",
      "location": "header",
      "name": "x_acs_btrip_so_corp_token",
      "options": [
        "--x-acs-btrip-so-corp-token"
      ],
      "raw_name": "x-acs-btrip-so-corp-token",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/apply/v1/car",
  "product_code": "btripOpen",
  "protocol": "HTTPS"
}
