{
  "camel_example": "",
  "cmd_full_name": "computenest upgrade-service-instance",
  "cmd_name": "upgrade-service-instance",
  "deprecated": false,
  "description_en": "Upgrades a service instance to a new version",
  "description_zh": "升级服务实例的版本",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UpgradeServiceInstance",
  "operation": {
    "action": "UpgradeServiceInstance",
    "api_style": "RPC",
    "api_version": "2021-06-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID",
      "help_zh": "地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "si-d6ab3a63ccbb4bxxxxxx",
      "help_en": "The service instance ID",
      "help_zh": "服务实例ID",
      "location": "query",
      "name": "service_instance_id",
      "options": [
        "--service-instance-id"
      ],
      "raw_name": "ServiceInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "2",
      "help_en": "The service version to upgrade to",
      "help_zh": "要升级到的服务版本",
      "location": "query",
      "name": "service_version",
      "options": [
        "--service-version"
      ],
      "raw_name": "ServiceVersion",
      "required": true,
      "type": "string"
    },
    {
      "example": "{ \\\"RegionId\\\": \\\"cn-hangzhou\\\", \\\"InstanceType\\\": \\\"ecs.g5.large\\\"}",
      "help_en": "The parameters required for the upgrade. This is used when new parameters are added to the new service version",
      "help_zh": "升级时所需参数，用于新版本服务有新增参数时",
      "location": "query",
      "name": "parameters",
      "options": [
        "--parameters"
      ],
      "param_style": "json",
      "raw_name": "Parameters",
      "required": false,
      "type": "map",
      "value": {
        "type": "any"
      }
    },
    {
      "example": "123e4567-e89b-12d3-a456-426655440000",
      "help_en": "A client-generated token that ensures the idempotence of the request. The token must be unique for each request. It can contain only ASCII characters and must be no more than 64 characters long",
      "help_zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符，且不能超过64个字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to perform a dry run. A dry run checks for issues such as permissions and the instance status. Valid values:\n\ntrue: Sends the request without upgrading the service instance.\n\nfalse: Sends the request and upgrades the service instance after the check is passed",
      "help_zh": "是否对此次创建请求执行预检，包括权限、实例状态校验等。可能的值：\n\ntrue：发送请求，不创建服务实例。\n\nfalse：发送请求，检查通过后创建服务实例",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "ComputeNest",
  "protocol": "HTTP|HTTPS"
}
