{
  "cmd_full_name": "config list-supported-products",
  "cmd_name": "list-supported-products",
  "deprecated": false,
  "description_en": "Queries the cloud services and resource types that are supported by Cloud Config",
  "description_zh": "查询配置审计服务支持的云产品和资源类型列表",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "ListSupportedProducts",
  "operation": {
    "action": "ListSupportedProducts",
    "api_style": "RPC",
    "api_version": "2020-09-07",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "list",
    "url": ""
  },
  "parameters": [
    {
      "example": "D3AjqMNSy0ls7zBNCf3a****",
      "help_en": "The pagination token that is used in the next request to retrieve a new page of results. You do not need to specify this parameter for the first request. You must specify the token that is obtained from the previous query as the value of NextToken",
      "help_zh": "当请求的返回结果被截断时，您可以使用NextToken再次发起请求，获取从当前截断位置之后的内容",
      "location": "query",
      "name": "next_token",
      "options": [
        "--next-token"
      ],
      "raw_name": "NextToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "200",
      "help_en": "The maximum number of entries to return in a request.\n\nValid values: 1 to 500. Default value: 200",
      "help_zh": "单次请求返回结果的最大条数。\n\n取值范围：1~500。默认值：200",
      "location": "query",
      "maximum": "500",
      "minimum": "1",
      "name": "max_results",
      "options": [
        "--max-results"
      ],
      "raw_name": "MaxResults",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "Config",
  "protocol": "HTTPS"
}
