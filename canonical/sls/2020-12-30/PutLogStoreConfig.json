{
  "camel_example": "",
  "cmd_full_name": "sls put-log-store-config",
  "cmd_name": "put-log-store-config",
  "deprecated": false,
  "description_en": "Updates the configuration of a Logstore",
  "description_zh": "修改日志库的配置",
  "kebab_example": "",
  "method": "PUT",
  "multi_version": false,
  "name": "PutLogStoreConfig",
  "operation": {
    "action": "PutLogStoreConfig",
    "api_style": "ROA",
    "api_version": "2020-12-30",
    "body_mapping": {
      "client_ip_headers": "clientIpHeaders"
    },
    "content_type": "application/json",
    "method": "PUT",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "json",
    "system_operation_type": "update",
    "url": "/logstores/{logstore}/config"
  },
  "parameters": [
    {
      "example": "ali-test-project",
      "help_en": "The name of the Project",
      "help_zh": "Project 名称",
      "location": "host",
      "name": "project",
      "options": [
        "--project"
      ],
      "raw_name": "project",
      "required": true,
      "type": "string"
    },
    {
      "example": "ali-test-logstore",
      "help_en": "The name of the Logstore",
      "help_zh": "Logstore 名称",
      "location": "path",
      "name": "logstore",
      "options": [
        "--logstore"
      ],
      "raw_name": "logstore",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "Specifies a list of header keys from which to extract the client IP address during log ingestion. Simple Log Service (SLS) searches these headers in the specified order and uses the first valid IP address that it finds. The key names are case-insensitive, but their order is significant. To use this feature, you must also enable the `appendMeta` setting for the Logstore. This setting adds the extracted IP address as a log tag",
      "help_zh": "配置 logstore 写入时提取 IP 地址的来源 header keys。顺序敏感，大小写不敏感。配置后需要打开 logstore 的 appendMeta 记录外网 Ip 来源功能才会生效，将从请求的 Header 中按照配置的 keys 提取 IP，添加到日志 Tag 中",
      "location": "body",
      "name": "client_ip_headers",
      "options": [
        "--client-ip-headers"
      ],
      "param_style": "json",
      "raw_name": "clientIpHeaders",
      "required": false,
      "type": "array"
    }
  ],
  "pathPattern": "/logstores/[logstore]/config",
  "product_code": "Sls",
  "protocol": "HTTPS",
  "v1_body_parameters": [
    {
      "help_en": "The request body",
      "help_zh": "请求体",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    }
  ]
}
