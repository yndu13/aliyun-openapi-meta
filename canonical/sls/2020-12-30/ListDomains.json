{
  "camel_example": "",
  "cmd_full_name": "sls list-domains",
  "cmd_name": "list-domains",
  "deprecated": false,
  "description_en": "Queries the custom domain names that are bound to projects",
  "description_zh": "列出Project绑定的自定义域名",
  "kebab_example": "",
  "method": "GET",
  "multi_version": false,
  "name": "ListDomains",
  "operation": {
    "action": "ListDomains",
    "api_style": "ROA",
    "api_version": "2020-12-30",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": "/domains"
  },
  "parameters": [
    {
      "example": "ali-test-project",
      "help_en": "The name of the project",
      "help_zh": "Project名称",
      "location": "host",
      "name": "project",
      "options": [
        "--project"
      ],
      "raw_name": "project",
      "required": true,
      "type": "string"
    },
    {
      "example": "0",
      "help_en": "The line from which the query starts. Default value: 0",
      "help_zh": "分页查询开始的位置。默认值为0",
      "location": "query",
      "name": "offset",
      "options": [
        "--offset"
      ],
      "raw_name": "offset",
      "required": false,
      "type": "int"
    },
    {
      "example": "10",
      "help_en": "The number of entries per page. Default value: 500. Maximum value: 500",
      "help_zh": "分页查询时，设置的每页行数。默认值为500，最大值为500",
      "location": "query",
      "name": "size",
      "options": [
        "--size"
      ],
      "raw_name": "size",
      "required": false,
      "type": "int"
    },
    {
      "example": "example.com",
      "help_en": "The domain name that is used to match custom domain names. For example, if you set domainName to `example.com`, the matched domain names are `a.example.com` and `b.example.com`",
      "help_zh": "用于匹配自定义域名，例如`example.com`会匹配到`a.example.com`与`b.example.com`",
      "location": "query",
      "name": "domain_name",
      "options": [
        "--domain-name"
      ],
      "raw_name": "domainName",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/domains",
  "product_code": "Sls",
  "protocol": "HTTP|HTTPS"
}
