{
  "camel_example": "",
  "cmd_full_name": "sls update-consumer-group",
  "cmd_name": "update-consumer-group",
  "deprecated": false,
  "description_en": "Updates the attributes of a consumer group",
  "description_zh": "修改指定消费组属性",
  "kebab_example": "",
  "method": "PUT",
  "multi_version": false,
  "name": "UpdateConsumerGroup",
  "operation": {
    "action": "UpdateConsumerGroup",
    "api_style": "ROA",
    "api_version": "2020-12-30",
    "body_mapping": {
      "order": "order",
      "timeout": "timeout"
    },
    "content_type": "application/json",
    "method": "PUT",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "json",
    "system_operation_type": "update",
    "url": "/logstores/{logstore}/consumergroups/{consumerGroup}"
  },
  "parameters": [
    {
      "example": "ali-test-project",
      "help_en": "The name of the project",
      "help_zh": "Project名称",
      "location": "host",
      "name": "project",
      "options": [
        "--project"
      ],
      "raw_name": "project",
      "required": true,
      "type": "string"
    },
    {
      "example": "logstore-test",
      "help_en": "The name of the Logstore",
      "help_zh": "Logstore名称",
      "location": "path",
      "name": "logstore",
      "options": [
        "--logstore"
      ],
      "raw_name": "logstore",
      "required": true,
      "type": "string"
    },
    {
      "example": "consumer-group-1",
      "help_en": "The name of the consumer group",
      "help_zh": "消费组名称",
      "location": "path",
      "name": "consumer_group",
      "options": [
        "--consumer-group"
      ],
      "raw_name": "consumerGroup",
      "required": true,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to consume data in sequence. Valid values:\n\ntrue: If a shard is split, the data in the original shard is consumed first. Then, the data in the new shards is consumed at the same time. If shards are merged, the data in the original shards is consumed first. Then, the data in the new shard is consumed.\n\nfalse: The data in all shards is consumed at the same time. If a new shard is generated after a shard is split or shards are merged, the data in the new shard is immediately consumed",
      "help_zh": "是否按顺序消费。\n\ntrue：在Shard中按顺序消费。Shard分裂后，先消费原Shard数据，然后同时消费由该Shard分裂的Shard数据。Shard合并后，先消费原Shard数据，然后消费由原Shard合并后的新Shard数据。\nfalse：不按顺序消费。即所有同时开始消费，Shard分裂或者合并后产生的新Shard，也会立即消费",
      "location": "body",
      "name": "order",
      "options": [
        "--order"
      ],
      "param_style": "json",
      "raw_name": "order",
      "required": false,
      "type": "bool"
    },
    {
      "example": "300",
      "help_en": "The timeout period. If Simple Log Service does not receive heartbeats from a consumer within the timeout period, Simple Log Service deletes the consumer. Unit: seconds",
      "help_zh": "超时时间。在超时时间段内没有收到心跳，消费者将被删除。单位：秒",
      "location": "body",
      "name": "timeout",
      "options": [
        "--timeout"
      ],
      "param_style": "json",
      "raw_name": "timeout",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "/logstores/[logstore]/consumergroups/[consumerGroup]",
  "product_code": "Sls",
  "protocol": "HTTP|HTTPS",
  "v1_body_parameters": [
    {
      "help_en": "The request body. At least one of order and timeout has a value",
      "help_zh": "请求体参数。order和timeout至少一个有值",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": true,
      "type": "string"
    }
  ]
}
