{
  "camel_example": "",
  "cmd_full_name": "sgw describe-express-syncs",
  "cmd_name": "describe-express-syncs",
  "deprecated": false,
  "description_en": "Queries the details of express synchronization groups, including their names, IDs, and bucket names",
  "description_zh": "查询极速同步组，包括极速同步组名称、极速同步组ID、Bucket名称等详情",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeExpressSyncs",
  "operation": {
    "action": "DescribeExpressSyncs",
    "api_style": "RPC",
    "api_version": "2018-05-11",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "sgw****",
      "help_en": "The name of the bucket. If you do not specify this parameter, all express synchronization groups that belong to the current user are queried",
      "help_zh": "Bucket 名称。未传递参数时，默认查询该用户下的所有极速同步组",
      "location": "query",
      "name": "bucket_name",
      "options": [
        "--bucket-name"
      ],
      "raw_name": "BucketName",
      "required": false,
      "type": "string"
    },
    {
      "example": "test****",
      "help_en": "The subdirectory of the bucket. If you do not specify this parameter, the express synchronization groups of the bucket are queried. If you specify an empty string, the express synchronization groups in the root directory of the bucket are queried by default.\n\nYou cannot leave the bucket name empty and query only the bucket subdirectory",
      "help_zh": "Bucket 子目录。未传递参数时，查询该Bucket的极速同步组；传入空字符串时，默认查询该Bucket根目录的极速同步组。\n不支持Bucket名称为空，只查询Bucket子目录的场景",
      "location": "query",
      "name": "bucket_prefix",
      "options": [
        "--bucket-prefix"
      ],
      "raw_name": "BucketPrefix",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "sgw",
  "protocol": "HTTP|HTTPS"
}
