{
  "camel_example": "",
  "cmd_full_name": "sgw expand-cache-disk",
  "cmd_name": "expand-cache-disk",
  "deprecated": false,
  "description_en": "Expands the cache disk of a Cloud Storage Gateway",
  "description_zh": "扩容云上网关缓存盘",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ExpandCacheDisk",
  "operation": {
    "action": "ExpandCacheDisk",
    "api_style": "RPC",
    "api_version": "2018-05-11",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "gw-000eg44nmxbsfwbvq***",
      "help_en": "The ID of the gateway",
      "help_zh": "网关ID",
      "location": "query",
      "name": "gateway_id",
      "options": [
        "--gateway-id"
      ],
      "raw_name": "GatewayId",
      "required": true,
      "type": "string"
    },
    {
      "example": "101",
      "help_en": "The capacity of the cache disk after it is scaled out. Unit: GB. The following requirements must be met:\n\nThe capacity of the cache disk after it is scaled out cannot be smaller than the original capacity. You cannot scale in a cache disk.\nThe maximum capacity of a cache disk for a Basic gateway is 4 TB. The maximum capacity of a cache disk for a Medium gateway is 8 TB. The maximum capacity of a cache disk for an Enhanced or Performance gateway is 32 TB.\nThe minimum capacity of a cache disk for a file gateway is 40 GB. The minimum capacity of a cache disk for an iSCSI gateway is 20 GB",
      "help_zh": "扩容后的缓存容量。单位为GB。扩容要求如下：\n\n扩容后的缓存盘容量不能比原先缓存盘的容量小，即不支持缩减缓存盘。\n\n基础型网关扩容缓存盘上限为4 TB，标准型网关扩容缓存盘上限为8 TB，增强型和性能型网关扩容缓存盘上限为32 TB。\n\n文件网关缓存盘扩容下限为40 GB，块网关缓存盘扩容下限为20 GB",
      "location": "query",
      "name": "new_size_in_gb",
      "options": [
        "--new-size-in-gb"
      ],
      "raw_name": "NewSizeInGB",
      "required": true,
      "type": "int"
    },
    {
      "example": "/dev/vdb",
      "help_en": "The internal device name of the cache disk. You can call the DescribeGatewayCaches operation to query the internal device name of the cache disk",
      "help_zh": "缓存盘内部设备名。您可以通过DescribeGatewayCaches接口获得缓存盘内部设备名",
      "location": "query",
      "name": "local_file_path",
      "options": [
        "--local-file-path"
      ],
      "raw_name": "LocalFilePath",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "sgw",
  "protocol": "HTTP|HTTPS"
}
