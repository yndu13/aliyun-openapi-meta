{
  "camel_example": "",
  "cmd_full_name": "dyvmsapi cloud-list-online-agent",
  "cmd_name": "cloud-list-online-agent",
  "deprecated": false,
  "description_en": "Lists online agents",
  "description_zh": "查询在线的座席信息列表",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CloudListOnlineAgent",
  "operation": {
    "action": "CloudListOnlineAgent",
    "api_style": "RPC",
    "api_version": "2017-05-25",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "7000002",
      "help_en": "The call center ID",
      "help_zh": "呼叫中心 id",
      "location": "query",
      "name": "enterprise_id",
      "options": [
        "--enterprise-id"
      ],
      "raw_name": "EnterpriseId",
      "required": true,
      "type": "int"
    },
    {
      "example": "123,124",
      "help_en": "The queue numbers. Specify one or more queue numbers to filter agents by queue. Separate multiple queue numbers with a comma (,). If you do not specify this parameter, the API returns agents from all queues",
      "help_zh": "队列号，指定队列号则查询该队列号所对应队列的监控信息，支持同时查询多个队列号对应队列的监控信息，多个队列号使用英文逗号\",\"分隔，不指定则查询所有队列监控信息",
      "location": "query",
      "name": "qnos",
      "options": [
        "--qnos"
      ],
      "raw_name": "Qnos",
      "required": false,
      "type": "string"
    },
    {
      "example": "1111,1112",
      "help_en": "The agent IDs. You can specify one or more agent IDs to retrieve information for specific agents. Separate multiple agent IDs with a comma (,). If you do not specify this parameter, the API returns information for all agents under the current account",
      "help_zh": "坐席工号，指定座席工号则查询该工号座席的监控信息，可指定多个座席工号，指定多个座席工号使用英文逗号\",\"分隔，不指定则查询所有队列监控信息，不指定则查询该账户下所有座席的监控信息",
      "location": "query",
      "name": "cnos",
      "options": [
        "--cnos"
      ],
      "raw_name": "Cnos",
      "required": false,
      "type": "string"
    },
    {
      "example": "s1",
      "help_en": "The agent state. Use this parameter to filter agents by their current state. Separate multiple states with a comma (,). Valid values include `s1` for Idle, `s2` for Paused, `s3` for Wrap-up, `s4` for Calling, `s5` for Ringing, and `s6` for On a call",
      "help_zh": "坐席状态，s1:空闲，s2:置忙，s3:整理，s4:呼叫中，s5:响铃，s6通话   可传多个状态码，多个值之间以\",\"分隔",
      "location": "query",
      "name": "state_code",
      "options": [
        "--state-code"
      ],
      "raw_name": "StateCode",
      "required": false,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The pause type. Use this parameter to filter agents based on their pause type. You can specify multiple pause types, separated by a comma (,).<br>Valid values: `1` for Normal, `2` for Break, `3` for IM, and `4` for Forced.<br>",
      "help_zh": "置忙类型，根据座席置忙类型过滤监控数据，可指定多个置忙类型，指定多个置忙类型使用英文逗号\",\"分隔，不指定则默认查询所有设置状态的座席。<br>取值说明1普通，2休息，3 IM，4 强制",
      "location": "query",
      "name": "pause_type",
      "options": [
        "--pause-type"
      ],
      "raw_name": "PauseType",
      "required": false,
      "type": "string"
    },
    {
      "example": "示例值",
      "help_en": "The pause reason. Use this parameter to filter agents based on the reason they are paused. You can specify multiple pause reasons, separated by a comma (,). If you omit this parameter, the query returns all paused agents regardless of the reason",
      "help_zh": "置忙原因，根据座席置忙原因过滤监控数据，可指定多个置忙原因，指定多个置忙原因使用英文逗号\",\"分隔，不指定则默认查询所有设置状态的座席",
      "location": "query",
      "name": "pause_description",
      "options": [
        "--pause-description"
      ],
      "raw_name": "PauseDescription",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Dyvmsapi",
  "protocol": "HTTPS"
}
