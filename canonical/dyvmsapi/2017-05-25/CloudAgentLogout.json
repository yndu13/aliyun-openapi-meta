{
  "camel_example": "",
  "cmd_full_name": "dyvmsapi cloud-agent-logout",
  "cmd_name": "cloud-agent-logout",
  "deprecated": false,
  "description_en": "Logs out an agent",
  "description_zh": "下线座席",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CloudAgentLogout",
  "operation": {
    "action": "CloudAgentLogout",
    "api_style": "RPC",
    "api_version": "2017-05-25",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "7000002",
      "help_en": "The call center ID",
      "help_zh": "呼叫中心 id",
      "location": "query",
      "name": "enterprise_id",
      "options": [
        "--enterprise-id"
      ],
      "raw_name": "EnterpriseId",
      "required": true,
      "type": "int"
    },
    {
      "example": "1111",
      "help_en": "The agent ID. It must be a positive integer with 3 to 10 digits",
      "help_zh": "座席工号；取值3-10位正整数",
      "location": "query",
      "name": "cno",
      "options": [
        "--cno"
      ],
      "raw_name": "Cno",
      "required": true,
      "type": "string"
    },
    {
      "example": "0",
      "help_en": "Specifies whether to unbind the phone number from the agent. Valid values are `0` (do not unbind) and `1` (unbind). The default value is `0`",
      "help_zh": "取值： 0:不解除绑定电话， 1:解除绑定电话 默认为0",
      "location": "query",
      "name": "remove_binding",
      "options": [
        "--remove-binding"
      ],
      "raw_name": "RemoveBinding",
      "required": false,
      "type": "int"
    },
    {
      "example": "1",
      "help_en": "Specifies whether to send a kickout event to the agent. Valid values are `0` (do not send) and `1` (send). The default value is `1`",
      "help_zh": "是否给座席发生kickout事件；取值： 0:不发送， 1:发送 默认为1，发送",
      "location": "query",
      "name": "is_kickout",
      "options": [
        "--is-kickout"
      ],
      "raw_name": "IsKickout",
      "required": false,
      "type": "int"
    },
    {
      "example": "0",
      "help_en": "Specifies whether to ignore errors from repeated logout requests. Valid values are `0` (do not ignore) and `1` (ignore). The default value is `0`",
      "help_zh": "是否忽略重复下线报错；取值：0:不忽略 1:忽略 默认为0，不忽略",
      "location": "query",
      "name": "ignore_offline",
      "options": [
        "--ignore-offline"
      ],
      "raw_name": "IgnoreOffline",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "Dyvmsapi",
  "protocol": "HTTPS"
}
