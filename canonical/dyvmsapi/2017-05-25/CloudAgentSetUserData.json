{
  "camel_example": "",
  "cmd_full_name": "dyvmsapi cloud-agent-set-user-data",
  "cmd_name": "cloud-agent-set-user-data",
  "deprecated": false,
  "description_en": "Sets the user data for an agent",
  "description_zh": "使用此接口设置座席随路数据",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CloudAgentSetUserData",
  "operation": {
    "action": "CloudAgentSetUserData",
    "api_style": "RPC",
    "api_version": "2017-05-25",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "7000002",
      "help_en": "The call center ID",
      "help_zh": "呼叫中心 id",
      "location": "query",
      "name": "enterprise_id",
      "options": [
        "--enterprise-id"
      ],
      "raw_name": "EnterpriseId",
      "required": true,
      "type": "int"
    },
    {
      "example": "1111",
      "help_en": "The agent ID. The value must be a 3- to 10-digit positive integer",
      "help_zh": "座席工号；取值3-10位正整数",
      "location": "query",
      "name": "cno",
      "options": [
        "--cno"
      ],
      "raw_name": "Cno",
      "required": true,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The user data direction. Valid values: `1` (agent side) and `2` (non-agent side)",
      "help_zh": "随路数据方向；取值说明：1： 座席侧，2： 非座席侧",
      "location": "query",
      "name": "direction",
      "options": [
        "--direction"
      ],
      "raw_name": "Direction",
      "required": true,
      "type": "int"
    },
    {
      "example": "{\"key\":\"value\"}",
      "help_en": "The user data, formatted as a JSON string. This value is injected as a channel variable. The format must be a simple key-value map, such as `{\"key\":\"value\"}`. Arrays and nested objects are not supported",
      "help_zh": "json格式字符串,传入的值会打入通道变量,格式：json字符串{\"key\":\"value\"} 随路数据格式：key=value,不支持数组，嵌套",
      "location": "query",
      "name": "user_data",
      "options": [
        "--user-data"
      ],
      "raw_name": "UserData",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Dyvmsapi",
  "protocol": "HTTPS"
}
