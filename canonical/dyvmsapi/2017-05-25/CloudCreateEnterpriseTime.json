{
  "camel_example": "",
  "cmd_full_name": "dyvmsapi cloud-create-enterprise-time",
  "cmd_name": "cloud-create-enterprise-time",
  "deprecated": false,
  "description_en": "Creates a time condition configuration",
  "description_zh": "通过本接口，可以新增时间条件设置配置",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CloudCreateEnterpriseTime",
  "operation": {
    "action": "CloudCreateEnterpriseTime",
    "api_style": "RPC",
    "api_version": "2017-05-25",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "7000002",
      "help_en": "The ID of the call center",
      "help_zh": "呼叫中心 id",
      "location": "query",
      "name": "enterprise_id",
      "options": [
        "--enterprise-id"
      ],
      "raw_name": "EnterpriseId",
      "required": true,
      "type": "int"
    },
    {
      "example": "demo01",
      "help_en": "The name of the time condition. The name must be unique within the same call center",
      "help_zh": "时间条件名称；同一呼叫中心下不能重复",
      "location": "query",
      "name": "name",
      "options": [
        "--name"
      ],
      "raw_name": "Name",
      "required": true,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The priority of the time condition. The priority must be unique within the same call center. The minimum value is 1. A smaller value indicates a higher priority",
      "help_zh": "时间条件优先级；同一呼叫中心下不能重复，值从1开始，值越小优先级越高",
      "location": "query",
      "name": "priority",
      "options": [
        "--priority"
      ],
      "raw_name": "Priority",
      "required": true,
      "type": "int"
    },
    {
      "example": "1",
      "help_en": "The condition type. Valid values: `1` (By day of the week) and `2` (By a fixed date range)",
      "help_zh": "条件类型 1:按照星期配置 2:按照固定时间配置",
      "location": "query",
      "name": "type",
      "options": [
        "--type"
      ],
      "raw_name": "Type",
      "required": true,
      "type": "int"
    },
    {
      "example": "1",
      "help_en": "The time type. Valid values: `1` (Continuous) and `2` (Interval)",
      "help_zh": "时间类型 1.连续 2.间隔",
      "location": "query",
      "name": "time_type",
      "options": [
        "--time-type"
      ],
      "raw_name": "TimeType",
      "required": true,
      "type": "int"
    },
    {
      "example": "2026-04-20",
      "help_en": "The start date. This parameter is required when `Type` is set to `2`. The value must be in the `YYYY-MM-DD` format. If `Type` is set to `1`, pass an empty string. The value of `FromDay` cannot be later than the value of `ToDay`",
      "help_zh": "起始日期；当type=2 时必选，格式为：yyyy-MM-dd 、type=1时为空串 fromDay不能大于toDay",
      "location": "query",
      "name": "from_day",
      "options": [
        "--from-day"
      ],
      "raw_name": "FromDay",
      "required": false,
      "type": "string"
    },
    {
      "example": "2026-04-25",
      "help_en": "The end date. This parameter is required when `Type` is set to `2`. The value must be in the `YYYY-MM-DD` format. If `Type` is set to `1`, pass an empty string. The value of `ToDay` cannot be earlier than the value of `FromDay`",
      "help_zh": "终止日期；当type=2 时必选，格式为：yyyy-MM-dd 、type=1时为空串 fromDay不能大于toDay",
      "location": "query",
      "name": "to_day",
      "options": [
        "--to-day"
      ],
      "raw_name": "ToDay",
      "required": false,
      "type": "string"
    },
    {
      "example": "2,3,4",
      "help_en": "This parameter is required when `Type` is set to `1`. Specifies the days of the week. Valid values: Monday=`2`, Tuesday=`3`, Wednesday=`4`, Thursday=`5`, Friday=`6`, Saturday=`7`, and Sunday=`1`. Separate multiple values with commas (,), for example, `2,3,4`. If `Type` is set to `2`, pass an empty string",
      "help_zh": "当type=1 时必选,周一：2、周二：3、周三：4、周四：5、周五：6、周六：7、周日：1。星期值以,分隔 例如： 2,3,4代表周一周二周三、type=2时为空串",
      "location": "query",
      "name": "day_of_week",
      "options": [
        "--day-of-week"
      ],
      "raw_name": "DayOfWeek",
      "required": false,
      "type": "string"
    },
    {
      "example": "06:00",
      "help_en": "The start time in the `HH:mm` format. For example, `06:00`. The value of `StartTime` cannot be later than the value of `EndTime`",
      "help_zh": "开始时间；格式为 HH:mm 例如、 06:00 startTime不能大于endTime",
      "location": "query",
      "name": "start_time",
      "options": [
        "--start-time"
      ],
      "raw_name": "StartTime",
      "required": true,
      "type": "string"
    },
    {
      "example": "19:00",
      "help_en": "The end time in the `HH:mm` format. For example, `19:00`. The value of `EndTime` cannot be earlier than the value of `StartTime`",
      "help_zh": "结束时间；格式为 HH:mm 例如、 19:00 startTime不能大于endTime",
      "location": "query",
      "name": "end_time",
      "options": [
        "--end-time"
      ],
      "raw_name": "EndTime",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Dyvmsapi",
  "protocol": "HTTPS"
}
