{
  "camel_example": "",
  "cmd_full_name": "dyvmsapi cloud-create-agent-group",
  "cmd_name": "cloud-create-agent-group",
  "deprecated": false,
  "description_en": "Use this operation to create an agent group",
  "description_zh": "通过本接口，可以进行新增外呼组操作",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CloudCreateAgentGroup",
  "operation": {
    "action": "CloudCreateAgentGroup",
    "api_style": "RPC",
    "api_version": "2017-05-25",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "7000002",
      "help_en": "The ID of the call center",
      "help_zh": "呼叫中心 id",
      "location": "query",
      "name": "enterprise_id",
      "options": [
        "--enterprise-id"
      ],
      "raw_name": "EnterpriseId",
      "required": true,
      "type": "int"
    },
    {
      "example": "WH12",
      "help_en": "The outbound group number, which must be unique within an enterprise. The number must be 2 to 20 characters long, start with a letter, and contain only letters and digits. An enterprise can have a maximum of 1,000 agent groups",
      "help_zh": "说明：同一企业外呼组编号不能重复，外呼组编号长度为2-20，以字母开头，须同时且只能包含字母和数字，同一企业最多可创建1000个外呼组",
      "location": "query",
      "name": "gno",
      "options": [
        "--gno"
      ],
      "raw_name": "Gno",
      "required": true,
      "type": "string"
    },
    {
      "example": "gpname1",
      "help_en": "The outbound group name. The name cannot be empty, must not exceed 50 characters, and must be URL-encoded in UTF-8",
      "help_zh": "说明：外呼组名称不能为空，长度不能大于50,需进行UTF-8格式的URLEncode编码",
      "location": "query",
      "name": "group_name",
      "options": [
        "--group-name"
      ],
      "raw_name": "GroupName",
      "required": true,
      "type": "string"
    },
    {
      "example": "comment3",
      "help_en": "The description. The value can be up to 100 characters long and must be URL-encoded in UTF-8",
      "help_zh": "说明：描述长度不能大于100,需进行UTF-8格式的URLEncode编码",
      "location": "query",
      "name": "comment",
      "options": [
        "--comment"
      ],
      "raw_name": "Comment",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Dyvmsapi",
  "protocol": "HTTPS"
}
