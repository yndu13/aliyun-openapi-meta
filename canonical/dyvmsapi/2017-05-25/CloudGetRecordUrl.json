{
  "camel_example": "",
  "cmd_full_name": "dyvmsapi cloud-get-record-url",
  "cmd_name": "cloud-get-record-url",
  "deprecated": false,
  "description_en": "Call this operation to obtain a preview or download URL for a recording file based on its name",
  "description_zh": "通过本接口，可以根据录音文件名获取录音试听或下载地址",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CloudGetRecordUrl",
  "operation": {
    "action": "CloudGetRecordUrl",
    "api_style": "RPC",
    "api_version": "2017-05-25",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "7000002",
      "help_en": "The ID of the call center",
      "help_zh": "呼叫中心 id",
      "location": "query",
      "name": "enterprise_id",
      "options": [
        "--enterprise-id"
      ],
      "raw_name": "EnterpriseId",
      "required": true,
      "type": "int"
    },
    {
      "example": "record",
      "help_en": "The recording type. If you do not specify this parameter, the system automatically determines the type from the `RecordFile` name. Valid values: `record` (a standard recording), `voicemail` (a voicemail), `tsi` (a color ringback tone used for a preview outbound call to a mobile number that goes unanswered when recording status detection is enabled), `rasr` (a customer-side recording for a voice bot), `transfer` (a transfer), `consult` (a consult), and `threeway` (a three-way call)",
      "help_zh": "录音类型，不填可根据 recordFile 解析；record:录音voicemail:留言tsi:彩铃、当开启号码录音状态识别，发起预览外呼，客户号码是手机且客户未接听时返回该地址 rasr:语音机器人客户侧录音 transfer:转接consult:咨询threeway:三方",
      "location": "query",
      "name": "record_type",
      "options": [
        "--record-type"
      ],
      "raw_name": "RecordType",
      "required": false,
      "type": "string"
    },
    {
      "example": "7000101-20160815140025-01087120766-01087120766--record-sip-1-1471240825.87",
      "help_en": "The recording file name. Example: `7000101-20160815140025-01087120766-01087120766--record-sip-1-1471240825.87`",
      "help_zh": "录音文件名；如7000101-20160815140025-01087120766-01087120766--record-sip-1-1471240825.87",
      "location": "query",
      "name": "record_file",
      "options": [
        "--record-file"
      ],
      "raw_name": "RecordFile",
      "required": true,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The recording file type. Valid values: `0` for MP3 (default) and `1` for WAV",
      "help_zh": "录音文件类型；取值说明：0为mp3，1为wav，默认为mp3类型",
      "location": "query",
      "name": "record_format",
      "options": [
        "--record-format"
      ],
      "raw_name": "RecordFormat",
      "required": false,
      "type": "int"
    },
    {
      "example": "4",
      "help_en": "The call type. This parameter is required to retrieve `customer-side` or `agent-side` recordings when `separate-channel recording` is enabled. It is effective only when `RecordFormat` is set to `1` (WAV) and is ignored if `RecordFormat` is set to `0` (MP3). Valid values: `1` (inbound call), `2` (Webcall), `4` (preview outbound call), and `5` (predictive outbound call)",
      "help_zh": "呼叫类型；说明：开启分线录音时，获取客户侧或座席侧录音需要，recordFormat=1时有效，recordFormat=0时忽略。取值范围：1,2,4,5（数字含义：呼入,webcall,预览外呼,预测外呼）",
      "location": "query",
      "name": "call_type",
      "options": [
        "--call-type"
      ],
      "raw_name": "CallType",
      "required": false,
      "type": "int"
    },
    {
      "example": "2",
      "help_en": "Specifies the recording side for a `separate-channel recording`. This parameter is required to retrieve `customer-side` or `agent-side` recordings when `separate-channel recording` is enabled. It is effective only when `RecordFormat` is set to `1` (WAV) and is ignored if `RecordFormat` is set to `0` (MP3). If you specify this parameter, `CallType` is also required. Valid values: `1` (customer side) and `2` (agent side)",
      "help_zh": "分线录音侧；说明：开启分线录音时，获取客户侧或座席侧录音需要，recordFormat=1时有效，recordFormat=0时忽略。取值范围：1,2 (数字含义：客户侧，座席侧)recordSide不为空时，callType必选",
      "location": "query",
      "name": "record_side",
      "options": [
        "--record-side"
      ],
      "raw_name": "RecordSide",
      "required": false,
      "type": "int"
    },
    {
      "example": "1",
      "help_en": "Specifies whether to generate a download link. Set this to `1` for a download URL. If you set this to `0` or leave it empty, a preview URL is generated",
      "help_zh": "是否下载；１为下载，空或０表示试听",
      "location": "query",
      "name": "download",
      "options": [
        "--download"
      ],
      "raw_name": "Download",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "Dyvmsapi",
  "protocol": "HTTPS"
}
