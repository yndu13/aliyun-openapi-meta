{
  "camel_example": "",
  "cmd_full_name": "vpc create-vpn-route-entry",
  "cmd_name": "create-vpn-route-entry",
  "deprecated": false,
  "description_en": "Creates a destination route for a VPN gateway instance by calling the CreateVpnRouteEntry operation. After a destination route is created, the VPN gateway instance matches the destination route based on the destination IP address of the traffic and then forwards the traffic based on the matched destination route",
  "description_zh": "调用CreateVpnRouteEntry接口为VPN网关实例创建目的路由，创建目的路由后，VPN网关实例将基于流量的目的IP地址匹配目的路由，然后根据流量匹配到的目的路由转发流量",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CreateVpnRouteEntry",
  "operation": {
    "action": "CreateVpnRouteEntry",
    "api_style": "RPC",
    "api_version": "2016-04-28",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID of the VPN gateway instance.\n\nYou can call the https://help.aliyun.com/document_detail/36063.html operation to query the region ID",
      "help_zh": "VPN网关实例所在的地域ID。\n\n您可以通过调用https://help.aliyun.com/document_detail/36063.html接口获取地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "d7d24a21-f4ba-4454-9173-b3828dae****",
      "help_en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The client token can contain only ASCII characters.\n\nIf you do not specify this parameter, the system automatically uses the RequestId of the API request as the ClientToken. The RequestId may be different for each API request",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId不一样",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "vpn-bp1a3kqjiiq9legfx****",
      "help_en": "The instance ID of the VPN gateway",
      "help_zh": "VPN网关实例ID",
      "location": "query",
      "name": "vpn_gateway_id",
      "options": [
        "--vpn-gateway-id"
      ],
      "raw_name": "VpnGatewayId",
      "required": true,
      "type": "string"
    },
    {
      "example": "10.0.0.0/24",
      "help_en": "The destination CIDR block of the destination route",
      "help_zh": "目的路由的目标网段",
      "location": "query",
      "name": "route_dest",
      "options": [
        "--route-dest"
      ],
      "raw_name": "RouteDest",
      "required": true,
      "type": "string"
    },
    {
      "example": "vco-bp15oes1py4i66rmd****",
      "help_en": "The next hop of the destination route",
      "help_zh": "目的路由的下一跳",
      "location": "query",
      "name": "next_hop",
      "options": [
        "--next-hop"
      ],
      "raw_name": "NextHop",
      "required": true,
      "type": "string"
    },
    {
      "example": "0",
      "help_en": "The weight of the destination route.\n\nIf you use the same VPN gateway instance to establish active/standby IPsec-VPN connections, you can specify the active and standby links by configuring the weight of the destination routing. A destination route with a weight of 100 is the active link by default, and a destination route with a weight of 0 is the standby link by default.\n\nYou can configure health checks for the IPsec-VPN connection to automatically detect the connectivity of the link. If the active link is unavailable, the system automatically switches traffic to the standby link, which ensures high availability of the cloud connection. For more information, see https://help.aliyun.com/document_detail/120391.html.\n\n100: The IPsec-VPN connection associated with the destination route serves as the active link.\n0: The IPsec-VPN connection associated with the destination route serves as the standby link.\n\nWhen you specify active and standby links, the active and standby destination routes must have the same destination CIDR block, different next hops, and different weights.\nFor VPN gateway instances that support dual-tunnel pattern IPsec-VPN connections, you do not need to configure this parameter. A dual-tunnel pattern IPsec-VPN connection contains two tunnels that automatically form active/standby links. You do not need to specify active/standby links by using the parameter settings. If you configure this parameter, the parameter settings do not take effect",
      "help_zh": "目的路由的权重值。\n\n在您使用同一个VPN网关实例搭建主备IPsec-VPN连接的场景中，您可以通过配置目的路由的权重值来指定主链路和备链路，权重值为100的目的路由默认为主链路，权重值为0的目的路由默认为备链路。\n\n您可以通过为IPsec连接配置健康检查来自动探测链路的连通性，在主链路不通的情况下，系统自动将流量切换至备链路进行传输，实现上云链路的高可用。具体操作，请参见https://help.aliyun.com/document_detail/120391.html。\n\n100：表示当前目的路由关联的IPsec连接为主链路。\n0：表示当前目的路由关联的IPsec连接为备链路。\n\n指定主备链路时，主目的路由和备目的路由的目标网段需相同，下一跳需不同，权重值也需不同。\n对于支持双隧道模式IPsec-VPN连接的VPN网关实例，无需配置该参数。一个双隧道模式IPsec-VPN连接包含两条隧道，两条隧道自动形成主备链路，无需再通过配置该参数指定主备链路。如果您配置了该参数，则该参数配置后不生效",
      "location": "query",
      "name": "weight",
      "options": [
        "--weight"
      ],
      "raw_name": "Weight",
      "required": true,
      "type": "int"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to publish the destination route to the VPC route table. Valid values:\n\ntrue: Publishes the destination route to the VPC route table. The system publishes the route only to the VPC system route table, not to VPC custom route tables.\n\n  If you want the VPC custom route table to contain this route, manually add the route. For more information, see https://help.aliyun.com/document_detail/448722.html.\nfalse: Does not publish the destination route to the VPC route table.\n\n  You must manually add a destination route whose next hop points to the VPN gateway instance in both the VPC system route table and custom route tables. Otherwise, the VPC cannot access resources in the destination CIDR block through the IPsec-VPN connection",
      "help_zh": "是否发布目的路由到VPC中。取值：\n\ntrue：发布目的路由到VPC中。系统仅会将路由发布至VPC系统路由表中，而不会发布至VPC自定义路由表中。\n\n  如果VPC自定义路由表中需要包含该路由，请您手动添加。具体操作，请参见https://help.aliyun.com/document_detail/448722.html。\nfalse：不发布目的路由到VPC中。\n\n  您需要在VPC系统路由表和自定义路由表中手动添加下一跳指向VPN网关实例的目的路由，否则VPC将无法通过IPsec-VPN连接访问该网段下的资源",
      "location": "query",
      "name": "publish_vpc",
      "options": [
        "--publish-vpc"
      ],
      "raw_name": "PublishVpc",
      "required": true,
      "type": "bool"
    },
    {
      "example": "mytest",
      "help_en": "The description of the destination route.\n\nThe description must be 1 to 100 characters in length and cannot start with `http://` or `https://`",
      "help_zh": "目的路由的描述信息。\n\n描述信息长度为1～100个字符，不能以`http://`或`https://`开头",
      "location": "query",
      "name": "description",
      "options": [
        "--description"
      ],
      "raw_name": "Description",
      "required": false,
      "type": "string"
    },
    {
      "example": "Ipsec",
      "help_en": "The tunneling protocol. Set the value to Ipsec (IPsec tunneling)",
      "help_zh": "隧道协议。仅取值：Ipsec（IPsec隧道协议）",
      "location": "query",
      "name": "overlay_mode",
      "options": [
        "--overlay-mode"
      ],
      "raw_name": "OverlayMode",
      "required": false,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to perform a dry run. Valid values:\ntrue: performs a dry run without creating the route. The system checks the required parameters, request syntax, and business restrictions. If the check fails, the corresponding error is returned. If the check passes, the error code `DryRunOperation` is returned.\nfalse (default): performs a dry run and sends the request. If the check passes, an HTTP 2xx status code is returned and the operation is performed",
      "help_zh": "是否只预检此次请求。取值：\ntrue：发送检查请求，不会创建路由。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\nfalse（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接进行操作",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Vpc",
  "protocol": "HTTP|HTTPS"
}
