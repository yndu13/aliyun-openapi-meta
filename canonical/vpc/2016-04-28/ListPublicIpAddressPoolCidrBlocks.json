{
  "camel_example": "",
  "cmd_full_name": "vpc list-public-ip-address-pool-cidr-blocks",
  "cmd_name": "list-public-ip-address-pool-cidr-blocks",
  "deprecated": false,
  "description_en": "Queries the CIDR block information in an IP address pool",
  "description_zh": "调用ListPublicIpAddressPoolCidrBlocks接口查询IP地址池中的IP地址网段信息",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ListPublicIpAddressPoolCidrBlocks",
  "operation": {
    "action": "ListPublicIpAddressPoolCidrBlocks",
    "api_style": "RPC",
    "api_version": "2016-04-28",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "pippool-6wetvn6fumkgycssx****",
      "help_en": "The instance ID of the IPAM pool",
      "help_zh": "IP地址池的实例ID",
      "location": "query",
      "name": "public_ip_address_pool_id",
      "options": [
        "--public-ip-address-pool-id"
      ],
      "raw_name": "PublicIpAddressPoolId",
      "required": true,
      "type": "string"
    },
    {
      "example": "47.0.XX.XX/24",
      "help_en": "The CIDR block",
      "help_zh": "IP地址网段",
      "location": "query",
      "name": "cidr_block",
      "options": [
        "--cidr-block"
      ],
      "raw_name": "CidrBlock",
      "required": false,
      "type": "string"
    },
    {
      "example": "10",
      "help_en": "The maximum number of entries to return. Valid values: 10 to 100. Default value: 10",
      "help_zh": "本次读取的最大数据记录数量，取值范围：10~100，默认为10",
      "location": "query",
      "name": "max_results",
      "options": [
        "--max-results"
      ],
      "raw_name": "MaxResults",
      "required": false,
      "type": "int"
    },
    {
      "example": "FFmyTO70tTpLG6I3FmYAXGKPd****",
      "help_en": "The pagination token. Valid values:\nYou do not need to specify this parameter for the first request or if no subsequent query exists.\nIf a subsequent query exists, set the value to the NextToken value returned in the previous API call",
      "help_zh": "是否拥有下一次查询的令牌（Token）。取值：\n第一次查询和没有下一次查询时，均无需填写。\n如果有下一次查询，取值为上一次API调用返回的NextToken值",
      "location": "query",
      "name": "next_token",
      "options": [
        "--next-token"
      ],
      "raw_name": "NextToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to perform a dry run. Valid values:\ntrue: performs a dry run without querying the CIDR block information in the IP address pool. The system checks the required parameters, request format, and service limits. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\nfalse (default): performs a dry run and sends the request. If the request passes the dry run, an HTTP 2xx status code is returned and the operation is performed",
      "help_zh": "是否只预检此次请求。取值：\ntrue：发送检查请求，不会查询IP地址池中的IP地址网段信息。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\nfalse（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接进行操作",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    },
    {
      "example": "cn-chengdu",
      "help_en": "The region ID of the CIDR block that you want to query",
      "help_zh": "要查询的IP地址网段所在的地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Vpc",
  "protocol": "HTTP|HTTPS"
}
