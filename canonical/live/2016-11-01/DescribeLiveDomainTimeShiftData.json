{
  "camel_example": "",
  "cmd_full_name": "live describe-live-domain-time-shift-data",
  "cmd_name": "describe-live-domain-time-shift-data",
  "deprecated": false,
  "description_en": "Queries time shifting usage data for live streaming",
  "description_zh": "查询直播时移用量数据",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeLiveDomainTimeShiftData",
  "operation": {
    "action": "DescribeLiveDomainTimeShiftData",
    "api_style": "RPC",
    "api_version": "2016-11-01",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-shanghai",
      "help_en": "The region ID",
      "help_zh": "地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": false,
      "type": "string"
    },
    {
      "example": "example.com",
      "help_en": "The streaming domain to query.\n\n\nWhen you specify DomainName, make sure that the specified domain is a live streaming domain and that the user calling this operation has permissions on the specified domain.\nYou can specify a single domain or multiple domains. Separate multiple domains with commas (,).\nIf this parameter is left empty, the merged data of all live streaming domains is returned by default",
      "help_zh": "需要查询的主播流域名。\n\n\n传入DomainName时，请保证传入域名为直播域名，同时调用本接口的用户拥有传入域名的操作权限。\n支持单个或批量域名查询。批量查询时多个域名用英文逗号（,）分隔。\n若参数为空，默认返回所有直播域名合并后的数据",
      "location": "query",
      "name": "domain_name",
      "options": [
        "--domain-name"
      ],
      "raw_name": "DomainName",
      "required": false,
      "type": "string"
    },
    {
      "example": "2021-03-03T00:00:00Z",
      "help_en": "The start time. Specify the time in the <i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z format in UTC.\n\nThe minimum data granularity is 5 minutes.\nIf you do not specify this parameter, data of the last 24 hours is returned by default",
      "help_zh": "起始时间。格式为：<i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z（UTC时间）。\n\n最小数据粒度为5分钟。\n不写默认读取过去24小时数据",
      "location": "query",
      "name": "start_time",
      "options": [
        "--start-time"
      ],
      "raw_name": "StartTime",
      "required": false,
      "type": "string"
    },
    {
      "example": "2021-03-03T02:00:00Z",
      "help_en": "The end time. The end time must be later than the start time. Specify the time in the <i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z format in UTC",
      "help_zh": "结束时间。结束时间需大于起始时间，格式为：<i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z（UTC时间）",
      "location": "query",
      "name": "end_time",
      "options": [
        "--end-time"
      ],
      "raw_name": "EndTime",
      "required": false,
      "type": "string"
    },
    {
      "example": "300",
      "help_en": "The time granularity of the queried data. Unit: seconds. Valid values:\n\n300.\n3600.\n86400.\n\nIf you do not specify this parameter or specify an unsupported value, the default value 300 is used",
      "help_zh": "查询数据的时间粒度。单位：秒。取值：\n\n300。\n3600。\n86400。\n\n不传或传值不支持时，使用默认值300秒",
      "location": "query",
      "name": "interval",
      "options": [
        "--interval"
      ],
      "raw_name": "Interval",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "live",
  "protocol": "HTTP|HTTPS"
}
