{
  "camel_example": "",
  "cmd_full_name": "sas get-security-score-rule",
  "cmd_name": "get-security-score-rule",
  "deprecated": false,
  "description_en": "Queries the details of custom security scoring rules",
  "description_zh": "查询自定义安全评分规则详情",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "GetSecurityScoreRule",
  "operation": {
    "action": "GetSecurityScoreRule",
    "api_style": "RPC",
    "api_version": "2018-12-03",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "zh",
      "help_en": "The language type for the request and response messages. Default value: zh. Valid values:\nzh: Chinese\nen: English",
      "help_zh": "设置请求和接收消息的语言类型，默认为zh。取值：\nzh：中文\nen：英文",
      "location": "query",
      "name": "lang",
      "options": [
        "--lang"
      ],
      "raw_name": "Lang",
      "required": false,
      "type": "string"
    },
    {
      "example": "home_security_score",
      "help_en": "Specifies whether to query the new or legacy security score rules. If the value is home_security_score, the new security score rules are queried. Otherwise, the legacy security score rules are queried by default",
      "help_zh": "选择查询新版安全分或旧版安全分规则，当取值为home_security_score时，查询新版安全分规则，否则默认查询旧版安全分规则",
      "location": "query",
      "name": "cal_type",
      "options": [
        "--cal-type"
      ],
      "raw_name": "CalType",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The ResourceDirectoryAccountId",
      "help_zh": "The ResourceDirectoryAccountId",
      "location": "query",
      "name": "resource_directory_account_id",
      "options": [
        "--resource-directory-account-id"
      ],
      "raw_name": "ResourceDirectoryAccountId",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "Sas",
  "protocol": "HTTP|HTTPS",
  "v1_parameters": [
    {
      "help_en": "Specifies whether to query the new or legacy security score rules. If the value is home_security_score, the new security score rules are queried. Otherwise, the legacy security score rules are queried by default",
      "help_zh": "选择查询新版安全分或旧版安全分规则，当取值为home_security_score时，查询新版安全分规则，否则默认查询旧版安全分规则",
      "name": "CalType",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The language type for the request and response messages. Default value: zh. Valid values:\nzh: Chinese\nen: English",
      "help_zh": "设置请求和接收消息的语言类型，默认为zh。取值：\nzh：中文\nen：英文",
      "name": "Lang",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "name": "ResourceDirectoryAccountId",
      "position": "query",
      "required": false,
      "type": "int"
    }
  ]
}
