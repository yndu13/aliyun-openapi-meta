{
  "camel_example": "",
  "cmd_full_name": "sas verify-check-result",
  "cmd_name": "verify-check-result",
  "deprecated": false,
  "description_en": "Validates check items",
  "description_zh": "检查项维度验证",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "VerifyCheckResult",
  "operation": {
    "action": "VerifyCheckResult",
    "api_style": "RPC",
    "api_version": "2018-12-03",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "element": {
        "type": "int"
      },
      "help_en": "The list of check item IDs",
      "help_zh": "检查项ID列表",
      "location": "query",
      "name": "check_ids",
      "options": [
        "--check-ids"
      ],
      "param_style": "repeatList",
      "raw_name": "CheckIds",
      "required": false,
      "type": "array"
    },
    {
      "example": "YAO_CHI",
      "help_en": "The task source. Valid values:\n\nYAO_CHI: ApsaraDB console",
      "help_zh": "任务来源。取值：\n\nYAO_CHI：瑶池控制台",
      "location": "query",
      "name": "task_source",
      "options": [
        "--task-source"
      ],
      "raw_name": "TaskSource",
      "required": false,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The list of instance IDs of the assets associated with the check items",
      "help_zh": "检查项资产的实例ID列表",
      "location": "query",
      "name": "instance_ids",
      "options": [
        "--instance-ids"
      ],
      "param_style": "repeatList",
      "raw_name": "InstanceIds",
      "required": false,
      "type": "array"
    },
    {
      "help_en": "Specifies whether to forcibly run the specified check items. Default value: false.\n\ntrue: Forcibly runs the specified check items. Forced execution bypasses frequency and quantity limits and initiates the check directly, which may cause duplicate checks to run multiple times within a short period.\nfalse (default): Does not forcibly run the specified check items. This ensures that the same check item is executed only once within a short period",
      "help_zh": "是否强制执行指定检查项，默认为false。\n\ntrue：强制执行指定检查项。强制执行相当于检查项不受频率、数量限制，直接发起，会导致短时间内多次检查重复执行。\nfalse（默认值）：不强制执行指定检查项，可以控制短时间内相同检查项只执行一次检查",
      "location": "query",
      "name": "force",
      "options": [
        "--biz-force"
      ],
      "raw_name": "Force",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Sas",
  "protocol": "HTTP|HTTPS"
}
