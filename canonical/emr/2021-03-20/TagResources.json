{
  "camel_example": "",
  "cmd_full_name": "emr tag-resources",
  "cmd_name": "tag-resources",
  "deprecated": false,
  "description_en": "Binds tags to a specified EMR cluster",
  "description_zh": "为指定的EMR集群绑定标签",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "TagResources",
  "operation": {
    "action": "TagResources",
    "api_style": "RPC",
    "api_version": "2021-03-20",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The ID of the region in which you want to create the instance",
      "help_zh": "区域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "cluster",
      "help_en": "The type of the resource to which the tag belongs. Valid values:\n\ncluster: cluster",
      "help_zh": "资源类型。 取值范围：\ncluster：集群",
      "location": "query",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "example": "cluster",
      "help_en": "The list of resource IDs. Valid values of N: 1 to 1",
      "help_zh": "资源ID列表。数组元素个数N的取值范围：1~1",
      "location": "query",
      "name": "resource_ids",
      "options": [
        "--resource-ids"
      ],
      "param_style": "flat",
      "raw_name": "ResourceIds",
      "required": true,
      "type": "array"
    },
    {
      "element": {
        "fields": [
          {
            "example": "department",
            "help_en": "The key of the tag. This parameter is required and cannot be an empty string. The key can be up to 128 characters in length. It cannot start with `aliyun` or `acs:` and cannot contain `http://` or `https://`",
            "help_zh": "标签键。必填参数，不允许为空字符串。最多支持128个字符，不能以```aliyun```和```acs:```开头，不能包含```http://```或 ```https://```",
            "name": "key",
            "options": [
              "--key"
            ],
            "raw_name": "Key",
            "required": true,
            "type": "string"
          },
          {
            "example": "IT",
            "help_en": "The value of the tag This parameter is optional and can be an empty string. The value can be up to 128 characters in length. It cannot start with `acs:` and cannot contain `http://` or `https://`",
            "help_zh": "标签值。非必填，可以为空字符串。最多支持128个字符，不能以```acs:```开头，不能包含```http://```或者```https://```",
            "name": "value",
            "options": [
              "--value"
            ],
            "raw_name": "Value",
            "required": false,
            "type": "string"
          }
        ],
        "type": "object"
      },
      "example": "[null]",
      "help_en": "The tags",
      "help_zh": "绑定的标签列表",
      "location": "query",
      "name": "tags",
      "options": [
        "--tags"
      ],
      "param_style": "flat",
      "raw_name": "Tags",
      "required": true,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "Emr",
  "protocol": "HTTP|HTTPS"
}
