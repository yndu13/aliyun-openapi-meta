{
  "camel_example": "",
  "cmd_full_name": "pvtz describe-resolver-rules",
  "cmd_name": "describe-resolver-rules",
  "deprecated": false,
  "description_en": "Describes one or more forwarding rules",
  "description_zh": "获取转发规则列表",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeResolverRules",
  "operation": {
    "action": "DescribeResolverRules",
    "api_style": "RPC",
    "api_version": "2018-01-01",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "list",
    "url": ""
  },
  "parameters": [
    {
      "example": "test",
      "help_en": "The keyword for the name of the forwarding rule. The search is case-insensitive and supports fuzzy match",
      "help_zh": "”转发规则名称“关键字。支持模糊查询，不区分大小写",
      "location": "query",
      "name": "keyword",
      "options": [
        "--keyword"
      ],
      "raw_name": "Keyword",
      "required": false,
      "type": "string"
    },
    {
      "example": "en",
      "help_en": "The language of the response. Valid values:\n\nzh: Chinese.\n\nen: English.\n\nDefault value: en",
      "help_zh": "返回值语言，取值：\n\nzh：中文。\n\nen：英文。\n\n默认值：en",
      "location": "query",
      "name": "lang",
      "options": [
        "--lang"
      ],
      "raw_name": "Lang",
      "required": false,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The number of the page to return. The start value is 1. Default value: 1",
      "help_zh": "分页查询时设置查询的页码，起始值为 1，默认为 1",
      "location": "query",
      "maximum": "9999999",
      "minimum": "1",
      "name": "page_number",
      "options": [
        "--page-number"
      ],
      "raw_name": "PageNumber",
      "required": false,
      "type": "int"
    },
    {
      "example": "20",
      "help_en": "The number of entries to return on each page. Maximum value: 100. Default value: 20",
      "help_zh": "分页查询时设置的每页数据量，最大值 100，默认为 20",
      "location": "query",
      "maximum": "100",
      "minimum": "1",
      "name": "page_size",
      "options": [
        "--page-size"
      ],
      "raw_name": "PageSize",
      "required": false,
      "type": "int"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to return the list of virtual private clouds (VPCs) that are associated with the forwarding rule. Valid values:\n\ntrue: returns the list.\n\nfalse: does not return the list.\n\nDefault value: false",
      "help_zh": "是否需要额外信息（转发规则关联的VPC列表）。取值：\n\ntrue：需要。\nfalse：不需要。\n\n默认值：false",
      "location": "query",
      "name": "need_detail_attributes",
      "options": [
        "--need-detail-attributes"
      ],
      "raw_name": "NeedDetailAttributes",
      "required": false,
      "type": "bool"
    },
    {
      "example": "hr****",
      "help_en": "The ID of the outbound endpoint",
      "help_zh": "出站终端节点ID",
      "location": "query",
      "name": "endpoint_id",
      "options": [
        "--endpoint-id"
      ],
      "raw_name": "EndpointId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "pvtz",
  "protocol": "HTTP|HTTPS"
}
