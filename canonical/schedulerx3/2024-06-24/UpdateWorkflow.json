{
  "camel_example": "",
  "cmd_full_name": "schedulerx3 update-workflow",
  "cmd_name": "update-workflow",
  "deprecated": false,
  "description_en": "Updates an existing workflow",
  "description_zh": "更新工作流",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "UpdateWorkflow",
  "operation": {
    "action": "UpdateWorkflow",
    "api_style": "RPC",
    "api_version": "2024-06-24",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "xxljob-d6a5243b6fa",
      "help_en": "The cluster ID",
      "help_zh": "集群ID",
      "location": "formData",
      "name": "cluster_id",
      "options": [
        "--cluster-id"
      ],
      "raw_name": "ClusterId",
      "required": true,
      "type": "string"
    },
    {
      "docRequired": true,
      "example": "test-app",
      "help_en": "The application name",
      "help_zh": "应用名称",
      "location": "formData",
      "maxLength": "64",
      "minLength": "1",
      "name": "app_name",
      "options": [
        "--app-name"
      ],
      "pattern": "^[0-9a-zA-Z_-]+$",
      "raw_name": "AppName",
      "required": true,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The workflow ID",
      "help_zh": "工作流ID",
      "location": "formData",
      "name": "workflow_id",
      "options": [
        "--workflow-id"
      ],
      "raw_name": "WorkflowId",
      "required": true,
      "type": "int"
    },
    {
      "example": "myWorkflow",
      "help_en": "The workflow name",
      "help_zh": "工作流名称",
      "location": "formData",
      "maxLength": "32",
      "minLength": "6",
      "name": "name",
      "options": [
        "--name"
      ],
      "pattern": "^([^\\x00-\\xff]|[\\w.,;/@\\n-])+$",
      "raw_name": "Name",
      "required": false,
      "type": "string"
    },
    {
      "example": "my first workflow",
      "help_en": "The workflow description",
      "help_zh": "工作流描述",
      "location": "formData",
      "maxLength": "64",
      "name": "description",
      "options": [
        "--description"
      ],
      "raw_name": "Description",
      "required": false,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The scheduling type. Valid values:\n\n`-1` (none): The workflow is not scheduled and must be triggered on demand.\n\n`1` (cron): The workflow runs based on the cron expression in the `TimeExpression` parameter.\n\n`100` (api): The workflow is triggered by an API call",
      "help_zh": "调度类型，目前支持以下几种类型：\n-1: none\n1：cron\n100:：api",
      "location": "formData",
      "name": "time_type",
      "options": [
        "--time-type"
      ],
      "raw_name": "TimeType",
      "required": false,
      "type": "int"
    },
    {
      "example": "0 0/10 * * * ?",
      "help_en": "The cron expression for the schedule. This parameter is required only when `TimeType` is set to `1` (cron).\n\nIf `TimeType` is `-1` (none), this parameter is not required.\n\nIf `TimeType` is `1` (cron), specify a standard cron expression.\n\nIf `TimeType` is `100` (api), this parameter is not required",
      "help_zh": "时间表达式，根据选择的时间类型设置时间表达式。\nnone：无需填写\ncron：填写标准的cron表达式，支持在线验证。\napi：无需填写",
      "location": "formData",
      "name": "time_expression",
      "options": [
        "--time-expression"
      ],
      "raw_name": "TimeExpression",
      "required": false,
      "type": "string"
    },
    {
      "example": "GMT+8",
      "help_en": "The time zone for the schedule.\n\nDefaults to the time zone of the SchedulerX server",
      "help_zh": "时区\n默认使用schedulerx服务器所在时区",
      "location": "formData",
      "name": "timezone",
      "options": [
        "--timezone"
      ],
      "raw_name": "Timezone",
      "required": false,
      "type": "string"
    },
    {
      "example": "workday",
      "help_en": "The name of a custom calendar to exclude specific dates from the schedule",
      "help_zh": "自定义日历",
      "location": "formData",
      "name": "calendar",
      "options": [
        "--calendar"
      ],
      "raw_name": "Calendar",
      "required": false,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The maximum concurrency for the workflow.\n\nThe maximum number of concurrent instances that can run for the same workflow. A value of `1` prevents overlapping runs. If the number of running instances reaches this limit, subsequent scheduled runs are skipped",
      "help_zh": "最大并发数\n同一个工作流同一时间允许运行的最大实例个数，1表示不允许重复执行。如果超过并发数，会跳过当前调度",
      "location": "formData",
      "name": "max_concurrency",
      "options": [
        "--max-concurrency"
      ],
      "raw_name": "MaxConcurrency",
      "required": false,
      "type": "int"
    },
    {
      "example": "D0DE9C33-992A-580B-89C4-B609A292748D",
      "help_en": "A unique token that you provide to ensure the request is idempotent. The token can be up to 64 ASCII characters long",
      "help_zh": "用于保证请求的幂等性。由客户端生成该参数值，要保证在不同请求间唯一，最大不超过64个ASCII字符",
      "location": "formData",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "SchedulerX3",
  "protocol": "HTTPS"
}
