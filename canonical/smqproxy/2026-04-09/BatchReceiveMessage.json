{
  "camel_example": "",
  "cmd_full_name": "smqproxy batch-receive-message",
  "cmd_name": "batch-receive-message",
  "deprecated": false,
  "description_en": "Batch receives up to 16 messages from a specified queue for consumption",
  "description_zh": "从指定队列中批量获取最多16条消息进行消费",
  "kebab_example": "",
  "method": "GET",
  "multi_version": false,
  "name": "BatchReceiveMessage",
  "operation": {
    "action": "BatchReceiveMessage",
    "api_style": "ROA",
    "api_version": "2026-04-09",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": "/queues/{queueName}/messages"
  },
  "parameters": [
    {
      "example": "MyQueue",
      "help_en": "The queue name",
      "help_zh": "队列名称",
      "location": "path",
      "name": "queue_name",
      "options": [
        "--queue-name"
      ],
      "raw_name": "queueName",
      "required": true,
      "type": "string"
    },
    {
      "example": "10",
      "help_en": "The maximum number of messages to receive in this request",
      "help_zh": "本次最多获取的消息条数",
      "location": "query",
      "name": "num_of_messages",
      "options": [
        "--num-of-messages"
      ],
      "raw_name": "numOfMessages",
      "required": false,
      "type": "int"
    },
    {
      "example": "0",
      "help_en": "The maximum polling wait time for this request, in seconds. Set to 0 for an immediate return",
      "help_zh": "本次请求最长的 Polling 等待时间（秒）。设为 0 表示立即返回",
      "location": "query",
      "name": "waitseconds",
      "options": [
        "--waitseconds"
      ],
      "raw_name": "waitseconds",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "/queues/[queueName]/messages",
  "product_code": "SMQProxy",
  "protocol": "HTTPS",
  "v1_parameters": [
    {
      "help_en": "The maximum number of messages to receive in this request",
      "help_zh": "本次最多获取的消息条数",
      "name": "numOfMessages",
      "position": "query",
      "required": false,
      "type": "int"
    },
    {
      "help_en": "The queue name",
      "help_zh": "队列名称",
      "name": "queueName",
      "position": "path",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The maximum polling wait time for this request, in seconds. Set to 0 for an immediate return",
      "help_zh": "本次请求最长的 Polling 等待时间（秒）。设为 0 表示立即返回",
      "name": "waitseconds",
      "position": "query",
      "required": false,
      "type": "int"
    }
  ]
}
