{
  "camel_example": "",
  "cmd_full_name": "eventbridge query-event-house",
  "cmd_name": "query-event-house",
  "deprecated": false,
  "description_en": "Query event store data",
  "description_zh": "查询事件仓数据",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "QueryEventHouse",
  "operation": {
    "action": "QueryEventHouse",
    "api_style": "RPC",
    "api_version": "2020-04-01",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "list",
    "url": ""
  },
  "parameters": [
    {
      "example": "SELECT * FROM \"test-es\".\"default\".\"product_info\"",
      "help_en": "The query statement. Typically uses a three-part table name in the format catalog.namespace.table",
      "help_zh": "查询语句，通常使用 catalog.namespace.table 三段式表名",
      "location": "query",
      "name": "query",
      "options": [
        "--query"
      ],
      "raw_name": "Query",
      "required": true,
      "type": "string"
    },
    {
      "example": "10",
      "help_en": "The maximum number of rows to return. If a LIMIT clause is already specified in the SQL statement, the SQL's own LIMIT takes precedence",
      "help_zh": "返回行数上限；如果 SQL 里已经写了 LIMIT，以 SQL 自身为准",
      "location": "query",
      "name": "limit",
      "options": [
        "--limit"
      ],
      "raw_name": "Limit",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "eventbridge",
  "protocol": "HTTPS"
}
