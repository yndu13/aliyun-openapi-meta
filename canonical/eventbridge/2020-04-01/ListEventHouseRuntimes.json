{
  "camel_example": "",
  "cmd_full_name": "eventbridge list-event-house-runtimes",
  "cmd_name": "list-event-house-runtimes",
  "deprecated": false,
  "description_en": "Queries the list of EventHouse runtimes",
  "description_zh": "查询 EventHouse Runtime 列表",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "ListEventHouseRuntimes",
  "operation": {
    "action": "ListEventHouseRuntimes",
    "api_style": "RPC",
    "api_version": "2020-04-01",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "list",
    "url": ""
  },
  "parameters": [
    {
      "example": "20",
      "help_en": "The maximum number of runtimes to return per page. If this parameter is not specified or set to 0, the default value 20 is used. Maximum value: 100",
      "help_zh": "每页最多返回的 Runtime 数量。未传或传 0 时使用默认值 20，最大值为 100",
      "location": "query",
      "name": "max_results",
      "options": [
        "--max-results"
      ],
      "raw_name": "MaxResults",
      "required": false,
      "type": "int"
    },
    {
      "help_en": "Not required for the first query. For subsequent queries, use the NextToken returned in the previous response. An empty value indicates that no more pages are available",
      "help_zh": "首次查询无需传入。后续查询使用上一次响应返回的 NextToken；为空表示没有下一页",
      "location": "query",
      "name": "next_token",
      "options": [
        "--next-token"
      ],
      "raw_name": "NextToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "eventbridge",
  "protocol": "HTTPS"
}
