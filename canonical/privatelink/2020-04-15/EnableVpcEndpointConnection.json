{
  "camel_example": "",
  "cmd_full_name": "privatelink enable-vpc-endpoint-connection",
  "cmd_name": "enable-vpc-endpoint-connection",
  "deprecated": false,
  "description_en": "Calls the EnableVpcEndpointConnection operation, and the endpoint service accepts the connection request from the endpoint",
  "description_zh": "调用EnableVpcEndpointConnection接口，终端节点服务接受终端节点的连接请求",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "EnableVpcEndpointConnection",
  "operation": {
    "action": "EnableVpcEndpointConnection",
    "api_style": "RPC",
    "api_version": "2020-04-15",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-huhehaote",
      "help_en": "The region ID of the endpoint connection to be accepted.\n\nYou can call the https://help.aliyun.com/document_detail/120468.html operation to query the region IDs. ",
      "help_zh": "要接受的终端节点连接的地域ID。\n\n您可以通过调用https://help.aliyun.com/document_detail/120468.html接口获取地域ID。 ",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "0c593ea1-3bea-11e9-b96b-88e9fe637760",
      "help_en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the value, but you must ensure that it is unique among different requests. ClientToken can contain only ASCII characters",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to perform only a dry run, without performing the actual request. Valid values:\n\ntrue: performs only a dry run. The system checks the request for potential issues, including missing required parameters, the request format, and service limits. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n\nfalse (default): performs a dry run and performs the actual request. If the request passes the dry run, an HTTP 2xx status code is returned and the connection request is accepted",
      "help_zh": "是否只预检此次请求。取值：\n\ntrue：发送检查请求，不会接受连接请求。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，会返回错误码`DryRunOperation`。\n\nfalse（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接接受连接请求",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    },
    {
      "example": "epsrv-hp3vpx8yqxblby3i****",
      "help_en": "The endpoint service ID",
      "help_zh": "终端节点服务ID",
      "location": "query",
      "name": "service_id",
      "options": [
        "--service-id"
      ],
      "raw_name": "ServiceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "ep-hp33b2e43fays7s8****",
      "help_en": "The endpoint ID",
      "help_zh": "终端节点ID",
      "location": "query",
      "name": "endpoint_id",
      "options": [
        "--endpoint-id"
      ],
      "raw_name": "EndpointId",
      "required": true,
      "type": "string"
    },
    {
      "example": "3072",
      "help_en": "The bandwidth of the endpoint connection. Unit: Mbit/s.\n\nThe valid values vary depending on the service resource type:\nNLB: 100 to 50000.\nALB: 100 to 25000.\nCLB: 100 to 10240. Default value: 3072.\nGWLB: 100 to 25000",
      "help_zh": "终端节点的连接带宽。单位：Mbps。\n\n对于不同的服务资源资源类型，取值范围分别如下：\nNLB：100~50000。\nALB：100~25000。\nCLB：100~10240。默认值 3072。\nGWLB：100～25000",
      "location": "query",
      "name": "bandwidth",
      "options": [
        "--bandwidth"
      ],
      "raw_name": "Bandwidth",
      "required": false,
      "type": "int"
    },
    {
      "example": "BandwidthLimit",
      "help_en": "Scalability: automatic scaling. In this mode, the connection bandwidth configured for the endpoint connection does not take effect.\n\nBandwidthLimit: supports setting a bandwidth upper limit for the endpoint connection. The bandwidth upper limit is the value of Bandwidth.\n \nWhen the service resource is NLB, TrafficControlMode is set to Scalability by default. You can set it to BandwidthLimit and modify the value of Bandwidth to provide a bandwidth upper limit.\nWhen the service resource is CLB, TrafficControlMode can only be set to BandwidthLimit, which indicates that the service provider provides default bandwidth throttling for each endpoint.\nWhen the service resource is GWLB, TrafficControlMode can only be set to Scalability",
      "help_zh": "Scalability：自动弹性，在该模式下，针对终端连接设置的连接带宽无效。\n\nBandwidthLimit：支持对终端连接设置带宽上限，带宽上限为Bandwidth值。\n \n当服务资源为NLB时，TrafficControlMode默认设置为Scalability，可以设置为BandwidthLimit并修改Bandwidth值来提供带宽上限。\n当服务资源为CLB时，TrafficControlMode仅支持设置BandwidthLimit，表示服务使用方为每个终端节点提供默认限速。\n当服务资源为GWLB时，TrafficControlMode仅支持设置Scalability",
      "location": "query",
      "name": "traffic_control_mode",
      "options": [
        "--traffic-control-mode"
      ],
      "raw_name": "TrafficControlMode",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Privatelink",
  "protocol": "HTTPS",
  "v1_parameters": [
    {
      "help_en": "The bandwidth of the endpoint connection. Unit: Mbit/s.\n\nThe valid values vary depending on the service resource type:\nNLB: 100 to 50000.\nALB: 100 to 25000.\nCLB: 100 to 10240. Default value: 3072.\nGWLB: 100 to 25000",
      "help_zh": "终端节点的连接带宽。单位：Mbps。\n\n对于不同的服务资源资源类型，取值范围分别如下：\nNLB：100~50000。\nALB：100~25000。\nCLB：100~10240。默认值 3072。\nGWLB：100～25000",
      "name": "Bandwidth",
      "position": "query",
      "required": false,
      "type": "int"
    },
    {
      "help_en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the value, but you must ensure that it is unique among different requests. ClientToken can contain only ASCII characters",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符",
      "name": "ClientToken",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "Specifies whether to perform only a dry run, without performing the actual request. Valid values:\n\ntrue: performs only a dry run. The system checks the request for potential issues, including missing required parameters, the request format, and service limits. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n\nfalse (default): performs a dry run and performs the actual request. If the request passes the dry run, an HTTP 2xx status code is returned and the connection request is accepted",
      "help_zh": "是否只预检此次请求。取值：\n\ntrue：发送检查请求，不会接受连接请求。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，会返回错误码`DryRunOperation`。\n\nfalse（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接接受连接请求",
      "name": "DryRun",
      "position": "query",
      "required": false,
      "type": "bool"
    },
    {
      "help_en": "The endpoint ID",
      "help_zh": "终端节点ID",
      "name": "EndpointId",
      "position": "query",
      "required": true,
      "type": "string"
    },
    {
      "help_en": "The region ID of the endpoint connection to be accepted.\n\nYou can call the https://help.aliyun.com/document_detail/120468.html operation to query the region IDs",
      "help_zh": "要接受的终端节点连接的地域ID。\n\n您可以通过调用https://help.aliyun.com/document_detail/120468.html接口获取地域ID",
      "name": "RegionId",
      "position": "query",
      "required": true,
      "type": "string"
    },
    {
      "help_en": "The endpoint service ID",
      "help_zh": "终端节点服务ID",
      "name": "ServiceId",
      "position": "query",
      "required": true,
      "type": "string"
    },
    {
      "help_en": "Scalability: automatic scaling. In this mode, the connection bandwidth configured for the endpoint connection does not take effect.\n\nBandwidthLimit: supports setting a bandwidth upper limit for the endpoint connection. The bandwidth upper limit is the value of Bandwidth.\n \nWhen the service resource is NLB, TrafficControlMode is set to Scalability by default. You can set it to BandwidthLimit and modify the value of Bandwidth to provide a bandwidth upper limit.\nWhen the service resource is CLB, TrafficControlMode can only be set to BandwidthLimit, which indicates that the service provider provides default bandwidth throttling for each endpoint.\nWhen the service resource is GWLB, TrafficControlMode can only be set to Scalability",
      "help_zh": "Scalability：自动弹性，在该模式下，针对终端连接设置的连接带宽无效。\n\nBandwidthLimit：支持对终端连接设置带宽上限，带宽上限为Bandwidth值。\n \n当服务资源为NLB时，TrafficControlMode默认设置为Scalability，可以设置为BandwidthLimit并修改Bandwidth值来提供带宽上限。\n当服务资源为CLB时，TrafficControlMode仅支持设置BandwidthLimit，表示服务使用方为每个终端节点提供默认限速。\n当服务资源为GWLB时，TrafficControlMode仅支持设置Scalability",
      "name": "TrafficControlMode",
      "position": "query",
      "required": false,
      "type": "string"
    }
  ]
}
