{
  "camel_example": "",
  "cmd_full_name": "paiautoml restart-hpo-trials",
  "cmd_name": "restart-hpo-trials",
  "deprecated": false,
  "description_en": "Restarts failed trials in an hyperparameter optimization (HPO) experiment. The system extracts the configuration of the hyperparameters of a specific trial. If none of the trials that use this configuration are successful in the experiment, the system restarts trials by using this configuration",
  "description_zh": "重启一个HPO实验里失败的trials。对于指定的trial，提取它使用的超参配置。如果该实验所有使用这个配置的trials都没有成功过，那么用这个配置重新启动一个trial",
  "kebab_example": "",
  "method": "PUT",
  "multi_version": false,
  "name": "RestartHpoTrials",
  "operation": {
    "action": "RestartHpoTrials",
    "api_style": "ROA",
    "api_version": "2022-08-28",
    "body_mapping": {
      "trial_hyper_parameters": "TrialHyperParameters",
      "trial_ids": "TrialIds"
    },
    "content_type": "application/json",
    "method": "PUT",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "json",
    "system_operation_type": "update",
    "url": "/api/automl/v1/hpo/experiment/{ExperimentId}/restart_trials"
  },
  "parameters": [
    {
      "example": "abcde",
      "help_en": "The ID of the experiment",
      "help_zh": "实验id",
      "location": "path",
      "name": "experiment_id",
      "options": [
        "--experiment-id"
      ],
      "raw_name": "ExperimentId",
      "required": true,
      "type": "string"
    },
    {
      "example": "‘{\"${batch_size}\": \"32\", \"${lr}\": \"0.01\"}'",
      "help_en": "A new hyperparameter configuration of a trial. The system starts a new trial by using this configuration",
      "help_zh": "一个新的trial的超参配置，实验会用这组超参启动一个新trial",
      "location": "body",
      "name": "trial_hyper_parameters",
      "options": [
        "--trial-hyper-parameters"
      ],
      "param_style": "json",
      "raw_name": "TrialHyperParameters",
      "required": false,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The IDs of the trials that you want to restart",
      "help_zh": "需要重新运行的、已经存在的trial ids",
      "location": "body",
      "name": "trial_ids",
      "options": [
        "--trial-ids"
      ],
      "param_style": "json",
      "raw_name": "TrialIds",
      "required": false,
      "type": "array"
    }
  ],
  "pathPattern": "/api/automl/v1/hpo/experiment/[ExperimentId]/restart_trials",
  "product_code": "paiAutoML",
  "protocol": "HTTP|HTTPS",
  "v1_body_parameters": [
    {
      "help_en": "The request body. You must specify one of the TrialIds and TrialHyperParameters parameters. If you leave both parameters empty, no operation is performed on the server. If you specify both parameters, the TrialIds parameter prevails",
      "help_zh": "请求体。TrialIds和TrialHyperParameters二者必须填写一个。如果都不提供则不会触发服务端任何操作。如果都填了，则只对TrialIds执行操作",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    }
  ],
  "v1_parameters": [
    {
      "help_en": "The ID of the experiment",
      "help_zh": "实验id",
      "name": "ExperimentId",
      "position": "path",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The request body. You must specify one of the TrialIds and TrialHyperParameters parameters. If you leave both parameters empty, no operation is performed on the server. If you specify both parameters, the TrialIds parameter prevails",
      "help_zh": "请求体。TrialIds和TrialHyperParameters二者必须填写一个。如果都不提供则不会触发服务端任何操作。如果都填了，则只对TrialIds执行操作",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    }
  ]
}
