{
  "camel_example": "",
  "cmd_full_name": "rtc stop-cloud-record",
  "cmd_name": "stop-cloud-record",
  "deprecated": false,
  "description_en": "StopCloudRecord",
  "description_zh": "本接口用于停止云端录制任务",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "StopCloudRecord",
  "operation": {
    "action": "StopCloudRecord",
    "api_style": "RPC",
    "api_version": "2018-01-11",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "eo85****",
      "help_en": "The application ID, created and queried via the console. Only a single ID is supported",
      "help_zh": "应用ID，通过控制台创建和查询，仅支持传单个ID",
      "location": "query",
      "name": "app_id",
      "options": [
        "--app-id"
      ],
      "raw_name": "AppId",
      "required": true,
      "type": "string"
    },
    {
      "example": "testid",
      "help_en": "The existing channel ID. Only a single ID is supported",
      "help_zh": "已存在的频道ID，仅支持传单个ID",
      "location": "query",
      "name": "channel_id",
      "options": [
        "--channel-id"
      ],
      "raw_name": "ChannelId",
      "required": true,
      "type": "string"
    },
    {
      "example": "123",
      "help_en": "The existing cloud recording job ID. Only a single ID is supported",
      "help_zh": "已存在的云端录制的任务ID，仅支持传单个ID",
      "location": "query",
      "max_length": "55",
      "min_length": "1",
      "name": "task_id",
      "options": [
        "--task-id"
      ],
      "pattern": "^[0-9a-zA-Z_-]+$",
      "raw_name": "TaskId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "rtc",
  "protocol": "HTTPS"
}
