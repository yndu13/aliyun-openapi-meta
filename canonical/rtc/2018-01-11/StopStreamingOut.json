{
  "camel_example": "",
  "cmd_full_name": "rtc stop-streaming-out",
  "cmd_name": "stop-streaming-out",
  "deprecated": false,
  "description_en": "StopStreamingOut",
  "description_zh": "本接口用于停止旁路推流任务",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "StopStreamingOut",
  "operation": {
    "action": "StopStreamingOut",
    "api_style": "RPC",
    "api_version": "2018-01-11",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "eo85****",
      "help_en": "The application ID, which is created and queried via the console. Only a single ID is supported.  ",
      "help_zh": "应用ID，通过控制台创建和查询，仅支持传单个ID",
      "location": "query",
      "name": "app_id",
      "options": [
        "--app-id"
      ],
      "raw_name": "AppId",
      "required": true,
      "type": "string"
    },
    {
      "example": "testid",
      "help_en": "An existing channel ID. Only a single ID is supported.  ",
      "help_zh": "已存在的频道ID，仅支持传单个ID",
      "location": "query",
      "name": "channel_id",
      "options": [
        "--channel-id"
      ],
      "raw_name": "ChannelId",
      "required": true,
      "type": "string"
    },
    {
      "example": "123",
      "help_en": "The ID of an existing bypass stream ingest job. Only a single ID is supported. It can contain uppercase and lowercase letters, digits, underscores, and hyphens (-), with a maximum length of 55 bytes. If not specified, the system will automatically generate one.  ",
      "help_zh": "已存在的旁路推流的任务ID，仅支持传单个ID。由大小写字母、数字、下划线、短划线（-）组成，最大55字节。不填，则由系统自动生成",
      "location": "query",
      "maxLength": "55",
      "minLength": "1",
      "name": "task_id",
      "options": [
        "--task-id"
      ],
      "pattern": "^[0-9a-zA-Z_-]+$",
      "raw_name": "TaskId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "rtc",
  "protocol": "HTTPS",
  "v1_parameters": [
    {
      "help_en": "The application ID, which is created and queried via the console. Only a single ID is supported",
      "help_zh": "应用ID，通过控制台创建和查询，仅支持传单个ID",
      "name": "AppId",
      "position": "query",
      "required": true,
      "type": "string"
    },
    {
      "help_en": "An existing channel ID. Only a single ID is supported",
      "help_zh": "已存在的频道ID，仅支持传单个ID",
      "name": "ChannelId",
      "position": "query",
      "required": true,
      "type": "string"
    },
    {
      "help_en": "The ID of an existing bypass stream ingest job. Only a single ID is supported. It can contain uppercase and lowercase letters, digits, underscores, and hyphens (-), with a maximum length of 55 bytes. If not specified, the system will automatically generate one",
      "help_zh": "已存在的旁路推流的任务ID，仅支持传单个ID。由大小写字母、数字、下划线、短划线（-）组成，最大55字节。不填，则由系统自动生成",
      "name": "TaskId",
      "position": "query",
      "required": true,
      "type": "string"
    }
  ]
}
