{
  "camel_example": "",
  "cmd_full_name": "gws describe-cluster-policy",
  "cmd_name": "describe-cluster-policy",
  "deprecated": false,
  "description_en": "Get cluster policy",
  "description_zh": "获取集群策略",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeClusterPolicy",
  "operation": {
    "action": "DescribeClusterPolicy",
    "api_style": "RPC",
    "api_version": "2019-06-18",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "t-xxx",
      "help_en": "This value is only valid when `AsyncMode` is `true`\n\nWhen not empty, used to obtain the execution status of a previously submitted query command",
      "help_zh": "此值只在 `AsyncMode` 为 `true` 时有效\n\n非空时用来获得之前查询命令的执行状态",
      "location": "query",
      "name": "task_id",
      "options": [
        "--task-id"
      ],
      "raw_name": "TaskId",
      "required": false,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Asynchronous mode invocation:\nfalse (default): The API returns only after the command execution is complete.\ntrue: The API returns immediately.\n    * When `TaskId` is empty: The query command is executed asynchronously in the background. The caller needs to use the TaskId from the return value to continue calling this API to obtain the query command result.\n    * When `TaskId` is not empty: Obtain the result of the query command",
      "help_zh": "异步模式调用：\nfalse（默认）：命令执行完成后，API才会返回。\ntrue：API 立即返回。\n    * 当 `TaskId` 为空：查询命令在后台异步执行。调用方需使用返回值中的TaskId，继续调用此API来获得查询命令的结果。\n    * 当 `TaskId` 不为空：获得查询命令的结果",
      "location": "query",
      "name": "async_mode",
      "options": [
        "--async-mode"
      ],
      "raw_name": "AsyncMode",
      "required": false,
      "type": "bool"
    },
    {
      "example": "gws-xxx",
      "help_en": "Cluster ID",
      "help_zh": "集群 id",
      "location": "query",
      "name": "cluster_id",
      "options": [
        "--cluster-id"
      ],
      "raw_name": "ClusterId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "gws",
  "protocol": "HTTP|HTTPS"
}
