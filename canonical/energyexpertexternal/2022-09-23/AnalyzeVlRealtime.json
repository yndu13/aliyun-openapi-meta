{
  "camel_example": "",
  "cmd_full_name": "energyexpertexternal analyze-vl-realtime",
  "cmd_name": "analyze-vl-realtime",
  "deprecated": false,
  "description_en": "Extracts information from a VL in real time",
  "description_zh": "实时VL信息抽取",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "AnalyzeVlRealtime",
  "operation": {
    "action": "AnalyzeVlRealtime",
    "api_style": "ROA",
    "api_version": "2022-09-23",
    "is_file_transfer_api": true,
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": "/api/v1/aidoc/document/analyzeVlRealtime"
  },
  "parameters": [
    {
      "example": "fileUrl: https://example.com/example.pdf\nfileUrlObject: FileInputStream generated from a local file",
      "help_en": "Choose either fileUrl or fileUrlObject.\n\nfileUrl: The URL of a single document. The document can have up to 1,000 pages and be up to 100 MB in size.\n\nfileUrlObject: The local file to upload when you call the API. The document can have up to 1,000 pages and be up to 100 MB in size.\n\nSupported formats:\n\nbmp, jpe, jpg, jpeg, png, tif, tiff, webp, heic",
      "help_zh": "fileUrl/fileUrlObject二选一：\n\nfileUrl：以文档URL方式使用，单个文档（支持1000页以内、100MB以内的文档）\n\nfileUrlObject：以本地文件上传方式调用接口时使用，单个文档（支持1000页以内、100 MB以内的文档）\n\n支持格式如下：\nbmp, jpe, jpg, jpeg, png, tif, tiff, webp, heic",
      "is_file_transfer_url": true,
      "location": "query",
      "name": "file_url",
      "options": [
        "--file-url"
      ],
      "raw_name": "fileUrl",
      "required": false,
      "type": "string"
    },
    {
      "example": "test.png",
      "help_en": "The filename must include the file extension",
      "help_zh": "文件名需带文件类型后缀",
      "location": "query",
      "name": "file_name",
      "options": [
        "--file-name"
      ],
      "raw_name": "fileName",
      "required": false,
      "type": "string"
    },
    {
      "example": "572d24k0c95a",
      "help_en": "The unique ID of the extraction template, which specifies the content to extract from the document. To obtain the ID, log on to the template management page and configure a template",
      "help_zh": "唯一的抽取模板ID，用于指定文档要抽取的内容，需登录模板管理页面配置模板后获取对应的模板ID",
      "location": "query",
      "name": "template_id",
      "options": [
        "--template-id"
      ],
      "raw_name": "templateId",
      "required": false,
      "type": "string"
    },
    {
      "example": "zh-CN",
      "help_en": "The unique ID of the extraction template, which specifies the content to extract from the document. To obtain the ID, log on to the template management page and configure a template",
      "help_zh": "语言，可传参数\nzh-CN：中文（默认）\nen-US：英语",
      "location": "query",
      "name": "language",
      "options": [
        "--biz-language"
      ],
      "raw_name": "language",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/api/v1/aidoc/document/analyzeVlRealtime",
  "product_code": "energyExpertExternal",
  "protocol": "HTTPS"
}
