{
  "camel_example": "",
  "cmd_full_name": "expressconnectrouter modify-express-connect-router",
  "cmd_name": "modify-express-connect-router",
  "deprecated": false,
  "description_en": "Modifies the properties of an Express Connect Router (ECR), such as its name",
  "description_zh": "修改专线网关ECR（Express Connect Router）的名称等属性",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "ModifyExpressConnectRouter",
  "operation": {
    "action": "ModifyExpressConnectRouter",
    "api_style": "RPC",
    "api_version": "2023-09-01",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "ecr-fu8rszhgv7623c****\n",
      "help_en": "The ID of ECR",
      "help_zh": "专线网关实例ID",
      "location": "formData",
      "name": "ecr_id",
      "options": [
        "--ecr-id"
      ],
      "raw_name": "EcrId",
      "required": true,
      "type": "string"
    },
    {
      "example": "test\n",
      "help_en": "The name of ECR.\n\nThe name can be empty or 0 to 128 characters in length. It cannot start with http\\:// or https\\://",
      "help_zh": "专线网关实例的名称。\n名称可以为空或长度为 0～128 个字符，不能以 http://或 https://开头",
      "location": "formData",
      "name": "name",
      "options": [
        "--name"
      ],
      "raw_name": "Name",
      "required": false,
      "type": "string"
    },
    {
      "example": "test\n",
      "help_en": "The description of ECR.\n\nThe description can be empty or 0 to 256 characters in length. It cannot start with http\\:// or https\\://",
      "help_zh": "专线网关实例的描述。\n描述可以为空或长度为 0～256 个字符，不能以 http://或 https://开头",
      "location": "formData",
      "name": "description",
      "options": [
        "--description"
      ],
      "raw_name": "Description",
      "required": false,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to perform a dry run. Valid values:\n\ntrue: Sends a check request without modifying the service resource.\nfalse (default): Sends a normal request",
      "help_zh": "是否只预检此次请求。取值：\n\ntrue：发送检查请求，不会修改服务资源。\n\nfalse（默认值）：发送正常请求",
      "location": "formData",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    },
    {
      "example": "02fb3da4-130e-11e9-8e44-00****\t\n",
      "help_en": "The client token that is used to ensure the idempotence of the request.\n\nGenerate a unique value for this parameter from your client. The client token can contain only ASCII characters.\n\nIf you do not specify this parameter, the system uses the RequestId as the ClientToken. The RequestId of each request is unique",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId不一样",
      "location": "formData",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "ExpressConnectRouter",
  "protocol": "HTTPS"
}
