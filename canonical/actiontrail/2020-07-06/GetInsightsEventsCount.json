{
  "camel_example": "",
  "cmd_full_name": "actiontrail get-insights-events-count",
  "cmd_name": "get-insights-events-count",
  "deprecated": false,
  "description_en": "Queries the number of Insights events for the current account",
  "description_zh": "获取当前账号的Insights事件数量",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "GetInsightsEventsCount",
  "operation": {
    "action": "GetInsightsEventsCount",
    "api_style": "RPC",
    "api_version": "2020-07-06",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "2026-01-07",
      "help_en": "The date to query. The format is `yyyy-MM-dd`",
      "help_zh": "指定查询日期。格式：`yyyy-MM-dd`",
      "location": "query",
      "name": "date",
      "options": [
        "--date"
      ],
      "raw_name": "Date",
      "required": false,
      "type": "string"
    },
    {
      "example": "2026-01-07T06:00:00Z",
      "help_en": "The end of the time range to query. Specify the time in the ISO 8601 standard in the `yyyy-MM-ddTHH:mm:ssZ` format. The time must be in UTC.\n\n- If Date, StartTime, and EndTime are all left empty, the system queries the number of events in the last 24 hours.\n\nIf Date is specified, the StartTime and EndTime parameters are ignored. The system queries the number of events on the specified date.\n\nIf Date is left empty and both StartTime and EndTime are specified, the system queries the number of events in the specified time range",
      "help_zh": "检索事件的结束时间。日期格式按照ISO8601标准，并使用UTC时间。格式为：`yyyy-MM-ddTHH:mm:ssZ`。\n\n- 当Date、StartTime、EndTime都为空时，查询距今24小时内的日志量。\n- 当Date不为空时，StartTime与EndTime参数无效，查询Date内的日志量。\n- 当Date为空且StartTime和EndTime都不为空时，查询范围内的日志量",
      "location": "query",
      "name": "end_time",
      "options": [
        "--end-time"
      ],
      "raw_name": "EndTime",
      "required": false,
      "type": "string"
    },
    {
      "example": "2025-12-01T02:00:00Z",
      "help_en": "The beginning of the time range to query. Specify the time in the ISO 8601 standard in the `yyyy-MM-ddTHH:mm:ssZ` format. The time must be in UTC",
      "help_zh": "检索事件的开始时间。日期格式按照ISO8601标准，并使用UTC时间。格式为：`yyyy-MM-ddTHH:mm:ssZ`",
      "location": "query",
      "name": "start_time",
      "options": [
        "--start-time"
      ],
      "raw_name": "StartTime",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Actiontrail",
  "protocol": "HTTP|HTTPS"
}
