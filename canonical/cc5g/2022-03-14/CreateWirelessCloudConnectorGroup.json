{
  "camel_example": "",
  "cmd_full_name": "cc5g create-wireless-cloud-connector-group",
  "cmd_name": "create-wireless-cloud-connector-group",
  "deprecated": false,
  "description_en": "Create a CC5G instance group",
  "description_zh": "创建cc5g实例分组",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "CreateWirelessCloudConnectorGroup",
  "operation": {
    "action": "CreateWirelessCloudConnectorGroup",
    "api_style": "RPC",
    "api_version": "2022-03-14",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "example": "test1",
      "help_en": "The name of the instance group. The name can be up to 128 characters in length",
      "help_zh": "实例组名称，长度不超过128字符",
      "location": "query",
      "name": "name",
      "options": [
        "--name"
      ],
      "raw_name": "Name",
      "required": false,
      "type": "string"
    },
    {
      "example": "cc5gg-test",
      "help_en": "The description of the Express Cloud Connect group",
      "help_zh": "高速上云服务分组的描述信息",
      "location": "query",
      "name": "description",
      "options": [
        "--description"
      ],
      "raw_name": "Description",
      "required": false,
      "type": "string"
    },
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID",
      "help_zh": "地域id",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "TF-******-1633255280-43c94bf7-2dd3-4c14-8",
      "help_en": "Ensures the idempotence of the request. Generate a unique value from your client for this parameter to guarantee uniqueness across different requests. Only ASCII characters are supported, and the value cannot exceed 64 characters.\nIf you do not specify this parameter, the system automatically uses the RequestId of the API request as the ClientToken. The RequestId may differ for each API request",
      "help_zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。只支持ASCII字符，且不能超过64个字符。\n若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId可能不一样",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to perform a dry run of the request. Valid values:\ntrue: Sends a dry run request without creating the instance. The system checks whether required parameters are specified, whether the request format is valid, and whether business limits are exceeded. If the check fails, an error is returned. If the check passes, the error code `DryRunOperation` is returned.\nfalse (default): Sends a normal request. If the check passes, an HTTP 2xx status code is returned and the instance is created",
      "help_zh": "是否只预检此次请求，取值：\ntrue：发送检查请求，不会删除实例。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\nfalse（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并删除实例",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "CC5G",
  "protocol": "HTTP|HTTPS"
}
