{
  "camel_example": "",
  "cmd_full_name": "onsmqtt active-ca-certificate",
  "cmd_name": "active-ca-certificate",
  "deprecated": false,
  "description_en": "ApsaraMQ for MQTT supports device verification using X.509 certificates. A certificate authority (CA) certificate is required to issue and validate these device certificates, and it must be active to be used. While CA certificates are active by default upon registration, this operation allows you to reactivate any certificate that has become inactive",
  "description_zh": "云消息队列 MQTT 版支持X.509证书认证，客户端可通过设备证书完成接入认证。CA证书用于为客户端签发设备证书，并验证设备证书的正确性，需要在激活状态下使用。\nCA证书注册后默认为激活状态，若您需要使用的CA证书被修改为未激活状态，您可以调用本接口将CA证书重新激活",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "ActiveCaCertificate",
  "operation": {
    "action": "ActiveCaCertificate",
    "api_style": "RPC",
    "api_version": "2020-04-20",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "post-cn-7mz2d******",
      "help_en": "The ID of the ApsaraMQ for MQTT instance to which the CA certificate is bound",
      "help_zh": "CA证书所绑定的实例ID，即云消息队列 MQTT 版的实例ID",
      "location": "query",
      "name": "mqtt_instance_id",
      "options": [
        "--mqtt-instance-id"
      ],
      "raw_name": "MqttInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "007269004887******",
      "help_en": "The serial number of the CA certificate that you want to reactivate. The serial number is the unique identifier of a CA certificate.\n\nThe serial number of a CA certificate cannot exceed 128 bytes in size",
      "help_zh": "待激活CA证书的SN序列号，用于唯一标识一个CA证书。\n\n取值范围：不超过128 Byte",
      "location": "query",
      "name": "sn",
      "options": [
        "--sn"
      ],
      "raw_name": "Sn",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "OnsMqtt",
  "protocol": "HTTP|HTTPS"
}
