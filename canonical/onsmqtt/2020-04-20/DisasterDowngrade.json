{
  "camel_example": "",
  "cmd_full_name": "onsmqtt disaster-downgrade",
  "cmd_name": "disaster-downgrade",
  "deprecated": false,
  "description_en": "Downgrades the virtual IP address (VIP) access of a specific instance during the disaster recovery of multiple instances. Only Enterprise Platinum Edition instances support this operation. To call the operation, you must submit a ticket",
  "description_zh": "多实例容灾降级，该接口作用是降级其中某个实例的公网VIP接入，目前只支持铂金版实例，需要提交工单申请",
  "kebab_example": "",
  "method": "POST",
  "multi_version": true,
  "name": "DisasterDowngrade",
  "operation": {
    "action": "DisasterDowngrade",
    "api_style": "RPC",
    "api_version": "2020-04-20",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "mqtt-cn-xxx",
      "help_en": "The ID of the ApsaraMQ for MQTT instance",
      "help_zh": "云消息队列 MQTT 版实例的 ID",
      "location": "formData",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "mqtt-cn-xxxx",
      "help_en": "The ID of the ApsaraMQ for MQTT instance for which you want to downgrade the VIP access",
      "help_zh": "降级的实例ID",
      "location": "formData",
      "name": "downgrade_instance_id",
      "options": [
        "--downgrade-instance-id"
      ],
      "raw_name": "DowngradeInstanceId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "OnsMqtt",
  "protocol": "HTTPS"
}
