{
  "camel_example": "",
  "cmd_full_name": "onsmqtt disaster-recovery",
  "cmd_name": "disaster-recovery",
  "deprecated": false,
  "description_en": "Restores Internet access for an instance using its virtual IP address (VIP) for graceful recovery in a multi-instance disaster recovery architecture. This operation is available only for Platinum Edition instances. To use this operation, submit a ticket",
  "description_zh": "多实例容灾降级恢复，该接口作用是恢复其中某个实例的公网VIP接入，目前只支持铂金版实例，需要提交工单申请",
  "kebab_example": "",
  "method": "POST",
  "multi_version": true,
  "name": "DisasterRecovery",
  "operation": {
    "action": "DisasterRecovery",
    "api_style": "RPC",
    "api_version": "2020-04-20",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "mqtt-cn-xxx",
      "help_en": "The ID of the ApsaraMQ for MQTT instance",
      "help_zh": "消息队列 MQTT 版实例的 ID",
      "location": "formData",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "mqtt-cn-xxx",
      "help_en": "The ID of the instance to recover",
      "help_zh": "需要恢复的实例ID",
      "location": "formData",
      "name": "recovery_instance_id",
      "options": [
        "--recovery-instance-id"
      ],
      "raw_name": "RecoveryInstanceId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "OnsMqtt",
  "protocol": "HTTPS"
}
