{
  "camel_example": "",
  "cmd_full_name": "onsmqtt refresh-device-credential",
  "cmd_name": "refresh-device-credential",
  "deprecated": false,
  "description_en": "Updates the access credential of a device",
  "description_zh": "调用RefreshDeviceCredential更新设备的访问凭证",
  "kebab_example": "",
  "method": "POST",
  "multi_version": true,
  "name": "RefreshDeviceCredential",
  "operation": {
    "action": "RefreshDeviceCredential",
    "api_style": "RPC",
    "api_version": "2020-04-20",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "GID_test@@@test",
      "help_en": "The client ID of the device whose access credential you want to update",
      "help_zh": "需要更新访问凭证的设备的Client ID",
      "location": "query",
      "name": "client_id",
      "options": [
        "--client-id"
      ],
      "raw_name": "ClientId",
      "required": true,
      "type": "string"
    },
    {
      "example": "post-cn-0pp12gl****",
      "help_en": "The ID of the ApsaraMQ for MQTT instance. The ID must be consistent with the ID of the instance that the ApsaraMQ for MQTT client uses. You can obtain the instance ID on the Instance Details page that corresponds to the instance in the ApsaraMQ for MQTT console",
      "help_zh": "云消息队列 MQTT 版实例的ID，一定要和客户端实际使用的实例ID匹配。您可以在控制台实例详情页面获取该ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "OnsMqtt",
  "protocol": "HTTP|HTTPS"
}
