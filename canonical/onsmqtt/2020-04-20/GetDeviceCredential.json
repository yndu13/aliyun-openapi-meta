{
  "camel_example": "",
  "cmd_full_name": "onsmqtt get-device-credential",
  "cmd_name": "get-device-credential",
  "deprecated": false,
  "description_en": "Queries the access credential of a device. If unique-certificate-per-device authentication is used as the authentication method on an ApsaraMQ for MQTT broker, an access credential that you apply for in advance is required for authentication when you connect your device to the broker. The connection can be established only after the authentication is passed",
  "description_zh": "一机一密认证方式下，设备与云消息队列 MQTT 版建立连接时，需要携带提前申请的设备访问凭证信息向云消息队列 MQTT 版发起认证请求，请求通过则允许设备接入服务端。本接口用于查询指定设备的访问凭证",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "GetDeviceCredential",
  "operation": {
    "action": "GetDeviceCredential",
    "api_style": "RPC",
    "api_version": "2020-04-20",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "GID_test@@@test",
      "help_en": "The client ID of the device whose access credential you want to query",
      "help_zh": "需要查询访问凭证的设备的Client ID",
      "location": "query",
      "name": "client_id",
      "options": [
        "--client-id"
      ],
      "raw_name": "ClientId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "post-cn-0pp12gl****",
      "help_en": "The ID of the ApsaraMQ for MQTT instance. The ID must be consistent with the ID of the instance that the ApsaraMQ for MQTT client uses. You can obtain the instance ID on the Instance Details page that corresponds to the instance in the ApsaraMQ for MQTT console",
      "help_zh": "云消息队列 MQTT 版实例的ID，一定要和客户端实际使用的实例ID匹配。您可以在控制台实例详情页面获取该ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "OnsMqtt",
  "protocol": "HTTP|HTTPS"
}
