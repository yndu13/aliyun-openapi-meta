{
  "cmd_full_name": "aiccs get-quality-project-list",
  "cmd_name": "get-quality-project-list",
  "deprecated": false,
  "description_en": "Invoke GetQualityProjectList to obtain the quality inspection job list.  ",
  "description_zh": "调用GetQualityProjectList获取质检任务列表",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "GetQualityProjectList",
  "operation": {
    "action": "GetQualityProjectList",
    "api_style": "RPC",
    "api_version": "2019-10-15",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "docRequired": true,
      "example": "ccc_xp_pre***",
      "help_en": "Artificial Intelligence Cloud Call Service (AICCS) instance ID. You can obtain it from the Artificial Intelligence Cloud Call Service console.  ",
      "help_zh": "AICCS实例ID。可在智能联络中心控制台上获取",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "15",
      "help_en": "Quality inspection job ID (supports fuzzy search).  ",
      "help_zh": "质检任务ID（支持模糊搜索）",
      "location": "query",
      "maximum": "9223372036854775807",
      "minimum": "0",
      "name": "project_id",
      "options": [
        "--project-id"
      ],
      "raw_name": "ProjectId",
      "required": false,
      "type": "int"
    },
    {
      "docRequired": true,
      "example": "质检",
      "help_en": "Quality inspection job name (supports fuzzy search).  ",
      "help_zh": "质检任务名（支持模糊搜索）",
      "location": "query",
      "name": "project_name",
      "options": [
        "--project-name"
      ],
      "raw_name": "ProjectName",
      "required": false,
      "type": "string"
    },
    {
      "docRequired": true,
      "example": "0",
      "help_en": "Quality inspection job status. Valid values:  \n\n0: Start  \n1: Shutdown  ",
      "help_zh": "质检任务状态。取值：\n\n0：启动\n1：关闭",
      "location": "query",
      "name": "status",
      "options": [
        "--status"
      ],
      "raw_name": "Status",
      "required": false,
      "type": "int"
    },
    {
      "docRequired": true,
      "example": "1",
      "help_en": "The current page. Default value: 1",
      "help_zh": "当前页。默认为：1",
      "location": "query",
      "maximum": "1000",
      "minimum": "1",
      "name": "page_no",
      "options": [
        "--page-no"
      ],
      "raw_name": "PageNo",
      "required": false,
      "type": "int"
    },
    {
      "docRequired": true,
      "example": "10",
      "help_en": "Page size. Default value: 10.  ",
      "help_zh": "每页大小。默认为：10",
      "location": "query",
      "maximum": "100",
      "minimum": "1",
      "name": "page_size",
      "options": [
        "--page-size"
      ],
      "raw_name": "PageSize",
      "required": false,
      "type": "int"
    },
    {
      "example": "1",
      "help_en": "The check frequency type. Valid values:\n\n1: Periodic quality inspection\n4: Temporary quality inspection",
      "help_zh": "检查频次类型。取值：\n\n1：周期质检\n4：临时质检",
      "location": "query",
      "name": "check_freq_type",
      "options": [
        "--check-freq-type"
      ],
      "raw_name": "checkFreqType",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "aiccs",
  "protocol": "HTTP|HTTPS"
}
