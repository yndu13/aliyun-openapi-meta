{
  "camel_example": "",
  "cmd_full_name": "ens prepare-upload",
  "cmd_name": "prepare-upload",
  "deprecated": false,
  "description_en": "Prepares for an upload and retrieves information about the physical storage location",
  "description_zh": "准备上传，获取物理存储位置信息",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "PrepareUpload",
  "operation": {
    "action": "PrepareUpload",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "test",
      "help_en": "The bucket name",
      "help_zh": "bucket名称",
      "location": "query",
      "name": "bucket_name",
      "options": [
        "--bucket-name"
      ],
      "raw_name": "BucketName",
      "required": true,
      "type": "string"
    },
    {
      "example": "180.166.XX.XX",
      "help_en": "The client's IP address. Use this parameter when the client's IP address differs from the IP address used to call the API. For example, a server obtains authorization and then sends it to a client",
      "help_zh": "指定IP，适用于用户IP与接口调用IP不一致的情况，如服务端获取授权后下发给客户端的场景",
      "location": "query",
      "name": "client_ip",
      "options": [
        "--client-ip"
      ],
      "raw_name": "ClientIp",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS"
}
