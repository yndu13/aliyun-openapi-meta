{
  "camel_example": "",
  "cmd_full_name": "ens create-key-pair",
  "cmd_name": "create-key-pair",
  "deprecated": false,
  "description_en": "Creates an SSH key pair",
  "description_zh": "调用CreateKeyPair创建一对SSH密钥对",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CreateKeyPair",
  "operation": {
    "action": "CreateKeyPair",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "example": "TestKeyPairName",
      "help_en": "The name of the key pair. The name must conform to the following naming conventions:\n\nThe name must be 2 to 128 characters in length, and can contain letters, digits, colons (:), underscores (_), and hyphens (-).\n\nIt must start with a letter but cannot start with `http://` or `https://`",
      "help_zh": "密钥对名称。命名规则如下所示：\n\n长度为2~128个英文或中文字符。\n\n必须以大小字母或中文开头，不能以`http://`和`https://`开头。\n\n可以包含数字、半角冒号（:）、下划线（_）、或者连字符（-）",
      "location": "query",
      "name": "key_pair_name",
      "options": [
        "--biz-key-pair-name"
      ],
      "raw_name": "KeyPairName",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS"
}
