{
  "camel_example": "",
  "cmd_full_name": "ens modify-instance-charge-type",
  "cmd_name": "modify-instance-charge-type",
  "deprecated": false,
  "description_en": "Call the `ModifyInstanceChargeType` operation to change the billing method for one or more Edge Node Service (ENS) instances. You can convert instances between pay-as-you-go and subscription plans. For a pay-as-you-go instance, you can also convert a cloud disk that was provisioned with the instance from pay-as-you-go to a subscription",
  "description_zh": "调用ModifyInstanceChargeType更换一台或者多台ENS实例的计费方式。支持在实例级后付费实例和包年包月实例间相互转换，同时针对实例级后付费实例可以将随实例创建的后付费云盘转换为包年包月云盘",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ModifyInstanceChargeType",
  "operation": {
    "action": "ModifyInstanceChargeType",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "element": {
        "type": "string"
      },
      "help_en": "A list of instance IDs",
      "help_zh": "实例ID列表",
      "location": "query",
      "name": "instance_ids",
      "options": [
        "--instance-ids"
      ],
      "param_style": "json",
      "raw_name": "InstanceIds",
      "required": true,
      "type": "array"
    },
    {
      "example": "PostPaid",
      "help_en": "The target billing method of the instance. Valid values:\n\n`PrePaid`: Changes the billing method of a pay-as-you-go instance to subscription.\n\n`PostPaid` (default): Changes the billing method of a subscription instance to pay-as-you-go",
      "help_zh": "实例需要修改的目标计费方式。取值：\n\nPrePaid：将实例级后付费实例转换为包年包月实例。\n\nPostPaid：将包年包月实例转换为实例级后付费实例（默认值）",
      "location": "query",
      "name": "instance_charge_type",
      "options": [
        "--instance-charge-type"
      ],
      "raw_name": "InstanceChargeType",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to enable auto-renewal for the instance. This parameter is valid only when you change the billing method of an instance from pay-as-you-go to subscription. Valid values:\n\n`true`: Auto-renewal is enabled.\n\n`false` (default): Auto-renewal is disabled",
      "help_zh": "实例级后付费转为包年包月的时候是否开启自动续费。取值：\n\ntrue：自动续费。\n\nfalse：不自动续费（默认值）",
      "location": "query",
      "name": "auto_renew",
      "options": [
        "--auto-renew"
      ],
      "raw_name": "AutoRenew",
      "required": false,
      "type": "bool"
    },
    {
      "example": "1",
      "help_en": "The subscription duration. This parameter is required when you change the billing method from pay-as-you-go to subscription. The valid values of this parameter depend on the `PeriodUnit` parameter.\n\nIf `PeriodUnit` is set to `Day`, the valid value of `Period` is `3`.\n\nIf `PeriodUnit` is set to `Month`, the valid values of `Period` are `1` to `9`, and `12`",
      "help_zh": "实例级后付费转为包年包月的时候的包年包月时长，如果是此场景下该字段必填。取值：\n\n如果 PeriodUnit 取值为 Day 时，则 Period 仅可设置为 3。\n\n如果 PeriodUnit 取值为 Month 时，则 Period 可设置为1-9,12",
      "location": "query",
      "name": "period",
      "options": [
        "--period"
      ],
      "raw_name": "Period",
      "required": false,
      "type": "string"
    },
    {
      "example": "Month",
      "help_en": "The unit of the subscription duration. This parameter is required when you change the billing method of an instance from pay-as-you-go to subscription. Valid values:\n\n`Month`\n\n`Day`",
      "help_zh": "实例级后付费转为包年包月的时候的购买资源的时长单位，如果是此场景下该字段必填。取值：\n\nMonth：按月购买。\n\nDay：按天购买",
      "location": "query",
      "name": "period_unit",
      "options": [
        "--period-unit"
      ],
      "raw_name": "PeriodUnit",
      "required": false,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to change the billing method of all pay-as-you-go data disks that are created with the instance to subscription. This parameter is valid only when you change the billing method of an instance from pay-as-you-go to subscription. Valid values:\n\n`true`: The billing method of the data disks is changed to subscription.\n\n`false` (default): The billing method of the data disks is not changed",
      "help_zh": "实例级后付费转为包年包月的时候是否将随实例创建的所有按量付费数据盘一起转换为包年包月数据盘。取值：\n\ntrue：是。\n\nfalse：否（默认值）",
      "location": "query",
      "name": "include_data_disks",
      "options": [
        "--include-data-disks"
      ],
      "raw_name": "IncludeDataDisks",
      "required": false,
      "type": "bool"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to enable automatic payment. This parameter is valid only when you change the billing method of an instance from pay-as-you-go to subscription. Valid values:\n\n`true`: Automatic payment is enabled. Make sure that your account balance is sufficient.\n\n`false` (default): An order is generated but no payment is made",
      "help_zh": "实例级后付费转为包年包月的时候是否自动支付。取值：\n\ntrue：自动支付，您需要确保账户余额充足。\n\nfalse：只生成订单不扣费（默认值）",
      "location": "query",
      "name": "auto_pay",
      "options": [
        "--auto-pay"
      ],
      "raw_name": "AutoPay",
      "required": false,
      "type": "bool"
    },
    {
      "help_en": "The BillingCycle",
      "help_zh": "The BillingCycle",
      "location": "query",
      "name": "billing_cycle",
      "options": [
        "--billing-cycle"
      ],
      "raw_name": "BillingCycle",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS",
  "v1_parameters": [
    {
      "help_en": "Specifies whether to enable automatic payment. This parameter is valid only when you change the billing method of an instance from pay-as-you-go to subscription. Valid values:\n\n`true`: Automatic payment is enabled. Make sure that your account balance is sufficient.\n\n`false` (default): An order is generated but no payment is made",
      "help_zh": "实例级后付费转为包年包月的时候是否自动支付。取值：\n\ntrue：自动支付，您需要确保账户余额充足。\n\nfalse：只生成订单不扣费（默认值）",
      "name": "AutoPay",
      "position": "query",
      "required": false,
      "type": "bool"
    },
    {
      "help_en": "Specifies whether to enable auto-renewal for the instance. This parameter is valid only when you change the billing method of an instance from pay-as-you-go to subscription. Valid values:\n\n`true`: Auto-renewal is enabled.\n\n`false` (default): Auto-renewal is disabled",
      "help_zh": "实例级后付费转为包年包月的时候是否开启自动续费。取值：\n\ntrue：自动续费。\n\nfalse：不自动续费（默认值）",
      "name": "AutoRenew",
      "position": "query",
      "required": false,
      "type": "bool"
    },
    {
      "name": "BillingCycle",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "Specifies whether to change the billing method of all pay-as-you-go data disks that are created with the instance to subscription. This parameter is valid only when you change the billing method of an instance from pay-as-you-go to subscription. Valid values:\n\n`true`: The billing method of the data disks is changed to subscription.\n\n`false` (default): The billing method of the data disks is not changed",
      "help_zh": "实例级后付费转为包年包月的时候是否将随实例创建的所有按量付费数据盘一起转换为包年包月数据盘。取值：\n\ntrue：是。\n\nfalse：否（默认值）",
      "name": "IncludeDataDisks",
      "position": "query",
      "required": false,
      "type": "bool"
    },
    {
      "help_en": "The target billing method of the instance. Valid values:\n\n`PrePaid`: Changes the billing method of a pay-as-you-go instance to subscription.\n\n`PostPaid` (default): Changes the billing method of a subscription instance to pay-as-you-go",
      "help_zh": "实例需要修改的目标计费方式。取值：\n\nPrePaid：将实例级后付费实例转换为包年包月实例。\n\nPostPaid：将包年包月实例转换为实例级后付费实例（默认值）",
      "name": "InstanceChargeType",
      "position": "query",
      "required": true,
      "type": "string"
    },
    {
      "help_en": "A list of instance IDs",
      "help_zh": "实例ID列表",
      "name": "InstanceIds",
      "param_style": "json",
      "position": "query",
      "required": true,
      "type": "string"
    },
    {
      "help_en": "The subscription duration. This parameter is required when you change the billing method from pay-as-you-go to subscription. The valid values of this parameter depend on the `PeriodUnit` parameter.\n\nIf `PeriodUnit` is set to `Day`, the valid value of `Period` is `3`.\n\nIf `PeriodUnit` is set to `Month`, the valid values of `Period` are `1` to `9`, and `12`",
      "help_zh": "实例级后付费转为包年包月的时候的包年包月时长，如果是此场景下该字段必填。取值：\n\n如果 PeriodUnit 取值为 Day 时，则 Period 仅可设置为 3。\n\n如果 PeriodUnit 取值为 Month 时，则 Period 可设置为1-9,12",
      "name": "Period",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The unit of the subscription duration. This parameter is required when you change the billing method of an instance from pay-as-you-go to subscription. Valid values:\n\n`Month`\n\n`Day`",
      "help_zh": "实例级后付费转为包年包月的时候的购买资源的时长单位，如果是此场景下该字段必填。取值：\n\nMonth：按月购买。\n\nDay：按天购买",
      "name": "PeriodUnit",
      "position": "query",
      "required": false,
      "type": "string"
    }
  ]
}
