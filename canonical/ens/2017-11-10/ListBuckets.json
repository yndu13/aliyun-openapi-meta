{
  "camel_example": "",
  "cmd_full_name": "ens list-buckets",
  "cmd_name": "list-buckets",
  "deprecated": false,
  "description_en": "Queries all buckets of a user",
  "description_zh": "调用ListBuckets查询指定用户拥有的所有存储空间（Bucket）",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ListBuckets",
  "operation": {
    "action": "ListBuckets",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "list",
    "url": ""
  },
  "parameters": [
    {
      "example": "image",
      "help_en": "The prefix that returned bucket names must contain. If this parameter is not specified, prefix information will not be used as a filter",
      "help_zh": "限定返回的bucket name必须以prefix作为前缀。可以不设定该参数，不设定时则不过滤前缀信息",
      "location": "query",
      "name": "prefix",
      "options": [
        "--prefix"
      ],
      "raw_name": "Prefix",
      "required": false,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The page number. Pages start from page 1",
      "help_zh": "翻页页码，从1开始",
      "location": "query",
      "name": "page_number",
      "options": [
        "--page-number"
      ],
      "raw_name": "PageNumber",
      "required": false,
      "type": "string"
    },
    {
      "example": "5",
      "help_en": "The maximum number of returned buckets. You can leave this parameter empty. The default value is 10. The value cannot be greater than 100",
      "help_zh": "限定此次返回bucket的最⼤数。可以不设定该参数，不设定时默认值为10，取值不能⼤于100",
      "location": "query",
      "name": "page_size",
      "options": [
        "--page-size"
      ],
      "raw_name": "PageSize",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS"
}
