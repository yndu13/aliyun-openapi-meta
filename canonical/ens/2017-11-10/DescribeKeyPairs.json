{
  "camel_example": "",
  "cmd_full_name": "ens describe-key-pairs",
  "cmd_name": "describe-key-pairs",
  "deprecated": false,
  "description_en": "Queries one or more key pairs",
  "description_zh": "调用DescribeKeyPairs查询一个或多个密钥对",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeKeyPairs",
  "operation": {
    "action": "DescribeKeyPairs",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "TestKeyPairName",
      "help_en": "The name of the key pair that you want to bind to the simple application server. The name must be 2 to 128 characters in length. The name must start with a letter but cannot start with `http://` or `https://`. The name can contain the following characters:\n\nNumbers.\n\n:\n\n_\n\n.\n\nYou can specify only one name. By default, all key pairs are queried",
      "help_zh": "密钥对名称。长度为2~128个英文或中文字符。必须以大小字母或中文开头，不能以`http://`和`https://`开头。可以包含以下字符：\n数字\n:\n_\n.\n\n仅支持传单个名称。默认查询所有密钥对信息",
      "location": "query",
      "name": "key_pair_name",
      "options": [
        "--biz-key-pair-name"
      ],
      "raw_name": "KeyPairName",
      "required": false,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The page number of the returned page. Valid values: integers that are greater than 0. Default value: 1",
      "help_zh": "查询列表的页码。取值：>0，默认查询第1页",
      "location": "query",
      "name": "page_number",
      "options": [
        "--page-number"
      ],
      "raw_name": "PageNumber",
      "required": false,
      "type": "string"
    },
    {
      "example": "10",
      "help_en": "The number of entries per page. Valid values: integers that are greater than 0. Default value: 10",
      "help_zh": "分页查询时设置的每页行数。取值：>0，默认为10",
      "location": "query",
      "name": "page_size",
      "options": [
        "--page-size"
      ],
      "raw_name": "PageSize",
      "required": false,
      "type": "string"
    },
    {
      "example": "ssh-50cynkq42sgj4ej1tn78t4***",
      "help_en": "The ID of the key pair",
      "help_zh": "密钥对的ID",
      "location": "query",
      "name": "key_pair_id",
      "options": [
        "--key-pair-id"
      ],
      "raw_name": "KeyPairId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS"
}
