{
  "camel_example": "",
  "cmd_full_name": "ens deploy-instance-sdg",
  "cmd_name": "deploy-instance-sdg",
  "deprecated": false,
  "description_en": "Deploy SDG to a compute instance",
  "description_zh": "部署SDG到计算实例",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeployInstanceSDG",
  "operation": {
    "action": "DeployInstanceSDG",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "element": {
        "type": "string"
      },
      "help_en": "The IDs of the instances. The value is a JSON array that consists of up to 100 IDs",
      "help_zh": "实例的编号列表。参数为一个带有格式的JSON数组，最多100个ID",
      "location": "query",
      "name": "instance_ids",
      "options": [
        "--instance-ids"
      ],
      "param_style": "json",
      "raw_name": "InstanceIds",
      "required": true,
      "type": "array"
    },
    {
      "example": "sdg-xxxx",
      "help_en": "The ID of the SDG",
      "help_zh": "SDG的ID",
      "location": "query",
      "name": "sdg_id",
      "options": [
        "--sdg-id"
      ],
      "raw_name": "SDGId",
      "required": true,
      "type": "string"
    },
    {
      "example": "shared",
      "help_en": "The deployment type of the SDG. shared: shared read/write splitting deployment. The content of the SDG is read-only, and data updates are written to the local storage of the instance",
      "help_zh": "SDG部署类型，取值shared表示读写分离共享部署，SDG内容只读，数据更新内容写入实例本地",
      "location": "query",
      "name": "deployment_type",
      "options": [
        "--deployment-type"
      ],
      "raw_name": "DeploymentType",
      "required": false,
      "type": "string"
    },
    {
      "example": "standard",
      "help_en": "The disk type of the shared disk to be generated:\n\nstandard (standard disk).\n\nrbd (RBD disk).\n\nIf the shared disk type is not specified, a standard disk is generated by default.\n\nRBD shared disks can be generated only when the source disk type is RBD",
      "help_zh": "生产的共享盘磁盘类型：\n\nstandard(标准云盘)。\nrbd（rbd云盘）。\n\n\n不指定共享盘磁盘类型时默认生成标准云盘。\n仅母盘类型为rbd时支持生成rbd共享盘",
      "location": "query",
      "name": "disk_type",
      "options": [
        "--disk-type"
      ],
      "raw_name": "DiskType",
      "required": false,
      "type": "string"
    },
    {
      "example": "iscsi",
      "help_en": "The protocol type for disk access:\n\niscsi: The default protocol for standard disks. Both RBD disks and standard disks support this protocol.\n\nrbd: The default protocol for RBD disks. Only RBD disks support this mount protocol",
      "help_zh": "云盘访问协议类型：\n\niscsi：标准云盘默认协议，rbd云盘和标准云盘都支持该协议。\n\nrbd：rbd云盘默认协议，只有rbd云盘支持该挂载协议",
      "location": "query",
      "name": "disk_access_protocol",
      "options": [
        "--disk-access-protocol"
      ],
      "raw_name": "DiskAccessProtocol",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS"
}
