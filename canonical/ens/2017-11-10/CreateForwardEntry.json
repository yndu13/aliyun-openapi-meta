{
  "camel_example": "",
  "cmd_full_name": "ens create-forward-entry",
  "cmd_name": "create-forward-entry",
  "deprecated": false,
  "description_en": "Adds a Destination Network Address Translation (DNAT) entry to a DNAT table",
  "description_zh": "调用CreateForwardEntry添加DNAT条目",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CreateForwardEntry",
  "operation": {
    "action": "CreateForwardEntry",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "example": "nat-5t7nh1cfm6kxiszlttr38****",
      "help_en": "The ID of the Network Address Translation (NAT) gateway",
      "help_zh": " NAT网关的ID。 ",
      "location": "query",
      "name": "nat_gateway_id",
      "options": [
        "--nat-gateway-id"
      ],
      "raw_name": "NatGatewayId",
      "required": true,
      "type": "string"
    },
    {
      "example": "121.XXX.XXX.28",
      "help_en": "The elastic IP address (EIP) that is used to access the Internet",
      "help_zh": "提供公网访问的弹性公网IP地址。\n\n该 EIP 不得被同一 NAT 网关的 SNAT 规则占用；可通过 DescribeNatGateways 返回的 IpLists.UsingStatus 确认其为 Idle 状态",
      "location": "query",
      "name": "external_ip",
      "options": [
        "--external-ip"
      ],
      "raw_name": "ExternalIp",
      "required": true,
      "type": "string"
    },
    {
      "example": "22",
      "help_en": "The external port or port range that is used for port forwarding.\n\nValid values: 1 to 65535.\n\nTo specify a port range, separate the first port and the last port with a forward slash (/), such as 10/20.\n\nIf you set ExternalPort to a port range, you must also set InternalPort to a port range. The number of ports in the port ranges must be the same. For example, if you set ExternalPort to 10/20, you can set InternalPort to 80/90",
      "help_zh": "需要进行端口转发的外部端口或端口段。\n输入的端口范围需要在1~65535之间。\n如果需要在端口段内转发，请在输入时以正斜线（/）隔开起始端口，例如10/20。\n如果ExternalPort设置为端口段，则InternalPort也需要设置为端口段，且端口段的端口个数相同，例如ExternalPort设置为10/20，InternalPort设置为80/90",
      "location": "query",
      "name": "external_port",
      "options": [
        "--external-port"
      ],
      "raw_name": "ExternalPort",
      "required": true,
      "type": "string"
    },
    {
      "example": "10.0.XXX.XXX",
      "help_en": "The private IP address of the instance that uses the DNAT entry for Internet communication",
      "help_zh": "通过DNAT条目进行公网通信的实例的私网IP地址",
      "location": "query",
      "name": "internal_ip",
      "options": [
        "--internal-ip"
      ],
      "raw_name": "InternalIp",
      "required": true,
      "type": "string"
    },
    {
      "example": "22",
      "help_en": "The internal port or port range that is used for port forwarding.\n\nValid values: 1 to 65535.\n\nTo specify a port range, separate the first port and the last port with a forward slash (/), such as 10/20",
      "help_zh": "进行端口转发的内部端口或端口段。\n\n输入的端口范围需要在1~65535之间。\n如果需要在端口段内转发，请在输入时以正斜线（/）隔开起始端口，例如10/20",
      "location": "query",
      "name": "internal_port",
      "options": [
        "--internal-port"
      ],
      "raw_name": "InternalPort",
      "required": true,
      "type": "string"
    },
    {
      "example": "test0",
      "help_en": "The name of the DNAT entry. The name must be 2 to 128 characters in length. The name cannot start with `http://` or `https://`",
      "help_zh": "DNAT规则的名称。长度为2~128个字符，不能以`http://`或`https://`开头",
      "location": "query",
      "name": "forward_entry_name",
      "options": [
        "--forward-entry-name"
      ],
      "raw_name": "ForwardEntryName",
      "required": false,
      "type": "string"
    },
    {
      "example": "Any",
      "help_en": "The protocol. Valid values:\n\nTCP: forwards TCP packets.\n\nUDP: forwards UDP packets.\n\nAny (default): forwards all packets",
      "help_zh": "协议类型，取值： \n\nTCP：转发TCP协议的报文。   \n\nUDP：转发UDP协议的报文。   \n\nAny（默认）：转发所有协议的报文",
      "location": "query",
      "name": "ip_protocol",
      "options": [
        "--ip-protocol"
      ],
      "raw_name": "IpProtocol",
      "required": false,
      "type": "string"
    },
    {
      "example": "80",
      "help_en": "The probe port. The port must be within the internal port range. By default, this parameter is left empty",
      "help_zh": "DNAT的探测端口，需要在内网端口范围内。默认值为空",
      "location": "query",
      "name": "health_check_port",
      "options": [
        "--health-check-port"
      ],
      "raw_name": "HealthCheckPort",
      "required": false,
      "type": "int"
    },
    {
      "example": "101.XXX.XXX.4",
      "help_en": "The secondary EIP that is used to access the Internet. You need to select a secondary EIP that is bound to NAT. After the DNAT entry is created, the secondary EIP takes effect",
      "help_zh": "提供公网访问的备用弹性公网IP地址，需要选择绑定在NAT上的备用弹性IP。DNAT创建成功后，备用公网IP会生效",
      "location": "query",
      "name": "standby_external_ip",
      "options": [
        "--standby-external-ip"
      ],
      "raw_name": "StandbyExternalIp",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS",
  "v1_parameters": [
    {
      "help_en": "The elastic IP address (EIP) that is used to access the Internet",
      "help_zh": "提供公网访问的弹性公网IP地址。\n\n该 EIP 不得被同一 NAT 网关的 SNAT 规则占用；可通过 DescribeNatGateways 返回的 IpLists.UsingStatus 确认其为 Idle 状态",
      "name": "ExternalIp",
      "position": "query",
      "required": true,
      "type": "string"
    },
    {
      "help_en": "The external port or port range that is used for port forwarding.\n\nValid values: 1 to 65535.\n\nTo specify a port range, separate the first port and the last port with a forward slash (/), such as 10/20.\n\nIf you set ExternalPort to a port range, you must also set InternalPort to a port range. The number of ports in the port ranges must be the same. For example, if you set ExternalPort to 10/20, you can set InternalPort to 80/90",
      "help_zh": "需要进行端口转发的外部端口或端口段。\n输入的端口范围需要在1~65535之间。\n如果需要在端口段内转发，请在输入时以正斜线（/）隔开起始端口，例如10/20。\n如果ExternalPort设置为端口段，则InternalPort也需要设置为端口段，且端口段的端口个数相同，例如ExternalPort设置为10/20，InternalPort设置为80/90",
      "name": "ExternalPort",
      "position": "query",
      "required": true,
      "type": "string"
    },
    {
      "help_en": "The name of the DNAT entry. The name must be 2 to 128 characters in length. The name cannot start with `http://` or `https://`",
      "help_zh": "DNAT规则的名称。长度为2~128个字符，不能以`http://`或`https://`开头",
      "name": "ForwardEntryName",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The probe port. The port must be within the internal port range. By default, this parameter is left empty",
      "help_zh": "DNAT的探测端口，需要在内网端口范围内。默认值为空",
      "name": "HealthCheckPort",
      "position": "query",
      "required": false,
      "type": "int"
    },
    {
      "help_en": "The private IP address of the instance that uses the DNAT entry for Internet communication",
      "help_zh": "通过DNAT条目进行公网通信的实例的私网IP地址",
      "name": "InternalIp",
      "position": "query",
      "required": true,
      "type": "string"
    },
    {
      "help_en": "The internal port or port range that is used for port forwarding.\n\nValid values: 1 to 65535.\n\nTo specify a port range, separate the first port and the last port with a forward slash (/), such as 10/20",
      "help_zh": "进行端口转发的内部端口或端口段。\n\n输入的端口范围需要在1~65535之间。\n如果需要在端口段内转发，请在输入时以正斜线（/）隔开起始端口，例如10/20",
      "name": "InternalPort",
      "position": "query",
      "required": true,
      "type": "string"
    },
    {
      "help_en": "The protocol. Valid values:\n\nTCP: forwards TCP packets.\n\nUDP: forwards UDP packets.\n\nAny (default): forwards all packets",
      "help_zh": "协议类型，取值： \n\nTCP：转发TCP协议的报文。   \n\nUDP：转发UDP协议的报文。   \n\nAny（默认）：转发所有协议的报文",
      "name": "IpProtocol",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The ID of the Network Address Translation (NAT) gateway",
      "help_zh": "NAT网关的ID",
      "name": "NatGatewayId",
      "position": "query",
      "required": true,
      "type": "string"
    },
    {
      "help_en": "The secondary EIP that is used to access the Internet. You need to select a secondary EIP that is bound to NAT. After the DNAT entry is created, the secondary EIP takes effect",
      "help_zh": "提供公网访问的备用弹性公网IP地址，需要选择绑定在NAT上的备用弹性IP。DNAT创建成功后，备用公网IP会生效",
      "name": "StandbyExternalIp",
      "position": "query",
      "required": false,
      "type": "string"
    }
  ]
}
