{
  "camel_example": "",
  "cmd_full_name": "ens release-instance",
  "cmd_name": "release-instance",
  "deprecated": false,
  "description_en": "Releases an instance. to release only Elastic IP Addresses (EIPs), Edge Load Balancer (ELB) instances, and cloud disk-based instances. We recommend that you call service-specific operations to release or unsubscribe from instances",
  "description_zh": "释放实例，此接口只支持弹性公网EIP、边缘负载均衡实例ELB、云盘实例释放。建议对实例的释放调用每个产品对应的释放或退订接口。\n释放EIP接口：DeleteEip，释放ELB接口：DeleteLoadBalancer，释放云盘实例接口：DeleteDisk",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ReleaseInstance",
  "operation": {
    "action": "ReleaseInstance",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "eip-****",
      "help_en": "The ID of the EIP",
      "help_zh": "EIP实例的ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS"
}
