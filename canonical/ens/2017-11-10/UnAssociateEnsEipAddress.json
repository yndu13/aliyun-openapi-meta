{
  "camel_example": "",
  "cmd_full_name": "ens un-associate-ens-eip-address",
  "cmd_name": "un-associate-ens-eip-address",
  "deprecated": false,
  "description_en": "Disassociates an elastic IP address (EIP) from an instance",
  "description_zh": "调用UnAssociateEnsEipAddress将弹性公网IP（EIP）从绑定的云产品上解绑",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UnAssociateEnsEipAddress",
  "operation": {
    "action": "UnAssociateEnsEipAddress",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "eip-5sqa431nx3vee8heqxfxp****",
      "help_en": "The ID of the EIP",
      "help_zh": "边缘弹性公网IP的实例ID",
      "location": "query",
      "name": "allocation_id",
      "options": [
        "--allocation-id"
      ],
      "raw_name": "AllocationId",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to disassociate the EIP from a NAT gateway if a DNAT or SNAT entry is added to the NAT gateway. Valid values:\n\nfalse (default): does not disassociate the EIP from a NAT gateway if a DNAT or SNAT entry is added to the NAT gateway.\n\ntrue: disassociates the EIP from a NAT gateway if a DNAT or SNAT entry is added to the NAT gateway",
      "help_zh": "当EIP绑定了NAT网关，且NAT网关添加了DNAT或SNAT条目时，是否强制解绑EIP，取值：\nfalse（默认值）：不强制解绑EIP。\ntrue：强制解绑EIP",
      "location": "query",
      "name": "force",
      "options": [
        "--biz-force"
      ],
      "raw_name": "Force",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS"
}
