{
  "camel_example": "",
  "cmd_full_name": "ens describe-image-infos",
  "cmd_name": "describe-image-infos",
  "deprecated": false,
  "description_en": "Queries available image resources",
  "description_zh": "查询可以使用的镜像资源",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeImageInfos",
  "operation": {
    "action": "DescribeImageInfos",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "linux",
      "help_en": "The operating system type. Only a single type can be specified. If this parameter is not specified, all types are queried.\n\nValid values:\n\nLinux\n\nwindows\n\nIn practice, passing linux or Linux may return an empty list, whereas omitting this parameter can return images with an OSType of linux. Verify the actual results before relying on this filter",
      "help_zh": "系统类型，仅支持传单个类型。不传值，默认查询全部。\n取值：\n\nLinux\n\nwindows\n\n治理实测中，传入 linux 或 Linux 均返回空列表，而省略该参数可返回 OSType 为 linux 的镜像；依赖该筛选条件前请核对实际返回结果",
      "location": "query",
      "name": "os_type",
      "options": [
        "--os-type"
      ],
      "raw_name": "OsType",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS"
}
