{
  "camel_example": "",
  "cmd_full_name": "paistudio list-node-types",
  "cmd_name": "list-node-types",
  "deprecated": false,
  "description_en": "Lists the available node types",
  "description_zh": "获取节点规格列表",
  "kebab_example": "",
  "method": "GET",
  "multi_version": true,
  "name": "ListNodeTypes",
  "operation": {
    "action": "ListNodeTypes",
    "api_style": "ROA",
    "api_version": "2022-01-12",
    "method": "GET",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": "/api/v1/nodetypes"
  },
  "parameters": [
    {
      "example": "rg123,rg456",
      "help_en": "A comma-separated list of resource group IDs. You must specify either this parameter or `QuotaId`. The `UserId` for the specified `ResourceGroupId` must match the `UserId` of the requesting user. All specified `resource group` instances must be of the same type and associated with the same Virtual Private Cloud (VPC)",
      "help_zh": "指定资源组ID列表，按逗号分隔，和`QuotaIds`参数二选一必填。约束：需校验传入的`ResourceGroupId`的`UserId`和请求的`UserId`一致；传入的`ResourceGroup`列表类型需一致；传入的`ResourceGroup`列表现阶段记录的VPC需一致",
      "location": "query",
      "name": "resource_group_ids",
      "options": [
        "--resource-group-ids"
      ],
      "raw_name": "ResourceGroupIds",
      "required": false,
      "type": "string"
    },
    {
      "example": "quota123",
      "help_en": "The quota ID. You must specify either this parameter or `ResourceGroupIds`",
      "help_zh": "指定资源配额ID，和`ResourceGroupIds` 二选一必填",
      "location": "query",
      "name": "quota_id",
      "options": [
        "--quota-id"
      ],
      "raw_name": "QuotaId",
      "required": false,
      "type": "string"
    },
    {
      "example": "ecs.c6.large,ecs.gn7i-c32g1.8xlarge",
      "help_en": "A comma-separated list of node types. If omitted, all node types are returned",
      "help_zh": "资源节点规格列表，按逗号分隔，不设置会返回所有资源节点规格的机器列表",
      "location": "query",
      "name": "node_types",
      "options": [
        "--node-types"
      ],
      "raw_name": "NodeTypes",
      "required": false,
      "type": "string"
    },
    {
      "example": "GPU",
      "help_en": "The accelerator type used to filter the results. Valid values: `CPU` and `GPU`",
      "help_zh": "按加速器类型（CPU/GPU）",
      "location": "query",
      "name": "accelerator_type",
      "options": [
        "--accelerator-type"
      ],
      "raw_name": "AcceleratorType",
      "required": false,
      "type": "string"
    },
    {
      "example": "A10",
      "help_en": "The GPU type used to filter the results. Fuzzy matching is supported",
      "help_zh": "按卡类型，支持模糊匹配",
      "location": "query",
      "name": "gpu_type",
      "options": [
        "--gpu-type"
      ],
      "raw_name": "GPUType",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/api/v1/nodetypes",
  "product_code": "PaiStudio",
  "protocol": "HTTP|HTTPS"
}
