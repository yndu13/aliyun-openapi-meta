{
  "cmd_full_name": "paistudio stop-experiment",
  "cmd_name": "stop-experiment",
  "deprecated": false,
  "description_en": "Stops all running jobs submitted by the pipeline draft. The stop operation is asynchronous. After the API returns, you need to wait for a period of time before the jobs are successfully stopped",
  "description_zh": "停止工作流草稿提交的所有运行中的作业，停止是异步操作，接口返回后，需要等待一段时间才能停止成功",
  "kebab_example": "",
  "method": "POST",
  "multi_version": true,
  "name": "StopExperiment",
  "operation": {
    "action": "StopExperiment",
    "api_style": "ROA",
    "api_version": "2021-02-02",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": "/api/v1/experiments/{ExperimentId}/stop"
  },
  "parameters": [
    {
      "example": "draft-i1udvvwm2rlvxug29n",
      "help_en": "The pipeline draft ID",
      "help_zh": "工作流草稿ID",
      "location": "path",
      "name": "experiment_id",
      "options": [
        "--experiment-id"
      ],
      "raw_name": "ExperimentId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "/api/v1/experiments/[ExperimentId]/stop",
  "product_code": "PaiStudio",
  "protocol": "HTTP|HTTPS"
}
