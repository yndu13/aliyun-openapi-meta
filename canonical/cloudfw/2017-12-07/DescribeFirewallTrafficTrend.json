{
  "camel_example": "",
  "cmd_full_name": "cloudfw describe-firewall-traffic-trend",
  "cmd_name": "describe-firewall-traffic-trend",
  "deprecated": false,
  "description_en": "Queries the traffic trend of a firewall",
  "description_zh": "查询防火墙流量趋势",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeFirewallTrafficTrend",
  "operation": {
    "action": "DescribeFirewallTrafficTrend",
    "api_style": "RPC",
    "api_version": "2017-12-07",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "zh",
      "help_en": "The language of the response message.\n\nzh (default): Chinese\nen: English",
      "help_zh": "接收消息的语言类型。\n\nzh（默认）：中文\nen：英文",
      "location": "query",
      "name": "lang",
      "options": [
        "--lang"
      ],
      "raw_name": "Lang",
      "required": false,
      "type": "string"
    },
    {
      "example": "1758470400",
      "help_en": "The start time of the query. Specify a UNIX timestamp in seconds. This parameter is required. If this parameter is not specified, ErrorTimeError (400) is returned.\n\nThe query interval (EndTime − StartTime) cannot exceed 90 days. If the interval exceeds 90 days, ErrorTimeError is returned. If the value is later than the current time, it is silently adjusted to the current time. If StartTime is later than EndTime, no error is returned, but the response contains empty data",
      "help_zh": "查询开始时间，使用秒级 Unix 时间戳。本参数必填，缺失时返回 ErrorTimeError（400）。\n\n查询区间（EndTime − StartTime）不得超过 90 天，超出返回 ErrorTimeError；晚于当前时间的取值会被静默调整为当前时间。StartTime 晚于 EndTime 时后端不报错，只会返回空数据",
      "location": "query",
      "name": "start_time",
      "options": [
        "--start-time"
      ],
      "raw_name": "StartTime",
      "required": true,
      "type": "int"
    },
    {
      "example": "1758474000",
      "help_en": "The end time of the query. Specify a UNIX timestamp in seconds. This parameter is required. If this parameter is not specified, ErrorTimeError (400) is returned.\n\nThe query interval (EndTime − StartTime) cannot exceed 90 days. If the interval exceeds 90 days, ErrorTimeError is returned. If the value is later than the current time, it is silently adjusted to the current time",
      "help_zh": "查询结束时间，使用秒级 Unix 时间戳。本参数必填，缺失时返回 ErrorTimeError（400）。\n\n查询区间（EndTime − StartTime）不得超过 90 天，超出返回 ErrorTimeError；晚于当前时间的取值会被静默调整为当前时间",
      "location": "query",
      "name": "end_time",
      "options": [
        "--end-time"
      ],
      "raw_name": "EndTime",
      "required": true,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "Cloudfw",
  "protocol": "HTTPS"
}
