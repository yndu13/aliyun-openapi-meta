{
  "cmd_full_name": "linkwan update-owned-local-join-permission-enabling-state",
  "cmd_name": "update-owned-local-join-permission-enabling-state",
  "deprecated": false,
  "description_en": "Enables or disables a dedicated join permission owned by the current user account",
  "description_zh": "启用、停用当前用户账号所拥有的专用入网凭证",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "UpdateOwnedLocalJoinPermissionEnablingState",
  "operation": {
    "action": "UpdateOwnedLocalJoinPermissionEnablingState",
    "api_style": "RPC",
    "api_version": "2018-12-30",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "docRequired": true,
      "example": "123",
      "help_en": "The ID of the dedicated join permission to operate on",
      "help_zh": "要操作的专用入网凭证ID",
      "location": "formData",
      "name": "join_permission_id",
      "options": [
        "--join-permission-id"
      ],
      "raw_name": "JoinPermissionId",
      "required": true,
      "type": "string"
    },
    {
      "docRequired": true,
      "example": "true",
      "help_en": "Specifies whether to enable the dedicated join permission. Valid values:\ntrue: Enable the dedicated join permission.\nfalse: Disable the dedicated join permission",
      "help_zh": "true为启用，false为停用",
      "location": "formData",
      "name": "enabled",
      "options": [
        "--enabled"
      ],
      "raw_name": "Enabled",
      "required": true,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "LinkWAN",
  "protocol": "HTTP|HTTPS"
}
