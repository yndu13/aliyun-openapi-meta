{
  "cmd_full_name": "linkwan update-owned-local-join-permission",
  "cmd_name": "update-owned-local-join-permission",
  "deprecated": false,
  "description_en": "Updates a proprietary join permission owned by the current user account",
  "description_zh": "调用UpdateOwnedLocalJoinPermission更新当前用户账号所拥有的专用入网凭证",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "UpdateOwnedLocalJoinPermission",
  "operation": {
    "action": "UpdateOwnedLocalJoinPermission",
    "api_style": "RPC",
    "api_version": "2018-12-30",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "docRequired": true,
      "example": "123",
      "help_en": "The ID of the proprietary join permission to update",
      "help_zh": "专用入网凭证ID，用来指定要更新的入网凭证",
      "location": "formData",
      "name": "join_permission_id",
      "options": [
        "--join-permission-id"
      ],
      "raw_name": "JoinPermissionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "201",
      "help_en": "The ID of the frequency band used by the proprietary join permission. This parameter is used to update the existing frequency band",
      "help_zh": "专用入网凭证所采用的频段的频段ID，用于更新旧频段",
      "location": "formData",
      "name": "freq_band_plan_group_id",
      "options": [
        "--freq-band-plan-group-id"
      ],
      "raw_name": "FreqBandPlanGroupId",
      "required": false,
      "type": "int"
    },
    {
      "enum": [
        "A",
        "B",
        "C"
      ],
      "example": "A",
      "help_en": "The LoRaWAN Class mode used by the proprietary join permission. This parameter is used to update the existing Class mode. Valid values: A, B, and C",
      "help_zh": "专用入网凭证采用的LoRaWAN Class模式，用于更新旧的 Class 模式。可取值：A、B、C",
      "location": "formData",
      "name": "class_mode",
      "options": [
        "--class-mode"
      ],
      "raw_name": "ClassMode",
      "required": false,
      "type": "string"
    },
    {
      "example": "凭证1",
      "help_en": "The custom name of the join permission. This parameter is used to update the existing name",
      "help_zh": "自定义入网凭证名称，用于更新旧名称",
      "location": "formData",
      "name": "join_permission_name",
      "options": [
        "--join-permission-name"
      ],
      "raw_name": "JoinPermissionName",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "LinkWAN",
  "protocol": "HTTP|HTTPS"
}
