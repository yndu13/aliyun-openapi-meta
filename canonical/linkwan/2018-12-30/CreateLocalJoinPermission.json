{
  "cmd_full_name": "linkwan create-local-join-permission",
  "cmd_name": "create-local-join-permission",
  "deprecated": false,
  "description_en": "The current user account becomes the owner of the credential and has full ownership of it",
  "description_zh": "当前用户账号将成为该凭证的拥有者，拥有该凭证",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "CreateLocalJoinPermission",
  "operation": {
    "action": "CreateLocalJoinPermission",
    "api_style": "RPC",
    "api_version": "2018-12-30",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "example": "101",
      "help_en": "The frequency band ID of the frequency band used by the dedicated join permission",
      "help_zh": "专用入网凭证所采用的频段的频段ID",
      "location": "formData",
      "name": "freq_band_plan_group_id",
      "options": [
        "--freq-band-plan-group-id"
      ],
      "raw_name": "FreqBandPlanGroupId",
      "required": true,
      "type": "int"
    },
    {
      "example": "A",
      "help_en": "The LoRaWAN Class mode used by the dedicated join permission. Valid values: A, B, and C",
      "help_zh": "专用入网凭证采用的LoRaWAN Class模式。可取值：A、B、C",
      "location": "formData",
      "name": "class_mode",
      "options": [
        "--class-mode"
      ],
      "raw_name": "ClassMode",
      "required": true,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether the dedicated join permission uses the Alibaba Cloud unified JoinEUI",
      "help_zh": "专用入网凭证是否使用阿里云统一JoinEUI",
      "location": "formData",
      "name": "use_default_join_eui",
      "options": [
        "--use-default-join-eui"
      ],
      "raw_name": "UseDefaultJoinEui",
      "required": true,
      "type": "bool"
    },
    {
      "example": "test",
      "help_en": "The custom name of the dedicated join permission",
      "help_zh": "自定义专用入网凭证名称",
      "location": "formData",
      "name": "join_permission_name",
      "options": [
        "--join-permission-name"
      ],
      "raw_name": "JoinPermissionName",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "LinkWAN",
  "protocol": "HTTP|HTTPS"
}
