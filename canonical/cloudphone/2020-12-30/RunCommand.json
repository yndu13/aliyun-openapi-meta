{
  "camel_example": "",
  "cmd_full_name": "cloudphone run-command",
  "cmd_name": "run-command",
  "deprecated": false,
  "description_en": "Executes a command on cloud phones",
  "description_zh": "在云手机中执行命令",
  "kebab_example": "aliyun cloudphone run-command --biz-region-id cn-hangzhou --command ls_-al_/ --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "RunCommand",
  "operation": {
    "action": "RunCommand",
    "api_style": "RPC",
    "api_version": "2020-12-30",
    "method": "POST",
    "protocol": "HTTPS",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID",
      "help_zh": "地域",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The instance ID of the instance on which to execute the command. Valid values of N: 1 to 10",
      "help_zh": "待执行命令的实例ID。 <br> N的范围：1~10",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "param_style": "repeatList",
      "raw_name": "InstanceId",
      "required": false,
      "type": "array"
    },
    {
      "example": "ls -al /",
      "help_en": "The command to execute. The command can be up to 1024 bytes in length and can contain only letters, digits, underscores (_), periods (.), forward slashes (/), colons (:), and hyphens (-)",
      "help_zh": "待执行的命令\n<br>\n最大长度为1024字节，只支持大小写字母、数字、下划线（_）、点（.）、斜线（/）、冒号（:）、中划线（-）",
      "location": "query",
      "name": "command",
      "options": [
        "--command"
      ],
      "raw_name": "Command",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "cloudphone",
  "protocol": "HTTP|HTTPS",
  "responses": {
    "200": {
      "schema": {
        "description_en": "",
        "description_zh": "",
        "properties": {
          "RequestId": {
            "description_en": "The request ID",
            "description_zh": "请求ID",
            "example": "55C15B1C-D507-45F0-B490-69D6E0F113C6",
            "type": "string"
          },
          "TaskId": {
            "description_en": "",
            "description_zh": "",
            "itemNode": true,
            "properties": {
              "TaskId": {
                "description_en": "The task ID",
                "description_zh": "任务ID",
                "items": {
                  "description_en": "The list of task IDs. Valid values of N: 1 to 100",
                  "description_zh": "任务ID列表。N的取值范围：1~100",
                  "example": "t-bp67acfmxazb4p****",
                  "type": "string"
                },
                "type": "array"
              }
            },
            "type": "object"
          }
        },
        "type": "object"
      }
    }
  }
}
