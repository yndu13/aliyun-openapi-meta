{
  "camel_example": "",
  "cmd_full_name": "cloudphone import-key-pair",
  "cmd_name": "import-key-pair",
  "deprecated": false,
  "description_en": "Imports the public key portion of an RSA key pair generated by another tool",
  "description_zh": "导入由其他工具产生的RSA密钥对的公钥部分",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ImportKeyPair",
  "operation": {
    "action": "ImportKeyPair",
    "api_style": "RPC",
    "api_version": "2020-12-30",
    "method": "POST",
    "protocol": "HTTPS",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID",
      "help_zh": "地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "testKeyPairName",
      "help_en": "The name of the key pair. The name must be unique. The name must be 2 to 128 characters in length and can contain letters, digits, colons (:), underscores (_), and hyphens (-). It must start with a letter. It cannot start with http:// or https://",
      "help_zh": "密钥对名称。必须保持名称唯一性。 长度为2~128个英文或中文字符。必须以大小字母或中文开头，不能以 http:// 和 https:// 开头。可以包含数字、半角冒号（:）、下划线（_）或者连字符（-）",
      "location": "query",
      "name": "key_pair_name",
      "options": [
        "--biz-key-pair-name"
      ],
      "raw_name": "KeyPairName",
      "required": true,
      "type": "string"
    },
    {
      "example": "ABC1234567",
      "help_en": "The public key content of the key pair",
      "help_zh": "密钥对的公钥内容",
      "location": "query",
      "name": "public_key_body",
      "options": [
        "--public-key-body"
      ],
      "raw_name": "PublicKeyBody",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "cloudphone",
  "protocol": "HTTP|HTTPS"
}
