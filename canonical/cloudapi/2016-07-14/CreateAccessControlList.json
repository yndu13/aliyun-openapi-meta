{
  "camel_example": "",
  "cmd_full_name": "cloudapi create-access-control-list",
  "cmd_name": "create-access-control-list",
  "deprecated": false,
  "description_en": "This feature provides instance-level access control capabilities for dedicated instances. Creates an Access Control List (ACL). Each user is allowed to create five ACLs in each region",
  "description_zh": "本功能为API网关专享实例提供实例级别的访问控制能力；\n创建访问控制策略，每个用户每个region下只允许创建5个访问控制策略",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "CreateAccessControlList",
  "operation": {
    "action": "CreateAccessControlList",
    "api_style": "RPC",
    "api_version": "2016-07-14",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "example": "testAcl",
      "help_en": "The name of the ACL. The name must be 1 to 30 characters in length, and can contain letters, digits, periods (.), hyphens (-), forward slashes (/), and underscores (_). The name must be unique within the region",
      "help_zh": "访问控制策略组名称，长度限制为1~30个字符，只支持中文、字母、数字和半角句号（.）、短划线（-）、正斜线（/）和下划线（_）。访问控制策略组名称必须为地域内唯一",
      "location": "query",
      "name": "acl_name",
      "options": [
        "--acl-name"
      ],
      "raw_name": "AclName",
      "required": true,
      "type": "string"
    },
    {
      "example": "ipv4",
      "help_en": "The IP protocol version of the ACL. Valid values:\n\nIPv4\n\nIPv6",
      "help_zh": "访问控制策略组的IP版本。取值：\nIPv4。\nIPv6",
      "location": "query",
      "name": "address_ip_version",
      "options": [
        "--address-ip-version"
      ],
      "raw_name": "AddressIPVersion",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "CloudAPI",
  "protocol": "HTTP|HTTPS"
}
