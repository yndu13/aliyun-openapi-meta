{
  "camel_example": "",
  "cmd_full_name": "agentcore update-agent-im-channel-credential",
  "cmd_name": "update-agent-im-channel-credential",
  "deprecated": false,
  "description_en": "Performs a full update of the channel credential for a specified IM channel of an agent. Secrets are not returned in the response",
  "description_zh": "全量更新指定 IM 渠道的渠道凭据。Secret 不会在响应中回显",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "UpdateAgentIMChannelCredential",
  "operation": {
    "action": "UpdateAgentIMChannelCredential",
    "api_style": "ROA",
    "api_version": "2026-08-04",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "none",
    "url": "/workspaces/{workspaceId}/agents/{agentId}/im-channels/{imChannelId}/actions/update-credential"
  },
  "parameters": [
    {
      "example": "ws-1",
      "help_en": "The workspace ID",
      "help_zh": "工作空间 ID",
      "location": "path",
      "name": "workspace_id",
      "options": [
        "--workspace-id"
      ],
      "raw_name": "workspaceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "agent-1",
      "help_en": "The agent ID",
      "help_zh": "智能体 ID",
      "location": "path",
      "name": "agent_id",
      "options": [
        "--agent-id"
      ],
      "raw_name": "agentId",
      "required": true,
      "type": "string"
    },
    {
      "example": "imc-1",
      "help_en": "The IM channel ID",
      "help_zh": "IM 渠道 ID",
      "location": "path",
      "name": "im_channel_id",
      "options": [
        "--im-channel-id"
      ],
      "raw_name": "imChannelId",
      "required": true,
      "type": "string"
    },
    {
      "fields": [
        {
          "help_en": "The channel credential. All fields must be provided and field values must be non-empty strings. DingTalk uses clientID and clientSecret. Lark uses appId and appSecret. WeCom uses botId and secret",
          "help_zh": "渠道凭据，必须全量传入且字段值为非空字符串。钉钉使用 clientID 和 clientSecret，飞书使用 appId 和 appSecret，企业微信使用 botId 和 secret",
          "name": "credential",
          "options": [
            "--credential"
          ],
          "raw_name": "credential",
          "required": true,
          "type": "map",
          "value": {
            "type": "string"
          }
        }
      ],
      "help_en": "The request body",
      "help_zh": "请求体",
      "location": "formData",
      "name": "body",
      "options": [
        "--biz-body"
      ],
      "param_style": "json",
      "raw_name": "body",
      "required": false,
      "type": "object"
    },
    {
      "example": "client-token-1",
      "help_en": "A reserved idempotency token. The backend does not provide persistent idempotence guarantee in the current version",
      "help_zh": "预留幂等令牌，本期后端不提供持久化幂等保证",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "clientToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/workspaces/[workspaceId]/agents/[agentId]/im-channels/[imChannelId]/actions/update-credential",
  "product_code": "AgentCore",
  "protocol": "HTTPS"
}
