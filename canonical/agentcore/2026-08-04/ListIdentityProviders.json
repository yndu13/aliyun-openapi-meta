{
  "camel_example": "",
  "cmd_full_name": "agentcore list-identity-providers",
  "cmd_name": "list-identity-providers",
  "deprecated": false,
  "description_en": "Queries the external identity provider bound to a specified workspace. Each workspace can be bound to at most one external identity provider, so the response returns at most one record. Application secret configurations are not returned",
  "description_zh": "查询指定工作空间已绑定的外部身份提供商。每个工作空间最多绑定一个外部身份提供商，因此响应最多返回一条记录。应用密钥类配置不会返回",
  "kebab_example": "",
  "method": "GET",
  "multi_version": false,
  "name": "ListIdentityProviders",
  "operation": {
    "action": "ListIdentityProviders",
    "api_style": "ROA",
    "api_version": "2026-08-04",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "list",
    "url": "/workspaces/{workspaceId}/identity-providers"
  },
  "parameters": [
    {
      "example": "ws-123456",
      "help_en": "The workspace ID",
      "help_zh": "工作空间 ID",
      "location": "path",
      "name": "workspace_id",
      "options": [
        "--workspace-id"
      ],
      "raw_name": "workspaceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "10",
      "help_en": "The maximum number of records per page. Valid values: 1 to 100. Default value: 10",
      "help_zh": "单页返回的最大记录数，取值范围为 1~100。不指定该参数时默认返回 10 条",
      "location": "query",
      "name": "max_results",
      "options": [
        "--max-results"
      ],
      "raw_name": "maxResults",
      "required": false,
      "type": "int"
    },
    {
      "example": "aWRlbnRpdHktcHJvdmlkZXItb2Zmc2V0OjEw",
      "help_en": "The pagination token for the next page. Do not specify this parameter for the first request. For subsequent requests, specify the nextToken value returned in the previous response",
      "help_zh": "查询下一页的游标。首次调用不指定该参数，后续调用指定上一次返回的 nextToken",
      "location": "query",
      "name": "next_token",
      "options": [
        "--next-token"
      ],
      "raw_name": "nextToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/workspaces/[workspaceId]/identity-providers",
  "product_code": "AgentCore",
  "protocol": "HTTPS"
}
