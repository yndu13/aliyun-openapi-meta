{
  "camel_example": "",
  "cmd_full_name": "qualitycheck upload-data",
  "cmd_name": "upload-data",
  "deprecated": true,
  "description_en": "Upload offline plain text quality inspection data (plain text sessions). This applies to online agent scenarios. Use the UploadDataV4 API. Differences between UploadDataV4 and UploadData include the following: UploadDataV4 supports only POST requests, and it supports longer JsonStr values",
  "description_zh": "上传离线文本质检数据（纯文本会话）：适用于在线坐席场景。\n推荐使用UploadDataV4接口。\nUploadDataV4与UploadData的差异：1、V4仅支持POST请求。2、V4支持更长的JsonStr",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UploadData",
  "operation": {
    "action": "UploadData",
    "api_style": "RPC",
    "api_version": "2019-01-15",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "xxx",
      "help_en": "JsonStr is a JSON string that contains all custom parameters for this API. See the JsonStr property description below",
      "help_zh": "jsonStr是一个JSON字符串，里面是该接口所有的自定义参数，具体内容参见下方的jsonStr属性说明",
      "location": "query",
      "name": "json_str",
      "options": [
        "--json-str"
      ],
      "raw_name": "JsonStr",
      "required": true,
      "type": "string"
    },
    {
      "example": "123456",
      "help_en": "Workspace ID. Use this to select a specific workspace in multi-workspace scenarios. Defaults to the default workspace",
      "help_zh": "业务空间Id，用于区分多业务空间场景下选择指定业务空间，默认为默认业务空间",
      "location": "query",
      "name": "base_me_agent_id",
      "options": [
        "--base-me-agent-id"
      ],
      "raw_name": "BaseMeAgentId",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "Qualitycheck",
  "protocol": "HTTP|HTTPS"
}
