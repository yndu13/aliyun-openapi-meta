{
  "camel_example": "",
  "cmd_full_name": "qualitycheck upload-audio-data",
  "cmd_name": "upload-audio-data",
  "deprecated": false,
  "description_en": "Uploads offline voice quality inspection data (recording session files). This operation is applicable to hotline agent scenarios. Scenario 1: Natively integrates with Alibaba Cloud Call Center (CCC), requiring no development. You can enable one-click push of call data to SCA. Scenario 2: Integrates with your own call center system. Each time the call center generates a recording, it pushes the recording to SCA for analysis",
  "description_zh": "上传离线语音质检数据（录音会话文件）：适用于热线坐席场景。场景1：天然集成阿里云呼叫中心（CCC），无需开发，可以一键开启推送通话数据到SCA；场景2：与自有呼叫中心系统对接，呼叫中心每产生一条录音，就将录音推送至SCA进行分析",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UploadAudioData",
  "operation": {
    "action": "UploadAudioData",
    "api_style": "RPC",
    "api_version": "2019-01-15",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "{“callList”:“xxxxx”}",
      "help_en": "The complete JSON string. For details about the content, see the following detailed information",
      "help_zh": "完整JSON字符串信息，具体内容参见以下详细信息",
      "location": "query",
      "name": "json_str",
      "options": [
        "--json-str"
      ],
      "raw_name": "JsonStr",
      "required": true,
      "type": "string"
    },
    {
      "example": "123456",
      "help_en": "The business space ID. In multi-business space scenarios, this parameter specifies the business space to use. Default value: the default business space",
      "help_zh": "业务空间Id，用于区分多业务空间场景下选择指定业务空间，默认为默认业务空间",
      "location": "query",
      "name": "base_me_agent_id",
      "options": [
        "--base-me-agent-id"
      ],
      "raw_name": "BaseMeAgentId",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "Qualitycheck",
  "protocol": "HTTP|HTTPS"
}
