{
  "camel_example": "",
  "cmd_full_name": "tablestore describe-regions",
  "cmd_name": "describe-regions",
  "deprecated": false,
  "description_en": "Retrieves a list of available regions",
  "description_zh": "获取可用地域列表",
  "kebab_example": "",
  "method": "GET",
  "multi_version": false,
  "name": "DescribeRegions",
  "operation": {
    "action": "DescribeRegions",
    "api_style": "ROA",
    "api_version": "2020-12-09",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": "/region/DescribeRegions"
  },
  "parameters": [
    {
      "example": "ETnLKlblzczshOTUbOCzxxxxxxxxxx",
      "help_en": "A parameter to ensure idempotence.\n\nThis parameter ensures that each request is processed only once. Generate a unique value on the client for each request. The value can be up to 64 ASCII characters long and must not contain non-ASCII characters",
      "help_zh": "幂等参数。\n用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过 64 个 ASCII 字符，且该参数值中不能包含非 ASCII 字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/region/DescribeRegions",
  "product_code": "Tablestore",
  "protocol": "HTTP|HTTPS"
}
