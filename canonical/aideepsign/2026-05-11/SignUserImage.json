{
  "camel_example": "",
  "cmd_full_name": "aideepsign sign-user-image",
  "cmd_name": "sign-user-image",
  "deprecated": false,
  "description_en": "Embeds a C2PA digital signature into a user-uploaded image and returns the download URL",
  "description_zh": "对用户上传的图片嵌入C2PA数字签名并返回下载地址",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "SignUserImage",
  "operation": {
    "action": "SignUserImage",
    "api_style": "RPC",
    "api_version": "2026-05-11",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "5A2CFF0E-5718-45B5-9D4D-70B3FF3898",
      "help_en": "The client token that is used to ensure the idempotence of the request. The client generates this value. The value must be unique across different requests. ClientToken supports only ASCII characters and cannot exceed 64 characters in length",
      "help_zh": "用于保证请求的幂等性。由客户端生成该参数值，要保证在不同请求间唯一。ClientToken 只支持 ASCII 字符，且不能超过 64 个字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "https://example.com/photo.jpg",
      "help_en": "The URL of the image to be signed. HTTP and HTTPS URLs are supported. You must specify at least one of `ImageUrl` and `ObjectKey`",
      "help_zh": "待签名图片的 URL 地址。支持 HTTP/HTTPS 链接。`ImageUrl` 与 `ObjectKey` 二选一，至少提供一个",
      "location": "query",
      "name": "image_url",
      "options": [
        "--image-url"
      ],
      "raw_name": "ImageUrl",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "AIDeepSign",
  "protocol": "HTTPS"
}
