{
  "camel_example": "",
  "cmd_full_name": "aideepsign detect-aigc-image",
  "cmd_name": "detect-aigc-image",
  "deprecated": false,
  "description_en": "Determines whether an image is AI-generated and returns detection labels and confidence levels",
  "description_zh": "判断图片是否由AI生成，并返回检测标签及置信度",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DetectAigcImage",
  "operation": {
    "action": "DetectAigcImage",
    "api_style": "RPC",
    "api_version": "2026-05-11",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "deepsign/123456789/image-generation/abc12345-def6-7890-abcd-ef1234567890.png",
      "help_en": "The ObjectKey of the image to detect in OSS. When you use ObjectKey, make sure that the key belongs to the namespace of the current caller. You must provide at least one of ImageUrl and ObjectKey",
      "help_zh": "待检测图片在 OSS 中的 ObjectKey。使用 ObjectKey 时需确保该 Key 属于当前调用者的命名空间。ImageUrl 与 ObjectKey 二选一，至少提供一个",
      "location": "query",
      "name": "object_key",
      "options": [
        "--object-key"
      ],
      "raw_name": "ObjectKey",
      "required": false,
      "type": "string"
    },
    {
      "example": "https://example.com/photo.jpg",
      "help_en": "The URL of the image to detect. Only HTTP and HTTPS protocols are supported. You must provide at least one of ImageUrl and ObjectKey",
      "help_zh": "待检测图片的 URL 地址，仅支持 HTTP/HTTPS 协议。`ImageUrl` 与 `ObjectKey` 二选一，至少提供一个",
      "location": "query",
      "name": "image_url",
      "options": [
        "--image-url"
      ],
      "raw_name": "ImageUrl",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "AIDeepSign",
  "protocol": "HTTPS"
}
