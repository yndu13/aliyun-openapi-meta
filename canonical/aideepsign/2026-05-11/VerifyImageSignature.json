{
  "camel_example": "",
  "cmd_full_name": "aideepsign verify-image-signature",
  "cmd_name": "verify-image-signature",
  "deprecated": false,
  "description_en": "Verifies the C2PA digital signature in an image and returns the signature status and issuer information",
  "description_zh": "验证图片中的C2PA数字签名，返回签名状态及签发者信息",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "VerifyImageSignature",
  "operation": {
    "action": "VerifyImageSignature",
    "api_style": "RPC",
    "api_version": "2026-05-11",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "https://example.com/signed-photo.jpg",
      "help_en": "The URL of the image to verify. Specify either ImageUrl or ObjectKey. At least one of them is required",
      "help_zh": "待验证图片的 URL 地址。`ImageUrl` 与 `ObjectKey` 二选一，至少提供一个",
      "location": "query",
      "name": "image_url",
      "options": [
        "--image-url"
      ],
      "raw_name": "ImageUrl",
      "required": false,
      "type": "string"
    },
    {
      "example": "deepsign/123456789/image-generation/abc12345-def6-7890-abcd-ef1234567890.png",
      "help_en": "The ObjectKey of the image in OSS. When you use ObjectKey, ensure that the key belongs to the namespace of the current caller. Specify either ImageUrl or ObjectKey. At least one of them is required",
      "help_zh": "待验证图片在 OSS 中的 `ObjectKey`。使用 `ObjectKey` 时需确保该 Key 属于当前调用者的命名空间。`ImageUrl` 与 `ObjectKey` 二选一，至少提供一个",
      "location": "query",
      "name": "object_key",
      "options": [
        "--object-key"
      ],
      "raw_name": "ObjectKey",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "AIDeepSign",
  "protocol": "HTTPS"
}
