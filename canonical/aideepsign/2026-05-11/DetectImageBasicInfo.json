{
  "camel_example": "",
  "cmd_full_name": "aideepsign detect-image-basic-info",
  "cmd_name": "detect-image-basic-info",
  "deprecated": false,
  "description_en": "Retrieves basic information about a specified image, such as the file name, format, size, and resolution",
  "description_zh": "获取指定图片的文件名、格式、大小和分辨率等基本信息",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "DetectImageBasicInfo",
  "operation": {
    "action": "DetectImageBasicInfo",
    "api_style": "RPC",
    "api_version": "2026-05-11",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "https://example.com/photo.jpg",
      "help_en": "The URL of the image. Only HTTP and HTTPS protocols are supported. Specify either ImageUrl or ObjectKey. At least one of them is required",
      "help_zh": "图片的 URL 地址，仅支持 HTTP/HTTPS 协议。`ImageUrl` 与 `ObjectKey` 二选一，至少提供一个",
      "location": "query",
      "name": "image_url",
      "options": [
        "--image-url"
      ],
      "raw_name": "ImageUrl",
      "required": false,
      "type": "string"
    },
    {
      "example": "deepsign/123456789/image-generation/abc12345-def6-7890-abcd-ef1234567890.png",
      "help_en": "The ObjectKey of the image in OSS. When using ObjectKey, ensure that the key belongs to the namespace of the current caller. Specify either ImageUrl or ObjectKey. At least one of them is required",
      "help_zh": "图片在 OSS 中的 `ObjectKey`。使用 `ObjectKey` 时需确保该 Key 属于当前调用者的命名空间。`ImageUrl` 与 `ObjectKey` 二选一，至少提供一个",
      "location": "query",
      "name": "object_key",
      "options": [
        "--object-key"
      ],
      "raw_name": "ObjectKey",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "AIDeepSign",
  "protocol": "HTTPS"
}
