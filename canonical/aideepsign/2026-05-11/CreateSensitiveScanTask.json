{
  "camel_example": "",
  "cmd_full_name": "aideepsign create-sensitive-scan-task",
  "cmd_name": "create-sensitive-scan-task",
  "deprecated": false,
  "description_en": "Creates an image sensitive information scan task and returns the task ID",
  "description_zh": "用于创建图片敏感信息扫描任务并返回任务ID",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CreateSensitiveScanTask",
  "operation": {
    "action": "CreateSensitiveScanTask",
    "api_style": "RPC",
    "api_version": "2026-05-11",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "example": "5A2CFF0E-5718-45B5-9D4D-70B3FF3898",
      "help_en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length",
      "help_zh": "用于保证请求的幂等性。由客户端生成该参数值，要保证在不同请求间唯一。`ClientToken` 只支持 ASCII 字符，且不能超过 64 个字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "deepsign/123456789/scan/abc12345.jpg",
      "help_en": "The ObjectKey of the image to scan in OSS. When you use ObjectKey, make sure that the key belongs to the namespace of the current caller. You must specify at least one of ImageUrl and ObjectKey",
      "help_zh": "待扫描图片在 OSS 中的 `ObjectKey`。使用 `ObjectKey` 时需确保该 Key 属于当前调用者的命名空间。`ImageUrl` 与 `ObjectKey` 二选一，至少提供一个",
      "location": "query",
      "name": "object_key",
      "options": [
        "--object-key"
      ],
      "raw_name": "ObjectKey",
      "required": false,
      "type": "string"
    },
    {
      "example": "https://example.com/document.jpg",
      "help_en": "The URL of the image to scan. Only HTTP and HTTPS protocols are supported. The image size cannot exceed 10 MB. You must specify at least one of ImageUrl and ObjectKey",
      "help_zh": "待扫描图片的 URL 地址，仅支持 HTTP/HTTPS 协议。图片大小不超过 10 MB。`ImageUrl` 与 `ObjectKey` 二选一，至少提供一个",
      "location": "query",
      "name": "image_url",
      "options": [
        "--image-url"
      ],
      "raw_name": "ImageUrl",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "AIDeepSign",
  "protocol": "HTTPS"
}
