{
  "camel_example": "",
  "cmd_full_name": "agentteams update-service-endpoint",
  "cmd_name": "update-service-endpoint",
  "deprecated": false,
  "description_en": "Updates the domain name and SSL certificate information for a specified endpoint",
  "description_zh": "用于更新指定Endpoint的域名和SSL证书信息",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "UpdateServiceEndpoint",
  "operation": {
    "action": "UpdateServiceEndpoint",
    "api_style": "RPC",
    "api_version": "2026-06-05",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "update",
    "url": "/"
  },
  "parameters": [
    {
      "example": "mep-abc123",
      "help_en": "The globally unique endpoint ID",
      "help_zh": "Endpoint ID，全局唯一",
      "location": "query",
      "name": "endpoint_id",
      "options": [
        "--endpoint-id"
      ],
      "raw_name": "EndpointId",
      "required": true,
      "type": "string"
    },
    {
      "example": "api.example.com",
      "help_en": "The new domain name. If this parameter is not specified, the existing domain name is retained",
      "help_zh": "新域名。不传则保持原域名",
      "location": "query",
      "name": "domain",
      "options": [
        "--domain"
      ],
      "raw_name": "Domain",
      "required": false,
      "type": "string"
    },
    {
      "example": "22584627-cn-hangzhou",
      "help_en": "The new SSL certificate identifier. If this parameter is not specified, the existing certificate is retained",
      "help_zh": "新 SSL 证书标识。不传则保持原证书",
      "location": "query",
      "name": "cert_identifier",
      "options": [
        "--cert-identifier"
      ],
      "raw_name": "CertIdentifier",
      "required": false,
      "type": "string"
    },
    {
      "example": "client-token-xxx",
      "help_en": "The idempotency token that is used to ensure the idempotence of the request",
      "help_zh": "幂等性 Token，用于保证请求幂等性",
      "location": "formData",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "agentteams-demo",
      "help_en": "The instance ID",
      "help_zh": "实例 ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "/",
  "product_code": "AgentTeams",
  "protocol": "HTTPS"
}
