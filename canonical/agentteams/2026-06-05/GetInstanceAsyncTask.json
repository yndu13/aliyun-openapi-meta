{
  "camel_example": "",
  "cmd_full_name": "agentteams get-instance-async-task",
  "cmd_name": "get-instance-async-task",
  "deprecated": false,
  "description_en": "Queries the status of asynchronous tasks associated with a specified AgentTeams instance, with pagination support",
  "description_zh": "用于查询指定AgentTeams实例关联的异步任务状态，支持分页",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "GetInstanceAsyncTask",
  "operation": {
    "action": "GetInstanceAsyncTask",
    "api_style": "RPC",
    "api_version": "2026-06-05",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": "/"
  },
  "parameters": [
    {
      "example": "at-xxx",
      "help_en": "The ID of the AgentTeams instance",
      "help_zh": "AgentTeams 实例 ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "LIFECYCLE_MAGIC_PAY_ORDER_CALLBACK_CREATE",
      "help_en": "The task code. If not specified, all asynchronous tasks are queried. You can pass in a `TaskCode` to filter results. To query multiple task types, submit separate requests.\n\nCurrently supported `taskCode` values include: `CreateInstance`, `RenewInstance`, `UpdateInstance`, `ReleaseInstance`, `ConfigureNatGateway`, `UpgradeInstance`, `CreateWorkerEndpoint`, and `DeleteWorkerEndpoint`.\n\nLegacy task codes such as `LIFECYCLE_MAGIC_PAY_ORDER_CALLBACK_CREATE` are supported for backward compatibility, but `CreateInstance` is recommended",
      "help_zh": "任务 Code。不传默认查询全部异步任务；支持传入`TaskCode` 进行过滤，多个任务类型需分别查询。\n\n当前支持的 `taskCode` 包括：`CreateInstance`、`RenewInstance`、`UpdateInstance`、`ReleaseInstance`、`ConfigureNatGateway`、`UpgradeInstance`、`CreateWorkerEndpoint`、`DeleteWorkerEndpoint`。\n\n兼容历史任务码，例如 `LIFECYCLE_MAGIC_PAY_ORDER_CALLBACK_CREATE`，但推荐使用 `CreateInstance`",
      "location": "query",
      "name": "task_code",
      "options": [
        "--task-code"
      ],
      "raw_name": "TaskCode",
      "required": false,
      "type": "string"
    },
    {
      "example": "100",
      "help_en": "The page size. Default value: `100`. Valid values: `1` to `100`",
      "help_zh": "分页大小，默认 `100`，范围 `1..100`",
      "location": "query",
      "name": "max_results",
      "options": [
        "--max-results"
      ],
      "raw_name": "MaxResults",
      "required": false,
      "type": "int"
    },
    {
      "example": "null",
      "help_en": "The token for the next page. Do not specify this parameter for the first request",
      "help_zh": "下一页 token，首次查询不传",
      "location": "query",
      "name": "next_token",
      "options": [
        "--next-token"
      ],
      "raw_name": "NextToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "PAUSED",
      "help_en": "The task status filter",
      "help_zh": "任务状态过滤",
      "location": "query",
      "name": "status",
      "options": [
        "--status"
      ],
      "raw_name": "Status",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/",
  "product_code": "AgentTeams",
  "protocol": "HTTPS"
}
