{
  "camel_example": "",
  "cmd_full_name": "agentteams list-identity-providers",
  "cmd_name": "list-identity-providers",
  "deprecated": false,
  "description_en": "Queries the list of upstream identity provider bindings for a specified instance, with support for paginated responses",
  "description_zh": "用于查询指定实例的上游身份提供商绑定列表，支持分页返回",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ListIdentityProviders",
  "operation": {
    "action": "ListIdentityProviders",
    "api_style": "RPC",
    "api_version": "2026-06-05",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "list",
    "url": ""
  },
  "parameters": [
    {
      "example": "agentteams-xxx",
      "help_en": "The instance ID",
      "help_zh": "实例 ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "10",
      "help_en": "The maximum number of entries to return in this query. Valid values: 1 to 100. Default value: 10",
      "help_zh": "本次查询返回的最大数量，取值范围 1 至 100，不传时默认为 10",
      "location": "query",
      "name": "max_results",
      "options": [
        "--max-results"
      ],
      "raw_name": "MaxResults",
      "required": false,
      "type": "int"
    },
    {
      "example": "10",
      "help_en": "The pagination token that indicates the offset from which to start the query. The value must be a non-negative integer. Do not specify this parameter for the first query",
      "help_zh": "分页游标，表示从哪个偏移量开始查询，需为非负整数，首次查询不传",
      "location": "query",
      "name": "next_token",
      "options": [
        "--next-token"
      ],
      "raw_name": "NextToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "AgentTeams",
  "protocol": "HTTPS"
}
