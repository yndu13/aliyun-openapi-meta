{
  "camel_example": "",
  "cmd_full_name": "agentteams list-credentials",
  "cmd_name": "list-credentials",
  "deprecated": false,
  "description_en": "Queries the credential list under a specified AgentTeams instance with paging, returning credential summary information and the number of Workers attached to each credential",
  "description_zh": "用于分页查询指定 AgentTeams 实例下的凭证列表，返回凭证摘要信息及各自绑定的 Worker 数量",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ListCredentials",
  "operation": {
    "action": "ListCredentials",
    "api_style": "RPC",
    "api_version": "2026-06-05",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "list",
    "url": ""
  },
  "parameters": [
    {
      "example": "ami-2ze8x9c6f",
      "help_en": "The AgentTeams instance ID",
      "help_zh": "AgentTeams 实例 ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "10",
      "help_en": "The maximum number of entries per page. Valid values: 1 to 100. Default value: 10",
      "help_zh": "每页返回的最大数量，取值范围 1-100，默认 10",
      "location": "query",
      "name": "max_results",
      "options": [
        "--max-results"
      ],
      "raw_name": "MaxResults",
      "required": false,
      "type": "int"
    },
    {
      "example": "0",
      "help_en": "The pagination token. Leave this parameter empty for the first request. For subsequent requests, use the NextToken value returned in the previous response",
      "help_zh": "分页令牌，首次调用留空，后续使用上一次返回的 NextToken 值",
      "location": "query",
      "name": "next_token",
      "options": [
        "--next-token"
      ],
      "raw_name": "NextToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "OPENAI",
      "help_en": "The keyword for fuzzy match of the credential name",
      "help_zh": "凭证名称模糊匹配关键字",
      "location": "query",
      "name": "name_like",
      "options": [
        "--name-like"
      ],
      "raw_name": "NameLike",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "AgentTeams",
  "protocol": "HTTPS"
}
