{
  "camel_example": "",
  "cmd_full_name": "agentteams create-service-endpoint",
  "cmd_name": "create-service-endpoint",
  "deprecated": false,
  "description_en": "Creates an endpoint for a specified instance. Multiple component types and gateway types are supported",
  "description_zh": "用于创建指定实例的Endpoint，支持多种组件和网关类型",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "CreateServiceEndpoint",
  "operation": {
    "action": "CreateServiceEndpoint",
    "api_style": "RPC",
    "api_version": "2026-06-05",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "create",
    "url": "/"
  },
  "parameters": [
    {
      "example": "ins-001",
      "help_en": "The instance ID",
      "help_zh": "实例 ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "WORKER",
      "help_en": "The component type. Valid values: `ELEMENT`, `TUNNEL`, `MATRIX`, and `WORKER`",
      "help_zh": "组件类型：`ELEMENT` / `TUNNEL` / `MATRIX` / `WORKER`",
      "location": "query",
      "name": "component",
      "options": [
        "--component"
      ],
      "raw_name": "Component",
      "required": true,
      "type": "string"
    },
    {
      "example": "cert-001",
      "help_en": "The certificate ID",
      "help_zh": "证书 ID",
      "location": "query",
      "name": "cert_identifier",
      "options": [
        "--cert-identifier"
      ],
      "raw_name": "CertIdentifier",
      "required": false,
      "type": "string"
    },
    {
      "example": "my-service",
      "help_en": "The service name",
      "help_zh": "服务名称",
      "location": "query",
      "name": "resource_name",
      "options": [
        "--resource-name"
      ],
      "raw_name": "ResourceName",
      "required": false,
      "type": "string"
    },
    {
      "example": "matrix.example.com",
      "help_en": "The custom domain name. The server trims and converts the value to lowercase",
      "help_zh": "自定义域名，服务端会进行 trim 和 lowercase 处理",
      "location": "query",
      "name": "domain",
      "options": [
        "--domain"
      ],
      "raw_name": "Domain",
      "required": false,
      "type": "string"
    },
    {
      "example": "client-token-xxx",
      "help_en": "The idempotency token that ensures the idempotence of the request",
      "help_zh": "幂等性 Token，用于保证请求幂等性",
      "location": "formData",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/",
  "product_code": "AgentTeams",
  "protocol": "HTTPS"
}
