{
  "camel_example": "",
  "cmd_full_name": "agentteams update-instance",
  "cmd_name": "update-instance",
  "deprecated": false,
  "description_en": "Changes the name of a specified AgentTeams instance. This operation supports GET and POST methods. You can only modify the instance name through this operation. You cannot change the namespace through this operation",
  "description_zh": "用于更改指定 AgentTeams 实例的名称，支持通过 GET 或 POST 方法调用。该接口仅允许修改实例名称，不允许通过此接口变更命名空间",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "UpdateInstance",
  "operation": {
    "action": "UpdateInstance",
    "api_style": "RPC",
    "api_version": "2026-06-05",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "update",
    "url": "/"
  },
  "parameters": [
    {
      "example": "agentteams-abc123",
      "help_en": "The ID of the AgentTeams instance",
      "help_zh": "AgentTeams 实例 ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "新的实例名称",
      "help_en": "The new instance name. This parameter cannot be empty",
      "help_zh": "新实例名称，不能为空",
      "location": "query",
      "name": "instance_name",
      "options": [
        "--instance-name"
      ],
      "raw_name": "InstanceName",
      "required": false,
      "type": "string"
    },
    {
      "example": "client-token-xxx",
      "help_en": "The idempotency token that is used to ensure the idempotence of the request",
      "help_zh": "幂等令牌，用于保证请求幂等性",
      "location": "formData",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "element": {
        "fields": [
          {
            "example": "vsw-bp1xxxx",
            "help_en": "The vSwitch ID",
            "help_zh": "虚拟交换机 ID",
            "name": "vswitch_id",
            "options": [
              "--vswitch-id"
            ],
            "raw_name": "VSwitchId",
            "required": false,
            "type": "string"
          },
          {
            "example": "cn-hangzhou-h",
            "help_en": "The zone ID",
            "help_zh": "可用区 ID",
            "name": "zone_id",
            "options": [
              "--zone-id"
            ],
            "raw_name": "ZoneId",
            "required": false,
            "type": "string"
          }
        ],
        "type": "object"
      },
      "example": "[...]",
      "help_en": "The list of zones and vSwitches",
      "help_zh": "可用区及交换机列表",
      "location": "query",
      "name": "zones",
      "options": [
        "--zones"
      ],
      "param_style": "json",
      "raw_name": "Zones",
      "required": false,
      "type": "array"
    },
    {
      "enum": [
        "PrivatePubNet",
        "PrivateNet"
      ],
      "example": "PrivateNet",
      "help_en": "The network type",
      "help_zh": "网络类型",
      "location": "query",
      "name": "network_type",
      "options": [
        "--network-type"
      ],
      "raw_name": "NetworkType",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/",
  "product_code": "AgentTeams",
  "protocol": "HTTPS"
}
