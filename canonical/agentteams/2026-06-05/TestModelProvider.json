{
  "camel_example": "",
  "cmd_full_name": "agentteams test-model-provider",
  "cmd_name": "test-model-provider",
  "deprecated": false,
  "description_en": "Tests the connectivity of an AI model provider and model under a specified AgentTeams instance by sending a test prompt and returning the call result, latency, and token usage",
  "description_zh": "用于测试指定 AgentTeams 实例下 AI 模型供应商和模型的连通性，发送一条测试 Prompt 并返回调用结果、延迟和 Token 用量",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "TestModelProvider",
  "operation": {
    "action": "TestModelProvider",
    "api_style": "RPC",
    "api_version": "2026-06-05",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "i-xxx",
      "help_en": "The instance ID",
      "help_zh": "实例 ID",
      "location": "formData",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "RUNNING",
      "help_en": "The provider ID. Optional. Filters by exact match on instance status",
      "help_zh": "可选，实例状态精确匹配",
      "location": "formData",
      "name": "provider_id",
      "options": [
        "--provider-id"
      ],
      "raw_name": "ProviderId",
      "required": true,
      "type": "string"
    },
    {
      "example": "AgentTeams",
      "help_en": "The model name. Optional. Filters by fuzzy match on instance name",
      "help_zh": "可选，按实例名称模糊匹配",
      "location": "formData",
      "name": "model_name",
      "options": [
        "--model-name"
      ],
      "raw_name": "ModelName",
      "required": true,
      "type": "string"
    },
    {
      "example": "Hello, how are you?",
      "help_en": "The content of the test prompt",
      "help_zh": "测试 Prompt 内容",
      "location": "formData",
      "name": "prompt",
      "options": [
        "--prompt"
      ],
      "raw_name": "Prompt",
      "required": true,
      "type": "string"
    },
    {
      "example": "my-provider",
      "help_en": "The name of the model provider",
      "help_zh": "模型供应商名称",
      "location": "formData",
      "name": "provider_name",
      "options": [
        "--provider-name"
      ],
      "raw_name": "ProviderName",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "AgentTeams",
  "protocol": "HTTPS"
}
