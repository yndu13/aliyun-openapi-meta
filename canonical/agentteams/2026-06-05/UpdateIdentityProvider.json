{
  "camel_example": "",
  "cmd_full_name": "agentteams update-identity-provider",
  "cmd_name": "update-identity-provider",
  "deprecated": false,
  "description_en": "Updates the upstream identity provider configuration bound to a specified instance. You can adjust the logon switch and user synchronization switch",
  "description_zh": "用于更新指定实例已绑定的上游身份提供商配置，支持调整登录开关与用户同步开关",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UpdateIdentityProvider",
  "operation": {
    "action": "UpdateIdentityProvider",
    "api_style": "RPC",
    "api_version": "2026-06-05",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "agentteams-xxx",
      "help_en": "The instance ID",
      "help_zh": "实例 ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to enable logon. If this parameter is not specified, the existing configuration is retained",
      "help_zh": "是否开启登录，不传时保持原配置不变",
      "location": "query",
      "name": "login_enabled",
      "options": [
        "--login-enabled"
      ],
      "raw_name": "LoginEnabled",
      "required": false,
      "type": "bool"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to enable user synchronization. If this parameter is not specified, the existing configuration is retained",
      "help_zh": "是否开启用户同步，不传时保持原配置不变",
      "location": "query",
      "name": "sync_enabled",
      "options": [
        "--sync-enabled"
      ],
      "raw_name": "SyncEnabled",
      "required": false,
      "type": "bool"
    },
    {
      "example": "ct-1234abcd",
      "help_en": "The client idempotency token that is used to ensure the idempotence of the request",
      "help_zh": "客户端幂等令牌，用于保证请求幂等性",
      "location": "formData",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "DingTalk",
      "help_en": "The identity provider type. Currently, only DingTalk is supported",
      "help_zh": "身份提供商类型，目前仅支持钉钉",
      "location": "query",
      "name": "identity_provider_type",
      "options": [
        "--identity-provider-type"
      ],
      "raw_name": "IdentityProviderType",
      "required": true,
      "type": "string"
    },
    {
      "example": "{\"appKey\":\"dingxxxx\",\"appSecret\":\"xxxxx\",\"corpId\":\"dingxxxx\",\"encryptKey\":\"xxxxx\",\"verificationToken\":\"xxxxx\"}",
      "help_en": "The identity provider metadata. In the DingTalk scenario, this is a JSON object string that contains the appKey, appSecret, corpId, encryptKey, and verificationToken fields",
      "help_zh": "身份提供商元数据，钉钉场景下为 JSON 对象字符串，包含 appKey、appSecret、corpId、encryptKey、verificationToken 字段",
      "location": "query",
      "name": "idp_metadata",
      "options": [
        "--idp-metadata"
      ],
      "raw_name": "IdpMetadata",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "AgentTeams",
  "protocol": "HTTPS"
}
