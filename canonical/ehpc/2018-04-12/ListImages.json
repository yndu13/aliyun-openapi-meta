{
  "camel_example": "",
  "cmd_full_name": "ehpc list-images",
  "cmd_name": "list-images",
  "deprecated": false,
  "description_en": "Queries the list of images that can be installed in a cluster",
  "description_zh": "调用ListImages查询E-HPC支持的镜像列表",
  "kebab_example": "",
  "method": "GET",
  "multi_version": true,
  "name": "ListImages",
  "operation": {
    "action": "ListImages",
    "api_style": "RPC",
    "api_version": "2018-04-12",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "url": ""
  },
  "parameters": [
    {
      "example": "CentOS_7.2_64",
      "help_en": "The image tag of the operating system. The tag is used only for management nodes",
      "help_zh": "基础镜像标签，仅管控节点使用",
      "location": "query",
      "name": "base_os_tag",
      "options": [
        "--base-os-tag"
      ],
      "raw_name": "BaseOsTag",
      "required": false,
      "type": "string"
    },
    {
      "example": "ecs.n1.medium",
      "help_en": "The instance type.\n\nIf a value is passed to the parameter, the list of images that are supported by the specified instance type is queried.\nIf no value is passed to the parameter, the list of images that are supported by all instance types is queried",
      "help_zh": "实例规格。\n该参数传入值，查询指定实例规格支持安装的镜像信息。\n该参数不传值，默认查询所有实例规格支持安装的镜像信息",
      "location": "query",
      "name": "instance_type",
      "options": [
        "--instance-type"
      ],
      "raw_name": "InstanceType",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "EHPC",
  "protocol": "HTTP|HTTPS"
}
