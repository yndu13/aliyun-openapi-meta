{
  "camel_example": "",
  "cmd_full_name": "ehpc list-community-images",
  "cmd_name": "list-community-images",
  "deprecated": false,
  "description_en": "Queries a list of community images",
  "description_zh": "查询社区镜像",
  "kebab_example": "",
  "method": "GET",
  "multi_version": true,
  "name": "ListCommunityImages",
  "operation": {
    "action": "ListCommunityImages",
    "api_style": "RPC",
    "api_version": "2018-04-12",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "url": ""
  },
  "parameters": [
    {
      "example": "CentOS_7.2_64",
      "help_en": "The tag of the base operating system (BOS)",
      "help_zh": "基础镜像操作系统标签",
      "location": "query",
      "name": "base_os_tag",
      "options": [
        "--base-os-tag"
      ],
      "raw_name": "BaseOsTag",
      "required": false,
      "type": "string"
    },
    {
      "example": "ecs.n1.tiny",
      "help_en": "The type of the Elastic Compute Service (ECS) instance. If you do not specify the instance type, the community images that are supported by all instance types are queried",
      "help_zh": "指定实例规格。如果不指定实例规格，则默认查询所有实例规格支持的镜像列表",
      "location": "query",
      "name": "instance_type",
      "options": [
        "--instance-type"
      ],
      "raw_name": "InstanceType",
      "required": false,
      "type": "string"
    },
    {
      "example": "ehpc-hz-FYUr32****",
      "help_en": "The ID of the cluster. If the cluster supports multiple operating systems, all community images in the region where the cluster resides are queried.\n\nIf you do not specify the cluster ID, the community images that are supported by all clusters are queried",
      "help_zh": "指定集群ID。如果该集群支持多操作系统，则查询集群所在地域下的所有镜像列表。\n\n如果不指定集群ID，则默认查询所有集群支持的镜像列表",
      "location": "query",
      "name": "cluster_id",
      "options": [
        "--cluster-id"
      ],
      "raw_name": "ClusterId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "EHPC",
  "protocol": "HTTP|HTTPS"
}
