{
  "camel_example": "",
  "cmd_full_name": "elasticsearch delete-instance",
  "cmd_name": "delete-instance",
  "deprecated": false,
  "description_en": "All physical resources used by the instance are reclaimed, all related data is permanently lost and unrecoverable, and the cloud disks attached to the instance nodes along with their corresponding snapshots are released",
  "description_zh": "实例所使用的物理资源都被回收，相关数据全部丢失且不可恢复；挂载实例节点的云盘和相应的快照都会被释放",
  "kebab_example": "",
  "method": "DELETE",
  "multi_version": true,
  "name": "DeleteInstance",
  "operation": {
    "action": "DeleteInstance",
    "api_style": "ROA",
    "api_version": "2017-06-13",
    "method": "DELETE",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": "/openapi/instances/{InstanceId}"
  },
  "parameters": [
    {
      "docRequired": true,
      "example": "es-cn-t57p81n7ai89v****",
      "help_en": "The instance ID",
      "help_zh": "实例ID",
      "location": "path",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "5A2CFF0E-5718-45B5-9D4D-70B3FF****",
      "help_en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the value, but you must make sure that the value is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length",
      "help_zh": "用于保证请求的幂等性。由客户端生成该参数值，要保证在不同请求间唯一，最大不超过64个ASCII字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "clientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "protective",
      "help_en": "The release type. Valid values:\n\nimmediate: The instance is immediately deleted. After deletion, the system permanently clears all data, and the instance no longer appears in the instance list.\nprotective: The instance is frozen for 24 hours before the data is permanently cleared. During this period, the instance still appears in the instance list, and you can choose to https://help.aliyun.com/document_detail/202195.html or https://help.aliyun.com/document_detail/202195.html",
      "help_zh": "释放类型。可选值：\n\nimmediate：立即删除。删除后，系统会彻底清除所有数据，且实例不再显示在实例列表中。\nprotective：实例会被冻结24小时后，再彻底清除数据，期间实例仍在实例列表中显示，您可以选择https://help.aliyun.com/document_detail/202195.html或https://help.aliyun.com/document_detail/202195.html",
      "location": "query",
      "name": "delete_type",
      "options": [
        "--delete-type"
      ],
      "raw_name": "deleteType",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/openapi/instances/[InstanceId]",
  "product_code": "elasticsearch",
  "protocol": "HTTP|HTTPS"
}
