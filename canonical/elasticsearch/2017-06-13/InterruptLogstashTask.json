{
  "camel_example": "",
  "cmd_full_name": "elasticsearch interrupt-logstash-task",
  "cmd_name": "interrupt-logstash-task",
  "deprecated": false,
  "description_en": "After the interruption, the instance enters the suspended state",
  "description_zh": "中断后，实例会进入中断中（suspended）状态",
  "kebab_example": "",
  "method": "POST",
  "multi_version": true,
  "name": "InterruptLogstashTask",
  "operation": {
    "action": "InterruptLogstashTask",
    "api_style": "ROA",
    "api_version": "2017-06-13",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": "/openapi/logstashes/{InstanceId}/actions/interrupt"
  },
  "parameters": [
    {
      "example": "ls-cn-4591f1y6****",
      "help_en": "The instance ID",
      "help_zh": "实例ID",
      "location": "path",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "5A2CFF0E-5718-45B5-9D4D-70B3FF****",
      "help_en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length",
      "help_zh": "用于保证请求的幂等性。由客户端生成该参数值，要保证在不同请求间唯一，最大不超过64个ASCII字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "clientToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/openapi/logstashes/[InstanceId]/actions/interrupt",
  "product_code": "elasticsearch",
  "protocol": "HTTP|HTTPS"
}
