{
  "camel_example": "",
  "cmd_full_name": "das get-auto-increment-usage-statistic",
  "cmd_name": "get-auto-increment-usage-statistic",
  "deprecated": false,
  "description_en": "Queries the usage of auto-increment table IDs",
  "description_zh": "调用GetAutoIncrementUsageStatistic接口，获取表自增ID的使用数据",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "GetAutoIncrementUsageStatistic",
  "operation": {
    "action": "GetAutoIncrementUsageStatistic",
    "api_style": "RPC",
    "api_version": "2020-01-16",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "rm-2ze8g2am97624****",
      "help_en": "The instance ID",
      "help_zh": "实例ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "['db1','db2']",
      "help_en": "The database name. If you specify a database, the operation queries the usage of auto-increment table IDs in the specified database. Otherwise, the operation queries the usage of auto-increment table IDs in all databases on the instance.\n\nSpecify the parameter value as a JSON array, such as ['db1','db2']. Separate multiple database names with commas (,)",
      "help_zh": "数据库名。传入此参数时，查询指定数据库中表自增ID使用情况，不传入时查询实例上所有数据库的表自增ID使用情况。\n数据格式为JSONArray，例如['db1','db2']，数据库名间用英文逗号（,）分隔",
      "location": "query",
      "name": "db_names",
      "options": [
        "--db-names"
      ],
      "raw_name": "DbNames",
      "required": false,
      "type": "string"
    },
    {
      "example": "0.9",
      "help_en": "The usage threshold of auto-increment IDs. Only usage that exceeds the threshold can be returned. Valid values are decimals that range from 0 to 1",
      "help_zh": "自增ID使用比例过滤值，只返回超过该比例的自增ID使用数据。取值为0~1的小数",
      "location": "query",
      "name": "ratio_filter",
      "options": [
        "--ratio-filter"
      ],
      "raw_name": "RatioFilter",
      "required": true,
      "type": "float"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to query real-time data. Valid values:\n\ntrue: queries data in real time except for data generated in the last 10 minutes.\\\\\n\nfalse: queries data generated in the last 2 hours. If no such data exists, queries the latest data",
      "help_zh": "是否获取实时数据：\n\ntrue：实时查询实例上数据并计算。最小查询时间粒度为10分钟，即有10分钟内的数据时，即使传递true也不进行实时查询。\nfalse：当有近两小时的数据时，返回该数据，否则查询实例上最新数据",
      "location": "query",
      "name": "real_time",
      "options": [
        "--real-time"
      ],
      "raw_name": "RealTime",
      "required": true,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "DAS",
  "protocol": "HTTP|HTTPS"
}
