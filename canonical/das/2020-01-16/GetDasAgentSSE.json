{
  "camel_example": "",
  "cmd_full_name": "das get-das-agent-sse",
  "cmd_name": "get-das-agent-sse",
  "deprecated": false,
  "description_en": "This API uses Server-Sent Events (SSE) to interact with the DAS agent. You can use this API for features such as Q&A and performance diagnostics",
  "description_zh": "DAS大模型交互SSE接口。可以进行知识问答，性能诊断等功能",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "GetDasAgentSSE",
  "operation": {
    "action": "GetDasAgentSSE",
    "api_style": "RPC",
    "api_version": "2020-01-16",
    "is_sse": true,
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": "/getDasAgentSSE"
  },
  "parameters": [
    {
      "example": "Are there any issues or abnormalities with my instance rm-xxx?",
      "help_en": "The natural language query for the agent",
      "help_zh": "问答自然语言描述",
      "location": "query",
      "name": "query",
      "options": [
        "--query"
      ],
      "raw_name": "Query",
      "required": true,
      "type": "string"
    },
    {
      "example": "rm-xxxx",
      "help_en": "This parameter is deprecated. Specify the instance ID in the Query parameter",
      "help_zh": "废弃参数， 实例Id通过Query字段传入",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": false,
      "type": "string"
    },
    {
      "example": "123e4567-e89b-12d3-a456-xxxxxxxxxxxx",
      "help_en": "The session ID, which must be in the UUID format. This parameter is optional. If you do not provide a session ID, the service creates a new session. To continue an existing conversation, use the same session ID in subsequent requests",
      "help_zh": "可选，会话Id，格式为UUID字符串，未传入会创建新的会话，上下文对话需保持同一个会话Id",
      "location": "query",
      "name": "session_id",
      "options": [
        "--session-id"
      ],
      "raw_name": "SessionId",
      "required": false,
      "type": "string"
    },
    {
      "example": "ag-472T0DxtmjIxxxxx",
      "help_en": "The ID of the agent to use. This parameter is optional. If you do not specify this parameter, the service uses the default agent. You can provide the ID of an agent that was generated when you enabled the DAS Agent service or that you manually created",
      "help_zh": "可选，默认使用默认Agent进行服务，或者传入开启DASAgent服务后生成的或者主动创建的Agent",
      "location": "query",
      "name": "agent_id",
      "options": [
        "--agent-id"
      ],
      "raw_name": "AgentId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/getDasAgentSSE",
  "product_code": "DAS",
  "protocol": "HTTPS|SSE"
}
