{
  "camel_example": "",
  "cmd_full_name": "ram list-users-for-group",
  "cmd_name": "list-users-for-group",
  "deprecated": false,
  "description_en": "Queries Resource Access Management (RAM) users in a RAM user group",
  "description_zh": "调用ListUsersForGroup接口列出指定用户组所包含的RAM用户",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ListUsersForGroup",
  "operation": {
    "action": "ListUsersForGroup",
    "api_style": "RPC",
    "api_version": "2015-05-01",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "Dev-Team",
      "help_en": "The name of the RAM user group",
      "help_zh": "用户组名称",
      "location": "query",
      "name": "group_name",
      "options": [
        "--group-name"
      ],
      "raw_name": "GroupName",
      "required": true,
      "type": "string"
    },
    {
      "example": "EXAMPLE",
      "help_en": "The `marker`. If part of a previous response is truncated, you can use this parameter to obtain the truncated part",
      "help_zh": "当请求的返回结果被截断时，可以使用`Marker`获取从当前截断位置之后的内容",
      "location": "query",
      "name": "marker",
      "options": [
        "--marker"
      ],
      "raw_name": "Marker",
      "required": false,
      "type": "string"
    },
    {
      "example": "100",
      "help_en": "The number of entries to return. If a response is truncated because it reaches the value of `MaxItems`, the value of `IsTruncated` will be `true`.\n\nValid values: 1 to 1000. Default value: 100",
      "help_zh": "指定返回结果的条数。当返回结果达到`MaxItems`限制被截断时，返回参数`IsTruncated`将等于`true`。\n\n取值范围：1 ~ 1000，默认值：100",
      "location": "query",
      "name": "max_items",
      "options": [
        "--max-items"
      ],
      "raw_name": "MaxItems",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "Ram",
  "protocol": "HTTPS"
}
