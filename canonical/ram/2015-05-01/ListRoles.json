{
  "camel_example": "",
  "cmd_full_name": "ram list-roles",
  "cmd_name": "list-roles",
  "deprecated": false,
  "description_en": "Lists RAM roles",
  "description_zh": "列出角色",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ListRoles",
  "operation": {
    "action": "ListRoles",
    "api_style": "RPC",
    "api_version": "2015-05-01",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "EXAMPLE",
      "help_en": "The marker. If the response is truncated, you can use `Marker` to retrieve the content that starts from the position after the truncation point",
      "help_zh": "当请求的返回结果被截断时，可以使用`Marker`获取从当前截断位置之后的内容",
      "location": "query",
      "name": "marker",
      "options": [
        "--marker"
      ],
      "raw_name": "Marker",
      "required": false,
      "type": "string"
    },
    {
      "example": "100",
      "help_en": "The number of entries to return. If the response is truncated because it reaches the `MaxItems` limit, the `IsTruncated` parameter is set to `true`.\n\nValid values: 1 to 1000. Default value: 100",
      "help_zh": "返回结果的条数。当返回结果达到`MaxItems`限制被截断时，返回参数`IsTruncated`将等于`true`。\n\n取值范围：1 ~ 1000。默认值：100",
      "location": "query",
      "name": "max_items",
      "options": [
        "--max-items"
      ],
      "raw_name": "MaxItems",
      "required": false,
      "type": "int"
    },
    {
      "element": {
        "fields": [
          {
            "example": "owner",
            "help_en": "The tag key",
            "help_zh": "标签键",
            "name": "key",
            "options": [
              "--key"
            ],
            "raw_name": "Key",
            "required": false,
            "type": "string"
          },
          {
            "example": "alice",
            "help_en": "The tag value",
            "help_zh": "标签值",
            "name": "value",
            "options": [
              "--value"
            ],
            "raw_name": "Value",
            "required": false,
            "type": "string"
          }
        ],
        "type": "object"
      },
      "help_en": "The tags",
      "help_zh": "标签",
      "location": "query",
      "name": "tag",
      "options": [
        "--tag"
      ],
      "param_style": "json",
      "raw_name": "Tag",
      "required": false,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "Ram",
  "protocol": "HTTPS"
}
