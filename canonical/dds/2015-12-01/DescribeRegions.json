{
  "camel_example": "",
  "cmd_full_name": "dds describe-regions",
  "cmd_name": "describe-regions",
  "deprecated": false,
  "description_en": "Queries all regions and zones supported for an ApsaraDB for MongoDB instance",
  "description_zh": "查看MongoDB实例支持的所有地域和可用区",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeRegions",
  "operation": {
    "action": "DescribeRegions",
    "api_style": "RPC",
    "api_version": "2015-12-01",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID.\n\nIf you do not specify this parameter, all supported regions are queried",
      "help_zh": "地域ID。\n如果不填写，默认查询所有支持的地域",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": false,
      "type": "string"
    },
    {
      "example": "zh",
      "help_en": "The language of the values of the returned RegionName and ZoneName parameters. Valid values:\n\nzh (default)\n\nen",
      "help_zh": "指定返回参数RegionName和ZoneName的语言，取值说明：\nzh：默认值，中文。\nen：英文",
      "location": "query",
      "name": "accept_language",
      "options": [
        "--accept-language"
      ],
      "raw_name": "AcceptLanguage",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Dds",
  "protocol": "HTTP|HTTPS"
}
