{
  "camel_example": "",
  "cmd_full_name": "dds transfer-cluster-backup",
  "cmd_name": "transfer-cluster-backup",
  "deprecated": false,
  "description_en": "Switches the backup mode of an ApsaraDB for MongoDB sharded cluster instance to the cluster backup mode. After the instance is switched to the cluster backup mode, the instance supports high-frequency backup",
  "description_zh": "将云数据库 MongoDB 版分片集群实例的备份模式切换为集群备份模式。切换至集群备份模式后，实例将支持高频备份",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "TransferClusterBackup",
  "operation": {
    "action": "TransferClusterBackup",
    "api_style": "RPC",
    "api_version": "2015-12-01",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "dds-bp2235****",
      "help_en": "The instance ID",
      "help_zh": "实例ID",
      "location": "query",
      "name": "db_instance_id",
      "options": [
        "--db-instance-id"
      ],
      "raw_name": "DBInstanceId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Dds",
  "protocol": "HTTP|HTTPS"
}
