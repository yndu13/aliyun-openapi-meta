{
  "camel_example": "",
  "cmd_full_name": "dds describe-cluster-backups",
  "cmd_name": "describe-cluster-backups",
  "deprecated": false,
  "description_en": "Queries the cluster backup sets for an ApsaraDB for MongoDB sharded cluster instance that uses cloud disks",
  "description_zh": "查询云数据库 MongoDB 版云盘版分片集群的集群备份集列表",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "DescribeClusterBackups",
  "operation": {
    "action": "DescribeClusterBackups",
    "api_style": "RPC",
    "api_version": "2015-12-01",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "dds-bp16cb162771****",
      "help_en": "The ID of the instance",
      "help_zh": "实例ID",
      "location": "query",
      "name": "db_instance_id",
      "options": [
        "--db-instance-id"
      ],
      "raw_name": "DBInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "5664****",
      "help_en": "The ID of the cluster backup",
      "help_zh": "集群备份ID",
      "location": "query",
      "name": "backup_id",
      "options": [
        "--backup-id"
      ],
      "raw_name": "BackupId",
      "required": false,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The number of the page to return. The value must be a positive integer. Default value: 1",
      "help_zh": "分页页号。默认值为1，取值范围为正整数",
      "location": "query",
      "minimum": "1",
      "name": "page_no",
      "options": [
        "--page-no"
      ],
      "raw_name": "PageNo",
      "required": false,
      "type": "int"
    },
    {
      "enum": [
        "30",
        "50",
        "100",
        "10",
        "20",
        "5"
      ],
      "example": "30",
      "help_en": "The number of entries to return on each page. Valid values:\n\n30 (Default)\n\n50\n\n100",
      "help_zh": "每页记录数，取值：\n\n30（默认值）\n50\n100",
      "location": "query",
      "name": "page_size",
      "options": [
        "--page-size"
      ],
      "raw_name": "PageSize",
      "required": false,
      "type": "int"
    },
    {
      "example": "2019-03-13T12:11Z",
      "help_en": "The beginning of the time range to query. Specify the time in the yyyy-MM-ddTHH:mmZ format. The time must be in Coordinated Universal Time (UTC).\n\nThis parameter is invalid if you specify the BackupId parameter",
      "help_zh": "查询开始时间，格式为<i>yyyy-MM-dd</i>T<i>HH:mm</i>Z（UTC时间）。\n当传入BackupId时，此参数无效",
      "location": "query",
      "name": "start_time",
      "options": [
        "--start-time"
      ],
      "raw_name": "StartTime",
      "required": false,
      "type": "string"
    },
    {
      "example": "2019-03-14T13:10Z",
      "help_en": "The end of the time range to query. The end time must be later than the start time. Specify the time in the yyyy-MM-ddTHH:mmZ format. The time must be in UTC.\n\nThis parameter is invalid if you specify the BackupId parameter",
      "help_zh": "查询结束时间，必须晚于查询开始时间，格式为<i>yyyy-MM-dd</i>T<i>HH:mm</i>Z（UTC时间）。\n当传入BackupId时，此参数无效",
      "location": "query",
      "name": "end_time",
      "options": [
        "--end-time"
      ],
      "raw_name": "EndTime",
      "required": false,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to query the information about the child nodes in the cluster backup. Valid values:\n\ntrue: Returns only the basic information about the cluster backup, not the backup information about all child nodes.\n\nfalse (Default): Returns the backup information about all child nodes",
      "help_zh": "是否查询集群备份下的子节点信息。取值说明：\ntrue：只返回集群备份基本信息，不返回全量的子节点的备份信息。\nfalse（默认 ）：返回全量的子节点的备份信息",
      "location": "query",
      "name": "is_only_get_cluster_back_up",
      "options": [
        "--is-only-get-cluster-back-up"
      ],
      "raw_name": "IsOnlyGetClusterBackUp",
      "required": false,
      "type": "bool"
    },
    {
      "example": "cn-hangzhou",
      "help_en": "The region where the instance resides.\n\nThis parameter is required when you query the backup sets of a released instance.\n\nThis parameter is required when you query geo-redundant backups",
      "help_zh": "实例所在地域。\n\n查询已释放实例的备份集列表式，该参数必填。\n查询异地备份时，该参数必填",
      "location": "query",
      "name": "src_region",
      "options": [
        "--src-region"
      ],
      "raw_name": "SrcRegion",
      "required": false,
      "type": "string"
    },
    {
      "example": "cn-shanghai",
      "help_en": "The region where the geo-redundant backup resides.\n\nThis parameter is required when you query geo-redundant backups",
      "help_zh": "异地备份所在地域。\n\n查询异地备份时，该参数必填",
      "location": "query",
      "name": "dest_region",
      "options": [
        "--dest-region"
      ],
      "raw_name": "DestRegion",
      "required": false,
      "type": "string"
    },
    {
      "example": "rg-xxxx",
      "help_en": "The ID of the resource group",
      "help_zh": "资源组ID",
      "location": "query",
      "name": "resource_group_id",
      "options": [
        "--resource-group-id"
      ],
      "raw_name": "ResourceGroupId",
      "required": false,
      "type": "string"
    },
    {
      "example": "775051",
      "help_en": "The ID of the backup job",
      "help_zh": "备份任务ID",
      "location": "query",
      "name": "backup_job_id",
      "options": [
        "--backup-job-id"
      ],
      "raw_name": "BackupJobId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Dds",
  "protocol": "HTTP|HTTPS"
}
