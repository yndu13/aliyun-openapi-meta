{
  "camel_example": "",
  "cmd_full_name": "dds create-backup",
  "cmd_name": "create-backup",
  "deprecated": false,
  "description_en": "Creates a backup for a MongoDB instance",
  "description_zh": "创建MongoDB实例备份",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CreateBackup",
  "operation": {
    "action": "CreateBackup",
    "api_style": "RPC",
    "api_version": "2015-12-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "example": "d-bp2235****",
      "help_en": "The instance ID",
      "help_zh": "实例ID",
      "location": "query",
      "name": "db_instance_id",
      "options": [
        "--db-instance-id"
      ],
      "raw_name": "DBInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "Logical",
      "help_en": "The backup method. Valid values:\n\nSnapshot: A snapshot backup.\n\nLogical: A logical backup.\n\nPhysical (default): A physical backup.\n\nInstances with cloud disks support only snapshot backups.\n\nThis parameter applies only to replica set and sharded cluster instances. This parameter is not required for standalone instances, which use snapshot backups",
      "help_zh": "实例的备份方式，取值：\nSnapshot：快照备份。\nLogical：逻辑备份。\nPhysical（默认值）：物理备份。\n\n云盘版实例仅支持快照备份。\n仅副本集和分片集群实例支持选择备份方式。单节点实例无需传入本参数，固定为快照备份",
      "location": "query",
      "name": "backup_method",
      "options": [
        "--backup-method"
      ],
      "raw_name": "BackupMethod",
      "required": false,
      "type": "string"
    },
    {
      "example": "30",
      "help_en": "The backup retention period in days. This parameter applies only to replica set and sharded cluster instances that use cloud disks.\n\nIf you do not specify this parameter, the retention period from the default backup policy is used. You can set this parameter to one of the following values:\n\nA value from 7 to 730\n\n-1 (permanent retention)",
      "help_zh": "备份保留天数，只适用于云盘副本集与云盘分片集群实例。\n\n不传表示和默认的备份策略保持一致。传递时，可传 \n7-730天\n-1 （长久保留）",
      "location": "query",
      "name": "backup_retention_period",
      "options": [
        "--backup-retention-period"
      ],
      "raw_name": "BackupRetentionPeriod",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "Dds",
  "protocol": "HTTP|HTTPS"
}
