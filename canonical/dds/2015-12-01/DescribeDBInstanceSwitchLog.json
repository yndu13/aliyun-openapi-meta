{
  "camel_example": "",
  "cmd_full_name": "dds describe-db-instance-switch-log",
  "cmd_name": "describe-db-instance-switch-log",
  "deprecated": false,
  "description_en": "Queries the primary/secondary switching logs of an ApsaraDB for MongoDB instance",
  "description_zh": "查询实例的主备切换日志",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeDBInstanceSwitchLog",
  "operation": {
    "action": "DescribeDBInstanceSwitchLog",
    "api_style": "RPC",
    "api_version": "2015-12-01",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "list",
    "url": ""
  },
  "parameters": [
    {
      "example": "2023-05-27T02:46Z",
      "help_en": "The beginning of the time range to query. Specify the time in the ISO 8601 standard in the yyyy-mm-ddThh:mmZ format. The time must be in UTC",
      "help_zh": "查询开始时间，格式为<i>yyyy-mm-dd</i>T<i>hh:mm</i>Z（UTC时间）",
      "location": "query",
      "name": "start_time",
      "options": [
        "--start-time"
      ],
      "raw_name": "StartTime",
      "required": false,
      "type": "string"
    },
    {
      "example": "2023-05-28T02:00Z",
      "help_en": "The end of the time range to query. Specify the time in the ISO 8601 standard in the yyyy-mm-ddThh:mmZ format. The time must be in UTC. The end time must be later than the start time",
      "help_zh": "查询结束时间，必须晚于查询开始时间，格式为<i>yyyy-mm-dd</i>T<i>hh:mm</i>Z（UTC时间）",
      "location": "query",
      "name": "end_time",
      "options": [
        "--end-time"
      ],
      "raw_name": "EndTime",
      "required": false,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The number of the page to return. The value must be an integer that is greater than 0 and less than or equal to the maximum value supported by the integer data type. Default value: 1",
      "help_zh": "页码，取值为大于0且不超过integer数据类型的最大值，默认值为1",
      "location": "query",
      "name": "page_number",
      "options": [
        "--page-number"
      ],
      "raw_name": "PageNumber",
      "required": false,
      "type": "int"
    },
    {
      "enum": [
        "30",
        "50",
        "100"
      ],
      "example": "30",
      "help_en": "The number of entries to return on each page. Valid values: 30, 50, and 100. Default value: 30",
      "help_zh": "每页记录数，取值：30、50、100，默认值为30",
      "location": "query",
      "name": "page_size",
      "options": [
        "--page-size"
      ],
      "raw_name": "PageSize",
      "required": false,
      "type": "int"
    },
    {
      "example": "dds-uf68f1b5a57exxxx",
      "help_en": "The instance ID",
      "help_zh": "实例ID",
      "location": "query",
      "name": "db_instance_id",
      "options": [
        "--db-instance-id"
      ],
      "raw_name": "DBInstanceId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Dds",
  "protocol": "HTTP|HTTPS"
}
