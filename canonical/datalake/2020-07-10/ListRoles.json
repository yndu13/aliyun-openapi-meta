{
  "camel_example": "",
  "cmd_full_name": "datalake list-roles",
  "cmd_name": "list-roles",
  "deprecated": false,
  "description_en": "Retrieves DLF roles",
  "description_zh": "获取数据湖角色列表",
  "kebab_example": "",
  "method": "GET",
  "multi_version": false,
  "name": "ListRoles",
  "operation": {
    "action": "ListRoles",
    "api_style": "ROA",
    "api_version": "2020-07-10",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": "/api/metastore/auth/roles/list"
  },
  "parameters": [
    {
      "example": "token!",
      "help_en": "The pagination token used to retrieve the next page of results. If a NextPageToken is not returned in the response, pass an empty string (\"\")",
      "help_zh": "翻页令牌，用于获取下一页数据。如果响应结果中没有提供，则传空字符串（\"\"）或空字符（''）",
      "location": "query",
      "name": "next_page_token",
      "options": [
        "--next-page-token"
      ],
      "raw_name": "NextPageToken",
      "required": false,
      "type": "string"
    },
    {
      "example": ".*test.*",
      "help_en": "A regular expression that the role name must match. Fuzzy search is supported. For example, .\\test",
      "help_zh": "角色名称匹配的正则表达式。支持模糊搜索，比如.\\test.\\",
      "location": "query",
      "name": "role_name_pattern",
      "options": [
        "--role-name-pattern"
      ],
      "raw_name": "RoleNamePattern",
      "required": false,
      "type": "string"
    },
    {
      "example": "100",
      "help_en": "The number of entries to return on each page. The maximum value is 1,000",
      "help_zh": "分页大小，最大值为1000",
      "location": "query",
      "name": "page_size",
      "options": [
        "--page-size"
      ],
      "raw_name": "PageSize",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "/api/metastore/auth/roles/list",
  "product_code": "DataLake",
  "protocol": "HTTP|HTTPS"
}
