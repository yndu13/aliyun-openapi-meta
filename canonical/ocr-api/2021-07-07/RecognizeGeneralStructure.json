{
  "camel_example": "",
  "cmd_full_name": "ocr-api recognize-general-structure",
  "cmd_name": "recognize-general-structure",
  "deprecated": false,
  "description_en": "The general ticket extraction feature combines the capabilities of ReadLight OCR and the Qwen large language model (LLM) to extract key key-value (KV) information from long-tail receipts not supported by OCR, such as structured recognition and output of critical fields including Name, Address, and Date",
  "description_zh": "通用票证抽取结合读光OCR和通义千问大模型的能力，针对OCR不支持的长尾票据，提供关键KV信息抽取，例如名称、地址、开票日期等关键字段结构化识别输出",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "RecognizeGeneralStructure",
  "operation": {
    "action": "RecognizeGeneralStructure",
    "api_style": "RPC",
    "api_version": "2021-07-07",
    "content_type": "application/octet-stream",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "req_body_type": "byte",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "https://example.png",
      "help_en": "This field and the body field are mutually exclusive; you cannot pass both or leave both empty.  \nImage URL (must not exceed 2048 bytes in length and does not support base64)",
      "help_zh": "本字段和 body 字段二选一，不可同时透传或同时为空。\n图片链接（长度不超过2048字节，不支持 base64）",
      "location": "query",
      "maxLength": "2048",
      "name": "url",
      "options": [
        "--url"
      ],
      "raw_name": "Url",
      "required": false,
      "type": "string"
    },
    {
      "example": "图片二进制文件\n",
      "format": "binary",
      "help_en": "This field and the URL field are mutually exclusive; you cannot pass both or leave both empty.  \nBinary file of the image, up to 10 MB in size.  \nWhen calling via HTTP, upload the image binary file in the HTTP body.  \nWhen using the software development kit (SDK), place the image in the SDK body",
      "help_zh": "本字段和 URL 字段二选一，不可同时透传或同时为空。\n图片二进制文件，最大 10MB。\n使用 HTTP 方式调用，把图片二进制文件放到 HTTP body 中上传即可。\n使用 SDK 的方式调用，把图片放到 SDK 的 body 中即可",
      "location": "body",
      "name": "body",
      "options": [
        "--biz-body"
      ],
      "raw_name": "body",
      "required": false,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "An array of strings specifying all keys to be extracted.  \nDefault value is empty, which means the LLM automatically determines the keys to extract.  \nThe maximum number of keys allowed is 30 (inclusive).  \nIt is recommended to pass this parameter when invoking the API to reduce duration. Note: The response time is positively correlated with the number of keys",
      "help_zh": "需要抽取的所有Key（字符串数组）。\n默认值为空，表示由大模型自动判断需要抽取的Key。\nKey的上限数量为30（包含30）。\n建议调用接口时传此参数，减小接口耗时。请注意：接口响应时间和Key的数量呈正相关关系",
      "location": "query",
      "name": "keys",
      "options": [
        "--keys"
      ],
      "param_style": "simple",
      "raw_name": "Keys",
      "required": false,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "ocr-api",
  "protocol": "HTTP|HTTPS"
}
