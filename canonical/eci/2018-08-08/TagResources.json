{
  "camel_example": "",
  "cmd_full_name": "eci tag-resources",
  "cmd_name": "tag-resources",
  "deprecated": false,
  "description_en": "The TagResources operation attaches tags to specified ECI resources",
  "description_zh": "调用TagResources为指定的ECI资源绑定标签",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "TagResources",
  "operation": {
    "action": "TagResources",
    "api_style": "RPC",
    "api_version": "2018-08-08",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "url": ""
  },
  "parameters": [
    {
      "docRequired": true,
      "example": "cn-hangzhou",
      "help_en": "The ID of the region where the resource is located",
      "help_zh": "资源所属的地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "docRequired": true,
      "element": {
        "type": "string"
      },
      "help_en": "The list of resource IDs",
      "help_zh": "资源ID列表",
      "location": "query",
      "name": "resource_id",
      "options": [
        "--resource-id"
      ],
      "param_style": "repeatList",
      "raw_name": "ResourceId",
      "required": true,
      "type": "array"
    },
    {
      "element": {
        "fields": [
          {
            "example": "testkey",
            "help_en": "The key of the tag. The key cannot be an empty string. The key can be up to 128 characters in length. It cannot start with `aliyun` or `acs:`, and it cannot contain `http://` or `https://`",
            "help_zh": "标签键。一旦传入该值，则不允许为空字符串。最多支持 128 个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`",
            "name": "key",
            "options": [
              "--key"
            ],
            "raw_name": "Key",
            "required": false,
            "type": "string"
          },
          {
            "example": "testvalue",
            "help_en": "The value of the tag. The value can be an empty string. The value can be up to 128 characters in length. It cannot start with `aliyun` or `acs:`, and it cannot contain `http://` or `https://`",
            "help_zh": "标签键。一旦传入该值，可以为空字符串。最多支持 128 个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`",
            "name": "value",
            "options": [
              "--value"
            ],
            "raw_name": "Value",
            "required": false,
            "type": "string"
          }
        ],
        "type": "object"
      },
      "help_en": "The list of tags. You can attach a maximum of 20 tags to a resource",
      "help_zh": "标签列表。一个资源最多可以绑定20个标签",
      "location": "query",
      "name": "tag",
      "options": [
        "--tag"
      ],
      "param_style": "repeatList",
      "raw_name": "Tag",
      "required": true,
      "type": "array"
    },
    {
      "example": "ContainerGroup",
      "help_en": "The type of the resource. Valid values:\n\nContainerGroup: ECI instance\n\nImageCache: image cache\n\nDataCache: data cache\n\nVirtualNode: virtual node",
      "help_zh": "资源类型。取值范围：\n\nContainerGroup：ECI实例\nImageCache：镜像缓存\nDataCache：数据缓存\nVirtualNode：虚拟节点",
      "location": "query",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Eci",
  "protocol": "HTTP|HTTPS"
}
