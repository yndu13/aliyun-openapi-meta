{
  "camel_example": "",
  "cmd_full_name": "eci exec-container-command",
  "cmd_name": "exec-container-command",
  "deprecated": false,
  "description_en": "Executes a command in a container",
  "description_zh": "调用ExecContainerCommand在容器内部执行命令",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ExecContainerCommand",
  "operation": {
    "action": "ExecContainerCommand",
    "api_style": "RPC",
    "api_version": "2018-08-08",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "cn-hangzhou",
      "help_en": "The region ID of the instance",
      "help_zh": "实例所属的地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "eci-2zebxkiifulhl****",
      "help_en": "The instance ID, which is the container group ID",
      "help_zh": "实例ID，即容器组ID",
      "location": "query",
      "name": "container_group_id",
      "options": [
        "--container-group-id"
      ],
      "raw_name": "ContainerGroupId",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "nginx",
      "help_en": "The name of the container",
      "help_zh": "容器名称",
      "location": "query",
      "name": "container_name",
      "options": [
        "--container-name"
      ],
      "raw_name": "ContainerName",
      "required": true,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "[\"/bin/sh\", \"-c\", \"ls -a\"]",
      "help_en": "The commands to execute in the container. You can specify up to 20 commands. Each command can be up to 256 characters long.<br>\nTo execute multiple commands, pass the string in JSON format. For example, `[\"/bin/sh\", \"-c\", \"ls -a\"]`.<br>",
      "help_zh": "要在容器内执行的命令序列。最多20个命令，单个命令最长256个字符。    \n执行多个命令时，字符串需按照JSON格式传入，例如：`[\"/bin/sh\", \"-c\", \"ls -a\"]`",
      "location": "query",
      "name": "command",
      "options": [
        "--command"
      ],
      "raw_name": "Command",
      "required": true,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to enable interaction. The default value is false.<br>\nIf you set Command to `/bin/bash`, set this parameter to true.<br>",
      "help_zh": "是否开启交互。默认为false。    \n当Command为`/bin/bash`命令时，需要设置为true",
      "location": "query",
      "name": "tty",
      "options": [
        "--tty"
      ],
      "raw_name": "TTY",
      "required": false,
      "type": "bool"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to enable standard input. The default value is true",
      "help_zh": "是否标准输入。默认为true",
      "location": "query",
      "name": "stdin",
      "options": [
        "--stdin"
      ],
      "raw_name": "Stdin",
      "required": false,
      "type": "bool"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to execute the command immediately and return the result synchronously. The default value is false.<br>\nIf you set this parameter to true, you must set TTY to false. This means Command cannot be `/bin/bash`.<br>",
      "help_zh": "是否立即执行命令并同步返回结果。默认为false。    \n如果设置为true，TTY只能设置为false（即Command不是`/bin/bash`命令）",
      "location": "query",
      "name": "sync",
      "options": [
        "--sync"
      ],
      "raw_name": "Sync",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Eci",
  "protocol": "HTTP|HTTPS"
}
