{
  "camel_example": "",
  "cmd_full_name": "polardb describe-slow-logs",
  "cmd_name": "describe-slow-logs",
  "deprecated": false,
  "description_en": "Queries the slow query log statistics for a PolarDB cluster",
  "description_zh": "查询PolarDB集群的慢日志统计信息",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeSlowLogs",
  "operation": {
    "action": "DescribeSlowLogs",
    "api_style": "RPC",
    "api_version": "2017-08-01",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region ID",
      "help_zh": "地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "pc-****************",
      "help_en": "The cluster ID",
      "help_zh": "集群ID",
      "location": "query",
      "name": "db_cluster_id",
      "options": [
        "--db-cluster-id"
      ],
      "raw_name": "DBClusterId",
      "required": true,
      "type": "string"
    },
    {
      "example": "2021-05-01Z",
      "help_en": "The start date of the query range. The date can be up to 31 days in the past. Specify the date in `yyyy-MM-ddZ` format. The time is in UTC",
      "help_zh": "查询开始日期，与当前日期间隔不超过31天。格式：yyyy-MM-ddZ（UTC时间）",
      "location": "query",
      "name": "start_time",
      "options": [
        "--start-time"
      ],
      "raw_name": "StartTime",
      "required": true,
      "type": "string"
    },
    {
      "example": "2021-05-30Z",
      "help_en": "The end date of the query range. The end date cannot be earlier than the start date, and the interval between the start and end dates cannot exceed 31 days. Specify the date in `yyyy-MM-ddZ` format. The time is in UTC",
      "help_zh": "查询结束日期，不能小于查询开始日期，与查询开始日期间隔不超过31天。格式：yyyy-MM-ddZ（UTC时间）",
      "location": "query",
      "name": "end_time",
      "options": [
        "--end-time"
      ],
      "raw_name": "EndTime",
      "required": true,
      "type": "string"
    },
    {
      "example": "PolarDB_MySQL",
      "help_en": "The database name",
      "help_zh": "数据库名称",
      "location": "query",
      "name": "db_name",
      "options": [
        "--db-name"
      ],
      "raw_name": "DBName",
      "required": false,
      "type": "string"
    },
    {
      "example": "30",
      "help_en": "The number of logs to return on each page. Valid values: 30 to 100. The default value is 30",
      "help_zh": "每页记录数，取值：30~100。默认值：30",
      "location": "query",
      "name": "page_size",
      "options": [
        "--page-size"
      ],
      "raw_name": "PageSize",
      "required": false,
      "type": "int"
    },
    {
      "example": "1",
      "help_en": "The page number.\n\nThe default value is 1",
      "help_zh": "页码，取值：大于0且不超过Integer的最大值。\n\n默认值：1",
      "location": "query",
      "name": "page_number",
      "options": [
        "--page-number"
      ],
      "raw_name": "PageNumber",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "polardb",
  "protocol": "HTTP|HTTPS"
}
