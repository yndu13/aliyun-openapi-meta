{
  "camel_example": "",
  "cmd_full_name": "polardb modify-db-cluster-description-zonal",
  "cmd_name": "modify-db-cluster-description-zonal",
  "deprecated": false,
  "description_en": "Modifies the description of a cluster",
  "description_zh": "修改集群描述",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "ModifyDBClusterDescriptionZonal",
  "operation": {
    "action": "ModifyDBClusterDescriptionZonal",
    "api_style": "RPC",
    "api_version": "2017-08-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "pc-**************",
      "help_en": "The cluster ID",
      "help_zh": "集群 ID",
      "location": "query",
      "name": "db_cluster_id",
      "options": [
        "--db-cluster-id"
      ],
      "raw_name": "DBClusterId",
      "required": true,
      "type": "string"
    },
    {
      "example": "pc-****************",
      "help_en": "The cluster name. The cluster name must meet the following requirements:\n\nIt cannot start with `http://` or `https://`.\n\nIt must be 2 to 256 characters in length",
      "help_zh": "集群名称，集群名称需满足如下要求：\n\n不能以`http://`或`https://`开头。\n长度为 2~256 个字符",
      "location": "query",
      "name": "db_cluster_description",
      "options": [
        "--db-cluster-description"
      ],
      "raw_name": "DBClusterDescription",
      "required": true,
      "type": "string"
    },
    {
      "example": "6000170000591aed949d0f54a343f1a4233c1e7d1c5c******",
      "help_en": "A client token to ensure the idempotence of the request. The client generates this value. It must be unique among different requests. The token is case-sensitive and can contain up to 64 ASCII characters",
      "help_zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，保证在不同请求间唯一，大小写敏感、不超过 64 个 ASCII 字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "polardb",
  "protocol": "HTTPS"
}
