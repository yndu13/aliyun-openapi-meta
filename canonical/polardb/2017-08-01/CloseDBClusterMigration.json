{
  "camel_example": "",
  "cmd_full_name": "polardb close-db-cluster-migration",
  "cmd_name": "close-db-cluster-migration",
  "deprecated": false,
  "description_en": "Cancels or completes the migration task that upgrades an RDS cluster to a PolarDB cluster",
  "description_zh": "取消或完成RDS一键升级到PolarDB的迁移任务",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CloseDBClusterMigration",
  "operation": {
    "action": "CloseDBClusterMigration",
    "api_style": "RPC",
    "api_version": "2017-08-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "docRequired": true,
      "example": "pc-**************",
      "help_en": "The ID of the cluster",
      "help_zh": "集群ID",
      "location": "query",
      "name": "db_cluster_id",
      "options": [
        "--db-cluster-id"
      ],
      "raw_name": "DBClusterId",
      "required": true,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to continue to enable binary logging. Valid values:\n\nTrue: continues to enable binary logging.\n\nFalse: disables binary logging.\n\nDefault value: True.\n\nIf binary logging is disabled, your PolarDB cluster is restarted",
      "help_zh": "是否继续打开Binlog，取值范围如下： \ntrue：继续打开Binlog\nfalse：关闭Binlog\n\n默认取值为true。 \n\n关闭Binlog将会重启您的PolarDB集群",
      "location": "query",
      "name": "continue_enable_binlog",
      "options": [
        "--continue-enable-binlog"
      ],
      "raw_name": "ContinueEnableBinlog",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "polardb",
  "protocol": "HTTP|HTTPS"
}
