{
  "camel_example": "",
  "cmd_full_name": "cloudsso update-directory",
  "cmd_name": "update-directory",
  "deprecated": false,
  "description_en": "Changes the name of a directory",
  "description_zh": "调用UpdateDirectory修改目录名称",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UpdateDirectory",
  "operation": {
    "action": "UpdateDirectory",
    "api_style": "RPC",
    "api_version": "2021-05-15",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "d-00fc2p61****",
      "help_en": "The ID of the directory",
      "help_zh": "目录ID",
      "location": "query",
      "name": "directory_id",
      "options": [
        "--directory-id"
      ],
      "raw_name": "DirectoryId",
      "required": true,
      "type": "string"
    },
    {
      "example": "new-example",
      "help_en": "The new name of the directory. The name must be globally unique.\n\nThe name can contain lowercase letters, digits, and hyphens (-). The name cannot start or end with a hyphen (-) and cannot contain two consecutive hyphens (-). If you want the new name of the directory to start with `d-`, you must set this parameter to the ID of the directory.\n\nThe name must be 2 to 64 characters in length",
      "help_zh": "新的目录名称。该参数必须全局唯一。\n\n格式：包含小写字母、数字和短划线（-）。不能以短划线（-）开头或结尾，且不能有两个连续的短划线（-）。如果新的目录名以`d-`开头，则只能与本目录ID相同，而不能设置为其他值。\n\n长度：2~64个字符",
      "location": "query",
      "name": "new_directory_name",
      "options": [
        "--new-directory-name"
      ],
      "raw_name": "NewDirectoryName",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "cloudsso",
  "protocol": "HTTPS"
}
