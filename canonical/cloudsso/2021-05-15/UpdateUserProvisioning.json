{
  "camel_example": "",
  "cmd_full_name": "cloudsso update-user-provisioning",
  "cmd_name": "update-user-provisioning",
  "deprecated": false,
  "description_en": "Modifies a Resource Access Management (RAM) user provisioning",
  "description_zh": "调用UpdateUserProvisioning修改RAM用户同步",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UpdateUserProvisioning",
  "operation": {
    "action": "UpdateUserProvisioning",
    "api_style": "RPC",
    "api_version": "2021-05-15",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "doc_required": true,
      "example": "d-003qew84****",
      "help_en": "The ID of the resource directory",
      "help_zh": "目录ID",
      "location": "query",
      "name": "directory_id",
      "options": [
        "--directory-id"
      ],
      "raw_name": "DirectoryId",
      "required": false,
      "type": "string"
    },
    {
      "doc_required": true,
      "example": "up-002axzhapcbz6e63****",
      "help_en": "The ID of the RAM user provisioning",
      "help_zh": "RAM用户同步ID",
      "location": "query",
      "name": "user_provisioning_id",
      "options": [
        "--user-provisioning-id"
      ],
      "raw_name": "UserProvisioningId",
      "required": false,
      "type": "string"
    },
    {
      "example": "KeepBoth",
      "help_en": "The new conflict handling policy. The policy is used when a RAM user has the same username as the CloudSSO user who is synchronized to RAM. Valid values:\n\nKeepBoth: When a CloudSSO user is synchronized to RAM, if a RAM user who has the same username as the CloudSSO user exists, the system creates a RAM user whose username is the username of the CloudSSO user plus the suffix `_sso`.\n\nTakeOver: When a CloudSSO user is synchronized to RAM, if a RAM user who has the same username as the CloudSSO user exists, the system replaces the RAM user with the CloudSSO user",
      "help_zh": "新的冲突策略。当云SSO用户同步到RAM时，如果RAM中存在同名用户时的处理策略。取值：\n\nKeepBoth：两者都保留。当云SSO用户被同步到RAM时，如果RAM已经存在同名用户，则对云SSO用户的用户名添加后缀`_sso`后尝试创建该用户名的RAM用户。\nTakeOver：替换。当云SSO用户被同步到RAM时，如果RAM已经存在同名用户，则直接将已经存在的RAM用户替换为云SSO同步用户",
      "location": "query",
      "name": "new_duplication_strategy",
      "options": [
        "--new-duplication-strategy"
      ],
      "raw_name": "NewDuplicationStrategy",
      "required": false,
      "type": "string"
    },
    {
      "example": "Delete",
      "help_en": "The new deletion policy. The policy is used to manage synchronized users when you delete the RAM user provisioning. Valid values:\n\nDelete: When you delete the RAM user provisioning, the system deletes the synchronized users.\n\nKeep: When you delete the RAM user provisioning, the system retains the synchronized users",
      "help_zh": "新的删除策略。删除RAM用户同步时，对已同步的RAM用户的处理策略。取值：\n\nDelete：删除。删除RAM用户同步时，会删除从云SSO已经同步到RAM中的RAM用户。\nKeep：保留。删除RAM用户同步时，会保留从云SSO已经同步到RAM中的RAM用户",
      "location": "query",
      "name": "new_deletion_strategy",
      "options": [
        "--new-deletion-strategy"
      ],
      "raw_name": "NewDeletionStrategy",
      "required": false,
      "type": "string"
    },
    {
      "example": "description*****",
      "help_en": "The new description of the RAM user provisioning",
      "help_zh": "新的RAM用户同步描述信息",
      "location": "query",
      "name": "new_description",
      "options": [
        "--new-description"
      ],
      "raw_name": "NewDescription",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "cloudsso",
  "protocol": "HTTPS"
}
