{
  "camel_example": "",
  "cmd_full_name": "cr create-repo-trigger",
  "cmd_name": "create-repo-trigger",
  "deprecated": false,
  "description_en": "Creates a trigger for a repository",
  "description_zh": "创建镜像仓库触发器",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CreateRepoTrigger",
  "operation": {
    "action": "CreateRepoTrigger",
    "api_style": "RPC",
    "api_version": "2018-12-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "example": "cri-xkx6vujuhay0****",
      "help_en": "The ID of the instance",
      "help_zh": "实例ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "crr-xwvi3osiy4ff****",
      "help_en": "The ID of the image repository",
      "help_zh": "镜像仓库ID",
      "location": "query",
      "name": "repo_id",
      "options": [
        "--repo-id"
      ],
      "raw_name": "RepoId",
      "required": true,
      "type": "string"
    },
    {
      "example": "trigger1",
      "help_en": "The name of the trigger",
      "help_zh": "触发器名称",
      "location": "query",
      "name": "trigger_name",
      "options": [
        "--trigger-name"
      ],
      "raw_name": "TriggerName",
      "required": true,
      "type": "string"
    },
    {
      "example": "http://www.mysite.com",
      "help_en": "The URL of the trigger",
      "help_zh": "触发器URL",
      "location": "query",
      "name": "trigger_url",
      "options": [
        "--trigger-url"
      ],
      "raw_name": "TriggerUrl",
      "required": true,
      "type": "string"
    },
    {
      "example": "ALL",
      "help_en": "The type of the trigger. Valid values:\n\n`ALL`: a trigger that supports both tags and regular expressions.\n`TAG_LIST`: a tag-based trigger.\n`TAG_REG_EXP`: a regular expression-based trigger",
      "help_zh": "触发器类型，取值：\n`ALL`：全部触发\n`TAG_LIST`：Tag触发\n`TAG_REG_EXP`：表达式触发",
      "location": "query",
      "name": "trigger_type",
      "options": [
        "--trigger-type"
      ],
      "raw_name": "TriggerType",
      "required": true,
      "type": "string"
    },
    {
      "example": "[1]",
      "help_en": "The image tag based on which the trigger is set.\n\n\n\nIf `TriggerType` is set to `ALL`, `TriggerTag` can be set to a string or an array, for example, `*`.\n\nIf `TriggerType` is set to `TAG_LIST`, `TriggerTag` must be set to an array, for example, `[1]`.\nIf `TriggerType` is set to `TAG_REG_EXP`, `TriggerTag` must be set to a string, for example, `*`",
      "help_zh": "触发触发器的镜像版本。\n\n`TriggerType`为`ALL`，`TriggerTag`可以为任意字符串，示例`*`。\n`TriggerType`为`TAG_LIST`，`TriggerTag`为数组，示例`[1]`。\n`TriggerType`为`TAG_REG_EXP`，`TriggerTag`为字符串，示例`*`",
      "location": "query",
      "name": "trigger_tag",
      "options": [
        "--trigger-tag"
      ],
      "raw_name": "TriggerTag",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "cr",
  "protocol": "HTTP|HTTPS"
}
