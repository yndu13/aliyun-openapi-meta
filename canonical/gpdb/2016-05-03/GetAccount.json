{
  "camel_example": "",
  "cmd_full_name": "gpdb get-account",
  "cmd_name": "get-account",
  "deprecated": false,
  "description_en": "Retrieves information about a specific account",
  "description_zh": "获取特定的账号信息",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "GetAccount",
  "operation": {
    "action": "GetAccount",
    "api_style": "RPC",
    "api_version": "2016-05-03",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "gp-xxxxxxxxx",
      "help_en": "The instance ID.\nYou can specify up to 30 instance IDs for batch operations. Separate multiple instance IDs with commas (,)",
      "help_zh": "实例ID。\n支持传入最多30个实例ID进行批量操作，用英文逗号（,）隔开",
      "location": "query",
      "name": "db_instance_id",
      "options": [
        "--db-instance-id"
      ],
      "raw_name": "DBInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "testuser",
      "help_en": "The name of the initial account. The name must meet the following requirements:\n\nThe name can contain lowercase letters, digits, and underscores (_).\nThe name must start with a lowercase letter and end with a lowercase letter or digit.\nThe name cannot start with gp.\nThe name must be 2 to 16 characters in length",
      "help_zh": "初始账号名称，限制如下：\n\n由小写字母，数字，下划线组成。\n以小写字母开头，小写字母或数字结尾。\n不能以gp开头。\n长度为2~16个字符",
      "location": "query",
      "name": "account_name",
      "options": [
        "--account-name"
      ],
      "raw_name": "AccountName",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "gpdb",
  "protocol": "HTTPS"
}
