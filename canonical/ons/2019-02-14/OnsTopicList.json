{
  "camel_example": "",
  "cmd_full_name": "ons ons-topic-list",
  "cmd_name": "ons-topic-list",
  "deprecated": false,
  "description_en": "Queries information about all topics in your account",
  "description_zh": "查询账号下所有Topic的信息列表",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "OnsTopicList",
  "operation": {
    "action": "OnsTopicList",
    "api_style": "RPC",
    "api_version": "2019-02-14",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "list",
    "url": ""
  },
  "parameters": [
    {
      "example": "test",
      "help_en": "Specify the topic to query. If you leave this parameter empty, all topics are queried",
      "help_zh": "查询指定Topic时设置，若不输入，则查询所有Topic",
      "location": "query",
      "name": "topic",
      "options": [
        "--topic"
      ],
      "raw_name": "Topic",
      "required": false,
      "type": "string"
    },
    {
      "docRequired": true,
      "example": "MQ_INST_188077086902****_BXSuW61e",
      "help_en": "The ID of the instance that contains the topics you want to query",
      "help_zh": "需获取的Topic信息列表所对应的实例ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": false,
      "type": "string"
    },
    {
      "element": {
        "fields": [
          {
            "example": "CartService",
            "help_en": "The tag key of the topic. You must specify the Key and Value parameters at the same time, or leave them both empty. If you specify both parameters, this operation queries topics that have the specified tag. If you leave both parameters empty, this operation queries all topics.\n\nIf you specify this parameter, its value cannot be an empty string.\n\nThe value can be up to 128 characters in length. It cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`",
            "help_zh": "Topic的标签键。Key和Value值必须同时输入或同时不输入。若同时输入表示根据Tag过滤符合条件的Topic，若不输入表示查询所有Topic。\n\n如果输入该值，则取值不允许为空字符串。\n最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`",
            "name": "key",
            "options": [
              "--key"
            ],
            "raw_name": "Key",
            "required": true,
            "type": "string"
          },
          {
            "example": "ServiceA",
            "help_en": "The tag value of the topic. You must specify the Key and Value parameters at the same time, or leave them both empty. If you specify both parameters, this operation queries topics that have the specified tag. If you leave both parameters empty, this operation queries all topics.\n\nIf you specify this parameter, its value can be an empty string.\n\nThe value can be up to 128 characters in length. It cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`",
            "help_zh": "Topic的标签值。Key和Value值必须同时输入或同时不输入。若同时输入表示根据Tag过滤符合条件的Topic，若不输入表示查询所有Topic。\n\n如果输入该值，取值允许为空字符串。\n最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`",
            "name": "value",
            "options": [
              "--value"
            ],
            "raw_name": "Value",
            "required": true,
            "type": "string"
          }
        ],
        "type": "object"
      },
      "help_en": "The list of tags that are attached to the topic. The list can contain a maximum of 20 elements",
      "help_zh": "Topic所绑定的标签列表。列表元素个数不得超过 20",
      "location": "query",
      "name": "tag",
      "options": [
        "--tag"
      ],
      "param_style": "repeatList",
      "raw_name": "Tag",
      "required": false,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "Ons",
  "protocol": "HTTP|HTTPS"
}
