{
  "camel_example": "",
  "cmd_full_name": "ons ons-instance-in-service-list",
  "cmd_name": "ons-instance-in-service-list",
  "deprecated": false,
  "description_en": "Queries all ApsaraMQ for RocketMQ 4.x instances in a specified region",
  "description_zh": "查询当前账号在指定地域（Region）下所有的云消息队列 RocketMQ 版4.x实例信息",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "OnsInstanceInServiceList",
  "operation": {
    "action": "OnsInstanceInServiceList",
    "api_style": "RPC",
    "api_version": "2019-02-14",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "list",
    "url": ""
  },
  "parameters": [
    {
      "element": {
        "fields": [
          {
            "example": "CartService",
            "help_en": "The tag key. Specify both Key and Value to filter instances by tag. Omit both to query all instances.\n\nThis parameter cannot be an empty string.\n\nThe tag key can be up to 128 characters long. It cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`",
            "help_zh": "实例的标签键。Key和Value值必须同时输入或同时不输入。若同时输入表示根据Tag过滤符合条件的实例，若不输入表示查询所有实例。\n\n如果输入该值，则取值不允许为空字符串。\n最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`",
            "name": "key",
            "options": [
              "--key"
            ],
            "raw_name": "Key",
            "required": false,
            "type": "string"
          },
          {
            "example": "SericeA",
            "help_en": "The tag value. Specify both Key and Value to filter instances by tag. Omit both to query all instances.\n\nThis parameter can be an empty string.\n\nThe tag value can be up to 128 characters long. It cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`",
            "help_zh": "资源的标签值。Key和Value值必须同时输入或同时不输入。若同时输入表示根据Tag过滤符合条件的实例，若不输入表示查询所有实例。\n\n如果输入该值，取值允许为空字符串。\n最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`",
            "name": "value",
            "options": [
              "--value"
            ],
            "raw_name": "Value",
            "required": false,
            "type": "string"
          }
        ],
        "type": "object"
      },
      "help_en": "A list of tags attached to the instance. The list can contain up to 20 elements",
      "help_zh": "实例所绑定的标签列表。列表元素个数不得超过 20",
      "location": "query",
      "name": "tag",
      "options": [
        "--tag"
      ],
      "param_style": "repeatList",
      "raw_name": "Tag",
      "required": false,
      "type": "array"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to return resource statistics. The statistics include the number of topics and groups in the instance",
      "help_zh": "是否需要资源统计信息。即实例下Topic的数量和Group的数量",
      "location": "query",
      "name": "need_resource_info",
      "options": [
        "--need-resource-info"
      ],
      "raw_name": "NeedResourceInfo",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Ons",
  "protocol": "HTTP|HTTPS"
}
