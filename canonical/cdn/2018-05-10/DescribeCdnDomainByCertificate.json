{
  "camel_example": "",
  "cmd_full_name": "cdn describe-cdn-domain-by-certificate",
  "cmd_name": "describe-cdn-domain-by-certificate",
  "deprecated": false,
  "description_en": "Queries accelerated domain names by SSL certificate",
  "description_zh": "调用DescribeCdnDomainByCertificate根据证书信息获取加速域名",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeCdnDomainByCertificate",
  "operation": {
    "action": "DescribeCdnDomainByCertificate",
    "api_style": "RPC",
    "api_version": "2018-05-10",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "******",
      "help_en": "The public key of the SSL certificate. You must encode the public key in Base64 and then call the encodeURIComponent function to encode the public key again.\n\nThe public key must be in the PEM format",
      "help_zh": "证书公钥，需要base64编码后再进行encodeURIComponent。\n\n支持pem格式",
      "location": "query",
      "name": "ssl_pub",
      "options": [
        "--ssl-pub"
      ],
      "raw_name": "SSLPub",
      "required": true,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether the domain name list to return contains only domain names with HTTPS enabled or disabled.\n\ntrue: The domain name list contains only domain names with HTTPS enabled.\n\nfalse: The domain name list contains only domain names with HTTPS disabled",
      "help_zh": "表示返回的域名列表只包含开启或关闭HTTPS的域名。\n\ntrue：开启\nfalse：关闭",
      "location": "query",
      "name": "ssl_status",
      "options": [
        "--ssl-status"
      ],
      "raw_name": "SSLStatus",
      "required": false,
      "type": "bool"
    },
    {
      "example": "true",
      "help_en": "Specifies whether the domain name list to return match the SSL certificate.\n\ntrue: The domain name list match the SSL certificate.\n\nfalse: The domain name list do not match the SSL certificate",
      "help_zh": "表示返回的域名列表是否与证书精准匹配。\ntrue：开启\nfalse：关闭",
      "location": "query",
      "name": "exact",
      "options": [
        "--exact"
      ],
      "raw_name": "Exact",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Cdn",
  "protocol": "HTTP|HTTPS"
}
