{
  "camel_example": "",
  "cmd_full_name": "cdn set-cdn-full-domains-block-ip",
  "cmd_name": "set-cdn-full-domains-block-ip",
  "deprecated": false,
  "description_en": "Blocks or unblocks IP addresses. This setting applies to all domain names in your account",
  "description_zh": "设置封禁的IP或解禁IP，该操作对您账号下所有的CDN域名同时生效",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "SetCdnFullDomainsBlockIP",
  "operation": {
    "action": "SetCdnFullDomainsBlockIP",
    "api_style": "RPC",
    "api_version": "2018-05-10",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "1.XXX.XXX.1,2.XXX.XXX.2",
      "help_en": "The IP addresses that you want to block or unblock. Separate multiple IP addresses with commas (,). You can specify up to 1,000 IP addresses",
      "help_zh": "封禁或解禁IP列表，有多个IP时请用半角逗号（,）分隔，最大支持1000个IP",
      "location": "formData",
      "name": "ip_list",
      "options": [
        "--ip-list"
      ],
      "raw_name": "IPList",
      "required": true,
      "type": "string"
    },
    {
      "example": "block",
      "help_en": "The type of the operation.\n\nblock\n\nunblock",
      "help_zh": "操作类型。\n\nblock：封禁。\nunblock：解禁",
      "location": "formData",
      "name": "operation_type",
      "options": [
        "--operation-type"
      ],
      "raw_name": "OperationType",
      "required": true,
      "type": "string"
    },
    {
      "example": "3000",
      "help_en": "The duration for which IP addresses or CIDR blocks are blocked. Unit: seconds. The value 0 specifies that IP addresses or CIDR blocks are permanently blocked. This parameter is available only if you set OperationType to block",
      "help_zh": "封禁时长，单位为秒。设置为0时表示永久封禁。仅当OperationType为block时有效",
      "location": "formData",
      "name": "block_interval",
      "options": [
        "--block-interval"
      ],
      "raw_name": "BlockInterval",
      "required": false,
      "type": "int"
    },
    {
      "example": "cover",
      "help_en": "The type of the blocking duration. This parameter is available only if you set OperationType to block. Valid values:\n\ncover: Default. The blocking duration that is specified in the request takes effect.\n\nuncover: The longer of the blocking duration in the request or the remaining blocking duration takes effect.\n\nWhen the value is left empty, cover is used",
      "help_zh": "封禁时间类别。仅当OperationType为block时有效。取值：\n\ncover：当前请求是采取配置覆盖方式。\nuncover：采取时间计算比对最长方式。\n该参数为空时默认采取配置覆盖方式",
      "location": "formData",
      "name": "update_type",
      "options": [
        "--update-type"
      ],
      "raw_name": "UpdateType",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Cdn",
  "protocol": "HTTPS"
}
