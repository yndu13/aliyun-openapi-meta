{
  "cmd_full_name": "appstream-center tag-cloud-resources",
  "cmd_name": "tag-cloud-resources",
  "deprecated": false,
  "description_en": "Creates and attaches tags to cloud resources. For resources that already have the specified tags, updates the tag values",
  "description_zh": "为云资源创建并绑定标签，对于资源已有的标签，则更新标签的值",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "TagCloudResources",
  "operation": {
    "action": "TagCloudResources",
    "api_style": "RPC",
    "api_version": "2021-09-01",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "enum": [
        "AliUid",
        "AppId",
        "AppInstanceGroupId",
        "WyId",
        "CenterImageId"
      ],
      "example": "AppInstanceGroupId",
      "help_en": "The cloud resource type",
      "help_zh": "云资源类型",
      "location": "formData",
      "maxLength": "30",
      "minLength": "1",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The list of resource IDs. A maximum of 50 IDs are supported. You do not need to specify this parameter when the resource type is tenant ID",
      "help_zh": "资源ID列表，最多支持50个。资源类型为租户ID时不需要指定",
      "location": "formData",
      "name": "resource_ids",
      "options": [
        "--resource-ids"
      ],
      "param_style": "repeatList",
      "raw_name": "ResourceIds",
      "required": false,
      "type": "array"
    },
    {
      "element": {
        "fields": [
          {
            "example": "Resolution",
            "help_en": "The tag key. Tags are case-sensitive. The key must be 1 to 128 characters in length",
            "help_zh": "标签键。区分大小写。长度范围：1\\~128 个字符",
            "maxLength": "128",
            "minLength": "1",
            "name": "key",
            "options": [
              "--key"
            ],
            "raw_name": "Key",
            "required": true,
            "type": "string"
          },
          {
            "example": "720p",
            "help_en": "The tag value. Tags are case-sensitive. The value must be 1 to 128 characters in length",
            "help_zh": "标签值。区分大小写。长度范围：1\\~128 个字符",
            "maxLength": "256",
            "minLength": "0",
            "name": "value",
            "options": [
              "--value"
            ],
            "raw_name": "Value",
            "required": true,
            "type": "string"
          }
        ],
        "type": "object"
      },
      "help_en": "The list of tags. System tags and custom tags are supported.\n\nSystem tag enumeration values:\n`System/Scheduler/GRAYSCALE`: canary release tag\n`System/Scheduler/STOP_NEW_USER_CONNECTION`: tag that prevents newly bound users in a delivery group from establishing connections\n\nCustom tags: A maximum of 20 custom tags can be created.\n\nEach tag key on the same resource can have only one tag value. If you add a tag key that already exists, the corresponding tag value is updated to the new value",
      "help_zh": "标签列表。支持系统标签和自定义标签。\n\n系统标签枚举值：\n`System/Scheduler/GRAYSCALE`：灰度标签\n`System/Scheduler/STOP_NEW_USER_CONNECTION`：阻止交付组新绑定用户建连的标签\n\n自定义标签：最多可创建20个。\n\n同一个资源上的同一个标签键只能对应一个标签值。如果您尝试添加已有标签键，则对应的标签值会更新为新值",
      "location": "formData",
      "name": "tags",
      "options": [
        "--tags"
      ],
      "param_style": "repeatList",
      "raw_name": "Tags",
      "required": true,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "appstream-center",
  "protocol": "HTTPS"
}
