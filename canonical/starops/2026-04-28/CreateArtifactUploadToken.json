{
  "camel_example": "",
  "cmd_full_name": "starops create-artifact-upload-token",
  "cmd_name": "create-artifact-upload-token",
  "deprecated": false,
  "description_en": "Creates an artifact upload credential",
  "description_zh": "创建产物上传凭证",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "CreateArtifactUploadToken",
  "operation": {
    "action": "CreateArtifactUploadToken",
    "api_style": "ROA",
    "api_version": "2026-04-28",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": "/digitalEmployee/{name}/artifacts/uploadToken"
  },
  "parameters": [
    {
      "example": "sample-agent",
      "help_en": "The name of the digital human",
      "help_zh": "数字员工名称",
      "location": "path",
      "name": "name",
      "options": [
        "--name"
      ],
      "raw_name": "name",
      "required": true,
      "type": "string"
    },
    {
      "example": "upload/2026-05-25/",
      "help_en": "The artifact upload directory, relative to the digital human artifact root directory. The value can only be empty or a directory under upload/. If not specified, upload/{YYYY-MM-DD}/ is used by default",
      "help_zh": "产物上传目录，相对于数字员工产物根目录；只能为空或 upload/ 下的目录；未传时默认使用 upload/{YYYY-MM-DD}/",
      "location": "query",
      "name": "artifact_path",
      "options": [
        "--artifact-path"
      ],
      "raw_name": "artifactPath",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/digitalEmployee/[name]/artifacts/uploadToken",
  "product_code": "STAROps",
  "protocol": "HTTPS"
}
