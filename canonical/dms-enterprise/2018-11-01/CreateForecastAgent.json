{
  "camel_example": "",
  "cmd_full_name": "dms-enterprise create-forecast-agent",
  "cmd_name": "create-forecast-agent",
  "deprecated": false,
  "description_en": "Creates a forecast agent",
  "description_zh": "创建 forecast agent 实例",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "CreateForecastAgent",
  "operation": {
    "action": "CreateForecastAgent",
    "api_style": "RPC",
    "api_version": "2018-11-01",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "example": "cn-hangzhou",
      "help_en": "The region where the data lake is located",
      "help_zh": "数据湖所属的地域",
      "location": "query",
      "name": "data_region",
      "options": [
        "--data-region"
      ],
      "raw_name": "DataRegion",
      "required": false,
      "type": "string"
    },
    {
      "example": "True",
      "help_en": "Specifies whether to perform a dry run. Set this to `true` to test the API call without making any changes. This checks if you have the required permissions and if the request parameters are valid. The default is `false`. When `DryRun` is `true`, the service returns a failure response with a reason, such as `DryRunOperationError` (the request would have succeeded), `ValidationError` (invalid parameters), or `AccessDeniedError` (insufficient permissions on the KMS resource)",
      "help_zh": "是否开启DryRun模式。true：开启false（默认值）：关闭DryRun模式用于测试API调用，验证您是否具有相应资源的权限，以及请求参数是否配置正确。DryRun模式开启后，KMS会始终返回失败并提示失败原因。失败原因包含如下：DryRunOperationError：不配置DryRun参数时，请求会成功。ValidationError：请求中指定的参数有误。AccessDeniedError：您无权在KMS资源上执行该操作",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "dms-enterprise",
  "protocol": "HTTPS"
}
