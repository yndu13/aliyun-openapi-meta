{
  "camel_example": "",
  "cmd_full_name": "ims list-users-in-recycle-bin",
  "cmd_name": "list-users-in-recycle-bin",
  "deprecated": false,
  "description_en": "Queries the basic information about all Resource Access Management (RAM) users in the recycle bin",
  "description_zh": "查询回收站中的所有RAM用户的基本信息",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ListUsersInRecycleBin",
  "operation": {
    "action": "ListUsersInRecycleBin",
    "api_style": "RPC",
    "api_version": "2019-08-15",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "list",
    "url": ""
  },
  "parameters": [
    {
      "example": "EXAMPLE",
      "help_en": "The `marker`. If part of a previous response is truncated, you can use this parameter to obtain the truncated part",
      "help_zh": "当请求的返回结果被截断时，可以使用`Marker`获取从当前截断位置之后的内容。 ",
      "location": "query",
      "name": "marker",
      "options": [
        "--marker"
      ],
      "raw_name": "Marker",
      "required": false,
      "type": "string"
    },
    {
      "example": "100",
      "help_en": "The number of entries to return. If a response is truncated because it reaches the value of `MaxItems`, the value of `IsTruncated` will be true.\n\nValid values: 1 to 100. Default value: 100",
      "help_zh": "返回结果的条数。当返回结果达到`MaxItems`限制被截断时，返回参数`IsTruncated`将等于true。 \n\n取值范围：1~100。默认值：100",
      "location": "query",
      "name": "max_items",
      "options": [
        "--max-items"
      ],
      "raw_name": "MaxItems",
      "required": false,
      "type": "int"
    },
    {
      "example": "UserPrincipalName eq test@example.onaliyun.com",
      "help_en": "The filter condition that is used to query information about a specified RAM user in the recycle bin.\n\nYou must specify this parameter in the `UserPrincipalName eq <username>@<AccountAlias>.onaliyun.com` format",
      "help_zh": "过滤条件，查询回收站中指定RAM用户登录名的信息。\n\n格式：`UserPrincipalName eq <username>@<AccountAlias>.onaliyun.com`",
      "location": "query",
      "name": "filter",
      "options": [
        "--filter"
      ],
      "raw_name": "Filter",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ims",
  "protocol": "HTTP|HTTPS"
}
