{
  "camel_example": "",
  "cmd_full_name": "ims list-oidc-providers",
  "cmd_name": "list-oidc-providers",
  "deprecated": false,
  "description_en": "Queries OIDC IdPs",
  "description_zh": "调用ListOIDCProviders查询OIDC身份提供商列表",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ListOIDCProviders",
  "operation": {
    "action": "ListOIDCProviders",
    "api_style": "RPC",
    "api_version": "2019-08-15",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "list",
    "url": ""
  },
  "parameters": [
    {
      "example": "EXAMPLE",
      "help_en": "The `marker`. If part of a previous response is truncated, you can use this parameter to obtain the truncated part",
      "help_zh": "当请求的返回结果被截断时，可以使用`Marker`获取从当前截断位置之后的内容。 ",
      "location": "query",
      "name": "marker",
      "options": [
        "--marker"
      ],
      "raw_name": "Marker",
      "required": false,
      "type": "string"
    },
    {
      "example": "100",
      "help_en": "The number of entries per page. If a response is truncated because it reaches the value of `MaxItems`, the value of `IsTruncated` will be `true`.\n\nValid values: 1 to 100. Default value: 100",
      "help_zh": "返回结果的条数。当返回结果达到`MaxItems`限制被截断时，返回参数`IsTruncated`将等于`true`。\n \n取值范围：1~100。默认值：100",
      "location": "query",
      "name": "max_items",
      "options": [
        "--max-items"
      ],
      "raw_name": "MaxItems",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "Ims",
  "protocol": "HTTP|HTTPS",
  "v1_parameters": [
    {
      "help_en": "The `marker`. If part of a previous response is truncated, you can use this parameter to obtain the truncated part",
      "help_zh": "当请求的返回结果被截断时，可以使用`Marker`获取从当前截断位置之后的内容",
      "name": "Marker",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The number of entries per page. If a response is truncated because it reaches the value of `MaxItems`, the value of `IsTruncated` will be `true`.\n\nValid values: 1 to 100. Default value: 100",
      "help_zh": "返回结果的条数。当返回结果达到`MaxItems`限制被截断时，返回参数`IsTruncated`将等于`true`。\n \n取值范围：1~100。默认值：100",
      "name": "MaxItems",
      "position": "query",
      "required": false,
      "type": "int"
    }
  ]
}
