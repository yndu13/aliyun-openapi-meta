{
  "camel_example": "",
  "cmd_full_name": "dms workspace-code-publish",
  "cmd_name": "workspace-code-publish",
  "deprecated": false,
  "description_en": "This asynchronous API returns a key. Use this key to query the WorkspaceActionStatus API for the code deployment status",
  "description_zh": "工作空间代码部署接口。这是个异步接口，请求返回一个key，请根据key查询 WorkspaceActionStatus接口获取状态",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "WorkspaceCodePublish",
  "operation": {
    "action": "WorkspaceCodePublish",
    "api_style": "RPC",
    "api_version": "2025-04-14",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "12345678",
      "help_en": "The workspace ID (numeric ID) for the code deployment",
      "help_zh": "部署操作的工作空间ID（数字ID）",
      "location": "query",
      "name": "workspace_id",
      "options": [
        "--workspace-id"
      ],
      "raw_name": "WorkspaceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "{\"repos\":[{\"repo\":\"git@xxxx.git\", \"branch\":\"master\"}], \"exclude\":[\"/.dms\", \"/username\"]}",
      "help_en": "The configuration for the code deployment, specified as a JSON string. The `repos` array identifies the Git repositories in the workspace and specifies the branch to deploy. The `exclude` array lists directories to skip during the deployment",
      "help_zh": "部署的配置，json string。 repos列表为工作空间内的git repo，主要是设置发布的branch； exclude指定哪些目录跳过",
      "location": "formData",
      "name": "config",
      "options": [
        "--config"
      ],
      "raw_name": "Config",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Dms",
  "protocol": "HTTPS"
}
