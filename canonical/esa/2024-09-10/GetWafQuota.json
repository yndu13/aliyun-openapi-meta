{
  "camel_example": "",
  "cmd_full_name": "esa get-waf-quota",
  "cmd_name": "get-waf-quota",
  "deprecated": false,
  "description_en": "Retrieves quota details for Web Application Firewall (WAF) resources. These quotas define the quantity limits of corresponding resources that a customer can use, such as managed rule groups, custom lists, custom response pages, and scenario-specific protection rules",
  "description_zh": "获取Web应用防火墙（WAF）相关资源的配额详情。这些配额定义了客户可以使用的对应资源的数量限制，例如托管规则组、自定义列表、自定义响应页面以及场景防护规则数量等",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "GetWafQuota",
  "operation": {
    "action": "GetWafQuota",
    "api_style": "RPC",
    "api_version": "2024-09-10",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "example": "page",
      "help_en": "The paths of WAF quotas. Valid values:\nmanaged_rules_group: the quota for WAF managed rule groups.\nlist: the quota for custom lists.\npage: the quota for custom response pages.\nscene_policy: the quota for scenario-specific protection rules",
      "help_zh": "WAF配额的路径，例如：\nmanaged_rules_group：表示WAF托管规则组的配额\nlist：表示自定义列表的配额\npage：表示自定义响应页面的配额\nscene_policy：表示场景防护的配额",
      "location": "query",
      "name": "paths",
      "options": [
        "--paths"
      ],
      "raw_name": "Paths",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "ESA",
  "protocol": "HTTPS"
}
