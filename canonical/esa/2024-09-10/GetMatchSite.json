{
  "camel_example": "",
  "cmd_full_name": "esa get-match-site",
  "cmd_name": "get-match-site",
  "deprecated": false,
  "description_en": "Retrieves the information of the longest-matching active site for a given record name under the current user. For example, if the input record name is www.test.example.com and the user has two active sites (test.example.com and example.com), the API returns the longest-matching active site test.example.com. If no matching active site is found, an error is returned",
  "description_zh": "用户输入记录名称，根据此记录名称，匹配此用户下激活的站点，获取最长匹配的那个激活站点的信息，如：用户输入记录：www.test.example.com，此时用户下有两个激活站点 test.example.com、example.com，接口会返回最长匹配的激活站点test.exapmle.com，如果没有匹配到激活站点则返回错误",
  "kebab_example": "",
  "method": "GET",
  "multi_version": false,
  "name": "GetMatchSite",
  "operation": {
    "action": "GetMatchSite",
    "api_style": "RPC",
    "api_version": "2024-09-10",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "www.example.com",
      "help_en": "The record name",
      "help_zh": "记录名称",
      "location": "query",
      "name": "record_name",
      "options": [
        "--record-name"
      ],
      "raw_name": "RecordName",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "ESA",
  "protocol": "HTTPS"
}
