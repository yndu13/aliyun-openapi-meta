{
  "camel_example": "",
  "cmd_full_name": "edas authorize-role",
  "cmd_name": "authorize-role",
  "deprecated": false,
  "description_en": "Grant permissions to RAM roles",
  "description_zh": "授权角色",
  "kebab_example": "",
  "method": "DELETE|POST|PUT",
  "multi_version": false,
  "name": "AuthorizeRole",
  "operation": {
    "action": "AuthorizeRole",
    "api_style": "ROA",
    "api_version": "2017-08-01",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": "/pop/v5/account/authorize_role"
  },
  "parameters": [
    {
      "example": "test@133******",
      "help_en": "The ID of the RAM user to which you want to assign the roles",
      "help_zh": "待授权子账号 ID",
      "location": "query",
      "name": "target_user_id",
      "options": [
        "--target-user-id"
      ],
      "raw_name": "TargetUserId",
      "required": true,
      "type": "string"
    },
    {
      "example": "1;2",
      "help_en": "The ID of the role to be assigned. If you want to assign multiple roles to the specified RAM user, separate the IDs of the roles with semicolons (;). If you leave this parameter empty, the roles assigned to the specified RAM user are revoked",
      "help_zh": "角色 ID，用“;”号分割，null 代表取消角色授权",
      "location": "query",
      "name": "role_ids",
      "options": [
        "--role-ids"
      ],
      "raw_name": "RoleIds",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "/pop/v5/account/authorize_role",
  "product_code": "Edas",
  "protocol": "HTTP|HTTPS"
}
