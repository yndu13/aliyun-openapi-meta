{
  "camel_example": "",
  "cmd_full_name": "edas synchronize-resource",
  "cmd_name": "synchronize-resource",
  "deprecated": false,
  "description_en": "Synchronizes the basic Alibaba Cloud resources that belong to your account to Enterprise Distributed Application Service (EDAS). This operation is applicable to Elastic Compute Service (ECS) clusters",
  "description_zh": "同步用户下的阿里云基础资源到EDAS。适用于ECS集群",
  "kebab_example": "",
  "method": "GET",
  "multi_version": false,
  "name": "SynchronizeResource",
  "operation": {
    "action": "SynchronizeResource",
    "api_style": "ROA",
    "api_version": "2017-08-01",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": "/pop/v5/resource/pop_sync_resource"
  },
  "parameters": [
    {
      "example": "ecs",
      "help_en": "The type of the resource to be synchronized. Valid values: `ecs, slb, vpc, and all`. These values are case-sensitive",
      "help_zh": "同步的资源类型，该参数可选的参数值为：`ecs、slb、vpc、all`其中的一个，不支持其他格式的参数值",
      "location": "query",
      "name": "type",
      "options": [
        "--type"
      ],
      "raw_name": "Type",
      "required": true,
      "type": "string"
    },
    {
      "example": "i-bp17c***5q8x,i-bp1**5q8x",
      "help_en": "The ID of the resource. This parameter is required only when you set the Type parameter to `ecs`. If you specify multiple IDs, separate them with commas (,). You can synchronize up to 50 resources at a time",
      "help_zh": "指定的资源ID，仅当Type为`ecs`时支持填写此参数，如有多个ID，需要以逗号分隔，一次最多同步50个资源",
      "location": "query",
      "name": "resource_ids",
      "options": [
        "--resource-ids"
      ],
      "raw_name": "ResourceIds",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/pop/v5/resource/pop_sync_resource",
  "product_code": "Edas",
  "protocol": "HTTP|HTTPS"
}
