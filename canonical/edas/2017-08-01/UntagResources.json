{
  "camel_example": "",
  "cmd_full_name": "edas untag-resources",
  "cmd_name": "untag-resources",
  "deprecated": false,
  "description_en": "Removes one or more tags from one or more resources",
  "description_zh": "调用UntagResources接口为指定的资源列表统一解绑标签",
  "kebab_example": "",
  "method": "DELETE",
  "multi_version": false,
  "name": "UntagResources",
  "operation": {
    "action": "UntagResources",
    "api_style": "ROA",
    "api_version": "2017-08-01",
    "method": "DELETE",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": "/pop/v5/tag/tags"
  },
  "parameters": [
    {
      "docRequired": true,
      "example": "cn-beijing",
      "help_en": "The region in which the resource resides",
      "help_zh": "资源所属地域",
      "location": "query",
      "name": "resource_region_id",
      "options": [
        "--resource-region-id"
      ],
      "raw_name": "ResourceRegionId",
      "required": true,
      "type": "string"
    },
    {
      "docRequired": true,
      "example": "application",
      "help_en": "The type of the resource. Valid values:\n\napplication: Enterprise Distributed Application Service (EDAS) application\n\ncluster: EDAS cluster",
      "help_zh": "资源类型，取值：\napplication：EDAS应用。 \ncluster：EDAS集群",
      "location": "query",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": true,
      "type": "string"
    },
    {
      "docRequired": true,
      "example": "[\"f5ad6ff7-xxxx-xxxx-xxxx-2axxxx82xxxx\"]",
      "help_en": "The IDs of the resources from which you want to remove tags. You can specify up to 20 IDs",
      "help_zh": "资源ID列表，列表范围为1~20",
      "location": "query",
      "name": "resource_ids",
      "options": [
        "--resource-ids"
      ],
      "raw_name": "ResourceIds",
      "required": true,
      "type": "string"
    },
    {
      "example": "[\"tagKey1\",\"tagKey2\"]",
      "help_en": "The tags that you want to remove. You can specify up to 20 tags. Set this parameter to a JSON array",
      "help_zh": "要解绑的标签键列表，列表的取值范围1~20，json数组格式",
      "location": "query",
      "name": "tag_keys",
      "options": [
        "--tag-keys"
      ],
      "raw_name": "TagKeys",
      "required": false,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to remove all existing tags from the specified resources. Default value: false. Valid values:\n\ntrue: removes all existing tags from the specified resources.\n\nfalse: does not remove all existing tags from the specified resources.\n\nAll existing tags of a resource are removed only if the tagKeys parameter is left empty and the DeleteAll parameter is set to true",
      "help_zh": "是否删除选定资源下所有的用户标签，默认为false。\n\ntrue：删除资源的标签。\n\nfalse：不删除资源的标签。\n\n仅当tagKeys为空，deleteAll=true时，才可以删除所有的客户标签",
      "location": "query",
      "name": "delete_all",
      "options": [
        "--delete-all"
      ],
      "raw_name": "DeleteAll",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "/pop/v5/tag/tags",
  "product_code": "Edas",
  "protocol": "HTTP|HTTPS"
}
