{
  "camel_example": "",
  "cmd_full_name": "dbs renew-backup-plan",
  "cmd_name": "renew-backup-plan",
  "deprecated": false,
  "description_en": "Renews a backup schedule",
  "description_zh": "该接口用于续费一个DBS备份计划",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "RenewBackupPlan",
  "operation": {
    "action": "RenewBackupPlan",
    "api_style": "RPC",
    "api_version": "2019-03-06",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "dbstooi01exxx",
      "help_en": "The ID of the backup schedule",
      "help_zh": "备份计划ID，可通过调用https://help.aliyun.com/document_detail/2869825.html接口获取此参数的值",
      "location": "query",
      "name": "backup_plan_id",
      "options": [
        "--backup-plan-id"
      ],
      "raw_name": "BackupPlanId",
      "required": true,
      "type": "string"
    },
    {
      "example": "Month",
      "help_en": "Specifies whether to use yearly subscription or monthly subscription for the instance. Valid values:\n\nYear\n\nMonth",
      "help_zh": "指定预付费实例为包年或者包月类型，取值：\nYear：包年\nMonth：包月",
      "location": "query",
      "name": "period",
      "options": [
        "--period"
      ],
      "raw_name": "Period",
      "required": true,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The subscription duration of the instance. Valid values:\n\nIf the Period parameter is set to Year, the value of the UsedTime parameter ranges from 1 to 9.\n\nIf the Period parameter is set to Month, the value of the UsedTime parameter ranges from 1 to 11",
      "help_zh": "指定购买时长，取值：\n当参数Period为Year时，UsedTime取值为1~5。\n当参数Period为Month时，UsedTime取值为1~11",
      "location": "query",
      "name": "used_time",
      "options": [
        "--used-time"
      ],
      "raw_name": "UsedTime",
      "required": true,
      "type": "int"
    },
    {
      "example": "HKAJHFIUEQWBFIJSNFOIWHN",
      "help_en": "The client token that is used to ensure the idempotence of the request",
      "help_zh": "用于保证请求的幂等性，防止重复提交请求",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Dbs",
  "protocol": "HTTP|HTTPS"
}
