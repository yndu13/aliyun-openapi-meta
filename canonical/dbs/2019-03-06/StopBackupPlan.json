{
  "camel_example": "",
  "cmd_full_name": "dbs stop-backup-plan",
  "cmd_name": "stop-backup-plan",
  "deprecated": false,
  "description_en": "This operation pauses a DBS backup plan",
  "description_zh": "该接口用于暂停一个DBS备份计划",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": true,
  "name": "StopBackupPlan",
  "operation": {
    "action": "StopBackupPlan",
    "api_style": "RPC",
    "api_version": "2019-03-06",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "dbstooi01XXXX",
      "help_en": "The ID of the backup plan",
      "help_zh": "备份计划ID",
      "location": "query",
      "name": "backup_plan_id",
      "options": [
        "--backup-plan-id"
      ],
      "raw_name": "BackupPlanId",
      "required": true,
      "type": "string"
    },
    {
      "example": "ALL",
      "help_en": "The method used to pause the backup plan. Valid values:\n\nALL: Pauses the backup schedule, full data backup jobs, incremental log backup jobs, and restore jobs.\n\nPLAN: Pauses only the backup schedule",
      "help_zh": "暂停方式，取值：\nALL：暂停备份计划、全量数据备份任务、增量日志备份任务、恢复任务\nPLAN：只暂停备份计划",
      "location": "query",
      "name": "stop_method",
      "options": [
        "--stop-method"
      ],
      "raw_name": "StopMethod",
      "required": true,
      "type": "string"
    },
    {
      "example": "ETnLKlblzczshOTUbOCzxxxxxxx",
      "help_en": "A client token to ensure the idempotence of the request. This prevents the same request from being sent repeatedly",
      "help_zh": "用于保证请求的幂等性，防止重复提交请求",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Dbs",
  "protocol": "HTTP|HTTPS"
}
