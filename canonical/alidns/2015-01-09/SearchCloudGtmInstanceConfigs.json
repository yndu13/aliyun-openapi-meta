{
  "camel_example": "",
  "cmd_full_name": "alidns search-cloud-gtm-instance-configs",
  "cmd_name": "search-cloud-gtm-instance-configs",
  "deprecated": false,
  "description_en": "Retrieves instance configurations that match the specified parameters",
  "description_zh": "根据传入参数搜索实例配置列表",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "SearchCloudGtmInstanceConfigs",
  "operation": {
    "action": "SearchCloudGtmInstanceConfigs",
    "api_style": "RPC",
    "api_version": "2015-01-09",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "list",
    "url": ""
  },
  "parameters": [
    {
      "example": "zh-CN",
      "help_en": "The language of the response. Valid values:\n\n`zh-CN`: Chinese\n\n`en-US` (default): English",
      "help_zh": "返回结果显示的语言。取值：\n\nzh-CN：中文\n\nen-US（默认值）：英文",
      "location": "query",
      "name": "accept_language",
      "options": [
        "--accept-language"
      ],
      "raw_name": "AcceptLanguage",
      "required": false,
      "type": "string"
    },
    {
      "example": "1ae05db4-10e7-11ef-b126-00163e24****",
      "help_en": "A client-generated token to ensure the idempotence of the request. The token must be unique across requests and can contain up to 64 ASCII characters",
      "help_zh": "用户生成的请求token。用于保证请求的幂等性。该参数值可自定义，但是要保证在不同请求间唯一，最大值不超过64个ASCII字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The page number. Pages start from 1. The default value is 1",
      "help_zh": "当前页数，起始值为1，默认为1",
      "location": "query",
      "name": "page_number",
      "options": [
        "--page-number"
      ],
      "raw_name": "PageNumber",
      "required": true,
      "type": "int"
    },
    {
      "example": "20",
      "help_en": "The number of entries per page for a paged query. The maximum value is 100 and the default value is 20",
      "help_zh": "分页查询时设置的每页行数，最大值100，默认为20",
      "location": "query",
      "name": "page_size",
      "options": [
        "--page-size"
      ],
      "raw_name": "PageSize",
      "required": true,
      "type": "int"
    },
    {
      "example": "gtm-cn-wwo3a3h****",
      "help_en": "The ID of the Global Traffic Manager (GTM) 3.0 instance",
      "help_zh": "全局流量管理GTM 3.0实例ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": false,
      "type": "string"
    },
    {
      "example": "example.com",
      "help_en": "The primary domain name (for example, `example.com`) or subdomain name (for example, `a.example.com`) of the GTM access domain. This is typically a domain name managed by Alibaba Cloud DNS under the same account that owns the GTM instance",
      "help_zh": "GTM接入域名的主域（example.com）或子域（a.example.com）。一般为GTM实例所在账号下，在云解析DNS控制台权威解析中托管的域名",
      "location": "query",
      "name": "schedule_zone_name",
      "options": [
        "--schedule-zone-name"
      ],
      "raw_name": "ScheduleZoneName",
      "required": false,
      "type": "string"
    },
    {
      "example": "www.example.com",
      "help_en": "The GTM access domain. It is formed by combining the host record (`ScheduleHostname`) with the primary or subdomain name (`ScheduleZoneName`)",
      "help_zh": "GTM接入域名。格式为接入域名主机记录（ScheduleHostname参数值）+主域或子域（ScheduleZoneName参数值）",
      "location": "query",
      "name": "schedule_domain_name",
      "options": [
        "--schedule-domain-name"
      ],
      "raw_name": "ScheduleDomainName",
      "required": false,
      "type": "string"
    },
    {
      "example": "enable",
      "help_en": "The status of the instance configuration. Valid values:\n\n`enable`: Enabled. The intelligent scheduling policy of the GTM instance is in effect.\n\n`disable`: Disabled. The intelligent scheduling policy of the GTM instance is unavailable",
      "help_zh": "域名实例的启用状态：\n\nenable：启用状态，GTM实例智能调度策略生效。\n\ndisable：禁用状态，GTM实例智能调度策略不可用",
      "location": "query",
      "name": "enable_status",
      "options": [
        "--enable-status"
      ],
      "raw_name": "EnableStatus",
      "required": false,
      "type": "string"
    },
    {
      "example": "test",
      "help_en": "A note for the instance configuration",
      "help_zh": "域名实例的备注",
      "location": "query",
      "name": "remark",
      "options": [
        "--remark"
      ],
      "raw_name": "Remark",
      "required": false,
      "type": "string"
    },
    {
      "example": "ok",
      "help_en": "The health status of the instance configuration. Valid values:\n\n`ok`: Normal. All address pools referenced by the GTM access domain are available.\n\n`ok_alert`: Alert. Some address pools referenced by the GTM access domain are unavailable. In this state, DNS resolution continues for available address pools but stops for unavailable ones.\n\n`exceptional`: Exceptional. All address pools referenced by the GTM access domain are unavailable. In this case, failover resolution uses the addresses from the non-empty address pool with the smallest sequence number to ensure clients receive a resolution result",
      "help_zh": "域名实例健康状态：\n\nok：正常，接入域名引用的所有地址池全部可用。\n\nok_alert：警告，接入域名引用的所有地址池部分不可用。警告状态下，可用的地址池正常解析，不可用的地址池停止解析。\n\nexceptional：异常，接入域名引用的所有地址池全部不可用。此时，使用实例所引用的顺序号较小的非空地址池中的地址进行兜底解析，尽可能帮助客户端获得解析结果",
      "location": "query",
      "name": "health_status",
      "options": [
        "--health-status"
      ],
      "raw_name": "HealthStatus",
      "required": false,
      "type": "string"
    },
    {
      "example": "available",
      "help_en": "The service availability status of the instance configuration. Valid values:\n\n`available`: The service for the GTM access domain is available when the instance configuration is enabled and its health status is Normal or Alert.\n\n`unavailable`: The service for the GTM access domain is unavailable when the instance configuration is disabled or its health status is Exceptional",
      "help_zh": "域名实例服务可用状态：\n\navailable：可用，域名启用且健康状态为正常，接入域名的服务可用状态为可用。\n\nunavailable：不可用，域名禁用或健康状态为异常，接入域名的服务可用状态为不可用",
      "location": "query",
      "name": "available_status",
      "options": [
        "--available-status"
      ],
      "raw_name": "AvailableStatus",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Alidns",
  "protocol": "HTTP|HTTPS"
}
