{
  "camel_example": "",
  "cmd_full_name": "alb dissociate-additional-certificates-from-listener",
  "cmd_name": "dissociate-additional-certificates-from-listener",
  "deprecated": false,
  "description_en": "Dissociates one or more additional certificates from a listener",
  "description_zh": "将扩展证书与监听解除关联",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DissociateAdditionalCertificatesFromListener",
  "operation": {
    "action": "DissociateAdditionalCertificatesFromListener",
    "api_style": "RPC",
    "api_version": "2020-06-16",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "lsn-5qnirjhpt7******",
      "help_en": "The listener ID. This parameter applies to HTTPS and QUIC listeners",
      "help_zh": "监听ID。HTTPS和QUIC监听有效",
      "location": "query",
      "name": "listener_id",
      "options": [
        "--listener-id"
      ],
      "raw_name": "ListenerId",
      "required": true,
      "type": "string"
    },
    {
      "element": {
        "fields": [
          {
            "example": "103705******",
            "help_en": "The certificate ID",
            "help_zh": "证书ID",
            "name": "certificate_id",
            "options": [
              "--certificate-id"
            ],
            "raw_name": "CertificateId",
            "required": true,
            "type": "string"
          }
        ],
        "type": "object"
      },
      "help_en": "A list of additional certificates. Only server certificates are supported. Specify a maximum of 20 certificates",
      "help_zh": "扩展证书列表。当前仅支持服务器证书。最多支持20个证书",
      "location": "query",
      "name": "certificates",
      "options": [
        "--certificates"
      ],
      "param_style": "flat",
      "raw_name": "Certificates",
      "required": true,
      "type": "array"
    },
    {
      "example": "5A2CFF0E-5718-45B5-9D4D-70B3******",
      "help_en": "The client token that is used to ensure the idempotence of the request. The client token must be a unique value that is generated by the client. The token can contain only ASCII characters.\n\nGenerate a unique value on your client. The token can contain only ASCII characters.\n\nIf you do not specify this parameter, the system uses the RequestId of the request as the ClientToken. The RequestId of each request is unique",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId不一样",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to perform a dry run. Valid values:\n\ntrue: performs a dry run to check the request. The system checks for missing parameters, invalid formats, and service limits. If the request fails the check, an error is returned. If the request passes the check, the `DryRunOperation` error code is returned. No certificates are dissociated from the listener.\n\nfalse (default): sends a normal request. After the request passes the check, an HTTP 2xx status code is returned and the operation is performed",
      "help_zh": "是否只预检此次请求，取值：\n\ntrue：发送检查请求，不会从HTTPS和QUIC监听移除扩展证书。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n\nfalse（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接进行操作",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Alb",
  "protocol": "HTTP|HTTPS"
}
