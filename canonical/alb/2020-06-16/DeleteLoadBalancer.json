{
  "camel_example": "",
  "cmd_full_name": "alb delete-load-balancer",
  "cmd_name": "delete-load-balancer",
  "deprecated": false,
  "description_en": "Deletes the specified load balancer instance",
  "description_zh": "删除指定的负载均衡实例",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DeleteLoadBalancer",
  "operation": {
    "action": "DeleteLoadBalancer",
    "api_style": "RPC",
    "api_version": "2020-06-16",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "example": "alb-o9ulmq5hgn68jk****",
      "help_en": "The ID of the Application Load Balancer instance",
      "help_zh": "应用型负载均衡实例ID",
      "location": "query",
      "name": "load_balancer_id",
      "options": [
        "--load-balancer-id"
      ],
      "raw_name": "LoadBalancerId",
      "required": true,
      "type": "string"
    },
    {
      "example": "5A2CFF0E-5718-45B5-9D4D-70B3FF3898",
      "help_en": "The client token used to ensure the idempotence of the request.\n\nYou can generate a token by using your client. The token must be unique across requests and contain only ASCII characters",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to perform a dry run. Valid values:\n\ntrue: Performs a dry run to check the request's validity without deleting the Application Load Balancer instance. The system checks the required parameters, request format, and service limits. If the request fails the dry run, an error is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n\nfalse (default): sends a normal request. After the request passes the check, an `HTTP 2xx` status code is returned and the instance is deleted",
      "help_zh": "是否只预检此次请求，取值：\n\ntrue：发送检查请求，不会删除应用型负载均衡实例。检查项包括是否填写了必需参数、请求格式和业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n\nfalse（默认值）：发送正常请求，通过检查后返回`HTTP 2xx`状态码并直接进行操作",
      "location": "query",
      "name": "dry_run",
      "options": [
        "--dry-run"
      ],
      "raw_name": "DryRun",
      "required": false,
      "type": "bool"
    },
    {
      "element": {
        "type": "string"
      },
      "help_en": "The RetainResourceType",
      "help_zh": "The RetainResourceType",
      "location": "query",
      "name": "retain_resource_type",
      "options": [
        "--retain-resource-type"
      ],
      "param_style": "flat",
      "raw_name": "RetainResourceType",
      "required": false,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "Alb",
  "protocol": "HTTP|HTTPS",
  "v1_parameters": [
    {
      "help_en": "The client token used to ensure the idempotence of the request.\n\nYou can generate a token by using your client. The token must be unique across requests and contain only ASCII characters",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符",
      "name": "ClientToken",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "Specifies whether to perform a dry run. Valid values:\n\ntrue: Performs a dry run to check the request's validity without deleting the Application Load Balancer instance. The system checks the required parameters, request format, and service limits. If the request fails the dry run, an error is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n\nfalse (default): sends a normal request. After the request passes the check, an `HTTP 2xx` status code is returned and the instance is deleted",
      "help_zh": "是否只预检此次请求，取值：\n\ntrue：发送检查请求，不会删除应用型负载均衡实例。检查项包括是否填写了必需参数、请求格式和业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n\nfalse（默认值）：发送正常请求，通过检查后返回`HTTP 2xx`状态码并直接进行操作",
      "name": "DryRun",
      "position": "query",
      "required": false,
      "type": "bool"
    },
    {
      "help_en": "The ID of the Application Load Balancer instance",
      "help_zh": "应用型负载均衡实例ID",
      "name": "LoadBalancerId",
      "position": "query",
      "required": true,
      "type": "string"
    },
    {
      "name": "RetainResourceType",
      "param_style": "flat",
      "position": "query",
      "required": false,
      "type": "array"
    }
  ]
}
