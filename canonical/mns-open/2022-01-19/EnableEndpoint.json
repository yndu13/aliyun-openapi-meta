{
  "camel_example": "",
  "cmd_full_name": "mns-open enable-endpoint",
  "cmd_name": "enable-endpoint",
  "deprecated": false,
  "description_en": "This operation enables an endpoint of a specified type. After the endpoint is enabled, requests that originate from the endpoint and are on the Access Control List (ACL) whitelist are not blocked",
  "description_zh": "本接口用于启用指定类型的接入点。开启后，源自对应接入点且处于Acl白名单中的请求都将不会被拦截",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "EnableEndpoint",
  "operation": {
    "action": "EnableEndpoint",
    "api_style": "RPC",
    "api_version": "2022-01-19",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "example": "public",
      "help_en": "The type of the endpoint. Valid value:\n\npublic: The Internet endpoint. Only public is supported",
      "help_zh": "接入点类型。取值：\n\npublic：表示公网接入点。(目前仅支持public)",
      "location": "query",
      "name": "endpoint_type",
      "options": [
        "--biz-endpoint-type"
      ],
      "raw_name": "EndpointType",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Mns-open",
  "protocol": "HTTPS"
}
