{
  "camel_example": "",
  "cmd_full_name": "fc delete-session",
  "cmd_name": "delete-session",
  "deprecated": false,
  "description_en": "Deletes the specified session and prohibits new requests from being routed to it. Clears the session metadata from the database, so subsequent requests with the same session ID are treated as new sessions. Releases resources and performs session cleanup. In session isolation scenarios, terminates running requests and releases the instance bound to the session. In non-session isolation scenarios, allows running requests to continue and gracefully terminates them",
  "description_zh": "删除指定会话，禁止新请求路由，会话元数据从数据库清除，客户携带相同 SessionID 的后续请求视为新会话。实现资源释放与会话清理。在会话隔离场景下，系统将中止运行的请求，释放会话绑定的实例。非会话隔离场景下，正在运行中的请求将继续运行，优雅终止",
  "kebab_example": "",
  "method": "DELETE",
  "multi_version": false,
  "name": "DeleteSession",
  "operation": {
    "action": "DeleteSession",
    "api_style": "ROA",
    "api_version": "2023-03-30",
    "method": "DELETE",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": "/2023-03-30/functions/{functionName}/sessions/{sessionId}"
  },
  "parameters": [
    {
      "example": "my-func",
      "help_en": "The function name",
      "help_zh": "函数名称",
      "location": "path",
      "name": "function_name",
      "options": [
        "--function-name"
      ],
      "raw_name": "functionName",
      "required": true,
      "type": "string"
    },
    {
      "example": "test-session-id-1",
      "help_en": "The ID of the session to be deleted",
      "help_zh": "需要删除的会话Id值",
      "location": "path",
      "name": "session_id",
      "options": [
        "--session-id"
      ],
      "raw_name": "sessionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "aliasName1",
      "help_en": "The function alias or version associated with the session to be deleted",
      "help_zh": "需要删除的SessionId关联的函数别名或版本信息",
      "location": "query",
      "name": "qualifier",
      "options": [
        "--qualifier"
      ],
      "raw_name": "qualifier",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/2023-03-30/functions/[functionName]/sessions/[sessionId]",
  "product_code": "FC",
  "protocol": "HTTPS",
  "v1_parameters": [
    {
      "help_en": "The function name",
      "help_zh": "函数名称",
      "name": "functionName",
      "position": "path",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The function alias or version associated with the session to be deleted",
      "help_zh": "需要删除的SessionId关联的函数别名或版本信息",
      "name": "qualifier",
      "position": "query",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The ID of the session to be deleted",
      "help_zh": "需要删除的会话Id值",
      "name": "sessionId",
      "position": "path",
      "required": false,
      "type": "string"
    }
  ]
}
