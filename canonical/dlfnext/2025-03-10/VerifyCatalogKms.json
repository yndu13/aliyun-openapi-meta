{
  "camel_example": "",
  "cmd_full_name": "dlfnext verify-catalog-kms",
  "cmd_name": "verify-catalog-kms",
  "deprecated": false,
  "description_en": "Validates the BYOK SSE-KMS configuration of a Catalog: performs an SSE-KMS write probe on the storage bucket using the issued temporary credentials. This operation only validates whether the specified customer master key (CMK) can be used for BYOK SSE-KMS of the Catalog. It does not automatically enable encryption for all tables in the Catalog. The CMK is configured at the Catalog level through oss.sse.kms.key-id. When creating a table, set the table property oss.sse.kms.enabled=true to enable SSE-KMS for the specified table",
  "description_zh": "校验 Catalog 的 BYOK SSE-KMS 配置:用下发的临时凭证对存储桶做一次 SSE-KMS 写探针并校验。本接口仅校验指定客户主密钥能否用于 Catalog 的 BYOK SSE KMS，不会自动为 Catalog 下的所有表启用加密。客户主密钥通过 oss.sse.kms.key-id 配置在 Catalog 级别。创建表时，需要通过表属性 oss.sse.kms.enabled=true 为指定表启用 SSE KMS",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "VerifyCatalogKms",
  "operation": {
    "action": "VerifyCatalogKms",
    "api_style": "ROA",
    "api_version": "2025-03-10",
    "body_mapping": {
      "kms_key_id": "kmsKeyId"
    },
    "content_type": "application/json",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "req_body_type": "json",
    "system_operation_type": "none",
    "url": "/dlf/v1/catalogs/{catalog}/kms/verify"
  },
  "parameters": [
    {
      "example": "my_catalog",
      "help_en": "The name of the Catalog whose BYOK SSE-KMS configuration you want to validate",
      "help_zh": "待校验 BYOK SSE-KMS 配置的 Catalog 名称。   ",
      "location": "path",
      "name": "catalog",
      "options": [
        "--catalog"
      ],
      "raw_name": "catalog",
      "required": true,
      "type": "string"
    },
    {
      "example": "key-1234567890abcdef",
      "help_en": "The ID of the KMS customer master key (CMK) to be validated. The server uses this key to perform an SSE-KMS write probe",
      "help_zh": "待验证的 KMS 客户主密钥（CMK）ID。服务端将使用该密钥执行 SSE-KMS 写探针",
      "location": "body",
      "name": "kms_key_id",
      "options": [
        "--kms-key-id"
      ],
      "param_style": "json",
      "raw_name": "kmsKeyId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "/dlf/v1/catalogs/[catalog]/kms/verify",
  "product_code": "DlfNext",
  "protocol": "HTTPS",
  "v1_body_parameters": [
    {
      "help_en": "The validation request body, which contains the KMS customer master key (CMK) key ID to authenticate",
      "help_zh": "校验请求体，包含待验证的 KMS 客户主密钥 ID",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    }
  ],
  "v1_parameters": [
    {
      "help_en": "The validation request body, which contains the KMS customer master key (CMK) key ID to authenticate",
      "help_zh": "校验请求体，包含待验证的 KMS 客户主密钥 ID",
      "name": "body",
      "param_style": "json",
      "position": "body",
      "required": false,
      "type": "string"
    },
    {
      "help_en": "The name of the Catalog whose BYOK SSE-KMS configuration you want to validate",
      "help_zh": "待校验 BYOK SSE-KMS 配置的 Catalog 名称",
      "name": "catalog",
      "position": "path",
      "required": true,
      "type": "string"
    }
  ]
}
