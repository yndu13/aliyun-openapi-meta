{
  "camel_example": "",
  "cmd_full_name": "vs modify-rendering-charge-type",
  "cmd_name": "modify-rendering-charge-type",
  "deprecated": false,
  "description_en": "Change the billing method for a Graphic Computing Service instance",
  "description_zh": "变配云渲染资源实例付费类型",
  "kebab_example": "",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ModifyRenderingChargeType",
  "operation": {
    "action": "ModifyRenderingChargeType",
    "api_style": "RPC",
    "api_version": "2018-12-12",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "example": "true",
      "help_en": "This value is valid only when `InstanceChargeType` is `PrePaid` (subscription).\n\nEnable or disable auto-renewal. Valid values:\n\ntrue: Enable.\n\nfalse: Disable",
      "help_zh": "此值仅在 `InstanceChargeType` 为 `PrePaid`（包年包月）时有效。\n\n开启或关闭自动续费，取值：\ntrue：开启。\nfalse：关闭",
      "location": "query",
      "name": "auto_renew",
      "options": [
        "--auto-renew"
      ],
      "raw_name": "AutoRenew",
      "required": false,
      "type": "bool"
    },
    {
      "example": "1",
      "help_en": "This value is valid only when `InstanceChargeType` is `PrePaid` (subscription).\n\nThe duration for subscription. Valid values (Note: If you select 12, it converts to one year; other values are in months):\n\n1 (default)\n\n2\n\n3\n\n4\n\n5\n\n6\n\n7\n\n8\n\n9\n\n12",
      "help_zh": "此值仅在 `InstanceChargeType` 为 `PrePaid`（包年包月）时有效。\n\n包年包月的时长。取值范围（注：当选12的时候会被换算为一年，其余以月为周期单位）：\n\n1（默认）\n2\n3\n4\n5\n6\n7\n8\n9\n12",
      "location": "query",
      "name": "period",
      "options": [
        "--period"
      ],
      "raw_name": "Period",
      "required": false,
      "type": "string"
    },
    {
      "example": "Hour",
      "help_en": "This value is valid only when `InstanceChargeType` is `PostPaid` (pay-as-you-go).\n\nBilling type. Valid values:\n\nHour: Hourly",
      "help_zh": "此值仅在 `InstanceChargeType` 为 `PostPaid`（按量付费）时有效。\n\n计费类型，取值范围：\n\nHour：小时",
      "location": "query",
      "name": "instance_billing_cycle",
      "options": [
        "--instance-billing-cycle"
      ],
      "raw_name": "InstanceBillingCycle",
      "required": false,
      "type": "string"
    },
    {
      "example": "render-9f8c57355d224ad7beaf95e145f22111",
      "help_en": "The ID of the Graphic Computing Service instance",
      "help_zh": "云应用服务实例 ID",
      "location": "query",
      "name": "rendering_instance_id",
      "options": [
        "--rendering-instance-id"
      ],
      "raw_name": "RenderingInstanceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "PostPaid",
      "help_en": "The target billing method for the instance. Valid values:\n\nPrePaid (default): Subscription.\n\nPostPaid: Pay-as-you-go",
      "help_zh": "实例的目标付费方式，取值范围：\n\nPrePaid（默认）：预付费包年包月。\nPostPaid：后付费按量付费",
      "location": "query",
      "name": "instance_charge_type",
      "options": [
        "--instance-charge-type"
      ],
      "raw_name": "InstanceChargeType",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "vs",
  "protocol": "HTTPS"
}
