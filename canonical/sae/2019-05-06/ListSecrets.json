{
  "camel_example": "",
  "cmd_full_name": "sae list-secrets",
  "cmd_name": "list-secrets",
  "deprecated": false,
  "description_en": "Call ListSecrets to list Secrets in a namespace",
  "description_zh": "调用ListSecrets接口获取命名空间内Secret实例列表",
  "kebab_example": "",
  "method": "GET",
  "multi_version": false,
  "name": "ListSecrets",
  "operation": {
    "action": "ListSecrets",
    "api_style": "ROA",
    "api_version": "2019-05-06",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "url": "/pop/v1/sam/secret/secrets"
  },
  "parameters": [
    {
      "example": "cn-beijing:test",
      "help_en": "The ID of the namespace that contains the Secret. For the default namespace, use the region ID, such as `cn-beijing`",
      "help_zh": "Secret实例所在命名空间ID。如果所在命名空间为默认命名空间，则只需填写RegionId，例如`cn-beijing`",
      "location": "query",
      "name": "namespace_id",
      "options": [
        "--namespace-id"
      ],
      "raw_name": "NamespaceId",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "/pop/v1/sam/secret/secrets",
  "product_code": "sae",
  "protocol": "HTTP|HTTPS"
}
