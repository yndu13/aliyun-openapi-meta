{
  "camel_example": "",
  "cmd_full_name": "sae delete-secret",
  "cmd_name": "delete-secret",
  "deprecated": false,
  "description_en": "Deletes a secret",
  "description_zh": "调用DeleteSecret接口删除Secret实例",
  "kebab_example": "",
  "method": "DELETE",
  "multi_version": false,
  "name": "DeleteSecret",
  "operation": {
    "action": "DeleteSecret",
    "api_style": "ROA",
    "api_version": "2019-05-06",
    "method": "DELETE",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "url": "/pop/v1/sam/secret/secret"
  },
  "parameters": [
    {
      "example": "cn-beijing:test",
      "help_en": "The ID of the namespace. If the secret is in the default namespace, you need to specify only the region ID, for example, `cn-beijing`",
      "help_zh": "Secret实例所在命名空间ID。如果所在命名空间为默认命名空间，则只需填写RegionId，例如`cn-beijing`",
      "location": "query",
      "name": "namespace_id",
      "options": [
        "--namespace-id"
      ],
      "raw_name": "NamespaceId",
      "required": true,
      "type": "string"
    },
    {
      "example": "16",
      "help_en": "The ID of the secret to delete. You can obtain the ID by calling the https://help.aliyun.com/document_detail/466929.html operation",
      "help_zh": "需要删除的Secret实例ID。需要调用https://help.aliyun.com/document_detail/466929.html接口查看",
      "location": "query",
      "name": "secret_id",
      "options": [
        "--secret-id"
      ],
      "raw_name": "SecretId",
      "required": true,
      "type": "int"
    }
  ],
  "pathPattern": "/pop/v1/sam/secret/secret",
  "product_code": "sae",
  "protocol": "HTTP|HTTPS"
}
