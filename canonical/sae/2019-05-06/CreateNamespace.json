{
  "camel_example": "",
  "cmd_full_name": "sae create-namespace",
  "cmd_name": "create-namespace",
  "deprecated": false,
  "description_en": "Creates a namespace",
  "description_zh": "创建命名空间",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "CreateNamespace",
  "operation": {
    "action": "CreateNamespace",
    "api_style": "ROA",
    "api_version": "2019-05-06",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": "/pop/v1/paas/namespace"
  },
  "parameters": [
    {
      "example": "cn-beijing:test",
      "help_en": "The long-format namespace ID. If you specify this parameter, NameSpaceShortId is ignored. This parameter is retained for compatibility. Use the short-format namespace ID instead. The format is `<RegionId>:<NamespaceId>`. The `NamespaceId` can contain only lowercase letters and digits and cannot exceed 32 characters in length. Example: `cn-beijing:test`. For information about the regions that SAE supports, see https://help.aliyun.com/document_detail/126213.html",
      "help_zh": "长版命名空间ID。如果设置了该参数会继续生效而忽略NameSpaceShortId，为了兼容保留，推荐使用短版命名空间ID；格式为`<RegionId>:<NamespaceId>`，`NamespaceId`仅允许小写英文字母和数字，例如`cn-beijing:test`,不超过32个字符。SAE支持的RegionId，请参见https://help.aliyun.com/document_detail/126213.html",
      "location": "query",
      "name": "namespace_id",
      "options": [
        "--namespace-id"
      ],
      "raw_name": "NamespaceId",
      "required": false,
      "type": "string"
    },
    {
      "example": "name",
      "help_en": "The name of the namespace. The name cannot exceed 64 characters in length",
      "help_zh": "命名空间名称。不超过64个字符",
      "location": "query",
      "name": "namespace_name",
      "options": [
        "--namespace-name"
      ],
      "raw_name": "NamespaceName",
      "required": true,
      "type": "string"
    },
    {
      "example": "desc",
      "help_en": "The description of the namespace. The description cannot exceed 100 characters in length",
      "help_zh": "命名空间描述信息，不超过100个字符",
      "location": "query",
      "name": "namespace_description",
      "options": [
        "--namespace-description"
      ],
      "raw_name": "NamespaceDescription",
      "required": false,
      "type": "string"
    },
    {
      "example": "test",
      "help_en": "The short-format namespace ID. You do not need to specify a region ID. This parameter is recommended. The ID cannot exceed 20 characters in length and can contain only lowercase letters and digits",
      "help_zh": "短版命名空间ID，不需要带上region，推荐；不超过20个字符，仅允许小写英文字母和数字",
      "location": "query",
      "name": "name_space_short_id",
      "options": [
        "--name-space-short-id"
      ],
      "raw_name": "NameSpaceShortId",
      "required": false,
      "type": "string"
    },
    {
      "example": "true",
      "help_en": "Specifies whether to enable the built-in service registry of SAE.\n\ntrue\n\nfalse\n\nThe default value is true. If you do not use the built-in service registry, set this parameter to false to speed up namespace creation",
      "help_zh": "是否启用SAE内置注册中心：\ntrue\nfalse\n\n默认为true，若不使用内置注册中心，填false可加快命名空间创建速度",
      "location": "query",
      "name": "enable_micro_registration",
      "options": [
        "--enable-micro-registration"
      ],
      "raw_name": "EnableMicroRegistration",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "/pop/v1/paas/namespace",
  "product_code": "sae",
  "protocol": "HTTP|HTTPS"
}
