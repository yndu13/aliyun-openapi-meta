{
  "camel_example": "",
  "cmd_full_name": "sae tag-resources",
  "cmd_name": "tag-resources",
  "deprecated": false,
  "description_en": "Adds one or more tags to specified resources",
  "description_zh": "调用TagResources接口为指定资源添加标签",
  "kebab_example": "",
  "method": "POST",
  "multi_version": false,
  "name": "TagResources",
  "operation": {
    "action": "TagResources",
    "api_style": "ROA",
    "api_version": "2019-05-06",
    "content_type": "application/x-www-form-urlencoded",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "req_body_type": "formData",
    "url": "/tags"
  },
  "parameters": [
    {
      "example": "cn-beijing",
      "help_en": "The region ID",
      "help_zh": "地域ID",
      "location": "formData",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": false,
      "type": "string"
    },
    {
      "example": "application",
      "help_en": "The resource type. Only `application` is supported",
      "help_zh": "资源类型，仅支持`application`",
      "location": "formData",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": true,
      "type": "string"
    },
    {
      "example": "[{\"key\":\"k1\",\"value\":\"v1\"}]",
      "help_en": "The key-value pairs of the tags. This parameter is required unless you specify the ResourceIds parameter. The following rules apply:\n\nkey: The tag key. The key must be 1 to 128 characters in length.\n\nvalue: The tag value. The value must be 1 to 128 characters in length.\n\nTags are case-sensitive. If you specify multiple tags, they are created and bound to the specified resources. For a single resource, each tag key must be unique. If you specify a tag key that already exists for a resource, the operation updates the existing tag value.\n\nA tag key cannot start with `aliyun` or `acs:` and cannot contain `http://` or `https://`",
      "help_zh": "标签键值对，当您未输入ResourceIds参数时必填。取值说明如下：\nkey：标签键，字符长度范围[1,128]。\nvalue：标签值，字符长度范围[1,128]。\n\n区分大小写。如果指定多个标签，则会为指定资源同时创建并绑定多个标签。同一个资源上的同一个标签键只能对应一个标签值。如果您尝试添加已有标签键，则对应的标签值会更新为新值。\n\n不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`",
      "location": "formData",
      "name": "tags",
      "options": [
        "--tags"
      ],
      "raw_name": "Tags",
      "required": false,
      "type": "string"
    },
    {
      "example": "[\"d42921c4-5433-4abd-8075-0e536f8b****\"]",
      "help_en": "The resource IDs. You can specify up to 50 resource IDs in a JSON array. This parameter is required unless you specify the Tags parameter",
      "help_zh": "资源ID。可填写多个资源ID，用英文逗号（,）分隔。当您未输入Tags参数时必填",
      "location": "formData",
      "name": "resource_ids",
      "options": [
        "--resource-ids"
      ],
      "raw_name": "ResourceIds",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "/tags",
  "product_code": "sae",
  "protocol": "HTTP|HTTPS"
}
