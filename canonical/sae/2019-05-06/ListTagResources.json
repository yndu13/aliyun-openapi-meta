{
  "camel_example": "",
  "cmd_full_name": "sae list-tag-resources",
  "cmd_name": "list-tag-resources",
  "deprecated": false,
  "description_en": "Call the `ListTagResources` API to list the tags associated with your applications",
  "description_zh": "调用ListTagResources接口查询应用和标签的对应关系",
  "kebab_example": "",
  "method": "GET",
  "multi_version": false,
  "name": "ListTagResources",
  "operation": {
    "action": "ListTagResources",
    "api_style": "ROA",
    "api_version": "2019-05-06",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": "/tags"
  },
  "parameters": [
    {
      "example": "cn-beijing",
      "help_en": "The region ID",
      "help_zh": "地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "application",
      "help_en": "The resource type. Only `application` is supported",
      "help_zh": "资源类型，仅支持`application`",
      "location": "query",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": true,
      "type": "string"
    },
    {
      "example": "A2RN",
      "help_en": "A query can return a maximum of 50 results. If the number of results exceeds this limit, the response includes a NextToken. To retrieve the next page of results, pass this token in your next request",
      "help_zh": "一次查询最多返回50条结果，当结果超过50条时，会同时返回一个NextToken，下一次查询使用该NextToken即可查询前50条结果之外的结果",
      "location": "query",
      "name": "next_token",
      "options": [
        "--next-token"
      ],
      "raw_name": "NextToken",
      "required": false,
      "type": "string"
    },
    {
      "example": "[\"d42921c4-5433-4abd-8075-0e536f8b****\"]",
      "help_en": "The resource IDs, specified as a JSON array of strings. This parameter is required if the Tags parameter is not specified",
      "help_zh": "资源ID。可填写多个资源ID，用英文逗号（,）分隔。当您未输入Tags参数时必填",
      "location": "query",
      "name": "resource_ids",
      "options": [
        "--resource-ids"
      ],
      "raw_name": "ResourceIds",
      "required": false,
      "type": "string"
    },
    {
      "example": "[{\"key\":\"k1\",\"value\":\"v1\"}]",
      "help_en": "The tags used to filter resources. This parameter is required if the ResourceIds parameter is not specified. A tag is a key-value pair.\n\nkey: The tag key. The key can be 1 to 128 characters in length.\n\nvalue: The tag value. The value can be 1 to 128 characters in length.\n\nTag keys and tag values are case-sensitive. If you specify multiple tags, the operation returns only resources that have all the specified tags.\n\nA tag key cannot start with `aliyun` or `acs:` and cannot contain `http://` or `https://`",
      "help_zh": "标签键值对，当您未输入ResourceIds参数时必填。取值说明如下：\nkey：标签键，字符长度范围[1,128]。\nvalue：标签值，字符长度范围[1,128]。\n\n区分大小写。如果指定多个标签，则会为指定资源同时创建并绑定多个标签。同一个资源上的同一个标签键只能对应一个标签值。如果您尝试添加已有标签键，则对应的标签值会更新为新值。\n\n不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`",
      "location": "query",
      "name": "tags",
      "options": [
        "--tags"
      ],
      "raw_name": "Tags",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/tags",
  "product_code": "sae",
  "protocol": "HTTP|HTTPS"
}
