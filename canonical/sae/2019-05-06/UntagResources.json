{
  "camel_example": "",
  "cmd_full_name": "sae untag-resources",
  "cmd_name": "untag-resources",
  "deprecated": false,
  "description_en": "Removes one or more tags from specified resources",
  "description_zh": "调用UntagResources接口解除指定资源和标签之间的绑定关系",
  "kebab_example": "",
  "method": "DELETE",
  "multi_version": false,
  "name": "UntagResources",
  "operation": {
    "action": "UntagResources",
    "api_style": "ROA",
    "api_version": "2019-05-06",
    "method": "DELETE",
    "operation_type": "write",
    "protocol": "HTTPS",
    "url": "/tags"
  },
  "parameters": [
    {
      "example": "cn-beijing",
      "help_en": "The region ID",
      "help_zh": "地域ID",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "example": "application",
      "help_en": "The resource type. Only `application` is supported",
      "help_zh": "资源类型，只支持`application`",
      "location": "query",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": true,
      "type": "string"
    },
    {
      "example": "[\"d42921c4-5433-4abd-8075-0e536f8b****\"]",
      "help_en": "The resource IDs. To specify multiple resource IDs, separate them with commas (,)",
      "help_zh": "资源ID。可填写多个资源ID，用英文逗号（,）分隔",
      "location": "query",
      "name": "resource_ids",
      "options": [
        "--resource-ids"
      ],
      "raw_name": "ResourceIds",
      "required": true,
      "type": "string"
    },
    {
      "example": "[\"k1\",\"k2\"]",
      "help_en": "The tag keys. To specify multiple tag keys, separate them with commas (,)",
      "help_zh": "标签键列表。可填写多个标签键，用英文逗号（,）分隔",
      "location": "query",
      "name": "tag_keys",
      "options": [
        "--tag-keys"
      ],
      "raw_name": "TagKeys",
      "required": false,
      "type": "string"
    },
    {
      "example": "false",
      "help_en": "Specifies whether to remove all tags. This parameter applies only when TagKeys is not specified. Valid values:\n\ntrue: All tags are removed.\n\nfalse: Only the tags specified in the TagKeys parameter are removed",
      "help_zh": "是否删除所有标签，当已传入标签键时生效。取值说明如下：\n\ntrue：删除所有标签。\nfalse：不删除所有标签",
      "location": "query",
      "name": "delete_all",
      "options": [
        "--delete-all"
      ],
      "raw_name": "DeleteAll",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "/tags",
  "product_code": "sae",
  "protocol": "HTTP|HTTPS"
}
