{
  "camel_example": "",
  "cmd_full_name": "sae describe-namespace-list",
  "cmd_name": "describe-namespace-list",
  "deprecated": false,
  "description_en": "Queries a list of namespaces",
  "description_zh": "调用DescribeNamespaceList接口获取命名空间列表",
  "kebab_example": "",
  "method": "GET",
  "multi_version": false,
  "name": "DescribeNamespaceList",
  "operation": {
    "action": "DescribeNamespaceList",
    "api_style": "ROA",
    "api_version": "2019-05-06",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": "/pop/v1/sam/namespace/describeNamespaceList"
  },
  "parameters": [
    {
      "example": "true",
      "help_en": "Specifies whether to return custom namespaces. Valid values:\n\ntrue: The system returns custom namespaces.\n\nfalse: The system does not return custom namespaces",
      "help_zh": "是否返回自定义命名空间。取值说明如下：\n\ntrue：返回自定义命名空间。\nfalse：不返回自定义命名空间",
      "location": "query",
      "name": "contain_custom",
      "options": [
        "--contain-custom"
      ],
      "raw_name": "ContainCustom",
      "required": false,
      "type": "bool"
    },
    {
      "example": "true",
      "help_en": "Indicates whether hybrid cloud namespaces are excluded. Valid values:\n\ntrue: Hybrid cloud namespaces are excluded.\n\nfalse: Hybrid cloud namespaces are included",
      "help_zh": "是否排除混合云命名空间。取值说明如下：\n\ntrue：排除混合云命名空间。   \nfalse：不排除混合云命名空间",
      "location": "query",
      "name": "hybrid_cloud_exclude",
      "options": [
        "--hybrid-cloud-exclude"
      ],
      "raw_name": "HybridCloudExclude",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "/pop/v1/sam/namespace/describeNamespaceList",
  "product_code": "sae",
  "protocol": "HTTP|HTTPS"
}
