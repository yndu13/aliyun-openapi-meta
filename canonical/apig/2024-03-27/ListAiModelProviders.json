{
  "camel_example": "",
  "cmd_full_name": "apig list-ai-model-providers",
  "cmd_name": "list-ai-model-providers",
  "deprecated": false,
  "description_en": "Queries the list of AI model providers",
  "description_zh": "查询AI模型供应商列表",
  "kebab_example": "",
  "method": "GET",
  "multi_version": false,
  "name": "ListAiModelProviders",
  "operation": {
    "action": "ListAiModelProviders",
    "api_style": "ROA",
    "api_version": "2024-03-27",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": "/v1/ai-model-providers"
  },
  "parameters": [
    {
      "example": "gw-8c13d2b4f8a1",
      "help_en": "The ID of the AI gateway instance. The target instance must exist, belong to the current account, and be of the AI gateway type",
      "help_zh": "AI 网关实例 ID。目标实例必须存在、属于当前账号且类型为 AI 网关",
      "location": "query",
      "name": "gateway_id",
      "options": [
        "--gateway-id"
      ],
      "raw_name": "gatewayId",
      "required": true,
      "type": "string"
    },
    {
      "example": "qwen",
      "help_en": "The fuzzy match condition for the model provider identifier. If left empty, all model providers under the current gateway are queried",
      "help_zh": "模型供应商标识的模糊匹配条件。留空时查询当前网关下全部模型供应商",
      "location": "query",
      "name": "provider",
      "options": [
        "--provider"
      ],
      "raw_name": "provider",
      "required": false,
      "type": "string"
    },
    {
      "example": "1",
      "help_en": "The page number. Default value: 1. The value must be greater than or equal to 1",
      "help_zh": "页码。默认值为 1，取值大于或等于 1",
      "location": "query",
      "name": "page_number",
      "options": [
        "--page-number"
      ],
      "raw_name": "pageNumber",
      "required": false,
      "type": "int"
    },
    {
      "example": "10",
      "help_en": "The number of entries per page. Default value: 10. Valid values: 1 to 500",
      "help_zh": "每页数量。默认值为 10，取值范围为 1～500",
      "location": "query",
      "name": "page_size",
      "options": [
        "--page-size"
      ],
      "raw_name": "pageSize",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "/v1/ai-model-providers",
  "product_code": "APIG",
  "protocol": "HTTPS"
}
