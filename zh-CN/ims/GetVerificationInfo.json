{
  "name": "GetVerificationInfo",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "UserPrincipalName",
      "description": "RAM用户的登录名称。\n\n本参数实际必填；未提供时，接口返回 `InvalidParameter.UserPrincipalName`。可通过 `ListUsers` 获取现有 RAM 用户的登录名称。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
