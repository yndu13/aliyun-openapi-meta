{
  "name": "RemoveClientIdFromOIDCProvider",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientId",
      "description": "指定的客户端ID。\n\n格式：允许英文字母、数字、特殊字符`.-_:/`，不能以特殊字符`.-_:/`开头。\n\n长度：最大128个字符。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "OIDCProviderName",
      "description": "已存在且已绑定目标 `ClientId` 的 OIDC 身份提供商名称。如果尚未创建或绑定，可先调用 `CreateOIDCProvider`（设置 `ClientIds`）或 `AddClientIdToOIDCProvider`。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
