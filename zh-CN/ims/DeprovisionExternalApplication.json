{
  "name": "DeprovisionExternalApplication",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "description": "当前账号已安装的外部应用 ID。可通过 `ListExternalApplications` 或 `ListApplicationProvisionInfos`（`SourceType=external`）获取；如果尚未安装，需先调用 `ProvisionExternalApplication`。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
