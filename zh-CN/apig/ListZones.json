{
  "name": "ListZones",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/v1/zones",
  "parameters": [
    {
      "name": "gatewayEdition",
      "description": "查询可用区的目标网关版本。可选值：Professional（普通网关，默认）、ServerlessV2（API 多租 Serverless V2）。不传时按 Professional 处理。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
