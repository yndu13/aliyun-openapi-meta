{
  "name": "UpdateHttpApi",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "PUT",
  "pathPattern": "/v1/http-apis/[httpApiId]",
  "parameters": [
    {
      "name": "body",
      "description": "请求体参数。",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "dryRun",
      "description": "是否只做预检校验（dry run）。true 时执行与真实更新一致的全部同步校验但不更新任何配置、不产生副作用；缺省或 false 时行为与现有版本一致。",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "httpApiId",
      "description": "待更新的HTTP API ID。",
      "position": "Path",
      "type": "String",
      "required": true
    }
  ]
}
