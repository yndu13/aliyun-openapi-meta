{
  "name": "CreateConsumerAuthorizationRules",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/v1/authorization-rules",
  "parameters": [
    {
      "name": "body",
      "description": "请求体参数。本接口为 ROA 风格 API，所有业务参数必须整体嵌套在 body 键下作为 JSON 请求体传输，不能将 authorizationRules 等字段平铺到请求顶层参数中，否则服务端将因请求体为空返回 400（authorizationRules 参数为空）。",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ]
}
