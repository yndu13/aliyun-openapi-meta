{
  "name": "RunPluginPipeline",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/v1/plugin-workspaces/[workspaceId]/pipeline-run",
  "parameters": [
    {
      "name": "workspaceId",
      "description": "工作空间 ID；按当前用户 + workspaceId 查 t_plugin_workspace，查不到报 ErrRecordNotFound",
      "position": "Path",
      "type": "String",
      "required": false
    }
  ]
}
