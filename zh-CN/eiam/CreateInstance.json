{
  "name": "CreateInstance",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符，且不能超过64个字符。重试时传入相同值可取回首次调用的完整响应，避免重复创建实例。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Description",
      "description": "实例描述，最大长度限制128字符。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
