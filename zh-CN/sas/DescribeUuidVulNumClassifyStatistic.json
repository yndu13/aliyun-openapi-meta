{
  "name": "DescribeUuidVulNumClassifyStatistic",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ImageVul",
      "description": "是否查询镜像漏洞统计。取值：true：Uuids 为镜像 ID；false：Uuids 为主机 UUID。默认值：false。",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "Uuids",
      "description": "资产唯一标识。ImageVul 为 false 时传入主机 UUID；ImageVul 为 true 时传入镜像 ID。多个值使用半角逗号（,）分隔。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
