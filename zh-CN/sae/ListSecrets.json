{
  "name": "ListSecrets",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/pop/v1/sam/secret/secrets",
  "parameters": [
    {
      "name": "NamespaceId",
      "description": "Secret实例所在命名空间ID。如果所在命名空间为默认命名空间，则只需填写RegionId，例如`cn-beijing`。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
