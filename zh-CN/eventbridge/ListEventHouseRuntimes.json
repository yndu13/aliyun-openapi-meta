{
  "name": "ListEventHouseRuntimes",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "MaxResults",
      "description": "每页最多返回的 Runtime 数量。未传或传 0 时使用默认值 20，最大值为 100",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "NextToken",
      "description": "首次查询无需传入。后续查询使用上一次响应返回的 NextToken；为空表示没有下一页",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
