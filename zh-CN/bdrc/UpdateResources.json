{
  "name": "UpdateResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/api/v1/resources/update",
  "parameters": [
    {
      "name": "ResourceType",
      "description": "资源类型，不填代表更新所有类型。支持使用英文逗号（,）分隔传入多个资源类型。",
      "position": "Body",
      "type": "String",
      "required": false
    }
  ]
}
