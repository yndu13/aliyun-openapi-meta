{
  "name": "DeleteChannel",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "description": "应用ID，仅支持传单个ID。本参数实际必填，缺少时服务返回InvalidInput。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ChannelId",
      "description": "已入会的频道ID，仅支持传单个ID。本参数实际必填，缺少时服务返回InvalidInput。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
