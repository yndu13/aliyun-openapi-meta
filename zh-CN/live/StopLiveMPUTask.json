{
  "name": "StopLiveMPUTask",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "description": "应用ID，仅支持传单个ID。由大小写字母、数字、下划线、短划线（-）组成，最大64字符。通过访问 **视频直播>直播+>实时音视频>应用管理** 可以查看拥有的应用 ID。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "TaskId",
      "description": "任务ID，仅支持传单个ID。由大小写字母、数字、下划线、短划线（-）组成，最大55字符。此ID为旁路转推的标识，需保证唯一。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
