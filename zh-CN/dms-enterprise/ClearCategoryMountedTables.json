{
  "name": "ClearCategoryMountedTables",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/",
  "parameters": [
    {
      "name": "CategoryId",
      "description": "待清理的业务目录ID，层级不限；仅解除其下（含全部子孙目录）所有表的挂载关系，目录本身及层级结构不变；目录不存在返回 404",
      "position": "Query",
      "type": "Long",
      "required": true
    },
    {
      "name": "DryRun",
      "description": "是否仅预演：true 时只统计将被清理的挂载关系数与表数，不实际删除；默认 false",
      "position": "Query",
      "type": "Boolean",
      "required": false
    }
  ]
}
