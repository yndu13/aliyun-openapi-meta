{
  "name": "DecodeDiagnosticMessage",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EncodedDiagnosticMessage",
      "description": "API请求因无权限被拒绝访问时，从响应体 `AccessDeniedDetail.EncodedDiagnosticMessage`字段中获取的编码诊断信息。该参数用于解码无权限诊断信息，调用本接口时需传入。",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ]
}
