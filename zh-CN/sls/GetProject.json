{
  "name": "GetProject",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/",
  "parameters": [
    {
      "name": "crossRegion",
      "description": "是否跨地域查找日志项目。默认值 false，开启后可跨地域查找日志项目。此功能仅在特定地域有效。",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "project",
      "description": "Project名称。",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ]
}
