{
  "name": "DescribeFirewallDropTrend",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EndTime",
      "description": "设置查询结束时间。使用秒级时间戳格式表示。本参数实际为必填项且需与 StartTime 同时提供，需传秒级 Unix 时间戳且应晚于 StartTime；未提供时 API 返回 ErrorTimeError(400)。",
      "position": "Query",
      "type": "Long",
      "required": true
    },
    {
      "name": "Lang",
      "description": "接收消息的语言类型。取值：\n\n- **zh**（默认）：中文\n- **en**：英文",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "StartTime",
      "description": "设置查询开始时间。使用秒级时间戳格式表示。本参数实际为必填项且需与 EndTime 同时提供，需传秒级 Unix 时间戳且应早于 EndTime；未提供时 API 返回 ErrorTimeError(400)。",
      "position": "Query",
      "type": "Long",
      "required": true
    }
  ]
}
