{
  "name": "StopScheduledPreloadExecution",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Id",
      "description": "预热计划ID，由 CreateScheduledPreloadExecutions 返回的 SuccessExecutions[].Id 字段获取。调用本接口前需先通过 CreateScheduledPreloadJob 创建预热任务，再通过 CreateScheduledPreloadExecutions 创建执行计划。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
