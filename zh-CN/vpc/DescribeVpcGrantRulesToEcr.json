{
  "name": "DescribeVpcGrantRulesToEcr",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EcrInstanceId",
      "description": "要查询的专线网关实例的ID。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "EcrOwnerId",
      "description": "ECR实例所属阿里云账号（主账号）ID。\n\n> 如果您要加载跨账号的网络实例，本参数必填。",
      "position": "Query",
      "type": "Long",
      "required": false
    },
    {
      "name": "InstanceId",
      "description": "要查询的网络实例的ID。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "InstanceType",
      "description": "要查询授权关系的实例类型。取值：\n\n- **VBR**：边界路由器VBR（Virtual border router）实例，表示查询VBR实例拥有哪些VPC实例的授权。\n- **VPC**：专有网络VPC（Virtual Private Cloud）实例，表示查询VPC实例对哪些VBR实例进行了授权。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "MaxResults",
      "description": "分批次查询时每次显示的条目数。取值范围：**1**~**100**，默认值为**100**。",
      "position": "Query",
      "type": "Integer",
      "required": false
    },
    {
      "name": "NextToken",
      "description": "是否拥有下一次查询的令牌（Token）。取值：\n\n* 第一次查询和没有下一次查询时，均无需填写。\n\n* 如果有下一次查询，取值为上一次API调用返回的**NextToken**值。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RegionId",
      "description": "要查询的网络实例所在的地域。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ResourceGroupId",
      "description": "要查询的网络实例所属的资源组ID。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Tags",
      "description": "标签信息。",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "Key",
          "description": "资源的标签键。至少输入1个标签键，最多支持输入20个标签键。如需传入该值，则不能输入空字符串。\n\n一个标签键最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "Value",
          "description": "资源的标签值。最多支持输入20个标签值。如需传入该值，可以输入空字符串。\n\n最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    }
  ]
}
