{
  "name": "CreateRouteTargetGroup",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "description": "客户端Token，用于保证请求的幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。说明 若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId不一样。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ConfigMode",
      "description": "路由目标组的配置模式，支持模式如下：\n\n- **Active-Standby**: 主备模式。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "RegionId",
      "description": "路由目标组所属的地域ID。您可以通过调用DescribeRegions接口获取地域ID。",
      "position": "Query",
      "type": "String",
      "required": true
    },
    {
      "name": "ResourceGroupId",
      "description": "资源组ID。\n\n关于资源组的更多信息，请参见[什么是资源组](~~2381067~~)。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RouteTargetGroupDescription",
      "description": "路由目标组的描述信息。\n描述长度为1～256个字符，不能以http://或https://开头。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RouteTargetGroupName",
      "description": "路由目标组的名称。\n\n名称长度为1～128个字符，不能以http://或https://开头。",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RouteTargetMemberList",
      "description": "路由目标组的成员列表。\n\n主备模式下，对路由目标组成员有如下限制：\n\n1. 路由目标组成员数量必须为2\n2. 路由目标组成员必须属于不同可用区",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "sub_parameters": [
        {
          "name": "MemberId",
          "description": "路由目标组成员实例ID。",
          "position": "Query",
          "type": "String",
          "required": true
        },
        {
          "name": "MemberType",
          "description": "路由目标组的成员类型。\n\n当前支持的类型：\n\n- **GatewayLoadBalancerEndpoint**\n\n主备模式下，路由目标组所有成员的类型必须相同。",
          "position": "Query",
          "type": "String",
          "required": true
        },
        {
          "name": "Weight",
          "description": "路由目标组成员的权重值。取值：\n\n- **100**：表示成员为主实例。\n- **0**：表示成员为备实例。\n\n权重值仅支持在创建时设置，不支持修改。",
          "position": "Query",
          "type": "Integer",
          "required": true
        }
      ]
    },
    {
      "name": "Tag",
      "description": "资源的标签。",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "sub_parameters": [
        {
          "name": "Key",
          "description": "资源的标签键。最多支持输入20个标签键。如需传入该值，则不能输入空字符串。\n\n一个标签键最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
          "position": "Query",
          "type": "String",
          "required": false
        },
        {
          "name": "Value",
          "description": "资源的标签值。最多支持输入20个标签值。如需传入该值，可以输入空字符串。\n\n最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
          "position": "Query",
          "type": "String",
          "required": false
        }
      ]
    },
    {
      "name": "VpcId",
      "description": "路由目标组所属的VPC的ID。",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
