{
  "name": "SendDiagnosticInterrupt",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DryRun",
      "description": "是否只预检此次请求。取值范围： ● true：发送检查请求，不会发送NMI命令。 ● false（默认）：发送正常NMI请求触发crash dump。",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "InstanceId",
      "description": "需要发送诊断中断的实例ID。\n",
      "position": "Query",
      "type": "String",
      "required": true
    }
  ]
}
